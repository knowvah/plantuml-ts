@startuml
start
:act1;
if (cond ?) then (label1)
  stop
endif
:act2;
if (cond ?) then (label2)
  :act3;
else (label3)
  :act4;
endif
:act5;

partition foo {
  :act1;
  if (cond ?) then (label1)
    stop
  endif
  :act2;
  if (cond ?) then (label2)
    :act3;
  else (label 3)
    :act4;
  endif
  :act5;
}
stop
@enduml
