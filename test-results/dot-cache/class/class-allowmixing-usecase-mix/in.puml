@startuml
' Authored fixture (SI10/T3, not a hashed corpus import). Covers:
' `allowmixing` + a `usecase` alongside a real `class` in a class diagram.
' Routing verified: classAccepts(lines) === true AND renderSync's root
' <svg data-diagram-type="CLASS"> (not "DESCRIPTION") -- see the mission
' decision journal for the exact verification transcript.
allowmixing

class Foo {
  +bar(): void
}

usecase "Do something" as UC1
@enduml
