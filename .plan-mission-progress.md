# Plan Mission Progress — fix-sequence-rendering

## Phase 1: done

**Stack**: TypeScript, vitest (`npm test` = `vitest run --coverage`, 90/90/90),
eslint (`npm run lint`), `npm run typecheck` (both tsconfigs), `vite build`.
Architecture overview exists at `docs/architecture/overview.md`.

**Measured state of sequence rendering** (1124 fixtures, canvas viewBox vs jar):

| | within 1% of the jar |
|---|---|
| width | 416 (37.0%) |
| height | **23 (2.0%)** |
| both | **3 (0.3%)** |
| median error | width 5.5%, height **15.0%** |

Height is wrong on 98% of the corpus. That is the defect behind "sequence
diagrams are just wrong".

**Per-attribute distance** (`scripts/sequence-geometry-distance.ts`, at
`123d9c39`): Y-family (y, y1, y2, height, cy) 2 031 467; X-family (x, x1, x2,
width, cx) 1 931 743; axis-mixed (points, d) 2 093 695. Cohort
`descended=891 shortCircuited=233`, concentration 27.6% on `vofupo-09-gafe466`
(above the 20% alarm, so corpus totals are not quotable).

**KEY FINDING — a complete brief for this work already exists and is unstarted:**
`plans/sequence-text-and-y-convergence/batch-5/` (C1 instrument axis-split,
C2 derive vertical terms from the Java, C3 apply as ONE change, C4 close out).
It is parked behind a hard checkpoint that says "report after batch 4 and wait";
batch 4 has reported (B1-B3 done, B4..Bn halted under stop condition 9), so the
checkpoint is satisfied.

Its headline premise is STALE: it says "the Y axis is 63.9% of the real
remaining error", measured 2026-09-01 before Phase A and before the
sequence-creole mission. The split above is now roughly even. Its own C1 task
is precisely to re-derive that split, so the brief's STRUCTURE survives the
staleness; only its README numbers need refreshing.

## Phase 2: pending
