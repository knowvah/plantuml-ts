# Mission Index — the executable grind queue

The `roadmap.md` is the *why*. This is the *what-next, executably*: a work
queue a loop can pull from. Each mission has a **status**, a **blocked-by**
list, an **exit bar** (how you know it's done), and a **measurement** (the
command that reports progress). The grind loop reads this file, picks the
lowest-ID mission that is `todo` and unblocked, executes it to its exit bar,
flips it to `done`, and repeats.

This is the whole-port analog of `plans/dot-oracle-sync/loop-protocol.md`:
that file loops *within* one mission; this file loops *across* missions.

> **Package rename, 2026-08-01.** Every published package moved to the
> `@knowvah` scope, which already hosts `@knowvah/dot-engine`: the library is
> `@knowvah/plantuml-ts`, and the four stdlib packages are
> `@knowvah/plantuml-stdlib{,-aws,-tupadr3,-all}`. **Rows below still name
> them `plantuml-ts` and `@plantuml-ts/*` and are left that way on purpose** —
> they record what was true when written, per this file's own convention of
> amending rather than rewriting dated entries. Read any `@plantuml-ts/x` in
> a historical row as today's `@knowvah/plantuml-x`. Nothing is published:
> the names are not reserved on the registry.

## Grind protocol (meta-loop)

1. **Pick.** Lowest-ID mission with `status: todo` and every `blocked-by`
   already `done`. Ties: lower ID first.
2. **Brief.** If the mission has no `plans/<name>/` brief, generate one with
   `/plan-mission` before executing. Missions that are themselves loops
   (depth passes, new diagram types) get a `loop-protocol.md` like
   dot-oracle-sync's.
3. **Execute** to the exit bar. Depth/type missions run as their own inner
   loop (diagnose → port → ratchet → commit per iteration).
4. **Verify** the exit bar with the measurement command. Do not flip to `done`
   on vibes — the command must report the bar met.
5. **Record.** Flip `status` here, one commit. Ledger anything deferred.
6. **Repeat.** Compact between missions; re-read this file after compaction.

**Standing rule (maintainer ruling 2026-07-14): every conformance bar is
100% minus known divergences** — a mission closes only when each
non-conformant fixture is carried by a named DIVERGENCES.md or ledger
entry; percentage slack is not a place misses may hide.
**Standing rule (from roadmap §1): never mark a diagram type `done` on
breadth alone.** A type is `done` only when its depth gate is met. The gate is
a **conformance verdict**, not a feeling — see `planning/conformance.md`.
Graded (dot-engine model): `conformant` (structure + numeric-in-tolerance +
non-numeric-exact) is the bar; `structural-match` (shape right, numbers
drifting) is progress; report both percentages. Today's DOT bar measures
`structural-match` (sizes tolerant); S1i unlocks `conformant`. Types that
render but are unverified are `shallow`, not `done`.

## Status legend

- `done` — exit bar met and verified.
- `shallow` — renders output, depth UNVERIFIED (breadth-only). Needs a depth pass.
- `wip` — in progress.
- `todo` — not started, unblocked or blocked (see blocked-by).
- `spike` — needs a decision/experiment before it becomes a normal mission.

---

## Phase A — Depth passes (highest ROI: convert shallow → faithful)

Reuse the dot-oracle-sync harness (`scripts/dot-sync-report.ts`, the ratchet,
per-type goldens). Each is an inner loop to 100%-minus-known-divergences EQUAL (maintainer ruling 2026-07-14; was ≥90% + zero unexplained — the closed A-phase missions already exceeded it in practice, all misses ledgered).

| ID | Mission | Status | Blocked-by | Exit bar | Measurement |
|----|---------|--------|-----------|----------|-------------|
| A1 | description DOT-sync | done | — | ≥90% structural-match now (→ conformant after S1L) + every miss ledgered, both corpora. **DONE 2026-07-12: component 251/259 (97%), usecase 79/87 (91%), zero unledgered** — plans/dot-oracle-sync/ (P2/i1–i28; ledger: kermor pragma, set-separator, remove-<<stereotype>> link removal, stdlib SI5 family, TIM-json family, rich-text measurement). Conformant flip still gated on S1L. | `npx tsx scripts/dot-sync-report.ts component usecase` |
| A2 | class DOT-sync | done | A1, S1L | class ≥90% conformant + ledger. **Baseline 2026-07-06: 1% EQUAL (9/680)** — needs the STRUCTURAL port (HTML-table class nodes + compartments, qualifier ports, newpage, edge decorations), NOT sizing. Scoped in `planning/a2-class-dot-sync.md`; warrants `/plan-mission`. Reuses S1L infra. **DONE 2026-07-11: 680/680 comparable EQUAL (100%), zero divergences, empty ledger** — plans/class-dot-sync/. Unblocks A3/A4; queue class SVG-conformance (tier 2) next. **Size-gated 2026-07-27 (A-phase conformant flip):** the RATCHET suite now asserts node width/height (`scripts/measure-class-size-deltas.ts`, `oracle/goldens/class/size-backlog.json`, per-fixture pin in `class-dot-parity.test.ts`) — completing uniform A-phase size-gating (description/object/state already had it). Measured **219/708 = 30.9% size-conformant**; the 489 non-conformant are pinned shrink-only and routed to **A2s** below (class record-node sizing subsystem — no divergences). Structure stays 100% EQUAL. | `npx tsx scripts/dot-sync-report.ts class` · `npx tsx scripts/measure-class-size-deltas.ts` |
| A2s | class record-node sizing (→ size-`conformant`) | done (round 1) | A2, S1L | **DONE 2026-08-05 (rounds 1+2): 709/711 = 99.7% size-conformant, widened 0** (round 2 closed 22 of the 24 queue rows: TIM/embedded notes, emoji/monospace creole, package-scoped hide, association diamonds, font tiers) — plans/a2s-class-record-sizing/. THE finding: the 489-entry backlog was ORACLE POLLUTION — `oracle-corpus.ts#runOracle` omitted `-DPLANTUML_DETERMINISTIC_TEXT=true`, so class goldens carried AWT metrics (description goldens clean; maintainer-approved regen, ADR-5). Our pipeline was already near-exact: 442 phantom deltas closed by regen; G2 diagnosed 20 genuine mechanisms; fix round 1 closed 49 more (member tags, annotation-@, USymbol routing, hide family, notes creole incl. hline/table/bullets, wrapWidth/sameClassWidth/groupInheritance/minClassWidth/iconSize plumbing, blank-line preprocessor fix). Class dot-cache + parity survey regenerated deterministic. **Remaining 2** (ledger): gujigi-63 (SI1-gated folder leaf) + rotisi-30 (0.015in enhanced-body sprite-row fingerprint, raster−1 hypothesis falsified). `npx tsx scripts/measure-class-size-deltas.ts` |
| A3 | object DOT-sync | done | A2 | object ≥90% conformant + ledger. **DONE 2026-07-11: 78/80 comparable EQUAL (98%), 2 ledgered (creole `{{}}` embedded sub-diagrams — unimplemented subsystem), zero unledgered** — plans/object-dot-sync/. Object absorbed into the class engine (upstream has no object engine); `map`/embedded-`json` ported; 78-golden ratchet with per-fixture size pins (29-entry shrink-only size backlog in oracle/goldens/object/size-backlog.json). | `npx tsx scripts/dot-sync-report.ts object` |
| A4 | state DOT-sync | done | A2 | state ≥90% conformant + ledger. **DONE 2026-07-12: 260/261 comparable EQUAL (99.6%), 1 ledgered (dot-engine render crash on verified-correct DOT — file upstream), zero unledgered** — plans/state-dot-sync/. Svek-faithful rewrite (two-pass ParserPass parse, quark name resolution incl. dotted-id hierarchy, autarkic child passes in getOrdered global order, CONC regions as first-class firing units, cluster envelopes + zaent, notes/json leaves); 260-golden ratchet with shrink-only size backlog (~90 entries = the size-fidelity work queue). | `npx tsx scripts/dot-sync-report.ts state` |
| A5 | json/yaml/hcl depth | **wip (substantial; NOT at bar)** | S2 (done) | SVG conformance at 100%-minus-known-divergences — **amended mid-mission by ADR-2b**: this family is Smetana-laid-out upstream and this port uses one layout engine, so byte-exact geometry is explicitly NOT the target. Bar is structure + node sizing + everything this port controls, readability first. | **Batches 1-3 DONE, Batch 4 closed the books honestly: 0/92 byte-conformant, and the mission is NOT claiming its bar.** Brief `plans/a5-json-family-conformance/`; per-fixture accounting in its `ledger.md` (all 92 named against 5 mechanisms). **What landed:** harness + 92-fixture corpus (the jar emits no DOT for this family — S2); the shared document shell (`jsonShell`, which also forced the `src/index.ts` split, 500→317); `TextBlockJson` sizing ported from the Java, replacing four unsourced constants and finding two mechanisms no measurement could reveal (`JsonDiagram.java:78-88` — empty containers and primitive roots become a one-entry `JsonArray`); the array-index-keys divergence RETIRED on measurement; upstream's transposed graph (T6); real `shape=record` ports (T7); and `JsonCurve` edge splines (T8). **Measured:** mean node Δy vs jar 6.65, 21/557 nodes exact, node Δw/Δh 3.08/0.96, child↔parent-row 64.34. **THE tool that made it possible:** `scripts/json-node-oracle.ts`, a per-node positional oracle built because ADR-3 left this family with only a scalar metric — it caught a FULLY MIRRORED diagram on its first run that document dimensions read identically before and after. **Remainder (ledger M2-M5, all PORT GAPs):** the root emits many children where the jar emits 2 (`<defs/>` + one content `<g>`), so `withRootGroupAttributes` no-ops and **no fixture's interior has ever been compared** — M2 gates M4/M5 and is the next task. Also an unattributed engine warning (`routesplines, Pshortestpath failed`) seen once in a full census and not isolated. |

Note: A2–A4 assert node sizes (not tolerant) from the start — goldens are now
captured under deterministic measurement (**S1i** done), but the leaf-box sizing
port (**S1L**) must land first so the port's sizes actually match; otherwise the
size assertion fails on the same box-height/multi-line gaps found for description.

## Phase B — Decisions/spikes that unblock large tranches

Resolve before spending on the missions they gate.

| ID | Mission | Status | Blocked-by | Exit bar | Notes |
|----|---------|--------|-----------|----------|-------|
| S1 | text-measurement fidelity strategy | done | — | ADR-001 accepted: port PlantUML's `UnicodeFontWidthSansSerif` width table + neutralize oracle via `SVG_DETERMINISTIC` | Resolved 2026-07-05 — dot-engine neutralization pattern; see `planning/adr/ADR-001-text-measurement.md` |
| S1i | S1-impl: `WidthTableMeasurer` + oracle re-baseline | done | S1 | ✅ width table + `WidthTableMeasurer` (14 tests). ✅ oracle re-captured under `-DPLANTUML_DETERMINISTIC_TEXT` + all 294 goldens re-pinned deterministic (commit `e8d124d`); structure unchanged (component 234/259, usecase 59/87), ratchet green. **Sizes stay tolerant** — asserting them is blocked on S1L (measurement is now neutral; residual is proven layout) | Neutralization done; the tolerant→asserted flip moves to **S1L** below |
| S1L | description leaf-box sizing port (unlocks `conformant`) | done | S1i | Port `EntityImageDescription.calculateDimension` + USymbol `asSmall` margins + multi-line text-block height into `measureLeafNode`, then move `width`/`height` tolerant→asserted in `compareStructural`. Exit: ≥90% description `conformant` (≤0.01in) | **Diagnosed 2026-07-05**: box branch `layout-helpers.ts:171-175` has 3 gaps — (1) single-line height 35.6px vs oracle 44px (0.611in); (2) no multi-line term (oracle +14px/line: 1-line 0.611→2-line 0.806); (3) variable/markup `display` measures empty → width falls to `BOX_MIN_WIDTH` vs oracle's real content. Only 4/262 EQUAL fixtures within ±0.01in today. `BOX_HEIGHT_FACTOR`/`EXTRA` are tolerant-era approximations. **Plateaued 2026-07-06 at 62% component / 44% usecase** size-conformance among structurally-EQUAL fixtures after ~14 passes (from ~1%) — everything cleanly exact is done; remainder is the messy/tolerance tier (package/folder tab geometry, interface shield, LaTeX/emoji/sprites, wrapWidth) with near-zero per-fix gains. Per `planning/s1l-leaf-sizing.md`: proceed to A2 (its infra is what A2 needs); revisit with a tolerance-exclusion policy (~67% conformant-among-sizeable). **CLOSED 2026-07-27 (honest-accounting close, maintainer-selected):** sizes are now measured + asserted + regression-proof — `sizeConformantOk` in `svek-dot.ts`, `scripts/measure-description-size-deltas.ts`, `oracle/goldens/description/size-backlog.json` (120 shrink-only pins), and a per-fixture size assertion in `description-parity.ratchet.test.ts`. Measured **231/351 = 65.8% conformant** at the S1L close (66.2% excluding the LaTeX divergence); **236/351 = 67.2%** after S1L-b (2026-07-27, +5: dexigu/kenece/zifaji/butebe/zavitu). Only LaTeX (2 fixtures) is excluded as an inherent-tolerance DIVERGENCE; the other 113 misses are portable gaps, each named in `plans/s1l-leaf-sizing/ledger.md` and routed to sub-missions **S1L-a..g** below (re-bucketed 2026-07-27: container/cluster 46 is the dominant gap; package-tab is only 3) — NOT excluded. The ≥90% raw bar is superseded by the 2026-07-14 "100% minus named divergences" rule, which this close satisfies |
| S1L-e | description container/cluster sizing | wip | S1L | Bucket **47 → 13**. Six fixes landed, and NONE was cluster geometry — `container-cluster` is a first-match heuristic tagging any fixture with a container keyword + `{`, so it collects unrelated bugs: note `\n` resolution, link-endpoint `\n` resolution, the multi-line bracket-open colour regex, the **USymbolSimpleAbstract family** (control/entity/boundary stack a drawing above the label — Control 32×32, EntityDomain 32×32, Boundary 49×32), and EMPTY `[ ]` bodies (two bugs: `finishElementBlock`'s `body.length > 0` guard, and `lineCount('')` returning 1). Remaining 13 are triaged in `size-backlog.json` `_doc`: ~5 genuine `computeContainerBbox`, 4 sprite, 2 creole `{{ }}` embedded sub-diagram (UNIMPLEMENTED, ledgered under A3), rest misc. Exit: those entries → ≤0.01in, deleted |
| S1L-f | description sprite/stdlib-macro/icon sizing | done | S1L | **DONE 2026-07-28 (parts 1/2a/2b).** The bucket's premise was wrong three times: (1) kofuca-08's 478in was ENGINE MISROUTING via a leaking scoped `skinparam X<<stereo>> {` block → 0.306in; (2) the shared residual was a missing `fontSize/13` scale that `CommandCreoleSprite.java:82` applies to EVERY sprite kind (jar-verified at two font sizes and two kinds), which on fixing unmasked per-atom font sizes, a stereotype extracted from inside a quoted display, and `<<x>>`→`«x»` display guillemets; (3) `sprite N <svg …>` was not in the grammar AT ALL, and bootstrap + archimate are both SVG-sprite bundles — ported `CommandSpriteSvg`/`CommandSpriteSvgMultiline` + `UImageSvg#getData` (viewBox wins over width/height, which is why archimate's `mm` units never matter). **280 → 286/351 (79.8% → 81.5%)**, zero widened; structure unmoved on all three types; class ratchet unmoved. See `plans/s1l-leaf-sizing/ledger.md` § S1L-f |
| S1L-k | use-case ellipse footprint (sprite ink + point fit) | done | S1L-f | **DONE 2026-07-28.** Two parts. (1) Sprite INK bounds: `Footprint#drawPath` feeds the ellipse a path's min/max corners, computed by `UPath#addInternal` — ONLY a `SEG_ARCTO`'s endpoint, EVERY other coordinate pair incl. Bézier control points, i.e. the control-polygon box, no curve flattening (`svg-path-bbox.ts`). Explains bi-globe (inks 16×13.846) vs bi-bootstrap-fill (16×16) from identical `<svg width="16" height="16">`. (2) The real FIT: `ContainingEllipse` maps `y → y/alpha` and takes the SMALLEST ENCLOSING CIRCLE (`width = 2r`, `height = 2r·alpha`) of positionally-collected points — the old closed form is exact only for 2 opposite corners or a rectangle's 4, and a MIXED block is order-dependent. Text-box placement derived from `AtomText#drawU`'s `ypos = height − descent` with `getDescent = size/4.5` (→ box starts 1.611 above its line top; a numeric scan hit 10.9 before the constant was found). Stereotype lines merge above the label and contribute points. Jar-verified on SEVEN shapes to 5e-4 px. Also fixed `stripUrl` over-reaching into a quoted display. **286 → 291/351 (81.5% → 82.9%)**, zero widened; ruziru-69 + bootstrap-0 conformant. See `plans/s1l-leaf-sizing/ledger.md` § S1L-k |
| S1L-h | description per-element font skinparam sizing | done | S1L | **DONE 2026-07-28.** `resolveElementFontSize(theme, sname, 'title')` already existed and the RENDERER already called it (`renderer-symbol.ts#textFont`) — only the sizer never did (the 4th instance this session of that lock-step gap). Threaded `ClassifyCtx.fontSizeFor` → `BoxSizingOpts.fontSize`, resolved once in `measureLeafNode`. Only SIZE matters: `glyphWidth` ignores the family. The bucket UNDERSTATED it — the classifier matches only the `skinparam` spelling, so `<style> <el> { FontSize N }` landed in container-cluster; 9 fixtures carried a declaration, not 2. Nested gap fixed with it (jar-verified): an `<img>` cannot-decode fallback run draws at the DIAGRAM-DEFAULT font, not the element's — `<img:x/y.svg>` is 100.362×14 with or without a per-element font, and 100.362 = `(Cannot decode)`@14. **306 → 311/351 (87.2% → 88.6%)**, zero widened |
| S1L-g | description minClassWidth/MinimumWidth floor | done | S1L | **DONE.** Skinparam path (2026-07-27): `skinparam minClassWidth` → leaf-box content-width floor. Scoped `<style> <sname> { MinimumWidth }` path (2026-07-27, S1L-b T5): `resolveElementMinimumWidth` cascades a per-element floor over the global, threaded per-USymbol through ClassifyCtx→measureBox. dexigu/kenece/zifaji now **conformant** (min-width + S1L-b HR-height); zotiru-33's `not_nested` package is exact at 4.583in (delta 2.655→0.914). Residual: zotiru's `nested` package **cluster** floor → **S1L-e** (container sizing), not a leaf min-width gap |
| S1L-b | description display-text expansion | done | S1L | **Bracket-body + creole-`====` HR DONE (2026-07-27, `plans/s1l-b-display/`):** `[ … ]` element bodies now reach `measureLeafNode` as the display (parser T2); creole HR renders via the stencil interceptor (T1 — root cause was a dropped `UGraphicStencil.create` in `USymbolCloud`, not `renderer-cluster`) and sizes at 8px, formatting tags contribute 0 width (T3, creole lexer). Flipped dexigu/kenece/zifaji + butebe/zavitu conformant. **Codepoint decode-ordering DONE (2026-07-27, `plans/s1l-b-unicode/`):** `<U+…>`/`&#…;` decode per-line at measure/render time AFTER the `\n` split (T1, ADR-1, mirrors `AtomText.manageSpecialChars`) — codepoint newlines are inline, output-neutral (dot-sync 262/262+90/90 EQUAL, zero widened); heights de-inflated: gafico-37 5.68→3.75, nujito-06 3.35→3.12 (pins shrunk). **CLOSED 2026-07-27 (all misses named, per "100% minus named divergences"):** the remaining pins are DIAGNOSED, NAMED residuals, NOT emoji/quoted-title-literal bugs as planning assumed — gafico-37 (3.75) + nujito-06 (3.12) are driven by node c's UNPORTED `<code>` creole block (T2, deferred E2r L2; **decisions.md Rule 2 corrected** — the quoted `"$var"` title is creole-processed, not literal); lurupu-11 (2.05) by a **sizer↔renderer creole-lexer divergence** on `<font Name>`/unclosed-`<b>` (T3; **ADR-3 corrected** — emoji width is already exact) → follow-on "sizer↔renderer creole visible-text unification" **DONE 2026-07-27 (creole-lexer-unification): lurupu-11 conformant**; xufexu-38 (1.46→0.15) → S1L-e; fariba-82 (1.02) → S1L-d/S1L-f (T6). 236/351 = 67.2% → **239/351 = 68.1%** conformant after creole-lexer-unification. See `plans/s1l-leaf-sizing/ledger.md` S1L-b-unicode T1/T2/T3 + the creole-lexer-unification section |
| S1L-a | description package/folder tab geometry (leaf) | done | S1L | **DONE 2026-07-28.** `folder` and `package` are ONE class — `USymbolFolder(sname, showTitle)` — whose `asSmall` dimension is `getMargin().addDimension(dimName.mergeTB(dimStereo, dimLabel))` with `dimName = showTitle ? title : FIXED (40,15)`. `mergeTB` takes max-width/sum-height, so the tab is a BLOCK, not a decoration: for `folder` the 40 **floors the width** (a fixed `SYMBOL_ICON_ALLOWANCE` could only add height — `folder b` measured 37.79 vs the jar's 70.00, and it only bites on a short name, which is why no fixture caught it); for `package` the title is the element CODE, the label carries the display only when it differs, and the shown title adds a measured **+12px**. New `measureFolderLeaf`; `leaf-sizing.ts` split at the 500-line cap into `leaf-sizing-text.ts` (shared per-line measurement). Jar-verified exact on all five forms. **272 → 279/351 (77.5% → 79.5%)**, zero widened, 7 pins deleted; structure unmoved. Bucket now EMPTY — the other two fixtures were classifier misattributions on a leading `artifact`/`package` keyword, re-bucketed to new **S1L-i**/**S1L-j**. See `plans/s1l-leaf-sizing/ledger.md` § S1L-a |
| S1L-i | description creole titled separators | todo | S1L | 3 fixtures (codabo-50-mupa164, …): `--title--` / `==title==` draw a rule CARRYING their title text, so the line contributes the TITLE's width and rule height — not the raw markup measured as glyphs (`--title1--` measures 62.5px here vs the jar's `title1` 37.6px). Sizer and renderer must move together (`isCreoleHrLine`/`classifyStripeLine` already share one classifier — extend it rather than special-casing the sizer). Exit: those backlog entries → ≤0.01in, deleted |
| S1L-j | description multi-line quoted display | todo | S1L | 2 fixtures (tajadu-40-juro990, …): a quoted display left OPEN at end of line continues onto the following source lines — upstream's `CommandCreateElementMultilines`. We stop at the first line, so the id becomes the literal `foo2 as "This artifact`. Exit: those backlog entries → ≤0.01in, deleted |
| S1L-d | description wrapWidth wrapping | done | S1L | **DONE 2026-07-28.** The premise was wrong: `Fission` (the Neutron word-wrap) was ALREADY ported and ALREADY used by the leaf RENDERER (`EntityImageDescriptionSupport.ts#buildWrappedLines`) — only `leaf-sizing.ts` never called it, so a wrapped diagram measured unwrapped boxes while the renderer drew wrapped ones (same sizer↔renderer family as creole-lexer-unification). New `leaf-sizing-text.ts#measureTextBlock` REUSES `getSplitted` (never re-derives breaks) via `ClassifyCtx.wrapWidth`→`BoxSizingOpts.wrapWidth`; `maxWidth 0` is zero-diff. That made mejoxi-96's heights exact at once and isolated a second bug: the `["…"]` bracket shorthand stripped quotes upstream KEEPS (its quote-strip runs while the brackets still wrap the display, so it no-ops). **279 → 280/351 (79.5% → 79.8%)**, zero widened; fariba-82 also shrank 1.024→0.389. See `plans/s1l-leaf-sizing/ledger.md` § S1L-d |
| S1L-audit | description leaf-sizing AUDIT (survey, not fixture-chasing) | **done (90.3%)** | S1L | **Planned 2026-07-28 — `plans/description-leaf-sizing-audit/`.** 239→311/351 (68.1%→88.6%) took EIGHT sub-missions, and post-hoc the SAME TWO defect classes were rediscovered one fixture at a time, FOUR TIMES EACH: (1) a symbol COMPOSES differently upstream (interface `hideText`; folder/package `mergeTB` block; usecase `Footprint` point fit; control/entity/boundary stacked drawing) — not a wrong constant; (2) a feature reaches the RENDERER and the SIZER never calls it (creole lexer, `wrapWidth`/`Fission`, the point fit, per-element `FontSize` — in each case the resolver ALREADY existed and the renderer ALREADY called it). Both are ENUMERABLE FROM UPSTREAM and should have cost two checklists. Deliverables: `planning/usymbol-composition.md` (all 36 USymbols × upstream class × `asSmall` shape × `calculateDimension` × our dispatch), `planning/sizer-renderer-parity.md` (every resolver/setting the renderer consults vs what `ClassifyCtx`/`BoxSizingOpts` thread into `measureLeafNode` — 10 `resolveElement*` exist, the sizer threads 2), a REPAIRED `CAUSE_PATTERNS` (it has misled every bucket opened: sprite regex lacked `/`; container-cluster swallowed four unrelated bugs; element-font read 2 fixtures when it was 9), and a FITNESS FUNCTION so class (2) fails CI instead of recurring. Batches 4–5 are TEMPLATES — the tasks are derived from the audit rows, because a survey cannot name its findings in advance. Class (2) is NOT description-only: `planning/mission-guide.md` already records "note measured at 14pt, rendered at 12pt" in files. Exit: both tables committed, classifier repaired, guard green, and every row either fixed or filed. **BATCHES 1-3 DONE 2026-07-28** (`787843f`): `planning/usymbol-composition.md` 36 rows / 6 MISMATCH (HEXAGON doubles width; PERSON head = `sqrt(surface)*0.42`; USECASE_BUSINESS margins BEFORE the ellipse fit so it refits alpha; ACTOR_AWESOME 55x61 + ACTOR_HOLLOW 26x33 vs our hardcoded stickman; ARCHIMATE never becomes a leaf) and `planning/sizer-renderer-parity.md` 20 rows / 6 GAP, verdicts JAR-PROVEN. Classifier repaired (element-font read 2 when it was 9 — two bugs: the regex demanded `skinparam` adjacent to the Font-word, AND container-cluster was tested first and shadowed it); `measure-class-size-deltas.ts` imports `detectCause`, so CLASS triage improved for free (element-font 10->32). Guard shipped: `tests/architecture/sizer-renderer-parity.test.ts`, named resolver-reachability with `KNOWN_GAPS` as a shrink-only ratchet. **Two structural findings reframe Batch 4:** (1) the RENDERER already imports the faithful ported `src/core/decoration/symbol/` classes and the four `leaf-sizing*` modules import NONE of them, so all 6 MISMATCHes are ONE divergence — weigh routing `measureLeafNode` through the ported classes (upstream's own boundary) over patching more tables; blocker is the `StringBounder` vs `StringMeasurer` seam. (2) The parity gap has a SECOND variant the brief missed — reaches `BoxSizingOpts` and is never READ (`measureTextBlock` has ONE caller, so `wrapWidth`/`guillemet` skip 5 of 6 measure paths). ADR-3's limit quantified: 1 of 4 historical instances was resolver-shaped. Batches 4-5 remain TEMPLATES, rows now determined not guessed; ADR-2's gate half-met (6 kinds >= 4). Findings ledgered in `plans/s1l-leaf-sizing/ledger.md`. **T6 DONE 2026-07-28 (`dd5f789`): 311 -> 316/351 = 90.0%**, zero widened, 5 pins deleted — `measureLeafNode` now calls `EntityImageDescription.calculateDimensionSlow` (upstream's own sizing entry point) for the box family via a `StringMeasurer`->`StringBounder` adapter. **ADR-6's premise was HALF FALSIFIED and that is the load-bearing finding:** the ported `decoration/symbol/` path and our flat tables are faithful in DIFFERENT PLACES, not faithful-vs-lossy. `EntityImageDescription.ts:43` states none of `BodyFactory`/`BodyEnhanced*` are ported, so the ported path has NO folder title margin (T2 traced our +12 to `BodyEnhanced1.getMarginX()`=6) — routing folder/package widened 8 fixtures; same shape for sprite ink offsets (no field on `AtomImageResolver`), the `<img>` cannot-decode default font (no seam on `buildLine`), and our deliberate `<latex>` 0-width divergence. Ported classes = SYMBOL-COMPOSITION facts from Java; flat tables = TEXT-BLOCK facts from fixtures (the accreted long tail). Routing narrowed 4x, each diagnosed to a `file:line` first, never a fitted constant. Only 2 of 6 expected wins confirmed (PERSON + `wrapWidth`/`guillemet` per-path uniformity, which drove all 5 pins); HEXAGON/USECASE_BUSINESS/Shadowing/LineThickness NOT verified — no corpus fixture isolates them, filed per ADR-4 rather than claimed. **Consequences: T9 rescoped (do NOT delete the tables wholesale), ADR-2 LIVE again (not moot), and a NEW follow-on is the real ceiling — port `BodyFactory`/`BodyEnhanced*` + add ink-offset/default-font seams to the shared atom pipeline.** **MISSION DONE 2026-07-29 (`0b4e9f2`+): 317/351 = 90.3%**, zero widened throughout. T7 ported `ActorAwesome`/`ActorHollow` verbatim (`ActorAwesome.java:50-55`, `ActorHollow.java:51-58`, defaults `SkinParam.java:1209-1218`) and wired `skinparam actorStyle` through ONE `resolveActorStyle` consumed by all three call sites — closing what T6's own note had diagnosed as mutere-78's residual. T8 wired `archimate` (52.025x34, jar-exact) and in doing so hit the **4th instance of the decoration-extracted-from-inside-a-quoted-display class via a 4th distinct path** (its mandatory `#color` PRECEDES code/display, defeating `parseNameSection`'s `splitLeadingQuote` guard) — treat any new grammar whose prefix is not the quote as suspect. `CommandArchimateMultilines`/`Package` FILED with probes per ADR-4 (upstream inconsistency found: multilines creates its leaf with `USymbols.RECTANGLE`, not `ARCHIMATE`). T9 deleted only the dead `inkSprites` chain — verification first showed ALL 24 `leaf-sizing-consts.ts` exports still live, so nothing was superseded. **BATCH 5 SKIPPED on ADR-2's own gate**: batch 4 added ZERO new symbol tables and REMOVED one (`SIMPLE_SYMBOL_DRAWING`, absorbed by T6), net 5->4 — ADR-2 did its job by PREVENTING a refactor. **FOLLOW-ON (the real ceiling, NOT done): port `BodyFactory`/`BodyEnhanced*` + add ink-offset and default-font seams to the shared atom pipeline** — until then the routing cannot widen past the box family, and folder/package, usecase-with-sprites and `<img>`/`<latex>` displays stay on their pre-T6 paths for diagnosed reasons. Also filed as a SIZING gap (orchestrator correction to T8): `archimate` maps to the `'rectangle'` sname, so `<style> archimate { FontSize N }` moves the node in the jar and does nothing here |
| S2 | Smetana-vs-svek oracle for json/yaml/hcl | **done** | — | decision: extend oracle / new oracle / SVG-only scope | **RESOLVED 2026-08-08 — SVG-only, and the decision is forced, not chosen.** Settled by experiment, not inference: the pinned jar run with `-DPLANTUML_DUMP_DOT=<dir> -DPLANTUML_DETERMINISTIC_TEXT=true` over an `@startjson` diagram writes the SVG and **no `.dot` file at all**. Upstream lays json out through `jsondiagram/SmetanaForJson.java` — in-process Smetana, never an external dot process — so there is no `svek-N.dot` to capture and a DOT-parity gate for json/yaml/hcl cannot be built at any price. A5's exit bar is therefore SVG conformance directly, using the per-type harness already proven four times (`normalize`/`compare`/census/ratchet). **Risk this hands A5, to be settled by ADR there:** upstream's json layout IS Smetana (a graphviz-2.38 transpile) while ours is @knowvah/dot-engine, so the `smetana-not-a-conformance-target` note applies INVERTED here — for json, Smetana is the only target there is. Decide whether sub-tolerance geometry misses are acceptable or need naming. |
| S3 | stub-engine authenticity audit | spike | — | oracle-diff neato/fdp/sfdp/circo/twopi/osage stubs vs upstream on consuming types; list which (if any) need authentic ports | Authentic ports are 0.5k–16k C lines each — avoid unless proven needed |
| S4 | stdlib include surface audit | done | — | frequency table of `!include <…>` across pdiff corpus → bundle priority list. **DONE 2026-07-12** — `planning/s4-stdlib-audit.md` | **Inverts SI5's assumed shape.** `!include` is rare: 69/5688 fixtures (1.2%). Bundle priority: **tupadr3 (19) → c4 (10) → cloudinsight (8) → archimate (6) → aws (5)**; top-5 = ~48 of ~60 stdlib fixtures; rest ≤2 each. But the *preprocessor* half is 3–4× bigger (`!define` 123, `!procedure` 50, `!unquoted` 48, `!function` 46, `!definelong` 42, `!startsub` 7 — union ~200, cross-type). TIM is started not absent: we have 2 of upstream's **76 builtins** + a partial Eater set. **Therefore SI5 splits into SI5a (preprocessor, unblocked) and SI5b (bundling, licensing-gated).** |

## Phase C — Shared infrastructure (unblocks multiple types)

| ID | Mission | Status | Blocked-by | Exit bar | Notes |
|----|---------|--------|-----------|----------|-------|
| SI2 | `src/core/datetime.ts` | todo | — | parses PlantUML date formats + relative offsets + working days; unit-tested | Blocks Timing, Gantt |
| SI3 | `src/core/railroad/` | todo | — | terminal/nonterminal/seq/choice/opt/repeat → SVG; unit-tested | Blocks EBNF, Regex |
| SI4 | `src/core/golem/` grid | todo | — | 2D tile grid, edges between tile centers; unit-tested | Blocks Flow; eval Salt/Wire |
| SI5a | preprocessor / TIM completion | done | S4 | `!function` declare+return, `!foreach`/`!while`, `!$var` + scoping, `!startsub`/`!includesub`, `!elseif`, all builtins resolve; TIM-json ledger entry retired; no DOT regressions; four gates green. **DONE 2026-07-13** — `plans/si5a-tim/`, branch `feat/si5a-tim`. | **Ported upstream's entire TIM subsystem (~13.4k LOC Java) and CUT `preprocessor.ts` OVER TO IT.** `src/core/tim/` now holds the real `TContext` interpreter driving the `CodeIterator` decorator chain (`iterator/`), 25 `Eater*` subclasses, the expression evaluator (`expression/`: ShuntingYard→RPN, `TValue`), the memory/scoping model (`TMemory`/`TVariableScope`/`VariableManager`), **all 74 builtins** (`builtin/`) behind an injected `TimEnvironment` seam (clock/RNG/file/stdlib — nothing in `src/` touches `fs`/`process.env`/`Date.now()`/`Math.random()`), and a **sync `IncludeStore`** seam (`render()` prefetches async; `renderSync` takes a caller-supplied store). The flat line-loop — which **structurally could not** express nested `!foreach`/`!while`/`!if` — is gone, along with `resolveIncludes`, the textual pre-pass that ran *before* conditionals (so an `!include` in a false `!ifdef` was fetched AND inlined). **Results: 7,586 tests (+1,036 from the 6,550 baseline); DOT usecase 79→81/87, everything else at baseline (component 251/259, class 680/680, object 78/80, state 260/261) — zero regressions.** TIM-json ledger entry (zoriso-46, sidame) verified EQUAL and **retired**. `!include <bundle/…>`'s silent skip replaced by a typed `StdlibNotBundledError` — **seam only, nothing vendored** (SI5b still gated on the licensing ruling). 6 divergences recorded in `DIVERGENCES.md` § Preprocessor (TIM); one (orphan `!else`/`!elseif`/`!endif`) is **temporary, pending SI6**. |
| SI5b | stdlib bundling | done | S4, licensing ruling (signed off 2026-07-13/14) | `!include <bundle/…>` resolves for the vendorable bundles; C4/AWS smoke fixtures parse | **License audit done 2026-07-12; AWS verdict CORRECTED same day** (`planning/s4-stdlib-audit.md`). **ALL top-5 bundles are VENDOR-OK — no bundle is excluded on licensing grounds.** `c4`/`archimate`: MIT pure macro libs, no artwork. `tupadr3`/`cloudinsight`: MIT glue + CC BY 4.0 / Apache-2.0 / SIL OFL 1.1 artwork, attribution required. `aws`/`awslib*`: CC BY-**ND** 2.0 — an earlier draft called this NOT-VENDORABLE, which **was wrong**: §3(a)/(b) expressly grant incorporation into *Collective Works*, §1(a) says a Collective Work is not a Derivative Work, and §4(a) says the collective work "apart from the Work itself" need **not** be made subject to CC BY-ND (so our MIT stands). ND bites only on *modification*. **Hard constraint: vendor sprites VERBATIM** — any regenerate/re-encode/recolor step voids the AWS grant, so the pipeline must be a checksummed file copy, not a transform. Each packaged bundle ships with its own LICENSE + LICENSES.md. AWS Terms-of-Use page read 2026-07-13 (no extra conditions). **DONE 2026-07-14** — `plans/si5b-stdlib/` (branch `feat/si5b-stdlib`): ALL 34 bundles captured verbatim (pinned SHA bdbb819f, committed sha256 manifests, --verify gate); `stdlibStore`/`withStdlib` resolve with Stdlib.java's exact key semantics; four workspace packages generated from assets (`@plantuml-ts/stdlib` 1.7MB, `-aws` 8MB, `-tupadr3` 19.6MB, `-all` meta; adaml reserved for a GPL artifact; publish = maintainer, NOTE cloudogu has no upstream LICENSE file). E2's sprite/img half ported (6-bit+raw-DEFLATE decode, stored-block PNG, creole atoms, measurement, SVG image emission). **The 6 blocked fixtures are EQUAL: component 262/262 (100%), usecase 90/90 (100%).** 8,203 tests. Unblocks D15; residual: xusuxe full-render dot-engine crash (ledgered, file upstream). |
| SI6 | error-diagram path (`PSystemError`) | done | SI5a | `renderSync` renders an **error diagram** instead of throwing; orphan `!else`/`!elseif`/`!endif` error faithfully. **DONE 2026-07-13** — branch `feat/si6-error-diagram`; four gates green; DOT gate at baseline with no denominator moved (component 251/259, usecase 81/87, class 680/680, object 78/80, state 260/261). | **The premise this mission was written on was FALSE, and the record is corrected in `DIVERGENCES.md`:** the port DID have an error path — `src/index.ts#errorSvg`, a homegrown red box wired into every `catch` — so a preprocessor throw never escaped `renderSync`. It simply was not FAITHFUL. Ported `net/sourceforge/plantuml/error/` (`PSystemError`, `PSystemErrorV2`, `PSystemErrorEmpty`, `PSystemErrorPreprocessor`, `PSystemUnsupported`, `PSystemErrorUtils`, `ErrorUml`) + `eggs/PSystemWelcome` into `src/core/error/`, and REPLACED the red box with it: Welcome block (only under 5 source lines, as upstream gates it), version banner, `[From string (line N) ]`, the executed-source listing with the offending line waved red, the message. **Prerequisite discovered mid-mission:** `LineLocation` was an `unknown` stand-in and `readLines` stored a bare array index, so nothing could say WHERE an error was — ported `LineLocation`/`LineLocationImpl` and the real reader chain (top-level = `"string"`, includes described by target and parented on the `!include` line). `preprocessOrError()` is upstream's `TimLoader#load` (captures instead of throwing, marks the trace's last line); `preprocess()` stays as the throwing facade. **Retired all 3 divergences:** orphan conditionals now throw `No if related to this <directive>`; `TContext` throws `Function not found <name>`; `RetrieveProcedure`'s invented null guard removed. **Fixed a coupling this exposed:** `!undefine`'s macro removal was built ON the passthrough (it left the name in the `functions3` trie), so the trie is now rebuilt from survivors. Unclosed `!ifdef` at EOF still tolerated and pinned. Jar-verified line-for-line on the orphan `!endif` and the `Function not found` cases. 7,636 tests (+50). |
| SI7 | pipeline order: split `@start…@end` **before** TIM | done | SI5a | `extractBlocks` runs on near-raw lines; TIM preprocesses each block's *interior*. `buveco-86-tibo673` renders. | **Architectural divergence found 2026-07-13 (SI6 verification).** Our order is `preprocess() → extractBlocks()`. **Upstream's is the reverse**, and the distinction is load-bearing: `BlockUmlBuilder` reads through `preproc2.Preprocessor` — which does **only** config-injection + line-merging, **NOT** TIM — so `@start`/`@end` are detected on near-raw lines and **cannot be swallowed by a conditional**. TIM (`TContext`, `!ifdef`) then runs on the block's *interior*. Because we invert this, a conditional can eat our `@enduml`: `buveco-86-tibo673` (an unclosed `!ifdef`, itself a PlantUML bug report — forum.plantuml.net/6808) loses its `@enduml`, so `extractBlocks` finds no block and we emit "no diagram found" where the jar renders the title. **Do NOT patch this in `extractBlocks` by closing unclosed blocks at EOF** — that is a symptom fix at the wrong layer and gives a *new* wrong answer on a *false* unclosed `!ifdef` (where the jar correctly reports an empty document). Mirror upstream's order instead. Also needs a **default diagram type** for a content-less `@startuml` (jar → `CLASS`; we → "unknown diagram type"). **Impact: 1 of 5,694 pdiff fixtures; 0 of the 1,428 DOT-gating fixtures** — real but not urgent. **Done 2026-07-13** (`feat/si7-pipeline-order`): `src/core/BlockUmlBuilder.ts` mirrors upstream — raw split, then TIM per block. Blocks now carry their own theme/skinparam/`<style>`, an empty block renders the jar's `Empty description`, and a `@startuml` no probe claims falls back to `class` (upstream factory order). DOT gate unmoved on all five types (component 251/259, usecase 81/87, class 680/680, object 78/80, state 260/261); 7643 tests green. Residual: `buveco-86` now renders as the CLASS diagram the jar renders, but its **title is not drawn** — `title` is an ignored directive in every engine of this port (`state-commands.ts:96`, `description/parser.ts:379`), a pre-existing gap SI7 does not close. |
| SI9 | authored-fixture registration in the DOT-parity corpus | done | — | an authored fixture under `oracle/goldens/svg-description/` can obtain a real `parity.json` entry and become ratchet-eligible; the three sprite fixtures ratchet in | **Raised by `svg-sprite-nanoparser` 2026-07-31.** Its three authored sprite fixtures match the jar with **zero diffs** under `compareSvg(…, 'deterministic')` and are **DOT-equal** (verified with the survey's own `computeDotEqual` against freshly generated jar DOT) — yet they cannot be ratcheted. The ratchet's AC3 eligibility test needs a `parity.json` entry; `parity.json` is generated by `scripts/svg-parity-survey.ts` iterating `test-results/dot-cache`, a LOCAL cache built by `dot-sync-report.ts` over the pdiff corpus. Fixtures authored directly under `oracle/goldens/` never enter that pipeline, so no entry can exist. Hand-writing one would fabricate oracle data, and the survey's `verdict`/`maxDelta` come from a different render path (`renderSync` + `WidthTableMeasurer`) than the ratchet's (`renderFixture` + `DeterministicMeasurer`), so they cannot be filled honestly by hand either. **This is a structural hole in CLAUDE.md's own "author fixtures to cover gaps" doctrine** — authoring is supported, registering the result is not. **Consequence until it lands: the sprite corpus has NO regression guard.** `svg-sprite-nanoparser` proved that concretely — a change that made sprites render as nothing passed `npm test`, all 389 goldens and the size-delta script. **DONE 2026-07-31** — `plans/si9-authored-fixture-registration/` (on `main`, 4 commits). `scripts/dot-sync-fixtures.ts#enumerateFixtures` merges `oracle/goldens/svg-description/<type>/*/in.puml` with the `tests/visual/data/<type>.json` manifest, deduplicated by slug (manifest wins a collision, and says so) — the reader-side fix, so none of the manifest's six other consumers sees anything (ADR-1). ADR-2 was the non-obvious half and it was **confirmed live, not assumed**: `ensureCanonical` early-returned whenever the canonical directory held any `.svg`, so the newly-enumerated fixtures would have been given no canonical, no `data-diagram-type` tag, and dropped by `buildAgg` **in silence** — the mission would have looked done and changed nothing. Freshness is now per-slug (first run reported "missing 3 of 354" against a directory of 359 SVGs), and `buildAgg`'s skips are named on stderr, split into "no canonical SVG" (actionable) and "canonical but tagged another type" (expected bulk). **usecase DOT parity 90/90 → 93/93, still 100% EQUAL** (this row only, per ADR-4 — the A1/S1L rows keep their dated 90/90). All three sprite fixtures re-measured at **0 diffs** via `renderFixture` + `DeterministicMeasurer` and ratcheted in with `source: "authored"`; ratchet suite 51 → 54 tests, 11176 total. The goldens README documents the five-step authoring→registration path, and `svg-sprite-nanoparser`'s ADR-5 carries a dated amendment rather than a rewrite. **Finding worth carrying:** regenerating `usecase` parity also moved **47 pre-existing rows** — the committed baseline was from 2026-07-15 with 137 intervening `src/` commits (33 deltas shrank, 8 grew, 6 moved path only; **0 `dotEqual` flips, 0 verdict transitions**). Reported before committing, per stop condition 5, and taken deliberately on a maintainer ruling. **Still stale and unmeasured: the 265 `component` rows**, plus the committed `tests/oracle/svg-conformance/PARITY-SVG.md`, which no test asserts and which is now 3 rows behind. SI8 (stdlib package registration) and SI10 (class-engine `measureUsecase` coupling) remain open. |
| SI10 | retire `measureUsecase`'s class-engine coupling | done | — | `usecase-footprint.ts` → `footprintBoxes` → `measureUsecase` no longer called unconditionally from the class engine; the analytic substitute retired | **Inherited, not new.** `src/diagrams/class/class-layout-leaf-shapes.ts:14,27` calls `measureUsecase` unconditionally from the CLASS engine, predating the guard mechanism — which is why `svg-sprite-nanoparser`'s ADR-3 scoped it out and made touching it a STOP. Now inherited by whoever next touches `leaf-sizing.ts`'s resolver. See `plans/s1l-leaf-sizing/ledger.md` § "The SVG-sprite ink gap". **DONE 2026-08-02** — `plans/si10-usecase-actor-routing/` (on `main`, 4 commits `4f25be0d`…`6bee402a`). **Shipped:** the class engine's `usecase`/`actor` leaves are sized by the description engine's faithful `EntityImageDescription` path via a new `measureUsecaseOrActorLeaf` (ADR-2 — one owner for the formula, the class engine calls in rather than reimplementing the routing decision); `sprites` threaded into `class-layout-helpers.ts:286`, which had it in scope and simply never forwarded it, so class-diagram usecases were sized with NO sprite awareness; the multi-line `<$sprite>` arm of `hasUnroutedUsecaseMarkup` removed; and the path's FIRST EVER fixtures authored. **THIS ROW'S "the analytic substitute retired" CLAUSE WAS NOT ACHIEVABLE AS WRITTEN, and is re-scoped rather than restated.** `measureUsecase` has TWO live callers, not one: closing the class-engine caller leaves the description engine's `<latex>` route, and that route is **measurably load-bearing, not merely permanent-by-policy** — disabling both arms of the guard measures **widened 2**, where disabling the sprite arm alone measures **widened 0** with an IDENTICAL cause histogram and both named fixtures (`bootstrap-0`, `ruziru-69-xixo434`) at `delta 0, conformant true`. So `measureUsecase`, `measureActor`, `usecase-footprint.ts` and `footprintBoxes` all SURVIVE (ADR-1), and full retirement is a separate mission that must first find a home for latex-bearing usecases. **The sprite arm's removal is this mission's own re-test of a deferral:** its comment recorded that it was "not re-tested for dissolution" after `svg-sprite-nanoparser`, because `measureUsecase` was then that mission's STOP — and the 0.029321in widening it existed to prevent no longer reproduces, because that mission's two-channel architecture closed it. **The path had ZERO golden coverage before this mission** — 0 of 310 class goldens contained `usecase`, `actor` or `allowmixing`, the same hole `svg-sprite-nanoparser` shipped a sprites-render-as-nothing regression through while every gate stayed green. Three fixtures now cover it, each dual-verified to reach the CLASS engine (a usecase WITHOUT `allowmixing` routes to the description engine, so an unverified fixture would have silently tested the wrong one). Per ADR-4 they MEASURE rather than claim conformance, and none is zero-diff — **9, 6 and 3 diffs** — each pinned as a labelled characterisation guard, every `golden.svg` verbatim jar output, and all three ratchet-INELIGIBLE (see the SI9-extension row below). **Two findings worth carrying, both PRE-EXISTING and outside this mission's scope:** (1) **this port is more PERMISSIVE than upstream for `actor` in a class diagram** — the jar's golden for that fixture is upstream's ERROR page, because `CommandCreateElementFull2.java:197-198` gates `actor`/`usecase` behind `allowmixing` via `Mode.NORMAL_KEYWORD` and errors otherwise, while this port has no such gate and renders the diagram; that dates from mission A3 and also corrects this mission's own brief, whose reachability table recorded the input as simply "reached" without asking what upstream does with it. (2) **Sizing is sprite-aware but RENDERING is not** — the sprite fixture's ellipse matches while the label emits the literal escaped text rather than resolving to an image, because scope item 3 threaded sprites into the SIZER only. **Unchanged deliberately:** the `<latex>` arm, `DIVERGENCES.md`, and the description engine's non-usecase routing. Gates: 474 files / 11,382 tests; typecheck, lint, build exit 0; size-deltas **320/351, widened 0** throughout, with an identical cause histogram at every step; all 312 class goldens byte-identical (none contains a usecase, actor or sprite, so any movement would have meant the change reached further than intended); 449 golden+ratchet tests green. |
| SI8 | stdlib package registration seam | done | SI5b (done) | a browser-safe way for a consumer to register which stdlib packages back `!include <thing>`, with per-bundle lazy loading; `renderFixture` wires it and the three sprite goldens revert from inlined declarations to `!include <bootstrap/bootstrap>` | **Raised by the maintainer 2026-07-30.** SI5b delivered *resolution* — `stdlibStore(...bundles)` + `withStdlib(base, stdlib)` — but not *registration*: the only builder is `scripts/stdlib-assets-store.ts`, which imports `node:fs`, so a browser consumer had to hand-assemble `BundleData[]` eagerly, and `@plantuml-ts/stdlib-tupadr3` is 19.54 MB. **CORRECTION to this row's original framing** — it said the async fetch "must happen in a `prefetchIncludes`-style pass", which reads as *build that pass*. `prefetchIncludes` already existed and `render()` already used it; what it lacked was stdlib awareness. Reading it literally would have produced a duplicate walk, which ADR-4 rejects. Recorded so the next reader does not repeat the misreading. **DONE 2026-07-31** — `plans/si8-stdlib-registration/` (on `main`, 10 commits). **A live shipped defect was found while planning and fixed first (T1):** `prefetchInner` gated `<bundle/thing>` on exact-key `store.has(url)` and never consulted `getPumlResource`, so `render()` + `withStdlib` returned an error card reading *"pass options.includeStore"* to a caller who had — the async API was unusable with stdlib bundles. `renderSync` was unaffected only because it skips prefetch, and all 10 in-repo `withStdlib` call sites feed `renderSync`, which is why no test caught it. The fix needed a second half the plan had not named: `BackedIncludeStore` (the store `render()` hands the interpreter) did not forward `getPumlResource` either. **Shipped:** `stdlibRegistry({ c4: () => import('@plantuml-ts/stdlib/c4') })` — explicit dynamic-`import()` thunks, per-bundle lazy, memoized per promise (concurrent resolves share one load), with `StdlibChunkLoadError` kept distinct from not-registered; the registry wired into the EXISTING prefetch walk as a third channel consulted only after both eager channels miss, with resolved bundle text re-entering the same walk (`assets/stdlib/c4/C4_Context.puml` opens with `!include <C4/C4>`, verified end-to-end against the real package: 21,410-char outer, 67,422-char nested); `prepareIncludeStore()`, the async warm-up a `renderSync` caller awaits — `renderSync` stays synchronous, unchanged; and a rewritten `StdlibNotBundledError` that names the bundle, says whether a registry was supplied, and points sync callers at the warm-up instead of the now-misleading "pass options.includeStore". **The module unwrap the plan assumed did not survive the real packages:** `@plantuml-ts/stdlib/bootstrap` emits TWO bundles in one chunk (an alias stub with `files: {}` plus the payload) and mangles identifiers (`bootstrap1.13.1` → `bootstrap1_13_1`), so the registry harvests every `BundleData`-shaped export and keys by the `name` FIELD; alias semantics stay in `StdlibStore#resolveBundle`. **Adjacent bug fixed (T5):** `render-fixture.ts` passed no options to `buildBlockUmls`, so no golden in any suite could use `!include` — its "mirrors the census exactly" claim was false in TWO ways (the missing store, and a seed taken from raw source vs the census's directive-stripped `block.lines`); both are now stated in the doc comment. **All three sprite fixtures reverted to `!include` and returned to ZERO diffs** (6/2/4 `<path>`, 0 `<image>`), goldens re-captured from the pinned jar. **NOT shipped, deliberately — see SI11:** per-*resource* splitting. Per-bundle laziness fully solves `@plantuml-ts/stdlib` (1.8 MB / 5 bundles, largest `bootstrap.js` 1.06 MB) and makes the other two opt-in, but cannot shrink a single bundle: `awslib14.js` is 7.93 MB and `tupadr3.js` 19.54 MB, each ONE bundle. Splitting them requires changing the generated package shape (6,849 tupadr3 `.puml` files; 29,101 across the assets tree) — a packaging mission, not a seam mission. Gates: 459 files / 11,219 tests; size-deltas 320/351 widened 0; 389 goldens byte-identical; 54-fixture ratchet zero-diff. **One maintainer ruling mid-mission:** T5 made two fixtures recorded as `status: "error"` in `diff-baseline.json` start rendering at **0 diffs** (`usecase/bootstrap-0`, `usecase/ruziru-69-xixo434`) — their recorded `reason` fields had named T5's exact mechanism as the cause. Moved to `status: "baseline"` on approval, since that manifest sits outside every task's write-set. Neither is ratchet-promotable: `parity.json` has `dotEqual: false` for both. SI10 (class-engine `measureUsecase` coupling) remains open. |
| SI11a | per-*resource* stdlib loading (fetch half) | done | SI8 (done) | a consumer pays for the stdlib RESOURCES a diagram names, not the whole bundle; `<tupadr3/font-awesome-5/ban>` does not cost 19.54 MB | **SI11 was SPLIT into SI11a and SI11b on 2026-07-31** (maintainer direction) — if you remember only "SI11", both halves are here. The split was not cosmetic: SI11b needs a TRANSFORM of a vendored file plus a licensing carve-out, and its sprite index rides on the manifest/asset shape SI11a establishes, so planning it first would have built on an unbuilt foundation. **DONE 2026-07-31** — `plans/si11a-per-resource-stdlib-fetch/` (on `main`, 14 commits). **Shipped:** a parallel `.remote` manifest module per bundle (`{name, aliasOf?, files: key→relPath}`, no content) emitted alongside the untouched eager module; `remoteStdlib({manifest, baseUrl, fetcher?})` with promise-memoized per-key fetching and drop-on-rejection; `resolveResource(bundle, key)` on the registry with a UNIFORM eager/remote implementation so the walk never branches; per-resource routing inside `prefetchInner`'s registry channel; a concurrent walk with walk-layer in-flight dedup; the two packages shipping their `.puml` assets plus `.remote` subpaths; and the public API with `docs/stdlib-remote.md`. **MEASURED end-to-end (T8), not projected — a 3-icon tupadr3 diagram:** manifest 51,415 B gzip + 9,564 B of resources = **60,979 B over the wire against the eager module's 20,488,276 B — a 99.702% reduction.** Four resources, not three: `common.puml` plus one per icon. **Per-bundle manifest floor, measured on real emitted output:** tupadr3 433,420 B raw / 51,803 B gzip; awslib14 71,848 B / 11,116 B gzip — **note ADR-6 projected 8.3 KB for awslib14; the real figure is 11.1 KB.** **NOT solved, per ADR-6 (measure and state, never assert):** request count — an N-icon diagram makes ~N+1 requests, and HTTP/2 multiplexing mitigates latency×N without erasing it (which is why the walk is concurrent); `awslib14`'s eleven `*/all.puml` aggregators still run 116–445 KB each, so `!include <awslib14/Compute/all>` fetches 445 KB — far better than 7.93 MB, but not small; **nothing for bootstrap** (see SI11b); and the manifest is a FLOOR paid before a single resource loads. **ADR-4 (no default `baseUrl`) held:** the library still makes no network call a consumer did not configure, which is what made this safe to land in an offline render path. **Three brief claims proved false under measurement and are corrected in the journal:** (1) `assets/stdlib/tupadr3` contains ZERO `!include` directives, so the "a tupadr3 icon includes `<tupadr3/common>`" example is wrong — `C4/C4_Context.puml` is the real analog; (2) ADR-6 names `<awslib14/Compute/all>` as a concurrency cascade risk, but that file has zero `!include` lines — its 455,460 bytes are inline sprite bodies, a payload concern only; (3) a bare `StdlibRemoteManifest` is structurally IDENTICAL at runtime to a `BundleData`, so registering one directly would serve file PATHS as file CONTENT — the working idiom pre-wraps via `remoteStdlib()` in the consumer's own thunk. **Three scope stops, all escalated rather than self-approved:** the generator wiring and the `Stdlib.java` key transform both needed files in NO task's write-set (resolved as `fix(T5)` and a new shared `src/core/tim/stdlib-path.ts`); a third was resolvable in-mission because the file belonged to another task. Gates: 463 files / 11,266 tests; typecheck, lint, build exit 0; `vendor-stdlib --verify` verbatim across 34,587 files; size-deltas 320/351 widened 0; 389 goldens byte-identical; 54-fixture ratchet zero-diff. **Consequence worth knowing before publish:** the two packages roughly DOUBLE (stdlib-aws 8.3 → 15.9 MB, stdlib-tupadr3 20.5 → 38.9 MB) because ADR-1 keeps the eager inlined module byte-identical while ADR-4 additionally ships the raw files — each bundle's content now exists twice, in two encodings, deliberately. `npm publish` remains maintainer-gated (SI5b) and was NOT part of this mission. SI10 (class-engine `measureUsecase` coupling) remains open. |
| SI11b | bootstrap per-*sprite* splitting | done | SI11a (done) | a consumer pays for the bootstrap SPRITES a diagram names, not the 1.06 MB file that holds all 2,078 | **The other half of the SI11 split (2026-07-31).** Per-RESOURCE loading does nothing here, and that is measured, not assumed: `bootstrap1.13.1` is **1.06 MB in THREE files, with 2,078 sprites in ONE 1,059.9 KB file** — so SI11a's per-resource granularity leaves it exactly where it was. Going finer means splitting WITHIN a file. **Three things make it a distinct mission, not a follow-up commit:** (1) it requires **transforming a vendored file**, which needs an explicit carve-out from SI5b's checksummed-file-copy rule — bootstrap is MIT so licensing permits it, but the carve-out must be documented and **confined to MIT bundles, never AWS** (`awslib14` is CC BY-**ND**, where any re-encode voids the grant); (2) sprites are **NOT resolved through the include seam** — `SpriteRegistry` is a per-diagram `Map` read SYNCHRONOUSLY at measure/render time (`getSprite`, `spriteDimsLookupFor`, `Stereotype#getSprite`), and no `await` is available at lookup while `renderSync` must stay sync, so loading must be **prefetch-driven off a `<$name>` scan**, not demand-driven at lookup. Verified during SI11a planning and recorded so it is not re-derived; (3) it builds on the manifest/asset shape SI11a established, which is why it was sequenced second. **Known characteristic to document, not fix (SI11a ADR-7):** the sprite NAME namespace is flat and global per diagram (`addSprite` is `byName.set`, last-write-wins), so two collections both defining `$star` collide. Bundle PATHS are namespaced; sprite NAMES are not. That is upstream's `SkinParam.sprites` behavior. | **DONE 2026-07-31** — `plans/si11b-bootstrap-sprite-splitting/` (on `main`, 11 commits). **CORRECTION to this row's original framing, and it is the big one: point (1) above is WRONG. This mission required NO transform of a vendored file and NO carve-out from SI5b's checksummed-copy rule.** ADR-1 checked the claim against the code instead of inheriting it: SI11a already DERIVES artifacts from `assets/stdlib/` into the package tree (`.remote.js` manifests, a verbatim asset copy) without mutating anything, and per-sprite fragments are derived output of exactly the same kind. The vendored file is only ever READ. **`vendor-stdlib --verify` was never touched and still reports all 34,587 files verbatim** — the mission's riskiest proposed edit disappeared entirely, and with it SI11b's reputation as the dangerous half of the split. A premise inherited from a previous mission's notes is still a claim to verify. Points (2) and (3) held exactly as written. **Shipped:** a splitter deriving 2,078 per-sprite fragments plus a sorted name manifest into `packages/stdlib/assets/bootstrap1.13.1/`, wired into `npm run build:stdlib` (not left as an emitter nothing calls); a **fail-closed MIT allowlist** — ADR-2 chose an allowlist over a denylist on measurement, because only 8 of 34 bundles carry a `license` field at all and **`awslib14` carries none**, so a denylist would have failed OPEN on precisely the CC BY-**ND** bundle where deriving a modified work voids the grant; `scanSpriteNames` reusing the ONE existing `SPRITE_PATTERN_SOURCE` rather than a second regex; `spriteSplitStdlib(...)` registration and per-sprite routing inside the existing prefetch walk, reusing `remoteStdlib`'s per-key memoization with no second cache; the packaging gate that resolves `npm pack --dry-run --json`'s real file list; and ADR-7's collision-warning seam — `SpriteRegistry.collisions` plus an opt-in `RenderOptions.onWarning`. **MEASURED end-to-end (T6), not projected — a 3-sprite bootstrap diagram:** manifest 7,402 B gzip + 3 fragments 2,066 B = **9,468 B over the wire against the 1,085,342 B whole-file baseline read from disk — a 99.128% reduction**, on 3 fetches. That is ahead of the brief's ~98.7% projection. **NOT solved, per ADR-6 (measure and state, never assert):** the **manifest floor dominates at small N** — 7,402 B of gzip buys nothing by itself, and at one sprite you pay all of it for ~431 B of content; **request count, not bytes, is the ceiling here — the inverse of SI11a** — a 20-sprite diagram makes ~21 requests; a `<$name>` produced by a macro is invisible to a source scan, so ADR-5 answers it with a NAMED error plus the explicit `RenderOptions.sprites` escape hatch (a whole-file fallback was rejected: it would silently restore the 1.06 MB this mission removes, for exactly the diagrams most likely to be large); and **sprite NAMES remain a flat global namespace**, last-write-wins, now warned about rather than fixed — `addSprite` is `byName.set` and that is upstream `SkinParam.sprites` behavior. **Two defects caught in orchestrator review, both the same shape — code that works in-repo and fails for a consumer:** (1) the ADR-5b escape hatch first applied only to the top-level source, so a sprite-split `!include` reached through a diagram author's own shared header dropped an option-named sprite **silently** — the outcome ADR-5a forbids, inside the valve ADR-5b exists to provide; fixed by making the names walk-constant, with a nested-include regression test. (2) `spriteSplitStdlib` shipped **unreachable from the public API** — `package.json`'s `exports` map has a single `"."` entry, so `src/index.ts` is the only surface a consumer can import from, and the helper was not re-exported there while its own doc comment advertised a deep `core/` path no export map serves. Every unit test passed, importing by relative path. Same class as SI11a's emitter that nothing called, one layer out. **Brief correction worth carrying:** T6's acceptance criterion specified asserting `<image>` / `data:image/...` elements, inherited from SI11a's PNG-form tupadr3 test — bootstrap's sprites are SVG-form exclusively and render as inline `<path>`; the real golden carries **zero** `<image>` tags. Gates: 468 files / 11,317 tests; typecheck, lint, build exit 0; `vendor-stdlib --verify` verbatim across 34,587 files; size-deltas 320/351 widened 0; goldens byte-identical; 54-fixture ratchet zero-diff. **Follow-ups, neither actioned (both outside every task's write-set):** `tests/unit/stdlib-packages.test.ts`'s comment that `packages/stdlib` "has no prepack and no assets" is half stale — the conclusion still holds, the reason does not; and `src/core/sprite-split-stdlib.ts` also holds the general `bundlesFor`/`stdlibContentFor` alias walk, so its name under-describes it. `npm publish` remains maintainer-gated and was NOT part of this mission. SI10 (class-engine `measureUsecase` coupling) remains open.
| SI12 | stop shipping each stdlib bundle twice | done | SI11a (done), SI11b (done) | `@knowvah/plantuml-stdlib-aws` and `-tupadr3` ship ONE encoding of their content, not two; neither package's install carries a payload its consumer cannot use | **Maintainer decision 2026-08-01, measured before deciding.** Each big bundle currently ships its content TWICE: an eager inlined `BundleData` module AND the raw `.puml` assets. Measured unpacked: `-tupadr3` **40.8 MB** = eager `tupadr3.js` **20.49 MB** + assets 19.9 MB + remote manifest **0.43 MB**; `-aws` **16.7 MB** = eager 8.4 + assets 8.2. A remote consumer therefore installs 40.8 MB to use a 0.43 MB manifest and then fetches content from a CDN. **Assets cannot be the half that goes** — `docs/stdlib-remote.md`'s pinned-CDN recipe points `baseUrl` straight at `.../plantuml-stdlib-tupadr3@<v>/assets/tupadr3/`, so jsDelivr serves them out of the published tarball. **Decision: drop the EAGER module for these two only.** `@knowvah/plantuml-stdlib` keeps its eager path — at 2.9 MB total it is not the problem, and it carries C4/archimate/bootstrap, the bundles most likely to be wanted offline. This is SI11a's own finding applied to packaging: per-bundle laziness "cannot shrink a single bundle: awslib14.js is 7.93 MB and tupadr3.js 19.54 MB". Projected: `-aws` 16.7 -> 8.3 MB, `-tupadr3` 40.8 -> 20.3 MB. Nothing is published, so no consumer breaks. **Cost, stated:** `stdlibRegistry({ tupadr3: () => import('@knowvah/plantuml-stdlib-tupadr3') })` stops working for those two; offline use moves to `prepareIncludeStore` + a filesystem fetcher over the shipped `assets/`, which is already a documented recipe. **Blast radius traced two levels, and it is why this is a mission and not a commit:** the generator (`package-specs.ts` `modules` drives BOTH the eager emit AND `emit-index.ts`'s re-export, so emptying it empties `.`), the two packages' `exports`/`files`, the `stdlib-packages.test.ts` VERBATIM round-trip cases that read the eager awslib14/tupadr3 modules, the packaging gates' size ceilings, and `docs/stdlib-remote.md`. **One design question to settle in planning: `@knowvah/plantuml-stdlib-all`.** Its stated purpose is one-call register-everything via eager re-export; with two of its three dependencies losing eager modules, decide whether it re-exports remotes instead, narrows to the eager bundle only, or retires. **Finding worth carrying:** `-tupadr3` already has NO dedicated eager subpath export — its 20.49 MB module is reachable only through `.` -> `generated/index.js`, unlike `-aws` which exports `./awslib14` and `./awslib` explicitly. The two packages are already inconsistent on this. **DONE 2026-08-01** — `plans/si12-eager-module-removal/` (on `main`). **Shipped:** the generator stops EMITTING (not merely shipping) the eager `BundleData` module for `-aws`/`-tupadr3` (ADR-5); `.` now re-exports the REMOTE manifests `awslib14Remote`/`awslibRemote`/`tupadr3Remote` instead of an eager bundle (ADR-1); `@knowvah/plantuml-stdlib-all` re-exports `stdlib`'s five eager bundles PLUS all three remote manifests, with its README stating plainly that the manifest bundles need a `baseUrl` (ADR-4); and the packaging size ceilings moved off vacuous headroom to measured-plus-margin: `-aws` 18 → **10 MB** (measured 8,346,761 B, ~26% headroom), `-tupadr3` 45 → **24 MB** (measured 20,292,674 B, ~24% headroom). **MEASURED unpacked, before → after:** `-aws` 16.7 MB (this row's own pre-mission figure) → **8,346,761 B / 904 files** (`npm pack --dry-run --json`); `-tupadr3` 40.8 MB → **20,292,674 B / 6,858 files** — both inside the new ceilings, both hitting the brief's 8.3/20.3 MB targets. **T5's re-measured reduction is 99.693%, NOT SI11a's 99.702%** — the two are not comparable, by design (ADR-3): SI11a's denominator was the eager `tupadr3.js` module file (20,488,276 B), which SI12 deleted; T5's denominator is the sum of the bundle's asset bytes read from disk (19,850,300 B across 6,849 files), which is 3.11% smaller than the eager file it replaces because it is the SAME content in a leaner encoding, not missing content (verified three independent ways: `find`+`stat`, a standalone Node script, and the test's own sum — all three agree exactly). **Unchanged, deliberately:** `packages/stdlib` keeps its eager path at 2.9 MB total — it is not the problem, and it carries the C4/archimate/bootstrap bundles most likely to be wanted offline; `assets/` still ships in every package's `files` because `docs/stdlib-remote.md`'s pinned-CDN recipe resolves `baseUrl` against the published tarball's `assets/` tree and SI11b's per-sprite loading reads the same tree. **Breaking by `architecture.md`'s taxonomy** (exports removed) **with zero published consumers** — no versioning, dual-write, or deprecation window was warranted; versions stay `0.1.0`. **Finding worth carrying:** batch 2's gates passed WARM and shipped a cold-tree worker race that corrupted the mission's own headline measurement. `copy-assets.mjs`'s `isUpToDate()` guard is file-COUNT based and short-circuits only when the asset tree is ALREADY complete, so during any partial state a parallel vitest worker mid-copy or mid-read of `packages/*/assets/` got its tree `rmSync`'d out from under it by another worker's `beforeAll` — the same failure class as an SI11a race. Cold runs read 8,195,997 / 7,399,179 / 8,315,921 B (99.256% / 99.176% / 99.267%) instead of the true 19,850,300 B / 99.693%. Caught by an orchestrator COLD-tree check, not the batch's own warm gates; diagnosed to that mechanism and fixed in an added batch 5 (T8, `105c1309`), then re-verified 3× cold plus one independent orchestrator cold run, all landing on 19,850,300 B / 99.693%. One scope escalation was raised under stop condition 1 rather than self-approved (beyond the batch-5 addition above): T1's generator change broke an SI11b test (`tests/unit/sprite-package-files.test.ts`, `spec.modules` narrowing, `error TS18048`) in NO SI12 task's write-set — resolved by extending T4 rather than opening a new task, since it was the identical narrowing fix T4 already made in the sibling file. SI10 (class-engine `measureUsecase` coupling) remains open. |
| SI13 | extend SI9's authored-fixture registration to the CLASS corpus | done | SI9 (done), SI10 (done) | an authored fixture under `oracle/goldens/svg-class/<slug>/` can obtain a `parity-class.json` entry and become ratchet-eligible, the way SI9 made description fixtures eligible | **Raised by SI10's T3, 2026-08-02.** SI9 closed exactly this hole for the DESCRIPTION corpus and its own row already names the general shape of the gap; it does not reach class. `scripts/dot-sync-fixtures.ts`'s `GOLDEN_DIR` is hardcoded to `oracle/goldens/svg-description`, and `authoredFixtures(goldenDir, type)` looks in `<goldenDir>/<type>/<slug>/` — but class goldens are `oracle/goldens/svg-class/<slug>/`, with NO `<type>` level (`oracle/goldens/svg-class/README.md` says so explicitly). Wrong root AND a different directory shape, so no authored class fixture can enter the survey that generates `parity-class.json`, and the class ratchet's add rule requires a `parity-class.json` entry for its DOT-EQUAL eligibility condition. **Consequence today:** SI10's three authored class fixtures (`class-allowmixing-usecase-mix`, `class-actor-bare-no-allowmixing`, `class-usecase-inline-sprite`) are guarded by a dedicated pinned-diff test (`tests/oracle/svg-conformance/class-usecase-actor.test.ts`) instead of by the ratchet — a real guard, but one that lives outside the manifest CI treats as the source of truth. Note none of the three is zero-diff today (9/6/3 diffs), so eligibility would ALSO need the underlying conformance gaps closed; this row is about the registration path existing at all, which is the part that is structurally impossible right now. **DONE 2026-08-04** — `plans/si13-class-authored-registration/` (branch `feature/si13-class-authored-registration`: f48cd01a brief, f3bf0a12 T1, 4c3ceb9b T2). **Shipped:** `dot-sync-fixtures.ts` per-type golden layout (class → flat `svg-class/<slug>/` root; description shape byte-identical, guarded by test); class parity regenerated class-only (`--out`, concurrency 2): 718 → 721 rows, ZERO dotEqual flips, ZERO verdict downgrades, 75 upgrades (conformant 271→308, diverged 384→325 — the measured fruit of SI10/SI14/SI15), 16 deltas grew within already-diverged fixtures (journaled, A2s-relevant); the 3 analysable authored fixtures' jar-DOT oracle entries committed under `test-results/dot-cache/class/`. **Two drafted premises falsified at execution and amended in the brief:** (1) `tests/visual/data/class.json` EXISTS (768 entries) — class merges rather than authored-only; 310/314 flat-root slugs collide BY DESIGN (ratchet corpus goldens share the root; 302 byte-identical, 8 differ by exactly the manifest's smetana pragma — benign, both pipelines internally consistent), so the collision warning now counts identical-markup and names only DIFFERING-markup; (2) `test-results/dot-cache/` is COMMITTED oracle data (gitignore negation), not a local cache. **Eligibility measured per ADR-3: zero ratchet additions** — `class-missing-label-URL-SVG-0` conformant but ALREADY ratcheted (2026-07-16); the 3 usecase fixtures are dotEqual TRUE and fail only AC1's zero-diff (the shared pre-existing 1px viewBox/width gap — close it and they ratchet in with no further registration work); `class-actor-bare-no-allowmixing` can never have a parity row (jar refuses the input → error-page canonical, no CLASS tag) and stays guarded by its dedicated pinned test. The svg-class README documents the five-step authoring→registration path incl. both caveats (survey verdict is a different render path than AC1; error-page fixtures are pipeline-ineligible by design). |
| SI14 | share the measurement OBJECT, not the measured number | done | SI10 (done) | the class engine's `usecase`/`actor` leaves are DRAWN by the same `EntityImageDescription`/`TextBlockInEllipse` instance shape that sizes them, so the label offset comes from the fitted ellipse's own centre instead of the constant `cy - 2 + fontSize/3`; `description/usecase-footprint.ts` retired in favour of the object-based `core/svek/image/Footprint.ts` | **Planned 2026-08-03** — `plans/si14-usymbol-measurement-sharing/`. **The framing that produced it:** upstream never shares measurement NUMBERS. It shares the object (`IEntityImage extends TextBlock`; `SvekNode.java:73,116,367` holds one instance, sizes it, then draws the same one; `TextBlockInEllipse`'s fit is computed once in the ctor into a `final` field read by both `calculateDimensionSlow` and `drawU`) and the measurer (`ug.getStringBounder()`). **This port already implements both — in the description engine** (`renderer-entity.ts:387` is `new EntityImageDescription({...}).drawU(translated)`), so SI14 is not new architecture, it is bringing the ONE holdout onto the existing precedent. **Two Phase-2 corrections worth carrying:** (1) `measureActor` is already DEAD in `src/` — SI10 cut the last caller and the doc comment at `leaf-sizing.ts:18-21` still claiming `class-layout-leaf-shapes.ts` imports it unconditionally is STALE; (2) `usecase-footprint.ts` is NOT an approximation — its header records jar verification on seven shapes to 5e-4 px, so the port carries TWO faithful ports of the same fit differing only in MECHANISM (draw-to-measure vs compute-the-boxes), and its retirement is a mechanism swap gated on numeric identity, NOT a behaviour change. **Hard boundary:** the `<latex>` route's measurement PATH is unchanged — SI10 measured rerouting it as `widened 2`, and it is a permanent divergence (`DIVERGENCES.md:260-285`). **Deliberately deferred, tracked not skipped:** the ellipse `ry` delta (13.4846 vs jar 13.0625) is a SECOND adjacent mechanism in the fit itself that the centring fix will not close — T6 diagnoses it under `rules/diagnosis.md` and files it, fix separately scoped. Converting the class renderer to `UGraphic` wholesale is likewise named as its own future mission rather than absorbed. **DONE 2026-08-03** — `plans/si14-usymbol-measurement-sharing/` (branch `feature/si14-usymbol-measurement-sharing`, 10 commits, merge-commit merge required — journal references per-task IDs). **Shipped:** T1 `renderDrawableToFragment`/`mergeFragmentDefs` (klimt fragment seam, per-node `UGraphicSvg` + string unwrap reusing the EXISTING extraction primitives, id-collision PROVED by test, ADR-2 honoured — `svg-graphics-core.ts` untouched); T2 non-latex usecase fit now routes through the object-based `Footprint#getEllipse` with parity ~1e-14 px on the seven jar-verified shapes, `usecase-footprint.ts` DELETED, `measureActor` deleted, stale doc comment corrected — **but the retirement is PARTIAL per ADR-3's own fallback:** wrapping the `<latex>` branch in a real fit MEASURED as widened 2 (0.611632in→1.041146in on both latex fixtures; KaTeX's box is already wider than JLaTeXMath's and an ellipse fit around it grows the gap), so that branch keeps its raw `measureNodeLabel` box, tracked in `leaf-sizing.ts` doc comments; T3 `measurer?`/`sprites?` carried on `ClassGeometry` (OPTIONAL, not required as drafted — hand-built fixture literals construct the geo bare; the real `layoutSync` path always sets them); T4 **the point of the mission** — usecase/actor leaves draw through the same `EntityImageDescription.drawU` object shape that sizes them, constant `cy - 2 + fontSize/3` retired, label baselines now content-dependent; T5 layout-time atom pre-resolution removed (`atomsWidth` kept as documented-dead field — sole reader is the pre-T4 fallback branch, deleting both together is named follow-up). **Measured on the pinned fixtures:** `class-allowmixing-usecase-mix` 9→2 diffs (every actor-specific delta closed; remaining 2 are a pre-existing 1px viewBox/width delta); `class-usecase-inline-sprite` 10→11 — the faithful path becoming reachable EXPOSED the pre-flagged unrounded sprite dims in `EntityImageDescriptionTextBlock.ts#drawAtoms` (`1406e139`), and `text/@x` stayed at 2.003 because it shares the FIT mechanism, not the centring. **T6 diagnosed that fit mechanism to root cause:** `UImage.ts:34-40` returns declared/scaled dims where upstream `UImage.java:87-92` returns raster pixel count − 1; substituting upstream's numbers through UNMODIFIED fit code reproduces jar `rx`/`ry` to 5 decimals; 9 hypotheses ruled out with evidence — **filed as GH #26 + mission SI15**, fix separately scoped per the brief. **One stop condition raised, not self-approved:** T4 needed `render-fixture-class.ts` (in NO task's write-set — the harness calls `layoutClass`, which never sets `geo.measurer`, so every conformance fixture would silently exercise the pre-T4 fallback); escalated under stop 8, maintainer approved same day. Gates at close (cold tree): 476 files / 11,428 tests; 449 goldens byte-identical; typecheck/lint/build exit 0; vendor 34,587 verbatim; size-deltas 320/351 widened 0, histogram identical. |
| SI15 | `UImage` raster-pixel dims for footprint measurement | done | SI14 (done) | the usecase ellipse fit measures an inline sprite by upstream's raster-pixel-count-minus-one, closing the pinned `rx`/`ry` residual on `class-usecase-inline-sprite` (13.4846 vs 13.0625) and, expected, the `cx`/`text-x` 2.003 family | **Diagnosed to root cause by SI14 T6 (2026-08-03), GH #26.** `UImage.ts:34-40` returns the declared/scaled placement dims (what `measureInlineAtom` computed for label sizing); upstream `UImage.java:87-92` returns the rasterized PNG's native pixel count − 1, independent of placement scale. `Footprint.MyUGraphic.drawImage` (faithful port) calls the right method — the method answers a different question. Verified: substituting W=2,H=1 through unmodified `ContainingEllipse`/`SmallestEnclosingCircle` reproduces jar rx=48.968/ry=13.0625 to 5 decimals, plus an independent sprite-only probe. **Fix shape (from the diagnosis, to verify at planning):** second optional raster-pixel-dims field on `UImage`, consumed by `Footprint.drawImage`; then re-measure the whole cx/rx/ry/image-x/text-x diff family (cx/text-x sharing this root is MEDIUM confidence, not re-measured end-to-end). **Bundle into the same mission:** the sibling `image/@width`/`@height` rounding residual (3.2308×2.1538 vs jar 3×2) in `EntityImageDescriptionTextBlock.ts#drawAtoms` — pre-flagged by `1406e139`, first made reachable from class diagrams by SI14 T4; round at the `<image>` EMISSION site only (D9 Amendment 1 — rounding the resolver corrupts cursor advance). Also sweep up: delete `atomsWidth` + `tryRenderUSymbol`'s dead pre-T4 fallback branch together (SI14 T5's named follow-up); and T2's observed ~0.9 px mechanism divergence for ink-offset sprites in `text+sprite` ordering (synthetic only; real stdlib sprites reproduce the jar exactly). **DONE 2026-08-04** — `plans/si15-uimage-raster-dims/` (branch `feature/si15-uimage-raster-dims`, commits 56fd6318 T1 / b277c2ed T2 / 4061a9b7 T3 / 702f11df T6; T1 carries `Closes #26`). **Shipped:** T1 optional `rasterWidth`/`rasterHeight` on `UImage`+resolvers, `Footprint.drawImage` uses raster−1 when present (ADR-1 GUARDED fallback — deliberately not the diagnosis note's blanket −1); T2 deleted `atomsWidth`+dead fallback closure (zero writers existed); T3 raster-gated `<image>` emission rounding in the shared driver, jar-verified for `<img>` atoms via new authored fixture `class-usecase-inline-img` (6.5×3.9→7×4, round-half-up); T4 diagnosed T1 as UNREACHABLE from the description-engine sizing path AND falsified the raw-grid raster formula (jar's PNG for grid 16 at 14/13 is 17px, IHDR-decoded); T6 fixed both — raster = `Math.round(declared)` at every producer, sizing fallback carries raster dims gated on real-raster-backing+positive dims (unresolvable sprite would fit round(0)−1=−1). **Measured:** BOTH pinned class fixtures collapsed to ONLY the pre-existing 1px viewBox/width gap (inline-sprite 11→2 entries across the mission, inline-img 10→2) — the entire cx/cy/rx/ry/image-x/y/text-x/y family cleared, EXCEEDING the plan (interim honest residuals 0.075/0.4246 closed by T6); SI14 T2's ink-offset text+sprite divergence closed jar-exact, pinned as a permanent guard through `measureUsecaseOrActorLeaf` (both orderings). Size-deltas 320/351 widened 0, improved 6 (2 sprite, 2 emoji-unicode, 1 element-font, 1 container-cluster) — none crossed the bar, so NO backlog pins deleted; sprite bucket remains 5 with smaller deltas. Gates at close (cold tree ×2): 478 files / 11,444 tests exit 0; typecheck/lint/build clean; vendor 34,587 verbatim. Two stop-1-shaped out-of-write-set test expectation updates self-resolved and flagged in the journal (unreviewed own-brief write-sets). |
| SI1 | `src/core/cucadiagram/` shared base | done | A2,A4 (both done) | class/state/object/description/component consume one entity/link model | **DONE 2026-08-06** — plans/si1-cucadiagram-base/ (branch feature/si1-cucadiagram-base). Ported ONCE per ADR-10 of bodyenhanced: plasma Quark/Plasma, abel Entity(60)/Link(65 incl. sameConnections)/LinkArg/enums (LeafType 51/51), decoration LinkType family (+WithLinkType.isSingle), Bodier family + Member + MethodsOrFieldsArea + BodyEnhanced1 + full BodyFactory, net/atmp CucaDiagram (75 members, Base/Base2 split) + TitledDiagram slice + GroupHierarchy/PortionShower/EntityFactory bridge contracts (the DotData shape). Payoffs wired: shared `-[single]->` dedup in description+class+state (authored jar-oracle fixtures si1-single-dedup-{class,state}); folder/package titles via the real create2→BodyEnhanced1 (FOLDER_SHOWN_TITLE_EXTRA_WIDTH deleted — it WAS getMarginX=6×2) closing **gujigi-63 → class 711/712 = 99.9% w0** and shrinking 8 description pins. Contract doc planning/cucadiagram-contract.md + import-hygiene guard. Engines NOT migrated (each is a follow-on mission); skin/ remainder ~8k lines deferred; export pipeline typed-deferred. |
| SI16 | re-capture the stale `test-results/dot-cache/` for class, state and usecase | **done 2026-08-12** | — | each type's cache re-rendered through the pinned jar; that type's SVG census reports a real number, never 0; its DOT gate unmoved | **DONE — all three re-captured (1,086 fixtures), exit bar met on every axis.** Censuses stop reporting artifacts: **class 2 → 343/722, state 0 → 58/271, usecase 2 → 25/93** (deterministic pass). **DOT gates unmoved**, which was the whole risk: class 688/711 with an identical breakdown (7 oracle-blind · 22 `portOk` · 1 `directionOk`), state 267/267, usecase 93/93 — because only `in.svg` moved; **zero `.dot` files changed**, verified by `git status` over the whole cache. Staleness was re-proved by BYTES per type before touching anything, not taken from the git dates: `git merge-base --is-ancestor` places the class cache's `9b54513f` **54 minutes before** the SVG-reduction merge `7c30f8ae`. The D4 freshness guard is widened from one type to all 8 cached types in ONE jar start (2.4s), and its failure path was DEMONSTRATED, not assumed — corrupting one class sentinel fails that type by name while the other 7 pass. `dot` is deliberately excluded with its evidence: `@startdot` shells out to the HOST graphviz, so its cache differs on the `<!-- Generated by graphviz version -->` line alone (`15.1.0` vs `15.1.1`) and a byte guard there would report the local binary's version. Four gates green (574 files / 12,772 tests). Findings in `.agent-notes/si16-oracle-cache-recapture.md` — read the `-o` one before any future capture. **NOT included, tracked as SI19:** regenerating `parity-{class,state}.json` / `parity.json`, whose verdicts were computed against the same stale oracle. Superseded context: **Their SVG censuses are reporting an artifact, not a measurement.** `class` reads 0 zero-diff of 721 because `test-results/dot-cache/class` was last written by `9b54513f` (2026-08-08), which PREDATES the SVG-reduction merge `7c30f8ae` — the same mechanism object-close T1 fixed for `object` (23/80, not 0/80). A gate that cannot detect its own stale input is not a gate (object-close D4). Deliberately NOT done inside object-close: re-capturing another type's cache there would move G2/G4 frozen counts mid-mission (its stop condition 2). Pair with D4's freshness guard so this cannot silently recur. |
| SI17 | port the `RECTANGLE_HTML_FOR_PORTS` row-port node emission for the CLASS corpus | **done 2026-08-12** | shares a mechanism with object-close batch-2 B1 (M1+M4) | half MET, half **FALSIFIED**: `oracle/goldens/class/port-backlog.json` is empty and deleted ✅; "class DOT back to 711/711" ❌ — the mission proved 711/711 is **not reachable by this mission alone** and closed at **710/711**, naming `besepi-37-rori892` as the single outstanding cause | **DONE — class DOT 688/711 → 710/711, `portOk` 22 → 0.** Branch `feat/si17-class-row-ports`; brief `plans/si17-class-row-ports/`; per-item mechanisms in its `ledger.md`, full evidence in its `decision-journal.md`. Commits: `f25cce2c` (T0) · `f73736f6` (T1) · `5e074b8f` (T2) · `2e700181` (T3) · `4051eeb0` (B1) · `7fccbef5` (B2). **The 711/711 exit bar was not rewritten, it was disproven** — ADR-6's arithmetic: the denominator 711 breaks down as 688 EQUAL + 22 `portOk` + 1 `directionOk`, and the 7 oracle-blind (`!pragma layout smetana|elk`, the jar dumps no DOT) are already *inside* the EQUAL count, so they are neither a debt nor a subtraction. Closing all 22 therefore lands at **710/711**; the 711th is `besepi-37-rori892`, which fails `directionOk`, not `portOk`, and belongs to **object-close B33's remainder**. It is the single outstanding cause and it is named, not hand-waved. **Mechanism.** ADR-1 (deliberately unresolved at plan time) was settled by measurement against jar oracles, not argument: the bands come from upstream's live block tree — `EntityImageClass#getPorts` (`svek/image/EntityImageClass.java:247-253`) → `TextBlockVertical`/`TextBlockLineBefore`/`TextBlockMarged` → `MethodsOrFieldsArea#getPorts` (`cucadiagram/MethodsOrFieldsArea.java:194-211`), i.e. `position = headerHeight + 4 + Σ(prior member heights in this compartment)`. The flat-sizer alternative (`mapPortRows`' recipe) missed on every banded control and cannot express one: for a class, `MeasuredClassifier.dividerYs` is the *compartment* separator list, not one entry per row. Zero fitted constants; the discriminating fields+methods control had to be **authored** and jar-rendered because no backlog slug had one. T1 landed the pure producer (`class-port-rows.ts`), T2 wired it and flipped the node to `RECTANGLE_HTML_FOR_PORTS`, T3 re-measured (706/711) and diagnosed the residue, B1 and B2 closed it. **Two decisions flagged for maintainer review — read them before merging.** (1) **Two write-set expansions**, both crossing the brief's own stop condition, deliberately and on the record: T2 gained `class-layout-generic-classifier.ts` (publish-only — the unique site where `headerRowHeight` and the per-member heights are simultaneously live, and `MeasuredClassifier` published none of them); B2 gained `class-classifier-ast.ts` + `class-port-rows.ts` (a persistent `Classifier.portShortNames` registry mirroring `abel/Entity.java:112,538`, because subsumption splices the originating relationship out of `ast.relationships` before render time). (2) **A frozen count MOVED: object DOT 74/80 → 77/80** under B1. `SvekNode#isShielded` (`svek/SvekNode.java:383-396`) is type-independent, so fixing the `:h` fall-through at its origin (`src/core/svek-dot-emit.ts:146`) necessarily fixed object too; jar-verified (`guzojo-14-muxa584`'s oracle emits a bare `sh0007`) and the genuinely-shielded control `fonulu-92-libi014` stayed EQUAL, proving the `:h` path was gated rather than disabled. Three object pins were deleted accordingly. **If the ruling is that 74/80 was inviolable, the remedy is to revise the frozen table, not to re-break object.** **Also corrected in the record:** the brief froze the description SVG census at a "48-set intact", which is a pre-SI16 figure (`48/355`, from `plans/g4-state-svg/ledger.md`); the real post-SI16 baseline is **26/358**, established by a transitive import-closure check (none of the changed files appears in the description engine's 481-module closure) after a `git worktree` baseline failed 358-of-358 on missing gitignored assets. Class census **343**/722 and object **35**/80 both intact. **Follow-up:** `rozuxo-44-fudi093` is object's own missing row-port producer — the object-corpus equivalent of this mission — and needs its own mission; it correctly did not close here. Also filed: `sametail` is unchecked by `compareStructural`, so a real `pijiju-95-xexi872` divergence is invisible to every gate (`.agent-notes/si17-sametail-gate-blindness.md`). **Original filing, retained:** Fallout of making the DOT gate honest (2026-08-11), not a new defect. The gate discarded edge endpoint ports, so 22 class fixtures scored structurally EQUAL while anchoring every edge to the whole node (`:h`) where upstream anchors to a specific member ROW (`shNNNN:p<md5>`, `svek/SvekNode.java:269-311` + `svek/Ports.java:53-55`). Making it port-aware moved class 711 -> 689. The 22 were pinned in `port-backlog.json`, which was NOT an exemption: the suite asserted `portOk` was their ONLY failing check. Same mechanism as object's M1+M4. Slugs: `bicabi-42-coto932`, `cidepu-54-bemo048`, `dekaba-54-fafi485`, `garizu-98-nixo496`, `gekope-01-ricu859`, `gojofu-46-xaci340`, `juxora-90-fisu720`, `kicolo-81-sidi387`, `kidugi-68-noje040`, `kuxosa-67-keko885`, `minuko-19-pobo264`, `monoda-73-guto455`, `mulafo-23-tove961`, `nenepe-70-keri784`, `pajoka-72-reju527`, `paroxa-83-lofa387`, `pegeso-72-mana305`, `pijiju-95-xexi872`, `refeku-65-gapu585`, `rocere-18-faza042`, `sijisi-94-ripu606`, `xefeme-77-fagu709` — all 22 closed. |
| SI18 | `b35-body-width-ink` — classifier ink max-X is `max(x+w-1, x+bodyWidth)` | **done 2026-08-11** | — | `jocamu-71-nuvo330` reaches 0 diffs; both authored jar controls still reproduce (body-driven 269, title-driven 289); object census non-dropping; any class movement stated with its cause | **DONE — landed `1d574e41` (object-close mission line, merged `bab3b223`). This row read `todo` / "no code has been written" until 2026-08-12; corrected here.** The mechanism shipped CORRECTED and NARROWER than the brief: the `+1` is a real `UEmpty`, but sized to the BODY, not the box — `TextBlockMarged#drawU`'s `ug.draw(UEmpty.create(dim))` (`klimt/shape/TextBlockMarged.java:83`) is the only `UEmpty` draw site in the whole upstream tree, and `LimitFinder#drawEmpty:159-162` bounds it with no inset. The brief's "corpus-wide, expect class movement" scope claim is **FALSE**: `HeaderLayout#drawU:89-109` centers a class name in a `suppWith` that is exactly 0 when the header drove the width, so the fixed rule was already right there — all 317 byte-exact class goldens held unchanged. `jocamu-71-nuvo330` reached 0 diffs; object census 34 → 35. Ledger row: `plans/object-close/ledger.md` B35. Original brief, retained for its jar controls: **Diagnosis COMPLETE and jar-verified — brief is `plans/b35-body-width-ink/README.md`.** A classifier's body block is drawn at the box's left edge with its OWN width, so it reaches the box's right edge only when the body SET the width (`EntityImageObject.java:150-153`'s `max(dimFields, dimTitle + 2*xMarginCircle)`); a title-driven box falls back to `LimitFinder#drawRectangle`'s own `-1` inset (`LimitFinder.java:184-188`). This is the `+1` that `class-ink-box.ts#addRectInk` has attributed to an invisible `UEmpty` reservation since G2 N5 — object-close B5 proved `UEmpty` is drawn nowhere on this path but could not name the real source. Deferred out of `direction-close` T7 only because `addRectInk` serves EVERY class/object/map/json leaf, so it needs its own iteration and a full class re-measure — not because anything is unknown. |
| SI19 | regenerate the parity surveys against the fresh oracle — and raise the survey's own timeout | **done 2026-08-12** | SI16 (done) | `parity-class.json`, `parity-state.json` and `parity.json` carry a `generatedAt` after 2026-08-12 with zero `timeout` verdicts; the class ratchet's AC3 (`dotEqual=true` per manifest slug) still passes | **DONE.** All three regenerated against the fresh oracle, **zero `timeout` verdicts in any of them**, four gates green (574 files / 12,772 tests). Verdicts, before → after: class **328/91/302 → 341/89/292** (conformant/structural-match/diverged), state **52/7/212 → 58/8/205**, component+usecase **2/2/353 → 2/3/352** (+1 `oracle-error`, unchanged). `parity-class.json` also went **721 → 722 rows**: the fixture `0db7ca40` added had never been surveyed. **State the modesty of this honestly** — the fresh oracle did NOT move these the way it moved the censuses (class 2 → 343/722), and that is expected, not a disappointment: the survey renders through `renderSync` + `WidthTableMeasurer` while the census uses `renderFixture*` + `DeterministicMeasurer`, and this file's own header says description verdicts are `diverged` by construction pre-cutover. The survey's verdict is a candidate signal, never the AC1 measurement. **Root cause of the timeout defect, since a raised number without one would be fitting:** the budget must cover process START, and a spawned worker spends **~10.5s importing `src/index.js`** through jiti — measured directly, with the render and every other import in the tens of ms, and `JITI_FS_CACHE=true` changing nothing. `src` simply outgrew the 10s default. Cheaper fixes were checked and rejected on evidence, not taste: no `tsx` in `node_modules` (the script deliberately avoids installing), and batching fixtures per worker is a harness redesign that CLAUDE.md puts on the YAGNI side. Default raised to 60s with the measurement in the comment. Original filing: **Split out of SI16, which fixed the caches but not the files derived from them.** The three parity files were surveyed against the SAME stale oracle SI16 replaced (`parity-state.json` is from 2026-07-21), so their verdicts are artifacts by the same mechanism — the census numbers moved enormously once the cache was fresh, and these did not move at all. **A second, INDEPENDENT defect blocks the regeneration and is the real work here:** the survey's own `RENDER_TIMEOUT_MS` default of 10s (`scripts/svg-parity-survey.ts:57`) is no longer survivable on this tree. Each fixture is rendered in a spawned `jiti` subprocess that re-transpiles `src` every time, measured at **11.8s for a single `--render-one`** — so a full run returns nearly all `timeout`. Measured, not inferred: a first attempt wrote `parity-state.json` with `{timeout: 270, diverged: 1}` of 271 and was reverted; the discriminating experiment is the 3-fixture `hcl` type, which gives **2 timeouts of 3 at the 10s default and 0 of 3 at 60s**, with real verdicts. So the regeneration needs `SVG_PARITY_TIMEOUT_MS` raised (60s clears it with headroom) — and the default itself should be raised or the per-spawn transpile cached, since a harness whose default setting silently converts every measurement into `timeout` is the same class of defect as the stale cache. Budget ~1,351 fixtures × ~12s; use `--out` per type and NEVER a bare shared-file write (it truncates the other types' rows). |
| SI20 | port the `RECTANGLE_HTML_FOR_PORTS` row-port emission for the OBJECT corpus | **done 2026-08-12** | SI17 (done) | MET: `oracle/goldens/object/port-backlog.json` is empty and deleted ✅; object DOT reaches **78/80** with every remaining miss carrying a named mechanism ✅ — and 78/80 was the stated ceiling, not a shortfall, so nothing was redefined to make it look met | **DONE — object DOT 77/80 → 78/80, object `portOk` 1 → 0.** Branch `feat/si20-object-row-ports`; brief `plans/si20-object-row-ports/`; per-task mechanisms in its `ledger.md`, full evidence in its `decision-journal.md`. Commits: `c28a9cca` (T0) · `c5be12be` (S1) · `3936ddb5` (S2) · `750387f7` (T1) · `62a356ca` (T2) · `83bc0e98` (T3) · `af82b0ee` (comment repoint). The object-corpus twin of SI17, deliberately left open there because closing it from SI17's change would have been a result without a mechanism. **The mission's own premise was wrong, and that is the finding.** SI17's follow-up note called this "scoped by precedent rather than by discovery"; the band frame transferred, the route and the files did not. An object body is `BodyEnhanced1` (`cucadiagram/BodierLikeClassOrObject.java:225-233` → `BodyFactory.java:71`), and its margin of 4 comes from `BodyEnhancedAbstract#decorate:111-113` — **not** from the class path's `MethodsOrFieldsArea#asBlockMemberImpl`, which an object leaf never reaches (`:234-235` asserts `type.isLikeClass()`). The two constructions coincide at 4 independently, so copying the class one gets the right number for the wrong reason. **ADR-1 resolved by measurement, zero fitted constants:** `position = H + 4 + Σ(prior member heights)` with `H = getNameAndSteretypeDimension().getHeight()` = our `title.height` (18 plain, 30 stereotyped). `rozuxo-44-fudi093` **cannot** decide it — its two equations subtract to `14 = 14`, pinning only the sum `H + m = 22` — so a stereotyped control had to be authored and jar-rendered. Option B (`m = 0`) was refuted by **absent markup**, not out-scored: `SvekNode#appendTr` drops rows of height `<= 0` (`svek/SvekNode.java:298-311`), so B predicts no trailer `<TR>` and the jar emits one at `HEIGHT="4"`. Generalized in `.agent-notes/si20-underdetermined-sum-fixture.md`. **Two things flagged for maintainer review.** (1) **A write-set expansion in T2**, deliberate and on the record: the `MinimumWidth` suppression landed in `class-object-map-sizing.ts` + `class-object-sizing.ts`, outside T2's declared write-set, because it needs `Theme` and `applyShapeAndPorts` is at the hook-enforced 5-parameter cap — threading it would have forced an edit to `class-dot-graph.ts`, an explicit stop condition in this brief. SI17's T2 made the identical call (`5e074b8f`). The behavior is upstream's own: `TextBlockMinWidth` does not implement `WithPorts` (`klimt/shape/TextBlockMinWidth.java:45`), so a `MinimumWidth > 0` object emits the flipped shape with a row carrying no `PORT=` while the edge still names the port ids — a suppression to reproduce, not a hazard to guard against; class has no analogue. (2) **A defect T2 found in its own wiring**, which is ADR-2's predicted silent drift firing in code: the election text was rebuilt with the **class** reconstructor (`formatMemberText`) for object leaves, which disagrees with `formatObjectMemberText` on `\t` and `=` handling; `getScore`'s `\bshortName\b` tier is sensitive to exactly that, dropping 100 → 50, and `Ports#add`'s strictly-greater replacement then hands the band to the wrong row. **No gate could see it** — `rozuxo`'s members are bare words that both reconstructors render identically — so every count stayed green while the code was wrong. Fixed by `electionTextFor(kind)` with a discriminating unit control. **No frozen count moved except object `portOk` shrinking.** Class DOT — this mission's named risk, since movement there would undo SI17 — is **unmoved at 710 with `portOk` 0**, its sole failure still `besepi-37-rori892`/`directionOk`; component 262/262, usecase 93/93, state 267/267, censuses 343/722, 35/80, 26/358 all unmoved; `map`/`json` untouched per ADR-4. **The remaining 2/80, named:** `zicope-62-pica490` and `zuvila-56-nuda425` are `no-candidate` — we feed nothing — both `!procedure`-generated `map` bodies with embedded `{{ }}` arrow legends, a separate mechanism this mission did not touch. **Correction to ADR-6's arithmetic, measured at close-out:** the one oracle-blind fixture (`robitu-34-vupe367`, `!pragma layout elk`) is **not** "already inside the 78" — `buildAgg` `continue`s before `analyzeFixture` (`scripts/dot-sync-report.ts:265-266`), so it is excluded from the denominator: 81 CLASS-tagged = 1 oracle-blind + 80 comparable = 78 EQUAL + 2 no-candidate. Headline unaffected, composition claim wrong; the same phrasing in SI17's row deserves the same check (`.agent-notes/si20-oracle-blind-is-outside-the-comparable-set.md`). **Follow-up filed, not fixed:** `formatObjectMemberText` normalizes the `=` separator (`a=1` → `"a = 1"` where `getDisplay(false)` gives `"a=1"`); invisible to every gate because `DeterministicMeasurer` measures a space as width 0, and election-neutral. A one-field fix that needs its own SVG-census measurement — a backlog item with a named mechanism, **not** a divergence, so no `DIVERGENCES.md` entry was added by this mission. **(Superseded: closed by SI21 — `9a698e18`. The fix carries `typeSeparator` through `tryStructuredObjectMember`; the oracle fixture authored for it turned out NOT to guard the behavior, verified by reverting.)** Four gates green: 12,811 tests / 1 todo, typecheck · lint · build rc=0. |
| SI21 | SI20 follow-through — code review, toolchain, and the licensing ruling | **done 2026-08-13** | SI20 (done) | MET: every finding from the SI20 code review closed or superseded ✅; `npm audit` **20 → 0** ✅; every frozen count unmoved ✅ | **DONE — 14 merges on `main`, `4d1befe0`..`023abac2`, all `--no-ff` so per-task commit ids stay citable.** A `/code-review` pass over the SI20 diff (11 dimensions; two agents returned empty and were retried, so no dimension went unreviewed) produced 3 warnings and 10 suggestions, all now closed — tracked in `code-review-tasks.md`, which ends at zero open items. **The theme worth carrying: four separate defects were invisible to a green gate.** (1) `@microsoft/api-extractor` became a PEER of `vite-plugin-dts` 5; without it `npm run build` **exits 0** while printing "skip bundle declaration files", leaving `dist/plantuml-ts.d.ts` a **41-byte stub** beside 972 loose files — a silently broken `types` entry for every consumer. Adding it restored the bundled declaration to 107,594 bytes, matching pre-upgrade. (2) `@knowvah/dot-engine` 1.2.7 broke 14 description tests, and the cause was a **text-format coupling, not geometry**: `frontier-shadow-layout.ts` scraped `bb=`/`pos=` out of `render(graph,'xdot')` with a `[^\n]*` capture, and 1.2.x wraps node attributes one per line — so the regex matched nothing, the layout threw, and the ratchet compared a PlantUML **error diagram** against its oracle. (3) The separator oracle fixture the review asked for **does not guard the separator**: with the fix reverted it still passes, because the ratchet compares structure and geometry and a zero-width space changes neither — verified by reverting, and recorded in `class-object-separator-spacing.test.ts`'s header so nobody inherits a false sense of coverage. (4) Two ratchet files timed out twice on the same fixtures, always a timeout rather than a diff, once on a docs-only commit; `sprite-svg-bootstrap-0` measures **1642ms in isolation**, so they are not slow tests but the slowest tests under worker contention — raised to 30s per-file with `vi.setConfig`, whose effect was **proven by forcing 1ms and watching 49 tests fail** rather than assumed. **Toolchain (`a8c76e30`, `e2feb98a`, `8b9eae9a`):** vitest 1.6.1 → 4.1.10, vite 5.4.21 → **7, not 8** — 6 does not clear the advisory (`≤6.4.2`), vitest 4 peers on `^6\|\|^7\|\|^8`, and 8 swaps Rollup/esbuild for Rolldown/Oxc, changing the bundler behind the published artifact while `vite-plugin-dts` still peers on `rollup >=3`; vite-plugin-dts 3 → 5; vitepress **could not be upgraded** (1.6.4 IS latest stable, which is why audit reported no fix) so its subtree is overridden to vite 7 + plugin-vue 6, verified by building the docs site. `@knowvah/dot-engine` 1.0.0 → 1.2.7. Breaking changes handled: `coverage.all` removed (its `false` value is the new default), `vi.fn`'s type parameter is now the whole signature, `rollupTypes` → `bundleTypes`. **`dist/` stopped inlining its runtime dependencies (`e2feb98a`):** `@knowvah/dot-engine` (EPL-2.0) and `jsonc-parser` joined `katex` in `rollupOptions.external`, so the package now DEPENDS ON rather than REDISTRIBUTES the EPL component — ESM **2.60 → 1.52 MB**, tarball 26.2 → 17.9 MB, both entries smoke-tested against the built output. A `NOTICE` records all three and ships in the tarball (npm auto-includes `LICENSE`, not `NOTICE`). **Licensing resolved as `planning/adr/ADR-002-mit-with-epl-dependency.md` (`cdbdf664`):** stay MIT. EPL-2.0's copyleft is file-level and reaches a larger work only when that work carries the covered files. Upstream PlantUML is the precedent and its position is HARDER — multi-licensed with MIT among the options, `@plantuml/core` shipped on npm under MIT, from a codebase that BUNDLES Smetana (transpiled Graphviz, EPL lineage). The ADR names the one change that would invalidate it: re-inlining the engine. **Structural work:** three headroom splits — `class-dot-graph.ts` 498 → 373 (`class-dot-edges.ts`), `class-port-rows.ts` 495 → 448 (`class-map-port-rows.ts`, seam pre-drawn by ADR-4), `class-object-sizing.ts` 495 → 289 (`class-object-fields.ts`); `classPortShortNamesById` from O(classifiers × relationships) to one pass, a path SI20 made newly live for object diagrams; `electionTextFor`'s coupling to `isRowPortKind` pinned by a fitness test that fails **both** ways (a `Record<ClassifierKind, true>` breaks compilation if the union grows, the pinned set fails if the predicate widens), with no defensive code added to the production path. **The rule this pass leaves behind, and it changed the outcome:** text parsing that PORTS UPSTREAM stays and gets validated against the Java; text parsing that is OUR OWN scaffolding around a dependency gap goes. `frontier-shadow-layout.ts`'s xdot scraping had no upstream counterpart (upstream parses graphviz's **SVG**, not xdot) and was replaced by `getLayout()`'s typed `clusters[]`, verified byte-identical on the gafegu-06 shape before switching. `graph-layout.ts`'s `points=` extraction IS the port of `SvgResult#extractList` (`svek/SvgResult.java:52,67-77`) and **stays**, now audited: upstream's `" MC"` `StringTokenizer` delimiter set matters only for path `d=` data this port does not parse, so whitespace splitting is equivalent for `points=`, not a shortcut. The same distinction decided the complexity work — `parseObjectMatch` (our own regex-group adapter) simplified CCN **10 → 2**, while `isBlockSeparatorLine` (line-for-line port of `BodyEnhancedAbstract#isBlockSeparator:67-82`) was marked `#lizard forgives` instead, the treatment this repo already applies 119 times. **Naming:** 880 bare `graphviz-ts` mentions renamed to `dot-engine` (`33bcc915`), 33 dead `~/git/graphviz-ts` paths repointed after the checkout moved (`db4bf0f4`), and the 8 remaining version pins retired once the old package was **unpublished from npm** (`404 Unpublished 2026-08-13T01:21:45Z`) — the argument for keeping them was that they resolved, which stopped being true mid-session; the engine repo's tags start at `v1.2.3`, so those pre-1.x builds are gone from git too. Five mentions deliberately remain, two of them recording the rename transition itself. `DIVERGENCES.md`'s elk entry re-grounded on the one-engine ruling (`dbdc295d`), dropping a licensing rationale whose premise — "the first non-permissive dependency in a tree that is otherwise MIT" — was already false. **No frozen count moved all session:** class DOT 710 `portOk` 0, object 78/80, component 262/262, usecase 93/93, state 267/267; censuses 343/722, 35/80, 26/358. Four gates green: **12,826 tests** / 1 todo, typecheck · lint · build rc=0, `npm audit` 0. |
| SI22 | `edge-label-box-and-class-ports` — edge-label reserved box + class-side ports + per-end port labels, and the D7 comparator widening | **closed 2026-08-15** | — | clauses 3/4/5 MET (`sokevu` rank-chain+`constraint=false` with `graph-layout-build-constraint.ts` untouched ✅; `tobuka-93-jale775` 41 → 0 byte-exact ✅; every miss named ✅); clauses 1/2 **UNMET and left so** (`class-inheritance-interface-assoc` at 13, its own watch-out's prediction, ratchet pins zero only; `jecici` 138 vs ≤133 — label boxes byte-exact, residue elsewhere) | **CLOSED — brief `plans/edge-label-box-and-class-ports/`, branch `feat/edge-label-box-closeout` (merge commit).** Batch 1 (creole-block label measurement + `arrowFontSize`), batch 2 T5/T6 (FIXEDSIZE box to the engine, class + description), batch 4 (`manageCollision` port, `b24f6038`) are this mission's; batch 3's mechanism landed under SI17 and was only PINNED here (`225107c0`). **The durable change is D7 (`d3ff29be`): `tests/oracle/svek-dot.ts#labelSizeOk` asserts every edge label's `WIDTHxHEIGHT` (label/tail/head/xlabel), folded into `structurallyEqual`; +1px on our box fails 228 pinned fixtures.** It surfaced 51 goldens EQUAL with a wrong box → per-type `label-size-backlog.json` (description 10, class 29, object 2, state 9 slugs), direction-backlog contract, shrink-only, contract shared in `tests/oracle/dot-parity-backlogs.ts`. **Corpus EQUAL re-baselined, honestly: component 263 → 257 (98%), usecase 93 → 88 (95%), class 710 → 680 (96%), object 78 → 76 (95%), state 268 → 259 (97%) — every drop is `labelSizeOk`.** The 51-entry backlog is the follow-on work queue (shapes triaged in the journal 2026-08-15: multi-line label measured as one line, `<<stereotype>>` head boxes, a `T19x13` tail in the givoli family, state note-on-link merged box). 14,307 tests, coverage 95.3/90.3/96.9. |
| SI23 | `edge-label-box-backlog` — work SI22's 50-slug `label-size-backlog` queue as four mechanisms | **closed 2026-08-16** | SI22 (done) | **3 of 5 MET.** Clause 2 (every residual slug named or explicitly undiagnosed) ✅ · clause 4 (no fixture rises) ✅ — **zero regressed, 24 improved** · clause 5 (four gates) ✅. Clause 1 (≤ 12 slugs) **UNMET at 22** and left so. Clause 3 (per-type DOT targets) **UNMET except object**, which reached 78/78. | **CLOSED — brief `plans/edge-label-box-backlog/`, branch `feat/edge-label-box-backlog` (merge commit; per-task ids are cited throughout its journal).** All four mechanisms delivered. **DOT EQUAL: class 680 → 699, state 259 → 266, usecase 88 → 89, object 76 → 78 (target met), component 257 → 257.** Census 779 → **783** doc-size-exact, 25975 → **26206** matched-shapes. Backlogs **50 → 22** (class 29→11, description 10→9, state 9→2, object 2→**0**). 594 files / 14,426 tests. **M1** — quantifier boxes were measured with the arrow label font in one `measurer.measure` call; now `computeQuantifierBox` at a real cardinality cascade (`GraphvizImageBuilder.java:124-126`) with `\n` splitting and, per `SvekEdge.java:447-467`, **no shield and no `marginLabel`**. `camuna-58-veca254`'s heads are exact (`23x10`, `41x20`). **M2** — `computeMergedLabelBox` ports `mergeLR`/`mergeTB` from `XDimension2D.java:94-98,108-112` (read, not inferred from the names, per D2) plus shield and half-width; `lozego-15-coci435` is exact at **137x135**, including its `<$test>` sprite. **M3** — not a sizing defect at all: upstream inverts entities and sided fields together (`LinkArg.java:115-117`) while this port normalized by arrowhead and left the quantifiers bound to the swapped ends (`class-dot-edges.ts`). Jar-verified WIDER than its four fixtures — `<\|--`, `<..`, `<\|..`, `<--*` all failed, `<-left-` inverted — and it moved rendered SVG through a second consumer at `class-edge-label-anchor.ts`. **M4** — the brief's "few-px width deltas on 4 slugs" was **false**; T4 established three independent sub-mechanisms over ~13 slugs with 22/22 widths reproduced exactly and a controlled experiment (`bugeli` = `gixesa` + `classAttributeIconSize 0`) isolating the gate: (A+B) strip the visibility char and reserve its **12px** icon, (C) `<<x>>` → `«x»` class-only, (D) strip the magic-arrow token and reserve a **13px** triangle — the `.80` at `TextBlockArrow2.java:65` being draw-only, and a fitting hazard the tasks were warned off. **Two maintainer-approved plan changes:** T12 split into T12a/b/c (the mis-scope stop condition), and **T14 added** after T6 and T7 both hit the same write-set escape and left T1's cardinality cascade with **zero callers** — D3 was half-delivered until it landed. **The generalisable finding, and it cost a caught regression:** a measurement fix is not finished until you check whether the same string is also **drawn**. T12b rewrote guillemets for measurement only; the DOT box became correct for `«alias»` while the glyphs stayed `<<alias>>`, and `tebore` regressed 15/15 → 14/15. The agent attributed it to the accepted dot-engine geometry delta — **wrong, that ruling covers Smetana paths and class shells out to real graphviz** — and it was caught by dumping shapes, not by trusting the attribution. The same question had **three different correct answers** across T12a (leave rendered text alone — this port draws no visibility icon), T12b (rewrite it) and T12c (rewrite for class, not description). **Why clause 1 was missed, measured not excused:** the bar assumed four mechanisms covered the 50, and they do not. Of the 22 residual, **9** belong to mechanisms the brief never named (`<latex>` sizing ×2, `skinparam maxMessageSize`, `<style> arrow { FontSize }` on the **main** label ×2, a `!define` macro, an `AssociationClass` text route ×2, an embedded `{{ }}` sub-diagram), **4** are M2 in the **description** engine which Batch 5 never scoped a task for, **3** need a real creole `TextBlock` or a per-line cause-D extension, and **2** are explicitly **undiagnosed** (`vuresa-33-kumu160`, `ticuxa-26-tixo262`) with their ruled-out observations attached. Full residue table with per-slug mechanisms in the brief's README close-out. **Diagnosed at close-out, unfixed:** `focaci-80-suzu938` — quantifier labels take cause A's strip but **not** cause B's icon, confirmed arithmetically (`61.1` → strip `~` → `53.46` → **53** = oracle). **Follow-ons filed:** description-M2 wiring (4 slugs, cheapest win — note T9's trap, the correct sizer is `ComponentRoseNote` at `pureText + 31`, **not** the `Opale`-based `measureNote` at `+21`); cause A for quantifiers; cause D per-line; the arrow-FontSize main-label cascade. **Two corrections paid:** the 2026-08-15 shape survey read `lozego` as "multi-line as one line" (it is M2) and the `givoli` family as a tail-box size delta (it is M3) — both wrong, both fixed. **Also flagged unowned:** `.claude/catalog.md` **does not exist** though `CLAUDE.md` directs every agent to check it before implementing, and `.claude/` is gitignored so nothing written there would commit; the mission's new public surface is recorded in the brief instead. `hasMiddleDecor` is categorically dead code (no `LinkMiddleDecor` concept in this port) — `DIVERGENCES.md` candidate, kept as faithful upstream arithmetic. |
| SI24 | `edge-label-box-followups` — work SI23's residue: ten slugs over five cheap mechanisms plus the two formerly-undiagnosed | **closed 2026-08-16** | SI23 (done) | **5 of 5 MET.** Clause 1 (backlogs 22 → ≤ 13) ✅ at **11** · clause 2 (every residual slug named) ✅ zero undiagnosed · clause 3 (DOT EQUAL non-decreasing per type) ✅ · clause 4 (no fixture rises) ✅ **zero regressed, 6 improved** · clause 5 (four gates) ✅. | **CLOSED — brief `plans/edge-label-box-followups/`, branch `feat/edge-label-box-followups` (merge commit; per-task ids cited in the journal).** Measured at `cb4de5c7`: **DOT EQUAL class 699 → 705, state 266, component 257 → 259, usecase 89 → 92, object 78.** Census 783 → **785** doc-size-exact, 26,206 → **26,255** matched-shapes. Backlogs **22 → 11** (class 11→5, description 9→4, state 2, object 0). 595 files / **14,492** tests, coverage 95.37/90.35/96.93/96.46. **T1** quantifier strip — the brief's `Display` visibility-strip mechanism was WRONG: `manageGuillemet(true)` has one caller (`LinkArg.java:71`, main label); the real strip is `CharHidder.hide` (`utils/CharHidder.java:59-90`, every creole line via `StripeSimple.java:150`) — same 53 for `focaci`, different for `+`/`-`/`#` prefixes (solo oracle renders 56/62 unstripped); the agent disproved the brief and was right. **T2** arrow-font cascade + `resolveArrowLabelFont` (D3), zero movement, with the `<style> FontSize` merge deliberately withheld because `description/layout.ts` already read the field (stop 8) — lifted by T6. **T3** description note-on-link merged box (D1/D2/D5): position captured, colour not (its only consumer is the polygon fill), `Link#addNote` is a replace (`Link.java:332-334`), `roseNoteDim` = `+31/+20` in new `src/core/rose-note-dim.ts` (`edge-label-box.ts` at 500/500) — all four slugs exact (fogiku/jafuke 80x33, dikexa 75x48/174x46, zavitu 268/272/393x48). **T4** `hasSeveralGuideLines` has FOUR conditions (`Display.java:729-747`), not two; per-line `TextBlockArrow2` 13×13; `stripCreoleMarkup` in the multi-line branch; stop 10 does NOT fire (`StringBounderFromWidthTable.java:67-69` has no bold branch); vuresa 142→128, gobuco/lapoma 24→29 exact — **but its SVG per-line glyph draw STOPPED** (needs `class-geo-types.ts` `EdgeGeo`, no write-set; human decision). **T5/T6/T7** wire the resolver into measurement AND SVG per engine in one commit each (D4): camuna 29x16 + `font-weight=700`, ticuxa 98x60 Courier/italic, zosuje 5x12/9x12/12x12; state zero movement (59 pins). **New mechanism named:** arrow-label font COLOUR unported (camuna/ticuxa still `fill=#000000`). **Also:** `component/ruciga-77-ruja233` fails `labelSizeOk` unbacklogged (pre-existing; shrink-only forbids adding); description `computeLinkDzeta` still flat-measures the main label (inert on corpus); `shared-seam-extraction` now has four Rose-note copies; `.claude/catalog.md` still absent. |
| SI25 | `class-guide-line-glyphs` — one magic-arrow glyph per guide line of a multi-line class edge label; class main-label ink at the resolved arrow font | **closed 2026-08-17** | SI24 (done) | **4 of 4 MET.** Clause 1 (`gobuco`/`lapoma` carry four glyph `<polygon>`s, right/left/left/right, shape byte-exact) ✅ · clause 2 (every other class fixture byte-identical; `camuna`/`ticuxa`/`nafiki` only toward jar) ✅ **exactly 4 fixtures moved in a 722-fixture SVG dump** · clause 3 (class DOT ≥ 705, ratchet holds, no fixture rises) ✅ · clause 4 (four gates) ✅. | **CLOSED — brief `plans/class-guide-line-glyphs/`, branch `feat/class-guide-line-glyphs` (merge commit; per-task ids `a9e40bc6` T1, `722c2bee` fix(T1), `ca590920` T2, `3fe4aca4` T3 cited in the journal).** Measured at `3fe4aca4`: **DOT EQUAL class 705/711 · state 266 · component 259 · usecase 92 · object 78 (all unchanged).** Census 785 doc-size-exact / 26,255 → **26,256** matched-shapes (`ticuxa` 13/14 → 14/14, `lapoma` doc 68 → 70 vs jar 72; zero fell). Class SVG ratchet 315/315. 595 files / **14,508** tests, coverage 95.37/90.35/96.93/96.46. **T1** `splitGuideLines` — the one per-line walk (`StringWithArrow.java:115-127`, `TextBlockArrow2.java:87-89`) behind `computeGuideLinesBox`, zero movement. **T2** `EdgeGeo.labelLines[i].glyph` placed by `guideLinesAnchor` mirroring `mergeLR`/`mergeTB` (`TextBlockHorizontal.java:79-91`, `TextBlockVertical.java:79-102`; jar's `gobuco` text x 79.68/80.046/81.508 confirm per-line centring); `LinkArrow#mute` read (`abel/LinkArrow.java:55-71`): a multi-guide-line link is `NONE_OR_SEVERAL`, so per-line direction reduces to the whole-label `magicArrowAngle`; D2 threads `resolveArrowLabelFont(theme)` through `EdgeGeoTextContext` (tail/head keep `theme.fontFamily`+13, `portLabelAnchor` now takes a `FontSpec`) — `ticuxa`'s `toto` lands at jar's 39.97/131.111/96.425, `camuna` at jar's 27.213/`.889`. **T3** one `magicArrowPolygon` emitter for whole-label and per-line glyphs, `<polygon>` before its `<text>` per line as jar interleaves. **Flagged:** `fix(T1)` `722c2bee` — `magicArrowGlyphPoints` pinned its ink radius at the 13-based `ARROW_GLYPH_SIZE`; `TextBlockArrow2.java:64-65` derives `(int)(size*.80)` from the resolved font — new `magicArrowTriSize`, byte-identical at 13. **Residual, not chased:** every `gobuco`/`lapoma` glyph and text sits 0.23 px right of jar — the arrowhead's own N25/N62 offset (`111.722` vs `111.72`), which is why `gobuco` stays 14/22 under `POSITION_TOL` 0.05. **Follow-ons named:** description/state per-line glyphs; tail/head cardinality ink font vs `theme.cardinalityFont*` DOT box; `addEdgeTextInk`'s constant-13 ascent; `lapoma` 70 vs 72; arrow-label font colour (from SI24); bare-token guide line has zero corpus reach. |
| SI26 | `arrow-label-font-colour` — colour class/description/state edge-label ink (main label, magic-arrow glyph, cardinality) from the arrow style's `FontColor`, as the jar's `labelFont`/`cardinalityFont` `FontConfiguration`s carry it | **closed 2026-08-17** | SI25 (done) | **3 of 4 MET as written.** Clause 1 (12 named fixtures draw jar's `fill`; class glyph fill+stroke and cardinality too) ✅ with a stated carve-out (7 labels the jar colours per-tag/positionally, unported) · clause 2 (every other fixture byte-identical) **✗ literally** — 97 moved, all `<text fill>` only, all understood: 80 state fixtures `#181818 → #000` = jar (the brief's "state byte-identical" premise was wrong), `lurevi`/`banatu`/`golati`/`kokodo`/`koxeca` unnamed-but-in-scope = jar · clause 3 (DOT unchanged ×5, no fixture rises, ratchets hold/re-pin toward jar) ✅ · clause 4 (four gates) ✅. | **CLOSED — brief `plans/arrow-label-font-colour/`, branch `feat/arrow-label-font-colour` (merge commit; per-task ids `437727db` T1, `8204d7d9` T2, `bbef1b5d` T4, `a5e6093e` T5, `13383da6` T3, `756ae57c` fix(T1) cited in the journal).** Measured at `756ae57c`: **DOT EQUAL class 705/711 · state 266 · component 259 · usecase 92 · object 78 (all unchanged)**; census 785 doc-size-exact / 26,256 matched-shapes, per-fixture **0 rows changed**; svg-class ratchet 313 → **314** (`lurevi`), svg-state 59 → **60** (`jomazi`), both newly zero-diff vs jar with parity `firstDiff` = `text/@fill`. 595 files / **14,533** tests (+2 pins), coverage 95.37/90.36/96.93/96.47. **T1** `colors.graph.arrowFontColor` + `theme.cardinalityFontColor` (D1), `arrowfontcolor` handler + `defaultfontcolor`'s second write (D4, `FromSkinparamToStyle.java:149,157,424-429`), `resolveArrowLabelFont` returns `color` (default `#000000`, `plantuml.skin:9` — D3, never `theme.colors.text`), `resolveCardinalityFontColor` (D5, `GraphvizImageBuilder.java:124-126`); zero movement (full dump). **T2** `<style>` `fontcolor` over `ARROW_SNAMES`/`CARDINALITY_SNAMES` through one `cascadeArrowFontColor` (`cascadeFontColorHex` against `base.colors.background` so `#?light:dark` resolves); i/k/l/m/camuna reproduced with no specificity logic (`StyleStorage.java:102-116`). **T3/T4/T5** (parallel sonnet agents) — class label/glyph(fill+stroke, `TextBlockArrow2.java:66-67`)/cardinality, description `arrowLabelFontConfig.color`, state transition fill (was `theme.colors.text`). **Oracle experiments a/glyph:** port = jar byte-for-byte on label, glyph fill+stroke, `card`/`1`/`*`. **`fix(T1)` flagged:** unresolvable tokens (`reddress`'s unexpanded `ARROWFONTCOLOR`) now leave the field unset like `cascadeHex`; jar draws them WHITE (`getColorOrWhite`, experiment `bad`) — named divergence. **Named gaps:** per-tag arrow FontColor ×3 forms (skinparam `Color<<tag>> text:Red`; description `<style> arrow { .tag { FontColor } }` — description reads no `arrowTagCascade` at all; positional `<style>` blocks in description); `skin-loader.ts` expands no `!define` (jar has no `reddress.skin` — no oracle); description quantifier labels never drawn; 236/266 component oracle SVGs are an older jar generation (`fill="#000000"`); `!theme` arrow FontColor unverified; accumulator `Map` first-slot ordering for repeated keys. |

## Phase D — New diagram types (breadth, each with a depth gate from birth)

Build the oracle goldens WHILE building the type, not after. Exit bar for each:
renders + (svek types) 100%-minus-known-divergences oracle EQUAL (ruling 2026-07-14), or (bespoke-layout types) an
SVG-structural bar defined at build time. mission-guide.md has Java sources.

| ID | Mission | Status | Blocked-by | Notes |
|----|---------|--------|-----------|-------|
| D1 | Timing `@starttiming` | todo | SI2 | custom vertical-time layout |
| D2 | Mind Map `@startmindmap` | todo | S3 | twopi engine |
| D3 | WBS `@startwbs` | todo | D2 | shares mind-map layout |
| D4 | Gantt `@startgantt` | todo | SI2 | constraint solver |
| D5 | Network nwdiag/rackdiag | todo | — | row-based layout |
| D6 | Git graph `@startgitgraph` | todo | — | **NOT a lane layout — it is a SMETANA consumer.** `gitlog/SmetanaForGit.java` via `GitDiagram` (`DiagramType.GIT`), a wholly separate Smetana user from the json family. Per the 2026-08-09 maintainer ruling (CLAUDE.md, "Smetana is NOT a porting target"), this type must NOT be held to the jar's geometry: use `@knowvah/dot-engine` and carry the delta as a named entry. Decide what "conformant" means for it BEFORE building, and expect no `svek-N.dot` oracle (no external dot call). |
| D7 | Salt `@startsalt` | todo | SI4? | grid; eval golem |
| D8 | DITAA `@startditaa` | todo | — | ASCII-art grid → SVG; highest P5 complexity |
| D9 | Chen EER `@startchen` | todo | — | dot layout + EER shapes |
| D10 | EBNF `@startebnf` | todo | SI3 | railroad |
| D11 | Regex `@startregex` | todo | D10 | regex → railroad IR |
| D12 | Wire `@startwire` | todo | SI4? | schematic grid |
| D13 | Flow `@startflow` | todo | SI4 | golem; alpha-doc only |
| D14 | DOT passthrough `@startdot` | **done** | — | **DONE 2026-08-08 — 0/5 → 5/5 SVG-conformant, zero diffs, whole known corpus pinned** (`oracle/goldens/svg-dot/`, commits `d590b8ea`+`26c9ed34`). **This row's own premise was wrong and is left here to be read as such:** "needs common/{arrows,shapes,htmltable,labels}.c for attrs" assumed the port must interpret DOT attributes itself. It must not. Upstream's `directdot/PSystemDot#exportDiagramNow` hands the DOT text to graphviz and writes graphviz's bytes to the output stream — the jar's `@startdot` output is graphviz's own SVG writer verbatim (`pt` units, `Times,serif`, negative y, `graph0`/`clust1`/`node1` ids). The work was not porting attribute handling; it was DELETING the re-implementation that projected dot-engine's `parse()` onto a local model, laid it out through the svek seam, and re-drew it with this port's emitters. Replaced by `renderSvg(dotContent, 'dot')`. **Two findings worth carrying forward:** (1) the baseline "6 diffs" was an undercount — root `svg[childCount]` diverged, and `compare.ts` stops recursing on a structural mismatch, so no interior was ever compared; a structural root diff means the real gap is unmeasured. (2) The engine's text measurer is load-bearing on any path where it measures labels itself: default `EstimateTextMeasurer` gives 8/5/4/36/33 diffs, `LutTextMeasurer` gives 0/0/0/0/0 (pinned in `src/core/dot-engine-measurer.ts`). Divergences: title/caption/legend retained (jar errors on them, so zero conformance cost); `skinparam`/`<style>` theming RETIRED as unfaithful (jar output is byte-identical with and without). |
| D15 | C4 | todo | SI5b (done — now unblocked) | macro library over description (10 corpus fixtures; C4-PlantUML is the most likely cleanly-vendorable bundle) |

## Phase G — SVG conformance depth passes ← **THE PRODUCT**

**Why this phase exists (measured 2026-07-12).** Phase A drove *DOT* parity to
97/91/100/98/99.6%. The DOT is an intermediate representation. **The SVG is what
we ship — and it is 1.7% conformant.**

Measured with the deterministic census (`scripts/svg-conformance-census.ts`, the
real metric — the `svg-parity-survey` reports ~0% *by construction* because it
compares production AWT output against a deterministic-mode oracle, the known
D12 gap, and is a triage tool only):

| diffs | fixtures (description, n=355) |
|---|---|
| **0 — conformant** | **6 (1.7%)** |
| 1–3 | 4 |
| 4–10 | 121 |
| 11–30 | 67 |
| 31+ | 156 |

**SVG conformance is NOT gated on S1L sizing** — tested and refuted. 174 fixtures
have provably-correct node sizes (≤0.01in via the oracle's own id-agnostic
`maxSizeDeltaIn`) and produce **zero** conformant SVG, with *more* diffs on
average (64.8) than the size-dirty bucket (54.9). **Every fixture routed through
graphviz→SVG is non-conformant, 100% of the time.** The 6 conformant ones never
reach graphviz. These are independent SVG-assembly bugs.

**Defect queue, ranked by fixture-reach:**
1. **Document dimensions short by exactly 1px** — 327/348 diverging fixtures.
   On the cleanest size-clean fixture it is the *only* defect (jar 190×65, ours
   189×64). This is `docs/svg-conformance.md` F4; needs the `LimitFinder` /
   `UGraphicNo` port. Highest reach in the codebase.
2. **`g[childCount]` mismatch** — 215 fixtures. Structural: we emit a different
   *number* of children. Missing/extra elements, not geometry.
3. **Geometry scatter** — `text@x/y`, `path@d`, `polygon@points`, `rect@x/y`,
   `ellipse@cx/cy`. Deltas scattered (+0.58, −7.29, −29.29, −60.99, −146.0), so
   several distinct bugs, not one global offset.

**Execution rules (maintainer, 2026-07-12):**
- **One diagram type at a time, to conformance, before starting the next.**
- **If the type routes through dot-engine, its DOT conformance must be 100%
  first.** 100% = 100% of *comparable* fixtures; `!pragma layout elk` fixtures
  are excluded (no ELK support — see `DIVERGENCES.md`). `smetana`/`vizjs`
  fixtures are **NOT** excluded: re-capture them with the pragma stripped so the
  jar uses real graphviz and emits `svek-N.dot` (see `DIVERGENCES.md`) — this
  moves ~34 fixtures out of oracle-blind and under the bar.
- Harness already exists (`normalize` / `compare` / `census` / `overlay` /
  `ratchet`) but is **description-only**; generalize it per type. Jar SVG cache
  already covers class (718), component (265), state (265), usecase (90),
  object (80).

| ID | Mission | Status | Blocked-by | Exit bar |
|----|---------|--------|-----------|----------|
| G0 | F4 document-dimension port (`LimitFinder`/`UGraphicNo`) + re-capture smetana/vizjs oracle w/ pragma stripped | done | — | doc dims exact on the size-clean tier; oracle-blind drops 42 → 8. **DONE 2026-07-13** — `plans/g0-limitfinder/` (branch `feat/g0-limitfinder`): `MinMax`/`MinMaxMutable`/`UGraphicNo`/`LimitFinder`/`TextBlockUtils.getMinMax` ported faithfully (every extent quirk pinned); description doc sizing cut over to SvekResult's recipe — the actual 1px was `CucaDiagram.getDefaultMargins` (0,5,5,0), and single-leaf diagrams keep the `EntityImageDegenerated` path; F4 fixtures jar-exact (cifaki 190×65, vapalu 290×65, jesibe 212×65); **census 6 → 12 conformant**. Re-capture: 42 smetana/vizjs fixtures comparable, 41 EQUAL on arrival, 39 pinned in goldens (2 state size-backlogged at 0.0556in); component tojitu-03-ruto643 structural-diff recorded (emits 0 clusters vs oracle 5 — description-DOT-100% queue); component kofovu-01 (jar errors) + potatu-55 (jar routes as CLASS) pre-excluded. **NEW PINNED DOT BASELINE: component 253/262 (97%) · usecase 84/90 (93%) · class 708/708 (100%) · object 78/80 (98%) · state 266/267 (99.6%); oracle-blind = elk-only (class 7, component 1, object 1)**. Gates: 7,929 tests (296 files), coverage ≥98/94/98. Mainframe/BigFrame re-deferred (branch (b)): the ink primitive now exists; residual blocker is threading annotations+styles into the klimt pass (layout.ts geo + plugin contract) — DIVERGENCES.md updated. |
| G0b | **`title` / `header` / `footer` / `legend` are not rendered by ANY engine** (= `docs/svg-conformance.md` F2, "largest bucket") | done | — | a titled diagram renders its title; the ~118 affected gating fixtures become SVG-eligible. **DONE 2026-07-13** — `plans/g0b-annotations/` (branch `feat/g0b-annotations`): ported upstream's `DiagramChromeFactory` (the AnnotatedBuilder/AnnotatedWorker refactor) as `src/core/annotations/` — DisplayPositioned model, the 11 command regexes (matcher position mirrors each upstream factory: FIRST for sequence, FALLBACK for class/state), plantuml.skin style defaults + skinparam/`<style>` overrides, DecorateEntityImage geometry (jar-verified incl. the TextBlockBordered +1 and rx/2 emission quirks). Plugins now return RenderFragment; chrome + svgRoot assemble centrally (mirrors getTextBlock→addChrome→exporter). title/caption/legend/header/footer render for ALL engines; json/dot/chart/yaml bespoke title bands removed; buveco-86-tibo673 renders. Gates: 7,837 tests (294 files, +194), coverage 98.27/94.53/98.39, DOT gate EXACT 251/259, 81/87, 680/680, 78/80, 260/261 (denominators unmoved), census conformant 6 (unchanged; its 7 errors pre-date the mission). Residuals: `mainframe` parsed but not drawn (BigFrame needs the G0 LimitFinder port — DIVERGENCES.md TEMPORARY); annotated-description output loses klimt root attrs (G1 note); D10 corrected — upstream @startdot errors on title (port-only feature, ledgered). | **Confirmed empirically 2026-07-13 (SI7 verification): `renderSync` emits NO title for class, sequence, component, or state — the jar emits one for all four.** `title` is in the ignore-patterns of every parser (`state-commands.ts:96`, `description/parser.ts:379`; the class parser drops it silently — no `title` key in its AST or renderer). **This is invisible to the DOT bar** — the title is drawn outside the graph, so class DOT reads 100% while every titled diagram's SVG is wrong. It is precisely the class of defect Phase G exists to catch. **Reach (gating corpus, 1,418 fixtures): `title` 78 (class 46, state 12, component 10, object 7, usecase 3); `header`/`footer`/`legend` 40 (class 23, usecase 5, object 5, state 4, component 3) — ~118 fixtures, ~8%, that CANNOT be conformant until this lands.** Blocks G1–G4 for those fixtures; do it before the per-type passes rather than ledgering ~8% five times over. |
| G1 | description (component/usecase) SVG conformance | done | G0 (done), DOT 100% (**MET 2026-07-14: component 262/262, usecase 90/90 — literal 100% of comparable after SI5b; G1 IS UNBLOCKED**) | **100% conformant minus known divergences** (every non-conformant fixture carried by a named DIVERGENCES.md/ledger entry — maintainer ruling 2026-07-14; supersedes ≥90%). **DONE 2026-07-15** — `plans/g1-description-svg/` (branch `feat/g1-description-svg`, 21 iterations I0–I10): census 12 → **30/355 byte-conformant**, SVG ratchet 5 → **26 pinned**, errors 8 → 1 (jar-side malformed golden, ledgered); **exit bar MET per the 2026-07-14 ruling: all 325 non-conformant fixtures attributed across 31 named ledger mechanisms (ledger.md § I10 table sums exactly to 325 — zero anonymous misses)**. Mechanisms landed: klimt root-attr reassembly, per-USymbol cluster border/roundCorner defaults, bold group titles + stereotype/arrow-label fonts, edge path-id dedup + decor mirroring, parse-interleaved uid numbering, javaFixed4 %.4f rounding, per-element FontSize cascade, creole text-content escapes, `scale` directive, port labels, multi-stereotype stacking, bracket-body `\n`, transparent elision, link-stereotype discriminator, hide/show family, bracket link modifiers (dashed/dotted/bold/thickness/color), **arrowhead=none through the dot-engine seam (+11 conformant, the largest single fix)**, creole separator `<line>`s. DOT gate frozen exact throughout (262/262 · 90/90 · 708/708 · 78/80 · 267/267); 8,451 tests. **Remaining named residue, reach-descending (README closing summary): ink-extent-margin/FrontierCalculator subsystem ~108 direct + cascades (NEEDS SIGNOFF — cross-cutting every renderer); named-CSS-color HColorSet table ~52; creole char-atom subsystem ~37 (blocked on E2-remainder); I5g childCount 58; chrome sibling-`<g>` nesting 19 (NEEDS MAINTAINER DECISION — all diagram types); rest ≤10 each.** |
| G1b | ink-extent margin + FrontierCalculator subsystem ("match the Java" — maintainer 2026-07-15) | done | G1 (done) | the G1b-attributed fixture set conformant or re-attributed, refreshed residue accounting. **DONE 2026-07-15** — `plans/g1b-ink-extent/` (branch `feat/g1b-ink-extent`, J1–J3): census 30 → **41/355 conformant**, ratchet 26 → **41 pinned** (parity.json staleness resolved via svg:survey). J1: `computeInkShift` (LimitFinder ink walk mirroring `SvekResult#calculateDimension`, dx/dy = 6 − inkMin) replaces the flat `LAYOUT_MARGIN_LEADING=7` — the old margin coincidentally matched rects (6−(−1)) while actors/ellipses drifted; 117 fixtures improved. J2: faithful `FrontierCalculator` + `manageEntryExitPoint` port (insides/points split, graphviz-cluster-rect floor via isolated shadow-graph xdot run, DELTA=18 push); gafegu port rects exact. J3: jar-derived title-label sizing from the literal `label=<TABLE>` in the jar's own dot caches; all 314 non-conformant fixtures attributed across 13 mechanism buckets (ledger § J3). DOT gate frozen exact throughout; 8,494 tests. Named remainders feeding later missions: geometry-cascade 181 (drill via `--families` next), childCount 73, color 23+2 (→G1c), shadow-graph ~8px fidelity gap ×4, mechanism-B mixed-children ×3, mechanism-C ellipse/interface ink sub-case ×4. |
| G1c | named-CSS-color `HColorSet` table port (~150 names; maintainer "do it" 2026-07-15) | done | G1b | the named-color family conformant or re-attributed. **DONE 2026-07-15** — `plans/g1c-hcolorset/` (branch `feat/g1c-hcolorset`, K1): verbatim 154-entry `ColorTrieNode.java` table (147 CSS + 7 Archimate-only names — a generic CSS list would have been wrong) as `klimt/color/{ColorTrieNode,HColorSet}.ts`; all color emission canonicalizes to jar `%02X` hex via the two choke points (paint.ts, svg-graphics-core) + tim color-utils; gradient stop-color + bare-hex closed. Census 41 → **48/355**, ratchet 41 → **48**, 0 regressions (53 improved, per-fixture scan). Ledgered: `?back:fore` scheme form, `automatic` keyword, activity start/stop/end raw-string bypass. DOT frozen exact; 8,536 tests. |
| G1d | unify chrome DOM shape across ALL diagram types (maintainer decision 2026-07-15: unify, not a permanent divergence) | done | E2r (order: G1b→G1c→E2r→G1d) | all engines emit the jar's chrome DOM shape. **DONE 2026-07-15** — `plans/g1d-chrome-dom/` (branch `feat/g1d-chrome-dom`, M1): one top-level content `<g>`, annotations nested in jar stacking order, no transform on annotation groups, coords baked via `coord-shift.ts#shiftFragmentBody` (eager UTranslate equivalent). `svg[childCount]` + `g/@transform` closed corpus-wide; the G0b/T4 divergence note in chrome.ts is resolved-historical. Bonus origin fixes: buildAnnotationBlock double-shift, masked duplicate-PI. Newly named: chrome-block text-styling gap (SansSerif/black/tspan vs jar sans-serif/hex/flat — 9 of 19 fixtures, a G1-I2 extension candidate). Census 48/355 held (bucket moves = childCount-bail unmasking, all traced); ratchet 48. DOT frozen exact; 8,635 tests. |
| G2 | class SVG conformance | done | G1 (done) | 100% conformant minus known divergences (ruling 2026-07-14). **DONE 2026-07-18** — `plans/g2-class-svg/` (branch `feat/g2-class-svg`, 68 iterations N0–N67): corrected baseline 0/718 (N0 census-pipeline bug; oracle cache re-captured N3 under the frozen-DOT go/no-go) → **292/718 byte-conformant**, class ratchet 0 → **292 pinned**; exit bar MET — all 426 non-conformant fixtures named (ledger § N67 breakdown). Landed: document shell, uid scheme w/ phantom burns, box chrome + badges + HeaderLayout centering, the whole note subsystem, creole in members/notes, URL wrapping, folder/rect namespaces w/ ink dispatch, full edge machinery (labels/ids/decor/reversal/manualArrowheads), style cascades (.tagname/snapshots/conditional/monochrome), MaximumWidth wrap, hide/show family. Remaining named residue: **gvts-blocked ~288 (67.6% — five verified dot-engine items w/ repros; getLayout-vs-render splines dominant; library fixes are the largest census unlock anywhere)**; maintainer-scoping: groupInheritance 9, dotted-namespace 13-43, item-20 port-tables 2; mode-dark 1; in-repo singleton tail ~85-128. DOT gate frozen exact + description 48-set intact through all 68 iterations; 9,818 tests. |
| G3 | object SVG conformance | done | G2 (done) | 100% conformant minus known divergences (ruling 2026-07-14). **DONE 2026-07-19** — `plans/g3-object-svg/` (branch `feat/g3-object-svg`, O0–O4): true-zero baseline (the 1/80 artifact had no object entities) → **22/80 byte-conformant**, object ratchet 22 pinned; **80/80 fixtures individually named** (the G1-I10 per-fixture standard). Landed: header centering (PlacementStrategyY1Y2), data-row baselines across object/map/json, empty-body ink, stacked stereotypes, objectDiagram style cascade + nested header{} selector, map/json row dividers, hide-by-kind + hide-stereotypes, tabSize incl. AtomText quirks, enhanced-body-for-object (+ the == double-line fix), strictuml underline, icon fills — several benefiting CLASS too (census 292→294 as side effects). The ~-strip root-caused behaviorally: a cross-cutting creole ~-escape + #-ordered-list gap absent engine-wide (named, feeds any future creole work). Residue: 41 path/@d + 4 node-separation gvts-blocked (same five library items), 3 awaiting-maintainer, the rest named singletons. All four gates held throughout; 9,913 tests. |
| G4 | state SVG conformance | done | G3 (done) | 100% conformant minus known divergences (ruling 2026-07-14). **DONE 2026-07-21** — `plans/g4-state-svg/` (branch `feat/g4-state-svg`, S0–S16): true-zero baseline (all 271 shared one four-part signature) → **52/271 byte-conformant**, state ratchet 52 pinned; **271/271 fixtures individually attributed** (52 pinned + 219 across 36 named mechanisms, 20+ spot-verified). 30 mechanisms landed: document shell + G1d chrome, inline arrowheads, svek ink geometry (the (6,6) INK-minimum moveDelta rule), concurrent regions (zero separator gap per ConcurrentStates.java, dashed separators, per-region pseudo-ids, diagram-global CONC numbering via cpt2), the full cpt1 creation-index uid replay (phantom ticks for -- separators and remove’d entities), transition routing via the class edge machinery (manualArrowheads + buildPathData; the "library spline gap" was OURS), the complete note subsystem (folded-corner path + Opale attached form + GMN dual-tick + #color), o-->/x--> extremities, <<sdlreceive>>, stereotype-scoped Border/Background/FontColor/FontSize, <style> arrow/activityBar cascade. Cross-mission wins en route: dot-engine 0.1.26072013 verified fixing all five filed issues (TRACKER all checked); the layout shape:box hardcode fix (class 294→303). Residue heads: mechanism 16 entity-vs-cluster 92+20 (needs dot-engine cluster-bbox exposure), PARKED edge-label-ink 30+6 (3-strike stop rule; StringMeasurer ~7% calibration gap named), <style> ancestor tier 10, hyperlinks 7, creole family, singletons — follow-up order in the README closing summary. Four gates held throughout; 10,128 tests. |
| G5 | measurer calibration + gvts adoption | done | G4 (done) | Investigate G4 §S13's "~7% StringMeasurer gap"; consume new dot-engine APIs. **DONE 2026-07-21** — `plans/g5-measurer-calibration/` (branch `feat/g5-measurer-calibration`, C0-C8): the gap DISPROVEN (measurer 0.000% mean error on 13,943 corpus samples) — real bug was fontSize 14-vs-13 at five call sites, 3 landed. Landed: calibration harness, DotLayoutResult.clusters seam, setHtmlAttr title-table + wrapper-margin adoption (issues 06/07 filed→fixed→verified→adopted), document order, body-fill. Ink attempts 4-6 (authorized) all reverted per one-attempt terms, converting the parked mechanism into THREE named work items over exactly six fixtures (insideAutonomPass gate; entrypoint WithLabel sizing; SvekEdge placement port). Math.max floor proven dead code. size-backlog 103→92, zero widenings; all census floors held; 10,150 tests. |
| G5 | sequence / activity SVG conformance (non-svek; no DOT gate) | todo | G4, E4 | bar defined by E4 |

## Phase E — Cross-cutting fidelity (fold in as consuming types land)

| ID | Mission | Status | Blocked-by | Exit bar |
|----|---------|--------|-----------|----------|
| E1 | full skinparam → theme wiring (`plans/skinparam/`) | todo | — | skinparam directives reach every renderer; corpus skinparam fixtures match |
| E2 | full Creole + sprite registry (Phase 4h) | done (E2r 2026-07-15; sprite/img half SI5b 2026-07-14) | SI5b (done) | **DONE 2026-07-15** — `plans/e2r-creole/` (branch `feat/e2r-creole`, L1–L3): the char-atom subsystem is ported. L1: klimt/creole legacy stripe/atom pipeline (CreoleStripeSimpleParser, StripeSimple, CommandCreoleStyle chain) — one `<text>` per styled run, `==` headings jar-exact; ubrex DSL not ported (observable semantics reproduced, journaled). L2: CommandCreole{SizeChange,ColorChange,ColorAndSizeChange,FontFamilyChange,Latex,Url} + the unified per-character scan architecture fix; `<latex>` = KaTeX, structural-only — **permanent DIVERGENCES.md entry, maintainer-approved 2026-07-15**. L3: Fission word-wrap (render-path, wrapWidth skinparam, AWS bundle reach) + note bodies through the engine (+ UGraphicStencil crash fix at origin). Census 48/355 held with diff mass down; ratchet 48. Ledgered remainders: `<back:>` (latex-overlap), `<u:color>` (needs colored-line primitive), `<U+NNNN>`-in-TIM (TIM-layer), `<code>` (upstream premise corrected, interception point TBD), `<a href>` wrapper (url-of subsystem), fontStereo-ITALIC. **Post-L3 accounting: leaf-sizing is now the dominant named family (~260/355) — the natural next depth mission.** DOT frozen exact; 8,615 tests. |
| E3 | CSS class names on SVG (Phase 4i) | todo | — | `puml-*` on every semantic element |
| E4 | non-svek depth oracle (sequence, activity) | spike | — | decide SVG-structural oracle vs eyeball QA |

## Phase F — Ecosystem / packaging

| ID | Mission | Status | Blocked-by | Exit bar |
|----|---------|--------|-----------|----------|
| F1 | Markdown integration (Phase 6) | todo | — | autoload + markdown-it + remark plugins ship |
| F2 | dot-engine npm cutover | todo | — | pinned tarball → published release; ratchets re-baselined |
| F3 | GitHub Pages docs site | done | — | **DONE 2026-07-11** (branch `feature/docs-site`): VitePress site at https://sseely.github.io/plantuml-ts/ — hero index, live playground (src-alias import), guide (getting-started + api), parity page regenerated from committed `docs/parity-report.md` (`dot-sync-report --markdown`), divergences mirrored per diagram type, `copy-reports.mjs` pipeline, `.github/workflows/docs.yml` deploy. Perf page deliberately deferred (D4). ONE MANUAL STEP left for maintainer: GitHub → Settings → Pages → Source = GitHub Actions (before first deploy can succeed). Original scope: Site published via GH Pages. Model: `~/git/knowvah/dot-engine/docs-site` (VitePress: hero index + live in-browser playground running the actual library, `guide/` getting-started + API + engines, and dedicated `parity.md` / `conformance.md` / `divergences.md` / `perf.md` pages + a `copy-reports.mjs`-style script folding generated reports into the site). MUST have: **parity status** (per-diagram-type EQUAL/conformance numbers, fed from `dot-sync-report` outputs — keep it regenerable, not hand-maintained) and **divergences organized per diagram type** (maintainer 2026-07-11; source remains `DIVERGENCES.md` — restructure its sections by diagram type, with cross-cutting entries like the preprocessor/!include deferral under a "General" section). OPEN QUESTION (maintainer, 2026-07-10): whether a performance page is even valuable — discuss before building (dot-engine has `perf.md` precedent; ours would need honest jar-vs-ts benchmarks to be worth anything). Generator: VitePress recommended over Docusaurus (discussed 2026-07-10 — sister-site reuse of playground + copy-reports pipeline, Vite-native alignment with our build, one SSG across both projects; Docusaurus only wins if release-versioned docs/i18n/React widgets become requirements — revisit post-1.0 if so; the generated parity/divergences markdown is SSG-agnostic either way. Search: non-factor — the dot-engine config we'd inherit already enables VitePress's built-in offline MiniSearch, `docs-site/.vitepress/config.ts:18-19`; Docusaurus's canonical search is external Algolia DocSearch). Warrants `/plan-mission` when picked up. Requested by maintainer 2026-07-10 mid-A2 |

---

## Snapshot (update as missions flip — last refreshed 2026-07-13)

- **G0b + G0 CLOSED (2026-07-13).** Annotations (title/caption/legend/header/
  footer) render for every engine; LimitFinder machinery ported; description
  doc dims jar-exact; smetana/vizjs re-captured. **Current DOT baseline:
  component 253/262 · usecase 84/90 · class 708/708 · object 78/80 ·
  state 266/267 (oracle-blind = elk-only). Census: 12/355 conformant.
  7,929 tests.** **SI5b CLOSED 2026-07-14** (plans/si5b-stdlib/): stdlib vendored+packaged,
  sprite/img rendering live. **DOT baseline now: component 262/262 (100%) ·
  usecase 90/90 (100%) · class 708/708 · object 78/80 · state 267/267 (100%, dot-engine@0.1.26071415 healed the A4-ledgered crash 2026-07-14).
  G1 IS UNBLOCKED (literal DOT 100%).** 8,203 tests.
  Description DOT drill CLOSED 2026-07-13 (plans/description-dot-100/):
  component 261/262, usecase 85/90 — 100% of non-SI5b-blocked. Five
  mechanisms ported (set-separator quark nesting, container-scoped identity,
  kermor svek variants, remove-stereotype, [[url]]-label resolution).
  G1 next but BLOCKED on the stdlib ruling.
  Mainframe rendering re-deferred (annotation plumbing into the klimt pass
  — see DIVERGENCES.md).

- **PHASE A IS CLOSED (2026-07-12).** A1–A4 all `done`; A5 stays `spike` on S2.
  Final: description component 251/259 (97%) + usecase 79/87 (91%), class
  680/680 (100%), object 78/80 (98%), state 260/261 (99.6%) — all
  structural-match, zero unledgered misses. Gates green at `1517ac2`: 6,550
  tests, typecheck, lint, build.
- **S4 done (2026-07-12)** — `planning/s4-stdlib-audit.md`. It **inverts SI5**:
  `!include` is rare (69/5688 = 1.2%; priority tupadr3 → c4 → cloudinsight →
  archimate → aws), while the preprocessor/TIM half is ~200 fixtures and
  cross-type. **SI5 split into SI5a (preprocessor — the real mass, unblocked)
  and SI5b (bundling — licensing-gated).** License audit: **all top-5 bundles are
  vendorable**, none excluded. `c4`/`archimate` are MIT pure-macro; `tupadr3`/
  `cloudinsight` need attribution; `aws` is CC BY-ND but §3/§4(a) expressly permit
  verbatim incorporation into a Collective Work without infecting our MIT — the
  binding constraint is **verbatim-only copying**, not exclusion. (A first-pass
  verdict of "AWS NOT vendorable" was published and then corrected the same day
  after reading the license text; see `s4-stdlib-audit.md` § AWS — CORRECTED.)
- **wip: SI5a** (preprocessor / TIM completion) — upstream `tim/` has 76 builtins
  + ~30 `Eater*`; we have 2 builtins + a partial Eater set. Missing: `!function`
  declare/return, `!foreach`/`!while`, `!$var` + scoping model,
  `!startsub`/`!includesub`, `!elseif`, and the builtin library (incl. the
  ledgered `%get_json_keys` TIM-json family). Also owns A1's last open drill,
  **silito-78** (`!definelong` → 3 links vs oracle's 1; stopped without root
  cause per `diagnosis.md`).
- **SI1 is now unblocked** (A2+A4 done — behavior is pinned, which was the
  precondition). Not urgent against SI5a (preprocessor-layer, adds no
  entity-model consumer), but **should land before the Phase D types**, each of
  which would otherwise become another migration.
- **S1L stays parked** (`wip`, plateaued at 62% component / 44% usecase size-
  conformance). It now has a fresh queue: **166 shrink-only size-backlog
  entries** (state 136, object 30). Its own diagnosis says the remaining
  description tier is compounding/near-zero-gain per fix; revisit with a
  tolerance-exclusion policy (~67% conformant-among-sizeable).
- **Still-open spikes:** S2 (json/yaml/hcl oracle — gates A5), S3 (stub-engine
  authenticity — gates D2/mind-map), E4 (non-svek depth oracle).

### Historical — svg-conformance Brief 2 (2026-07-10)

- **svg-conformance Brief 2 COMPLETE (2026-07-10), merged to main.** The
  description engine (component/usecase/deployment) now draws entirely through
  the klimt SVG emitter, with a dual-measurer conformance seam (production
  `jarMeasurer` vs injected `DeterministicMeasurer`) and a 5-fixture SVG
  ratchet gating inside `npm test`. Playwright raster visual-QA retired (T20)
  — **`plans/visual-qa-site/` is superseded; do not execute it.** Follow-ups
  tracked in `docs/svg-conformance.md`: ~~F1~~ spline-clip edge-drop **FIXED**
  post-merge (`e346b87` — faithful `simulateCompound`/`subdivide` port; zero
  dropped edges across 294 goldens); F2 legend/title/header/`<img>`/monospace
  creole (largest bucket); F3 measurer-mode residue (documented, not a
  defect); F4 ~1px document-dimension under-count (needs a
  `LimitFinder`/`UGraphicNo` port); F5 `newpage`. Three cross-engine
  unblockers (named-color→hex table, `transparent` background, `roundCorner`)
  **await maintainer write-set approval**. Final gates: 4640/4640 tests,
  coverage 97.7/93.4/98, DOT parity 357/234/60.
*(The A1/A2 status bullets that stood here are superseded — Phase A closed
2026-07-12; see the current snapshot above.)*

- **shallow (need depth pass):** json/yaml/hcl only. *(class, state, object were
  in this list until A2–A4 closed them.)*
- **done (breadth + at least eyeball depth):** sequence, activity, board,
  chronology, files, packetdiag, chart. (These are `done` for breadth; a
  depth-oracle decision — E4 — determines whether the non-svek ones need more.)
- **next: SI5a (preprocessor / TIM completion)** — largest unblocked tranche
  (~200 fixtures, cross-type), retires A1's last open drill (silito-78) and the
  TIM-json ledger entry. Brief via `/plan-mission` from
  `planning/s4-stdlib-audit.md` Finding 3. SI5b needs a maintainer licensing
  ruling; SI1 should land before Phase D.


## `bodyenhanced-atom-seams` — CLOSED 2026-07-30, **320/351 (91.2%)**

Branch `feat/bodyenhanced-atom-seams`, 46 commits. Gates at close: 449 files
/ 11023 tests, typecheck + lint + build clean. Description **320/351, widened
0** (from 317/351). Class **219/708 w0** and DOT **262/90/708 EQUAL** —
neither moved all mission. Perf: `gutute-00-gaki684` 17.39ms → 17.42ms
(+0.17%, inside noise).

**Delivered**
- The whole creole `Display`/`Sheet`/stripe layer — ~2,300 Java lines over
  batch 3a's 14 tasks (`Sheet`, `SheetBlock1/2`, `Sea`, `Position`,
  `CreoleParser`, `Display`, every `Stripe*`/`Atom*`, `EmbeddedDiagram`,
  `Ports`/`WithPorts`, `SignatureUtils`, `Stereotype`, `Pragma`, …). **None
  of this was in the original plan** — T2b hit it as a wall and the
  maintainer ruled a faithful port over a scoped substitute
- `BodyEnhancedAbstract`, `TextBlockLineBefore`, `BodyEnhanced2`,
  `BodyFactory.create3`; `desc` routed through the real pipeline (ADR-1)
- The class path consolidated onto ONE `TextBlockLineBefore` owner (ADR-7)
- Both pipeline seams (ADR-2 ink fields, ADR-3 `imgFallbackFont`)
- A renderer gate that did not exist: 22 diff-count baselines (ADR-5
  AMENDMENT), three of which fell — `codabo-50-mupa164` 388 → 11

**NOT delivered — stated as plainly**
- **Narrowing #1 (folder/package, 8 fixtures)** → **SI1** (ADR-10). Needs
  `create2`/`BodyEnhanced1` → `MethodsOrFieldsArea`, a ≈12,100-line cascade
  through `CucaDiagram`/`Entity`/`Bodier` and the 40-file `skin/` package
- **Narrowing #3 (box + `<img>`)** → **re-opened.** Believed closed by T3 for
  most of the mission; T5 proved T4 routed `desc` past the function T3 fixed
- **Narrowing #2 (usecase + sprite)** → PARTIAL. Single-line routes;
  multi-line guarded on a sub-case T5 found
- **S1L-i** → 1 of 3 fixtures. Blocker named and deliberately withheld:
  `CreoleStripeSimpleParser.ts:95` (a live-rendering change, so ADR-6 wants
  its own gated commit)
- The whole +3 conformance came from T4; T5's widening was numerically
  neutral because its target fixtures were already conformant via the
  guarded path

**Handed to SI1, in `plans/s1l-leaf-sizing/ledger.md`:** `create2` /
`BodyEnhanced1` / `MethodsOrFieldsArea` / `CucaDiagram` / `Entity` / the
skin-style subsystem, ~~plus the `Sea`/`SheetBlock1` single-resolved-value gap~~ — **RETIRED
2026-07-30 by `sizer-footprint-parity`: no such channel exists upstream.
Narrowing #3 is closed; #2's residue is a one-line `fitToInk` correction,
not a channel. See that mission's entry and the S1L ledger.** ADR-2 and ADR-3 added exactly those side channels to the OLD
pipeline; routing discarded them, so SI1 must re-add them at the new layer.

**Method lesson, earned three times.** ADR-5's golden count (4 vs 48), T5's
guard count (three vs two), and T3's narrowing-#3 credit were all stale, and
two were repeated to the maintainer before being checked. **Verify an
"already fixed" claim against the CURRENT call graph, not the commit that
introduced the fix.** Also: size a dependency cascade by tracing TWO levels
before asking for a ruling — the stripe estimate moved 1,100 → 2,300 and the
`MethodsOrFieldsArea` estimate 3,900 → 12,100 on the second level.

**SI1's scope grew accordingly** — it now owns `create2` and the
entity/skin/style cascade on top of its original `Bodier` scope. Still
should land before Phase D.

## `sizer-footprint-parity` — CLOSED 2026-07-30, **320/351 (91.2%)** flat

Branch `feat/sizer-footprint-parity`, 5 commits. 449 files / 11029 tests,
all gates clean. Perf **improved**: `gutute-00-gaki684` 17.42 → 16.79ms.

**Delivered**
- **T6 narrowing #3 (box + `<img>`) CLOSED.** `jecici-56-bimu826` widened
  0.398264in unguarded before, **0** now; diff count 143 → 133. Cause was
  never a missing font seam: `AtomImg.create` hardcodes
  `UFontFactory.monospace(14)` (`AtomImg.java:106-107`), and the jar's
  100.362×**14** was that constant. The 14 coinciding with the diagram
  default is what let the wrong diagnosis survive an entire mission.
- **Narrowing #2 single-line** routes.
- Two dead seams deleted (`imgFallbackFont`, `AtomImageResolver` ink fields);
  three unrelated stale pins cleaned.

**NOT delivered**
- **Narrowing #2 multi-line** — still guarded. **The fix named here on
  2026-07-30 was WRONG and is corrected in the ledger (3rd version).**
  "Remove `fitToInk`'s substitution" rested on "upstream never puts ink in an
  atom's dimension", which came from reading `klimt/sprite/SpriteSvg.java` —
  the wrong sprite path. Stdlib icon sprites route through `SvgNanoParser`,
  which decomposes into per-`<path>` draws, giving `Footprint` a genuine
  independent ink signal (jar-confirmed: same declared 16×16, `bi-globe`
  `rx=34.729` vs `bi-bootstrap-fill` `rx=37.4784`). The port's real defect is
  that `drawAtoms` emits ONE opaque `UImage`, so ink and layout share a
  channel. **Fix: mirror `SvgNanoParser`'s draw-time decomposition.**
- The analytic substitute could not be retired: `measureUsecase` is called
  unconditionally from the CLASS engine
  (`class-layout-leaf-shapes.ts:14,27`), predating the guard mechanism.

**SI1's inheritance changes shape, it does not disappear.** The
`Sea`/`SheetBlock1` side-channel gap SI1 inherited **does not exist** —
upstream has no such channel and ADR-3 stands. What SI1 (or whoever touches
`leaf-sizing.ts`'s resolver) inherits instead is the one-line `fitToInk`
correction above, plus the class-engine coupling that blocks retiring the
substitute.

**Method note.** Three premises in this mission line have now gone stale the
same way, and the third was mine after I wrote the rule: I traced
`usecase-footprint.ts`'s consumers ONE level and missed that
`measureUsecase` has a class-engine caller. **Trace two levels — including
in your own plan.**
