# Code Review Tasks
<!-- Generated by /code-review on the SI20 diff (a62cc197..93b04555).
     Review each item, remove any you don't want applied, then run:
     implement the tasks in code-review-tasks.md -->

## Should fix (Warning)

- [ ] `src/diagrams/class/class-object-sizing.ts:189` — the canonical object
  separator `' = '` is repeated as a bare literal here, while
  `class-object-commands.ts:303` defines it as `CANONICAL_OBJECT_SEPARATOR`.
  The two must stay byte-identical or the separator round-trip silently
  breaks, and no gate can observe the breakage (a space measures 0 wide under
  `DeterministicMeasurer`). Fix: export `CANONICAL_OBJECT_SEPARATOR` and import
  it here instead of repeating the literal.

- [ ] `plans/si20-object-row-ports/ledger.md` — record that commit `62a356ca`
  is **not** safely revertible in isolation. Its body claims "reverting
  restores the `:P` mechanism for objects"; verified empirically that
  reverting it alone leaves `npm test` RED, because `83bc0e98` already deleted
  the `port-backlog.json` pin that would tolerate the reverted state. A clean
  rollback requires reverting `83bc0e98` (or restoring the pin) as well.

- [ ] `src/diagrams/class/{class-dot-graph,class-object-sizing,class-port-rows}.ts`
  — 498 / 495 / 495 lines against a hook-enforced 500-line cap that BLOCKS the
  write. The next edit to any of these trips the hook mid-change. Fix: plan a
  split along an existing seam before the next feature lands in them, the way
  ADR-7 handled `class-object-map-sizing.ts` and `class-layout-helpers.ts`.

## Consider improving (Suggestion)

- [ ] `src/diagrams/class/class-dot-graph.ts:207-209` — the ADR-3 comment on
  `portRowIds` still says "a `LIKE_CLASS_KINDS` leaf with a declared port-name
  set", contradicting its sibling at `:195-196` which was updated to
  "row-port leaves only (`isRowPortKind`: class family + object)". Two comments
  in one file now disagree about the same set. Fix: reword to "an
  `isRowPortKind` leaf (class family or object) with a declared port-name set."

- [ ] `src/diagrams/class/class-layout-helpers.ts:83-90` — orphaned docblock
  left by the S2 relocation. It documents
  `packageEndpointAnchors`/`shieldedClassifierIds`, which now live in
  `class-shield-helpers.ts`, so it documents nothing in this file; its removal
  also left a double blank line. Fix: delete the block, collapse the blank
  lines. (Genuinely dead — no `@see` Java citation, an artifact of our own
  split, not ported upstream behavior.)

- [ ] `src/diagrams/class/class-object-display.ts:6` — doc comment cites
  `class-object-map-sizing.ts#computeObjectTitle`; that symbol moved to
  `class-object-sizing.ts` in S1. Fix: repoint the module qualifier.

- [ ] `src/diagrams/class/class-object-map-header.ts:321` — doc comment cites
  `class-object-map-sizing.ts#measureObjectFields`; moved to
  `class-object-sizing.ts` in S1. Fix: repoint the module qualifier.

- [ ] `src/diagrams/class/renderer-uid.ts:32` — doc comment cites
  `class-dot-graph.ts#packageEndpointAnchors`. Pre-existing error (the symbol
  never lived there; it was in `class-layout-helpers.ts`) and now doubly stale.
  Fix: repoint to `class-shield-helpers.ts#packageEndpointAnchors`.

- [ ] `src/diagrams/class/class-port-rows.ts:484-495`
  (`classPortShortNamesById`) — calls `classifierPortShortNames` (`:450-460`,
  a full scan of `ast.relationships`) once per row-port classifier, i.e.
  O(C x R). SI20 widened `isRowPortKind` to include `object`, so this path is
  newly live for object diagrams, where previously no classifier matched.
  Sub-millisecond at realistic diagram sizes; flagged for consistency, not
  urgency. Fix: build the map in a single pass over `ast.relationships`, the
  way `shieldedClassifierIds` already does.

- [ ] `src/diagrams/class/class-port-rows.ts:191-193` (`electionTextFor`) — a
  binary `kind === 'object' ? ... : ...` with no exhaustiveness tie to
  `ClassifierKind` or to `isRowPortKind`'s own set. A future third row-port
  kind would silently fall through to the class-family reconstructor — the
  exact defect class that already bit this mission once, and one no gate can
  see. Fix: make it an exhaustive switch so adding a kind forces a compile
  error here.

- [ ] `tests/unit/class/class-object-row-ports.test.ts` — the suppressed vs
  empty-but-shown compartment distinction is asserted only at the
  `measureObjectClassifier` publish layer
  (`class-object-port-bands.test.ts:61-82`), never through to
  `DotInputNode.portRows`. Fix: add an object-specific pair asserting the
  emitted node's port-row geometry differs by the compartment-floor margin.

- [ ] `oracle/goldens/object/` — the separator round-trip fix (`9a698e18`) has
  no oracle coverage; its own commit body says the gates cannot see it. Fix:
  author one `.puml` fixture with non-canonical `=` spacing and generate a jar
  oracle, so the DOT-parity ratchet exercises the path.

- [ ] `tests/oracle/object-dot-parity.test.ts:103` — the
  `directionBacklog.has(name)` branch is unreachable:
  `oracle/goldens/object/direction-backlog.json` holds `"slugs": []`.
  Pre-existing (emptied by an earlier commit), but it is dead by exactly the
  argument T3 used to delete `portBacklog`. Fix: delete the file and the
  branch, as T3 did for the port backlog.

## Standing project findings (NOT introduced by this diff)

- [ ] `package.json:64` — `@knowvah/dot-engine@1.0.0` is **EPL-2.0** (verified
  in its `package.json` and bundled `LICENSE`), is a production dependency,
  and is bundled into `dist/` (`vite.config.ts:23` externalizes only `katex`).
  This repo declares MIT in both `package.json:5` and `LICENSE`, with no
  `NOTICE`/`LICENSES.md`/third-party attribution anywhere. CLAUDE.md requires
  MIT-compatible dependencies. Separately, `DIVERGENCES.md:122` rejects `elkjs`
  partly because it "is EPL-2.0 — the first non-permissive dependency in a tree
  that is otherwise MIT" — a premise that is now false. Fix: get a licensing
  decision, then either add attribution, externalize the engine as a peer
  dependency, or revise the stated policy. Correct `DIVERGENCES.md:122` either
  way. **Not legal advice — this needs a human call.**

- [ ] `package.json` devDependencies — `vitest@1.6.1` and
  `@vitest/coverage-v8@1.6.1` carry a critical advisory
  (GHSA-5xrq-8626-4rwp); `vite@5.4.21` carries three high advisories. All are
  3 majors behind. Dev/CI only — nothing ships in `dist/`, and the production
  dependency graph reported zero vulnerabilities. Fix: schedule the vitest 4.x
  / vite 8.x upgrade as its own mission.

- [ ] `src/diagrams/class/class-object-commands.ts:156-166`
  (`parseObjectMatch`) — CCN 10, exactly at the hook cap. Untouched by this
  diff. One more branch trips the blocking hook. Awareness only.
