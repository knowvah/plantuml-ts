# Code Review Tasks
<!-- Generated by /code-review on the SI20 diff (a62cc197..93b04555).
     Updated after the fixes landed. Only the Standing findings section
     still contains work; everything above it is closed. -->

## Closed — Warnings (merged `afed1ffa`)

- [x] `class-object-sizing.ts` — canonical `' = '` was a named constant on the
  parser side and a bare literal on the formatter side, with nothing enforcing
  they stay identical and no gate able to see a drift. Now one shared
  `CANONICAL_OBJECT_SEPARATOR` in the leaf module `class-object-display.ts`,
  placed there to avoid the `parser.ts` import cycle.
- [x] `plans/si20-object-row-ports/ledger.md` — recorded that `62a356ca` is not
  revertible alone; `83bc0e98` had already deleted the pin that tolerated its
  red state. Verified in a worktree, not reasoned about.
- [x] File-size headroom — three splits: `class-dot-graph.ts` 498 → 373,
  `class-port-rows.ts` 495 → 448, `class-object-sizing.ts` 495 → 289.

## Closed — Suggestions (merged `afed1ffa`, `3c564117`, `c43a1e4d`)

- [x] Four stale cross-module doc references repointed
  (`class-dot-edges.ts`, `class-object-map-header.ts`, `renderer-uid.ts`,
  `class-shield-helpers.ts`), plus `class-object-display.ts` earlier.
- [x] `class-layout-helpers.ts` — orphaned docblock deleted. It described three
  functions S2 relocated, so it documented nothing. Genuinely dead: no `@see`
  Java citation, an artifact of our own split rather than ported behavior.
- [x] `tests/oracle/object-dot-parity.test.ts` — dead `directionBacklog` branch
  and its empty `direction-backlog.json` deleted, as T3 did for the port
  backlog. All 79 fixtures still passed, which is what proved it dead. Object
  now has no structural backlog at all.
- [x] `classPortShortNamesById` — was O(classifiers × relationships), newly
  live for object diagrams after `isRowPortKind` widened. Now one pass, with
  per-id insertion order deliberately preserved.
- [x] `electionTextFor` — its coupling to `isRowPortKind` is now pinned by a
  fitness test rather than convention. Two guards: a
  `Record<ClassifierKind, true>` fails to compile if the union grows, and the
  pinned row-port set fails if the predicate widens. No defensive code added
  to the production path.
- [x] Suppressed vs empty-but-shown — now asserted through to
  `DotInputNode.height` (18 header-only vs 34 = header + `OBJECT_EMPTY_FIELDS`),
  not only at the publish layer.
- [x] Separator oracle fixture — authored at
  `oracle/goldens/object/si20-member-separator-spacing`. **It does not guard
  the separator behavior**: with the fix reverted it still passes, because the
  ratchet compares structure and geometry and a zero-width space changes
  neither. Kept for the election/port-id coverage it does provide; the
  limitation is recorded in `class-object-separator-spacing.test.ts`'s header.

## Standing findings — still open, not introduced by the diff

- [ ] **Licensing decision — needs a human call.** `@knowvah/dot-engine@1.0.0`
  is EPL-2.0 and is *inlined* into `dist/` (verified: zero import statements
  for it in the bundle, unlike `katex`, which is external and imported), while
  this package declares MIT. A `NOTICE` now records what is redistributed and
  under which terms, and ships in the tarball (`package.json` `files`). That is
  attribution, not a conclusion: whether EPL-2.0-inside-an-MIT-bundle is
  acceptable for this distribution model remains to be decided. The related
  `DIVERGENCES.md` misstatement is already corrected (`dbdc295d`).

- [ ] **Dev-dependency CVEs.** `vitest@1.6.1` and `@vitest/coverage-v8@1.6.1`
  carry a critical advisory (GHSA-5xrq-8626-4rwp); `vite@5.4.21` carries three
  high advisories. All three are 3 majors behind. Dev/CI only — nothing ships
  in `dist/`, and the production dependency graph reported zero
  vulnerabilities. Worth its own mission.

- [ ] **`class-object-commands.ts` `parseObjectMatch` is at CCN 10**, exactly
  the hook cap, and untouched by this work. One more branch trips the blocking
  hook. Awareness only.

- [ ] **~772 bare `dot-engine` prose mentions** in `docs/` and `planning/`
  describe the engine by its former name. Low priority now that the old name
  is known to have been private. The 33 broken *path* references were fixed
  separately (`db4bf0f4`); `plans/` history is deliberately left alone.
