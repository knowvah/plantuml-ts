# Decision journal

| # | When | Task | Decision | Why | Flag for review? |
|---|------|------|----------|-----|------------------|
| 0 | 2026-09-20 | plan | Baseline recorded at `a012be16` (main, clean): typecheck 0, lint 0, `npm test` 730 files / 20066 tests, coverage 96.00 / 91.37 / 97.08 / 96.97. | Phase 8 pre-flight | no |
| 0a | 2026-09-20 | plan | `plans/` is NOT added to `.gitignore`: this repo commits its plan directories (130 in tree, `chore(plans): …` commits). `.claude/` already ignored. `.claude/settings.autonomous.json` left as-is: it already permits `npm`, `npx`, `scripts/oracle-render.sh`; specs use `npx jiti` for that reason. | project convention overrides the skill's step 9 | no |
| 0b | 2026-09-20 | plan | `scripts/dot-sync-report.ts` is 596 lines, over the 500-line complexity hook; T4 extracts `dot-parity-rows.ts` first so the edit can land. | hook would block any edit | no |
| 1 | 2026-09-20 | batch-1 | Branch `feat/parity-dashboard-refresh` cut at `a012be16`; brief committed as `a701a2f6`. Four worktrees at `.claude/worktrees/pdr-T{1..4}` on branches `pdr/T{1..4}`, each with the 14 gitignored asset trees symlinked (recipe: `.agent-notes/si24-census-worktree-needs-ignored-assets.md`). Symlink patterns added to `.git/info/exclude` because trailing-slash `.gitignore` rules do not match symlinks and the links showed as untracked. Task commits land on the task branches and are cherry-picked onto the mission branch in T-order, so each task keeps one linear commit. | [[batch-parallelism-needs-worktrees]]: one index cannot host four writers | no |
| 2 | 2026-09-20 | batch-1 | Agents run ONLY their own vitest file(s) (`npx vitest run tests/unit/scripts/<file>`, collected-count checked) plus `npm run typecheck` and `npm run lint`; the full `npm test` runs once at the batch gate on the merged mission branch. | Four concurrent full suites share one `packages/*/generated` and one `test-results/` through the symlinks; the cross-process build lock covers the rebuild but not four simultaneous 20k-test runs on one box, and the gate result is what the brief scores anyway. | no |
| 3 | 2026-09-20 | T1 | Accepted `captureOracleCache(type, manifest, opts, cacheRoot = CACHE_DIR)`: the agent added the fourth `cacheRoot` parameter so the whole capture path is testable against a temp dir. `classifyOutput` renames in place (spec wording "then it is renamed"). 12 tests, jar never invoked. Commit `513ab128` (cherry-picked from `pdr/T1` 4b434da9). | one parameter buys end-to-end coverage without touching `test-results/` | no |
| 4 | 2026-09-20 | T2 | JSON writer extracted to `scripts/svg-conformance-census-json.ts` (the spec's own fallback; census file 405→438 lines). `Bucket`/`bucketOf` moved with it so the import is one-directional. CLI entry guarded with the `import.meta.url` check so the module can be imported by tests. `--json` `type` field is `requested.join('+')`; T7 only ever passes one type. `CensusResult.reason` added (additive; no printed line changed, verified by grepping the diff for `console.`). Commit `a41596d7`. | write-set amended per the spec's own clause | no |
| 5 | 2026-09-20 | T3 | AC3 could not hold as written: committed `parity.json` (9a0ce1ea, 2026-09-08, 360 rows) was already newer than committed `PARITY-SVG.md` (7f22718f, 2026-07-31, 358 rows). Proved number-neutrality the other way: old script (a701a2f6) vs new script on the byte-identical `parity.json` differ ONLY in the preamble (hunks 6-7c6-7, 9-16c9-19; diff saved in scratchpad). The fully regenerated `PARITY-SVG.md` is committed. Agent also fixed a latent `parseSurveyArgs` bug (bare positional type was dropped when `--out` absent; `outIdx+1 === 0`) and made survey-all mode fire only when BOTH positionals and `--out` are absent. `.agent-notes/pdr-T3.md` records the staleness. Commit `e352ff19`. | the stale-report finding is D9's motivation, observed live | no |
| 6 | 2026-09-20 | T4 | Extraction landed (596→492 lines). Agent merged the two old "not yet measured" branches into one `no oracle captured` note; `NON_SVEK_TYPES` keeps `gitgraph` per the spec although no manifest exists (inert). Two string-concat→template conversions in untouched code to get under 500 lines (tooling, not ported logic). Old vs new `--markdown` in the MAIN checkout: five svek rows numerically identical (class 711/710, component 263/259…), only notes/legend differ; no file written under `test-results/`. Commit `3fcaa150`. Note for T6: the canonical tree `test-results/visual-qa-svg/canonical/` is gitignored, so a fresh checkout classifies the svek types as `no data-diagram-type classification` until T7 step 1 rebuilds it. | — | decisions 1 and 3 flagged by the agent; accepted |
| 7 | 2026-09-20 | batch-1 | WORKTREE RECIPE DEFECT: `git worktree add` checks out the committed `test-results/dot-cache/` as a real directory, so `ln -s $REPO/test-results $W/test-results` landed INSIDE it as `test-results/test-results`. Every agent saw only the committed cache and none of the gitignored `visual-qa-svg/`, `dot-sync-equal/`, manifest baselines. Harmless for T1–T3; for T4 it hid the canonical tree, so its AC3 before/after ran with all rows at zero (weak proof, redone in main) and its `dotParityRows` tests passed only because `buildAgg` was unreachable. Corrected recipe: link the CHILDREN of `test-results/` individually. | measurement artifact, not a code defect ([[measurement-artifacts-outnumber-defects]]) | no |
| 8 | 2026-09-20 | batch-1 gate | `npm test` RED (1 file: `dot-parity-rows.test.ts`, 9 timeouts; 733 files / 20102 passed otherwise); typecheck/lint/build GREEN. Mechanism: `dotParityRows` reaches `buildAgg` (`dot-parity-rows.ts:84-86`) for every svek type whose canonical dir exists, ~11 s per call, and the test calls it against ambient repo state; `class (cached, no local canonical SVGs)` asserts a fresh-checkout tree. Ruled out: the pick (worktree repro identical after linking the canonical tree); load (single-file run fails alone). Fix attempt 1 of 2: sent back to the T4 agent — inject `{dataDir, cacheDir, canonDir}` roots and test on a temp tree. | fix at origin, not the timeout | no |
| 9 | 2026-09-20 | T4 fix | Fix accepted as `54abc1bd` (cherry-picked from `pdr/T4` 911a5b87): `dotParityRows(jar, roots?)` with `DotParityRoots {dataDir, cacheDir, canonDir}`; tests build a temp tree with no canonical dir so `buildAgg` is structurally unreachable (per-test ≤9 ms). The agent found a second mechanism on the way: a module-level `DEFAULT_ROOTS` read `CACHE` from the circular `dot-sync-report.ts` import before that entry module's top level ran (`undefined` under jiti's CJS interop, crashing the real CLI); the default lives in the parameter list instead. `--markdown` before/after byte-identical with the canonical tree present. Judgment calls flagged: no test for the no-roots default (would re-couple to ambient state); `enumerateFixtures` still reads the real golden dir (read-only, harmless). | fix at origin; second mechanism caught by running the real entry point | no |
| 10 | 2026-09-20 | batch-1 gate | Re-run from scratch after the fix: `npm test` 733 files passed / 1 skipped (baseline 730 + 3 new files; no under-collection), 20107 tests, coverage 96.00/91.37/97.08/96.97 unchanged; typecheck 0; lint 0; build 0; `git diff --name-only a701a2f6..HEAD` = 14 paths, all in the batch write-set (`.agent-notes/pdr-T3.md`, `svg-conformance-census-json.ts` and `PARITY-SVG.md` are the journaled additions). Batch 1 CLOSED. | — | no |
| 11 | 2026-09-20 | batch-1 | SECOND WORKTREE HAZARD: the main checkout's `scripts/dot-parity-rows.ts` turned up modified at 09:22 with the T4 agent's first (broken) `DEFAULT_ROOTS` attempt while the agent was told to work only in its worktree. Serena MCP is registered at user scope with the MAIN checkout as its project root, so a Serena symbolic edit with a relative path writes to the main tree, not the worktree; its reads likewise read the main copy. Discarded with `git checkout --` (diff saved in scratchpad); pdr/T4 carried the authoritative version. Rule for every later worktree agent: Serena READ tools only with absolute worktree paths, NEVER Serena edit tools; edits through Edit/Write on absolute worktree paths. Recorded in `.agent-notes/pdr-batch1-worktrees.md`. | [[batch-parallelism-needs-worktrees]] gains a third mechanism | yes — worth a memory update at close |
| 12 | 2026-09-20 | T6 (pre) | AMENDMENT to T6's inputs, flagged: the spec has the dashboard call `dotParityRows(jar)` live. With the canonical tree present that is an ~11 s aggregate inside the 5 s drift test, and on CI the gitignored `test-results/visual-qa-svg/canonical/` is absent so every svek row degrades to `no data-diagram-type classification` and the committed report can never match — stop 9 by construction, and a D2 breach ("reads only committed JSON"). Instead: `dot-sync-report.ts --json <path>` writes `tests/oracle/svg-conformance/dot-parity.json` = `{ generatedAt, measuredAgainstCommit, rows: TypeRow[] }` (writer in `dot-parity-rows.ts`), the dashboard reads that file, T6 produces it once, T7 step 1 regenerates it. T6's write-set grows by `scripts/dot-parity-rows.ts`, `scripts/dot-sync-report.ts` (one flag hook; must stay <500 lines) and the JSON. | D2 + D9 over a task-spec sentence | yes |
| 13 | 2026-09-20 | T5 | Capture (T1 script, wall-clock): board 4 (9.0 s), chart 29 (62.5 s), chronology 1 (0.9 s), files 1 (2.4 s), packet 6 (13.0 s) = 41/41 captured, `jarFailed` and `renamed` both empty for every type, every dir exactly `{in.puml,in.svg,.done}`, 0 jar error pages, 40 of 41 goldens typed. Stdout JSON saved in the scratchpad (`capture-<type>.json`). | AC1 | no |
| 14 | 2026-09-20 | T5 | Measured all 41 through the gates' own seams BEFORE pinning (scratch `measure-new-trees.ts`, dry run): uniform per type — jar BOARD/CHART/FILES/PACKET vs ours NONE for all 40, `weErrored` false for all 41, engine `none`; chronology jar NONE / ours NONE. Pinned: 40 `known-misroute` on ONE cited mechanism (renderer returns no `diagramType`: `board/renderer.ts:75-81`, `chart/renderer.ts:336-341`, `files/renderer.ts:64-69`, `packetdiag/renderer.ts:104-109`; `assemble-svg.ts:496-497` vs `TextBlockExporter.java:292-294`), 1 `agree`. Refusal: 41 × `ok`. Additive-only proven: `git diff --numstat` 451/452 insertions, exactly ONE deletion each = the `$comment` line; zero existing rows touched. Counts re-derived, not bumped: routing 3402/139/31 over 3572, censused 138; refusal 3572/31/105/3467, gaps 9, non-activity defect still exactly nuvoja, activity queue 82. | [[new-corpus-tree-trips-two-gates]] — measure the distribution first: 41 pins collapsed to two judgments | no |
| 15 | 2026-09-20 | T5 | WRITE-SET AMENDMENT (flagged): `routing-conformance.test.ts` and `refusal-coverage.test.ts` hard-code the derivation counts, so re-pinning cannot land without editing them; neither is in T5's write-set or any other. Treated as part of "re-pin the way the gates' own headers say", the exact precedent of `activity-oracle-harness` T0b (user-ruled 2026-09-02). Each count edit carries its derivation comment in the gate file's own style. | stop 1 read against the T0b ruling | yes |
| 16 | 2026-09-20 | T5 | FINDING: chronology's only golden is the jar's own `PSystemUnsupported` page ("Diagram not supported by this release of PlantUML", `PSystemUnsupported.java:62`): `PSystemBuilder.java:284` returns it when no factory produced a system for the block, i.e. the pinned `plantuml-1.2026.8beta1` build has no `DiagramType.CHRONOLOGY` factory. NOT stop 4 — the jar did not misbehave, it rendered its documented placeholder; `isJarErrorPage` correctly classifies it as rendered (no version banner), so it agrees at NONE == NONE and counts as rendered in refusal. Consequence for T7: any chronology survey/census verdict compares our render against a placeholder and is meaningless as fidelity; the dashboard must show it but T8 must not cite it as a conformance number. Recorded in `.agent-notes/pdr-T5.md`. | push-forward: record, do not chase | yes |
| 17 | 2026-09-20 | T5 | `oracle-freshness.test.ts` went RED on the chronology sentinel: `renderAll` renders all sentinels in one jar call with `execFileSync` and threw on exit 200 — `SourceFileReaderAbstract.java:108-114` flags the run as errored for a `PSystemUnsupported` block exactly as for a `PSystemError`, after every SVG is written (aoh-T0 Finding 1). Fixed at origin: `renderAll` ignores the exit code (commented with the citation); each sentinel's byte comparison is the success test, and a truly unwritten `<type>.svg` fails that sentinel by name. Fix attempt 1 of 2; freshness 30/30 after. | the gate trusted the signal T1 was written to distrust | no |
| 18 | 2026-09-20 | batch-2 gate | With the five trees on disk: `npm test` 733 files passed / 1 skipped, 20157 tests (+50: 5 sentinels, 41 × routing + refusal per-fixture cases), coverage 96.14/91.56/97.26/97.11; typecheck 0; lint 0; build 0. `git diff --name-only bdd3344e..3e2c6e1d` = the five trees + the two baselines + three gate tests (row 15 amendment). `.git/info/exclude` had `test-results` from row 1's worktree fix, which hid the new carve-out trees from `git add`; removed before staging (the corrected worktree recipe no longer needs it). Batch 2 CLOSED at `3e2c6e1d`. | — | no |
| 19 | 2026-09-20 | T6 | Accepted as `54c260e5` + `f209f381` (cherry-picked from `pdr/T6` eb5d4021/c20f77fa). Row-12 amendment implemented: `dot-sync-report.ts --json` writes `tests/oracle/svg-conformance/dot-parity.json` (writer in `dot-parity-rows.ts`), dashboard reads it. Hook-driven splits beyond the spec (all journaled additions): `scripts/dot-sync-probe.ts` (extracted `--probe-json-dot`; report file 492→426 lines), `parity-dashboard-inputs.ts`, `-goldens.ts`, `-matrix.ts`. `object → class` engine mapping (no separate object engine upstream, per `EXPECTED_TAG`'s own comment). Routing/refusal cells as `n/total`. 46 tests incl. the D9 drift test run twice. | write-set widened by the 500-line/30-NLOC hook, not by scope | no |
| 20 | 2026-09-20 | T6 | Two new D8 words (push-forward, logged): `no data-diagram-type classification` (row 12 anticipated it) and `no diff-baseline yet` (only 3 of 11 ratchet families carry a `diff-baseline.json`). Also a computation rule, not a word: the older svg-description schema lacks `weightedScore`, so Σ falls back to `diffCount`. Both recorded in `.agent-notes/pdr-T6.md`. | D8 says add-and-log | yes — D8 vocabulary now 10 words |
| 21 | 2026-09-20 | batch-3 gate | D9 worked on first contact: cherry-picking T6 onto a branch that already carried T5's five trees turned the drift test RED, and `npm run parity:dashboard` (5.9 s) moved exactly the five `oracle` cells (`n/a (no oracle captured)` → 4/29/1/1/6) and their five freshness rows — nothing else. That regeneration is committed in this close-out so HEAD stays green; T7 regenerates again from fresh producers. Gates with it on disk: `npm test` 734 files passed / 1 skipped (+1 new file), 20194 tests, coverage 96.14/91.56/97.26/97.11; typecheck 0; lint 0 (8 min under a load-36 spike — WebStorm indexing plus vitest workers draining; not a defect); build 0. Write-set: 13 paths, all T6's incl. the three journaled splits. Batch 3 CLOSED. | — | no |
| 22 | 2026-09-20 | T5 addendum | Sharper cite for the chronology finding: upstream's own `PSystemBuilder.java:184` is `// factories.add(new ChronologyDiagramFactory());` — commented out — and no `chronology` package exists under `net/sourceforge/plantuml/` at all; `DiagramType.java` is the only file naming CHRONOLOGY. So `@startchronology` reaches `:284` (`PSystemUnsupported`) by design of this source tree, not by a build accident. | read the Java | no |
| 23 | 2026-09-20 | T7 (pre) | The five new families will render `n/a (no data-diagram-type classification)` in the DOT column once `dot-parity.json` is regenerated with their caches present, which reads as unfinished classification work. Upstream: `BoardDiagram`, `ChartDiagram`, `FilesDiagram`, `PacketDiagram` all `extends TitledDiagram` (never `CucaDiagram`, so no svek stage) and chronology has no factory (row 22). Decision: add the five to `NON_SVEK_TYPES` as `fix(pdr-T7)` before regenerating, so they read `n/a (no DOT stage (non-svek))` like sequence/activity. | D8: the reason must be the true reason | no |
| 24 | 2026-09-20 | T7 (pre) | Rehearsed the survey (25 min, all 16 types) and the 11 censuses into the scratchpad while T6 was live. Survey left 16 `timeout` rows in exactly three types (packet 5, usecase 5, yaml 6); the brief's re-run at `SVG_PARITY_CONCURRENCY=2` cleared packet and usecase but yaml kept 2 and took 9 min for 39 fixtures. DIAGNOSIS before touching T7: (a) `--render-one` on the two slugs and a control: ~1 s of work each; (b) one persistent `--render-many` worker driven through all 39 yaml dirs sequentially: 5.2 s total, slowest frame 33 ms — no per-worker accumulation; (c) parent-side `diffVerdict` runs AFTER `runPersistentPool` resolves (`svg-parity-survey.ts:393-397`), so no event-loop starvation from diffs; (d) yaml survey twice on the quiet box at default concurrency: 14 s, 0 timeouts, verdicts identical (4/7/28). Mechanism: the per-fixture 60 s budget (`svg-parity-survey.ts:105`) charged to workers starved by concurrent load — the T6 agent's runs plus my own probes, load 8–9 — exactly the class the file's own header records (48 spurious timeouts at load 4.9, lines 84–105). Stop 5 is therefore a rule about the run environment: T7's producers run ALONE, sequentially, with no agent live. | [[measurement-artifacts-outnumber-defects]], [[confounded-wall-clock-readings]] | no |
| 25 | 2026-09-20 | T7 (pre) | Rehearsal also showed: activity census vs `svg-activity/diff-baseline.json` agrees exactly on the comparable population (268 baseline→ok, 82 error→error); the 8 extra census errors are jar-error goldens our parser also refuses (stop 8 NOT triggered). `census object` took 959 s under the same load (its neighbours 5–8 s); re-measured in the real run below. Also fixed and amended (`993cc2ae`): my first fix commit carried a red `dotParityMarkdown` expectation because a `grep` swallowed vitest's exit code — the gate command now checks vitest's own exit. | — | no |
| 26 | 2026-09-20 | T7 | Producers run ALONE, sequentially (row 24), wall-clock: dot-sync aggregate 11 s; `--json` → `dot-parity.json` 11 s; `svg:survey` all 16 types 170 s, ZERO `timeout` rows (rehearsal under load: 1518 s, 16 timeouts); 11 censuses 5–11 s each (object 6 s vs 959 s under load); `svg:dashboard` + `parity:dashboard` 6 s. Committed artifacts: 16 `parity-<type>.json` + `parity.json` (360 rows, same slugs as HEAD, 0 `dotEqual` changes — D4 holds, no ratchet can move), 11 `census-<type>.json`, `dot-parity.json`, `PARITY-SVG.md`, `docs/parity-report.md` (128 lines). Report audit: zero bare `n/a`; matrix vocabulary = D8's ten words; the T4 legend's own `n/a (no DOT stage)` appears only inside the verbatim DOT section. Stop 8 re-checked on the real census: 268 baseline→ok, 82 error→error, exact. Matrix appended below the table. | — | no |

## T7 matrix (docs/parity-report.md at regeneration, 2026-09-20T18:50:23Z)

```
| type | engine | corpus | oracle | DOT equal | survey conformant / structural / diverged | census 0-diff | ratchet pins | diff-baseline (n · ΣweightedScore) | routing agree | refusal ok |
| --- | --- | ---: | --- | --- | --- | --- | --- | --- | --- | --- |
| activity | activity | 771 | 373 | n/a (no DOT stage (non-svek)) | 0 / 3 / 370 | 0 | 0 | 268 · 51390 | 268/373 | 373/373 |
| board | board | 4 | 4 | n/a (no DOT stage (non-svek)) | 0 / 0 / 4 | n/a (no census yet) | n/a (no ratchet yet) | n/a (no diff-baseline yet) | 0/4 | 4/4 |
| c4 | description | 11 | n/a (no oracle captured) | n/a (no oracle captured) | n/a (no survey yet) | n/a (no census yet) | n/a (no ratchet yet) | n/a (no diff-baseline yet) | n/a (no oracle captured) | n/a (no oracle captured) |
| chart | chart | 29 | 29 | n/a (no DOT stage (non-svek)) | 0 / 0 / 29 | n/a (no census yet) | n/a (no ratchet yet) | n/a (no diff-baseline yet) | 0/29 | 29/29 |
| chronology | chronology | 1 | 1 | n/a (no DOT stage (non-svek)) | 0 / 0 / 1 | n/a (no census yet) | n/a (no ratchet yet) | n/a (no diff-baseline yet) | 1/1 | 1/1 |
| class | class | 768 | 723 | 710/711 (100%) | 412 / 50 / 261 | 414 | 314 | n/a (no diff-baseline yet) | 720/723 | 723/723 |
| component | description | 384 | 266 | 259/263 (98%) | 0 / 13 / 253 | 44 | 32 | 15 · 853 | 266/266 | 266/266 |
| ditaa | n/a (no engine (D8 todo)) | 2 | n/a (no oracle captured) | n/a (no oracle captured) | n/a (no survey yet) | n/a (no census yet) | n/a (no ratchet yet) | n/a (no diff-baseline yet) | n/a (no oracle captured) | n/a (no oracle captured) |
| dot | dot | 62 | 5 | n/a (no DOT stage (non-svek)) | 5 / 0 / 0 | 5 | 5 | n/a (no diff-baseline yet) | 5/5 | 5/5 |
| ebnf | n/a (no engine (D10 todo)) | 44 | n/a (no oracle captured) | n/a (no oracle captured) | n/a (no survey yet) | n/a (no census yet) | n/a (no ratchet yet) | n/a (no diff-baseline yet) | n/a (no oracle captured) | n/a (no oracle captured) |
| files | files | 1 | 1 | n/a (no DOT stage (non-svek)) | 0 / 0 / 1 | n/a (no census yet) | n/a (no ratchet yet) | n/a (no diff-baseline yet) | 0/1 | 1/1 |
| gantt | n/a (no engine (D4 todo)) | 265 | n/a (no oracle captured) | n/a (no oracle captured) | n/a (no survey yet) | n/a (no census yet) | n/a (no ratchet yet) | n/a (no diff-baseline yet) | n/a (no oracle captured) | n/a (no oracle captured) |
| hcl | hcl | 12 | 3 | n/a (no DOT stage (non-svek)) | 1 / 2 / 0 | 1 | 1 | n/a (no diff-baseline yet) | 3/3 | 3/3 |
| json | json | 49 | 50 | n/a (no DOT stage (non-svek)) | 12 / 23 / 15 | 12 | 10 | n/a (no diff-baseline yet) | 50/50 | 50/50 |
| mindmap | n/a (no engine (D2 todo)) | 142 | n/a (no oracle captured) | n/a (no oracle captured) | n/a (no survey yet) | n/a (no census yet) | n/a (no ratchet yet) | n/a (no diff-baseline yet) | n/a (no oracle captured) | n/a (no oracle captured) |
| network | n/a (no engine (D5 todo)) | 3 | n/a (no oracle captured) | n/a (no oracle captured) | n/a (no survey yet) | n/a (no census yet) | n/a (no ratchet yet) | n/a (no diff-baseline yet) | n/a (no oracle captured) | n/a (no oracle captured) |
| object | class | 84 | 80 | 78/80 (98%) | 49 / 12 / 19 | 49 | 34 | n/a (no diff-baseline yet) | 80/80 | 80/80 |
| packet | packetdiag | 6 | 6 | n/a (no DOT stage (non-svek)) | 0 / 0 / 6 | n/a (no census yet) | n/a (no ratchet yet) | n/a (no diff-baseline yet) | 0/6 | 6/6 |
| regex | n/a (no engine (D11 todo)) | 46 | n/a (no oracle captured) | n/a (no oracle captured) | n/a (no survey yet) | n/a (no census yet) | n/a (no ratchet yet) | n/a (no diff-baseline yet) | n/a (no oracle captured) | n/a (no oracle captured) |
| salt | n/a (no engine (D7 todo)) | 51 | n/a (no oracle captured) | n/a (no oracle captured) | n/a (no survey yet) | n/a (no census yet) | n/a (no ratchet yet) | n/a (no diff-baseline yet) | n/a (no oracle captured) | n/a (no oracle captured) |
| sequence | sequence | 1271 | 1141 | n/a (no DOT stage (non-svek)) | 0 / 0 / 1135 | 0 | 0 | 1124 · 1158537 | 1124/1141 | 1132/1141 |
| state | state | 278 | 273 | 267/268 (100%) | 71 / 11 / 191 | 71 | 60 | n/a (no diff-baseline yet) | 269/273 | 273/273 |
| timing | n/a (no engine (D1 todo)) | 126 | n/a (no oracle captured) | n/a (no oracle captured) | n/a (no survey yet) | n/a (no census yet) | n/a (no ratchet yet) | n/a (no diff-baseline yet) | n/a (no oracle captured) | n/a (no oracle captured) |
| unknown | n/a (accounting bucket) | 825 | n/a (no oracle captured) | n/a (no oracle captured) | n/a (no survey yet) | n/a (no census yet) | n/a (no ratchet yet) | n/a (no diff-baseline yet) | n/a (no oracle captured) | n/a (no oracle captured) |
| usecase | description | 351 | 94 | 93/93 (100%) | 2 / 2 / 89 | 25 | 19 | 6 · 253 | 94/94 | 94/94 |
| wbs | n/a (no engine (D3 todo)) | 204 | n/a (no oracle captured) | n/a (no oracle captured) | n/a (no survey yet) | n/a (no census yet) | n/a (no ratchet yet) | n/a (no diff-baseline yet) | n/a (no oracle captured) | n/a (no oracle captured) |
| wire | n/a (no engine (D12 todo)) | 18 | n/a (no oracle captured) | n/a (no oracle captured) | n/a (no survey yet) | n/a (no census yet) | n/a (no ratchet yet) | n/a (no diff-baseline yet) | n/a (no oracle captured) | n/a (no oracle captured) |
| yaml | yaml | 40 | 39 | n/a (no DOT stage (non-svek)) | 7 / 28 / 4 | 7 | 6 | n/a (no diff-baseline yet) | 39/39 | 39/39 |
```
| 27 | 2026-09-20 | T7 gate | With every regenerated artifact on disk: `npm test` 734 files passed / 1 skipped, 20194 tests (drift test included, byte-equal twice), coverage 96.14/91.56/97.26/97.11; typecheck 0; lint 0; build 0. T7 committed as one commit (artifacts only, explicit paths); `fix(pdr-T7)` `993cc2ae` preceded it. | — | no |
| 28 | 2026-09-20 | T8 | The `technical-writer` agent has Read/Write/Grep/Glob only (no Edit, no Bash). It edited README and svg-conformance in place and correctly REFUSED to rewrite `planning/next-missions.md` (~2537 lines) and `planning/mission-index.md` (~87k tokens) through whole-file `Write`, handing back the two blocks with exact anchors. Orchestrator spliced them with a line-anchored script, corrected one clause (chronology's 1/1 is routing/refusal NONE == NONE against the placeholder page; its survey verdict is `diverged`), bumped the Snapshot's "last refreshed" date, prettier-checked all four files, committed as T8. Every number cross-checked by the agent against a report line. | [[architect-reviewer-cannot-write]] generalises: check an agent's tool list against the task's edit shape before routing | yes — route large in-place doc edits to an agent with Edit |
| 29 | 2026-09-20 | close | Final gates on `00b39d73`: `npm test` 734 files / 20194 tests / coverage 96.14/91.56/97.26/97.11, typecheck 0, lint 0, build 0. `npm run format:check` (T8's bar, not a brief gate) warns 45 files repo-wide; intersected with the branch's paths: `svg-parity-dashboard.ts` and `svg-parity-survey.ts` were already warned on main (left alone, pre-existing), while `svg-conformance-census.test.ts` (new) and `svg-parity.test.ts` (clean on main) came out of the worktree pre-commit hook unformatted — lint-staged runs prettier then `eslint --fix`, and the second pass can undo the first. Formatted both, tests unchanged (`style(pdr)` commit). | not a gate; fixed because it is ours | no |
| 30 | 2026-09-20 | post-close | Maintainer asked that chronology read as "plantuml-ts only" on the dashboard and that D8 get a word for it. Verified first: upstream has NO `chronology` package anywhere in its history and `PSystemBuilder.java:184` has had the factory commented out since the "remove DOT and Jcckit diagrams" commit, so the type genuinely exists only in this port. Implemented data-driven, never by type name: `parity-dashboard-inputs.ts#jarUnsupportedCountsOf` reads each cached golden's 4 KB head for the whole-element `PSystemUnsupported.java:62` banner (anchored like the gates' `isJarErrorPage`); `parity-dashboard-matrix.ts#isPlantumlTsOnly` flags a type when that count equals its whole oracle count (≥1); `columnsFor` then emits `n/a (plantuml-ts only)` for the oracle and all seven comparison cells with no freshness date. D8 text updated with this word AND the four added during execution. Report moved only on the chronology matrix/freshness rows and the preamble sentence. Branch `feat/parity-plantuml-ts-only` off main, merged with a merge commit. | maintainer request; a jar that supports chronology flips it back with no code change | no |
| 31 | 2026-09-20 | post-close | Maintainer: "for all the diagrams with no oracle captured, go capture the oracle so that we know where we land." Scope taken as the eleven typed buckets (c4, ditaa, ebnf, gantt, mindmap, network, regex, salt, timing, wbs, wire; 912 fixtures); `unknown` (825 unclassified, D7's accounting row) deliberately excluded pending a ruling. Branch `feat/oracle-capture-no-engine-types` off main. Capture with `capture-oracle-cache.ts`, wall-clock: c4 22 s, ditaa 4 s, ebnf 90 s, gantt 550 s, mindmap 295 s, network 6 s, regex 98 s, salt 109 s, timing 256 s, wbs 398 s, wire 39 s; 912/912 captured, 0 jarFailed, 0 renamed; c4 (5) and network (3) also wrote `svek-1.dot` (both carry description-engine sources). | same recipe as T5 | `unknown` exclusion flagged |
| 32 | 2026-09-20 | post-close | Measured all 912 through the gates' seams BEFORE pinning; the distribution was NOT uniform and split into cohorts, each with a read mechanism: (a) 848 no-engine sources: jar types them, this port's `DiagramRegistry#resolve` has zero candidates and returns the error SENTINEL (`dispatcher.ts:342`), a page with no PSystemError banner → routing `known-misroute`, refusal `ok` (both sides "render" by the gate's definition, exactly as upstream's `PSystemUnsupported` does); (b) 119 timing sources opening with `@startuml`: all ten legacy-UML factories refuse, class's refusal draws the banner page → `known-gap` (TimingDiagramFactory unported, `PSystemBuilder.java:187`); (c) c4 2: preprocessor defect — a user `!function` called with nested call arguments resolves to the wrong signature (`ReversePolishInterpretor.ts:74`; ShuntingYard arity), reproduced in isolation, filed as `.agent-notes/tim-nested-call-argcount.md`; (d) network 2: legacy `(*) -->` activity (`CommandLinkActivity.java:73`), the activity plugin ports activitydiagram3 only; (e) timing 1: STATE parser accepted a timing source instead of refusing it; (f) gantt 1: empty `@startgantt` → our "Empty description" page vs the jar's empty canvas; (g) 42 jar error pages; (h) 22 agree. Pinned additively: routing 3424/987/73 over 4484 (censused 986), refusal 4484/73/237/4247, gaps 9→133, non-activity defect still exactly nuvoja. `git diff --numstat` 10011/10157 insertions, ONE deletion each. Eleven freshness sentinels added (ditaa's is the PNG the jar writes under the `.svg` name — `PSystemDitaa extends DirectOsDiagram`). Three gates 1324/1324. | measure the distribution first, again | (c) is a real port defect to fix |
| 33 | 2026-09-20 | post-close | Dashboard rule added: a bucket whose engine cell is `n/a (no engine (Dn todo))` keeps its real oracle count but repeats that reason in every comparison cell (`parity-dashboard-matrix.ts#noEngineColumn`). Reason: "routing 0/265, refusal 265/265" beside "no engine" would read as measurements of a port that does not exist; the oracle count is what the future port mission starts from. The ten no-engine types also join `NON_SVEK_TYPES` (all `TitledDiagram`/`DirectOsDiagram` upstream; c4 stays out as a description diagram). | D8: a cell must say the true reason | no |
| 34 | 2026-09-20 | post-close | Maintainer asked where the 825 `unknown` fixtures map. Captured all 825 (0 jarFailed, 14 renamed) and measured through the gates' seams: jar types them 285 CLASS / 225 DESCRIPTION / 77 NONE / 64 NWDIAG / 52 SEQUENCE / 48 ACTIVITY / 26 jar-error / 13 TIMING / 8 FILES / 7 BPM / 7 HELP / 5 STATE / 4 GIT / 3 HCL / 1 FLOW; 573 agree with this port, 226 disagree over ~40 combinations, 111 error here on a rendered source. NOT pinned: ~95 rows are one-mechanism cohorts but ~130 need per-family adjudication with cited commands — mission-sized. Captures parked at `test-results/dot-cache-unknown-2026-09-20/` (gitignored) with the measurement JSON; full tally in `.agent-notes/unknown-bucket-mapping.md`. Gates stay green because the parked tree is outside `dot-cache/`. | scope decision handed to the maintainer with numbers | yes |
