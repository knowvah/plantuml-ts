# Decision Journal — S1L Tail Diagnosis

Append one row per non-trivial judgment call. "Non-trivial" means a reasonable
engineer might have chosen differently. Append during execution; never rewrite
an existing row.

| Date | Task | Decision / observation | Rationale | Needs review? |
|------|------|------------------------|-----------|---------------|
| 2026-08-06 | planning | Mission scoped diagnosis-ONLY; fixes deferred to a separate mission planned from its output. | Batch 2+ task boundaries depend on mechanisms that do not exist yet; batching on classifier labels would assign the wrong files to the wrong agents. Maintainer decision. | no |
| 2026-08-06 | planning | `gafico-37-cuma657` + `nujito-06-neca370` excluded (GH #24, unported `<code>` monospace block). | A creole feature port, not a sizing defect; its blast radius reaches every engine that renders creole. Stays pinned. Maintainer decision. | no |
| 2026-08-06 | planning | `container-cluster` kept whole in T1 rather than split for balance. | The identical-delta pairs (kovaxi/zidebi 0.772, lesori/ravodu 0.2429) are the strongest available shared-cause signal; splitting puts each pair in a separate agent context and destroys it. | no |
| 2026-08-06 | orchestration | Baseline re-measured before launching Batch 1: `measure-description-size-deltas.ts` exit 0, 351 total / 321 conformant / 91.5% / widened 0. Cause histogram matches the brief exactly (container-cluster 9, sprite 5, element-font 5, creole-titled-separator 2, multiline-display 2, other 2, icon 1, plus the excluded latex 2 + emoji-unicode 2). | The mission's exit bar is "still 321/351"; a pre-batch baseline captured on this branch is what that is compared against. Confirms the brief's fixture inventory is current, not stale. | no |
| 2026-08-06 | orchestration | Batch 1 launched as 7 parallel `debugger` agents on Opus, one per bucket, each given its `TN-*.md` as the whole prompt. Write sets are disjoint by construction (one findings file each), so no collapse was needed. | `~/.claude/rules/parallelism.md`: batch dependency-free subtasks with non-overlapping write sets. Opus over Sonnet because the failure mode this mission exists to prevent is a *plausible but fabricated* mechanism, which is a reasoning-depth failure, not a throughput one. | no |
| 2026-08-06 | T4/T5 | `nixura-77-bina738` (labelled `creole-titled-separator`) independently diagnosed by BOTH T4 and T5 to the same origin `src/diagrams/description/parser.ts:107` (missing TYPE0 open-quote multiline phase). Accepted as corroborated, not as one agent's claim. | Two agents with disjoint contexts reaching the same `file:line` is the strongest confirmation available in this mission's design. First empirical proof of ADR-3 — a bucket label cutting across a true mechanism. T8 must move this fixture's group membership. | no |
| 2026-08-06 | T4 | Prior recorded S1L-i mechanism (titled separators = title width + rule height; fix = extend `classifyStripeLine` at `CreoleStripeSimpleParser.ts:95`) REFUTED with evidence for both T4 fixtures. Recorded 62.5/37.6px figures do not reproduce (today 48.125/29.575); `measureNote` never calls `classifyStripeLine` (width byte-identical, 85.431px, with and without every separator line). | ADR-4 required re-verification rather than inheritance. Had the mechanism been inherited, the fix mission would have edited a line that changes neither delta. This is the second time in two days an inherited mechanism proved wrong. | no |
| 2026-08-06 | T4 | `nixura-77-bina738`'s headline delta 1.273091 identified as a SORTED-PAIRING ARTIFACT of the measurement harness (`\|1.930035 DB2w − 0.656944 DB1w\|`), not any single node's error; largest real node error is UC1 height 1.652575in. | A fix mission targeting 1.273091 would hunt a number no node carries. Flagged for T8 so the re-partition records true per-node error, and flagged as a possible harness-wide reporting issue affecting other fixtures' headline deltas. | **yes** |
| 2026-08-06 | T4/T6 | CONTRADICTION on `xufexu-38-fola855` ↔ `pivudu-29-pele178`. T4 asserts shared cause (predicted 5px, matching pivudu's pin). T6, which owns pivudu, jar-probed the classifier truth table and reports the pivudu fix alone moves xufexu **0px**: xufexu's rules `--`/`==`/`==toto==` are rejected by our `isCreoleHrLine`, and its jar body decomposes as `5x13 + 3x8 + 17 = 106` (titled rules cost 17px, untitled 8px; ours knows only `----`/`....` at a flat 8). | Resolved as compatible-but-mis-partitioned: `xufexu` is a TWO-mechanism fixture requiring both the `measureNote` height path (`leaf-sizing.ts:224`) and the classifier/titled-height gap (`leaf-sizing-text.ts:45` + `classifyStripeLine`); only the pair moves it. T4 measured the first and generalised; T6 measured the second. Neither record is wrong; the shared-cause LABEL is too coarse. T8 must not merge these two into one group on T4's link alone. | **yes** |
| 2026-08-06 | orchestration | Agents forbidden from running `npm test` / `npm run build` and from re-running the whole-corpus measure script; given the orchestrator's baseline JSONL instead. Probe filenames namespaced `t<N>-` in `scripts_scratch/`. | Seven concurrent vitest runs on one shared worktree produce worker races and gitignored-asset contention (a known trap in this repo), and seven full corpus measurements is ~7x wasted wall-clock for numbers already captured. Gates run once, by the orchestrator, after the batch. Filename namespacing prevents parallel probe collisions in the one shared scratch dir. | no |
