# Decision journal — creole-lexer-unification

Append one row per non-trivial judgment call during execution (per
`~/.claude/rules/autonomous-execution.md`). Empty at mission start.

| When | Task | Decision | Why |
|------|------|----------|-----|
| B1 start | T1 | Spike extracts leaf displays via the PARSE seam (`buildBlockUmls`→`registry.resolve(first.source).parse(first.source)`, walking `ast.nodes[].display`+children), not `captureGraphs`. | Verified `DotInputNode` carries only id/width/height — captured DOT graphs have NO display text, so the task file's "reuse captureGraphs for leaf displays" cannot recompute the stripe width. The parse seam is what `renderSync` itself uses, reached through already-exported fns (`scripts/oracle-corpus.ts` precedent) — faithful, not a rendering reimpl. Pass the stdlib `includeStore` so sprite/`!include` fixtures parse. |
| B1→B2 | — | User chose "Proceed, re-scope targets": gafico-37/nujito-06 dropped from mission targets (they measure neutral, separate root cause); fepuvo-06 widen to be verified vs pin in B3, re-STOP only if it exceeds. README Realistic-outcome + bug sections revised accordingly. | User decision on the T1 gate. |
| B3 exp | T3+ | Scope expanded (user): fixed `CANNOT_DECODE_TEXT` faithfully in `creole-atoms.ts` — added `cannotDecodeText(src)`: http/https raster → `(Cannot decode: <url>)`, `.svg` URL → `(Cannot decode SVG: <url>)` (AtomImg.java:214/218/226/230); data-URI/file stay short (megabyte/security-profile reasons). Result: togeke-15 now CONFORMANT (was pinned); nobiza-91 7.88→0.67in (shrink below its 1.46 pin → clean re-pin); pebace-74 3.62→0.55in but STILL widened past its 0.17 pin. | User chose "expand scope: fix decode text". Verified faithful: togeke exact 0.00, and the clean expected string `Application1 (Cannot decode: …)` @14 measures 476.79px = oracle content EXACTLY. |
| B3 diag | T3+ | STOP: pebace-74's 0.55in residual is a PRE-EXISTING, SEPARATE bug, not the decode text. Instrumented: the box-determining measure is `[Application1 (Cannot decode: …)] as A1` — the port's description parser leaks the `[…] as <alias>` wrapper into `node.display` for the `component [body] as alias` form (proven on synthetic `component [Foo Bar] as B` → node display `[Foo Bar] as B`; jar parses the clean body). The `[`+`] as A1` ≈ +39.6px = the residual. Affects the drawn SVG too (renderer shares the display); un-caught only because pebace has no SVG golden. Was MASKED pre-mission: old raw `<img:URL>` markup ≈ oracle length (pin 0.17). nobiza-91's parallel 0.67 residual = note cannot-decode text measured @13 (NOTE font) vs jar's @14 — also pre-existing/separate. Both are different subsystems (description declaration parser; note font) from this mission. Ruled out width-table residual (clean string measures oracle-exact) and my decode text (togeke exact). | Present to user: accept + re-pin + file the 2 un-masked bugs, vs expand scope a 3rd time into the parser. | T2's code is CORRECT and complete (renderer byte-identical, dot-sync 262/262+90/90, lurupu-11 now fully conformant, 9 fixtures improved) but the `measure` gate exits 2: TWO non-target fixtures widen PAST their pins — `nobiza-91-fimo741` (Δ7.88 vs pin 1.46, +6.42) and `pebace-74-cuca176` (Δ3.62 vs pin 0.17, +3.44). fepuvo-06 stayed exactly at pin (did not exceed). | Root cause (verified, agent diagnosis + my A/B confirmation): both fixtures embed non-decodable external-URL `<img:http…>`. The port's renderer draws the DELIBERATE, DOCUMENTED short divergence `CANNOT_DECODE_TEXT='(Cannot decode)'` (`creole-atoms.ts:181`) vs the jar's long `(Cannot decode: <URL>)`. The OLD sizer accidentally measured the long raw `<img:URL>` markup (via `measureLineWithAtoms`'s no-atom early-return, `creole-atoms.ts:428`), landing near the jar oracle. T2 correctly makes the sizer measure the SHORT text the port renderer actually draws (sizer↔renderer sync — the mission's whole purpose), so the box shrinks far below the long-form jar oracle and blows past the stale pins. This is honest surfacing of a pre-existing divergence, not a T2 regression. Reverting would reintroduce the exact box>ink bug this mission exists to kill; matching the jar's long form was already rejected (megabyte data-URIs). Per user guardrail (pin exceeded) → STOP and ask. | | (1) README STOP list names "a fixture that was NOT a target widens" as a hard stop — `fepuvo-06-rugi981` widens +18.55px/+0.258in (stripe reclassifies its `==foo3==`/`--foo2--` separator lines as HEADING/LITERAL). It is ALREADY pinned at 2.889in so the move likely has headroom, but direction-vs-oracle is unverifiable pre-Batch-2. (2) Targets `gafico-37`/`nujito-06` measure NEUTRAL (0px), NOT the shrink the brief predicted — verified real: their `<color:green>`/`<u:blue>` tags sit adjacent to a decoded `<U+000A>`/`\n`, and `buildStripeAtoms`'s command scanner leaves them literal there too (both lexers AGREE), so the two-lexer-disagreement fix does not touch them. Contradicts the brief premise "same root drives gafico/nujito nodes a+b". lurupu-11 shrinks -197px as predicted (the real win). Aggregate: 347/348 shrink-or-neutral (99.7%), 1 non-target widen. |
