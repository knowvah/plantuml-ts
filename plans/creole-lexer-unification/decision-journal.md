# Decision journal — creole-lexer-unification

Append one row per non-trivial judgment call during execution (per
`~/.claude/rules/autonomous-execution.md`). Empty at mission start.

| When | Task | Decision | Why |
|------|------|----------|-----|
| B1 start | T1 | Spike extracts leaf displays via the PARSE seam (`buildBlockUmls`→`registry.resolve(first.source).parse(first.source)`, walking `ast.nodes[].display`+children), not `captureGraphs`. | Verified `DotInputNode` carries only id/width/height — captured DOT graphs have NO display text, so the task file's "reuse captureGraphs for leaf displays" cannot recompute the stripe width. The parse seam is what `renderSync` itself uses, reached through already-exported fns (`scripts/oracle-corpus.ts` precedent) — faithful, not a rendering reimpl. Pass the stdlib `includeStore` so sprite/`!include` fixtures parse. |
| B1 gate | T1 | STOP after T1; do NOT auto-start Batch 2. Present two spike deviations to the human. | (1) README STOP list names "a fixture that was NOT a target widens" as a hard stop — `fepuvo-06-rugi981` widens +18.55px/+0.258in (stripe reclassifies its `==foo3==`/`--foo2--` separator lines as HEADING/LITERAL). It is ALREADY pinned at 2.889in so the move likely has headroom, but direction-vs-oracle is unverifiable pre-Batch-2. (2) Targets `gafico-37`/`nujito-06` measure NEUTRAL (0px), NOT the shrink the brief predicted — verified real: their `<color:green>`/`<u:blue>` tags sit adjacent to a decoded `<U+000A>`/`\n`, and `buildStripeAtoms`'s command scanner leaves them literal there too (both lexers AGREE), so the two-lexer-disagreement fix does not touch them. Contradicts the brief premise "same root drives gafico/nujito nodes a+b". lurupu-11 shrinks -197px as predicted (the real win). Aggregate: 347/348 shrink-or-neutral (99.7%), 1 non-target widen. |
