# Batch 3 — Re-baseline backlog + ledger + mission-index; full gate

Lock the improvement into the shrink-only ratchet and record the honest
accounting. Single task (docs + pins).

| ID | Description | Agent | Writes | Depends On | Done |
|----|-------------|-------|--------|-----------|------|
| T3 | Re-baseline size-backlog; update ledger + mission-index; full gate | typescript-pro | `oracle/goldens/description/size-backlog.json`, `plans/s1l-leaf-sizing/ledger.md`, `planning/mission-index.md` | T1, T2 | ☐ |

**Exit bar:** full gate green (measure exit 0, dot-sync 262/262 + 90/90, npm
test, typecheck, lint, build); every improved fixture's pin shrunk or deleted;
the new conformant % recorded; ledger + mission-index retire the "sizer↔renderer
creole visible-text unification" follow-on and note the residual causes that
remain (gafico/nujito node c `<code>`; per-atom font-size width).
