# Batch 3 — sweep, re-baseline, close

| ID | Description | Agent | Writes | Depends on | Done |
|----|-------------|-------|--------|-----------|------|
| T6 | Re-baseline note-order; journal summary; retire note-leaf-model Batch 3; next-missions; notes | typescript-pro | `plans/leaf-draw-order/*`, `plans/note-leaf-model/{README,decision-journal,batch-3/overview}.md`, `planning/next-missions.md`, `.agent-notes/leaf-draw-order.md` — no `src/` | T5 | [ ] |

## Batch exit bar

1. `baseline/note-order.txt` re-captured; `--check-order` against the
   ORIGINAL T1 baseline (kept as `baseline/note-order.t1.txt`) reports
   exactly the T4 moved set.
2. Every D1–D6 has an evidence row in the journal; the README's status
   checkboxes are all `[x]`; the session summary is written.
3. `plans/note-leaf-model` states that its Batch 3 was retired into this
   mission (with the commit range); `planning/next-missions.md` updated.
