# Decision Journal — si11b-bootstrap-sprite-splitting

Append one row per non-trivial judgment call. "Non-trivial" means: a
reasonable developer might have chosen differently.

Also log here: quality-gate results per batch, any brief line-number
correction, every measurement the brief asks you to record, and every STOP
with its full output.

**T1 must record the measured gzip size of the emitted name list.**
**T6 must record the measured payload — T7 quotes it rather than the brief's
~98.7% projection.**

| Date | Task | Decision | Rationale |
|---|---|---|---|
| 2026-07-31 | planning | Mission brief created | SI11b registered as a tracked row when SI11a closed; planned the same day |
| 2026-07-31 | planning | **ADR-1 overturned the mission's founding premise** | SI11a's notes and the SI11b index row both stated this mission "requires transforming a vendored file" and a carve-out from SI5b's checksummed-copy rule. Checking the code showed fragments can be DERIVED into the package tree exactly as SI11a derives `.remote.js` and its asset copy. The vendored file is never written and **`vendor-stdlib --verify` needs no change** — which removes the reason SI11b was called the dangerous half. Method rule 2 applied to the mission's own premise |
| 2026-07-31 | planning | **ADR-2 is an allowlist, not a denylist — and the reason is measured** | Only **8 of 34** bundles carry a `license` field in `assets/stdlib.manifest.json`. `bootstrap1.13.1` is `"MIT License"`; **`awslib14` carries NO license field**, its CC BY-**ND** status living only as prose in `packages/stdlib-aws/LICENSES.md`. A denylist would therefore fail **OPEN** on precisely the bundle where deriving a modified work voids the grant |
| 2026-07-31 | planning | **ADR-3 INVERTS SI11a's ADR-3, on measurement** | All 2,078 bootstrap sprite names are unique, lowercase and filesystem-safe — **zero uppercase**, versus 890 of 891 `awslib14` paths carrying uppercase. So path-by-convention works here where it could not there. Measured: name list **7,289 B gzip** vs key→path map **12,201 B**. That 40% saving matters disproportionately because the manifest floor DOMINATES the payload at small N (a 3-sprite diagram: ~8.8 KB vs ~13.7 KB) |
| 2026-07-31 | planning | Constraint 2 (sync sprite lookup) **verified, not assumed** | `addSprite` is `byName.set` (`sprite-commands.ts:71`), `getSprite` is `Map.get` (`:78`), and `matchSpriteCommand` is called from each diagram plugin's PARSER over preprocessed lines. No await exists at lookup and `renderSync` must stay sync — so loading must be prefetch-driven. Recorded so SI11b's executor does not re-derive it |
| 2026-07-31 | planning | ADR-5 = (a)+(b) on maintainer direction; whole-file fallback REJECTED | Prefetch sees raw source plus fetched include text, **not** macro-expanded text, so a `<$name>` built by a `!define` is invisible to the scan. A whole-file fallback would silently restore the 1.06 MB the mission exists to remove — and would do so for exactly the diagrams most likely to be large |
| 2026-07-31 | planning | **ADR-7 added mid-planning on maintainer request**, and its shape was set by a measurement | Asked for a dev-mode collision warning. Found `SpriteRegistry.skippedColorSprites` already collects diagnostics **and is surfaced nowhere** — asserted in exactly one unit test. Copying that pattern alone would have built a warning no consumer ever sees. Hence two halves: collect on the registry, surface via an injected `onWarning` callback. `process.env` is forbidden in `src/`, and a gated `console.warn` is an uninterceptable, untestable global side effect |
| 2026-07-31 | planning | T1 bundles the splitter AND its generator wiring into ONE task | SI11a split the equivalent work and the emitters shipped **unreferenced** — `npm run build:stdlib` produced nothing, the tests passed anyway because they called the emitter directly, and repairing it cost a stop-condition-12 escalation. T1's acceptance criterion 4 asserts the BUILD emits, not that the function works |
| 2026-07-31 | orchestration | Batch 1 execution plan: T1 ∥ T2 ∥ T3, three typescript-pro agents, disjoint write-sets | T1 owns `scripts/split-sprite-bundle/**` + `scripts/build-stdlib-packages*`; T2 owns `src/core/sprite-prefetch.ts` + one export in `creole-atoms.ts`; T3 owns `src/core/sprite-commands.ts` + `src/index.ts`. No file is written by two agents. Agents are forbidden from git mutations (shared worktree); the orchestrator commits per task after gates |
| 2026-07-31 | T1 | **Manifest is `{name, sprites}`, not a bare array — measured 38,533 B raw / 7,425 B gzip** | ADR-3 cited 34,338 B raw / 7,289 B gzip for a bare name list. Keeping the `SpriteSplitManifest` wrapper the batch-1 contract declares costs ~136 B gzip and stays well under criterion 1's 8,192 B ceiling. The contract shape won over the last 2% of the measurement |
| 2026-07-31 | T1 | Splitter walks the source with the RUNTIME's `matchSpriteCommand`/`scanSvgSpriteBlock`, not its own block parser | A fragment is then parseable by construction rather than by assertion — the same "one answer per question" reasoning ADR-4 applies to the sprite-reference regex |
| 2026-07-31 | T1 | Sprite-split spec exported as standalone `BOOTSTRAP_SPRITE_SPLIT`, not as a new `PackageSpec` field | Method rule 1: `types.ts`'s `PackageSpec` is read by the build script AND two test files. A standalone export leaves the shared type untouched, so no consumer needed tracing further |
| 2026-07-31 | T3 | `src/index.ts` held at exactly 500 lines by reflowing blank spacer lines inside three PRE-EXISTING JSDoc blocks | The file was AT the cap, and two documented option fields had to land in it. Wording was not cut — only blank paragraph separators inside TS-native option docs, which are not ported Java behavior. Alternative (splitting `index.ts`) would have expanded the write-set for a formatting problem |
| 2026-07-31 | T3 | Real bug caught by criterion 1's exact-arguments assertion | `onWarning` was first passed straight to `Array#forEach`, leaking `(value, index, array)` into every consumer call. `toHaveBeenCalledWith` failed on the extra args. This is why ADR-7 required asserting call ARGUMENTS, not call count |
| 2026-07-31 | batch 1 | **Gates green.** typecheck 0 · lint 0 · build 0 · `npm test` **465 files / 11,297 tests** (baseline 463 / 11,266) · `vendor-stdlib --verify` **34,587 files verbatim, unmoved** · size deltas **320/351, widened 0, improved 5** · goldens byte-identical (class 312 / state 59 / object 24 / skin 2) · 54-fixture description ratchet zero-diff | ADR-1 confirmed in practice: the derive-don't-transform design left the vendored gate untouched, exactly as predicted |
| 2026-07-31 | batch 1 | Brief line-number corrections, per push-forward | `T1` cited `build-stdlib-packages.ts:44-70` (now ~48-69) and `sprite-commands.ts:194-232` (now 194-218); `T2` cited `creole-atoms.ts:250` (now 246/251). Content matched in all three. `T3`'s `sprite-commands.test.ts:214` was exact |
| 2026-07-31 | batch 1 → T5 | **Handoff: `packages/stdlib/assets/` has no `.gitignore`** | `stdlib-aws` and `stdlib-tupadr3` each carry one; `stdlib` does not, so the 2,078 derived fragments show as untracked. Left uncommitted deliberately — generated output. Adding the ignore belongs in T5's `packages/stdlib/**` write-set |
| 2026-07-31 | T4 (orchestrator) | **Pre-authorized ONE write-set expansion: T4 may create a new `src/core/sprite-split-stdlib.ts`** | `include-resolver.ts` is at 499 of the hook-enforced 500-line cap, and T4 must add a scan-fetch-assemble path plus a registration helper. The brief's push-forward conditions already name "a ~500-line split" as expected; T4's own stop clause covers splitting a file OUTSIDE its write-set, which this is not — it is extracting T4's own new code out of the file it owns. Deciding it up front rather than paying a guaranteed stop-condition-13 round trip. No other task writes `src/core/sprite-split-*` |
| 2026-07-31 | T4 (orchestrator) | `RenderOptions.sprites` needs NO change to `src/index.ts` | Traced two levels: `render`/`renderAll` call `prepareIncludeStore(source, options)` passing the whole `RenderOptions` object (`src/index.ts:357,372`), and `IncludeWarmupOptions` lives in `include-resolver.ts` — T4's own file. Adding `sprites` there makes ADR-5b's escape hatch flow end to end structurally, with no edit to T3's file |
| 2026-07-31 | T4 | **Orchestrator sent T4's first escape-hatch implementation back.** It folded `options.sprites` into the top-level source as synthetic `<$name>` markup | That applies the hatch at ONE recursion level. Traced two levels: when a user's `!include <bootstrap/bootstrap>` sits inside their own included header file, the nested scan reads the FETCHED text, which never carried the markers — so the option-named sprite is never scanned, never fetched, and never registered. No `SpriteNotBundledError` fires, because the name was never seen: a **silently missing icon**, the exact outcome ADR-5a forbids, occurring inside the valve ADR-5b exists to provide. The agent's stated reason was the 500-line cap; a budget problem is not grounds to narrow an ADR guarantee |
| 2026-07-31 | T4 | Fix: `extraSpriteNames` is a walk-constant on `PrefetchWalk`, unioned with `scanSpriteNames` at every level | `prefetchIncludes`' public signature stays untouched (stop condition 4) — both entry points funnel through a private `startPrefetchWalk`. Line budget solved by MOVING `bundlesFor`/`stdlibContentFor` into `sprite-split-stdlib.ts`, not by cutting comments; `include-resolver.ts` ended at **464** and the deleted doc-comment separators were restored. A nested-include regression test now pins the behavior |
| 2026-07-31 | T4 | Split detection is an explicit symbol marker on the registered bundle, not "the key missed, so maybe it is split" | Criterion 4 requires byte-identical behavior without a split registration. A heuristic would have re-routed a diagram that references `<$foo>` while including a genuinely unregistered bundle, turning `StdlibNotBundledError` into a sprite error |
| 2026-07-31 | T5 | **No `copy-assets.mjs`/`prepack` added, deviating from the `stdlib-aws`/`stdlib-tupadr3` pattern** | Traced: the ROOT generator (`buildSpriteSplits`, T1) already writes `packages/stdlib/assets/` directly, which the sibling packages' generators do not do for theirs — that is why they need a copy step. Adding one here would create a SECOND writer of one directory, the shape of the race that cost SI11a a stop-condition-13 escalation. `packages/stdlib/generated/` already relies on the same no-per-package-script convention |
| 2026-07-31 | T5 | Measured package size 2,934,653 B / 2,099 files; ceiling set at 3.5 MB | ~19% headroom: catches an accidental doubling of the fragment tree while tolerating routine drift |
| 2026-07-31 | batch 2 | **Gates green.** typecheck 0 · lint 0 · build 0 · `npm test` **467 files / 11,313 tests** · `vendor-stdlib --verify` 34,587 verbatim · size deltas 320/351, widened 0 · goldens byte-identical · 54-fixture ratchet zero-diff · caps: `include-resolver.ts` 464, `sprite-split-stdlib.ts` 285, `index.ts` 500 | |
| 2026-07-31 | batch 2 | **Follow-ups recorded, not actioned** (both outside every task's write-set — escalating rather than self-approving, per stop condition 12) | (1) `tests/unit/stdlib-packages.test.ts:343`'s comment says `packages/stdlib` "has no prepack and no assets, so it cannot race" — the "no assets" half went stale when T1 landed. The claim still holds (no prepack ⇒ `npm pack --dry-run` mutates nothing), only the reason is now wrong. (2) `sprite-split-stdlib.ts` now also holds the GENERAL `bundlesFor`/`stdlibContentFor` alias walk, so its name under-describes its contents |
| 2026-07-31 | fix(T4) | **The registration helper shipped unreachable from the public API — caught in orchestrator review, fixed before T6** | `package.json`'s `exports` map has a single `"."` entry, and `src/index.ts:49-54` says so in a comment: that file is the ONLY surface a consumer of the built library can import from, which is why SI11a re-exports `remoteStdlib` there. `spriteSplitStdlib` was not re-exported, and its own doc comment advertised `plantuml-ts/core/sprite-split-stdlib.js` — a path no export map serves. Every unit test passed because tests import by relative path. **This is the same class of failure T1's criterion 4 exists to prevent** (SI11a's emitter that nothing called), one layer out: reachable in-repo, unreachable once published. Fixed with the re-export, a corrected recipe, and a test that imports from the ENTRY POINT rather than the module. `index.ts` held at 500 by folding four neighbouring type-only re-exports into their value exports |
| 2026-07-31 | T6 | **MEASURED — the number T7 must quote.** manifest gzip **7,402 B** + 3 fragments **2,066 B** = **9,468 B** over the wire, against the **1,085,342 B** whole-file baseline read from disk: **99.128% reduction**, on 3 fetches | Ahead of the brief's ~98.7% projection, so stop condition 15 does not fire. The projection assumed a 7,289 B manifest and ~8.8 KB total; the real manifest is 7,402 B gzip and the three real fragments are smaller than the 431 B median assumed |
| 2026-07-31 | T6 | Criterion 1's `<image>` / `data:image/...` assertion was **inherited from SI11a's PNG-form tupadr3 test and is factually wrong for this bundle** | bootstrap1.13.1's sprites are SVG-form exclusively and render as inline `<path>` via `SpriteSvg.ts`/`SvgNanoParser`. Verified by the orchestrator against `oracle/goldens/svg-description/usecase/sprite-svg-bootstrap-0/golden.svg`: **zero `<image>` tags, zero `data:image`, six `<path>`**. T6 substituted a stronger check — rendered path count equals the sum of paths in the three on-disk fragments, every sprite-bearing entity carries one, and their first `d` values are pairwise distinct so shared fallback ink cannot pass. Accepted as a brief correction, NOT a relaxed assertion |
| 2026-07-31 | T6 | ADR-5b proven against a macro the scan genuinely cannot see | A TIM `!function` returning `"<$bi" + "-heart>"` never spells the reference contiguously in source, so `SPRITE_PATTERN_SOURCE` cannot match it. `options.sprites: ['bi-heart']` drives exactly one fetch and the sprite draws — the escape hatch doing the job ADR-5a's limitation creates |
| 2026-07-31 | T6 | Criterion 4 asserted at `prepareIncludeStore`, not through `render()` | `render()` folds every error into an error SVG, so a named error is unobservable there. The public async seam is where a consumer actually sees `SpriteNotBundledError` |
| 2026-07-31 | batch 3 | **Gates green.** typecheck 0 · lint 0 · build 0 · `npm test` **468 files / 11,317 tests** · `vendor-stdlib --verify` 34,587 verbatim · size deltas 320/351, widened 0 · goldens byte-identical · 54-fixture ratchet zero-diff | |
