# Decision journal — edge-label-box-backlog

Appended during execution. One row per non-trivial judgment call: if a
reasonable developer might have chosen differently, it belongs here.

Also record, per task: the before/after DOT EQUAL counts, the
`shape-match-report` census delta, backlog counts, and ratchet pin status. A
task that changed a number without journalling it did not finish.

**Method reminders this mission line has paid for already:**

- Read the Java method body and the constructor that built its inputs. A diff
  says which fixture and attribute, never why.
- Trace two levels, including in your own plan. Three premises here have gone
  stale by stopping at one.
- Never fit a value. Every constant carries its upstream `file:line`.
- Verify status against the tree, not against a brief's checkbox.

| Date | Decision / finding | Rationale | Flagged for review |
|---|---|---|---|
| 2026-08-16 | **Batch 1 baseline captured and verified against the brief.** Backlogs 29/10/9/2 = 50. DOT EQUAL class 680, state 259, component 257, usecase 88, object 76. Census 779 doc-size-exact / **25975 matched-shapes**. | The brief's starting-state table says "25952 rigid-aligned"; the report emits no metric by that name. `doc-size-exact` matches exactly (779), so the 25952 is either a stale capture or a differently-named metric. Adopted my own captured per-fixture baseline as the D4 comparison point rather than the brief's number. | Yes — reconcile or correct the brief's census figure at T13. |
| 2026-08-16 | **Execution plan for Batch 1**: four parallel agents, no shared write targets — T1/T2 `typescript-pro` (sonnet), T3/T4 `debugger` (opus, for multi-path diagnosis). | `rules/parallelism.md` requires the plan be logged rather than presented in autonomous mode. Write-sets verified disjoint before launch: `src/core`+`tests/unit/core` (T1), `scripts`+`tests/unit/scripts` (T2), two distinct `.agent-notes/` files (T3, T4). | No |
| 2026-08-16 | **T1 contract deviation: the cardinality theme fields are OPTIONAL (`?:`), not required** as the task spec's interface sketch had them. Names: `cardinalityFontSize?: number`, `cardinalityFontFamily?: string`; resolver `computeCardinalityFontOverride(styleMap, stereotypeTags?)`. | Required fields broke three pre-existing hand-built `Theme` literals in test files outside T1's write-set (`gtile-text-tiles`, `chart/layout`, `chart/renderers/bar`). `defaultTheme`/`darkTheme` always set concrete values, so anything reading through `resolveTheme()` sees a real number. Spec allowed the spelling to differ; this changes the *shape*, so it is logged rather than assumed. | Yes — **T5/T6/T7 must not invent a `?? 13` fallback literal.** Read the value through `resolveTheme()`, which guarantees it. A literal fallback would be a fitted constant, violating stop condition 1. |
| 2026-08-16 | **T1 compacted 11 blank JSDoc separator lines in `theme.ts`.** Verified by diff: all 11 deletions are bare ` *` / `   *` lines; no content, no citation removed. | `theme.ts` sat at exactly the complexity hook's grandfathered HEAD baseline (565 lines) with zero slack, so the file could not grow without the hook blocking. Push-forward per the brief's "≤3 lines of dead code in a file already in the write-set", stretched to 11 lines of pure whitespace. | Yes — **`theme.ts` has no line budget left.** Any later task adding to it must free space first. |
| 2026-08-16 | **T3 verdict: M3 is tail/head ASSIGNMENT, not emission order. D5's STOP does not fire; T11 may proceed.** Origin `src/diagrams/class/class-dot-edges.ts:168` — `buildDotEdgeAttrs(rel, i, ctx)` is the only one of three calls in that block not passed the `swap` computed at `:163`. Upstream inverts entities and sided fields together (`LinkArg.java:115-117`). | Ruled out with evidence: measurement (`maxSizeDeltaIn 0.0000`, both correct values present on wrong ends), parser attachment, `dotEdgeReversed` (jar emits the same endpoints), emission order (edge sequence and colour-sequence ids identical to oracle; `labelSizeOk` is the only failing check), roles, node identity. | No |
| 2026-08-16 | **T3 scope expansion: M3 is wider than its four fixtures, and moves rendered SVG.** Jar-verified beyond the corpus that `<\|--`, `<..`, `<\|..`, `<--*` fail identically and `<-left-` fails in the opposite direction. A second consumer of the same pairing at `class-edge-label-anchor.ts:198-206` puts multiplicity strings in each other's slots (y=104.034 vs oracle 73.253). | The four backlogged fixtures are near-duplicates of one source line and under-represent the defect. | Yes — **T11's write-set is two call sites, not one**, and D4's census gate is load-bearing there rather than a formality. |
| 2026-08-16 | **T4 verdict: M4 is not "few-px width deltas". It is a bucket of three sub-mechanisms, and its scope is ~13 gate-failing slugs, not 4.** (A+B) strip leading visibility char + prepend a 12px visibility icon, gated on `classAttributeIconSize>0`; (C) `<<x>>` → `«x»`, class engine only; (D) strip the `<`/`>` magic-arrow token and prepend a 13px triangle block. | 22/22 widths reproduced exactly, including 10 rows on 4 slugs predicted before measurement. Every constant carries a Java `file:line` (`VisibilityModifier.java:100-102`, `TextBlockArrow2.java:57,87`, `Display.java:415-418`, `StringWithArrow.java:56-91`). Controlled experiment isolates the gate: `bugeli-63-mixa543` = `gixesa-28-feri809` + `skinparam classAttributeIconSize 0`; gixesa fails, bugeli passes. No fitted constant. | Yes — **T12 is mis-scoped in the brief.** See the row below. |
| 2026-08-16 | **T4 settles the brief's open question: `class-layout-edge-labels.ts:34`'s "theme.fontSize = 14" claim is STALE.** The path measures at 13. | Two proofs: (1) `class-dot-graph.ts:371` builds `{size: ARROW_LABEL_FONT_SIZE}` = 13 → `class-dot-edges.ts:101` → `edgeLabelAttrs`, no other caller; (2) gikipi emits 68 = `floor(measure('+parameter',13)+2)`, where 14 would give 73. Fixed by an earlier mission (G5/C0) whose own comment restated the gap as still open — which is how the stale premise propagated. The comment's *other* half stays true: `skinparam ArrowFontSize` is unported. | Yes — the stale comment must be corrected in T6 (the brief already owed one correction at `:25`; this is a second). |
| 2026-08-16 | **Two mechanisms exist that the brief's M1–M4 partition does not name**, surfaced by T2's triage and corroborated independently by T3. `class/nagega-30-poso418` (`label oracle=43x15 ours=157x15`) is a `!define` macro with no tail/head labels. `class/xamule-03-jeda376`'s `<size:30>to Foo >` (oracle `91x32`, ours `101x15`) is the per-run font change `core/edge-label-box.ts:60-64` already documents as needing a real TextBlock. | The brief asserts four mechanisms cover the 50. They do not. Recording rather than absorbing: these are candidates for the ≤12 residue, each with a named mechanism as exit-bar criterion 2 requires. | Yes — carry into T13's residue accounting. |
| 2026-08-16 | **Batch 1 gates: all green.** `npm test` exit 0 (592 files, 14352 passed, 1 todo; coverage 95.34/90.31/96.91/96.44), `npm run typecheck` exit 0, `npm run lint` exit 0, `npm run build` exit 0. **Census: zero fixtures moved** — all 1074 fixture lines byte-identical to baseline, totals 779/25975 unchanged. **DOT EQUAL unchanged**: 680/259/257/88/76. **Backlog unchanged at 50**, 0 clearable — as designed, batch 1 wires no consumer. | The batch's stated exit bar is zero movement; T1 and T2 add code nothing consumes. Verified rather than assumed: the only census diff was V8 stack-frame depth on a pre-existing unrelated `NestedDiagramRenderer` warning, which vanishes when `^\s+at ` lines are stripped. | No |
| 2026-08-16 | **T12 split into T12a / T12b / T12c — maintainer-approved.** T12a = cause A+B (visibility strip + 12px icon, gated on `classAttributeIconSize>0`); T12b = cause C (`<<x>>`→`«x»`, class engine only); T12c = cause D (magic-arrow token + 13px triangle block, both engines, plus the bare-arrow no-margin sub-case). Original `batch-6/T12-m4-fix.md` deleted; nothing referenced it yet. | The `rules/autonomous-execution.md` "task was mis-scoped and needs to be split" stop condition fired on T4's finding. Split by **cause, not engine**, because T4 verified each cause reproduces its oracle in isolation. Not raised as a blocking question at the time it was found: batches 2-5 are independent of M4, so no work was at risk either way. | No |
| 2026-08-16 | **T12a→T12b→T12c run SEQUENTIALLY, not in parallel.** T11 runs concurrently with the chain. | All three T12 tasks write `src/diagrams/class/class-layout-edge-labels.ts`; one-writer-per-file is not negotiable, so the split buys reviewability and three separate gate runs rather than wall-clock. T11's write-set (`class-dot-edges.ts`, `class-edge-label-anchor.ts`) is disjoint from all three, so it parallelizes. Order chosen by increasing blast radius. | No |
| 2026-08-16 | **The orchestrator owns every `label-size-backlog.json` edit in batch 6; no task edits one.** Each task reports the triage script's `CLEARABLE` list instead. | T11 and all three T12 tasks clear class slugs — four writers on one file. Moving the edit outside also enforces the shrink-only contract externally: a slug leaves only when the instrument says it passes, never because the task that touched it believes it should. | No |
| 2026-08-16 | **Two residues named in advance of batch 6, so they are not mistaken for failures at T13.** (1) `class/xamule-03-jeda376`'s `<size:30>to Foo >` — per-run font change inside a label, the case `core/edge-label-box.ts:60-64` documents as needing a real creole TextBlock (Phase 4h track); the slug may clear its bare-arrow edges and still fail `labelSizeOk`. (2) The description engine cannot represent a mid-string `<<x>>` — its parser route only lifts a post-colon stereotype. | Exit-bar criterion 2 requires every remaining slug carry a named mechanism. Naming these before the work starts prevents them being chased, which is how a "never fit a value" mission gets a fitted value. | No |
| 2026-08-16 | **T5 landed (`ca60cd0f`). Batch 2 complete.** `computeQuantifierBox(text, font, measurer)` → `{lines, reservedWidth, reservedHeight}`. No shield, no `marginLabel`, `Math.floor` (truncation, not rounding), width = max over lines, height = `lines.length * font.size`. | Verified against `camuna-58-veca254`'s oracle: `"customer\n1"` at size 10 → `41x20` (measured 41.0625 → floor 41); `"value"` → `23x10` (23.9375 → floor 23, where rounding would give 24). Both exact. Gates all exit 0; census unchanged at 779/25975. | No |
| 2026-08-16 | **T5's `src/core/` → `src/diagrams/class/` import accepted rather than stopped.** `computeQuantifierBox` imports `splitEdgeLabelLines` from `class-layout-edge-labels.ts`. | The task spec required reusing that splitter but flagged the layering as a stop-if-new. It is not new: `src/core/assemble-svg.ts:10-13` already imports from `../diagrams/description/`, `../diagrams/class/`, `../diagrams/state/`, `../diagrams/json/`. Verified independently by the orchestrator, not taken on the agent's word. | No |
| 2026-08-16 | **Zero-movement for T5 verified structurally, not just by census.** `grep` shows no caller of `computeQuantifierBox` anywhere in `src/` or `tests/` outside its own file (one doc-comment mention in `style-cascade-class.ts:235`), and the commit is +101/−0 so `computeReservedLabelBox` has no deletions. | Stronger than the census alone: the state engine's DOT is byte-identical because the label arm was not touched at all, which is what D1's relocation guarantee depends on. | No |
| 2026-08-16 | **Open question carried to T6/T7: `computeQuantifierBox` does not call `stripCreoleMarkup`, while the label arm does.** | Upstream builds both arms with `.create(font, …)`, i.e. a real creole `TextBlock` that consumes markup in both cases, so in principle the quantifier arm should strip too. Not added in T5: no fixture is known to exercise creole inside a multiplicity, and adding it speculatively is the kind of unmotivated change this mission's discipline forbids. If T6 or T7 finds a quantifier carrying creole, this is the reason. | Yes — watch for it in T6/T7; it is a candidate residue with a named mechanism. |
| 2026-08-16 | **Batch 3 complete. First geometry movement of the mission, and it is clean.** DOT EQUAL class **680→681**, object **76→77**; state/component/usecase unchanged. Census **identical** — all 1074 fixture lines byte-for-byte, totals 779/25975. Backlogs 50→48 (class 29→28, object 2→1). All four gates exit 0 (592 files, 14356 passed). No ratchet pin broke. | Verified by the orchestrator independently of both agents: per-fixture census diff, DOT counts re-run from scratch, and a set-difference of every backlog against the mission's first commit confirming **no slug was added** (shrink-only, stop condition 8). | No |
| 2026-08-16 | **T6 (`2d81e053`) cleared `class/rifuzu-80-nixo780` and `object/fonulu-92-libi014`.** `CARDINALITY_FONT_SIZE` now has one owner (`core/graph-layout-build-edges.ts:19`, re-exported); all three falsified comments corrected. | Both removals verified twice — `dot-sync-report --slug` showing `structurallyEqual=true`, and the triage instrument listing them `CLEARABLE` with 0 mismatched boxes. | No |
| 2026-08-16 | **The class engine has NO role-label path at all.** `Relationship.fromRole`/`toRole` are parsed and stored (`class-relationship-parser.ts:247-248,314`) but have **zero readers** anywhere in `src/`. Upstream's `else if` role fallback (`SvekEdge.java:447-466`) is unported. | T6 spec item 2 required answering this either way. Deliberately not built: it needs new emission plus new render/positioning in `class-geo-builders.ts`/`class-edge-label-anchor.ts`/`renderer.ts`, all outside the write-set, and would move geometry for role-only fixtures. | Yes — unported feature, not a defect in this mission's scope. Candidate for its own mission. |
| 2026-08-16 | **The description engine never emits role labels, and structurally cannot.** `CommandLinkElement.executeArg` (`descdiagram/command/CommandLinkElement.java:320-324`) always calls `withQuantifier(...)`; `LinkArg#withRole` has exactly one caller in the entire upstream tree — `classdiagram/command/CommandLinkClass.java:351`. | So `firstLabel`/`secondLabel` are always Quantifier1/Quantifier2 there. Unlike the class case above, this is not an unported feature — upstream has no role arm for this engine either. Settled, needs no follow-on. | No |
| 2026-08-16 | **T7 (`becf2833`) removed ZERO slugs — correctly.** No description backlog slug has an M1 quantifier defect to clear. | Verified independently by the orchestrator against T2's triage baseline: all **22** description mismatches are kind `label` (main label); **zero** are `taillabel`/`headlabel`. T7 also grepped every description golden's source — only `purevo-74-pamo264` (not backlogged) uses quantifier syntax at all. The brief's plan implied description carried M1 work; it does not. T7 still made the engine faithful (`\n` splitting now matches upstream) under the long-tail rule, which is why the task was not a no-op. | No |
| 2026-08-16 | **T7 deliberately did NOT reuse `computeQuantifierBox` in the dzeta/spacing sum.** | `SvekEdge.getHorizontalDzeta`/`getVerticalDzeta` (`SvekEdge.java:1159-1203`) sum the RAW un-truncated `calculateDimension()`; the `(int)` cast lives only in `appendTable`'s emission (`:504-507`). T5's contract exposes only the floored `reservedWidth`, so reusing it there would inject a truncation upstream never applies. Verified inert: census and DOT counts unchanged, so `nodesep`/`ranksep` did not move. | No |
| 2026-08-16 | **THREE more mechanisms outside the brief's M1–M4, named by T7 for the description residue.** `gevozu-46-sasu860`/`sunuju-01-pote718` — `<latex>` block sizing. `kafexo-72-xupa679` — `skinparam maxMessageSize` word-wrap unported to the main label. `zosuje-43-zebi775` — a `<style> arrow { FontSize 10 }` cascade not applied to *main-label* measurement (all its mismatches are `label`, heights 12 vs 15). | With `nagega` (`!define` macro) and `xamule` (per-run font change), the brief's four-mechanism partition is short by **five**. None is a fudge: each has a named mechanism, satisfying exit-bar criterion 2. But the residue is a different set than planned. | Yes — T13 residue accounting. `zosuje` looks cheap now that T1's arrow cascade exists; it is nobody's task. |
| 2026-08-16 | **D3 IS NOT DELIVERED — stop condition 2 fired, and no remaining task owns the fix.** `computeCardinalityFontOverride` (T1) has **zero callers**; verified by grep, only doc-comment mentions in both engines. T6 and T7 independently hit the same write-set escape: threading a StyleMap to the edge-label path needs `style-map-theme.ts` + `class-dot-graph.ts` (class) and `layout.ts` + `layout-dot-tree.ts` (description), none in any task's write-set. | Both agents stopped and logged rather than expanding scope, which is the correct behaviour their specs demanded. Consequence: `camuna-58-veca254` — M1's flagship fixture, the one in the brief's own evidence table — still fails `labelSizeOk` at `31x13` vs oracle `23x10`. The `\n` split half of M1 IS fixed (its second head label cleared); only the font-size half remains. Inert everywhere else: cardinality and arrow defaults coincide at 13/sans-serif, and only `camuna`/`nafiki` override `arrow.cardinality` in the whole corpus. | **YES — ESCALATED TO MAINTAINER.** Options: add a task to thread the cascade, or accept `camuna`/`nafiki` as named residue. D3 says "thread the resolved font into the quantifier/role box", so accepting residue arguably contradicts a locked decision (stop condition 4). |
| 2026-08-16 | **Removed stray untracked `.agent-notes/.gitignore` and `.agent-notes/.agent-notes/.gitkeep`.** | Serena scaffolding from an agent that mis-resolved `.agent-notes/` as a project root. Belongs to no task's write-set; a `.gitignore` inside the notes directory could later hide notes from git. | No |
| 2026-08-16 | **T7: description engine never emits role labels.** `CommandLinkElement.executeArg` (`descdiagram/command/CommandLinkElement.java:320-324`) always calls `linkArg.withQuantifier(labels.getFirstLabel(), labels.getSecondLabel())`; `LinkArg#withRole` is called ONLY by the class engine's link command (`classdiagram/command/CommandLinkClass.java:351`). Corpus-wide grep confirms: `LinkArg.java`'s `withRole` has exactly one caller. | So `link.firstLabel`/`secondLabel` (this port's `FIRST_LABEL`/`SECOND_LABEL` regex captures) are always Quantifier1/Quantifier2 for description diagrams, never Role1/Role2 — `applyQualifierLabels` has no role arm to write, and T6's role wiring is class-engine-only. | No |
| 2026-08-16 | **T7 landed (`becf2833`).** `applyQualifierLabels` now routes `link.firstLabel`/`secondLabel` through T5's `computeQuantifierBox` (`\n` split, max-line-width, `Math.floor`, no shield/margin) instead of a raw single-line `measureLineWithAtoms` call — matching `SvekEdge.java:447-467`'s emission (`startTailText`/`endHeadText.calculateDimension`, no `2*labelShield`, unlike the main label's `:440-445`). Gates: `npm test` 592/592 files, 14356/14356 tests (+1 todo, one unrelated flake — `sprite-package-files.test.ts` failed once inside the full run, passed in isolation both stashed and unstashed, not touched by this diff); `typecheck`/`lint`/`build` all exit 0. DOT EQUAL unchanged: component 257/263, usecase 88/93. Census unchanged: 779/1074 doc-size-exact, **25975** matched-shapes, **zero fixtures moved**. Backlog unchanged at 10, **0 slugs cleared** — `label-box-triage.ts` before/after is byte-identical (still 22 mismatched boxes, all `label` kind, none `taillabel`/`headlabel`). | Grepped every `oracle/goldens/description/*/input.puml`: only `purevo-74-pamo264` (not backlogged) uses quantifier syntax (`"1"-right-"1"`), single digit, no `\n`, no creole atoms — so the box-formula switch is verified inert on the one fixture it touches, and the ten backlogged slugs' mismatches are entirely `label` (main-label) boxes, not quantifier boxes, so no M1 quantifier defect exists in this backlog to clear. | No |
| 2026-08-16 | **T7 STOP: the true cardinality-font cascade (D3) is NOT wired for the description engine — write-set escape, logged per task instruction rather than silently substituted.** `applyQualifierLabels` measures at `ctx.fontSpec` (the ARROW label font, `edgeFontSpec` built in `layout.ts:348-351`), not `theme.cardinalityFontSize`/`cardinalityFontFamily` (T1). Wiring the real cascade needs a `cardinalityFontSpec` parameter threaded from `layoutDescription` (holds `theme`) through `runLayout` → `buildDotEdges`/`computeGraphSpacing` — i.e. edits to `src/diagrams/description/layout.ts` and `layout-dot-tree.ts`, both outside T7's `link-edge-attrs.ts`-only write-set and outside T6's write-set too (not a shared-file conflict; the task's own "may be shared with class engine" rationale doesn't materialize here, but the STOP instruction is unconditional on the antecedent, so it fires regardless). | Verified inert today, not guessed: default `cardinalityFontSize`/`cardinalityFontFamily` (13, sans-serif, `theme.ts:255-256`) equal the arrow-font default (`ARROW_LABEL_FONT_SIZE`=13); no description golden's `<style>` block overrides `arrow.cardinality` specifically (`zosuje-43-zebi775` overrides bare `arrow`, not `arrow.cardinality`). A future `<style> arrow { cardinality { FontSize N } } }` fixture on a component/usecase diagram would silently mismeasure until this is wired. | Yes — follow-on task: thread `cardinalityFontSpec` through `layoutDescription`→`runLayout`→`buildDotEdges` (2 files, `layout.ts`+`layout-dot-tree.ts`), reusing `computeCardinalityFontOverride` (T1) the same way T6 wires it for the class engine. |
| 2026-08-16 | **T7 scoped `computeLinkDzeta`/`dzetaTexts` OUT — deliberately left untouched.** `SvekEdge.getHorizontalDzeta`/`getVerticalDzeta` (`SvekEdge.java:1159-1203`) sum the RAW (un-truncated) `calculateDimension()` of `startTailText`/`endHeadText`; the `(int)` cast lives only in `appendTable`'s DOT-table emission (`:504-507`), a different call site. `computeQuantifierBox`'s only exposed fields are the FLOORED `reservedWidth`/`reservedHeight` built for that other (emission) call site, so reusing it in the dzeta sum would inject a truncation upstream never applies there. | The mission's own warning ("the description engine's label measurement ALSO feeds `computeGraphSpacing` via `computeLinkDzeta`... be alert for that") is satisfied by explicit non-change plus a citation, not by a blind rewrite: `dzetaTexts` already included `firstLabel`/`secondLabel` in the raw-string sum before this task (a stale doc comment above it claimed otherwise — fixed in the same commit, 1-line-scope pre-existing-violation fix per `pr-workflow.md`), and that path is unmodified, so `nodeSep`/`rankSep` provably did not move — confirmed by the unchanged census and DOT EQUAL counts above. | No |
