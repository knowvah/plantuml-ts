# Batch 7 — close-out

One task. Score the exit bar honestly, record the mechanisms, flip the rows.

| ID | Description | Agent | Writes | Depends On | Done |
|---|---|---|---|---|---|
| T13 | Close out the mission | `technical-writer` | `planning/mission-index.md`, `planning/next-missions.md`, `README.md`, `decision-journal.md`, `.claude/catalog.md`, `src/diagrams/class/class-layout-edge-labels.ts` (comment only, if not already corrected) | T1–T12 | [x] |

**Batch exit:** the exit bar is scored clause by clause with measurements
attached; every remaining backlog slug carries a named mechanism or an explicit
"undiagnosed"; the `mission-index.md` row exists; all four gates green.

## The rule that matters here

**Do not redefine the bar to make it look met.** SI22 closed 2-of-5 clauses
unmet and said so with the numbers. A brief that quietly restates its target to
match what happened is worse than one that misses and names the miss — it sends
the next reader off a false baseline, which is how three premises in this
mission line went stale.
