# Batch 3 — the content shift, then close

| ID | Description | Agent | Writes | Depends on | Done |
|----|-------------|-------|--------|-----------|------|
| T5 | Port `moveDelta(6 - minX, 6 - minY)` | typescript-pro | `src/diagrams/state/state-composite-geo.ts` (+ test) | B2 | [ ] |
| T6 | Sweep, backlog, ledger, close-out | typescript-pro | brief + `.agent-notes/` + `docs/` | T5 | [ ] |

Serial. T6 is measurement and documentation only — no `src/` writes.

## Batch exit bar

1. Inner content sits where jar puts it: ink starting at 6 on both axes.
2. The three named fixtures are fully exact — width AND the 0.261 offset on
   everything to the composite's right is gone.
3. T1's harness `exact` count at its mission high; no composite regressed.
4. state DOT-parity 268/268; all 59 pins hold; no non-state type moved.
5. `.agent-notes/class-ink-shared-offset-groups.md` item (c) updated to
   record the outcome — closed, or re-scoped with the residual named. A
   note whose claim is no longer true is worse than no note.
6. All four quality gates green.
