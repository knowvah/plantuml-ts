# Decision journal

Appended during execution. One row per non-trivial judgment call — if a
reasonable developer might have chosen differently, it belongs here.

| When | Task | Decision | Why | Evidence |
|---|---|---|---|---|
| 2026-08-23 | T1 | Branch created as `feat/routing-conformance` off `docs/routing-conformance-brief` (`0315454c`), not off `f66f6abb` | The brief names that branch. `0315454c` is `f66f6abb` plus the brief-authoring docs commit; it touches only `plans/`, so `src/` and both fixture trees are byte-identical to the stated baseline | `git diff f66f6abb..0315454c --stat` is `plans/` only |
| 2026-08-23 | T1 | `.claude/settings.autonomous.json` NOT installed | The `cp` is refused by the harness — exactly the block the brief predicts ("cannot be written from a session"). The brief also states execution is not gated on it; it only shapes permission prompts | Two refusals, both "Blocked by classifier" |
| 2026-08-23 | T1 | **The routing measurement passes the shared include store, so the pinned count is 79, not 86** | `renderSync` refuses `!include` with no `options.includeStore` (`src/index.ts:213`) and returns `errorSvg`, which carries no `data-diagram-type` — so a *resolution* failure reads as a *routing* answer of `NONE`. Pinning those as `known-misroute` would floor this mission's SLI above zero for a reason no routing change in T2–T7 can move. Confirmed with the maintainer before pinning, per T1's "Ask first" boundary | Measured both ways over all 3158 fixtures: no store 90, store 79, and 79 is a **strict subset** — 11 fixed, 0 broken, all 11 `!include`. The brief's 86 is the no-store number over dot-cache alone and reproduces bucket-for-bucket, so the tree has not moved. `.agent-notes/T1-routing-measurement-hazards.md` |
| 2026-08-23 | T1 | Both trees enumerated by a recursive walk, not a single-level `readdir` | `oracle/goldens/svg-description/{component,usecase}/<slug>/` and `svg-skin/rose/<slug>/` nest one level deeper than every other fixture — 52 of 484 | A single-level walk found 3106 fixtures; the recursive one finds 3158, matching `render-manifest`'s count |
| 2026-08-23 | T1 | `DeterministicMeasurer` passed explicitly rather than taking production's default | Under vitest's jsdom environment the `CanvasMeasurer` default hits the unimplemented `HTMLCanvasElement.prototype.getContext`, and the resulting layout failure becomes an `errorSvg` — another false `NONE` inside the measurement | Routing is measurer-independent, and that is measured, not assumed: 79 with an identical bucket table under either measurer |
| 2026-08-23 | T1 | The `agree` population is checked by ONE aggregate `it()`, not 3079 per-fixture ones; only the 79 pinned misroutes get their own `it()` | AC5 requires the jar→ours bucket table in the failure message, which only an aggregate can build, and 3079 `it()` blocks would cost more in runner overhead than the measurement they wrap. The pinned misroutes keep per-fixture `it()`s — that is the sibling ratchet's shape and it is where per-fixture reporting earns its cost | Sibling shape: `sequence.diff-baseline.ratchet.test.ts:110-235` |
| 2026-08-23 | T1 | A pinned misroute that lands on a *different* wrong engine is `[CHANGED]`, and PASSES | The gated quantity is the SET of misrouting fixtures. Such a fixture has not *newly* misrouted, so failing it would make the gate rise on movement that is not a regression | Stop condition wording: "Any fixture NEWLY misroutes" |
| 2026-08-23 | T1 | `CORPUS_BUDGET_MS = 120_000` | Derived, never fitted: base **15,492 ms** measured in situ under a full `npm test` (v8 coverage, 12 forks on 12 cores), times the **2.70x** 12→22-fork step recorded in `.agent-notes/ratchet-zudize-timeout.md` = ~41,800 ms, then a stated margin because the contended worker also runs the rest of its shard | `--reporter=verbose`, 2026-08-23. No per-fixture budget: `readHead` takes 4 KB, so the 8.26 MB `zudize-61-vomi445` golden costs this gate the same as any other |
| 2026-08-23 | T1 | Quality gates green; write-set clean | `npm test` 16275 passed (coverage 95.46/90.48/96.95/96.53) · `typecheck` · `lint` · `build` all exit 0. `git diff --name-only HEAD~1` returns exactly the two declared files | Suite 60.54 s → 60.94 s (+0.4 s wall-clock) |
| 2026-08-23 | T2 | Order derived by reading each factory's COMMAND LIST, not by name-matching | `activityPlugin` mirrors `ActivityDiagramFactory3` (`:140`), not the same-named `ActivityDiagramFactory` (`:137`): the `:137` factory's commands are `CommandLinkActivity`/`CommandLinkLongActivity` (`(*) --> "step"`), for which this port has no `accepts()` pattern; every pattern it does have is an `ActivityDiagramFactory3` command. So activity sorts AFTER state | `ActivityDiagramFactory.java:75-99` vs `ActivityDiagramFactory3.java:105-150`; `src/diagrams/activity/index.ts:38-50` |
| 2026-08-23 | T2 | `chronologyPlugin` placed at the commented-out `ChronologyDiagramFactory` slot rather than treated as "no counterpart" | The comment fixes its position unambiguously — between `GanttDiagramFactory` (`:183`) and `FlowDiagramFactory` (`:185`), both unported, both after packetdiag (`:159`) and before json (`:190`). T2's "Ask first" boundary covers a plugin upstream has NO counterpart for; this one has a disabled counterpart at a known line, so it is placed and recorded, not escalated | `PSystemBuilder.java:184` |
| 2026-08-23 | **T2 — STOP** | **Reorder implemented, measured, and REVERTED. Nothing landed.** | The gate moves **79 → 469**: 25 fixed, **415 newly misrouted**. T2's AC3 is unambiguous ("STOP — do not re-pin, do not proceed to batch 3"), and two further README stop conditions fire with it: a decisions.md decision (D1) is contradicted by what the code requires, and the residual is closable only by parse-attempt (D2's deferred mission) | Full artifact: `.agent-notes/T2-registration-order-halt.md`; summary in README |
| 2026-08-23 | T2 | Diagnosis — **mechanism A, 262 fixtures** | `SEQUENCE_PATTERNS[0]` (`src/diagrams/sequence/index.ts:20`) is `/->>?|-->>?/`, unanchored and context-free, tested against the first 20 lines. `resolve` (`src/core/dispatcher.ts:249-253`) returns the FIRST accepting plugin, so sequence-at-position-1 gives that regex first refusal corpus-wide | Measured: `sequencePlugin.accepts()` is true for **1351** of 3158 fixtures, **270 of them not sequence diagrams** (152 CLASS, 96 STATE, 12 DESCRIPTION, 10 NONE) — a 20% false-positive rate. 262 of the 270 were correct before |
| 2026-08-23 | T2 | Diagnosis — **mechanism B, 153 fixtures**, which the brief did not anticipate | Upstream has description (`:138`) before state (`:139`); this port had state first. Mirroring that pair alone moves every source both engines accept from state to description | Measured: **153** fixtures accepted by BOTH `descriptionPlugin` and `statePlugin`; the jar calls **152 STATE**, 1 NONE — exactly the observed regression, no residual |
| 2026-08-23 | T2 | Ruled out — **T3's candidate-type filter (D4) cannot rescue the reorder** | `DiagramType.getTypes` maps `@startuml` to `{SEQUENCE, STATE, CLASS, OBJECT, ACTIVITY, DESCRIPTION, COMPOSITE, TIMING, HELP, SPRITES}` — all candidates simultaneously — and `createPSystem` then takes the first whose PARSE succeeds. The filter separates nothing for `@startuml` | `DiagramType.java:197-201`, `PSystemBuilder.java:257-266`. Measured: **3039 of 3158 (96.2%)** fixtures are `@startuml`; the other 119 (`@startjson` 60, `@startyaml` 45, `@startdot` 10, `@starthcl` 4) already route correctly via the typed fast path |
| 2026-08-23 | T2 | Ruled out — **T4's arrow anchoring cannot rescue it either** | T4 narrows `SEQUENCE_PATTERNS[0]` while its AC2 requires `A -> B : msg` to keep matching. But a class relation IS that string | Measured: **217 of the 262 (82.8%)** fixtures mechanism A breaks contain a real arrow in real arrow position — `p1 --> cl2`, `Sally --> Bob`, `ClassA --> ClassB : -var1`. Anchoring removes at most the other 17.2% |
| 2026-08-23 | T2 | Conclusion recorded for the maintainer: **the current registration order is load-bearing**, not merely inverted | Upstream's order is safe *because* upstream breaks ties by attempting the parse; we break them with per-line regexes. Our order is compensating for heuristic over-claim. D1's premise — that the order is an independent variable — is disproven | Both `accepts()` seams would have to become as discriminating as upstream's parse, which is exactly the scope D2 measured as "genuinely large AND separable" and deferred |
| 2026-08-23 | T2 | All four gates green after the revert; tree clean | `npm test` 16275 passed · `typecheck` · `lint` · `build` all exit 0. `src/index.ts` byte-identical to `f66f6abb`; routing gate back to 79 of 79 | — |
