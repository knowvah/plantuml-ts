# Decision journal — stdlib-lock-sharing

| When | Task | Decision | Why | Evidence |
|---|---|---|---|---|
| 2026-08-22 | — | Branch `fix/stdlib-lock-sharing` cut from `main` @ `a9fb1a74` | Brief specifies "from main at or after dceb028c"; a9fb1a74 is the brief commit, a descendant | `git log --oneline -1` |
| 2026-08-22 | — | Execution plan: B0 (T0 solo) → B1 (T1 solo) → B2 (T2‖T3, disjoint write-sets) → B3 (T4 → T5). Agents run no git; orchestrator commits by pathspec. | Matches README batch table; parallelism.md autonomous exception logs the plan rather than presenting it | README batch table |
| 2026-08-22 | T0 | Machine load at mission start: load avg 37.66, `corespotlightd` 97.3% | Timing readings are invalid until it falls to ~0 (three prior missions burned by this) | `uptime`; `ps -Aceo pcpu,comm` |
| 2026-08-22 | T0 | ACCEPTED a permanent env-gated hook (`STDLIB_LOCK_TRACE_DIR`) in `build-lock.ts`, outside T0's nominal write-set | T0's write-set excluded the 8 reader call sites, the helper and the builder; none pass a `now`/`log` override, so no existing seam could observe a real `npm test`. Cost when unset: one `process.env` read + one `!==` check, no fs, no allocation. Per-pid trace files, so no cross-process append race. | Reviewed `git diff build-lock.ts`; probe: disabled 0 files/117.7 µs per call vs enabled 1 file/152.3 µs |
| 2026-08-22 | T0 | Baseline is T0's own measurement, NOT the README's figures | "Reproduce, do not inherit." Acquisitions reproduce (288; 576 vs 573) and the holding side reproduces tightly (max hold 7,524 vs 7,515 ms), but total wait moved −38% single / +39% concurrent — direction flips, so not a scale factor. Reported as machine/day variance rather than reconciled. | `.agent-notes/lsh-T0.md` baseline table |
| 2026-08-22 | T0 | T0 fixed a real methodology bug before trusting any number: `timeouts` counted "did throw" and so caught the lock's own unit tests (`build-stdlib-lock.test.ts:276,392` drive `maxWaitMs: 100`/`150` deliberately). Now counted against a fixed 30,000 ms threshold. | Would have silently poisoned T4's before/after; the hook fires process-wide, not just for the production lock | Single-run reading went `timeouts: 2` → `0` |
| 2026-08-22 | B0 | Batch 0 gates GREEN | npm test exit 0, 61 s wall (vitest 59.59 s), 629 files / 16,063 tests, coverage 95.44/90.47/96.95/96.53. typecheck 0, lint 0, build 0 (3 expected dts notes). `src/` empty, `packages/` empty, render-manifest "0 unexpected". | Load at test start 6.52/13.38/12.77, corespotlightd 0.0% (waited for it to fall from 239%) |
| 2026-08-22 | B0 | NOTED, not fixed: `npm run lint` globs `src tests demo` only — `scripts/` is unlinted, so neither the new harness nor `build-lock.ts` is covered by the lint gate | Pre-existing convention, not introduced here; means the lint gate is weaker evidence for this mission's files than it appears | `package.json` lint script |
| 2026-08-22 | T1 | PRE-AUTHORISED T1 to create one new module under `scripts/build-stdlib-packages/` (name its choice) to hold the reader-registry mechanics | `build-lock.ts` is 394 lines after T0's hook, leaving 106 before the 500-line complexity hook blocks; shared mode plus this file's doc-comment density will not fit. T1's own quality bar already says "extract a NAMED module rather than widening an exemption" — this makes the write-set match that instruction instead of forcing a mid-task stop. | `wc -l` = 394; hook limit 500 |
| 2026-08-22 | T1 | Shared mode built as specified; D1–D4 all honoured, none contradicted | Reader registry extracted to new `reader-registry.ts` (197 lines) as a generic presence-directory primitive, reused for BOTH `.readers` (D1) and `.writer-intent` (D3). `build-lock.ts` lands at exactly 500 lines — at the hook limit, zero headroom. | `wc -l`: 500 / 197 |
| 2026-08-22 | T1 | Stop 2 (unbounded blocking) VERIFIED CLEAR by orchestrator, not just claimed | Three loops exist (`acquireWriterFile`, `drainPresence`, `acquireShared`); all check one `deadline` computed once as `waitStart + maxWaitMs`, so exclusive mode's writer-file wait PLUS drain wait together cannot exceed the budget. A drain timeout releases `lockPath` before throwing, so a failed acquisition leaves no orphaned mutex. | `grep` audit of every loop + deadline check; `build-lock.ts:394,425,471` |
| 2026-08-22 | T1 | Stop 3 (weakened tests) VERIFIED CLEAR by orchestrator | Test count 10 → 21, zero `.skip`/`.only`/`.todo`. The only removed lines are 6 mechanical ones (import list, worker argv, worker import mechanism) — no assertion deleted or loosened. | `git diff` deletions; `grep -c` on `it(` before/after |
| 2026-08-22 | T1 | Node 25 type-stripping broke worker-spawn tests once `build-lock.ts` gained its first relative import; fixed by routing worker imports through `createJiti().import()` | Native TS stripping resolves the entry `.ts` but not its nested `.js`→`.ts` remap, and bare `import 'jiti'` will not resolve from a scratch dir outside `node_modules`. Applied to all three worker sources. Not a weakening — the tests still spawn real processes. | `.agent-notes/lsh-T1.md` |
| 2026-08-22 | B1 | Batch 1 gates GREEN | npm test exit 0, 629 files / 16,074 tests (up from 16,063), coverage 95.44/90.47/96.95/96.53. typecheck 0, lint 0, build 0. `src/` empty, `packages/` empty, render-manifest "0 unexpected". | 60 s wall (vitest 59.13 s) |
| 2026-08-22 | B1 | CAVEAT on the B1 wall-clock: `corespotlightd` was at 247% at test start (my own render-manifest run re-triggered indexing between the settle check and the run) | Reported rather than re-rolled. The reading (60 s) still matches B0's clean 61 s, and contamination inflates rather than deflates, so it is conservative — but it is not a clean reading and is not presented as one. | `ps` at 12:11 |
| 2026-08-22 | B1 | NOTED: the coverage gate proves NOTHING about T1's code — vitest `coverage.include` is `['src/**/*.ts']`, so `scripts/` is unmeasured, exactly as `npm run lint` does not glob it | The 95.44/90.47 figures are unchanged precisely because the new lock code is out of scope. The real evidence for T1 is its 21 named tests, not the coverage number. T4/T5 must not cite coverage as evidence the lock is tested. | `vitest.config.ts` coverage block |
| 2026-08-22 | T2 | Helper defaults to shared via `{ mode: 'shared', ...options }` — spread order lets an explicit caller mode win | D4: the helper opts in for its callers; `acquireBuildLock`'s own library default stays exclusive | `with-stdlib-build-lock.ts:54` |
| 2026-08-22 | T2 | T2 MODIFIED 6 pre-existing helper tests. Orchestrator ADJUDICATED this NOT a stop-3 violation: it is a strengthening. | The old assertions keyed on `existsSync(lockPath)`. Under shared mode that file is NEVER created, so `expect(existsSync(lockPath)).toBe(false)` in the release-on-throw/reject tests would have passed **vacuously** — silently gutting release-correctness coverage while looking untouched. Translating to `readerCount(readersDir) === 0` keeps the teeth. The sync test additionally GAINED `expect(mutexExistedDuringCall).toBe(false)`, proving the mode actually changed. Leaving them alone was the real weakening. | Full `git diff` read line by line; tests 6 → 8, zero `.skip`/`.only`/`.todo` |
| 2026-08-22 | T2 | RESIDUAL RISK (accepted, not fixed): the two-process proof asserts `elapsed < holdMs * 1.6` (640 ms) while spawning two jiti workers. A slow or heavily loaded machine could exceed that budget and fail for spawn cost, not for lock behaviour. | Measured 5/5 pass, 488–496 ms, tightly clustered — not flaky here. The companion overlap assertion (`acquiredA < acquiredB + holdMs`, and the converse) is spawn-cost-independent and is the load-bearing proof, so a timing flake would be visible as a `elapsed` failure only. Flagged to T4 so it does not misread such a flake as a lock defect. | 5 consecutive runs of the file |
| 2026-08-22 | T3 | Builder passes `{ mode: 'exclusive' }` explicitly, redundant-by-intent, with the rationale at the call site | So a future change to `acquireBuildLock`'s default cannot silently propagate to the one caller that `rmSync`s the tree readers are reading | `build-stdlib-packages.ts:293-301` |
| 2026-08-22 | T3 | Fitness function needed NO change — T1's mode option does not alter the call shape it keys on. Proven red/green, not asserted. | Injected a bare `readFileSync(generatedPath(...))` outside `withStdlibBuildLock` into `stdlib-eager-omission.test.ts` → RED (`unlocked canonical-tree access`, 1 failed / 7 passed); reverted → GREEN (8 passed) | T3 report, quoted output |
| 2026-08-22 | B2 | NOTE on parallel-batch hygiene: T3 finished first and its `npm test` showed 2 failures in T2's file, which was mid-edit. Correctly left alone; gate re-run by the orchestrator after BOTH agents finished. | A gate result taken while a peer agent is editing is meaningless — this is why the orchestrator re-runs gates at the batch boundary rather than trusting per-agent runs | T3 report |
