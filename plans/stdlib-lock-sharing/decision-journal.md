# Decision journal — stdlib-lock-sharing

| When | Task | Decision | Why | Evidence |
|---|---|---|---|---|
| 2026-08-22 | — | Branch `fix/stdlib-lock-sharing` cut from `main` @ `a9fb1a74` | Brief specifies "from main at or after dceb028c"; a9fb1a74 is the brief commit, a descendant | `git log --oneline -1` |
| 2026-08-22 | — | Execution plan: B0 (T0 solo) → B1 (T1 solo) → B2 (T2‖T3, disjoint write-sets) → B3 (T4 → T5). Agents run no git; orchestrator commits by pathspec. | Matches README batch table; parallelism.md autonomous exception logs the plan rather than presenting it | README batch table |
| 2026-08-22 | T0 | Machine load at mission start: load avg 37.66, `corespotlightd` 97.3% | Timing readings are invalid until it falls to ~0 (three prior missions burned by this) | `uptime`; `ps -Aceo pcpu,comm` |
| 2026-08-22 | T0 | ACCEPTED a permanent env-gated hook (`STDLIB_LOCK_TRACE_DIR`) in `build-lock.ts`, outside T0's nominal write-set | T0's write-set excluded the 8 reader call sites, the helper and the builder; none pass a `now`/`log` override, so no existing seam could observe a real `npm test`. Cost when unset: one `process.env` read + one `!==` check, no fs, no allocation. Per-pid trace files, so no cross-process append race. | Reviewed `git diff build-lock.ts`; probe: disabled 0 files/117.7 µs per call vs enabled 1 file/152.3 µs |
| 2026-08-22 | T0 | Baseline is T0's own measurement, NOT the README's figures | "Reproduce, do not inherit." Acquisitions reproduce (288; 576 vs 573) and the holding side reproduces tightly (max hold 7,524 vs 7,515 ms), but total wait moved −38% single / +39% concurrent — direction flips, so not a scale factor. Reported as machine/day variance rather than reconciled. | `.agent-notes/lsh-T0.md` baseline table |
| 2026-08-22 | T0 | T0 fixed a real methodology bug before trusting any number: `timeouts` counted "did throw" and so caught the lock's own unit tests (`build-stdlib-lock.test.ts:276,392` drive `maxWaitMs: 100`/`150` deliberately). Now counted against a fixed 30,000 ms threshold. | Would have silently poisoned T4's before/after; the hook fires process-wide, not just for the production lock | Single-run reading went `timeouts: 2` → `0` |
| 2026-08-22 | B0 | Batch 0 gates GREEN | npm test exit 0, 61 s wall (vitest 59.59 s), 629 files / 16,063 tests, coverage 95.44/90.47/96.95/96.53. typecheck 0, lint 0, build 0 (3 expected dts notes). `src/` empty, `packages/` empty, render-manifest "0 unexpected". | Load at test start 6.52/13.38/12.77, corespotlightd 0.0% (waited for it to fall from 239%) |
| 2026-08-22 | B0 | NOTED, not fixed: `npm run lint` globs `src tests demo` only — `scripts/` is unlinted, so neither the new harness nor `build-lock.ts` is covered by the lint gate | Pre-existing convention, not introduced here; means the lint gate is weaker evidence for this mission's files than it appears | `package.json` lint script |
