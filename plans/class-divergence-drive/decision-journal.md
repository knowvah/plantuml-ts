| # | date | task | decision / observation | evidence |
|---|---|---|---|---|
| 1 | 2026-09-21 | T0b | Built render-diff/render-all/pin-diff under `jiti` (not `tsx`, absent from this repo); imported `diffVerdict` from `scripts/svg-parity-survey.ts` directly rather than reimplementing it, since its CLI dispatch is guarded by `import.meta.url === pathToFileURL(process.argv[1]).href` and is provably import-safe; render-all's full 723-fixture class-corpus run took ~8.6s in-process (no worker pool needed); verdict cross-check against `parity-class.json` found 0 mismatches. | `render-diff.mts canuti-20-jotu614` = 9 structural/3 numeric (matches `parity-class.json`'s `firstDiff`/`maxDeltaPath`); `render-all.mts` output vs `parity-class.json`: 0 verdict mismatches across 723 rows; `pin-diff.mts` same-file and parity-vs-render-all both report "0 transitions", exit 0; full vitest run over `tools/` = 3 files / 32 tests passed; `npm test`/`typecheck`/`lint`/`build` all exit 0, unaffected by `plans/` additions (`.agent-notes/cdd-T0b.md`). |
| 2 | 2026-09-21 | T0b | Deviated from the write-set's literal `.test.ts` filenames: renamed the three test files and the mission's own `vitest.config.ts` to `.test.mts`/`.mts`. `.husky/pre-commit` (`lint-staged`) runs typed `eslint --fix` on every staged `*.{ts,tsx,mjs,js}` file; that glob excludes `mts`/`cts`, and no tsconfig's `include` reaches `plans/`, so a `.test.ts` file there throws the same `parserOptions`/typed-rule error `eslint.config.ts` already documents for `docs-site/**`. Fixing the glob or eslint config is outside T0b's write-set (package.json/eslint.config.ts); `--no-verify` is banned. Functionally identical under jiti/vitest either way. | Commit attempt with `.test.ts` failed `.husky/pre-commit` with `@typescript-eslint/await-thenable ... parserOptions set to generate type information`, reported against `pin-diff.test.ts`; lint-staged's own output showed only 4 of 7 staged TS-like files matched `*.{ts,tsx,mjs,js}` (the 3 `.test.ts` + `vitest.config.ts`, not the 3 `.mts` tool files) — confirming `mts` is excluded from that glob. After renaming to `.test.mts`/`vitest.config.mts`, the same commit succeeded; vitest run unchanged at 3 files/32 tests, `tsc --noEmit -p tools` clean. See `.agent-notes/cdd-T0b.md`. |
| 3 | 2026-09-21 | T0 | Baseline recorded at `ac40492c` (+T0b `1e60f4d4`, tools only): survey 412 conformant / 50 structural-match / 261 diverged over 723 (0 errored, 0 timeout; re-run idempotent, only `generatedAt` moved); census deterministic pass 414 zero-diff (2 errors); ratchet 314 pins (311 dot-cache + 3 authored) over 316 golden dirs; DOT 710/711 structurally equal (7 oracle-blind = the ELK 7, 1 `directionOk` fail `besepi-37-rori892`); `readlink oracle/dist/plantuml-oracle.jar` = `/Users/scottseely/git/plantuml/build/libs/plantuml-1.2026.8beta1.jar`; `oracle/pin.json` `plantumlVersion` = `1.2026.7beta11` (D12: recorded, not acted on). `measurements/base.json` = 723 rows, 0 verdict mismatches vs `parity-class.json`, `pin-diff` parity→base = 0 transitions. 7 ELK entries written (each verified: pragma in `in.puml`, no `svek-*.dot`). `npm run parity:dashboard` moved only the two freshness timestamps; the class Matrix/DOT rows are byte-identical. | `npm run svg:survey -- class --out tests/oracle/svg-conformance/parity-class.json`; `npx jiti scripts/svg-conformance-census.ts class`; `npx jiti scripts/dot-sync-report.ts class`; `git diff docs/parity-report.md` (2 lines) |
| 4 | 2026-09-21 | T0 | Three spec-vs-tree corrections, none a D-contradiction. (a) `npm run svg:survey class` as written in README/every close.md writes the LEGACY `tests/oracle/svg-conformance/parity.json` (component+usecase pin), NOT `parity-class.json` — `parseSurveyArgs` (`svg-parity-survey.ts:422-430`) routes any positional arg to `PARITY_OUT`; I overwrote and `git checkout`-restored `parity.json` before noticing. Every close must use `-- class --out tests/oracle/svg-conformance/parity-class.json`. (b) T0's acceptance "docs/parity-report.md's class row's Divergence ledger section lists 7 entries" is unattainable: `scripts/parity-dashboard.ts` has no ledger join at all, and the join that exists (`scripts/svg-parity-dashboard.ts:161-171`, `ledgerRows`) compares the BARE survey slug against `match.id`, so the D10-mandated `svg-class/<slug>` ids render as `_(none matched)_`. D10's ids are kept (locked); the join fix is outside every batch-0 write-set — filed in `planning/next-missions.md` §5, flagged for review. Verified instead: `entries.length === 7`, every id `svg-class/<slug>`, `retired` untouched, both ledger tests' shape checks satisfied. (c) Golden dirs are 316 not 318; the 2 unpinned dirs are authored fixtures owned by `class-usecase-actor.test.ts`, by design — no T38 action. | `npx jiti scripts/svg-parity-dashboard.ts --in tests/oracle/svg-conformance/parity-class.json --out <scratch>` → ledger table `_(none matched)_`; `tests/unit/scripts/svg-parity.test.ts:314` injects `svg-class/some-slug`; `.agent-notes/cdd-T0.md` |
| 5 | 2026-09-21 | T0 | Committing the brief itself hit T0b's row-2 defect: `diagnosis/scratch-render-one.ts` matches `lint-staged`'s `*.{ts,tsx,mjs,js}` and the typed eslint rules throw outside any tsconfig `include`. Renamed the seed to `scratch-render-one.mts` (identical under `npx jiti`; the brief's prose still says `.ts`) and ignored `diagnosis/out/` + `measurements/out/` (render scratch) in the plan's `.gitignore`. The brief lands as its own `docs(cdd)` commit ahead of T0 so every cited task id sits on a tree carrying the brief. | `husky - pre-commit script failed (code 1)` on `@typescript-eslint/await-thenable ... scratch-render-one.ts`, twice; `git diff --name-only HEAD~1 HEAD` after the T0 commit |
| 6 | 2026-09-21 | close-b0 | Batch 0 closed with zero movement: fresh survey 412/50/261 (0 errored/timeout, `parity-class.json` diff = `generatedAt` only, committed per close step 7); census 414 zero-diff; `measurements/b0.json` byte-identical to `base.json`, `pin-diff base b0` and `pin-diff parity-class b0` both 0 transitions; ratchet 314 before and after, no re-pin; dashboard moved only the two freshness timestamps. The ELK 7 (`cadutu-02-lazu601, cirojo-62-dubo306, gokoru-18-daba136, lagudi-03-rucu383, rutefe-49-xeju709, tegefa-14-koxo759, temofi-63-vega763`) remain `diverged` by design (D10) and will never gain an `after Bn` value in `fixtures.md`. Gates: typecheck/lint/build/test all 0; JSON reporter collected 755 files = 755 `tests/**/*.test.ts` on disk (e2e excluded). | `cmp base.json b0.json`; `npm test -- --reporter=json` `testResults.length`; `find tests -name '*.test.ts' -not -path 'tests/e2e/*' \| wc -l` |
| 7 | 2026-09-21 | T1 | Ported `eventuallyBuildPhantomGroups` as a GLOBAL sweep over all registered namespaces, not the spec's per-resolution collector: `CucaDiagram.java:325-336` walks `this.quarks()` (Plasma's append-only registration list, `Plasma.java:55,92-94`), and a collector cannot reach `pidagu`'s late segments, which the EXPLICIT package path creates. Java over spec. `ensureNamespaceChain` no longer ticks the chain; a supplied `counter` now means "this call is `gotoGroup`" and stamps only the innermost segment (`CucaDiagram.java:349-355` is handed ONE quark); `ResolveInput.counter` removed (parser.ts was its only supplier). The `:464` sweep at `getTextBlock` is ported into `finalizeParse`/`startNewPage`. | `xakatu-11-tapu041` 5/0 → 0/0, `pidagu-83-dopu070` 5/0 → 0/0, `vuresa-33-kumu160` 10/3 → 3/3 (residual is an unrendered `<b>` in an edge label, B7); SB1 detector 1/42 → 42/42 agreement; commit `51c86f1f` |
| 8 | 2026-09-21 | T1 | `pin-diff base→t1`: 30 transitions, all SB1 (14 → conformant, 14 → structural-match, 2 diff-count risers); 0 base-conformant fixtures lost; totals 412/50/261 → 426/64/233; DOT still 710/711. Risers `runane-30-vena766` and `vusute-48-xono099` (286 → 291) are EXPLAINED and adopted: every co-present entity now carries the jar's exact uid; the delta is one SURPLUS `class AudioFormat` leaf (`ent0014`, verified by the orchestrator: ours lists ent0014, the jar does not), which positional pairing inflates to +5. Mechanism: a dotted link endpoint naming an implicit package must resolve to that package (`quarkInContext` returns the existing quark, data = the phantom group); `class-namespace-resolve.ts#tryReuseExisting` guards bare names only. Entity-existence defect, not tick order — filed for T37/next-missions, not chased in T1. | entity→uid tables of both SVG pairs; `.agent-notes/cdd-T1.md` |
| 9 | 2026-09-21 | T1 | Write-set grew by two files, both forced by `npm test`: `tests/unit/class/class-container-mute.test.ts` (one assertion encoded the PRE-fix eager order on a synthesized source; corrected to the jar's order with the Java cited) and `docs/catalog.md` (generated, drift-gated; 2 new exports). Within the "tests under `tests/unit/class/`" clause and the generated-file convention; no stop. | `npm test` first run: 2 real failures, both fixed in-commit; second run 754/755 files green |
| 10 | 2026-09-21 | T2 | Ported `getOrderedLinks`/`addLinkNew` (`CucaDiagramFileMakerSvek.java:90-113`, keyed by `Link.java:462-470 sameConnections`) into `class-dot-edge-order.ts`; wired once in `class-dot-graph.ts#buildDotGraph` by reassigning `ast.relationships` on the object `layout.ts` also holds (`effAst`), matching `applySameClassWidthFloor`'s existing mutate-shared-input precedent, so DOT emission and `layout.ts#buildEdgeGeos` (the real SVG draw-order consumer — the spec's `:236/:245` citations were stale and pointed into `computeGroupInheritance`) stay in lock step. `class-dot-graph.ts` split `assembleDotInputGraph` into new `class-dot-graph-assembly.ts` for the 500-line cap. `renderer-uid.ts#assignExact` sorts by `creationIndex`, never array position, so the reorder cannot perturb uids (D7). | bicabi 14/1 → 0/1, kevoda 61/341 → 0/0, tedeba 18/55 → 0/2, momoba 15/44 → 1/0, gujigi 42/595 → 30/576; 7/7 link-comment document-order replay exact; DOT 710/711 unchanged; commit `c3e1f3fd` |
| 11 | 2026-09-21 | T2 | `pin-diff base→t2` (T2 alone): 5 transitions, all SB2 — bicabi/tedeba → structural-match, kevoda → conformant, cobumi 1003 → 1040 and delasa 10807 → 11582 diff-count rises EXPLAINED: both improved structurally (cobumi 27 → 2, delasa 1210 → 90) and the numeric count rose because positional pairing now aligns different elements; delasa also carries SB1 and needs both fixes (its combined post-T1+T2 reading is 40 structural). 0 movers outside the 7, 0 base-conformant losses. Named finding: golden `<!--(reverse )?link A to B-->` comments are a fixture-agnostic order oracle, but our comment carries the fully-qualified dotted id where the jar's carries the bare leaf name (pre-existing comment-text divergence, not an order bug — `.agent-notes/cdd-T2.md`). | `pin-diff.mts base.json t2.json`; `render-diff` tables in `.agent-notes/cdd-T2.md` |
| 12 | 2026-09-21 | merge | `cdd/t2` merged onto T1 as `fbcf3cef` (conflict only in generated `docs/catalog.md`, resolved by `npm run catalog`). Combined tree `measurements/t12.json`: 427 / 66 / 230, 0 base-conformant losses, `pin-diff t1→t12` = exactly the 5 SB2 transitions. T3 runs in `cdd-t3` (branched post-T1); T4 waits for T3 because both write `class-assoc-couple.ts`. | `tsc --noEmit` clean; `render-diff` on the merged tree: xakatu 0/0, kevoda 0/0, bicabi 0/1, delasa 40/11492 |
| 13 | 2026-09-21 | T3 | SB4 instrumentation: mechanism CONFIRMED, fix SITE corrected. Counter dump shows the tick where A1 SB4 said — `mujopi-30-zadi566` stamps phantom classifiers at ci 5 and 6 between namespace `p3`=4 and the first relationship=7; `nijeli-04-ponu844` at ci 33/34 before `REL[9]`=35 where the jar has `lnk33`. But the report's premise "the phantom row is already discarded downstream" is FALSE: the row reaches `ClassGeometry` (mujopi's rendered `data-entity-1="ent0005"` IS the phantom). Suppressing only the tick would leave a `creationIndex`-less classifier and flip `isExact` to the fallback path. Fix: do not materialise the row — `existingGroupAlias` returns an unregistered stub, mirroring `CommandLinkClass.java:326-334` reading `quark.getData()` and never reaching `reallyCreateLeaf` (`atmp/CucaDiagram.java:266-286`). Does not contradict D7. | counter dump of `creationIndex` for both fixtures; `<g>` id tables of `mujopi` jar vs ours |
| 14 | 2026-09-21 | T3 | SB5 instrumentation: mechanism DISPROVED as written (stop 11 procedure: journaled before the new approach). The `GMN` name tick and the note↔host connector are ALREADY burned (`class-notes.ts:213-230`, G2 N15/N68) and the parse-time counter is jar-EXACT on `cejili-77-gepe377`: `a=1, z=2, TIPS=3, TIPS-link=4, GMN=5, note=6, connector=7, b=8, lnk9`. The `-3` appears only AFTER `class-directives-removal.ts#filterRemovedEntities` drops `z` (rank 2) and the purged member-tip note (ranks 3+4); dense re-numbering closes the holes. Upstream `remove`/`restore` is an EXPORT-time skip (`GraphvizImageBuilder#printEntities:350`, `#printGroups:413`, `link:230`) long after `Entity.java:171`/`Link.java:135` burned the ranks. Fix: `computeRemovedRanks` + `ClassGeometry.removedRanks` phantom channel — still a phantom-slot fix, D7 unamended. General to any `remove`/`restore` fixture; `render-all` shows only the three named moved. | AST dump before/after `filterRemovedEntities`; hand-reconstructed jar rank tables for cejili/labele/zuxoxu, all three now conformant |
| 15 | 2026-09-21 | T3 | SB3 (HIGH) mechanism CORRECTED before the fix: `applyDoubleCouple` passed NO counter, so every `(A,B) . (C,D)` fixture failed `isExact` and used `assignFallback`; the report's "two missing apoint name ticks" was the coincidental arithmetic of that fallback (`pibifa`'s `lnk6` = 3 classifiers + 2 circles + 1). Ported the real order from `AbstractClassOrObjectDiagram.java:120-138`: two `getUniqueSequence("apoint")` NAMES, two point entities, two `insertPointBetween` (`:145-148` discarded default `new Link(NONE,NONE)` when no explicit A-B association exists; `:154-172` the two real links), then `point1ToPoint2`; `removeLink` (`:149`) un-burns nothing. A name tick two ranks below its entity cannot ride `phantomSlot`, hence the new `Classifier.apointNameCreationIndex`. | `pibifa-14-leno075` `lnk10..lnk14` and `begico-70-guva302` `lnk16..lnk20` exact (orchestrator re-read pibifa's ids: identical to the jar); 3 TDD cases in `class-assoc-couple2.test.ts` |
| 16 | 2026-09-21 | T3 | `besepi-37-rori892` is NOT an SB3 fixture — A1's reach of 3 is wrong. It has no double couple (single-couple form, stamped by G2 N19) and did not move (32/624). Its residual `+1` starts at `ia_125` and originates at `in.puml:32` `ia_1000042 -up-> ia_123`: a direction-inverted link burns TWO ranks (`Link` ctor + `Link#getInv()`, `abel/Link.java:135,145-146`), modelled as `Relationship.phantomSlot` + `creationIndex`; the couple at `:97` then SUBSUMES that link and `SubsumedLink.creationIndex` (`class-assoc-subsume.ts:57`) preserves only the SECOND rank. Filed as a separate defect for T37 (subsumption of an inverted link). | rank-by-rank reconstruction of every jar gap in `besepi` (`.agent-notes/cdd-T3.md`) |
| 17 | 2026-09-21 | T3 | Landed `8cd238f6`: cejili/labele/zuxoxu → conformant; begico 19→9, pibifa 16→11, mujopi 7→3, nijeli 43→38 structural; besepi unmoved. `pin-diff t1→t3` = 4 transitions, 0 t1-conformant losses, no verdict mover outside the 8. The one rise, `tegefa-14-koxo759` 473→474, is a declared ELK fixture (D10): the jar's flat 32-child ELK SVG vs our 15-child clustered tree makes positional pairing meaningless, and SB4 made its leaf uids jar-exact — an improvement recorded as +1. DOT 710/711 unchanged. Follow-ons filed for T37: the double-couple joining link ignores its `.` arrow token (solid where the jar dashes, pibifa/begico); `-backto-` naming on couple split links; nijeli's `packageFontName`/`classFontColor` skinparams and `childCount` 38 vs 39. | `render-diff` before/after all 8; `dot-sync-report.ts class`; gates all 0, 754/755 files |
| 18 | 2026-09-21 | T3 | STOP 1 EVENT, resolved by push-forward — FLAGGED FOR REVIEW. The corrected mechanisms could not be carried by T3's declared write-set: `Classifier.apointNameCreationIndex` needed `class-classifier-ast.ts` + `class-geo-types.ts` + `class-geo-builders.ts`; `ClassGeometry.removedRanks` needed `class-directives-removal.ts` + `class-directives.ts` + `layout.ts`; the `applyDoubleCouple` signature needed `class-command-containers.ts`; `tests/unit/annotations-parsers-a.test.ts` asserted the now-removed `bajotu` phantom row. Two 500-line splits were pre-authorised (`class-ensure-classifier.ts`, `class-assoc-double-couple.ts`); the other eight files are outside every batch-1 write-set. The agent proceeded and reported; the orchestrator verified (gates green, 0 losses, movers all named, D7 intact) and merged rather than halt, because the branch is review-gated (T38: do NOT merge) and the change is a plain revert of one commit. The maintainer should confirm at review. `class-namespace-resolve.ts` (T1) untouched. | `git diff --name-only 8cd238f6~1 8cd238f6` (18 paths); merge `e8103a31`; `measurements/t123.json` 430/66/227, `pin-diff t12→t123` = exactly the 4 T3 transitions |
| 19 | 2026-09-21 | T4 | Swapped `class-assoc-couple.ts`'s `aEdge`/`bEdge` decor assignment (A-side decor on A→circle's own end, B-side decor on circle→B's own end, NONE at the circle end always) to match the file's own doc comment (`:257-260`), `AbstractClassOrObjectDiagram.java:259-273` and `decoration/LinkType.java:237-242` (`getPart1()`=(decor1,NONE), `getPart2()`=(NONE,decor2)). `renderer-edge.ts#linkIdForSvg` untouched. `pajoka-72-reju527` 4/4 → 0/0 conformant; the other 7 SB8 fixtures byte-identical in diff count (their residuals are D8 paint / GEO4 / note-box, not decor). | render-diff before/after on all 8; `pin-diff t123→t4` = 2 transitions; gates green, 755/756 files; commit `340eae4d` |
| 20 | 2026-09-21 | T4 | Rise on `besepi-37-rori892` 656 → 664 (structural 32 → 30, numeric 624 → 634; verdict `diverged` both sides) EXPLAINED: its couple subsumes `ia_1000042 -up-> ia_123`, declared in the OPPOSITE order from the couple's (A,B); jar ids `ia_1000042-backto-apoint46`/`apoint46-ia_123` show upstream attaches the split edges to whichever entity the ORIGINAL link named first (`AbstractClassOrObjectDiagram.java:259-260`, `entity1real = existingLink.getEntity1()`, no `isInverted()` normalisation, unlike the double-couple `insertPointBetween:152-153`), where this port hardcodes A→circle, circle→B. A separate topology-direction defect on top of T3's inverted-subsumed-rank finding (row 16); both are besepi singletons for T37. A1's three invisible-to-the-gate comment findings did not fall out of the edit and are filed in `planning/next-missions.md` §5. | `.agent-notes/cdd-T4.md`; live render of besepi |
| 21 | 2026-09-21 | close-b1 | Batch 1 closed. Survey 412/50/261 → **431 / 66 / 226** (0 errored/timeout; fresh `parity-class.json` and `measurements/b1.json` agree, 0 transitions between them); census 414 → **433** zero-diff; DOT 710/711 unchanged; no `dotEqual` flip in the re-survey diff. `pin-diff b0→b1` = 39 transitions, every one mechanised: 36 verdict moves all in bucket B1 (ORD1 ×24 incl. 14 `B1 B2` rows that stop at `structural-match` on their GEO5 residual, ORD2 ×3, ORD5 ×3, ORD3/ORD8 pajoka, gufife) and 3 diff-count rises with journaled mechanisms — cobumi/delasa (row 11, positional re-pairing after T2), besepi (row 20, couple topology direction), tegefa (row 17, declared ELK). 0 base-conformant losses. | `<scratch>/pindiff-b0-b1.txt` classified against `fixtures.md` buckets; `dot-sync-report.ts class`; `git diff parity-class.json \| grep dotEqual` empty |
| 22 | 2026-09-21 | close-b1 | Re-pin: 119 fixtures added to `oracle/goldens/svg-class/ratchet.json` (314 → 433) with `source: "cdd-b1"`, each survey-conformant AND census-0-diff AND `dotEqual: true` (the AC3 eligibility rule in `class.golden.ratchet.test.ts`; 0 candidates were ineligible), golden dirs `in.puml` + `golden.svg` copied verbatim and `cmp`-verified; manifest diff is 595 insertions, 0 deletions. Only 19 of the 119 became conformant this batch — the other 100 had qualified before the mission and were never pinned (the un-adopted-drift pattern of memory). Write-set extension, flagged: `tests/oracle/svg-conformance/census-class.json` regenerated (`--json` to the committed path) because the dashboard otherwise reported 414 census-0-diff beside 433 pins — a generated measurement file exactly like `parity-class.json`, which the close does commit; no close.md lists it, so later closes should include it explicitly. Close.md step 7 also says `in.svg`; the golden layout is `golden.svg` (`oracle/goldens/svg-class/README.md`). | `node` pin script output: candidates 119 / eligible 119 / added 119; `git status oracle/goldens/svg-class` 119 new dirs; `docs/parity-report.md` class row |
| 23 | 2026-09-21 | close-b1 | Gate fix (attempt 1 of 2, one check): the first `npm test` failed 3 suites, all corpus-completeness pins reacting to the 119 new golden dirs (memory: a new corpus tree trips two gates). (a) `routing-conformance` and `refusal-coverage` walk `oracle/goldens/svg-*` and key `goldens:svg-class/<slug>`; 119 rows appended to BOTH baselines, each the clone of its dot-cache twin with `tree: goldens`, `type: svg-class`, `measuredAt 2026-09-21`, `measuredAgainstCommit 340eae4d` — verified first that all 316 pre-existing svg-class golden rows are field-identical to their twins in both files (0 differences), so cloning IS the prior procedure, and `golden.svg`/`in.puml` are `cmp`-identical to the cache. `git diff --numstat`: 1310/1 and 1191/1 (the 1 deletion each is the `$comment` line, appended to). Counts re-derived, not bumped: refusal 5309→5428, rendering 5040→5159 (erroring 269, jar-error 99, gaps 198, activity 39 unchanged); routing agree 4157→4276, total 5309→5428 (misroutes 1053, jar-error 99, censused 1052 unchanged). (b) `dot-sync-fixtures.test.ts`: `exposant-01-class` is a cache fixture with no `tests/visual/data/class.json` row (creole-exposant-port); its new golden dir makes `enumerateFixtures('class')` list it as authored — expected list extended with the citation. All three suites green (1406 tests). Baseline JSONs and these three test files are outside close.md's write-set (pre-authorised: test files; baselines are the gates' own pins, orchestrator-only). | `<scratch>/test-b1.log` failures; `npx vitest run` on the three files: 3 passed |
| 24 | 2026-09-21 | T5 | Widened `Relationship`/`LinkDecor` for A2a M3/M4/M6/M9/M12, AST-only (`b66c9bf6`). Three brief/spec errors corrected against the Java and tested as corrected: `^` in `<||--^` is EXTENDS = the existing `'triangle'` decor (`LinkDecor.java:71`), not a new `'arrowTriangle'` (Java's unrelated `ARROW_TRIANGLE`, glyphs `<<`/`>>`); `-0)-` is `LinkMiddleDecor.CIRCLE_CIRCLED1` → `middleDecor: 'circleCircled1'` (`LinkType.java:135-149`), not `'circleConnect'` (a head decor in a different enum); `UrlInfo`'s field is `.url` not `.href`. Binding for `<||--^`: source = REDEFINES, target = EXTENDS (`CommandLinkClass.java:492-494` builds `new LinkType(decors2, decors1)`; `SvekEdge.java:677-709` reads them crosswise). `dashedBody`/`hidden` are optional so no non-write-set constructor site changes; M9's text capture needed `class-notes.ts` + `class-command-containers.ts` (flagged extension); `renderer-arrowhead.ts`'s exhaustive decor map and `class-layout-edge-labels.ts`'s boolean read were the pre-authorised mechanical adaptations. Four 500-line splits. | `pin-diff b1→t5`: nixema 2/8 → 0/1 and zuramo 142 → 147 (both diverged → structural-match: REDEFINES/DEFINEDBY now resolve instead of `'none'`, so the pre-existing extremity factories draw; zuramo's numeric rise is comparator re-pairing, its residual is `LinkDecor.getMargin()`=30 unwired in DOT sizing, pre-existing); guxode/fitini/cenubi byte-identical; gates green 755/756 |
| 25 | 2026-09-21 | T5 | Declined 5 of the spec's 8 new `LinkDecor` members (`arrowTriangle`, `circleFill`, `circleConnect`-as-head, `halfArrowUp`, `halfArrowDown`): none is producible by `class-arrow-grammar.ts`'s `HEAD1_SAFE`/`HEAD2_CHARS` regex, so the map rows would be dead. Orchestrator measured the class corpus: 0 of 723 `in.puml` use `<<`/`>>`, `\\`/`//`, `@` or `(0`/`0)` at head position; 1 uses `-0)-` as a middle decor (cenubi, now `circleCircled1`). Corpus-inert, but the corpus is not a ceiling (CLAUDE.md): the head-glyph grammar gap is a T37/next-missions follow-on, and T7 dispatches only the decors the grammar can name. Also noted: `dot-sync-report.ts class` now reads 711/712 (was 710/711) because batch 1's golden dir for `exposant-01-class` makes `enumerateFixtures('class')` list it as authored and it is tagged CLASS; the single `directionOk` fail (`besepi`) is unchanged, so the exit bar's "710/711" reads "711/712" from here on. `lizard` finding: `parseRelationshipLine` was already ~69 NLOC but unreported (tokenizer confused by `{`/`<` in string literals); resolved by extracting real helpers (`class-relationship-field-builder.ts`), not by suppression. | `grep -lE` over `test-results/dot-cache/class/*/in.puml`; `dot-sync-report.ts class` "enumerated 774, analysed 719"; `.agent-notes/cdd-T5.md` |
| 26 | 2026-09-21 | T6 | M5 instrumentation (diagnosis mode, before any fix): A5's MEDIUM-LOW trigger hypothesis DISPROVED. Mechanism: `normalizeEdgePoints` keyed `nodeCenter` on `idEntity1FullId`/`idEntity2FullId`, stamped from the RAW arrow-token ids (`class-relationship-field-builder.ts:45`) before `class-command-relationships.ts:107-113` qualifies the endpoints; inside a `namespace` the bare id misses `posMap`, the `SvekEdge.java:643-655` distance verdict is skipped and `reversed` keeps its `dotSwap` seed — the wrong verdict for every DOT-swapped hierarchical edge. Origin `class-edge-geo.ts:368-369`. Ruled out: the report's "qualified lookup fails" (the 3-segment `f1.function.Fox` RESOLVED; bare `Rabbit` missed), anchor substitution (`anchorKeys: []`), and the fallback branch itself (upstream `:646` has none). Same class as the B6/T1 `direction-backlog.json` bug; the sound signal is `Relationship.dotEdgeReversed`. Stop 11 not triggered (declared MEDIUM-LOW, instrumentation requested). | logged tuples on delano edges 0/1: `c1undef:false, c2undef:true, id2:"Rabbit", posKeys:[…,"issues.Rabbit",…]`; candidate verdict `verdictNew:false` matches the golden's increasing-y `d` |
| 27 | 2026-09-21 | T6 | Fix + four carry-only fields (`7fe7f82c`): `normalizeEdgePoints` resolves centres from `rel.from`/`rel.to` re-ordered by `dotEdgeReversed` (upstream `cl1`/`cl2`) so the `:643-655` verdict always applies. Added `EdgeGeo.visibilityIcon` (+`Display.java:415-416` strip, `SvekEdge.java:302,363-374`), `.dashed` from `rel.dashedBody` (`CommandLinkClass.java:495-497`), `.noteBox` (`SvekEdge.java:307-327,440-445`, `Rose.java:65-66` inset), `.constraint` (`:994-1012`, `LinkConstraint.java:82-103`), `.quantifierLines` (`:330-340`) as a fixed `[tail, head]` 2-tuple of `{text,x,y,width}` (spec said `{text,x,y}[][]`; `width` needed for `textLength`; flagged). `t5→t6`: 431/68/224 → 451/52/220, 23 transitions all improvements (7 pure M5 direction → conformant, 12 `B1 B2` GEO5 rows → conformant, tiguma → conformant, 3 → structural-match), 44 falls incl. canuti 9/3 → 3/0 (`entries` at x 168.319 vs jar 168.32; the 3 residual are T7's `data-visibility-modifier` children), 0 losses, 0 rises. DOT 711/712; emitted layout graph byte-identical on all 723 fixtures. | orchestrator re-measured: delano/fexedu 0/0, canuti 3/0, `pin-diff t5→t6` 23, lost 0, rises 0; gates green 757 files |
| 28 | 2026-09-21 | T6 | Measured corrections to the diagnosis: A2a/M4's "MEDIUM risk: `dashed` feeds DOT `style=dashed` at `class-edge-geo.ts:286`" is wrong twice — `:286` is `buildStrokeOverride`'s SVG stroke, and `rel.dashed` reaches NO DOT attribute (0/723 emitted graphs and 0 jar `svek-*.dot` contain the token); the two M4 fixtures coxose/ririlu now emit `stroke-dasharray` id-for-id. camuna's AC values (y 228.853/238.853) are unreachable until its M1 whole-fixture displacement (≈20.5 x / ≈23.9 y) lands; the relative geometry is exact (step 10 = `cardinality FontSize`, x offset 17.75) — reported, not fitted; camuna's SVG is unchanged until T7 reads `quantifierLines`. A2a/M9 is implemented but DORMANT: `CONSTRAINT_SPOT` reserves an EMPTY-label 10×10 spot (`class-layout-edge-labels.ts:370-375`) and only `label` reaches `graph-layout-build-edges.ts:124-128`, so the engine places nothing and `SvekEdge.java:995-996` has no `labelXY`; upstream passes a sized TABLE. Fixing it moves DOT input — forbidden by D2 this batch; follow-on for a layout batch. The jar emits a dashed constraint `<line>` in BOTH links of a pair (gujigi lnk10-13), which `LinkConstraint.java:83-88`'s early return does not predict; the observed behaviour is ported, the mechanism is not guessed. | `setLayoutInputObserver` capture before/after; `grep -c style=dashed …/svek-1.dot`; gujigi layout result `edge-0/2 labelX=undefined`; gujigi golden lnk10/lnk11 |
| 29 | 2026-09-21 | T6 | Write-set extensions, flagged (precedent row 18): `layout.ts` — three carry-only lines at the `buildEdgeGeos` call PLUS a real defect: `shiftEdgeGeo` did not translate the four new fields (icon sat 9 px left / 7 px high — measured); `shiftEdgeExtras` added and the shift cluster moved to new `class-layout-shift.ts` (the "no-op seam gate cannot see consumers" class). `tests/oracle/svg-conformance/render-fixture-class.ts` — `layoutFixtureClass` extracted so geometry fields absent from the SVG can be unit-tested; `renderFixtureClass` calls it. Five pre-authorised splits (`class-geo-*`, `class-edge-*`). Two intermediate red `npm test` runs were machine load (1-min load 33.75 / 38: only `Test timed out` failures, collected count degraded to 755/751); green at load 4.22 with 757 — poll `uptime` before trusting a red whole-corpus gate. Three follow-ons in `.agent-notes/cdd-T6.md`: M9's sized `CONSTRAINT_SPOT` label, cardinality font threading to tail/head ink, the two-line constraint mechanism. | `git diff --name-only 7fe7f82c~1 7fe7f82c` (17 paths); `.agent-notes/cdd-T6.md` |
| 30 | 2026-09-21 | T7 | Rendered A2a M2/M3/M5/M6/M9/M10/M12 + A5 M4 (`e6f840a1`): `DotPath#getMiddle` ported (one de Casteljau subdivision per segment + `BezierUtils` angle helpers) and all four `LinkMiddleDecor` values drawn (`SvekEdge.java:982-988`); visibility icon drawn through `core/skin/VisibilityModifier.ts#getUDrawable` with `fill="none"` because `SvekEdge.java:363-374` passes a `null` background — a first attempt through the member-row renderer filled METHOD shapes solid and was caught by canuti's measurement; `[[url]]` wraps the whole group in one `<a>` (`SvekEdge.java:859-861,990-991`); note-on-link body via `renderer-note.ts#renderNote` unmodified; constraint line read from `LinkConstraint.java:82-103` (`stroke="#000"`, not the edge colour — new; `stroke-dasharray:3,3` confirmed) so A2a/M9 is now HIGH; one `<text>` per quantifier line; hidden links skipped at `renderer.ts` (`SvekEdge.java:835-836`). `t6→t7`: 451/52/220 → 455/55/213, 13 transitions, 0 losses: canuti/cenubi/fitini/gixesa → conformant; gikipi/kutazo(second url fixture)/focaci → structural-match; six rises all on `diverged` fixtures — lipazi/lozego/nuvake (note-on-link children now present but vertex order/paint and the LEFT/TOP-vs-BOTTOM/RIGHT child-order flip of `SvekEdge.java:307-327` are T8's, needing `linkNotePosition` on `EdgeGeo`), camuna/nafiki/rifuzu (correct quantifier lines expose their pre-existing M1 displacement). Flagged extensions: `renderClass(geo, theme)` has no AST access, so `url`/`hidden`/`middleDecor` reach the renderer as three carry-only `EdgeGeo` fields (`class-geo-types.ts`, `class-edge-geo.ts` — T6's files, "never touch" in the spec, unavoidable and minimal), a 1-line hidden guard in `renderer.ts`, `colorsFor` exported from `class-visibility-icon.ts`; `renderer-edge-extras.ts` split. `npm test` was red three times ONLY with worker timeouts at 1-min load 24-133 (isolated re-runs 1380/1380); the batch close runs the full suite on a quiet machine as the gate. | orchestrator re-measured: cenubi/fitini/canuti/gixesa 0/0, guxode 2/2, lipazi 10/9, `pin-diff t6→t7` 13, lost 0; DOT 711/712 |
| 31 | 2026-09-21 | close-b2 | Batch 2 closed. Survey 431/66/226 → **455 / 55 / 213** (0 errored/timeout; fresh `parity-class.json` = `b2.json`, 0 transitions, no `dotEqual` flip); census 433 → **457** zero-diff; DOT 711/712 (the exit bar's "710/711", see row 25). `pin-diff b1→b2` = 38 transitions, every one mechanised: 32 verdict moves — 13 `B1 B2` ORD1+GEO5 rows and 8 pure GEO5 rows → conformant (T6 M5 direction), tiguma/givoli/nadepi/tekena (GEO5), canuti/gixesa/gikipi (LNK2, T6/T7 M2), fitini/kutazo (LNK3 url, T7), cenubi (GEO4 middle decor, T7), nixema/zuramo (LNK6 decors, T5), focaci (B7, a bonus fall: its pre-existing displacement no longer crosses a structural threshold); 6 diff-count rises all on `diverged` fixtures with journaled mechanisms — lipazi/lozego/nuvake (LNK5 note-on-link children now present, vertex order + LEFT/TOP child-order flip are T8's, row 30), camuna/nafiki/rifuzu (LNK10 quantifier lines now correct, exposing their LNK1/M1 displacement, row 28). 0 base-conformant losses. | `<scratch>/pindiff-b1-b2.txt` classified against `fixtures.md` buckets |
| 32 | 2026-09-21 | close-b2 | Re-pin: 24 fixtures added to `ratchet.json` (433 → 457, `source: "cdd-b2"`), each survey-conformant AND census-0-diff AND `dotEqual` (0 ineligible; 0 conformant-only / zero-only unpinned; every existing pin still 0-diff), goldens `cmp`-verified, manifest additive (120/0). Same batch-1 procedure for the corpus-completeness gates: 24 `goldens:svg-class` rows cloned into both baselines (265/1, 241/1 numstat — the 1 is the `$comment` line), counts re-derived: refusal 5428 → 5452, rendering 5159 → 5183; routing agree 4276 → 4300, total 5452. `census-class.json` regenerated (`measuredAgainstCommit e6f840a1`). First full `npm test` on this tree (before the pins) was RED with 3 `Test timed out` failures and 744 of 758 files collected while `uptime` read 1-min load 114 (`mediaanalysisd` has pinned a core for 3 days on top of the suite's own forks) — the confounded-wall-clock pattern, not a code failure; the gate below is the quiet re-run. | `git diff --numstat oracle/goldens/`; `docs/parity-report.md` class row 455/55/213 · 457 · 457 |
| 33 | 2026-09-22 | T8 | Fixed the plain-note body and fold (`75e2bca6`): body is now `Opale.java:149-167 getPolygonNormal`'s order (`moveTo(0,0) → (0,h) → (w,h) → (w,10) → (w-10,0) → (0,0)`, `cornersize` = 10 at `:53`) as a `<path>` at the note style's 0.5 stroke; fold reuses the ported `opaleCorner` (`Opale.java:134-147`), filled with the note background and stroked at the diagram default 1, because `EntityImageNote.java:275-289` draws the body on `applyStroke(ug)` and the corner on the unstroked `ug`. New `renderPlainNote(note, theme) => { entityParts: [body, fold, text], connector? }`; `renderNote` kept as a thin wrapper, so `renderer.ts` needed no edit. Two pre-existing tests that pinned the buggy `<polygon>` output were re-captured against the jar-matching output (`renderer.test.ts`, `class-leaf-fold-render.test.ts`) — within the per-task test clause. | 37 polygon-vs-path and 36 fold stroke-width diffs → 0 across 15 fixtures; xekeje 8/0 → 0/0, nuxoni 8/90 → 0/130, befasi 26/614 → 10/614; `pin-diff b2→t8` 8 transitions, 0 losses; DOT 711/712 |
| 34 | 2026-09-22 | T8 | The 8 risers are all on `diverged`/`structural-match` fixtures and explained: nuxoni/temise/xadado/xoteci — a pre-existing coordinate offset was HIDDEN because the `<polygon>`-vs-`<path>` element-type mismatch short-circuited the comparator before per-coordinate `@d` diffs (memory: diff count is non-monotonic); lipazi/lozego/nuvake (note-on-link) — NEW FINDING: `Opale.java` has two stroke rules; `EntityImageNote.drawNormal` splits body 0.5 / fold 1 (plain notes, correct here), but `Opale#drawU` strokes both at 0.5 for opalised/link notes, and T7's `renderer-edge-extras.ts#renderEdgeNoteBox` calls the plain-note builder, so their folds now read `stroke-width exp=0.5 act=1` and the synthetic `NoteGeo` never threads the link's note colour. lipazi's LEFT/TOP child-order flip (row 30) is unchanged by T8, as predicted. Both note-on-link defects are outside every batch-3 write-set — T37 singletons (`renderer-edge-extras.ts`), filed in `.agent-notes/cdd-T8.md`. | per-slug `render-diff` before/after; `grep stroke-width.*exp=0.5` on lozego/nuvake |
| 35 | 2026-09-22 | T9 | E6 ported (`9018cf12`): (a) a plain note's connector is emitted as its own `<g class="link">` in `renderer.ts`'s edges phase through the same `wrapLink` as ordinary edges (`GraphvizImageBuilder.java:226-229`: nodes printed, then every `Link` incl. the note-host one); (b) a note whose host is a group never opalises — `isOpalisable` (`:133-148`) passes, but `buildImage` (`:245-259`) needs `Bibliotekon#getNode` (`:120-122`), populated only by `createNode` from `printEntity` (`:362`), never for a group. The (b) gate lives in `note-layout-tip.ts`'s `strictUml` dispatch (2-line extension outside the write-set, flagged; no other seam). `renderer.ts` split `renderer-note-dispatch.ts`. `pin-diff t8→t9` = 0 transitions (fogexa 13/2 → 4/0, pecabi/sanixi 27/6 → 5/0, zepeki 3/93 unchanged — its residual is note height, not E6); the six "partial E6" slugs are byte-identical before/after (`git stash` diff): none targets a group and every classifier-targeted note already opalises, so A2b's partial-reach list names a THIRD mechanism, likely `note-opale.ts#resolveOpaleConnector`'s geometry — filed in `.agent-notes/cdd-T9.md`. | render-diff 4 AC + 6 partial slugs; DOT 711/712; gates green 757/758 |
| 36 | 2026-09-22 | T9 | Residual the orchestrator measured on the merged tree and is fixing as T9b in the main tree (T10 is isolated in its worktree): the new link group still carries the NOTE's style (`renderPlainNote` builds it with `NOTE_STROKE_WIDTH` 0.5 and `'4 4'`), no `<path id>`, and a placeholder `<g id="lnk1">`. The jar (fogexa `lnk4`): `data-link-type="association"`, `stroke-width:1; stroke-dasharray:7,7` from `CommandFactoryNoteOnEntity.java:342` `new LinkType(NONE, NONE).goDashed()`, path id `GMN2-dummy` from `Link.java:106-113`'s no-decor form, uid from the rank `renderer-uid.ts` already burns as the G2 N68 phantom. Promoting that phantom touches `renderer-uid.ts` (batch 1) — flagged like row 18. pecabi's remaining path `d` (ours runs to y≈117, jar clips at the cluster top y≈57.5) is batch 4 T13's cluster-edge clip, not T9's. | `render-diff fogexa-30-zupo141` S-diffs: `@id lnk4/lnk1`, `path/@id GMN2-dummy/''`, `stroke-dasharray 7,7/4 4`, `stroke-width 1/0.5` |
| 37 | 2026-09-22 | T10 | Diagnosis (before any fix): the spec's read-set (`AtomHorizontalTexts`/`StripeSimple.java:154-155` HORIZONTAL_LINE) is the description/legend engine's mechanism, already ported as `CreoleHorizontalLine.ts`; a NOTE's `----` is split at the display-line level by `BodyEnhanced2#getArea`/`isBlockSeparator` (`BodyEnhancedAbstract.java:67-77`) and drawn by `#decorate` (`:107-121`) + `TextBlockLineBefore`/`UHorizontalLine` — ported for class bodies, never wired for notes. jovigo: `note-layout-measure-rows.ts#buildTableRow` measured the creole table jar-exact but returned `atoms: []`, so nothing drew (structural-only diff). ponono/sumocu: the brief's wrap-off-by-one hypothesis DISPROVED by an ordered `<text>` sequence diff (only two hunks, both the fixture's `#`-prefixed lines); real mechanism is `LIST_WITH_NUMBER` (`CreoleStripeSimpleParser.java:71,138-145`, `StripeStyle.java:57-66`) unrecognised by `note-layout-measure.ts#matchBulletLine` — filed in `planning/next-missions.md` §5 with the artifact, not fixed (needs cross-line counter state). Nothing was missing under `src/core/klimt/creole/`; no file added there. | `.agent-notes/cdd-T10.md`; `difflib` transcript (`jar[75:76]=['1.']` vs `ours[75:77]=['#','\xa0']`) |
| 38 | 2026-09-22 | T10 | Landed on `cdd/t10` (`17df1530`): `NoteTableDraw`/`NoteTableCell` + separator draw metadata threaded through `note-layout-measure(-rows).ts` → `note-layout-types.ts` → `note-layout-tip.ts` (four files outside the literal write-set, flagged; disjoint from T9's) and drawn by new `renderer-note-lines.ts` (`renderTableCells`/`renderTableGrid` byte-exact against jovigo's jar XML in unit tests). Also fixed a latent shared-probe bug: `ClassifierBodyGeometry.deriveHeightOffsets` hardcodes the dummy block's width to 0, so `TextBlockMarged.java:82`'s `width > 0` guard skips the probe whenever `marginX === 0` (the note caller) and `contentTop` read 0 instead of 4; `appendUntitledSeparatorBlock` now uses `BodyEnhancedAbstract.java:112`'s literal 4 — sodizo 1/10 → 1/0, fomofi 1/1 → 1/0 from that alone. Wiring NOT applied (stop 1, as the brief anticipated): the drop-in wrappers target `renderer.ts:189/192`, which T9 moved into `renderer-note-dispatch.ts` where `renderPlainNote` is called directly — the orchestrator integrates after T9b. kacico is a legend TABLE + tree (a third render path, B7), xicipi's `<line>`s are `<u>`/`<w>`/`<s>` decorations (undiagnosed) — both named, neither chased. `pin-diff t8→t10` 0 transitions; gates green 759/759. | render-diff 8 fixtures before/after; `tests/unit/class/renderer-note-lines.test.ts` (7 tests) |
| 39 | 2026-09-22 | T9b | Note connector finished as a real link (`d8db6596`): edge-default style (`strokeWidth` 1, `'7,7'` — `CommandFactoryNoteOnEntity.java:342` `new LinkType(NONE, NONE).goDashed()`), `<path id>` in `Link.java:106-113`'s no-decor form (`GMN<n>-host`, `GMN<n>` = the name tick one rank below the note's `creationIndex`), and a real `lnkN` uid promoted from the G2 N68 phantom in `renderer-uid.ts#assignExact` (new `'connector'` Ranked variant; two helpers extracted for the complexity caps — flagged extension of a batch-1 file, precedent row 18). New `renderer-note-connector.ts`; `renderPlainNote` no longer builds a connector at all. FLAGGED APPROXIMATION: entity1/entity2 order (`:344-357`, host-first for RIGHT/BOTTOM, note-first for LEFT/TOP) is recovered from the routed spline's endpoints against the note bbox (`noteIsConnectorSource`) because `NoteGeo` carries no `position`; it holds because `note-layout-groups.ts#groupEdge` already builds the DOT edge in that order, but threading the position is the faithful shape — T37/next close if any fixture disagrees. Pre-existing hand-built `renderer.test.ts` cases that asserted note-first-always and `'4 4'` were corrected to the jar rule. | fogexa 4/0 → 0/0 conformant; pecabi/sanixi 5/0 → 1/0 (residual = T13 cluster clip); `pin-diff t9→t9b` 1 transition, 0 rises, 0 losses; DOT 711/712; gates green 758/759 |
| 40 | 2026-09-22 | merge | `cdd/t10` merged onto T9b as `cb5ba2a7` (conflict only in generated `docs/catalog.md`; `note-layout-tip.ts` auto-merged: T9's 2-line guard + T10's field threading). T10's drop-in wrappers could not be dropped in: T9 moved the note dispatch to `renderer-note-dispatch.ts` and T9b removed `connector` from `renderPlainNote`'s return, so the orchestrator adapted `renderPlainNoteWithLines` to `{ entityParts }` and aliased the `WithLines` variants in the dispatch import. Measured: sodizo/jovigo unchanged (plain-note NoteGeo never receives `lineDividers`/`lineTables` — only `note-layout-tip.ts` builders set them) and fomofi 1 → 2 (tip path appends the `<line>` after the text run where the jar INTERLEAVES it per row: `rect path text line text …`). Handed back to the T10 agent, resumed in the main tree, as a `fix(cdd-T10)` commit. | `render-diff` child-order dumps for sodizo/fomofi jar vs ours; `grep lineDividers src/diagrams/class` |
| 41 | 2026-09-22 | T10 | Wiring follow-up landed (`cae310f1`): (a) the divider/table markup is now emitted inside `renderer-note.ts#renderNoteText`'s per-row loop (`renderNoteRowExtra`, the one place with the cumulative `lineTop`), matching `BodyEnhancedAbstract.java:107-121`'s interleave — `renderer-note-lines.ts` holds only per-row/per-cell primitives, avoiding an import cycle; (b) a THIRD `NoteGeo` constructor, `note-opale.ts#buildOpaleNoteGeo:32-58`, had a narrower parameter type that silently dropped `lineDividers`/`lineTables`; it is the first builder tried for any non-strictuml single-link note, and sodizo/jovigo are opalised (zigzag notch in their golden `d`), so neither could move until it was widened — the "no-op seam gate cannot see consumers" class again, as a constructor. `shiftNoteGeo` confirmed correct (the draw metadata is row/table-relative). | sodizo 1/10 → 0/0, jovigo 1/0 → 0/0, fomofi 1/1 → 0/0 (orchestrator re-measured); `pin-diff t9b→t10b` 3 transitions all → conformant, 0 rises, 0 losses; DOT 711/712; gates green 760/760 at load ≈8 |
| 42 | 2026-09-22 | close-b3 | Batch 3 closed. Survey 455/55/213 → **460 / 56 / 207** (0 errored/timeout; fresh `parity-class.json` = `b3.json`, no `dotEqual` flip); census 457 → **462**; DOT 711/712. `pin-diff b2→b3` = 12 transitions, every one mechanised: 5 → conformant (fogexa ENT6/T9+T9b, sodizo ENT13/T10, jovigo table/T10, fomofi ENT6+ENT13/T9+T10, xekeje GEO2/T8), nuxoni → structural-match (GEO2/T8, its numeric residual is the offset T8 unmasked, row 34), and 6 rises all on non-conformant fixtures with journaled mechanisms — lipazi/lozego/nuvake (note-on-link fold stroke + missing note colour after T8, row 34; T37 singleton in `renderer-edge-extras.ts`), temise/xadado/xoteci (GEO2 unmasking, row 34). lipazi re-verified per close.md step 4: 10/9 → 11/29, residual named (row 34) — not byte-exact this batch by design. 0 base-conformant losses. Named residuals this batch: pecabi/sanixi 1/0 (T13 cluster-edge clip), zepeki (note height), kacico (legend table + tree, B7), xicipi (`<u>/<w>/<s>` lines, undiagnosed), ponono/sumocu (`#` numbered lists, filed next-missions §5). | `<scratch>/pindiff-b2-b3.txt` classified against `fixtures.md` buckets |
| 43 | 2026-09-22 | close-b3 | Re-pin: 5 added to `ratchet.json` (457 → 462, `source: "cdd-b3"`: fogexa fomofi jovigo sodizo xekeje), each survey-conformant AND census-0-diff AND `dotEqual`, goldens `cmp`-verified, additive (25/0); every existing pin still 0-diff. Baselines +5 `goldens:svg-class` clones (56/1, 51/1 numstat, the 1 is `$comment`); counts re-derived: refusal 5452 → 5457, rendering 5183 → 5188; routing agree 4300 → 4305, total 5457. `census-class.json` regenerated at `cae310f1`. Two task commits beyond the plan's three (T9b, the T10 wiring fix) — both fix commits referencing their task per the commit-discipline rule. | `docs/parity-report.md` class row 460/56/207 · 462 · 462; `git diff --numstat oracle/goldens/` |
| 44 | 2026-09-22 | T11 | Added `Namespace.url/.color/.usymbol` (`18d501c7`, E4/M3): the `package` command's `[[url]]` group made capturing and both `NAMESPACE_COMMANDS`' already-captured url/colour groups threaded onto the AST via new `setNamespaceUrl`/`setNamespaceColor` (`class-namespace-decorations.ts`, split from `class-container.ts` at the cap). `Namespace.color` is resolved to its bare/`back:` half at PARSE time (unlike `Classifier.color`'s raw-spec convention) because the fill consumer needs only the background half — a deliberate, documented asymmetry. `usymbol` is a copy taken beside the existing `descriptiveContainers` write so T12 never reads `ParseState`. The `<style> package {}` cascade needed NO code: `resolveStyleCascade` is selector-agnostic and resolves `{root,element,classdiagram,package,group}` (`Cluster.java:293`) already — `style-cascade-class.ts` untouched. Found during TDD: a trailing catch-all with no `\s*` before `{` stole the colour capture on `#DDD {` (backtracking); fixed after an isolated regex probe. | 5 fixtures byte-identical before/after; `pin-diff b3→t11` 0 transitions, 0 rises, 0 falls; gates green 760/761 |
| 45 | 2026-09-22 | T13 | A5 M1 fixed (`fa8709f5`, merged `cc40f564`): the existing `spline-clip.ts` (`clipSplineStart`/`clipSplineEnd`, the verbatim `DotPath#simulateCompound` port) is wired into `class-edge-geo.ts#buildEdgeGeos` via new `class-shield-helpers.ts#clipClusterEdgeEnds` — after `normalizeEdgePoints`, `startId`/`endId` follow `matchesFromTo` exactly as `SvekEdge.java:252-258` sets `ltail`/`lhead` post-reversal (`:643-655`) and `:671-672` clips both ends independently; `clusterRects` threaded from `layout.ts` (namespace boxes, `Cluster#setPosition` frame); and into `note-layout-tip.ts#resolveGroupGeos` for `note top of <package>` connectors (the pecabi/sanixi residual rows 36/39 named as T13's). bajotu 1/0 → 0/0 EXACT — the diagnosis's predicted residual was a hand-replay artifact. 5 note-connector fixtures structural 1 → 0. `pin-diff b3→t13` 11 transitions, all inside the 17-slug GEO1 list, 0 losses. Stop 8 fired: on 6 of 17 the clip's crossing segment has a jar-exact leading point, identical cluster rect and node positions, but a trailing cp2/endpoint ~20 px (4 relationship edges) / ~4.9 px (2 note connectors) off, with a structurally identical sibling edge in bejusa clipping exact as the negative control — a per-curve dot-engine spline-shape delta near a cluster boundary, filed `docs/graphviz-issues/18-compound-clip-last-segment-shape-delta.md` + TRACKER line 18, not tuned. Extensions flagged: `layout.ts` (carry `clusterRects`), `note-layout-tip.ts` (+`groupConnectorPoints` for CCN). DOT 711/712. | render-diff before/after all 17 (commit body); orchestrator re-measured bajotu 0/0, pecabi 0/2, runane 0/7; gates green 760/761 |
| 46 | 2026-09-22 | T12 | E3/E4/M3 landed (`598e64cc`, merged `c50a5ee9`): the cluster USymbol is drawn by reusing the already-ported `core/svek/ClusterDecoration.ts` + `USymbol#asBig` through the klimt fragment seam `renderer-usymbol-entity.ts` established (a spike proved it byte-identical for dativu's `<<Node>>` before production code); `[[url]]` wraps the cluster body in one `<a>` inside the `<g class="cluster">` before the decoration (`Cluster.java:337-341,379-382`); colour precedence is group-own override then style (`:316-324,360-362`), `<style> package {}` routed through `theme.colors.elements.package`. The spec's "margins per USymbol" is two overrides, not a table: `ClusterHeader.java:87-94` adds `suppWidth/HeightBecauseOfShape`, only `USymbolNode` (60/5, `:191-199`) and `USymbolDatabase` (h+15, `:172-175`) override `USymbol.java:88-93`'s 0/0; the visible canvas delta is `LimitFinder.java:169-177` (polygon x-pad 10) and `:159-162` recording `USymbolNode.java:90`'s trailing `UEmpty(10,10)` — two new ink rules took dativu 0/196 → 0/0. Extensions flagged: `class-geo-namespace-types.ts` (3 carry fields + `inkShape` widened), `class-geo-builders.ts`, `class-dot-graph.ts`, `renderer.ts`, `class-ink-shapes.ts`/`class-ink-box.ts`; `usymbol-shapes.ts` (leaf `asSmall` icons) was correctly NOT reused. Snapshot is `measurements/t12-usymbol.json` (`t12.json` is batch 1's T1+T2 file). FLAGGED: `skinparam packageBorderColor` DOES recolour the collapsed empty-package leaf in the jar (cocube `stroke:#00F`) and cannot be routed until `ThemeGraphColors.packageBackground/packageBorder` become optional — a D8-sized widening for T18. Keyword-form containers (`rectangle X {}`, sijisi) still do not populate `Namespace.usymbol` (T11 wired only the stereotype branch) — parse-side follow-on. | dativu 6/344 → 0/0, garumi 1/0 → 0/0, dopuzi 1/0 → 0/0, tibatu 16/260 → 0/0, xitobu 5/58 → 0/58; `pin-diff t11→t12-usymbol` 13 transitions + 5 falls, all 18 movers proved by `grep in.puml` (symbol stereotype / inline or style colour / package url); risers diroxo 101 → 181 (structural 4 → 0, `<<cloud>>` ~100-coordinate `UPath` now comparable) and daxeno 116 → 147 (`<<Database>>`) — anti-monotone growth, adopted; 0 losses; DOT 711/712 |
| 47 | 2026-09-22 | T12 | On the merged tree `tsc` failed in T12's own new test (`class-namespace-usymbol-shape.test.ts`: `{ usymbol: undefined }` under `exactOptionalPropertyTypes`, and a partial `{ url }` for `UrlInfo`); the agent's worktree gates must have predated its last test edit. Fixed as `fix(cdd-T12)`: absent key instead of `undefined`, full `UrlInfo` with `tooltip = url` (`Url.java`'s ctor default) — which also exposed that the test's expected `<a>` lacked the `title`/`xlink:title` dopuzi's jar anchor carries. Lesson for the closes: run `tsc` on the MERGED tree before trusting a worktree's gate report. | `npx tsc --noEmit` 2 errors → 0; dopuzi jar `<a … title="http://www.google.com" xlink:title="…">` |
| 48 | 2026-09-22 | T14 | PUSH-FORWARD (task simpler than estimated): every E9 symptom is already absent on the batch-4 tree, so T14 reduced to attribution — the "double draw" and the "phantom `g.entity`" were one object, the phantom `Classifier` `ensureClassifier` materialised for package/namespace link endpoints, removed by T3's `existingGroupAlias` stub (row 13, `8cd238f6`, mirroring `CommandLinkClass.java:326-334`/`CucaDiagram.java:266-286`); the diagnosis's "collapseEmptyNamespace survivor" and "renderer leaf-loop duplicate" hypotheses are ruled out (the row count was wrong, not the loop). Verified: mujopi 6 `<path>` in both documents, pisobo 0 `g.entity` in both, runane/vusute's `ent0014` gone. No code change; artifact in `.agent-notes/cdd-T14.md`; `measurements/t14.json` = the merged-tree snapshot. Residuals: cocube's `#00F` leaf stroke (row 46 flag, T18), runane/vusute/pisobo numeric = issue 18. | b3 → now: mujopi 3/23 → 0/1, pisobo 1/0 → 0/2, cocube 3/164 → 2/177, runane 1/10 → 0/7, vusute 1/10 → 0/7 |
| 49 | 2026-09-22 | close-b4 | Batch 4 closed. Survey 460/56/207 → **468 / 68 / 187** (0 errored/timeout; fresh `parity-class.json` = `b4.json`, no `dotEqual` flip); census 462 → **470**; DOT 711/712. `pin-diff b3→b4` = 25 transitions, every one mechanised: 8 → conformant (bajotu GEO1/T13; dativu/tibatu ENT3, dopuzi/vacole ENT4, garumi/lujaje/momoba STY3 — T12, each proved by its `in.puml`), 11 → structural-match (GEO1 clip on bejusa/mujopi/pecabi/pisobo/runane/sanixi/vusute — T13; ENT3/STY3 on diroxo/domeki/foxata/ledepo/xitobu — T12), 6 rises all on non-conformant fixtures with journaled mechanisms (cocube/delasa/lojiga/sijisi: issue-18 trailing-segment delta + newly comparable geometry, row 45; daxeno/diroxo: `UPath` growth, row 46). 0 base-conformant losses. Named residuals: six issue-18 fixtures; cocube's empty-package leaf `packageBorderColor` (T18); keyword-form containers (`rectangle X {}`, sijisi) not populating `Namespace.usymbol`; xitobu's 58 numeric (element order). | `<scratch>/pindiff-b3-b4.txt` classified against `fixtures.md` buckets |
| 50 | 2026-09-22 | close-b4 | Re-pin: 8 added to `ratchet.json` (462 → 470, `source: "cdd-b4"`), each survey-conformant AND census-0-diff AND `dotEqual`, goldens `cmp`-verified, additive; every existing pin still 0-diff. Baselines +8 `goldens:svg-class` clones; counts re-derived: refusal 5457 → 5465, rendering 5188 → 5196; routing agree 4305 → 4313, total 5465. `census-class.json` regenerated at `2397dcde`. Task commits this batch: T11, T13, T12 (+ a `fix(cdd-T12)` for its test on the merged tree, row 47), T14 as a docs commit (row 48), three merges. | `docs/parity-report.md` class row 468/68/187 · 470 · 470 |
| 51 | 2026-09-22 | T15 | `Kal` ported faithfully (`4b887c6d`, D6): box `Kal.java:134-144` (`URectangle(dim)` at `textDim.delta(4,2)`, stroke 0.5, text at `(2,1)`); placement `:146-173` + `SvekEdge.java:1069-1077` anchored on `dotPathInit` — the endpoint BEFORE `simulateCompound`/extremity trim (`:658`), confirmed on baneru (rect y 54.82 vs drawn path start 75.82); margins `:106-121` via `Entity#ensureMargins` (`Margins.java:56-62` max-merge) into the shield table; extremity translate `:539-562`; style `{root,element,classDiagram,class_,qualified}` (`:93-97`). Blast radius measured: the spec's grep over- and under-matched; a refined arrow-adjacent regex hits exactly the 19 named fixtures. A SECOND margin mechanism surfaced by reading `EntityImageClass.java:100-128`: `Math.max(width, getKalWidth()*1.3)` (`:113`, UP/DOWN kals only) widens the drawn BOX (baneru `class1` 71.725 → 72.995) — ported as `class-dot-width-floors.ts#applyKalWidthFloor` after `applySameClassWidthFloor`. Not ported, recorded: `Kal#overlapx`/`SvekNode#fixOverlap` (no fixture has two same-side boxes in reach). | 17 of 19 → structural 0 (camuna 9 / nafiki 8 residual = `class { generic { BackgroundColor } }` and `arrow.cardinality` font, not M1); all 19 DOT node sizes + `Margins` cells EQUAL to `svek-N.dot`; Σ −2064 numeric across exactly 19 movers; 0 losses (468 → 468); DOT 711/712; gates green 764/764 |
| 52 | 2026-09-22 | T15 | FLAGGED write-set extensions into `src/core`: the margins had to reach the LAYOUT engine, not only the emitted DOT text — new `DotInputNode.shieldMargins` (`graph-layout.types.ts`), a `shape=plaintext` HTML-shield branch in `graph-layout-build.ts`, an asymmetric-corner rule in `graph-layout.ts#mapNodes`, and `svek-dot-emit-labels.ts#shieldTable` reading the field when present (the nominal `SHIELD_MARGIN_X/Y` stay for every other shielded node, so description/state geometry is untouched — their ratchets passed in the same suite); `:h` ports stamped on the layout edge (`tailport`/`headport`) as `Bibliotekon#getNodeUid` does, else baneru's arrow tip sat at 74.77 not 70.82. Carry-only: `EdgeGeo.kalBox` (+ `class-layout-shift.ts`), `EdgeGeoTextContext.kals`; three pre-authorised splits (`renderer-edge.ts` 501 → 475 via `renderer-edge-extras.ts`, `class-dot-width-floors.ts`, `style-cascade-class-snames.ts`). The task note was written to `plans/…/.agent-notes/` and moved to `.agent-notes/cdd-T15.md` by the orchestrator. | `git diff --name-only 4b887c6d~1 4b887c6d` (24 paths) |
| 53 | 2026-09-22 | T15 | STOP 8 fired and was handled per its own text ("file + halt the task only"): the mission's ONE rise, mucoti 93 → 94, is a `@knowvah/dot-engine` defect — a `minlen=0` FLAT edge ignores `PORT="h"` on an HTML-table node and anchors on the bounding box, where a RANKED edge through the identical seam honours it; real graphviz 16.1.0 fed the SAME cached oracle DOT bytes anchors at the cell (78.9 px vs dot-engine 142.879). Filed `docs/graphviz-issues/19-flat-edge-ignores-html-table-port.md` + TRACKER line 19, not compensated. The task's own work was complete and every AC passed, so the orchestrator rules the task closed rather than halted; mucoti stays `structural-match`. Also recorded: a post-measure width floor does not re-centre the header (`HeaderLayout.java:81-117` runs inside `measureClassifier`) — pre-existing, now reachable via `applyKalWidthFloor`; baneru keeps a uniform 0.572 px header offset. | `dot -Tplain mucoti/svek-1.dot`; `.agent-notes/cdd-T15.md` |
| 54 | 2026-09-22 | T16 | M7 + E11 landed together (`8faa3628`): a grouped (`sametail`) link is forced to `LinkType(NONE, NONE)` with `LinkStyle.NORMAL()` (`Link.java:238-239`, `LinkType.java:71-72`) — decor AND dash dropped; the per-edge triangle needed no `renderer-edge.ts` change because `decorName('none')` already returns nothing. The shared triangle + stubs are `Neighborhood#drawU`/`drawExtends` (`Neighborhood.java:69-121`: `UPolygon (0,0),(7,20),(-7,20)` rotated by `atan2(cx−px, −(cy−py))` at the rect/centre/contact intersection, `:132-179` Cramer's-rule intersection with ε=.001), drawn as a root-level sibling right after the protected leaf's image (`SvekResult.java:82-89`), inside the true box backed out of the 20 px `EntityImageProtected` border (`:56,77-79`). Rotation reuses `core/svek/extremity/rotate-point.ts` (the `UPolygon#rotate` formula); `ExtremityExtendsLike`'s triangle was deliberately NOT reused (different vertex set and sin convention). Carry: `EdgeGeo.sametail` (+ shift), `EdgeGeoTextContext.sametailByRelIndex`, `sametailByRelIndex` threaded out of `DotGraphParts`, `EdgeDecoration` exported (a lizard CCN artefact on an inline object type). `renderer.ts` (516 lines, over cap, hook blocks growth) absorbed its one call by collapsing an adjacent call — a split is overdue. | lazeju 8/646 → 0/642, xifuza 3/139 → 0/140; mefike 4 → 1, pijiju 5 → 1 (dasharray diffs gone), jakapi 3 → 1; `pin-diff t15→t16` 4 transitions all `groupInheritance` fixtures, 0 losses; rises on jakapi/pijiju = new-but-imperfect stubs (anti-monotone growth); DOT 711/712; gates green 765/765 |
| 55 | 2026-09-22 | T16 | Two findings. (1) The agent left `Neighborhood.java:97-113`'s `allButSametails` loop (a stub line for every NON-sametail link of the protected leaf) unported as "no AC requires it"; it is the exact childCount residual on mefike/jakapi/pijiju and the same Java method — the orchestrator handed it back as a `fix(cdd-T16)` (CLAUDE.md: the long tail is the deliverable). (2) Pre-existing, separate: `class-geo-builders.ts#contentBox` draws a protected classifier's own box at the PADDED (+40) size instead of insetting by the 20 px border — a flat +20 px canvas-height divergence present before T16 (lazeju's 642 numeric); out of the batch's write-sets, filed for T37/T35. | `Neighborhood.java:95-115`; `.agent-notes/cdd-T16.md` |
| 56 | 2026-09-22 | T16 | `fix(cdd-T16)` `a3770cd1`: `Neighborhood.java:97-113`'s `allButSametails` loop ported — for every non-sametail link of a protected leaf, a stub from the rect/centre/contact intersection to the link's contact (`link.getEntity1() == leaf ? getStartContactPoint() : getEndContactPoint()` = `normalizedPts[0]` / `.at(-1)`, since `normalizeEdgePoints` orders entity1 → entity2), excluding entity1's side only when the same edge is the leaf's own grouped edge (`removeAll(sametailLinks)`, `:62-65`). `protectedIds` threaded out of `DotGraphParts`; `EdgeGeo.leafContacts` carry; the whole group-inheritance cluster moved to new `class-edge-group-inheritance.ts` (cap + a lizard brace-depth false positive on an untouched neighbour, elimination trace in the note). mefike/jakapi/pijiju → structural 0; jakapi's root sequence `polygon,line,polygon,line,line,line` equals the jar's. Tests assert exact shape/order and endpoints within 0.01 of the port's own contacts (dot-engine sub-pixel routing, ruling 2026-08-09), not the jar's literal pixels. | `pin-diff t16→t16b` 3 transitions all → structural-match, 0 losses; DOT 711/712; gates green 765/765 |
| 57 | 2026-09-22 | T17 | M8 landed (`61f6698e`): the step-1 reach was measured with the real parser (a naive `"…"/"…"` regex misses mugobo's bare `/1` form) — exactly 2 of 712 fixtures use role syntax, both already `diverged`, so the zero-fixture-rise hazard had nothing to move. Two upstream mechanisms ported: the DOT-reservation fallback (`SvekEdge.java:447-466`: an end with a role but no multiplicity reserves the role in the SAME `taillabel`/`headlabel` slot — one line, `rel.fromMultiplicity ?? rel.fromRole`) and the additive second anchor (`:1029-1063 drawRoleLabel`, called at `:962-964`/`:975-977` only when the quantifier also exists: mirrored across the line direction from the placed quantifier box, gap 2, no DOT reservation of its own — new `class-edge-role-label-anchor.ts`). Three pre-authorised splits (`class-geo-geometry-types.ts`, `class-edge-role-label-anchor.ts`, `class-edge-label-measure.ts`); emitter change in `renderer-edge-extras.ts` (T6 precedent). Residual 2 px canvas width on both fixtures is pre-existing and unrelated. Latent gap found, corpus-unreached: `class-dot-edges.ts#swappedRel` does not swap `fromRole`/`toRole` for a `dotEdgeReversed` edge's fallback reservation as `Link.java:145-146 getInv()` → `LinkArg.java:116-117` does — handed back as `fix(cdd-T17)`. | mugobo/nenexe 5/5 → 0/2; `pin-diff t16b→t17` 2 transitions, 0 falls, 0 losses; DOT 711/712; gates green 767/767 |
| 58 | 2026-09-22 | T17 | `fix(cdd-T17)` `b025c23a` (flagged batch-2 file `class-dot-edges.ts`): `swappedRel` now swaps `fromRole`/`toRole` for a `dotEdgeRunsReversed` edge exactly as it swaps the multiplicities, mirroring `Link.java:145-146 getInv()` → `LinkArg.java:116-117` (`role2, role1` beside `quantifier2, quantifier1` and `kal2, kal1`); its stale "swapping them would be inert" doc comment was true only until the M8 fallback made roles readable. Tests in `class-tail-head-swap.test.ts` (`A /owner <-up- /child B`, the non-reversed guard, and a `renderSync` probe), each stated as asserting the ported rule since no corpus fixture reaches the branch. | `pin-diff t17→t17b` 0 transitions, 0 falls; DOT 711/712; gates green 767/767 |
| 59 | 2026-09-22 | close-b5 | Batch 5 closed. Survey 468/68/187 → **468 / 92 / 163** (0 errored/timeout; fresh `parity-class.json` = `b5.json`, no `dotEqual` flip); census 470 unchanged; DOT 711/712. `pin-diff b4→b5` = 24 transitions, all `diverged → structural-match`, all bucket B3: 17 LNK1 (T15 Kal), 4 LNK7 (T16 groupInheritance, incl. the `B3 B9` jakapi/xifuza rows), 2 LNK8 (T17 roles), plus rifuzu (LNK1+LNK10); 3 rises with journaled mechanisms — mucoti 93 → 94 (stop 8, dot-engine issue 19, row 53), jakapi 441 → 458 and pijiju 177 → 183 (new-but-imperfect stubs, rows 54/56). 23 falls, Σ ≈ −2100. 0 base-conformant losses. No fixture became conformant this batch (the B3 residuals are numeric: issue-19 flat-edge anchoring, the header-recentre gap of row 53, camuna/nafiki's `generic` background + cardinality font, lazeju's protected-box +20 px canvas of row 55, mugobo/nenexe's 2 px canvas). | `<scratch>/pindiff-b4-b5.txt` classified against `fixtures.md` buckets |
| 60 | 2026-09-22 | close-b5 | Re-pin: 0 candidates (468 conformant = 468 pinned+eligible; every existing pin still 0-diff; 0 conformant-only / zero-only unpinned) — ratchet stays 470, baselines and gate counts untouched. `census-class.json` regenerated at `b025c23a`. Task commits this batch: T15 (+ note move), T16 (+ `fix` for `allButSametails`), T17 (+ `fix` for the reversed-edge role swap). Follow-ons carried to T37/T35: the protected classifier's padded `contentBox` (row 55), `HeaderLayout` re-centre after a post-measure width floor (row 53), `renderer.ts` at 516 lines over the cap (row 54). | `node` pin check: candidates 0; `docs/parity-report.md` class row 468/92/163 · 470 · 470 |
| 61 | 2026-09-22 | T18 | Audit first (stop 12 does NOT fire): every consumer of `classifierFill`/`classBorder`/`class*`/`icon*Color` is inside `src/core` or `src/diagrams/class`; the one hit outside, `packetdiag/renderer.ts:13`, is a doc comment. D8 landed (`94069502`): `ThemeGraphColors.classBackground/classBorder/icon*Color` → `Paint`; the dedicated-key handlers get a 4th `KeyHandler` arg `paint` (`resolveColorPaint`, `parseColor` with `resolveColor`'s WHITE fallback and its CodeQL guard preserved) so only the 6 class/icon arms change; `classifierFill`/`classBorder` return `Paint`. The divider-line exception is a SHAPE-KIND branch in the Java, not a colour test: `DriverLineSvg.java:76-82` flattens a `ULine`'s gradient stroke to `getColor1()` while `DriverRectangleSvg.java:97-111` emits the def (`DriverPolygonSvg.java:63-64` delegates) — ported as `classBorderLine = noGradient(classBorder())` (`HColors.java:139-146`'s name), used at every `line(...)` site. `extractDecorations` gained `line?/text?/lineStyle?` (`Colors.java:96,117-122`; `CommandCreateClassMultilines.java:272-279` order). AC4 needed `parseColor` at `namespaceFill` on top of T11's field. Nine mechanical, typechecker-forced extensions inside the two trees (flagged). `theme.ts` (600 lines) untouched. | capode 4/0 → 2/0 (rect `stroke="url(#…)"`, both lines `#FFBD42`), tagofo 36 → 0 CONFORMANT and rakopi 20 → 17 (icon colours now through `resolveColorToSvgHex`, the last raw-token sink), dizuse/dacixi gradient defs present; `pin-diff b5→t18` 3 transitions + 3 falls, 0 losses, other engines' suites unmoved; DOT 711/712; gates green 769/769 |
| 62 | 2026-09-22 | T18 | Two structural findings. (1) Every `<linearGradient>` this port emits costs exactly ONE `@id` diff, permanently: the jar's id is `"g" + Long.toString(|seed|,36) + gradients.size()` (`SvgGraphics.java:162,393`, per-document seeded) while `core/paint.ts#paintToSvg` uses an FNV content hash (mission-render-fidelity D3); the jar's scheme is ALREADY ported for the klimt family (`svg-graphics-core.ts:185-186`, `svg-seed.ts#getSeed`) — the two are just not unified. It is the ENTIRE residual on capode and dacixi (2/0 each) and the mechanism of taceve 7 → 11 / mexaka 37 → 42. Cross-engine (every `Paint` consumer), so a T37 filing / own task, not a batch-6 edit. (2) `ColorType.HEADER` (`#header:blue/red`, mexaka's second def) has no ported consumer — handed to T20 with E1's header split. Row 46's `packageBackground/packageBorder` optionality was NOT done by T18 (different fields, different axis); the exact edit is in `.agent-notes/cdd-T18.md` §9 and runs now as T18b in the main tree, disjoint from T19-T23's worktrees. | taceve +4 = 5 new id diffs − 1 retired childCount diff |
| 63 | 2026-09-22 | T23 | HALTED at stop 1, no commit (correct). Diagnosis artifact: cutasu-32-zete658 and xogixe-78-zuro619 — E10's own two verification fixtures — both end their member list with a bare `--` and take `isEnhancedBody`; `renderer-classifier-box.ts:250-258`'s enhanced-body branch collapses the WHOLE body into one `UrlTaggedPrimitive` tagged `url: geo.url` before `wrapClassifierBody` runs, so `renderer-url.ts`'s merge/break logic (correct as written) receives exactly two primitives and has nothing to split. Upstream: the enhanced body reuses `MethodsOrFieldsArea` unchanged (`BodyEnhanced1.java:186-190`), whose per-member `TextBlockTracer` (`MethodsOrFieldsArea.java:305-323`) opens/closes each member's own `Url`; `class-body-enhanced-layout.ts:201` already threads `m.ownUrl` per part but `renderEnhancedBody` flattens it to one string. The other 5 `[[[url]]]` fixtures take the classic per-row loop and are already correct. Disposition (push-forward: reorder inside the batch): T23 now depends on T20 (`renderer-classifier-box.ts`) and resumes in the main tree after T20 merges with `renderer-body-enhanced.ts` added to its write-set (owned by no batch-6 task; T18 touched it mechanically). The diagnosis's E10 "fix shape" is wrong for its own two named fixtures — an addendum belongs in `fixtures.md`/T38. | `pin-diff t18→t23` 0 transitions (no code changed); artifact in the `cdd-t23` worktree's `.agent-notes/cdd-T23.md` |
| 64 | 2026-09-22 | T19 | On `cdd/t19` (`43e1b345`): M1's text half reuses T18's exported `parseDeclarationColors(geo.color).text` as the topmost font-colour tier (`Style.java:206-208,259-263`: a classifier's own `ColorType.TEXT` wins outright; header and member rows share the tier) — the brief's claim that T18's `text?` field already reached the rows was false (`class-declaration-parser.ts` drops it; the raw `geo.color` is what is reachable). M2: `classfontcolor`/`classattributefontcolor` handlers (`FromSkinparamToStyle.java:187,192`; the correct post-`cleanSkinKey` key is `classattributefontcolor`, not the spec's `attributefontcolor`) feeding the existing `classCascadeHeaderFontColor`/`classCascadeFontColor`, with an `automatic` guard after a transient nisune 3 → 4 rise was caught by pin-diff. M5 was already done by T18. Extensions flagged: `skinparam-accumulator.ts`, `skinparam-theme-builder.ts` (2 fields, 2 rows). | remanu 6/0 → 0/0, picija 14/0 → 0/0; `pin-diff t18→t19` 2 transitions, 0 rises, 0 losses; DOT 711/712; gates green 770/770 at load 45-110 (six suites), re-run twice clean |
| 65 | 2026-09-22 | T19 | Five sub-mechanisms halted at stop 1 with artifacts and filed in `planning/next-missions.md` §5 (each with `file:line`): M8a `defaultMonospacedFontName` needs a theme thread into the shared creole `Command.executeAndAdvance` (engine-wide); M8b is NOT in `class-declaration-extractors.ts` — `class-layout-header-creole.ts#buildHeaderLineMetrics:105-121` discards the header's atoms so the name row never carries `row.atoms`; stereotype-scoped `BackgroundColor<<stereo>>` (nagega's suffix and tabaxa's nested-block forms normalise to the SAME key `classbackgroundcolor<<foo1>>` — one grammar, not two) is a DIRECT stereotype-qualified lookup (`SkinParam.java:371-381`), the `classBorderThicknessByStereo` shape, consumed in T20's `renderer-classifier-colors.ts` this batch; icon selectors have two causes (`class-body-enhanced-layout.ts:199` duplicates `isMethodMember` without its fallbacks; no `<style> visibilityIcon {}` cascade exists); M7's premise was wrong — `<w>` already renders byte-exact, beruje's 2 diffs are the creole `<back:color>` deferral in `CommandCreoleStyle.ts`. These are class-diagram mechanisms inside the mission's exit bar, so T37 (singletons) must re-adopt the separable ones rather than leave them as next-missions entries. | `.agent-notes/cdd-T19.md`; tabaxa 1/0, curupe 3/0, nesivu 1/0, nagega 14/114, tuguku 7/0, filoxo 20/0, fijali 11/0, beruje 2/0 all unchanged |
| 66 | 2026-09-22 | T21 | On `cdd/t21` (`8a6e8797`): M3b — `badgeLetter` lacked `case 'entity'`; `EntityImageClassHeader.java:239-242` gives ENUM and ENTITY the same `'E'` (jar-verified byte-identical against xidura's ENTITY badge) → lilura/tepazu/xidura conformant. M3a — `BADGE_GLYPH_D` 9 → 17: R (vegubu), J (dacisu), O (gamevo via `!define`), W and Q (befasi), D (jikase), S (bejeli), X (rideze), each scraped from that fixture's cached `in.svg`, translated to the `(22,23)` reference and round-tripped byte-exact through the real `badgeGlyphPath` before commit (push-forward: scrape, never compute). Glyph data split to `class-badge-glyph-data.ts` (cap). Two pre-existing tests that asserted the old kind-default fallback were corrected. 19 movers, all with a `<<(X,colour)>>` spot or an ENTITY kind; 11 rises are D5's short-circuit reveal (a wrong badge SHAPE charged one diff and hid every coordinate diff beneath it; gamevo's QObject traced directly, non-uniform sub-pixel deltas = pre-existing node positions, not the translate math). Corpus structural total −104. | bejeli 4/2 → 0/2 (Δ1 height, pre-existing), jikase 4/0 → 0/0, dacisu 2 → 0, lilura/tepazu/xidura 1/0 → 0/0; `pin-diff t18→t21` 18 lines, 0 losses (469 → 474); DOT 711/712; gates green 770/770 at load ~92 after two load-induced flakes |
| 67 | 2026-09-22 | T20 | On `cdd/t20` (`3decd34a`): E1 header split ported for class/enum/interface/abstract (`EntityImageClass.java:216-234`, four shapes; new `renderer-classifier-header-split.ts`), with the gradient quirk gated on "no inline colour at all" — `:203-205` sets `headerBackcolor = backcolor` (same reference) whenever ANY inline colour exists, so taceve's inline-gradient Test1-4 must NOT split; a first `typeof fill !== 'string'` gate split them and was caught by render-diff, not algebra. `roundCorner == 0`'s two-shape sibling (`:198-206`) left undrawn as an unsampled combination (the `headerBackgroundPath` precedent). M1 border half: `classBorder()` reads `parseDeclarationColors(geo.color).line` first (`:193-200` fallback order), `classBorderStrokeWidth`/new `classBorderStrokeDasharray` reuse the ported `svek-edge-stroke.ts#strokeForStyle` (`LinkStyle.getStroke3`) — no re-derivation; dasharray threaded through every border-stroked rect/path/line. M6: `PlacementStrategyVisibility.java:56-69` centres on the WHOLE wrapped block; ported as a closed-form baseline shift (`+ (blockHeight − fontSize)/2`) into the untouched `visibilityIconOriginY` (substituting the block total as `rowHeight` moves the icon the wrong way); `visibilityBlockHeight` carried on the first row (`class-geo-types.ts`, flagged). | gojadi/murotu/najuxe/pakemi/vubofi → conformant; gojatu 32/0 → 8/0, sosono 27/0 → 3/0, mexaka 40/2 → 11/0; `pin-diff t18→t20` 9 transitions, 0 losses; the 4 rises (dizuse/taceve/givofi/popesa) decompose into gradient-def-id noise (row 62) + pre-existing diffs; DOT 711/712; gates green 771/771 at load 63 |
| 68 | 2026-09-22 | T20 | Four residuals located with `file:line`, outside T20's write-set, INSIDE the mission's exit bar — queued for a batch-6 follow-up round in the main tree after the parallel merges, not left as filings: (a) nisune-86-faji869 needs `classHeaderBackgroundColor` skinparam plumbing (`theme-graph-colors-a.ts` + `skinparam-key-handlers-table-*.ts`); (b) `core/color-override.ts#resolveBareOrBackColor` never extracts a bare leading token in a compound spec (`#yellow;line:red`) and treats a keyword-only spec (`#line.bold`) as a colour name (gojatu/sosono/mexaka residuals); (c) zubevi's generic clause: `EntityImageClassHeader.java:144` splits on `\n` into one `<text>` per line, but `class-stereotype-layout.ts:299-345 GenericTagGeo.text` is one string drawn once by `renderer-classifier-badge-tag.ts:150-173` — needs `lines[]` like `EdgeGeo.labelLines`; (d) the same M6 icon bug in `renderer-classifier-rows.ts:81 renderRow` (T19's file, reached from `renderer.ts:108`/`renderer-body-enhanced.ts:90,116`). The spec's "change the call in `class-member-rows.ts`" was inaccurate: the consuming call is in `renderer-classifier-box.ts`. | `.agent-notes/cdd-T20.md` |
| 69 | 2026-09-22 | T18 | `fix(cdd-T18)` `1abb7288` (row 46 / T18 §9 resolved): `packageBackground`/`packageBorder` optional; `theme.ts`'s baked cluster defaults (`'none'`/`'#000000'`) removed so "unset" is genuinely `undefined` (otherwise `deepMergeTheme` would have handed every unstyled leaf `#000000` — measured, not assumed); every `…package_,group`-signature draw site (`class-namespace-shape.ts` ×5 and the ONE other consumer, `description/renderer-cluster.ts:133`, patched with `?? 'none'` — flagged extension) supplies the cluster default explicitly (`Cluster.java:285-296`, `plantuml.skin:102-114` puts the defaults under `group {}` only); `emptyPackagePaint` cascades `<style> package {}` → flat `packageBorderColor` → the leaf's `…package_,title` defaults (`EntityImageEmptyPackage.java:87-88`). | cocube 2/177 → 0/177 (leaf stroke `#00F`); gatula/garumi/xitobu/dopuzi/mujopi byte-identical; `pin-diff t18→t18b` 1 transition, 0 losses; DOT 711/712; gates green 769/769 |
| 70 | 2026-09-22 | merge | `cdd/t19` (`3910badd`), `cdd/t20` (`73e44e51`), `cdd/t21` (`bd79422d`, catalog conflict) merged onto T18b. Combined tree `measurements/t18-21.json`: **482 / 101 / 140**, 0 base-conformant losses, `pin-diff t18→t18-21` 31 transitions (the union of rows 61-68's movers). T22 still in its worktree; T23 resumes in the main tree with `renderer-classifier-box.ts` + `renderer-body-enhanced.ts` added (row 63); the residuals of rows 65/68 run as a batch-6 follow-up round in worktree `cdd-b6fu` from this tip. | `tsc` clean on the merged tree |
| 71 | 2026-09-22 | T22 | On `cdd/t22` (`aec8e6ce`): E8 — `LeafType.CIRCLE` routes to `EntityImageDescription` with `USymbols.INTERFACE` (`GeneralImageBuilder.java:157-158`, `Entity.java:415`, `EntityImageDescription.java:137,294-330` `hideText`) through the already-ported `core/svek/image/EntityImageDescription.ts` + `USymbolInterface.ts`/`CircleInterface2.ts` (unused until now) and `leaf-sizing.ts`'s existing 18×18 `'circle'` case; sizing dispatched at `layout.ts#measureLeaf` (the spec's `class-layout-helpers.ts` guess would have created an import cycle). cacoma: usecase/actor sizing was already right (SI14 T4); the gap was draw-side only — `component` still used the hand-rolled `renderComponentIcon`; `usesClassUSymbolEntity` now covers usecase/circle/actor/component with `COMPONENT_ROUND_CORNER = 5.0`. `renderer.ts` net −1 line. The circle's DOT cell now emits `WIDTH="18" HEIGHT="18"` = the jar's icon cell. | conija 3/78 → 0/8, niduni 3/429 → 0/425, cacoma 9/4 → 0/2; `pin-diff t18→t22` 3 transitions, 0 falls, 0 losses; DOT 711/712; gates green 770/770 at load 31-62 |
| 72 | 2026-09-22 | T22 | Named numeric residual, diagnosed, not fixed (outside the write-set): the jar's `SvekNode.java:220-267` reserves `shield()` margins around a `hideText` leaf's icon cell (14 px top/bottom on conija, sized off the label height) and reads the symbol back from the padded table (`DotStringFactory#solve`); this port declares a bare `fixedsize` 18×18 node with no `shieldMargins`, so graphviz ranks it 18-28 px tighter — conija's Δ18 and niduni's uniform ≈(4.85, 3) document shift. The fix wires `EntityImageDescription.getShield()` into `DotInputNode.shieldMargins` in `class-dot-graph.ts`, reusing T15's now-generic `graph-layout.ts#portNodeSize`/`shieldCorner` reconciliation — queued as T22b in the main tree after T23 lands (`class-dot-graph.ts` is owned by no live task). cacoma's 1 px `svg/@width` is undiagnosed. | `.agent-notes/cdd-T22.md` |
| 73 | 2026-09-22 | T23 | Resumed in the main tree, resolved (`a5c7621c`): `renderer-body-enhanced.ts#buildEnhancedBodyPrimitives` (was `renderEnhancedBody`) emits one `UrlTaggedPrimitive` per divider/tree part and per ROW (`row.url ?? geo.url`) — `BodyEnhanced1.java:186-190` builds each rows block as the same `MethodsOrFieldsArea` whose `TextBlockTracer` (`:305-323`) opens/closes each member's own `Url`; the enhanced-body branch of `buildBodyPrimitives` returns them directly; `renderer-url.ts` unchanged (its run-break was correct, merely starved of rows). cutasu → conformant; xogixe's 50-`<a>` structure exact. The other 5 `[[[url]]]` and 22 `[[url]]` fixtures byte-identical. Exposed residual on xogixe (2 icon fills): `class-body-enhanced-layout.ts:198-200` sets `visibilityIsField` from `m.params === undefined` instead of the shared `isMethodMember` (upstream: any raw line containing `(`/`)` is a method) — the SAME site T19 named for the icon selectors (row 65) — fixed next in the main-tree round with T22's shield margins. | cutasu 6/0 → 0/0, xogixe 6/0 → 2/0; `pin-diff t18-21→t23` 1 transition, 0 losses; DOT 711/712; gates green 774/774 |
| 74 | 2026-09-22 | merge | `cdd/t22` merged as `096c9aaf` (catalog conflict only) onto T23 (`a5c7621c`). Two follow-up rounds now run in parallel from this tip: T6FU (worktree `cdd-b6fu`, rows 65/68's residuals: nisune header-colour plumbing, compound-colour-spec tokens, zubevi generic lines, `renderRow` icon centring, stereotype-scoped lookup) and T22b (main tree: `hideText` leaf shield margins in `class-dot-graph.ts`, `isMethodMember` in `class-body-enhanced-layout.ts:199`). Disjoint write-sets, verified by grep. Batch 6's close follows their merges. | `git worktree list` |
| 75 | 2026-09-22 | T22b | Item 1 (`694ef4d7`): `EntityImageDescription.getShield()` (`EntityImageDescription.java:239-262`, already ported at `EntityImageDescription.ts:369-374`) wired into `DotInputNode.shieldMargins` for `circle` leaves at DOT-node build (new `class-hidetext-shield.ts`), the table gated as `SvekNode.java:132-155` gates it, reusing T15's `portNodeSize`/`shieldCorner` reconciliation unchanged; `buildCircleInterfaceSizingParams` threads real `links` (default `[]`, byte-identical for existing callers). `class-dot-graph.ts` was 502 lines (over cap) — `buildDotClusters` moved to new `class-dot-clusters.ts`. Item 2 (`1fa378e7`): `class-body-enhanced-layout.ts:199`'s `m.params === undefined` (always FIELD for a raw-fallback member) → `!isMethodMember(m)` (`BodierLikeClassOrObject#isMethod`: any raw line with `(`/`)`); four unplanned same-mechanism improvements (pejone/xonamo 234 → 220, rakopi 17 → 13, rusuzi 10 → 3) proved by stash A/B. Remaining tuguku 6/0 and filoxo 19/0 are T19's second cause (no `<style> visibilityIcon {}` cascade) plus filoxo's shadow-filter id / stroke / font-family gaps. | conija 0/8 → 0/0 (shield cell byte-equal to `svek-1.dot`), niduni 0/425 → 0/0, xogixe 2/0 → 0/0, fijali 11/0 → 0/0; full-row sweep 10 movers, 0 rises, 0 losses (orchestrator re-checked); DOT 711/712; gates green 775/775 |
| 76 | 2026-09-22 | T6FU | Residual round landed (`df367fff` `1da6a3f1` `867ffd00` `d331865b` `883f2bf3` + chore `44734d00`, merged `3dc87cb3`). (1) `classHeaderBackgroundColor` plumbed to T20's split (`FromSkinparamToStyle.java:196`, `EntityImageClass.java:173-178,202-205`); the `header` signature is NOT more specific than `class_` — `StyleStorage.java:102-116` folds matches in registration order and `DarkString.java:50-66` keeps the bigger counter, so the LAST skinparam wins (jar-probed both orders; `classbackgroundcolor`'s handler now also writes the header slot after four corpus regressions caught it). (2) `resolveBareOrBackColor` re-ported as `Colors.java:96-104`'s tokenizer verbatim (any `;` token with no `:` and no `.` is BACK, last claimant wins, dotted = line style; `ColorType.java:41-47` truncates a name at its first `.`); documented divergence: no `NoSuchColorException` rejection path; shared core — a note fixture (xoxuni) moved too. (3) Generic clause split by `Display.getWithNewlines` (`EntityImageClassHeader.java:146-148`) into `GenericTagGeo.lines[]`. (4) `renderRow` gets T20's closed-form icon centring via one shared `wrappedVisibilityIconOriginY`; no corpus fixture reaches it (unit-tested as the ported rule). Flagged: `renderer-classifier-box.ts:198` threaded with `theme` + the alias hunks, outside T23's branch. | gojatu 4/0 → 0/0, lukexe 1/0 → 0/0, zubevi 3/2 → 0/0, tabaxa 1/0 → 0/0, nisune 3/0 → 1/0 (`classFontColor automatic`, excluded), mexaka 9 → 7, xoxuni 8 → 4, julixi/rulite 2/3 → 0/2, nagega 14 → 12; `pin-diff t18-21→t6fu` 6 transitions, 10 falls, 0 rises, 0 losses; DOT 711/712; gates green 777/777 |
| 77 | 2026-09-22 | T6FU | CORRECTION to row 65 (item 5): `classbackgroundcolor<<stereo>>` is NOT `SkinParam.java:371-381 getHtmlColor` (the class engine never calls it; `EntityImageClass.java:207` reads the STYLE); `SkinParam.java:228-233` routes every cleaned key through `FromSkinparamToStyle`, whose ctor peels `<<…>>` (`:292-301`) and whose `addStyle` (`:396-408`) applies `addPriorityForStereotype` (+1000) and `sig.addStereotype` — the stereotype-tagged style tier `classTagCascade` already models, consumed via `cleanStereotypeToken` in `classifierFill` and, because +1000 also wins `getStyleHeader()`'s merge, first in `resolveClassHeaderFill` (tabaxa rose 1 → 2 in the corpus pin-diff while passing alone — caught, fixed). `classBorderColor<<stereo>>` (same route, no fixture) not ported. nagega's residual named: a class LINK-colour grammar slot (`CommandLinkClass.java:160` `color().getRegex()`, `:368 link.setColors`) absent from the relationship parser — `!define` disproved as the cause by probe — plus a dot-engine node-order swap (±169.004). Follow-ons in `.agent-notes/cdd-T6FU.md` for T37. | probe: both key spellings normalise to `classbackgroundcolor<<label>>`; `A o-up-> B #MediumTurquoise` renders `#181818` |
| 78 | 2026-09-22 | close-b6 | Batch 6 closed at `3dc87cb3` + this commit. Survey 468/92/163 → 491/104/128 (0 errored/timeout); census 0-diff 470 → 493; ratchet 470 → 493 (23 pins, `source: cdd-b6`: conija cutasu dacisu fijali gojadi gojatu jikase lilura lukexe murotu najuxe niduni pakemi picija remanu sosono tabaxa tagofo tepazu vubofi xidura xogixe zubevi — every one survey-conformant AND census 0-diff AND `dotEqual`, golden `cmp`-equal to its cached `in.svg`); DOT 711/712 (floor 710/711 held); no `dotEqual` flip. pin-diff b5 → b6: 44 transitions, 0 b5-conformant losses, 42 falls, 15 rises, 28 in bucket B6 alone, 2 in B9 (julixi/rulite: ENT5 reached structural-match through T22's leaf shapes — a B9 bucket, but a B6 mechanism), 1 in B1/B4 (cocube: T18/T20's colour plumbing reached a B1-bucketed fixture; mechanism is B6's). Every rise carries a mechanism (row 79). Routing/refusal baselines +23 clones each (`measuredAgainstCommit 3dc87cb3`), gate counts 5465 → 5488 / 5196 → 5219 / 4313 → 4336, derivations in the test comments. | `measurements/b6.json`, `parity-class.json`, `census-class.json`, `docs/parity-report.md` |
| 79 | 2026-09-22 | close-b6 | The 15 rises, mechanised (D11). (a) Numeric reveal behind a structural fall, 11 fixtures: befasi/mububu/ribove/soboro/zakuta/ziruni (structural 10 → 0, numeric 614 → 818) and zosaxa (10 → 0, 682 → 886) — T21's W/Q/S/X badge glyphs (row 66) removed the `path/@d` structural diffs that short-circuited the badge and everything after it; gamevo (11+2 → 0+452) same, via the `O` glyph, the 452 being a 1 px canvas width (`viewBox` 1102 vs 1101) shifting every later x; puvono/sekame (5+1240 → 2+1360) T21 glyphs again, the 2 remaining structural being two dot-engine edge routes (`g[21]`/`g[22]` `path/@d`, x shift 653 px, canvas 1230 vs 1391 — GEO3a's dot delta, pre-existing); rusuzi (12+78 → 10+182 at T18–T21, → 3+182 at T22b row 75) — its 3 remaining structural are the title's unparsed `<font size=18>` creole (B7/T25's seam, not B6's). (b) Gradient-id permanent cost, 4 fixtures: dizuse 4 → 6, givofi 15 → 17, popesa 14 → 17, taceve 7 → 14 — T20's E1 header split emits one more `<linearGradient>` per split shape (row 67) and each costs exactly one `@id` (row 62, `SvgGraphics.java` seed-36 id; cross-engine follow-on). No rise is unexplained; none is a b5-conformant loss. | `scratchpad/pindiff-b5-b6.txt`, `render-diff` puvono/rusuzi/gamevo 2026-09-22 |
| 95 | 2026-09-22 | T27 | Diagnosis before any fix: moxobo-16-tipo829/zikabo-17-gugi332/gadufu-56-votu808 (class-body `{{ }}` embeds) diverge because `src/diagrams/class/parser.ts#handlePendingBodyLine` (~157-193), the ONLY consumer of a line inside an open `class C { ... }` body, has no embedded-block detection at all — every line is parsed individually via `class-member-parser.ts#parseMemberLine`, confirmed by decoding moxobo's current output: `<text>{{</text><text>file f</text><text>}}</text>`, three literal rows, not an `<image>`. `class-embedded-block.ts`/`class-multiline-element.ts` (this task's assigned write-set) are NOT the mechanism: `handlePendingBodyLine` is checked BEFORE `continueMultilineElement` in the main loop, so a brace-body line never reaches them — those two files only fire for TYPE0/TYPE1 `[ ... ]`/`as "..."` descriptive-leaf bodies, a different syntax, and even there (`tests/unit/class/class-multiline-element-embedded-block.test.ts`) the embedded region is kept as raw unrendered text, never an image. | `.agent-notes/cdd-T27.md` §Diagnosis; moxobo `plans/class-divergence-drive/measurements/out/moxobo-16-tipo829.ours.svg` |
| 96 | 2026-09-22 | T27 | Second, independent finding: `core/cucadiagram/MethodsOrFieldsArea.ts` — which D9 and the T27 brief both name as what a class-body embed "reaches" — is dead code for class-body rendering. Grepped every real (non-doc, non-type-only) import in `src/`: the only production consumer is `class-port-rows.ts:325`'s reuse of `MethodsOrFieldsArea.prototype.getElected` as a pure static utility; nothing under `src/diagrams/` constructs `new MethodsOrFieldsArea(...)`. Matches the pre-existing ADR-5 ruling ("the class engine's parser.ts fork is NOT touched here", cited at `MethodsOrFieldsArea.ts:188,373`, `BodierLikeClassOrObject.ts:200`, `Member.ts:24`) — this task's mechanism search re-confirms that fact from a fresh angle rather than contradicting it. Consequence: task item 4 ("wire the renderer into MethodsOrFieldsArea.ts's config-supply path") is necessary-but-not-sufficient — there is no real producer of `MethodsOrFieldsAreaConfig` anywhere in `src/diagrams/class/` to wire into. Stop condition 1 (needs files outside the write-set): fixing moxobo/zikabo/gadufu needs `src/diagrams/class/parser.ts#handlePendingBodyLine`, at least one class-body geometry/render file (`class-body-enhanced-geometry.ts`/`-layout.ts`, `renderer-body-enhanced.ts`/`renderer-classifier-box.ts`/`class-member-rows.ts`), and a `renderSync`-injection seam in `src/index.ts` (nothing under `src/diagrams/class/` can import `src/index.js` directly without a real circular import through the plugin registry) — none in T27's write-set. Recorded, not edited, per the task's own boundary instruction; filed as a follow-on task (write-set above) in `.agent-notes/cdd-T27.md`. | `.agent-notes/cdd-T27.md` §Consequence; import grep across `src/` for `MethodsOrFieldsArea\|BodyEnhanced1\|BodierLikeClassOrObject` |
| 97 | 2026-09-22 | T27 | Built, in-write-set: `src/diagrams/class/class-nested-diagram-renderer.ts` (new file, pre-authorized by the brief) — a real `NestedDiagramRenderer` taking an injected `renderFn` (avoids the circular import above), rendering the collected `{{ }}` lines, stripping `<?plantuml ...?>` PIs (`EmbeddedDiagram.java:199`), and wrapping the result as a `data:image/svg+xml;base64` `<image>` sized from the nested render's own `viewBox`. `EmbeddedDiagramDepthError` + `MAX_NESTED_DIAGRAM_DEPTH=24` (port-introduced, no upstream citation — upstream has no bound, it overflows the JVM stack) guard recursion via a closure-scoped counter, incremented before `renderFn` and decremented in `finally`. `tests/unit/class/class-nested-diagram-renderer.test.ts`: 8 real tests (real `renderSync`, no mocks) covering dimension/payload/`<image>` shape, wiring through a REAL `EmbeddedDiagram` instance (the exact seam `MethodsOrFieldsArea.ts` consumes), and the recursion guard (throws at the bound, names/messages itself correctly, resets cleanly via call-count accounting, default bound applies); plus 2 `it.todo()` entries (this repo's established precedent) documenting exactly what full fixture conformance needs, rather than a false-passing or silently-dropped test. `DIVERGENCES.md`: new entry "Embedded `{{ }}` sub-diagrams — SVG source, not a re-encoded raster (deliberate, CDD T27)", mirroring the Sprite/img entry's shape, with an explicit Status paragraph stating the renderer is unwired as of this task. | `npx vitest run tests/unit/class/class-nested-diagram-renderer.test.ts` → 8 passed, 2 todo; `npm run typecheck`/`lint` clean |
| 98 | 2026-09-22 | T27 | Readings unchanged before -> after (no production render-path file was edited; `MethodsOrFieldsArea.ts`/`class-embedded-block.ts`/`class-multiline-element.ts` were read, not edited — no in-write-set change was possible without the stop-1 wiring above): moxobo-16-tipo829 1/43, zikabo-17-gugi332 1/43, gadufu-56-votu808 1/3, bixogo-47-xulu385 1/47, roxosu-00-pini153 1/47 — all five identical to the brief's own "before" readings. bixogo/roxosu (legend/chrome path) confirmed still pending T28 as instructed; not attempted from this write-set. Per-fixture jar-dimension conformance cannot yet be assessed for moxobo/zikabo/gadufu (none of the three currently construct any `EmbeddedDiagram`/nested render at all) — recorded as an open question in `.agent-notes/cdd-T27.md`, not a false conformance claim; the renderer DOES reproduce jar-shaped `<image>` geometry when driven directly (proven against real `renderSync` output in the new test file). | `npx jiti plans/class-divergence-drive/tools/render-diff.mts moxobo-16-tipo829 zikabo-17-gugi332 gadufu-56-votu808 bixogo-47-xulu385 roxosu-00-pini153` — identical structural/numeric counts before and after this task's commit |
| 99 | 2026-09-22 | T27 | CORRECTION to `.agent-notes/r2b-embedded-42x42.md`: that finding (jar takes the `(42,42)` catch fallback, so class goldens encode the fallback size, not a real nested render) was true for the OLDER cache it probed (xadado-92-lazo250, 2026-08-05) but is FALSE for the CURRENT cache this mission uses. Direct evidence: `test-results/dot-cache/class/moxobo-16-tipo829/in.svg` holds `<image width="43" height="54" x="13" y="43" xlink:href="data:image/svg+xml;base64,...">`; base64-decoding the payload yields a genuine nested SVG (`<svg height="54" width="43" ...><g ...><!--entity f--><g class="entity" data-qualified-name="f" ...><path .../><text ...>f</text></g></g></svg>`) — a real `file f` render, not a 42x42 box. zikabo (67x64 @ 13,57) and gadufu (133x107 @ 13,75) cross-checked the same way. The `(42,42)` fallback mechanism r2b documented is still correctly ported (`core/EmbeddedDiagram.ts:409-421`, unit-tested) — it is simply not what this cache's class-body fixtures exercise; `xetase-70-zaza808` (unterminated `{{` in a state edge label) is the fixture that genuinely hits it, correctly out of this task's scope. | `python3 -c` base64-decode of moxobo/zikabo/gadufu's cached `in.svg` `<image>` payloads, this task |
| 112 | 2026-09-22 | T27FU | Resumed on `cdd/t27` from `f350acec` with an extended write-set (stop 1 accepted). Re-read the Java rather than trusting the prior citation: `BodierLikeClassOrObject#getBody` (java:214-255) routes to `BodyEnhanced1`/`BodyFactory.create1` whenever `isBodyEnhanced()` (java:93-100) is true, and that predicate's fourth OR-clause (`EmbeddedDiagram.getEmbeddedType(s) != null`, java:96) makes ALL THREE of moxobo/zikabo/gadufu "enhanced" upstream, with no separator/tree line needed. `BodyEnhanced1#buildTextBlock` (java:189-195) then builds `new MethodsOrFieldsArea(...)` PER ROWS-BLOCK — this port's analog is `class-body-enhanced-layout.ts#buildRowsBlockRows`. This port's `isEnhancedBody` (`class-body-enhanced.ts:94-97`) is missing that exact disjunct, so moxobo/zikabo (no separator/tree marker) still route to the CLASSIC path today; gadufu (`-- subsection --`) is already enhanced and fully reachable. One-line fix recorded, not applied (file EXCLUDED, T24 concurrent edit): add `\|\| rawLines.some((s) => getEmbeddedType(s) !== null)` to `isEnhancedBody`'s predicate. | `.agent-notes/cdd-T27.md` §"Corrected mechanism" |
| 113 | 2026-09-22 | T27FU | Landed: `class-body-enhanced-embeds.ts` (new sibling split, same 500-line-cap reason as `class-body-enhanced-geometry.ts`) ports `MethodsOrFieldsArea.java:109-123` (embed separation, reusing `class-embedded-block.ts#scanEmbeddedElementBlock`'s already-correct scan) and `:141-152/429-440` (stack below member rows); `class-body-enhanced-layout.ts` wires it into `buildRowsBlockRows` with an optional `EnhancedLayoutCtx.nestedRenderer` DI slot; `renderer-body-enhanced.ts` draws one `<image>` per embed via `core/svg.ts#image` (NOT the sprite/img inline-atom's own bottom-aligned-to-text-line branch — a different upstream mechanism); `src/index.ts#prepareBlock` registers the renderer closing over the CURRENT call's `options`. Two real bugs caught and fixed before commit: (1) the renderer was first registered ONCE at module load with no measurer, so a nested embed used a DIFFERENT (default) text measurer than the outer diagram — jar-inconsistent by architecture (`EmbeddedDiagram`'s nested `Diagram#exportDiagram` shares the enclosing `FileFormatOption`); moved registration into `prepareBlock`, closing over `options`. (2) the recursion-guard's depth counter was closed over PER renderer instance; since `prepareBlock` re-registers a fresh instance on every (including every recursive) `renderSync` call, this reset the counter to 0 on each level and would have let true self-embedding recurse until a raw stack overflow — moved `depth` to module scope, safe because `renderSync` is fully synchronous and every recursion chain fully unwinds it via `finally` before any top-level call returns. Also found, NOT fixable in this task: a real self-embedding fixture (`class C { {{ class C { ... } }} }`) hits a MORE SEVERE, pre-existing gap than `isEnhancedBody` — `parser.ts#handlePendingBodyLine` has NO embedded-block awareness at parse time at all, so a nested class declaration's own bare `}` inside a `{{ }}` region prematurely closes the OUTER body (verified: renders "Syntax Error?", not a depth error), for ANY recursion depth >= 2, regardless of the guard. The guard itself remains proven at the unit level (4 tests, real depth accounting). | gadufu: 0 structural/49 numeric -> 0/5 after the measurer fix; probe `renderSync(src)` (default) = 200x96, `renderSync(src, {measurer: WidthTableMeasurer})` = 121x96 |
| 114 | 2026-09-22 | T27FU | Remaining gadufu residual (5 numeric: image 121x96 vs jar 133x107, box height 168 vs 114) traced with arithmetic, not chased: `114 - header(32) - block1(~22) = 60`, and `42 (EmbeddedDiagram.java:150-152's own fallback) + ~18 (decorate()'s divider/title margin) = 60` exactly — the jar's OWN sizing pass for this embed took the `(42,42)` catch fallback (r2b's original finding, correct for THIS fixture) while its OWN drawing pass succeeded and drew a real, box-overflowing 133x107 image (canvas height 183 = image bottom 75+107+1, not the declared box) — an internally-inconsistent upstream quirk for this embed/environment combination, recorded per the coordinator's instruction, not reproduced. Mid-task addition (coordinator, from T24 journal row 83): `class-member-rows.ts#isMethodMember`'s raw-fallback paren scan now strips an embedded `[[url{tooltip}]]` bracket first (`BodierLikeClassOrObject#isMethod`, java:104-116, `URL_PATTERN = Pattern.compile(UrlBuilder.getRegexp())` — a new un-anchored `URL_BRACKET_RE` reproducing `UrlBuilder.java:52-88`'s 5-alternative grammar, find/replace-all mode since a bracket can sit anywhere in a raw-fallback line) — fixes `sejuzo-42-fini523` (a field's tooltip containing "(pagename)" was misbucketing it as a method). All four gates green (782 test files, 22043 tests, 0 failed, coverage 96.46/91.91/97.52/97.46); JSON-reporter collected count (782) matches `find tests -name '*.test.ts' -not -path 'tests/e2e/*'` exactly; no non-class test moved. | render-diff: moxobo 1/43, zikabo 1/43, gadufu 0/5, bixogo 1/47, roxosu 1/47 (all unchanged except gadufu), sejuzo-42-fini523 pass=true 0/0; `npm test` full run, `npm run typecheck`/`lint`/`build`/`catalog` clean |
| 100 | 2026-09-22 | T28 | Chrome text now runs the SHARED klimt creole pipeline, not a local parseCreole trio. `Style#createTextBlockBordered` (`style/Style.java:353-369`) opens with `note.create0(fc, alignment, spriteContainer, lineBreak, CreoleMode.FULL, null, null)` (java:358-359) and is the ONE method all five elements reach (`EntityImageLegend.java:47-55`; `DiagramChromeFactory.java:342-356/362-376/382-413`, the last via `DisplayPositioned.java:118-128#createRibbon`). New `src/core/annotations/blocks-creole.ts` (pre-authorised 500-line split) builds `Display.create(lines).create0(...)` over a local `AtomOps`/`ISkinSimple` bundle (the third such bundle in core -- `descAtomOps`/`titleAtomOps` are the precedent) and emits it through `document-shell.ts#renderDrawableToFragment`; `blocks.ts` keeps `TextBlockBordered`+`TextBlockMarged` (padding, rect, the +1 quirk, margin). Zero new creole code -- every primitive imported. | kacico-91-bati232 23 structural + 63 numeric -> 0 + 13, legend children 10 -> 34/34 (table AND tree drawn); manube-50-xora983 12+51 -> 4+0; galili-87-zivo129 7+48 -> 3+0; rusuzi-21-kile910 3+182 -> 0+0 CONFORMANT |
| 101 | 2026-09-22 | T28 | `TextBlockBordered#drawU`'s `ugOriginal.apply(color)` (`klimt/shape/TextBlockBordered.java:141`, `color` = border colour, or the resolved background when `noBorder()` java:114-119/126-134, else `HColors.none()`) is load-bearing for chrome: `CreoleHorizontalLine` and a table's rules paint with the graphic FOREGROUND. Ported as a `UDrawable` wrapper applying `new Fore(color)` in `blocks-creole.ts`; text is unaffected (each atom carries its own `FontConfiguration.color`). | usecase/pivudu-29-pele178's legend `----` rule went stroke:none -> style="stroke:#000;stroke-width:1;", byte-identical to the jar; that fixture's description diff-baseline ratchet FAIL -> PASS |
| 102 | 2026-09-22 | T28 | STOP 1 (a), blocking: an EMPTY klimt content `<g>` serialises SELF-CLOSING (`<g font-family="sans-serif" lengthAdjust="spacing"/>`), and `document-shell.ts#unwrapContentG` (:304-311) requires a trailing `</g>` -- it throws, `renderSync`'s outer catch (index.ts:386) returns an error page, so the fixture reports routing NONE. Reached two ways: a `{{ }}` embedded diagram (its chrome renderer throws per D9/T27, `EmbeddedDiagram.drawU` catches and draws nothing) and a chrome line whose only atom is an unresolvable sprite. Exact change, measured then REVERTED (out of write-set): `if (/^<g(?:\s[^>]*)?\/>$/.test(withoutPi)) return '';` as the first statement of `unwrapContentG`. | 14 fixtures newly errored (class bixogo/roxosu, object zuvila, sequence nereka, usecase sprite-SVG-fill-management-3/tatori/vivido, unknown basoto/locoge/nulevu/semutu/sofobo/sprite-SVG-fill-management-0/1); with the one-line probe: 1 |
| 103 | 2026-09-22 | T28 | STOP 1 (b), blocking: the chrome seam has no sprite/emoji/img channel, and the gap CRASHES rather than merely dropping ink. `sequence/nereka-67-deco609`'s legend cell `|<#blue><color:#CE93D8><$docker>    |` resolves to nothing, the cell measures 0 wide and `TextBlockMarged#drawU`'s `UEmpty.create(dim)` hits `UEmpty`'s faithful `width == 0` guard (`klimt/shape/UEmpty.java:45-47`). 24 corpus fixtures put a non-text atom in chrome text; before T28 they drew the literal markup. Exact change: thread the resolver bundle `src/index.ts` (both applyChrome call sites) + `tests/oracle/svg-conformance/render-fixture-{class,state,sequence}.ts` -> `applyChrome` -> `buildAnnotationBlock` -> `blocks-creole.ts#chromeAtomOps`. | nereka-67-deco609 errors with `width=0` under BOTH the probe and without it; grep over every cached in.puml counts 24 chrome lines carrying `<$`/`<:`/`<img`/`{{` |
| 104 | 2026-09-22 | T28 | STOP 5, blocking: `activity/letare-59-gore448`'s weightedScore ROSE 72 -> 88. Mechanism, not benign: its legend is a `#`-numbered list of `[[url label]]` links. The jar draws `1.` + an `<a target="_top" href=...>`-wrapped run; we now split the run (progress) but still draw a literal `#` and no `<a>`, so positional pairing shifts. Needs `CreoleMode.FULL`'s `#`/`*` list patterns (`CreoleStripeSimpleParser.java:119-147`, never ported for either mode -- `class-member-creole.ts`'s own doc comment records that) plus `<a>` emission in the klimt text driver. Both outside T28's write-set (legacy/ is read-only, T26 owns the atom files). | activity diff-baseline ratchet: letare-59-gore448 baseline 72, now 88; the jar's own legend group shows `1.` + `<a ... href="http://dx.doi.org/10.1056/NEJMoa1011923">` |
| 105 | 2026-09-22 | T28 | Non-class MOVERS, mechanised, awaiting re-pin (blocked with the rest): activity/letare-59-gore448 (style census fontSize 8: 3->5, textCount 5->7, width 345->186; text census fill #00F 0->2), activity/lidefe-01-vaki092 (fontSize 14: 1->2, textCount 4->5), activity/zejuso-92-kexo870 (fontSize 14: 7->16, strokeWidth 1: 22->31, textCount 24->33, width 776->782, height 682->700). One mechanism for all three: a chrome line that was ONE `<text>` is now one `<text>` per creole run/cell, so every census that counts text elements rises. Files to re-pin once unblocked: `oracle/goldens/svg-activity/{style,text}-baseline.json`. | the three census gates name the exact histogram deltas above; description/pivudu-29-pele178's rise self-resolved via row 101 |
| 106 | 2026-09-22 | T28 | `nucite-98-kuga991` and `nufini-44-jofo787` are NOT chrome creole: both are `<style> class|note { MaximumWidth N }` body word-wrap (nucite 100, nufini 150/100). Neither source has any chrome at all beyond nucite's plain `title only class`. `fixtures.md`'s T24 (member text) hint is CORRECT and was left untouched -- the mechanism-column correction T28 was authorised to make is not warranted. | render-diff before and after the seam change: nucite 10+7 -> 10+7, nufini 8+5 -> 8+5, byte-identical; our runs read `Long Long Long` where the jar wraps at 100px into 23/24 children |
| 107 | 2026-09-22 | T28 | `ropera-76-jico895` is NOT chrome either: its title renders byte-identical to the jar already. The 3 structural diffs are the class node's own `<style> class { FontStyle italic; FontSize 18; header { FontStyle bold; FontSize 14 } }` cascade -- member rows draw 14/upright where the jar draws 18/italic. `repuga-78-xora226` is a preprocessor defect, not chrome: `RE_SKINPARAM_LINE` (`src/core/preprocessor.ts:107`) is case-SENSITIVE while upstream compiles every command regex with `Pattern.CASE_INSENSITIVE` (`regex/Pattern2.java:114`, over `CommandSkinParam.java:58`'s `(skinparam|skinparamlocked)` leaf), so `skinParam CaptionFontSize 10` is dropped WHOLESALE -- `preprocess()` returns an empty skinparam map. General to any non-lowercase spelling anywhere in the corpus; one `/i`, in a file no batch-7 task's write-set names. | repuga/ropera unmoved by the seam change (3+47 and 7+55 both sides); `preprocess()` over repuga's source returns `[]` |
| 108 | 2026-09-22 | T28 | Named remainders introduced by the seam, all recorded rather than papered over (`.agent-notes/cdd-T28.md`): (1) sprite/emoji/img/`{{ }}` atoms in chrome draw nothing -- the same shape `.agent-notes/C1-sequence-creole-seam.md` records for sequence; (2) `<back:color>` still mints no `feFlood` filter (galili 3 structural, manube 4, beruje 2) because `FontConfiguration` has no `extendedColor` field, exactly as `CommandCreoleStyle.ts`'s own doc comment and `.agent-notes/cdd-T19.md` already state -- the class-side half of M7 is unchanged by this task; (3) legend's `LineBreakStrategy` is NONE for every element, because `plantuml.skin` declares `MaximumWidth` nowhere and `wrapWidth` maps to `SName.element` only (`FromSkinparamToStyle.java:250`), so `style.wrapWidth()` (`Style.java:330-333`, `EntityImageLegend.java:54`) resolves empty. | grep over plantuml.skin: zero MaximumWidth occurrences; galili/manube/beruje residual diffs are `defs[1][childCount]` + `text/@filter` only |
| 109 | 2026-09-22 | T28 | HALTED, nothing merged. `npm test` on `cdd/t28` is RED by 13 (catalog drift + 3 activity census pins + activity letare ratchet + refusal x2 + routing + sequence nereka x1 ... with the row-102 probe applied; 14 without it, and the seam's own new tests pass 13/15 with 2 `it.todo`). Two stop-1 items (rows 102, 103) and one stop-5 item (row 104) each need a file outside the write-set; per the task's own boundary the items were recorded, not taken. The code, its tests and these rows are committed on `cdd/t28` as WIP and MUST NOT be merged until rows 102/103 land and rows 104/105 are re-pinned. | full-suite runs at 455afc26 (green) and on the branch (13-14 failures) both recorded in `.agent-notes/cdd-T28.md` |
| 113 | 2026-09-22 | T28 | CORRECTION to rows 102/103/104/109 (write-set EXTENDED by the coordinator, main-tree row 112): all three blockers were taken and the task is COMPLETE, not halted. `npm test` is GREEN (780 files collected == 780 on disk, 22050 tests, 0 failed), typecheck (both tsconfigs), lint and build all pass. Row 102's one-liner landed in `document-shell.ts#unwrapContentG` (an EMPTY content `<g>` serialises self-closing; it now returns '' for exactly that shape and still throws for every other, with a unit test covering the empty/PI-prefixed/non-`<g>`/`<g/><rect>` cases). Row 103's resolver channel landed: `src/index.ts#spritesOf(ast)` (the SAME `'sprites' in ast` read `sprite-registry.ts#surfaceSpriteWarnings` uses) -> `applyChrome` -> `buildAnnotationBlock` -> `blocks-creole.ts#chromeAtomOps`, which now resolves `<img:>`/`<$sprite>` through the SHARED `makeAtomImageResolverFor` and draws emoji via `drawEmojiAtom`; `ISkinSimple.getSprite` is wired to the registry too. | kacico-91-bati232 0+0 CONFORMANT (was 23+63); repuga-78-xora226 0+0 CONFORMANT (was 3+47); rusuzi-21-kile910 0+0 CONFORMANT (was 3+182); zero fixtures error; all 27 chrome-markup fixtures fall or hold on structural diffs |
| 114 | 2026-09-22 | T28 | `CreoleMode.FULL`'s three list branches ported (`CreoleStripeSimpleParser.java:119-144`, all three patterns at java:70-72 incl. the SECOND asterisk arm and the `order = run.length-1` nesting depth), gated on FULL so `CreoleMode.SIMPLE_LINE` (every class member row) still draws a leading `*`/`#` literally. `StripeStyle#getHeader`'s two seams became real atoms: `atom/Bullet.ts` (`Bullet.java` in full) and `legacy/AtomTextUtils.ts#createListNumber` (`AtomTextUtils.java:145-159`, both DelayedDouble margins); `CreoleParser#createSimpleStripe` prepends the header to the line's atoms exactly as `StripeSimple.java:117-121` does. The `<a>` wrapper: `SvgGraphics#openLink/closeLink` + the `LinkData` nested class (`SvgGraphics.java:1130-1174,1202-1264`) ported over the D3-prime stubs, `UGraphicSvg#startUrl/closeUrl` (java:159-167, `_top` from `SkinParam.java:1082`), `UGraphicDelegator` forwards both (java:84-91 -- without that, `SheetBlock2`'s own `UGraphicStencil` wrapper swallowed the capability), and `AtomText.java:197-198,235-236`'s bracket is reproduced in `blocks-creole.ts#drawAtom`. | JAR-PROBED directly (scripts/oracle-render.sh, a fresh 6-line legend): the jar draws `<ellipse cx="22.5" cy="99.5" rx="2.5" ry="2.5">` + text at x=29 for `* Hyp 1`, and `1.` textLength 11.638 at x=17 + text at x=32.488 for `# Num 1` -- byte-identical to this port's output. letare-59-gore448 weightedScore 72 -> 37, structural 5 -> 0, numeric distance 1243.9 -> 280.73 |
| 115 | 2026-09-22 | T28 | `RE_SKINPARAM_LINE`/`_BLOCK_OPEN`/`_SELECTOR_BLOCK_OPEN` are `/i` now (`Pattern2.java:114` compiles EVERY upstream command regex `CASE_INSENSITIVE`, covering `CommandSkinParam.java:58` and `CommandSkinParamMultilines.java:49`). Movers, all with that one mechanism: `sequence/soxata-16-kafi688` was pinned `status: "error"` and its own pinned `reason` NAMED this exact fix as the cause -- it renders now and is re-pinned `status: "baseline"`, weightedScore 21 / diffCount 21, in `oracle/goldens/svg-sequence/diff-baseline.json`; `oracle/goldens/svg-sequence/diff-census.json` regenerated through the gate's OWN builder (`sequence-diff-census.ts#buildCensus`) because that file must hold every non-errored baseline fixture exactly once. Census totals: missing-element 281->269, extra-element 120->113, text-metrics 2509->2473, other 25966->25938, geometry 72911->72946 (+35); 22 rows moved, 15 fell, 6 rose, 1 new. | the three largest census risers re-measured element-level: ramive-48-vabu271 structural 28->19 w 161->96; rilefo-62-jedi773 structural 25->18 w 130->82; jujigi-41-zodo151 structural 24->25 w 121->117 -- every one's GATED weightedScore fell, and the sequence weighted-score ratchet is green |
| 116 | 2026-09-22 | T28 | Two scope notes and the re-pins. (a) SCOPE, flagged not silent: three files outside even the extended write-set were needed and are named here -- `src/core/klimt/creole/StripeStyle.ts` and the new `src/core/klimt/creole/atom/Bullet.ts` (upstream's own method/file for the list header; implementing them under `legacy/` would misplace an upstream API), and `src/diagrams/{class,sequence}/parser.ts`. The parser edit is the mechanism behind what would otherwise have been three red pins: BOTH parsers fully trim every line before the annotation matcher, so a legend body line ` * Hyp 1` lost the leading space that keeps the jar's column-0-anchored list patterns from matching. Each now passes its raw (trimEnd-only) lines with ONLY index i trimmed -- `description/annotation-line-trim.ts#trimLineForAnnotationMatch`'s established shape, duplicated per that file's own cross-engine convention. (b) The activity censuses re-pinned via the gate's own tool (`scripts/repin-activity-baselines.ts --slugs-file --write`), never by hand. | class/conara-44-fisa089 and class/zegeso-35-xiko243 back to 0+0 (the jar draws `* Hyp 1` as literal text, textLength 40.875, because of that leading space); sequence/dofuru-22-zuga032 back to its 48 baseline exactly; activity element-level letare 5/1243.9/72 -> 0/280.73/37, lidefe 16/4862.26/141 -> 13/4790.5/133, zejuso 59/23250.41/581 -> 43/22398.18/488 -- structural, distance AND weightedScore all fall on all three, which is what justified the re-pin |
| 80 | 2026-09-22 | T24 | A4 2a landed as ONE change (both halves, per the fix-shape note): `class-body-enhanced.ts#buildTreeRun` dropped its spurious `.trim()` (no Java counterpart — `StripeTree#analyzeAndAdd`, java:80-90, is `s.replaceFirst("^\\s*\\|_", "")` and nothing else, leaving a single leading space on `text`); `resolveOneAtom`'s mixed-content branch (new `class-member-creole-render-text.ts#stripLeadingSpacesAndTrin`/`trin`, `DriverTextSvg.java:112-125`, `StringUtils.java:505-534`) strips it back off at RENDER time only, mirroring the jar's own two-step (layout keeps the raw/untouched width, draw strips-then-`trin`s). No x-advance ported for the leading-space characters themselves: this port's deterministic `WidthTableMeasurer` reports a bare space as 0-wide (pre-existing, `SANS_SERIF_BLOCKS[0][32]`), so upstream's `x += space` is a no-op under the SAME table both sides render with — confirmed empirically (foxiki/juxora's tree-level x values match golden with ZERO added shift once the space is simply dropped from the drawn text, not by inventing a compensating offset). | foxiki-17-kosa114 6+3 → 0+0 (conformant); juxora-90-fisu720's ×2 instance of the same family also resolved (see row 82 for its unrelated residual) |
| 81 | 2026-09-22 | T24 | A4 4 landed: `buildMemberAtoms` (`class-member-creole.ts`) now calls `manageGuillemet` (`src/core/text/Guillemet.ts`, already used for stereotypes/edge labels) on the member-row text BEFORE `classifyStripeLine`, mirroring `CreoleParser.java:175-176`'s every-line `skinParam.guillemet().manageGuillemet(cs.toString())`. No skinparam-guillemet threading added (matches this seam's existing no-`skinParam`-param shape); `manageGuillemet`'s own default param is upstream's `Guillemet.GUILLEMET` pair, the correct fallback absent an override — verified against padapo's own fixture (no `skinparam guillemet` line). | padapo-73-beke177 2+42 → 0+0 (conformant) |
| 82 | 2026-09-22 | T24 | juxora-90-fisu720 residual (12+22 → 0+16) diagnosed as PRE-EXISTING and OUT OF SCOPE, not a regression or a remaining 2a/4 gap: `git stash` on the T24 diff reproduces the identical 16-numeric shape byte-for-byte on the UNMODIFIED tree, so neither this task's changes nor any A4-text.md bucket cause it. Mechanism: the `FlatWorks::prop3 -r-> FlatBar::prop` edge (g[5], the FIRST pair of classes — plain members, no tree/guillemet content at all) attaches to the WRONG row inside `FlatBar` — both boxes' own `<rect>` geometry is byte-identical to golden (105.15/93.512, unmoved), only the edge's own path/polygon endpoint differs, meaning the defect lives in the member-port resolution feeding graphviz (`class-port-rows.ts#edgePortAttrs`/`mapPortName`, or `class-dot-edges.ts`), not in any text/creole/layout file in this task's write-set. Ruled out: text/width formulas (identical `textLength` on every quantifier/member text checked); this task's own edits (stash-confirmed unchanged). Handed off — outside T24's write-set (`class-port-rows.ts`, `class-dot-edges.ts` are neither T24's nor named T25's files); flagging for whichever future task owns edge-port/DOT-input row resolution. | `git stash` re-render juxora: identical 0 structural / 16 numeric on `455afc26`; `<rect>` byte-diff on both `d`/`e`(sic FlatWorks/FlatBar) classifier boxes = none |
| 83 | 2026-09-22 | T24 | sejuzo-42-fini523 (3 structural, unchanged) diagnosed, NOT fixed — mechanism outside the write-set. **Mechanism**: `isMethodMember` (`src/diagrams/class/class-member-rows.ts:127`, `if (m.rawDisplay !== undefined) return m.rawDisplay.includes('(') || m.rawDisplay.includes(')');`) checks the member's RAW (unparsed, `[[url{tooltip} label]] : TEXT`-shaped) display text for parens directly, with no URL-stripping step first. **Origin**: `class-member-rows.ts:127`. **Causal chain**: sejuzo's one field is `[[https://…Property:Event_in_series{foreign key(pagename) of the Event series this event belongs to} series]] : TEXT`; the `[[url{…}…]]` shape fails the canonical attribute/method regexes and falls to `rawDisplayFallback` (`class-member-parser.ts:253`), setting `rawDisplay` to the ENTIRE line; `isMethodMember` then sees `(` from "(pagename)" INSIDE the URL's tooltip text and misclassifies the field as a METHOD, so it lands in `methodFlat` instead of `fieldFlat` — the (now-empty) fields compartment still gets a divider at `headerRowHeight` (y=43, coincidentally correct) while the methods compartment's divider lands at `headerRowHeight + 0` instead of `+ fieldsH`, producing the observed spurious `line y=51` and the missing `y=65` bottom divider. **Ruled out**: `measureGenericClassifier`'s own divider-push arithmetic (`class-layout-generic-classifier.ts#buildNormalClassifierResult`/`appendMemberSectionRows`) — read in full, correct GIVEN correct `fieldsH`/`methodsH` inputs; this task's own 2a/4 edits (`git stash` reproduces the identical 3-structural shape unchanged). **Upstream's own correct behaviour** (the fix shape for whoever owns `class-member-rows.ts`): `BodierLikeClassOrObject#isMethod` (`java:107-114`) strips the URL FIRST — `URL_PATTERN.matcher(s).replaceAll("")` — before the `contains("(")` test, so a paren inside a URL's own tooltip never counts. | `parseClass` probe on sejuzo's own `.puml`: `isMethodMember(member) === true`; jar source `BodierLikeClassOrObject.java:104-114` |
| 84 | 2026-09-22 | T24 | focaci-80-suzu938 (0 structural / 95 numeric, unchanged) diagnosed: the task brief's own bucket-7 hypothesis ("`~role` label loses its `~` in the jar but keeps it here") is DISPROVEN by direct measurement, not fixed by a new mechanism. `"~* initiators"` already renders as `"* initiators"` in BOTH text content and measured `textLength` (53.463, byte-identical to golden) via a PRE-EXISTING, already-ported mechanism: `stripLeadingEscapedChar` (`src/core/edge-label-box.ts:403-406`, jar-verified against a live `scripts/oracle-render.sh` probe in that function's own doc comment) — a faithful port of `CharHidder.hide`'s general `~X` creole-escape (`CharHidder.java:47-52`), NOT `Display#manageGuillemet`'s visibility-strip arm (`Display.java:415-416`) as `CommandLinkClass`'s cited lead suggested: that arm runs ONLY from `LinkArg.build` on the edge's own MAIN label (`SvekEdge.java:302`); a QUANTIFIER/ROLE string (`SvekEdge.java:329-351`) is built via `Display.getWithNewlines(...).create(...)` and never touches `LinkArg` at all. The REMAINING divergence is a uniform ~1.7-2.3px rightward shift of every drawn element plus a 2px wider canvas (`viewBox`/`width` 137 vs 135) with BOTH classifier boxes' own WIDTHS byte-identical to golden (only their X positions differ) — a graph-level (DOT/graphviz) x-coordinate-assignment gap, unrelated to text/creole content. Ruled out: the `~`-strip mechanism (byte-identical content+width, confirmed); a duplicate/inconsistent quantifier-width formula between the DOT-input and SVG-render paths (`class-dot-edges.ts:179` and `class-edge-geo.ts:214` call the SAME `edgeLabelAttrs`/`computeQuantifierBox`, so they cannot disagree); this task's own edits (`git stash` reproduces 0+95 unchanged on `455afc26`). Handed off: neither `class-edge-geo.ts`/`class-dot-edges.ts`/`class-dot-graph.ts` nor `@knowvah/dot-engine` are in T24's write-set — flagged to T25 (owns `class-edge-geo.ts`) as a starting point, with the dot-engine angle named for further instrumentation. | golden vs ours `<text>` dump: identical content+`textLength` for both quantifiers; `<rect>` widths identical (105.15/93.512), only `@x` shifted |
| 85 | 2026-09-22 | T24/T26 | Coordinator addition (gekope-01-ricu859, jar-verified `<&key>` byte-identical): ported `AtomText#getWidth`/`#drawU`'s tab-stop walk (`AtomText.java:210-256`) into a new `resolveTabbedTextRuns` (`class-member-creole-render-text.ts`), reusing the ALREADY-PORTED `hasTabulation`/`tokenizeOnTabs`/`tabStopWidth`/`advanceToTabStop` primitives from `core/klimt/creole/legacy/AtomText.ts` (built for description, never wired to class). A tab-bearing atom now resolves to MULTIPLE `MemberRenderAtom`s, one per non-tab token; each non-last token's `width` (the renderer's own x-advance field) folds in the gap to the NEXT tab stop while `renderWidth`/`renderText` are forced to the token's OWN natural/stripped width whenever `width` differs from it, so the glyph draws compact, never stretched — no `renderer-classifier-rows.ts` change needed (it already iterates an array of atoms, advancing by each one's own `width`). Opt-in only: `resolveMemberAtoms` gained a 5th `expandTabs = false` param, set `true` ONLY by `buildMemberRow`/`buildWrappedMemberRows` (the class engine's own two entry points) — `class-object-member-creole.ts#buildObjectMemberRow` (the one OTHER caller of the same shared function) keeps its own, pre-existing, skinparam-tabSize-aware re-tokenization untouched. A first (reverted-before-commit) attempt applying the render-text override unconditionally on tab-bearing text broke `object/nufoju-44-dabi767`'s ratchet (two duplicated "field5\tfield6" runs, `trin` treating `\t` same as space, ASCII<=32) — root-caused via `git stash`/direct render before landing the guard (`textRenderOverride` returns `undefined` for any text containing `\t`; per-token splitting happens ABOVE it, never below). Also found and ported: `Display.java:305-307`'s literal `\t`→real-tab-char conversion was ALREADY correctly ported in `class-member-display.ts#splitMemberDisplayLines` (misdiagnosed as missing mid-task; it was the missing `expandTabs=true` at BOTH of `buildWrappedMemberRows`'s two `resolveMemberAtoms` call sites that actually blocked it). Split `MemberRenderAtom`/`MemberRowBuild` into new `class-member-render-atom.ts` and the render-text helpers into `class-member-creole-render-text.ts` (both pure moves) to keep `class-member-creole.ts` under the 500-line cap through this addition. | gekope-01-ricu859 14+65 → 2+2 (residual = the KNOWN, separate `<b>`-in-quoted-class-name header-creole-routing gap, A4-text.md bucket 2b/5, not this mechanism); object ratchet green (`object/nufoju-44-dabi767` re-verified 0-diff); full suite 22028 passed, 0 failed |
| 85 | 2026-09-22 | T25 | Diagnosis artifact — diseka-11-gozu390 (`<color:#888888><plain>Enumeration</plain></color>\nBookCategory`). Mechanism: TWO causes. (1) `FontStyle.PLAIN` is unregistered in the creole command map — `src/core/klimt/creole/legacy/CommandCreoleBuilder.ts:23-27`'s own doc comment already says so; upstream registers it unconditionally in both FULL/OTHER maps (`CommandCreoleBuilder.java:83-84`, `CommandCreoleStyle.createLegacy`/`createLegacyEol(FontStyle.PLAIN)`, starters `<p`/`<P`, `FontStyle.java:47-48`) — `src/core/` is outside T25's write-set and inside T26's `src/core/klimt/creole/` carve-out (stop condition 1). (2) Independently, `class-layout-header-creole.ts#buildHeaderLineMetrics` built real per-atom creole atoms but discarded them (`class-stereotype-layout.ts#buildHeaderRows` never set `row.atoms`), so `renderer-classifier-rows.ts#renderRowText`'s plain-text branch drew the WHOLE header row at one font/color regardless of any resolved `<color:>`/`<size:>` command — this second cause IS inside T25's write-set (`src/diagrams/class/`) and was fixed (row 89). Causal chain: cause (1) means the literal `<plain>...</plain>` characters survive as ONE text atom's content; cause (2) means even the atom's OWN resolved color (`#888`, from the registered `<color:>` command) never reached the rendered `<text fill>` before this task. Ruled out: NOT a `<color:>` parsing bug (that command IS registered and correctly resolves the hex, confirmed by instrumenting `resolveMemberAtoms`'s output before any fix); NOT a `resolveTextEscapes`/`splitDisplayLines` gap (the `\n` line-break splits correctly into two header rows already, pre-T25). | jar oracle `test-results/dot-cache/class/diseka-11-gozu390/in.svg` vs `plans/.../measurements/out/diseka-11-gozu390.{jar,ours}.svg`; `FontConfiguration.java:301-309`, `DriverTextSvg.java:93-103` (why jar ALSO keeps line 1 bold despite `<plain>` — a two-tier styles-vs-base-face fallback this port's flat `FontConfiguration` (`src/core/klimt/shape/UText.ts:24-59`) doesn't model) |
| 86 | 2026-09-22 | T25 | Diagnosis artifact — daxeno-00-kasu166 (`package "<size:18>styled</size>\nshould be styled" <<Database>> {}`). Mechanism: a PACKAGE display name never reaches `class-layout-header-creole.ts` at all (classifier NAMEs and package titles are two structurally separate title-block families) — traced to TWO un-creole-aware call sites instead: `class-namespace-title-table.ts#namespaceTitleTableDims` (feeds the DOT cluster's `labelWidth`/`labelHeight`/`titleTableWidth`/`titleTableHeight` reservation via `class-dot-clusters.ts:74-90`) and `class-namespace-shape.ts#getWTitle`/`getHTitle`/`renderNamespaceFolder` (draws exactly ONE flat `<text>` per package title at ONE font size). Both call `measurer.measure(ns.display, font)` on the RAW, unsplit, un-parsed string. Upstream builds this title via `ClusterHeader#getTitleBlock` (`svek/ClusterHeader.java:115-142`, `label.create(fontConfiguration, alignment, skinParam)` — the SAME `Display`/creole machinery, real multi-line + per-run creole), and `Cluster.java:367-368` feeds that SAME built TextBlock into the cluster's own dimension — i.e. upstream's DOT-cluster sizing and drawn ink share ONE creole-aware source, never two flat-string call sites. Causal chain: since NEITHER call site splits `\n` or resolves `<size:18>`, both classifier titles get a WRONG cluster reservation (undersized: no multi-line height, no larger-font width for "styled") AND draw the literal tag text at one font size. Ruled out: NOT a header-creole-routing gap (the classifier header path is uninvolved for a package); NOT a missing creole-command registration (`<size:>` IS registered and already resolves correctly for xamule's edge label, row 87) — the gap is architectural (two title-block families, only one ported), not a parser gap. Disposition: genuinely large + separable (spans a DOT-cluster-sizing seam outside T25's write-set) — deferred as a new task, not attempted; corpus blast radius measured at exactly 2 fixtures (`daxeno-00-kasu166`, `jabama-09-kago823`). | jar oracle `in.svg` (two `<text>` elements per package, font-size 18 then 14, matching `Font { Size 14 }`); `class-dot-clusters.ts:74-90`; `.agent-notes/cdd-T25.md`'s own "daxeno" section |
| 87 | 2026-09-22 | T25 | Diagnosis + fix — xamule-03-jeda376 (`Book - Foo : <size:30>to Foo >`). Mechanism: the measurement and render arms of the SAME magic-arrow edge-label mechanism diverged. `class-edge-label-measure.ts#resolveMagicArrowText` (measurement, feeding the DOT box reservation) already called `resolveLineFont` (`core/edge-label-box.ts:76-80`) to strip the `<size:N>` tag and resolve the override font — already correct (a prior fix, "verified correct since 2026-08-08"). `class-edge-label-attach.ts#attachMagicArrow` (render, building `edgeGeo.label`/`edgeGeo.arrowGlyph`) used the RAW `magic.text` and the BASE `ctx.labelFont` unconditionally, drawing the literal `<size:30>to Foo` tag at 13pt instead of "to Foo" at 30pt — upstream has no such split (`SvekEdge.java:304`'s `addMagicArrow` builds the drawn TextBlock and the reserved dimension from ONE resolved font). Fix: exported `resolveMagicArrowText` (one resolver, two call sites) and reused it in `attachMagicArrow`; the arrow GLYPH itself stays at the base font size unconditionally (`TextBlockArrow2.calculateDimension`, `klimt/shape/TextBlockArrow2.java:57,87` — jar-verified "arrow block stays 13" in the pre-existing doc comment). Added `EdgeGeo['label'].fontSize?: number`, consumed in `renderer-edge.ts` (extracted `renderEdgeSingleLabel` to stay under the 30-NLOC hook). Ruled out: NOT a `parseMagicArrowLabel` bug (the arrow-token strip is correct, `magic.text === "<size:30>to Foo"`); NOT a `resolveLineFont` bug (already jar-verified via the measurement-side test). Verified byte-exact: `<text font-size="30" textLength="76.688">to Foo</text>`, matching jar's own element exactly (only `x` differs, from a PRE-EXISTING unrelated multi-edge routing divergence already present before this task touched anything). | `tests/unit/class/class-edge-labels.test.ts` (3 new `describe('fix(cdd-T25)...')` tests); `plans/.../measurements/out/xamule-03-jeda376.ours.svg` vs `test-results/dot-cache/class/xamule-03-jeda376/in.svg` |
| 88 | 2026-09-22 | T25 | Diagnosis artifact — lecelo-92-loma110 (`<:label:> label\n<:wrench:> wrench\n<:hammer_and_wrench:> hammer_and_wrench`), LOW confidence per the brief. Mechanism CONFIRMED (not merely instrumented): `src/core/klimt/creole/Emoji.ts:76,122,166` maps `label`/`wrench`/`hammer_and_wrench` to the SAME Unicode codepoints (1f3f7/1f527/1f6e0) the fixture's OWN `<U+1F3F7>` class already uses — `CommandCreoleEmoji` (`src/core/klimt/creole/command/CommandCreoleEmoji.ts`, T26-owned) was ALREADY resolving `<:name:>` to a platform-Unicode-glyph text atom BEFORE this task (confirmed: the mission's own pre-T25 baseline reading already showed the flattened "🏷 label" text). But jar's real SVG (`test-results/dot-cache/class/lecelo-92-loma110/in.svg`) draws the THIRD class's icons as real multi-`<path>` colored vector artwork (a label-tag glyph, a wrench, a hammer-and-wrench) — NOT a plain-text Unicode character, unlike the FIRST class's `<U+1F3F7>` (`<text>🏷 U+1F3F7</text>`, correctly a plain glyph both here and in jar). This matches `.agent-notes/r2i-creole-class-wiring.md`'s own already-filed "Twemoji artwork not ported" finding (AtomEmoji renders the platform glyph, never bundled vector icon artwork) — a pre-existing, cross-mission gap, not new to T25. T25's own M8b atom-wiring fix (row 89) changed lecelo's RENDER from flattened-plain-text to per-atom (the SAME already-resolved emoji atom now draws at its own font-size, 21, as a separate `<text>`) — `childCount` moved 4 → 7 of the expected 11, a directionally-correct EXPOSURE of the pre-existing gap, not a regression. Ruled out: NOT a new T25 defect (the emoji-resolution mechanism predates this task); NOT the SAME family as T26's OpenIconic `<&glyph>` atoms (per the brief's own caution) — `CommandCreoleEmoji` is a distinct, already-registered command. Disposition: named-but-open, T26 territory (real icon-artwork port), per the brief's own instruction not to force a fix on this LOW-confidence lead. | `.agent-notes/r2i-creole-class-wiring.md` (pre-existing "Twemoji artwork not ported" finding); `.agent-notes/cdd-T25.md`'s "lecelo" section |
| 89 | 2026-09-22 | T25 | Mechanisms/fixes landed + hand-off. Fixed: (a) xamule's render-side `<size:N>` resolution (row 87, full fix); (b) the M8b header-atom-wiring gap (`class-layout-header-creole.ts#buildHeaderLine`/`buildHeaderLineMetrics` now report per-line `hasMarkup`/`headerLineAtoms`, seeding the atoms' BASE font styles via `memberBaseFont` — already-exported, T24-owned, import-only — mirroring upstream's `FontConfiguration.create`→`getStyles(font)` bake-in; `class-layout-header-geo.ts#computeHeaderNameGeo`/`buildHeaderNameRowsGeo` thread it through; `class-stereotype-layout.ts#buildHeaderRows` sets `row.atoms` ONLY for a markup-bearing, non-blank line, so the ~700+ markup-free corpus headers render through the BYTE-IDENTICAL pre-T25 plain-text path — verified via full `npm test`, 22027 passed, zero new failures beyond the expected `docs/catalog.md` drift, resolved with `npm run catalog`). This lands diseka's COLOR half (fill now `#888`, matching jar) and, found via a blast-radius grep (3 real hits beyond diseka/lecelo; 2 more were regex false-positives on `Foo<int>`-style generic names, ruled out by reading the ITALIC activation pattern), FULLY fixes a bonus pre-existing divergence: `gekope-01-ricu859`'s `<b> Some dict </b>`/`<b> Some entity </b>` classifier headers now render `font-weight="700"`, matching jar (structural 14 → 12, the remaining 12 are an unrelated pre-existing JSON/icon-member-splitting bug, not header creole). Halted at stop condition 1 (per the launcher's explicit instruction, "anything under `src/core/` is stop condition 1"): PLAIN command registration (`src/core/klimt/creole/legacy/CommandCreoleBuilder.ts`, T26) and the two-tier styles-vs-base-face weight fallback (`src/core/klimt/shape/UText.ts` + its SVG driver) — needed for diseka's remaining `<plain>` tag-strip. Deferred as genuinely-large-and-separable (not a stop-1, a scope/size call): the namespace/package title creole port (daxeno, row 86) — a new task, not T25/T26. Lecelo (row 88) left named-but-open per the brief. Final before→after: diseka 3/7 → 2/7; daxeno 12/135 → 12/135 (unchanged, deferred); xamule 5/976 → 2/975; lecelo 7/8 → 6/7; gekope (bonus) 14/65 → 12/65. Commits on `cdd/t25`: `fix(cdd-T25): header/edge-label creole — plain, size, xamule`. Quality gates: `npm test` 22027/22031 passed (1 pre-existing skip, 1 todo), `npm run typecheck` (both tsconfigs) clean, `npm run lint` clean, `npm run build` clean. | `.agent-notes/cdd-T25.md`; `plans/class-divergence-drive/tools/render-diff.mts` readings above; `docs/catalog.md` (regenerated) |
| 117 | 2026-09-22 | T25 | Write-set extension (orchestrator-flagged, addendum to rows 85-89). The spec's 4 named files (`class-layout-header-creole.ts`, `class-edge-geo.ts`, `renderer-edge.ts`, `class-edge-label-lines.ts`) were not enough to land either fix: (1) diseka/gekope's M8b atom-wiring fix required `class-layout-header-geo.ts` (`computeHeaderNameGeo`/`buildHeaderNameRowsGeo` — the direct caller of `buildHeaderLineMetrics`, which must thread `headerLineAtoms` through) and `class-stereotype-layout.ts` (`buildHeaderRows` — the actual `ClassifierGeo['rows']` builder, which must set `row.atoms`); `class-edge-geo.ts` never held this logic at all (already delegated to sibling files at split time, per that file's own doc comment). (2) xamule's fix required `class-edge-label-attach.ts` (`attachMagicArrow` — the actual RENDER call site, not `class-edge-geo.ts`/`renderer-edge.ts`/`class-edge-label-lines.ts`, none of which build `edgeGeo.label`), `class-edge-label-measure.ts` (exporting `resolveMagicArrowText` so both the measurement and render arms share one resolver — DRY, avoids a second, driftable copy of the `<size:N>` regex/strip logic), and `class-geo-types.ts` (the `EdgeGeo['label']` interface, to add the optional `fontSize` field `renderer-edge.ts` then consumes). All 6 files are inside `src/diagrams/class/`, none owned by T24 (`class-member-creole.ts`, `class-body-enhanced.ts`, `class-layout-generic-classifier*.ts`, `class-layout-helpers.ts`), T26 (`src/core/klimt/creole/`, `renderer-openiconic.ts`, `renderer-bullet-atom.ts`, `class-member-atom-resolve.ts`), or T28 (`src/core/annotations/`) — confirmed by grep before editing, not assumed. `renderer-edge.ts` (one of the 4 named files) WAS used, as planned, for the `fontSize` consumption + the `renderEdgeSingleLabel` NLOC-cap split. Per-file complexity gate: `check-complexity.py` flagged two NLOC-cap risks mid-edit (`computeHeaderNameGeo` 45→46, `renderEdgeMainLabel` 34→35) — both resolved with a pure-move split (`splitAndWrapHeaderLines`, `renderEdgeSingleLabel`) rather than growing either function, net zero behavior change. Tooling note: two `mcp__serena__replace_symbol_body` calls resolved against the MAIN checkout instead of this worktree (Serena resolves relative paths against the project root, not the worktree cwd) — the orchestrator caught and reverted the stray main-tree writes before this row was written; every edit after that point used `Edit`/`Write` with an absolute worktree path, and `git -C .../plantuml-ts status --short` was re-confirmed clean of this task's changes before the commit below. | `git -C /Users/scottseely/git/knowvah/plantuml-ts status --short` (clean, re-verified); `git diff --stat` on `cdd/t25` (see commit) |
| 90 | 2026-09-22 | T26 | Diagnosis-first pass on all six named fixtures (per the brief's own "verify before implementing" instruction) found the diagnosis report mislabeled THREE of six as sprite/image work: `manube-50-xora983` and `ziripa-77-zizo842` contain ZERO `<$sprite>`/`<img:>` markup (manube is a `legend` pipe-table with `<back:color>` cells; ziripa is `<u:color>`/`<w:color>`/`<s:color>`/`<back:color>` creole decorations). A3-style.md §M7's own cited mechanism is disproven by direct jar SVG read: `beruje-75-jimu270`'s ONE `<filter>` (`feFlood`) is attached to `<back:#FFF000>`, not to `<w>This is wave</w>` (which already renders byte-exact `text-decoration="wavy underline"`, no filter, both here and on the port) — M7's "wave needs a filter" claim is factually wrong for its own fixture. | `.agent-notes/cdd-T26.md`; `beruje-75-jimu270` render-diff `structural=2` both attributable to the un-ported `<back:>` filter |
| 91 | 2026-09-22 | T26 | FIXED `jabama-09-kago823` (diverged -> conformant): `namespace "MyNamespaceName <img:HelloWorld.png{scale=1.5}>"` is a real `<img:>` reference to a plain file path, which this browser-safe port (no filesystem access) always resolves to upstream's own non-INSECURE-profile `(Cannot decode)` fallback (`AtomImg.java:171-177`) — already fully ported (`creole-atoms.ts#CANNOT_DECODE_TEXT`, `StripeSimple.ts#IMG_FALLBACK_FONT`/`buildLineAtoms`), just never wired into the namespace/cluster-title render path. New `src/diagrams/class/class-namespace-title-runs.ts` (`namespaceTitleRuns`/`namespaceTitleWidth`/`renderNamespaceTitleRuns`) routes `class-namespace-shape.ts#getWTitle`/`renderNamespaceFolder` and `class-namespace-title-table.ts#namespaceTitleTableDims` through the shared creole atom lexer instead of one raw string measure; a markup-free label reduces to the byte-identical pre-existing path (verified by test + full-corpus re-survey). `renderer.ts`'s one call site threads its already-available `measurer`. Write-set extended beyond T26's literal list (none of these files are owned by T24/T25/T27/T28) per `decisions.md`'s push-forward "ambiguous ownership" clause. | `npx jiti tools/render-diff.mts jabama-09-kago823`: structural=0 numeric=88 -> structural=0 numeric=0; full 723-fixture `npm run svg:survey -- class` + `pin-diff.mts` against committed `parity-class.json`: exactly 1 transition (`jabama-09-kago823: diverged -> conformant`), 0 regressions, 491->492/104/128->127 |
| 92 | 2026-09-22 | T26 | STOP (stop 1): `rotisi-30-loge424`/`malara-55-moce209`'s member-row sprites already render byte-exact `<image>` elements (R2i's prior work); their sole residual is the diagram TITLE (`title I am <$bug16>...`), drawn by `src/core/annotations/blocks.ts#buildAnnotationBlock` (D5's chrome seam, T28's write-set) as one raw string per line, no creole atom resolution. One-line fix needed: route `measureLines`/`drawLines` through `buildLineAtoms` the way `class-namespace-title-runs.ts` (row 91) now does for namespace titles. `manube-50-xora983` has NO sprite content (row 90) — its `legend`/`endlegend` pipe-table is drawn as literal unparsed text (creole/table parsing does not run inside `legend` at all); also D5's chrome seam (legend is explicitly named alongside title), and needs MORE than `buildLineAtoms` (the creole-TABLE-cell parser). Neither fixed; both out of T26's write-set (`src/core/annotations/`). | `.agent-notes/cdd-T26.md`; `measurements/out/rotisi-30-loge424.ours.svg`'s member-row `<image>` elements match the oracle byte-exact |
| 93 | 2026-09-22 | T26 | `ziripa-77-zizo842` disposition: NEITHER this task NOR M7 (row 90 disproves M7's own attribution) — a THIRD, previously-unnamed mechanism, "creole extended-colour decoration rendering". `<u:color>`/`<s:color>` need a manual coloured `<line>` under/through the text (jar: `stroke:#F00`/`stroke:#0FF`, `stroke-width:0.464`) instead of the CSS `text-decoration` this port always emits regardless of colour; `<back:color>` needs the SAME `<filter><feFlood>` mechanism `beruje-75-jimu270`/`manube-50-xora983` also need. Root cause: `CommandCreoleStyle.ts`'s `EXTENDED_COLOR_ARM` regex group is non-capturing (colour is matched, never extracted) and `AddStyle.ts`'s own doc comment already flags `FontConfiguration` has no `extendedColor` field — an engine-wide widening (comparable to cdd-T19 row 65's M8a), not a single-task fix. Not fixed; recommend filing to `planning/next-missions.md`. | `.agent-notes/cdd-T26.md`; render-diff clean 5 structural / 0 numeric, all 5 attributable to this one mechanism |
| 94 | 2026-09-22 | T26 | `gekope-01-ricu859` instrumented, STOP (stop 1): the OpenIconic `<&key>` glyph itself is byte-identical to the jar (path-for-path match); the actual divergence is that `class-member-creole.ts#resolveOneAtom`'s `'text'` branch (line ~343, T24's write-set, same batch) never calls the already-ported tab-stop expansion (`core/klimt/creole/legacy/AtomText.ts#atomTextWidth`/`tokenizeOnTabs`/`advanceToTabStop`, `AtomText.java:239-256`) — it measures a tab-bearing run as one literal string (tabs contribute 0 width, same as space), where the jar tokenizes on `\t` and draws one `<text>` per token at the advanced x (`tabStop = fontSize*4 = 56px`, jar-verified exact: x=135 = 23 + 2×56). Not `size:`/icon interaction as the brief guessed. Full gates green on `cdd/t26`: `npm test` 2366/2366 (class subset; full suite run separately), `tsc --noEmit` clean, `npm run lint`/`npm run build` clean, catalog unchanged. | `.agent-notes/cdd-T26.md`; `measurements/out/gekope-01-ricu859.ours.svg` textLength=57.487 == measure("ID")+measure("Integer"), zero tab-stop advance |
| 110 | 2026-09-22 | batch-7 | Launched from the batch-6 close commit `455afc26`: five worktrees `cdd/t24`…`cdd/t28`, all parallel (write-sets disjoint per `batch-7/overview.md`; the shared `src/core/klimt/creole/legacy/` is read-only for every task). Journal row ranges reserved to keep the append-only table mergeable: T24 80–84, T25 85–89, T26 90–94, T27 95–99, T28 100–109, orchestrator 110+. T28 on opus (shared `core/annotations/` seam, D5 full-suite gate); T24–T27 on sonnet. Two corrections briefed in: (1) `.agent-notes/r2b-embedded-42x42.md`'s 42×42 fallback is stale for the current cache — moxobo's golden holds a real 43×54 nested `file f` render (decoded base64 payload is a real SVG), so T27's target is the jar's real nested geometry; (2) rusuzi's unparsed `<font size=18>` title (row 79) is chrome text, added to T28's instrumented set. | `git worktree list`; `test-results/dot-cache/class/moxobo-16-tipo829/in.svg` |
| 111 | 2026-09-22 | T27 | Stop 1 at `f350acec`: the brief's reach point (`MethodsOrFieldsArea.ts:134-140`) is dead for class bodies — verified: the class engine's own row loop is the fork, and the jar's mechanism is `MethodsOrFieldsArea.java:110-120` (members iterated, `EmbeddedDiagram.createAndSkip` at `:114-116` consuming the block's lines) drawn at `:145`, with `BodierLikeClassOrObject.java:96` keeping `{{…}}` lines raw. No other batch-7 task owns the fork's files, so the write-set is EXTENDED (flagged for review, as rows 18/29/45/52 were): `src/diagrams/class/parser.ts` (only where the Java's `addFieldOrMethod` path demands it), `class-member-rows.ts`, `class-body-enhanced-layout.ts`, `class-body-enhanced-geometry.ts`, `renderer-body-enhanced.ts`, `renderer-classifier-box.ts`, and an injection seam in `src/index.ts` so the class engine never imports the entry module. EXCLUDED because T24 is editing them in flight: `class-body-enhanced.ts`, `class-member-creole.ts` — needing either is a second stop 1. Resumed with context intact. | agent report; `grep -rn EmbeddedDiagram ~/git/plantuml/src/main/java/net/sourceforge/plantuml/cucadiagram/` |
| 112 | 2026-09-22 | T28 | Halted at `70a81ca2` (stop 1 ×2, stop 5 ×1; agent rows 100–109). Write-set EXTENDED (flagged for review): (a) `src/core/klimt/document-shell.ts#unwrapContentG` — an empty content `<g>` serialises self-closing and the unwrap throws (row 102, 14 → 1 newly-erroring fixtures when probed); (b) `src/index.ts` at the two `applyChrome` call sites only, plus `tests/oracle/svg-conformance/render-fixture-{class,state,sequence}.ts`, to thread the sprite/emoji/img resolver bundle into the chrome seam (row 103; 24 fixtures otherwise lose their literal markup) — T27 is concurrently editing `src/index.ts` for the nested-renderer registration, textual conflict accepted and resolved at merge; (c) `src/core/klimt/creole/legacy/` for `CreoleMode.FULL`'s `#`/`*` list patterns (`CreoleStripeSimpleParser.java:119-147`, never ported) and the klimt SVG text driver's `<a>` emission (row 104, letare) — T26 has merged (`b372ca3f`), so no task owns those files any more; (d) `src/core/preprocessor.ts:107` `RE_SKINPARAM_LINE` gains `/i` (row 107; `Pattern2.java:114` compiles every command regex CASE_INSENSITIVE). Stop 5 does NOT fire for letare: its rise is mechanised (row 104) and is to be FIXED, not pinned. Activity `style/text-baseline.json` rows (row 105) may be re-pinned only after an element-level compare shows each new output no further from the jar than before — a rise is `weightedScore`'s known anti-monotone artefact only if that check passes; otherwise it is a regression. `<back:color>` `feFlood` (`FontConfiguration.extendedColor`, shared by galili/manube/beruje/ziripa) and ropera's node `<style>` cascade stay named-open for the batch-7 residual round. | agent report; `.agent-notes/cdd-T28.md` |
| 118 | 2026-09-22 | merge | `cdd/t26` (`b372ca3f`) and `cdd/t25` (`6af1d289`) merged; jabama conformant on the merged tree; T25's fixtures re-measured there: diseka 2+7 (only the unregistered `<plain>` left), xamule 2+975 (GEO residual), lecelo 6+7 (Twemoji artwork — permanent, see below), gekope 12+65 (T24 now holds its tab-stop half). T25's addendum row renumbered 90 → 117 (collided with T26's 90). daxeno re-read on the merged tree is NOT a header-creole fixture: (1) `package … <<Database>>` selects the database USymbol (jar path `M6,75.5 C6,65.5 …` is a cylinder; ours a rect) with `skinparam database {…}`'s fill/border/font — a USymbol-for-package mechanism, batch-7 residual round; (2) the non-empty package's title still goes through `renderNamespaceRect` as one literal `<size:18>styled</size> should be styled` text — the second title path T26 reported leaving unfixed, T26 resumed for it; (3) the empty package keeps a literal `\n` in `text[2]`. Both agents resumed on their own worktrees, fast-forwarded to `6af1d289`: T25 takes `legacy/CommandCreoleBuilder.ts`'s `FontStyle.PLAIN` registration (`CommandCreoleBuilder.java:83-84`) — T26 is merged, T28 edits other `legacy/` files; T26 takes daxeno's title paths (2)+(3). | render-diff daxeno/diseka on `6af1d289` |
| 119 | 2026-09-22 | T25 | Stop-1 item resolved: registered `FontStyle.PLAIN` in `src/core/klimt/creole/legacy/CommandCreoleBuilder.ts` (now free of T26, per the orchestrator; T28 owns OTHER `legacy/` files — the stripe-line `#`/`*`-list parser and the SVG text driver's `<a>` emission, row 104 — not touched here). Ported every branch of `CommandCreoleBuilder.java:83-84` (`CommandCreoleStyle.createLegacy`/`createLegacyEol(FontStyle.PLAIN)`, no creole-pure form — `FontStyle#getUbrexCreoleSyntax` throws for PLAIN, `FontStyle.java` has no `case PLAIN` in that method): `CommandCreoleStyle.ts` gained `ACTIVATION_SOURCE`/`DEACTIVATION_SOURCE`/`LEGACY_STARTERS` entries for PLAIN (`FontStyle.java:89-90` regex form cross-checked against `:114-115`/`:142-143`'s ubrex form — both `<[pP][lL][aA][iI][nN]>`, no `canHaveExtendedColor` arm since PLAIN is absent from that method's list at `:191-205`; starters `<p`/`<P`, `FontStyle.java:47-48`); `CommandCreoleBuilder.ts` registers the pair via a new `registerPlain(map)` helper (extracted, not inlined, to keep `buildCommandMap`'s own CCN under the project's per-function cap — a plain function call adds no branch, unlike a new `for`/`if` in that already-CCN-heavy function) called in BOTH the `FULL`/`OTHER` maps, in upstream's exact ctor window (between ITALIC's triplet and the FULL-gated UNDERLINE creole-pure line — starters `<p`/`<P` collide with no other L1/L2 command, so the exact position has no `searchCommand` tie-break effect, ported anyway per this project's "preserve upstream order" discipline). PLAIN's "clear all styles first" application: `FontConfiguration.add(FontStyle)` (`FontConfiguration.java:301-309`: `final EnumSet<FontStyle> r = styles.clone(); if (style == FontStyle.PLAIN) r.clear(); r.add(style);`) ported into `AddStyle.ts#addFontStyle` — a `style === FontStyle.PLAIN` branch replaces the tracked `styles` set outright (`new Set([FontStyle.PLAIN])`) instead of unioning in. `FontStyle#mutateFont` (`FontStyle.java:75-77`, `font.withFontFace(UFontFace.normal())`) is a MEASUREMENT-time concern (`FontConfiguration#getFont()`) this port's flat `FontConfiguration` (`shape/UText.ts`) has no separate mutable "current font face" for — see row 120 for the one render-side consequence this leaves open. TDD: `tests/unit/core/klimt/creole/command/CommandCreoleStyle.test.ts` gained a `describe('cdd-T25 — <plain>...</plain>')` block (7 tests: legacy form, case-insensitivity, legacyEol/no-closing-tag, clearing one tracked style, clearing two at once, a sibling run outside the tag keeping its own style, starter-collision regression guard) plus a new integration file `tests/unit/class/class-creole-plain-header-t25.test.ts` (4 tests) exercising diseka's exact source end to end via `renderSync`. | `tests/unit/core/klimt/creole/command/CommandCreoleStyle.test.ts`, `tests/unit/class/class-creole-plain-header-t25.test.ts` — 28 + 4 tests, all green |
| 120 | 2026-09-22 | T25 | Diseka before → after: structural 2 → 1, numeric 7 → 0 (NOT 0 structural — see below). Verified byte-exact against the jar oracle: `x`, `y`, `font-size`, `fill="#888"`, `textLength="79.363"`, and the text content itself ("Enumeration", not the literal `<plain>...</plain>` tag) all now match `test-results/dot-cache/class/diseka-11-gozu390/in.svg` exactly (`plans/.../measurements/out/diseka-11-gozu390.ours.svg`). The ONE remaining structural diff is `text[1]/@font-weight`: jar emits `font-weight="700"`, this port emits none. Root cause traced, not guessed: `DriverTextSvg.java:93-103`'s render-time weight decision is a TWO-TIER fallback — `containsStyle(FontStyle.BOLD) ? (forced >=700) : (face.getCssWeight() != 400 ? that weight : none)` — where `face = fontConfiguration.getFontFace()` reads `currentFont`'s OWN face, a value `FontConfiguration.add()` NEVER touches (only `styles` is cloned/cleared; `currentFont` is passed through verbatim, java :301-309). Since diseka's header font is built bold from `skinparam classFontStyle bold` (`FontConfiguration.create(ISkinParam, Style, Colors)` → `create(UFont, ...)` → `getStyles(font)` bakes BOLD into the INITIAL `styles` AND `currentFont`'s face independently), `<plain>` clearing `styles` removes the FIRST branch's true value but the SECOND branch's `face.getCssWeight()` is STILL 700 (never cleared) — so jar keeps the header bold regardless of `<plain>`. This port's `FontConfiguration` (`shape/UText.ts:24-59`, that file's own doc comment: "DRIVER-side concerns... will be ported alongside `DriverTextSvg` itself") is FLAT — `styles` is the ONLY place weight/style info lives, with no separate immutable "base face" `add()` could spare — so clearing it via `<plain>` correctly (per THIS port's own architecture, not a bug) drops the seeded base bold along with it. Closing this gap needs a `FontConfiguration` model change (e.g. an un-clearable `baseStyles` field, mirroring `currentFont`) in `src/core/klimt/shape/UText.ts` plus its render-side consumer (`renderer-classifier-rows.ts#renderRowAtoms` and any sibling atom renderer) — that file is OUTSIDE the specifically-freed `src/core/klimt/creole/` directory, and the change's blast radius is every `FontConfiguration` consumer project-wide (class member rows, notes, descriptions), not just class headers. NOT attempted here: unauthorized scope beyond this task's unlock, and a diagnosis-mode "stop and report" is the correct move when a stated expectation ("0 structural") is disproved by a traced mechanism rather than guessed past. Gekope-01-ricu859 (bonus fixture, unaffected by this registration — no `<plain>` in its source) unchanged: 12/65 → 12/65. | `plans/.../measurements/out/diseka-11-gozu390.{jar,ours}.svg`; `FontConfiguration.java:98-114,301-309`; `DriverTextSvg.java:93-103` |
| 121 | 2026-09-22 | T25 | Engine-wide blast-radius check: grepped every cached fixture across ALL 27 diagram-type caches (`test-results/dot-cache/*/*/in.puml`, not just `class`) for a literal `<plain>` — exactly ONE hit in the entire corpus (`class/diseka-11-gozu390`), so no OTHER diagram type's pinned golden could move from this registration. Full `npm test`: `success: true`, `numFailedTests: 0`, `numTotalTests: 22053` (22050 passed, 2 pending — both pre-existing skips, unrelated to this change), JSON-reporter `testResults.length` = 781 = `find tests -name '*.test.ts' -not -path 'tests/e2e/*' \| wc -l` (781) — no collection gap. Zero non-class golden/ratchet test moved; no mover to journal beyond the diseka/gekope pair already covered in row 120. Gates: `npm run typecheck` (both tsconfigs) clean, `npm run lint` clean, `npm run build` clean, `docs/catalog.md` unaffected (no new exported symbol — `registerPlain` is module-private). `git -C /Users/scottseely/git/knowvah/plantuml-ts status --short` re-confirmed clean before commit (no Serena write-tool calls used this round, per the `.agent-notes/serena-write-tools-ignore-worktree-cwd.md` lesson — every edit went through `Edit` with an absolute worktree path). Commit `fix(cdd-T25): register the creole <plain> style command` on `cdd/t25`. | `find tests -name '*.test.ts' -not -path 'tests/e2e/*' \| wc -l` = 781; `python3 -c "import json; ..."` on the JSON reporter output; `git -C /Users/scottseely/git/knowvah/plantuml-ts status --short` |
| 122 | 2026-09-22 | T26 | Follow-on round: `daxeno-00-kasu166` exercises the literal-string gap the earlier T26 round left in `renderNamespaceRect`/`renderEmptyPackageIcon` (my own `.agent-notes/cdd-T26.md` note) AND a THIRD path I had not diagnosed: the non-empty `<<Database>>` cluster's title routes through `class-namespace-usymbol-shape.ts#buildDecoration`/`buildTextBlock`, not `renderNamespaceFolder` (verified by instrumentation, not assumption). `buildTextBlock`'s own multi-line split is a REAL newline-character split, not the `Display#getWithNewlines` `\n`-ESCAPE split a namespace label carries — converting the escape to a real newline before `buildTextBlock` (one line, `splitDisplayLines(geo.label).lines.join('\n')`) reuses its already-verified multi-font-size stacking unchanged. `renderNamespaceRect`/`renderEmptyPackageIcon` now route through the SAME shared `renderNamespaceTitleAuto` helper `renderNamespaceFolder` already used, each keeping its own pre-existing horizontal placement rule (fixed left margin vs per-line centring). `core/cluster-title-table.ts#computeTitleTableHeight`'s `titleLines` param widened to accept `readonly number[]` (per-line font sizes) alongside the pre-existing plain `number` (line count) — a backward-compatible addition shared with state diagrams' identical call site, unaffected. | `.agent-notes/cdd-T26.md`; `daxeno-00-kasu166` render-diff structural=12/numeric=135 -> structural=10/numeric=77 (residual is row 118's `<<Database>>` colour/shape dispatch, correctly untouched); jabama-09-kago823 unaffected (structural=0/numeric=0) |
| 123 | 2026-09-22 | T26 | Root cause found and fixed for a REAL regression caught by `class-empty-package-no-cluster-box.test.ts`'s byte-level render proof (a plain, markup-free "Empty" label, no daxeno involvement): `NamespaceTitleLine`'s per-line height must NOT read `run.font.size` (the DECLARED size) for `getHTitle`/`namespaceTitleHeight` — it must read `measurer.measure(...).height` (the MEASURED size), matching the pre-existing single-line `dim.height` convention exactly. The two coincide under `WidthTableMeasurer`/`FormulaMeasurer` (both return `font.size` as height) but diverge under `tests/helpers/render.ts`'s test-only `FixedMeasurer(8, 16)` (a constant height independent of font size) -- caught the divergence, not guessed past it. Conversely `namespaceTitleTableDims`'s DOT-graph height (`ClusterHeader.java:78`'s `fontSize`-based formula) needs the DECLARED size, never a measured one -- `NamespaceTitleLine` now carries BOTH (`fontSize` measured, `nominalFontSize` declared), each consumer reading the one it always read before. | full `npm run svg:survey -- class` + `pin-diff.mts`: 2 transitions (jabama, curupe-50-kibu120), 0 regressions, matching the pre-regression baseline; `npm test` full suite green (96.47%/91.94%/97.52%/97.47% stmt/branch/fn/line coverage), `tsc`/`lint` clean |
| 124 | 2026-09-22 | T26 | Extra (per the mission brief's own "author a fixture rather than leave it unhandled" instruction): `namespaceTitleRuns`/`namespaceTitleLines` now resolve a `<$sprite>`/successfully-decoded `<img:data:...>` atom in a namespace title to a real `'image'` run via the SAME `resolveInlineAtom` member rows already use (`class-member-atom-resolve.ts`), instead of silently skipping it. New optional `sprites`/`spriteDims` params default `undefined` everywhere (zero behavior change for every existing call site, none of which threads a registry today -- no corpus fixture needs it). `renderNamespaceTitleRuns` draws an `'image'` run's `<image>` bottom-aligned to the text baseline (`note-layout-measure-rows.ts`'s identical altitude-0 convention for a member-row image atom) -- untested against a jar fixture (none exists), the honest generalization from the one other place this port aligns an image atom this way. Two `.puml` fixtures authored (`tests/fixtures/class/namespace-title-sprite.puml`, `namespace-title-img-data.puml`) documenting the shape; both exercised at the ATOM level only (`namespaceTitleRuns`' own returned data), never a full-SVG comparison, since no oracle exists for either shape. | `tests/unit/class/class-namespace-title-runs.test.ts`'s "sprite/img-data in a namespace title (no oracle -- atom-level only)" suite, 6 new tests, all green |
| 126 | 2026-09-22 | T26 | Fast-forwarded `cdd/t26` to `a5441e94` (merge commit `f9a24d69`, T24/T25/T28's chrome/creole/tree work) before the malara round: with T28's chrome seam live, `<size:18>`/newline-split fixes from rows 122-124 needed no rework (clean typecheck immediately after merge), confirming `class-namespace-usymbol-shape.ts`/`class-namespace-title-runs.ts` had no hidden coupling to the pre-merge chrome path. `malara-55-moce209` (`sprite $demo [13x26/color] {...}`, a 4096-colour sprite) ported `net/sourceforge/plantuml/klimt/sprite/ColorPalette4096.java:46-113` (decode only — `getColorFor(String)`'s 64-char alphabet + 12-bit-packed-RGB unpack + 4-bit-to-8-bit `dup()`), `SpriteColor.java` (as `SpriteColor4096.ts` — the data class only, `toUImage`'s klimt drawing not ported, matching `SpriteMonochrome.ts`'s own precedent), and `SpriteColorBuilder4096.java:50-65`'s `buildSprite` (the declared `[WxH/color]` header dims are IGNORED, exactly as upstream ignores them — width/height come from the BODY: jar-verified, `13x26` vs the real 26x24 body shape). `sprite-commands.ts#buildAndRegister`'s `/color` branch now calls `addSprite` instead of only journaling to `skippedColorSprites`. TWO separate atom resolvers needed the SAME `getSpriteMonochrome`-then-fallback wiring: `class-member-atom-resolve.ts#resolveInlineAtom` (member rows, T26's own write-set) and `core/creole-atoms-image-resolver.ts#resolveSpriteAtom` (chrome/description/title, T28's shared seam, not owned by any live batch-7 task once T28 merged) — malara's TITLE sprite only resolved once BOTH were wired (verified independently: fixing only the member-row resolver left the title's `<$demo>` unresolved). A `/color` sprite has NO gray-level tint (`SpriteColor4096`'s every cell is a real decoded RGB, never the `-1` gray-fallback sentinel `buildSpriteColor4096` never produces), so `font.color`/`atom.forcedColor` are not threaded to the new `spriteColor4096ToPngDataUri` raster path — there is nothing for them to tint. | `.agent-notes/cdd-T26.md`; `tests/unit/core/klimt/sprite/ColorPalette4096.test.ts` (6 tests, hand-traced against the ported alphabet, incl. malara's own "zw" background code -> RGB(255,255,204)), `SpriteColor4096.test.ts` (11 tests) |
| 127 | 2026-09-22 | T26 | malara-55-moce209 before -> after: structural=2/numeric=~96 (pre-merge baseline; coordinator reported 2+96 on `a5441e94`) -> structural=0/numeric=2. Both title (`<$bug16>`+`<$demo>`) and body (`Test <$bug16>`/`Test <$demo>`) now byte-match structurally; `<$bug16>` itself resolves to NOTHING in BOTH the jar and this port (verified against the cached oracle SVG directly: jar's own body row for `<$bug16>` is bare "Test" text, no icon — `$bug16` is undefined in this diagram's own source and not in the internal store either, a shared "unknown sprite" case pre-dating this task, not a residual). The 2 remaining numeric diffs (`text[3]/@y`, `image[1]/@y`, both Δ11.846) are a pre-existing, unrelated row-height cascade already visible throughout the ORIGINAL diff (dozens of Δ11.846 lines) before any sprite work — named, not chased. Full corpus re-survey (post-merge, all of T24/T25/T28 + this round's work): 10 transitions, ALL diverged -> conformant/structural-match, ZERO regressions (`curupe-50-kibu120`, `foxiki-17-kosa114`, `gekope-01-ricu859`, `jabama-09-kago823`, `juxora-90-fisu720`, `kacico-91-bati232`, `malara-55-moce209`, `padapo-73-beke177`, `repuga-78-xora226`, `rusuzi-21-kile910`). Full `npm test`/`tsc`/`lint` green; new unit-test files close the TDD gap this round's exploratory instrumentation opened ahead of the code. | `npx jiti tools/render-diff.mts malara-55-moce209`; `npm run svg:survey -- class` + `pin-diff.mts` against committed `parity-class.json` |
| 131 | 2026-09-23 | B7FU | All five batch-7 branches merged (`7c5b695b`; T27's rows 112–114 renumbered 128–130, T24's 85 → 125, T25's 90 → 117 — five agents appending to one table collide however the ranges are reserved). Merged-tree readings: sejuzo/kacico/rusuzi/repuga/foxiki/padapo/jabama/curupe conformant; malara 0+2, gekope 0+2, juxora 0+16, gadufu 0+5; diseka 1+0; bixogo/roxosu 1+4 (the chrome seam now builds the legend's `{{salt}}` `EmbeddedDiagram` but `blocks-creole.ts:399`'s nested renderer is a throwing stub, so `calculateDimensionSlow` falls back); moxobo/zikabo 1+43 (`isEnhancedBody` lacks `BodierLikeClassOrObject.java:96`'s `getEmbeddedType` disjunct — T27 row 129); daxeno 10+77 (package `<<Database>>` USymbol); rotisi 2+9; galili 3, manube 4, beruje 2, ziripa 5 (`<back:>`/`<u:>`/`<s:>` extended colour). Residual round launched on three worktrees from `7c5b695b`, disjoint write-sets: R1 (opus) core klimt font model — `FontConfiguration.extendedColor` + `feFlood` filter + coloured underline/strike lines, base-face weight vs `styles` (`DriverTextSvg.java:93-103`), DIVERGENCES emoji entry; R2 (sonnet) class body/nested — `isEnhancedBody` disjunct, note-region and chrome nested-renderer wiring, rotisi's sprite-row 14/13 inflation and title sprite, nucite/nufini `MaximumWidth`; R3 (sonnet) class node style/edges — daxeno's USymbol-for-package, ropera's `<style> class` cascade, juxora's member-port resolution, focaci's DOT coordinate gap. Rows reserved: R1 132–137, R2 138–144, R3 145–151, orchestrator 152+. | render-diff on `7c5b695b`; `git worktree list` |
