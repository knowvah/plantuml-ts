# Decision journal — note-leaf-model

Appended during execution. Every non-trivial judgement call gets a row.

Also record here, because later batches are measured against them:

- **T1's baseline** — the note-order/uid report, and the standing numbers
  (776 / 25695, DOT 100% EQUAL, pin counts).
- **Batch 2's verdict** — whether draw-time Opale resolution is
  byte-identical, and if NOT, which input the draw stage lacks. That answer
  is the mission's pivot: it either unblocks Batch 3 or ends the mission
  with a real finding.
- **Every fixture whose document order, uid or ink changed** — expected to
  be none; any entry here is a stop, not a note.
- **The member-tip (`::member`) fixtures checked by name** in Batch 2. A
  green suite is not evidence for that family.

| Date | Task | Decision | Why | Alternative rejected |
|---|---|---|---|---|
| 2026-08-15 | B1 | Batch 1 executed directly by the orchestrator, not via `typescript-pro` agents. | Both tasks were small (one script; one field + 4 producers + 10 test literals), well under the ~30-min delegation threshold, and T2's stamp rule needed a corpus probe first. Logged per the autonomous rules. | Two parallel agents — more tokens for two files' worth of work. |
| 2026-08-15 | T1 | **Baseline re-pinned.** The brief's standing numbers (776/25695, class DOT 100% EQUAL of 712, 14291 tests) predate `feat/edge-label-box-closeout` (merged `0cc54633`, AFTER the brief was drafted `b535c1d2`), which deliberately re-baselined class DOT to 680/711 EQUAL via the new `labelSizeOk` check (its journal, 2026-08-15) and moved shape-match. Measured on this mission's starting main: shape-match **779 / 25975**; class DOT **680/711 EQUAL** (7 oracle-blind, 1 `directionOk` fail besepi-37, 30 `labelSizeOk` fails — the named backlog); `npm test` **14307**. Saved verbatim under `plans/note-leaf-model/baseline/` (`shape-match.txt`, `dot-sync-class.txt`, `note-order.txt`). The bar is unchanged in kind — "exactly unchanged" — against THESE files. | A number that has drifted is worse than no number; the gate is a diff against the saved report, not a memorised figure. | Editing the brief's numbers in place — loses the provenance of why they moved. |
| 2026-08-15 | T1 | Report reads ORDER and UIDs back from the rendered SVG and note identity from the PARSE output (`ast.notes[].id`), never from `geo.notes`; adds a whole-SVG sha per fixture and a `--check` mode. | The report must survive Batch 3, which deletes the very array a geometry-side report would read; the sha makes "byte-identical" directly checkable on all 97 note fixtures, not inferred from shape-match's rigid-alignment score. | Reading `geo.notes[].tip/opale/dropped` — would need rewriting mid-mission. |
| 2026-08-15 | T1 | Baseline: 97/802 class-engine fixtures carry notes; 28 carry TIPS; 144 wrapped `note:` entries. Two runs byte-identical (`cmp`), `--check` exit 0. TIPS leaves appear only via the sha and `tips=` count (they draw unwrapped, no uid — `EntityImageTips#drawU`). | — | — |
| 2026-08-15 | T2 | `NoteGeo.leafType` is REQUIRED and stamped by the shape DRAWN: `buildTipNoteGeo`/`droppedNoteGeo` -> `'TIPS'`, `plainNoteGeo`/`buildOpaleNoteGeo` -> `'NOTE'`. Corpus probe (scratch script over all 802): 33 TIPS = 31 resolved + 2 dropped, 144 NOTE = 105 opale + 39 plain, **0** `::member` notes falling through to the plain path — so draw-side and parse-side (`targetPort`) stamps agree everywhere the corpus reaches. | The value is what `GeneralImageBuilder` dispatches on; Batch 3's dispatch-by-leaf-type can only be byte-identical if the stamp matches the drawn shape. The one divergent case (a `::member` note whose host never resolves) is a note upstream never creates (`CommandFactoryTipOnEntity:208-209`); documented on the field. | Stamping from `ClassNote.targetPort` regardless of draw path — identical today, but would make Batch 3's dispatch move output if that case ever appears. Optional field — hides which producer forgot it. |
| 2026-08-15 | B1 | Gates: typecheck/lint/build/test all exit 0 (14307 passed, 1 todo, no expectation moved); shape-match `diff` empty vs baseline; class DOT-sync `diff` empty; note-order `--check` identical. Batch 1 exit bar met on all three clauses. | — | — |
| 2026-08-15 | T3 | **Batch 2 verdict: draw-time resolution IS byte-identical — Batch 3 is reachable.** Scoping first: the NOTE half's opale (`buildOpaleNoteGeo`) already consumed only the routed edge spline, never a classifier; the layout-time classifier dependency was entirely the TIPS half (`resolveGroupTipContext`: host position, rows text/y/width/indent, `enhancedBody`; plus the `hostId` existence stamp). Java read: `EntityImageTips#drawU` gets these from `bibliotekon` at draw time; `EntityImageNote#drawU`'s `opaleLine` branch likewise. | The draw stage here has the same inputs at the same fidelity: `ClassGeometry.classifiers` carries `x/y/rows/enhancedBody`, and both draw passes already receive it. No re-plumbing was needed. | — |
| 2026-08-15 | T3 | Shape: `NoteGeo.tipRequest` (member, side, `baselineOffset`, `rowHeight`) + stacked `y` are baked at layout; new `note-tips-resolve.ts#resolveTips(notes, classifiers)` (pure) is called from BOTH draw passes — `class-ink-box.ts#buildInkBox` and `renderer.ts`. `NoteGeo.tip`/`dropped`/`hostId` removed; `target` (raw parse-side host id) added; renderer computes hosted-ness from `geo.classifiers`. | `buildInkBox` IS this port's `LimitFinder` — upstream also learns dropped-ness by running `drawU` into the limit finder; the ink walker already takes `classifiers`. The two font metrics are baked exactly like `lineHeights` so both passes stay measurer-free (`ClassGeometry.measurer` is optional). The `height` running term of `drawU` equals `note.y - group.y` because the geo build already advances by `height + ySpacing` per tip (proved on the resolved prefix; after a drop nothing later is drawn or inked, so later `y`s are unobservable). | Storing the resolution on the geo — that IS the phase dependency. Computing dims in the renderer instead — moves a public `ClassGeometry.width/height` contract for no fidelity gain. |
| 2026-08-15 | T3 | `leafType` is now stamped from the COMMAND (a `::member` note in a member-tip group → `'TIPS'`), and a `'TIPS'` whose host is not a drawn classifier draws nothing + no ink (`drawU`'s "Error1"/"Error2" `return`) — the pre-D3 fallthrough (`tipCtx === undefined` → opalised plain box via the invisible edge's spline) is retired. **Corpus impact: none** (0 fixtures reach it, T2 probe). Non-corpus corners, jar-verified live with `~/git/plantuml/build/libs/plantuml-1.2026.7beta11.jar -DPLANTUML_DETERMINISTIC_TEXT=true` (the `oracle/dist` jar is absent in this checkout): tip on a **package** (`note left of P::m`) — jar `Error2`, no tip, 130x148; port before 202x148 with a spurious note, after **130x148, matches**. Tip on a **`remove`d host** — jar 64x70, no tip; port before 146x70, after **64x70, matches**. **Undeclared** host — jar error diagram ("Nothing to note to"); port draws A alone (63x84). **`hide`n host** — jar draws the tip (node still in bibliotekon); port draws none, UNCHANGED before/after: the renderer's classifier loop `continue`s on a hidden host before `renderHostedNotes`, a pre-existing bug, noted in `.agent-notes/note-leaf-model-b1.md`, not touched (byte-identity). | Preserving the fallthrough would require the geo build to know host existence — the dependency being removed — and would keep a "TIPS drawn as an opale NOTE" special case D2 exists to prevent. Two corners moved TO jar as a by-product; none away. | Keep the fallthrough behind a layout-time existence check. |
| 2026-08-15 | T3 | Exit bar 3 — member-tip fixtures re-verified BY NAME via the note-order report's whole-SVG sha (all identical): cajicu-52-cego765, cejili-77-gepe377, dozugo-00-jado141, fabuje-68-gona310, fomofi-36-lova857, fopose-13-kase592 (enhanced-body host), fupope-12-zoku847 (dropped tip), gerima-02-fade831 (single-tip ySpacing), janeba-15-duja043, jiceke-84-xoze695, kixiso-09-lezo371, kogifo-65-xotu512, kugasi-68-josu446, labele-71-gudo044, maziju-71-cava125, mopesi-01-gapo101, nacuzu-17-bepi660, refeku-65-gapu585, rubuxe-58-peba652 (`+attribute` indent anchor), sanusa-54-keda128 (icon rows), taxemo-34-buro609, tenobo-24-liga464 (two stacked tips, different widths), tobigu-87-raci272, vipejo-56-nubi928, xumeli-52-keso732, zepeki-75-pifo352, zuduxu-90-kosi876, object/zagodo-28-ranu153. | These are the row-text-dependent cases the brief said a green suite alone cannot vouch for. | — |
| 2026-08-15 | B2 | Gates: typecheck/lint/build/test exit 0 (14313 passed, +1 test, no expectation moved); shape-match `diff` empty; class DOT-sync `diff` empty; note-order `--check` identical. Batch 2 exit bar met on all three clauses; `mapNoteGeos` reads no classifier (its `classifiers` parameter is gone — the type system is the proof). | — | — |
