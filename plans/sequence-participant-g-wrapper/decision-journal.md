# Decision journal — sequence-participant-g-wrapper

Appended during execution. Every non-trivial judgment call gets a row:
"non-trivial" means a reasonable developer might have chosen differently.

| Date | Task | Decision / finding | Consequence | Flag for review |
|---|---|---|---|---|
| 2026-08-27 | T1 | **Stop condition 1 as I wrote it is too blunt, and the repo already knew.** T1 raised `weightedScore` on 563 fixtures. Mechanism: `compareNodes` charges a child-count mismatch `sumUnits(actualChildren) + sumUnits(expectedChildren)` and stops (`compare.ts:396-406`); `units` counts one per node plus one per attribute (`:167-172`). T1 adds 3 nodes + 6 attributes per participant to OUR side, so the charge grows wherever the root `<g>` child count still mismatches — **before any of T1's work is examined**. Census over the committed corpus: **557 fixtures short-circuit at `svg/g[1][childCount]`, 567 descend.** This is NOT a new finding: `scripts/sequence-ratchet-adjudicate.ts:7-20` states it exactly, with a worked example (`bexoce-95-vibe195`, baseline 622 → live 950, entire delta one childCount record, distance 45 → 1). | Stop condition 1 is **amended**: a rise halts the mission only when the adjudicator classifies it `regression` (top-level child-count distance rose or held) or `inconclusive`. An `artefact` rise — distance fell — is expected and is re-pinned at batch close, per D5 of `plans/sequence-command-coverage/decisions.md`. Per-task ratchet runs are therefore advisory in this batch; the adjudicator at batch close is the gate. | **yes** — I wrote a stop condition that would have halted this mission on its own intended effect. The brief was drafted before the adjudicator was read. |
| 2026-08-27 | T1 | Σ weightedScore over the 1124 numeric fixtures **fell** 1 291 577 → 1 284 742 (−6 835) on T1 alone, i.e. the 567 descending fixtures improved by more than the 557 short-circuiting ones were over-charged. | Confirms the direction is right before T2/T3 land, and gives the batch a mid-point number to attribute against. | no |
| 2026-08-27 | T1 | `BoxStyle` had no `fill-opacity`; `LineStyle` did. Added it to `BoxStyle` and emitted it directly after `fill` in `svg-shapes.ts#rect`, matching the jar's order (`<rect ... fill="#000" fill-opacity="0"/>`). `formatOpacity(0)` already returns `"0"`, not `"0.00"` (`svg-format.ts:148`), so no new formatting rule was needed. | Touches a shared core primitive rather than the sequence engine, so it is called out here: the new attribute is opt-in and every existing caller omits it, leaving their bytes unchanged. | no |
| 2026-08-27 | T2 | **The faithful zero-height guard unmasks a pre-existing LAYOUT defect.** 3 fixtures moved from "child counts match" to "child counts mismatch" (`donuli-07-peje262`, `rugeco-70-muro754`, `rugoro-74-mepi153`; `jasixo-44-jide821` moved the other way). Mechanism: `ComponentRoseActiveLine.java:76-77` returns BEFORE `startGroup` at zero height, so mirroring it removes the box entirely — but this port's layout gives some activations `height === 0` where the jar's does not. `donuli` (`autoactivate on` + bare `deactivate`) lays out 2 activations, both `h:0`, while the jar emits both groups: our root child count went 18→16 against the jar's 18. Census over the corpus: **32 of the 121 activation-bearing fixtures carry at least one zero-height activation; 12 have ALL of theirs zero.** | **Guard kept.** It is quoted from upstream and the defect is upstream of it, in layout. Before the guard we emitted a zero-height `<rect>` — invisible ink that kept the child count aligned *by accident*. Trading a faithful guard for an accidental alignment is fitting, which D6 forbids. The layout defect is filed as a follow-on (`sequence-zero-height-activation`), sized at 32 fixtures. Net Σ still fell (1 284 742 → 1 282 031). | **yes** — 32 fixtures is a real population and the follow-on should be sequenced before any arrow-fidelity mission that expects activation alignment. |
| 2026-08-27 | T2 | **Two of my own instrument checks were vacuous and I nearly acted on them.** A zero-height census and an orphan-activation census both reported clean (`[]`, `0`). Both called `layoutSequence(ast, measurer)` — the real signature is `(ast, theme, measurer)` (`layout.ts:42-46`) — so every fixture threw into a bare `catch {}` and was silently skipped. A third check used `parseSequence` on the raw file rather than through `buildBlockUmls`, which refuses this exact population. Re-run correctly, the zero-height census returns **32 fixtures**, not zero. | Recorded because the failure mode is the one prior-observation 4 warns about, arriving from my own tooling rather than a subagent's: a clean result from a silently-skipping loop looks identical to a clean result from a real one. Any throwaway census in this repo must report its own skip count — the corrected script prints `skipped=19` for exactly that reason. | **yes** |
| 2026-08-27 | T3 | **A FOURTH pass divergence, unscoped: grouping tiles emit in the background pass.** `PlayingSpace#drawBackground` (`:109-117`) walks the whole tile tree, so each group emits a Blotter colour band AND its outline rect before the lifelines — and the outline again in the foreground, because `GroupingTile#drawU:267` calls `comp.drawU` OUTSIDE the `isBackground()` guard at `:262`. Verified byte-for-byte, not inferred: `pixopo-04-zitu732`'s golden carries the identical `<rect ... fill="none" stroke:#000;1.5/>` at top-level index 0 and index 15; `kejoke-76-curu931` shows band+outline pairs for all twelve groups. | 10 fixtures rise because of it. Attempted inside this mission and **reverted**: emitting the outline in the background pass aligned `pixopo` through child 13 and raised `improved` 557→600, but traded 0 regressions for 5, and further layers showed behind it (`luzapi-49-rati107` emits a bare `stroke-dasharray:1,4` delay line BETWEEN lifeline groups — `Rose.java:210`'s PARTICIPANT_LINE-only branch). `FrameGeo` carries no background colour, so the Blotter needs parser→AST→layout work. Filed as `sequence-frame-background-pass`. | **yes** |
| 2026-08-27 | T3 | **The 10 remaining rises are NOT benign, measured directly.** The charge-based `substructure` equality fails for all 10. An independent instrument — index-wise tag alignment of the root group's children against the golden — says **5 got WORSE, 5 held, none improved**. Mechanism: T3's footbox move shifts indices, and on a fixture whose frame is already in the wrong pass that breaks a coincidental alignment. | **The re-pin was not run.** `repin-sequence-baselines.ts` requires zero regressions AND zero unadjudicated rises; the second half fails. Re-pinning the 557 benign rises while these 10 stand would bake 5 measured regressions into the baseline, which is exactly what D5 exists to prevent. Mission HALTS before merge. | **yes** |
| 2026-08-27 | T3 | Checked whether a smaller landing exists by reverting T3 alone: arrow-point records fall to **45 fixtures / 857 records**, against 487 / 7 922 with it and 42 / 721 on main. | **T3 carries essentially all the mission's value**, so splitting the batch to land T1+T2 now is not viable. The mission lands whole, after `sequence-frame-background-pass`, or not at all. | no |
