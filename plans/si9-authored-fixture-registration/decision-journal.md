# Decision Journal — si9-authored-fixture-registration

Append one row per non-trivial judgment call. "Non-trivial" means: a
reasonable developer might have chosen differently.

Also log here: quality-gate results per batch, any brief line-number
correction, every measurement the brief asks you to record, and every STOP
with its full output.

| Date | Task | Decision | Rationale |
|---|---|---|---|
| 2026-07-31 | planning | Mission brief created | Gap found while closing `svg-sprite-nanoparser`: three fixtures matching the jar with zero diffs could not be ratcheted. Scope was investigated before planning, so the brief starts near Phase 2 |
| 2026-07-31 | planning | **ADR-2 exists because an assumption was checked and found FALSE** | "Canonicals will be generated automatically for new fixtures" was plausible and wrong: `ensureCanonical` early-returns when the directory merely has any `.svg`, so authored fixtures would get no canonical, no tag, and be silently dropped by `buildAgg`. Without ADR-2 this mission would have appeared to work while changing nothing |
| 2026-07-31 | planning | ADR-1's containment argument came from a level-two trace | `tests/visual/data/*.json` has **six** consumers. The rejected manifest-paste option would have touched all of them; the chosen reader-side change touches none. This was not part of the original rationale and is now the strongest one |
| 2026-07-31 | T1 | Split `scripts/dot-sync-fixtures.ts` out of `dot-sync-report.ts` | The complexity hook blocks any edit that leaves a file >500 lines, and `dot-sync-report.ts` was already at 526. This is the brief's named push-forward condition ("a ~500-line split"). Split boundary: *which fixtures exist, and is the cached classification of them current* — enumeration, `findFixture`, `taggedSlugs`, canonical generation/freshness, skip reporting. Report keeps DOT diffing, aggregation and the CLI. The brief already named the test file `dot-sync-fixtures.test.ts`, so the module name follows it |
| 2026-07-31 | T1 | `loadFixtures` renamed `enumerateFixtures`, with explicit `dataDir`/`goldenDir` params | The old name described the old behaviour (read one manifest file). It now merges two sources, and the parameters are what let the enumeration be tested against real temp directories instead of a mocked `fs`. All four call sites are in-file/in-module — see the trace row below. CLAUDE.md's preserve-upstream-names rule does not apply: this is our own harness, with no Java counterpart |
| 2026-07-31 | T1 | `undefined` return kept, but only when the type has **neither** source | The task asked which behaviour was chosen. `enumerateFixtures` returns `undefined` only if there is no manifest *and* no authored fixture; a type with authored fixtures but no manifest now succeeds. That is the doctrine case the mission exists for — a fixture authored for a type the classifier has never populated must still be measurable. `runType`'s "run npm run visual:classify first" message is therefore still correct for the only case that still returns `undefined` |
| 2026-07-31 | T1 | Level-two trace of the changed signature | Level 1 — `loadFixtures` had four callers, all inside `dot-sync-report.ts`: `findFixture`, `runType`, `markdownRowForType`, `probeType`. Level 2 — their callers are `drillDownSlug`/`runMarkdown`/`runProbeJsonDot`, all reached only from `main()`. Nothing outside the script imported it (grep over the repo: `dot-sync-report` appears only in `plans/**`, `CHANGELOG.md` and npm scripts). Signature change is contained |
| 2026-07-31 | T1 | Guarded `main()` behind `import.meta.url === pathToFileURL(process.argv[1])` | Required for the unit test to import the module at all; without it, importing runs `resolveJar()` and a full jar corpus pass. Copied verbatim from the convention already used by `svg-parity-survey.ts`, `svg-parity-dashboard.ts` and `measure-description-size-deltas.ts` |
| 2026-07-31 | T1 | **ADR-2 confirmed live, not just in theory** | First run after the change printed `canonical SVG cache for "usecase" is missing 3 of 354 fixture(s) (e.g. sprite-svg-archimate-0, …) — regenerating via oracle jar…`. The pre-existing check would have early-returned: the directory held 359 `.svg` files. Without ADR-2 the three fixtures would have been enumerated and then silently dropped by `buildAgg`, exactly as predicted |
| 2026-07-31 | T1 | Measurement — usecase corpus 90 → 93, still 100% EQUAL | `npx tsx scripts/dot-sync-report.ts usecase`: `93 DESCRIPTION fixtures`, `structurally EQUAL 93 (100%)`, no-candidate 0, oracle-blind 0, count-mismatch 0. Skip report: `enumerated 354, analysed 93, skipped 261 (0 with no canonical SVG, 261 canonical but not tagged DESCRIPTION)` — the actionable bucket is empty, which is the point of splitting it out. Collision report: 16 authored `usecase` goldens already in the manifest, manifest markup wins (all 16 are dot-cache-sourced goldens, so this is expected, not drift) |
| 2026-07-31 | T1 | Cache contract for T2 satisfied | `test-results/dot-cache/usecase/` now holds 93 directories; each of `sprite-svg-{bootstrap,archimate,multiline}-0` has `.done`, `in.puml`, `in.svg`, `svek-1.dot` |
| 2026-07-31 | T1 | Quality gates — all green | `npm test` exit 0 (457 files, 11173 tests); `npm run typecheck` exit 0; `npm run lint` exit 0; `npm run build` exit 0; `measure-description-size-deltas.ts` → `total 351, conformant 320, widened 0`; `git status` shows only the two new files and `dot-sync-report.ts` — no golden touched, so the 389 svg-class/object/state goldens are byte-identical |
