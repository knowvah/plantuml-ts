# Decision journal — `activity-loop-lane-translate`

| Date | Task | Decision | Why | Evidence |
|---|---|---|---|---|
| 2026-09-19 | B0 | Execution plan: branch `feat/activity-loop-lane-translate` cut from `9a47e8fd` (brief commit on main). T0 dispatched to `debugger` on the main tree (read-only on `src/`, writes `plans/`, `.agent-notes/`, `planning/`); T1 dispatched to `typescript-pro` concurrently in a prepared worktree `.claude/worktrees/allt-t1` (branch `allt-t1`, `node_modules` + `assets/stdlib` symlinked, `test-results/dot-cache` already tracked). T1's commit is cherry-picked onto the mission branch after T0 lands and re-verified with render-all on the merged tree. Orchestrator commits nothing while either agent is live | Disjoint write-sets; memory `batch-parallelism-needs-worktrees` (T0's render-all must read a tree nobody edits); prior mission's B0 row used the same split | `git worktree list` |
| 2026-09-19 | B0 | `tools/*.mts` hardcode the main-tree absolute path; a copy with the path rewritten to the worktree lives at `<wt>/.allt-tools/` so T1's render-all measures the WORKTREE's code, not main's. `.claude/worktrees/` added to `.git/info/exclude` (local only) so it never appears untracked in the main tree | Running the committed tool from the worktree would silently render main and report zero movers regardless of T1's edits | `render-all.mts:3-6` |
| 2026-09-19 | B0 | The brief's `measurements/base.json` is the probe's `--json` output (aggregate + per-slug scores + families), as the prior mission's T0 used it; render-all writes an SVG directory (`measurements/base-svg/`, gitignored-size permitting or scratchpad) for the byte-level `cmp`. Both are recorded; the JSON is what is committed | `render-all.mts` takes an out DIRECTORY and writes one SVG per slug; it has no JSON mode | `tools/render-all.mts:9-17` |
| 2026-09-19 | T0 | Baseline: `render-all` 268/268, diagonal scan 0/0, aggregate `weightedScore` 51371 (sum of `weightedScore` over the 268 `status:"baseline"` rows in `oracle/goldens/svg-activity/diff-baseline.json`, cross-checked against `scripts/activity-probe.ts --json`'s own `aggregate` field — identical) | README's exit bar requires the aggregate recorded at every batch close | `plans/activity-loop-lane-translate/measurements/base.json` |
| 2026-09-19 | T0 | Classified by INSTRUMENTING the actual lane variables (`ALLT_TRACE` env-gated `console.error` at `walk-while-branch.ts:185`, `walk-repeat.ts:240,345`; reverted before commit, `git diff` on `src/` empty) rather than reading `.puml` text, after the `.puml`-text reading gave the wrong answer for 4 of the 22 rows on a first pass | `.agent-notes/aitp-T7.md`: "measure before stating a mechanism"; `.puml` lane-tag position does not equal the walker's resolved `lane1`/`lane2` (inheritance via `laneAt`/`laneIn`/`laneOut`) | `.agent-notes/allt-T0.md` |
| 2026-09-19 | T0 | Corrected 4 of the filing's own row classifications: `judatu-15-xize591`, `gesogi-81-xoma900`, `bulasi-17-vafa634` ("both builders") and `tobajo-64-mipi810` ("repeat, cross-lane") reach NEITHER `while-back` NOR a cross-lane repeat shape; re-filed as a separate residual, not this mission's mechanism | Direct instrumentation showed `cross=false` on every while-back/repeat-back/repeat-out edge in these four rows | `fixtures.md` rows; `next-missions.md` re-filing |
| 2026-09-19 | T0 | D5 resolved: the jar DROPS a non-translatable cross-lane connection entirely (`UGraphicInterceptorOneSwimlane.java:92-104`'s `contained1 && contained2` gate never passes for a true cross-lane pair in ANY single lane's pass; `ConnectionCross.drawU`, `ftile/ConnectionCross.java:49-65`, draws nothing when `!(connection instanceof ConnectionTranslatable)`). No baseline row exercises it for `while` — `pushWhileBack`'s empty-body branch and `pushWhileOut` both pass the SAME lane variable as both endpoints, so our walker cannot construct a cross-lane instance of these two classes; confirmed no baseline `.puml` has an empty-body laned `while` | Task step 4, stop 9 check | `.agent-notes/allt-T0.md`; `Swimlanes.java:318-356`, `:178-215` |
| 2026-09-19 | T0 | D9: T5 struck. Grepped all 268 baseline `.puml` fixtures (not only the 14 named break rows) for `break` co-occurring with a `\|lane\|` line: zero matches. No break row shares a slug or connector class with any of the 22 lane-translate rows | Task step 5 acceptance criterion ("T5 confirmed" or "T5 struck" with evidence) | `.agent-notes/allt-T0.md` |
| 2026-09-19 | T0 | New shape not named by the filing: `FtileRepeat.ConnectionOut` crosses lanes independently of `backConnection` (`rujuxa-07-neco067`, `megara-21-rumi574` have `backConnection=simple1` but a cross-lane `repeat-out`; `becanu-19-diti597` reaches both `complex1` AND cross-lane `repeat-out`) — folded into T3's existing `repeat-out` task scope, not a new filing | `selectRepeatBackConnection` (`tile-layout.ts:164-168`) and `pushRepeatOut`'s own lane pair are independent checks | `walk-repeat.ts:236-240,337-345` instrumentation |
| 2026-09-19 | T0 | Orchestrator verification of the T0 report: `9afe03a4` touches only the write-set; `git diff --stat HEAD~1 -- src tests` empty and no `ALLT_TRACE` remains in `src/`; D5 cites re-read: `UGraphicInterceptorOneSwimlane.java:92-104` (`contained1 && contained2` gate, `connection.drawU(this)` only when both hold) and `Swimlanes.java:196-199` + `ConnectionCross.java:49-65` (no `else`) -- "dropped" confirmed as D5's second outcome. T0 ACCEPTED, T5 struck per D9 | Memory `verify-agent-claims-si31`: re-check every mechanism claim against the Java before acting on it | this row |
| 2026-09-19 | B0 | Reachability finding for T3: `FtileRepeat.create` gives `diamond1` lane `swimlane` (`:136`, or the entry tile) and, under the default `INSIDE_HEXAGON` style, `diamond2` lane `swimlaneOut` (`:149`, `:152`); `ConnectionBackSimple1/2` are selected only when `swimlane == null \|\| swimlane == swimlaneOut` (`:188`), and both connect `diamond2 -> diamond1` (`:542`, `:613`), so `Cross` (`Swimlanes.java:196`) never sees them cross-lane: their `drawTranslate` variants are unreachable in the jar under the default style and no golden row can exist (T0's census found none). `Complex1` (`:198`) and `ConnectionOut` (`repeat -> diamond2`, body's `swimlaneOut` vs the `repeat while` lane) ARE reachable, matching T0's rows. T3 still ports Simple1/Simple2 per the brief's five-shape scope with hand-derived tests only; the acceptance clause "golden coordinates for one row per kind" applies to `repeat-out` and `complex1` only | Brief scope is the five shapes; the long-tail rule keeps them; but an acceptance criterion no fixture can satisfy must be amended before dispatch, not discovered by the agent | `FtileRepeat.java:136-162,186-199,537-676` |
| 2026-09-19 | T1 | (agent-proposed) `pushEdge`'s trailing param widened to a `PushEdgeRouting` union (`EdgeShape \| {shape?, loop?}`) instead of a 6th positional param | The 5-parameter hook was already at cap; a union keeps the 3 existing bare-string call sites (fork/if-long-horizontal) untouched, avoiding an out-of-write-set edit (stop 1) | typecheck/lint green; no fork/if-long-horizontal files in `git diff --name-only HEAD~1` |
| 2026-09-19 | T1 | (agent-proposed) Extracted `swimlane-lane-origins.ts` (pure move of `computeLaneOrigins` + `computeDividers`/`trailingDivider`/`buildLaneOrigin`) from `swimlane-placement.ts` | 500-line hook blocked repeatedly (510 -> 501) after the seam edits even with comments trimmed; the block was grepped as unreferenced outside the file; same shape as the file's prior `swimlane-lanes.ts` split. Pre-authorised by README "Pure-move extractions at the 500-line hook" | 379 + 153 lines; orchestrator confirmed `swimlane-placement.ts` now imports only `computeLaneOrigins` from it |
| 2026-09-19 | T1 | (agent-proposed) `EdgeShape` extended with `LoopTranslate['kind']` per spec step 4; `crossLaneMiddleY`'s `case 'default'` became `default:` for exhaustiveness; dispatch keys on `meta.loop`, not `meta.shape` | TS strict requires the switch to cover every member; the five loop literals never reach `crossLaneMiddleY` because the loop branch runs first | typecheck green |
| 2026-09-19 | T1 | (agent-proposed) The stub elbow arithmetic is duplicated once per new module rather than shared | Sharing would create a value-level import cycle dispatcher <-> children; both bodies are replaced by T2/T3 | temporary, 2 copies of 3 lines |
| 2026-09-19 | T1 | Orchestrator verification: `215a3254` cherry-picked as `b0803534`; render-all on the merged tree vs T0's `base-svg`: `diff -rq` empty (268/268 byte-identical, stop 7 clear); diagonal scan 0; typecheck/lint/build exit 0; `npm test` exit 0, JSON collected 729 = on-disk 729, 20035 passed. The agent's "spec line drift" note is not borne out (the orchestrator verified `routeEdge` at `:373` and `PlacementResult` at `:82` on `9a47e8fd` before dispatch); harmless, no action. T1 ACCEPTED | Stop 7 is the batch's gate; every claim re-measured | `scratchpad/b0-merged-svg`, `scratchpad/b0-vitest.json` |
| 2026-09-19 | B0 | Batch 0 CLOSED at `b0803534`. Aggregate `weightedScore` 51371 (unchanged from baseline, by construction: zero byte movers). Worktree `allt-t1` removed; `allt-t2`/`allt-t3` cut from `b0803534` with `node_modules`/`assets/stdlib` symlinked and path-rewritten tools in `<wt>/.allt-tools/`. T2 and T3 dispatched concurrently; each returns its journal rows as text (neither edits the journal) and neither touches `fixtures.md`; `docs/catalog.md` conflicts at merge are resolved by regenerating | Batch close bookkeeping; memory `batch-parallelism-needs-worktrees` | `git worktree list` |
| 2026-09-19 | T3 | HALT, stop 1: D3's one-to-two `repeat-out` split makes `placeSwimlanes` return more edges than the walker's `edgeMeta`; `assign-coordinates-full.ts:222,226,235` still thread the pre-split `edgeMeta`, `compress/shapes-of.ts:391-393` zips by index and throws at `:236` on `becanu`/`rujuxa`/`megara`, `edge-draw-order.ts:101-124` would silently drop the second edge on the `compress:false` path. None of the three files is in any task's write-set. Orchestrator re-read all three sites and confirms the agent's mechanism; T3's edits stay uncommitted in `allt-t3`. Options and recommendation (thread `placed.edgeMeta`, ~15 lines, as fix task T3a) in `stop-1-edgemeta-zip.md` | README stop 1; D3's Consequences named the test-side hazard but not the production consumers, a planning gap | `stop-1-edgemeta-zip.md`; T3 report (`git diff --name-only` in `allt-t3`: 2 write-set files) |
| 2026-09-19 | T2 | Agent stalled (harness watchdog, no progress 600 s) after the diagonal scan with both write-set files edited and `base-svg`/`t2-svg` rendered but uncommitted; resumed via SendMessage under the original brief rather than re-spawned (context intact, memory `parallelism.md` resumption rule) | T2 is independent of stop 1 (one edge + one reservation, no length divergence) | worktree `allt-t2` `git status`: 2 files, +120/-55 |
| 2026-09-19 | T2 | Agent stalled a second time (watchdog) waiting on its own background `npm test`, which had finished. Orchestrator finished the verification in place (push-forward "running a small task in the orchestrator", journaled) and did NOT commit: arithmetic matches `FtileWhile.java:277-308`; movers exactly `kijazo`/`ruzica`; scan 0; typecheck/lint/build 0; `npm test` 7 red (ratchet rises 208->211, 545->559; style census +1/+2 strokeWidth-1; ruzica swimlane census canvas 423->452; two stub-era seam tests). Per-tag: the +1 line per back edge is the jar's four-segment shape; kijazo's other +2 lines are the pre-existing while-exit dog-leg | Stop 4 (rise without adjudicated mechanism) and the finding below block the commit | `allt-t2/.allt-measure/t2-vitest.json`, `t2-svg`, `stop-1-edgemeta-zip.md` addendum |
| 2026-09-19 | T2 | HALT, stop 14: D4's `midArrowAt` is an absolute point and `compress/compress-geometry.ts:135-140#transformEdge` maps only `edge.points`, so the mid-arrow renders at its pre-compression x (kijazo 477.431 vs the edge's 431.828, 45.6 px off). The jar draws `asToUp` through the same compressing `UGraphic` as the snake (`FtileWhile.java:307`). Fix site is the file stop 14 names; `shapes-of.ts` also needs the polygon as an occupant. Same root as T3's stop 1: seam outputs the no-op gate could not exercise | Two design gaps in the brief (D3 consumers, D4 under compression) found in one batch; both need files outside every write-set; the human must grant them (recommended: one fix task T1b) or re-plan | `stop-1-edgemeta-zip.md` |
| 2026-09-19 | B1 | Batch 1 HALTED. Both worktrees keep their uncommitted work (`allt-t2`: 2 src + 1 test + catalog + t2.json; `allt-t3`: 2 src). Mission branch HEAD is the docs-only state after Batch 0. Nothing merged from Batch 1; no re-pin; aggregate at HEAD 51371 | Autonomous-execution rule: stop and wait on stop conditions 1/14 | `git worktree list`, `git status` per worktree |
| 2026-09-20 | B1 | HUMAN DECISION: option 1 of `stop-1-edgemeta-zip.md`. Fix task T1b added (`batch-1/T1b-seam-consumers.md`) with an explicit write-set grant covering `assign-coordinates-full.ts`, `swimlane-placement.ts`, `compress/compress-geometry.ts`, `compress/shapes-of.ts` (+ `edge-draw-order.ts` if needed) and their tests; stops 1 and 14 lifted for exactly that set. T2 and T3 resume in their worktrees after T1b lands on the mission branch | User instruction "Go with T1b, grant the write-set, then resume T2 and T3" | this row |
| 2026-09-20 | B1 | Execution plan for the resumption: T1b in worktree `allt-t1b` cut from the mission HEAD; on acceptance it is cherry-picked onto the branch and re-verified byte-identical (no producer exists yet). Then each of `allt-t2`/`allt-t3` gets its uncommitted edits committed as a temporary `wip(allt-TN)` commit and its branch rebased onto the new mission HEAD (catalog conflicts resolved by `npm run catalog`); the resumed agents amend that WIP into their single task commit | Shared stash is forbidden across worktrees (session rule); a WIP commit + rebase keeps each agent's work attributable | `git worktree list` |
| 2026-09-20 | T1b | (agent-proposed) `edge-draw-order.ts` needed no change: `lanePassOrder`/`applyEdgeDrawOrder` size the permutation from the `edgeMeta` they are given; once both `assign-coordinates-full.ts` sites pass `placed.edgeMeta` the arrays are consistent by construction | read `edge-draw-order.ts:101-124` | — |
| 2026-09-20 | T1b | (agent-proposed) The N>1 `edgeMeta` repeat is tested as the pure `repeatEdgeMeta` helper plus one-edge end-to-end cases, because no stub returns two edges yet; T2/T3 bring the real producers | spec's documented fallback | `swimlane-placement.test.ts` |
| 2026-09-20 | T1b | Orchestrator verification: `227652e6` cherry-picked as `345ab672`; render-all on the merged tree vs T0's `base-svg`: `diff -rq` empty (268/268); scan 0; typecheck/lint/build 0; `compress-geometry.ts:148` now transforms `midArrowAt`; full `npm test` run on the branch recorded in the next gate row. `allt-t2`/`allt-t3` rebased onto `345ab672` without conflict (WIP commits `8b036fa7`, `e0c8b583`). T1b ACCEPTED | Stop 7 is the gate; the rebase precedes the resumes | `scratchpad/t1b-merged-svg` |
| 2026-09-20 | B1 | Mid-mission gate allowance, adopted from `activity-loop-tile-port` T2/T4/T5 rows: until T6's single re-pin, `npm test` may be red ONLY on the four activity oracle pin tests (`diff-baseline.ratchet`, `style-baseline`, `swimlane-baseline`, `text-baseline`) and only on rows that are declared movers with a journaled mechanism; every other test green, collected = on-disk. Any other red is a gate failure | The pins are re-pinned once at T6 by design (D6); a correct port that adds the jar's elements necessarily moves them | prior journal `plans/activity-loop-tile-port/decision-journal.md:21,36,45` |
| 2026-09-20 | T1b | Gate on the mission branch at `345ab672`: `npm test` exit 0, JSON collected 729 = on-disk 729, 0 failed (20048 passed per the agent's run, same tree); typecheck/lint/build 0 (previous row). Batch 1 unblocked; T2/T3 resumed | Quality gate record | `scratchpad/t1b-vitest.json` |
| 2026-09-20 | T2 | (agent-proposed) Ported `ConnectionBackSimple#drawTranslate` (`FtileWhile.java:277-308`) into `routeWhileBack`; `pushWhileBack` attaches the `WhileBackLoop` record only on the has-point-out branch (`ConnectionBackEmpty` stays untagged, not translatable) | hand-derived + golden-derived (kijazo) unit tests; formula matches the Java | `swimlane-loop-translate-while.test.ts` |
| 2026-09-20 | T2 | (agent-proposed) Rises kijazo 208->211, ruzica 545->559 classed ELEMENT GROWTH UNDER POSITIONAL PAIRING: kijazo's new jar-matching 4th segment reindexes every later element (family-diff: 8 old stray diffs resolved, 4 new for the added line, rest pure reindex); ruzica's `xx = max(dx1,dx2) + dimTotal.width` (`:297`) is wider than the stub's elbow, `computeBounds` (`assign-coordinates-full.ts:57-61`) folds edge points into `maxX`, so lane 2 grows and later nodes shift right (canvas 423->450, golden 528); its swimlane/style/text census moves share that one mechanism | Exit bar: rises grouped by class | `measurements/t2.json`, family-diff |
| 2026-09-20 | T2 | (agent-proposed) Named residual: kijazo mid-arrow Y anchor 199.556 vs golden 212.556 after T1b; the shape is right relative to its own endpoints, the endpoints inherit this fixture's pre-existing node-position divergence (body rect dy 20.25, header dy 5.5 -> anchor (20.25+5.5)/2 = 12.9 off); the 12->10 elbow squeeze under compression matches `xovano`'s same-lane elbow and is correct | Outside T2's write-set; T4 sweep candidate | kijazo golden vs `t2-svg` |
| 2026-09-20 | T2 | (agent-proposed) Two stub-era assertions in `swimlane-placement.test.ts` UPDATED with cites `FtileWhile.java:289-300` (5-point snake) and `:304` (UEmpty reservation), never deleted | Orchestrator's pre-authorised test-file clause | — |
| 2026-09-20 | T2 | Orchestrator verification: `bc30665f` cherry-picked as `8978bc7e`; `git diff --name-only HEAD~1` = write-set + catalog + t2.json; render-all on the merged tree vs `t1b-merged-svg`: movers exactly `kijazo`, `ruzica` (stop 5 clean); scan 0; typecheck/lint 0; kijazo mid-arrow polygon anchored at (431.828, 199.556) ON the vertical run x=431.828 (pre-T1b it was 477.431). Agent's own `npm test`: 730 collected = 730 on disk, red only on the four pin files for kijazo/ruzica (allowance row). Full `npm test` on the branch deferred to the Batch 1 close after T3. The 28.6 px difference in `xx - hookX` vs the golden (309.7 vs 338.3) is lane 1's pre-existing width divergence (ours 155.8, golden 193.8; lane 2 equal at 344.9), not the shape. T2 ACCEPTED | Every claim re-measured | `scratchpad/t2-merged-svg` |
| 2026-09-20 | T3 | (agent-proposed) `swimlane-placement.test.ts` (T1's write-set, closed) had two tests whose own comments said "STUB behaviour only" (the `it.each` dispatch test assumed one edge per `repeat-*` kind; the flat-map test used `repeat-out` as its second fixture); updated with a dedicated two-edge `repeat-out` dispatch test rather than leave `npm test` red on a correct D3 implementation | Pre-authorised test-file clause; same clause T2 used | `git diff --name-only HEAD~1` |
| 2026-09-20 | T3 | (agent-proposed) `rujuxa`/`megara` (cross-lane `repeat-out`, same-lane back) render BYTE-IDENTICAL after the port: `ConnectionOut#drawTranslate`'s `middle = (mp1a.y + mp2b.y)/2` (`FtileRepeat.java:317`) is the generic elbow's formula, so the two snakes' concatenated points equal the old four points, the first edge has no arrowhead and `tbout` is null in every corpus row | md5-identical SVGs; stop 12 clear (no row's counts changed) | `t3-svg` vs `base-svg` |
| 2026-09-20 | T3 | (agent-proposed) Residual, one mechanism for kudedo 35/37->34/37, mafete 30/33->29/33 and mafete's rise 177->179: `renderer.ts#renderEdge` emits segments, then the terminal tip, then the emphasize tip; `Worm.java:133-143` emits the emphasize decoration inline at its matching segment (`drawLine(ug, line, direction)` draws the polygon then the `ULine`, `:179-183`) with the end decoration last (`:164-171`). Connector coordinates verified exact vs golden on X (becanu, kudedo); the divergence is element ORDER under positional pairing, pre-existing, `renderer.ts` outside Batch 1's write-set | Diagnosis artifact; orchestrator re-read `Worm.java:125-185` and confirms | kudedo idx 30, mafete idx 22-26 |
| 2026-09-20 | T3 | (agent-proposed) becanu's `repeat-out` and `complex1` connectors match the golden on X after canvas offset (dx 4.0) using the walker's untranslated records and the real lane deltas (col1 10, col2 68.9875); Y differs only by the later Y-compression pass | Exit bar evidence | `t3-svg` |
| 2026-09-20 | T3 | Orchestrator verification: `dde6afdc` cherry-picked as `cbc04b83` with two conflicts resolved (`docs/catalog.md` regenerated; `swimlane-placement.test.ts` header comment merged, both sides' tests kept); `git diff --name-only HEAD~1` = write-set + catalog + t3.json; render-all on the merged tree vs `t2-merged-svg`: movers exactly the six `complex1` rows (stop 5 clean); scan 0; typecheck/lint 0; the four touched test files 75/75 green. Simple1/Simple2 ported with hand-derived tests only (unreachable in the jar, journaled B0). T3 ACCEPTED | Every claim re-measured | `scratchpad/t3-merged-svg` |
| 2026-09-20 | B1 | Batch 1 CLOSED at `cbc04b83` (+docs `ca6fc8ab`). Gate on the merged branch: `npm test` collected 731 = on-disk 731; red ONLY on the four activity pin files for the 8 mover rows (kijazo, ruzica; becanu, givanu, kasadu, kudedo, mafete, manata), within the allowance row; typecheck/lint/build 0; scan 0. Aggregate `weightedScore` 51371 -> 51390, risers exactly kijazo (+3), ruzica (+14), mafete (+2), each classed in a T2/T3 row; fallers 0. Sibling suites: per-file test counts identical between the Batch 0 and Batch 1 JSON reports outside `activity` files (stop 6 clear; the +2 files are the two new activity translate tests) | Exit bar: aggregate recorded, never gated; rises grouped by class | `scratchpad/b1-vitest.json`, `b1-probe.json` |
| 2026-09-20 | B1 | Residuals handed to T4 (all named, none a stop): (a) kijazo mid-arrow Y anchor 12.9 px off, inherited from this fixture's pre-existing node-position divergence; (b) kijazo lane-1 width 155.8 vs golden 193.8 (pre-existing); (c) while-exit dog-leg +2 lines on kijazo (pre-existing, `FtileWhile.ConnectionOut`, not translatable); (d) ruzica canvas 423 -> 450 vs golden 528: whether the translated snake's `xx` run widens the lane in the jar is unmeasured; (e) `renderer.ts#renderEdge` element order vs `Worm.java:133-143,179-183` (emphasize polygon inline before its segment's line; end decoration last) costs kudedo/mafete one aligned element each -- `renderer.ts` is outside T4's write-set, so T4 files it unless the human widens the set; (f) D5: no baseline row exercises a cross-lane non-translatable while connection (T0), so there is nothing to suppress; T4 journals that as the D5 outcome | Hand-off | this row |
| 2026-09-20 | T4 | Sweep confirms Batch 1's mover set is exhaustive: `render-all` + byte diff against T0's `base-svg` differ on exactly the 8 named rows; `t4.json` vs `base.json` aggregate 51371 -> 51390 (unchanged from Batch 1 close, by construction -- no code edited); risers exactly kijazo/mafete/ruzica, matching the pin-file allowance; diagonal scan 0/268 | Stop 5 acceptance | `measurements/t4.json`, `t4-svg` vs `allt-t0/base-svg` diff -rq |
| 2026-09-20 | T4 | Discovery: `becanu`/`givanu`/`kasadu`/`manata` render BYTE-DIFFERENT svgs than the baseline (T3's port visibly changes their cross-lane connector geometry, e.g. becanu's line moves from x=118.663/35.675 to x=144.337, golden 148.337) yet `weightedScore`/diff-count is IDENTICAL before/after per row (becanu 190/161 both). `compareSvg`'s default `weight=1` per diff doesn't shrink when a wrong value moves closer to golden without crossing the tolerance to become correct -- confirms `oracle-score-blind-to-magnitude`, not a new defect; the port is working, the gate just can't see it | Diagnosis before any fix; ruled out "T3's port made no difference" | diff-bag script comparing `compareSvg` per-path actual/expected, `.agent-notes/allt-T4.md` |
| 2026-09-20 | T4 | Residual (d) resolved (measured, not fixed -- origin outside write-set): the jar's canvas DOES grow from the translated connector, but via a SEPARATE sizing pass from lane-width computation. `Swimlanes#calculateDimension` (`Swimlanes.java:455-457`) calls `getMinMax` (`:483-487`) -> `TextBlockUtils.getMinMax(this,...)` (`TextBlockUtils.java:138-141`), a full `drawU` pass that re-runs `drawWhenSwimlanes` (lane translates already cached) and its trailing `Cross` (`Swimlanes.java:358-360`), which DOES draw the cross-lane connector. The EARLIER per-lane pass (`computeDrawingWidths`/`computeSizeInternal`, `:376-431`) that actually sets the dividers/translates never sees it: `UGraphicInterceptorAllSwimlanes#draw`'s `Connection` branch (`UGraphicInterceptorAllSwimlanes.java:129-143`) gates `contained1 && contained2` on ONE swimlane at a time, the same gate D5 already cited, so a true cross-lane connection is invisible to every per-lane `LimitFinder`. Verdict: golden (528) is right; our 450 falls short because `assign-coordinates-full.ts#computeBounds` (T1b's closed write-set) folds the translated edge's points into the SAME `maxX` that also drives lane placement -- one pass where the jar runs two. Ruled out: lane-width growth (the per-lane gate proves the connector cannot reach a lane's own `LimitFinder`) | Task step 2, D6's residual clause | `Swimlanes.java:376-431,455-457,483-487`; `TextBlockUtils.java:138-141`; `UGraphicInterceptorAllSwimlanes.java:129-143`; `assign-coordinates-full.ts` (read-only) |
| 2026-09-20 | T4 | Residual (e) re-confirmed with `--dump` evidence, unchanged from Batch 1 and still outside every write-set: `kudedo-31-pafi082` index 27 is a jar `<polygon lane=1>` where ours has a `<line lane=1>` (ours defers both its terminal and emphasize polygons to indices 29-30); `mafete-03-rapa918` shows the identical pattern at index 26. Matches `Worm.java:133-143,179-183` (emphasize decoration drawn inline at its segment, end decoration last) vs `renderer.ts#renderEdge`'s segments-then-terminal-then-emphasize order. Cost: exactly kudedo (34/37) and mafete (29/33), 1 alignment index each vs their stub-elbow numbers (35/37, 30/33); no other classified row shows this pattern. Filed, not fixed -- `renderer.ts` is outside every task's write-set per the README | Task step 2 | `.agent-notes/allt-T4.md`, `scripts/activity-probe.ts --dump kudedo-31-pafi082\|mafete-03-rapa918` |
| 2026-09-20 | T4 | D5 outcome applied: no code change, per T0's finding re-verified. `pushWhileBack`/`pushWhileOut` (`walk-while-branch.ts`) already attach a `WhileBackLoop` tag only on the has-point-out (`ConnectionBackSimple`) branch; `ConnectionBackEmpty`/`ConnectionOut` stay untagged and keep today's generic elbow, matching the jar's silent drop (`UGraphicInterceptorOneSwimlane.java:92-104`'s `contained1 && contained2` gate re-read: never both true for a genuine cross-lane pair; `ConnectionCross.java:49-65` draws nothing when `!(connection instanceof ConnectionTranslatable)`). No baseline row constructs a cross-lane instance of either class (T0), so there is nothing to suppress and no new tag is needed | Task step 3, stop 9 check | `UGraphicInterceptorOneSwimlane.java:92-104`; `walk-while-branch.ts` (read-only, unedited) |
| 2026-09-20 | T4 | Sibling suites unmoved: `npx vitest run tests/oracle/svg-conformance` per-file test counts identical to Batch 1 close's `b1-vitest.json` for all 27 files (4 activity pin files unchanged at 384/383/106/387; the rest -- sequence/state/class/description/json -- untouched). Full `npm test`: 731 collected = 731 on-disk, red ONLY on the 4 pin files for the 8 mover rows (19 failing assertions, all named), matching the mid-mission allowance row | Stop 6 check; quality gate | `t4-svgconf.json`, `t4-vitest.json` |
| 2026-09-20 | T4 | No fix applied: every named residual's origin (a-c: pre-existing node placement / non-translatable connector; d: `assign-coordinates-full.ts`; e: `renderer.ts`; f: no code needed) sits outside the four Batch 1 files (`swimlane-loop-translate-while.ts`, `swimlane-loop-translate-repeat.ts`, `walk-while-branch.ts`, `walk-repeat.ts`), so nothing in T4's write-set is the mechanism's origin (`diagnosis.md`: fix at the origin, never downstream). `fixtures.md`'s new "Align after (T4)" column and residual list are the D6 exit artifact for the 10 classified rows | Task step 2's fix-or-file rule | `git diff --name-only HEAD~1` (no `src/` changes) |
| 2026-09-20 | T4 | Orchestrator verification: `f7154050` is docs-only (`git diff --stat HEAD~1 -- src tests` empty), write-set only; the (d) mechanism was read against `Swimlanes.java:455-487` and `UGraphicInterceptorAllSwimlanes.java:129-143` and holds; (e) re-confirmed from `Worm.java:133-143,179-183` earlier. T4 ACCEPTED, no fixes were inside the write-set | Every claim checked | this row |
| 2026-09-20 | T6 | Rise list from `activity.diff-baseline.ratchet` at `f7154050`: exactly kijazo 208->211, mafete 177->179, ruzica 545->559, each classed in a T2/T3 row (stop 4 clear). Goldens snapshotted to `measurements/goldens-before/`. `repin-activity-baselines.ts --write --accept-rises kijazo-83-kipu485,ruzica-16-deli877,mafete-03-rapa918`: 19 changes (3 diff-baseline rises, 8 style, 7 swimlane, 1 text census equality pins, all on the 8 mover rows); diff against the snapshot: rises exactly the 3 accepted, falls 0, no other pin moved, `$comment` unchanged. `diff-census.json` skipped by the tool and left as-is: none of the five missions since `activity-klimt-compress` re-censused it (`git log -- diff-census.json`), so this follows the live precedent rather than the tool header's older one | T6 steps 1-3; memory `repin-script-raises-preexisting-red-pin` | `measurements/goldens-before/`, this row |
| 2026-09-20 | T6 | Gates on the re-pinned tree: `npm test` exit 0, JSON collected 731 = on-disk 731, 20066 passed / 0 failed; typecheck/lint/build 0. Close-out: README batch table ticked + summary appended; `next-missions.md` entry marked EXECUTED + CLOSED with six residuals re-filed under `activity-canvas-minmax-pass`, `activity-emphasize-tip-order`, `activity-lane-align-residuals` (+ notes on `activity-loop-backward` and the unreachable Simple1/2 translates); memory updated. One commit `test(allt-T6)`. Branch left UNMERGED for the human's merge-commit | T6 steps 4-5; mission-brief branches merge with a merge commit, never squash | this row |
