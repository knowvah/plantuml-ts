# Decision journal — `activity-loop-lane-translate`

| Date | Task | Decision | Why | Evidence |
|---|---|---|---|---|
| 2026-09-19 | B0 | Execution plan: branch `feat/activity-loop-lane-translate` cut from `9a47e8fd` (brief commit on main). T0 dispatched to `debugger` on the main tree (read-only on `src/`, writes `plans/`, `.agent-notes/`, `planning/`); T1 dispatched to `typescript-pro` concurrently in a prepared worktree `.claude/worktrees/allt-t1` (branch `allt-t1`, `node_modules` + `assets/stdlib` symlinked, `test-results/dot-cache` already tracked). T1's commit is cherry-picked onto the mission branch after T0 lands and re-verified with render-all on the merged tree. Orchestrator commits nothing while either agent is live | Disjoint write-sets; memory `batch-parallelism-needs-worktrees` (T0's render-all must read a tree nobody edits); prior mission's B0 row used the same split | `git worktree list` |
| 2026-09-19 | B0 | `tools/*.mts` hardcode the main-tree absolute path; a copy with the path rewritten to the worktree lives at `<wt>/.allt-tools/` so T1's render-all measures the WORKTREE's code, not main's. `.claude/worktrees/` added to `.git/info/exclude` (local only) so it never appears untracked in the main tree | Running the committed tool from the worktree would silently render main and report zero movers regardless of T1's edits | `render-all.mts:3-6` |
| 2026-09-19 | B0 | The brief's `measurements/base.json` is the probe's `--json` output (aggregate + per-slug scores + families), as the prior mission's T0 used it; render-all writes an SVG directory (`measurements/base-svg/`, gitignored-size permitting or scratchpad) for the byte-level `cmp`. Both are recorded; the JSON is what is committed | `render-all.mts` takes an out DIRECTORY and writes one SVG per slug; it has no JSON mode | `tools/render-all.mts:9-17` |
| 2026-09-19 | T0 | Baseline: `render-all` 268/268, diagonal scan 0/0, aggregate `weightedScore` 51371 (sum of `weightedScore` over the 268 `status:"baseline"` rows in `oracle/goldens/svg-activity/diff-baseline.json`, cross-checked against `scripts/activity-probe.ts --json`'s own `aggregate` field — identical) | README's exit bar requires the aggregate recorded at every batch close | `plans/activity-loop-lane-translate/measurements/base.json` |
| 2026-09-19 | T0 | Classified by INSTRUMENTING the actual lane variables (`ALLT_TRACE` env-gated `console.error` at `walk-while-branch.ts:185`, `walk-repeat.ts:240,345`; reverted before commit, `git diff` on `src/` empty) rather than reading `.puml` text, after the `.puml`-text reading gave the wrong answer for 4 of the 22 rows on a first pass | `.agent-notes/aitp-T7.md`: "measure before stating a mechanism"; `.puml` lane-tag position does not equal the walker's resolved `lane1`/`lane2` (inheritance via `laneAt`/`laneIn`/`laneOut`) | `.agent-notes/allt-T0.md` |
| 2026-09-19 | T0 | Corrected 4 of the filing's own row classifications: `judatu-15-xize591`, `gesogi-81-xoma900`, `bulasi-17-vafa634` ("both builders") and `tobajo-64-mipi810` ("repeat, cross-lane") reach NEITHER `while-back` NOR a cross-lane repeat shape; re-filed as a separate residual, not this mission's mechanism | Direct instrumentation showed `cross=false` on every while-back/repeat-back/repeat-out edge in these four rows | `fixtures.md` rows; `next-missions.md` re-filing |
| 2026-09-19 | T0 | D5 resolved: the jar DROPS a non-translatable cross-lane connection entirely (`UGraphicInterceptorOneSwimlane.java:92-104`'s `contained1 && contained2` gate never passes for a true cross-lane pair in ANY single lane's pass; `ConnectionCross.drawU`, `ftile/ConnectionCross.java:49-65`, draws nothing when `!(connection instanceof ConnectionTranslatable)`). No baseline row exercises it for `while` — `pushWhileBack`'s empty-body branch and `pushWhileOut` both pass the SAME lane variable as both endpoints, so our walker cannot construct a cross-lane instance of these two classes; confirmed no baseline `.puml` has an empty-body laned `while` | Task step 4, stop 9 check | `.agent-notes/allt-T0.md`; `Swimlanes.java:318-356`, `:178-215` |
| 2026-09-19 | T0 | D9: T5 struck. Grepped all 268 baseline `.puml` fixtures (not only the 14 named break rows) for `break` co-occurring with a `\|lane\|` line: zero matches. No break row shares a slug or connector class with any of the 22 lane-translate rows | Task step 5 acceptance criterion ("T5 confirmed" or "T5 struck" with evidence) | `.agent-notes/allt-T0.md` |
| 2026-09-19 | T0 | New shape not named by the filing: `FtileRepeat.ConnectionOut` crosses lanes independently of `backConnection` (`rujuxa-07-neco067`, `megara-21-rumi574` have `backConnection=simple1` but a cross-lane `repeat-out`; `becanu-19-diti597` reaches both `complex1` AND cross-lane `repeat-out`) — folded into T3's existing `repeat-out` task scope, not a new filing | `selectRepeatBackConnection` (`tile-layout.ts:164-168`) and `pushRepeatOut`'s own lane pair are independent checks | `walk-repeat.ts:236-240,337-345` instrumentation |
