# Decision journal — arrow-label-font-colour

Appended during execution. One row per non-trivial judgment call. Every count
comes from a command run on a clean tree.

| Date | Decision / finding | Evidence / why | Flag for review? |
|---|---|---|---|
| 2026-08-17 | Start baseline captured at `fae8ec9c` in a detached worktree (six ignored dirs symlinked, `.agent-notes/si24-census-…`): DOT EQUAL 705/266/259/92/78, `shape-match-report` 785/26,256, full per-engine SVG dump (class 722, object 80, state 272, component 266, usecase 93). Matches README's starting state. | Commands run in the worktree; dumps kept in the session scratchpad for the batch-exit diffs. | No |
| 2026-08-17 | T1 executed by the orchestrator directly, not `typescript-pro` — 6 small write-set files with an exact spec, and the resolver contract is what T2–T5 consume. | Same call SI25 made; parallel agents reserved for Batch 2 where the write-sets are disjoint. | No |
| 2026-08-17 | T1: `arrowFontColor` is stored PRE-RESOLVED (`resolveColorToSvgHex`) in the skinparam handlers, unlike its raw-valued neighbours (`classBorder` etc. resolve at consumption). | D1 says "resolved hex"; the `<style>` path (T2, `cascadeFontColorHex`) lands hex in the same field, and T3–T5 renderers draw it verbatim. `colors.text` stays raw as before (`skinparam.test.ts` pins both). | No |
| 2026-08-17 | T1: NO `cardinalityfontcolor` skinparam added. | `grep -i cardinality style/FromSkinparamToStyle.java` → no hits; only `<style> arrow { cardinality { FontColor } }` (T2) can set `theme.cardinalityFontColor`. | No |
| 2026-08-17 | T1 / stop 10 (`xenusu`): its `arrow<<bluebold>>`/`arrow<<bold>>` blocks set only `Color`/`line.Bold()`, never FontColor; the label colour comes from the plain `skinparam arrow { FontColor green }` block (last of two, `#666666` → green), which `applyNormalKey` already handles. Stereotyped keys route to `applyStereoOverride` and are ignored for `arrow<<X>>` — a pre-existing LINE-colour gap, named out of scope, no parser change. | `test-results/dot-cache/component/xenusu-76-sabi405/in.puml`; `skinparam-stereo-keys.ts` header. | No |
| 2026-08-17 | T1 named gap: the accumulator iterates a `Map` keyed by normalised key, so `defaultFontColor red; arrowFontColor green; defaultFontColor blue` keeps `defaultfontcolor` at its FIRST insertion slot (value blue) → arrow resolves green where the jar gives blue. Experiments b/g (two distinct keys) are exact. Not built — no corpus fixture, and it is a preprocessor Map-shape question, not a handler one. | `skinparam.ts:61` `for (const [rawKey, value] of skinparams)`. | No |
| 2026-08-17 | T1 gates: 595 files / 14,515 tests, coverage 95.37/90.35/96.93/96.46; typecheck/lint/build green; DOT EQUAL 705/266/259/92/78 unchanged; SVG dump `diff -rq` vs baseline: byte-identical (all five engines). Commit `437727db`. `theme.ts` held at 564 lines (doc prose trimmed, no citation removed). | Commands run on the working tree after commit-staging. | No |
| 2026-08-17 | Batch 2 execution plan: T3/T4/T5 → three parallel `typescript-pro` (sonnet) agents, write-sets disjoint (`class/renderer-edge.ts`+its test / `description/renderer-edge.ts`+test / `state-renderer-transitions.ts`+test); T2 by the orchestrator (touches core cascade files all engines read; I own the batch-exit dumps anyway). Agents do NOT commit (index-lock collisions) and run only targeted vitest + `tsc --noEmit`; the orchestrator commits one commit per task from each write-set, then runs the four gates, DOT ×5 and the per-engine dumps on the assembled tree. | `rules/parallelism.md` file-ownership + one-writer rule; brief overview batch-2. | No |
| 2026-08-17 | T3 pre-loaded observation was WRONG: `core/svg.ts` does NOT pass colours through verbatim — `svg-shapes.ts#text()`/`svg.ts#attrs()` route every fill/stroke through `svg-format.ts#shortenColor` (`#FF0000`→`#F00`, jar `SvgGraphics.java:543,884` `fillMe`). The T3 agent followed the code, not the note; T5 agent found the same. | Agent reports; `svg-format.ts:128`. | No |
| 2026-08-17 | T3/T4/T5 agent reports: T3 split `renderEdge`'s label/cardinality drawing into `renderEdgeMainLabel`/`renderEdgeCardinalityLabels` (NLOC hook); T5 extracted `buildTransitionLabelMarkup` (same reason); T4 dropped the now-unused `JAR_DEFAULT_TEXT_COLOR` import in `description/renderer-edge.ts` only. Commits: T2 `8204d7d9`, T4 `bbef1b5d`, T5 `a5e6093e`, T3 `13383da6`. | `git log`. | No |
| 2026-08-17 | T2: `cascadeFontColorHex` WITH the canvas background (`base.colors.background`) for both arrow and cardinality — threaded as an optional third `backgroundHex` param on both `compute*FontOverride` (undefined → plain `cascadeHex`, keeps every existing one-arg caller/test byte-identical). `computeStyleMapExtras` gained `base`. `style-cascade-class.ts` 499→498 by trimming doc prose only (header, SPOT/NOTE_SNAMES, cascadeHex, DEFAULT_NOTE_BACKGROUND, cardinality doc — no `file:line` removed). | push-forward 1 not needed; `#?red:green` on white→`#FF0000`, on black→`#008000` pinned in `style-cascade-class.test.ts`. | No |
| 2026-08-17 | T2 / push-forward 4: no corpus fixture uses `componentDiagram { arrow { FontColor } }` (`grep -l "componentDiagram" test-results/dot-cache/component/*/in.puml` → 0); `ARROW_SNAMES` left at `classdiagram`. | grep. | No |
| 2026-08-17 | **Batch 2 dump vs baseline: 97 fixtures moved, not the brief's 12.** All moves are `<text fill>` only (token-wise `difflib`, no structural change, no `<polygon>` — no corpus class fixture with an arrow FontColor has a magic-arrow glyph, so D6 is unit-tested + oracle experiment a only). (1) **state ×80**: transition labels `#181818`→`#000`; the jar draws `#000` for every state link label (325/325 texts across the 80; no jar state `<text>` is `#181818` anywhere; 3 fixtures — `sagica`,`susena`,`xetase` — have no jar link text at all, pre-existing structural divergence). D3's premise that state was "byte-identical today" was wrong: `theme.colors.text` was never faithful for state labels; the 59 pinned state fixtures are label-free, which is why the ratchet held. Toward jar; NOT stop 8. (2) **class**: `camuna`(#00F/#F00 incl. cardinality `customer`/`1` = jar), `nafiki`, `picija` (#FFA500), `ticuxa` (#F00) = jar; **`lurevi-57-reku842`** (unnamed, `<style>` FontColor white → `#FFF` = jar). (3) **usecase** `xozabi` #0F0 = jar. (4) **component**: `zosuje` #191970, `xenusu` #008000 (all 3 labels), `figika`/`banatu`(unnamed) `#F00` — their cached oracle SVGs are an OLDER jar generation (`fill="#000000"`, per-text font-family; 236/266 component oracles are that format), so re-rendered with the pinned jar via `oracle-render.sh`: both `#F00` = ours. `golati`/`kokodo`/`koxeca` (unnamed) + `minulo`/`pemifo`/`vaseda`/`cudazo` (named): every label WITHOUT a stereotype override now = jar (#008000/#FFC0CB); labels the jar colours through a mechanism the port lacks moved `#000`→the diagram-level arrow colour, jar red — see next row. | `$S/b2/moved.txt`, per-label scoring script output kept in the session log; oracle renders in scratchpad `oracle/`. | **Yes** — clause 2 literally not met (moves outside the brief's exception list), all understood and toward/equal jar except the next row. |
| 2026-08-17 | **Named gaps (do not widen — stop 10 / push-forward last bullet):** (a) skinparam `arrowColor<<tag>> text:Red` / `Color<<v1.1>> text:Red` inside `skinparam Arrow {}` (`golati`,`kokodo`,`koxeca`,`minulo`) — stereotype-qualified arrow skinparam WITH the `text:` colour sub-spec; both halves unported (`skinparam-stereo-keys.ts` ignores `arrow<<X>>`). (b) `<style> arrow { .va { FontColor } }` per-tag arrow cascade in the DESCRIPTION engine (`pemifo`,`vaseda`) — description does not read `arrowTagCascade` at all (its `.vb { LineColor }` lines are `#181818` today while jar draws red/blue), a whole per-tag arrow-style mechanism, class-only so far. (c) positional mid-document `<style>` blocks in description (`cudazo`: first arrow red, second pink in jar; port merges all blocks → both pink; class has `stylePositions`, description not). For (a)/(b)/(c) the affected labels moved `#000`→diagram-level colour ≠ jar's per-tag colour — LATERAL (were wrong, still wrong); every sibling label without an override now equals jar. Reverting them would require the port to know the tag override exists = building the feature. | Sources: `koxeca` lines 3-15, `pemifo` lines 3-25, `cudazo` whole file. | **Yes** — lateral label moves in 7 fixtures; scored explicitly in the close-out. |
| 2026-08-17 | Batch 2 gates: 595 files / 14,532 tests, coverage 95.37/90.35/96.93/96.46; typecheck/lint/build green (build exit 0 — the 3 `include-resolver-node.ts` TS lines in its output are pre-existing vite-dts warnings, untouched file); DOT EQUAL 705/266/259/92/78 unchanged; `shape-match-report` 785/26,256, zero per-fixture change; class 315 / state 59 / description / skin ratchets hold. | Commands on the assembled tree after the four commits. | No |
