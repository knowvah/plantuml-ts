# T6 — Close-out

Every number from a command run on a clean tree at the final code commit
(detached worktree, SI24 recipe). Template: `plans/class-guide-line-glyphs/
README.md#close-out-2026-08-17`.

1. Measure: `dot-sync-report` ×5 (all unchanged); `shape-match-report`
   per-fixture diff vs the start baseline; per-engine SVG dumps vs the start
   baseline — list every moved fixture; for each of the 12 named fixtures,
   compare each edge-label `<text>`/glyph `<polygon>`/cardinality `<text>`
   `fill` to the jar SVG's; for any skin fixture that moved, render its
   oracle and confirm toward-jar; ratchet pin counts; test count/coverage.
2. Append a "Close-out" section to `README.md`: exit bar scored ✓/✗ per
   clause with the measurement; fixtures moved (each toward jar, with the
   fill values); tasks planned vs executed; follow-ons (note-on-link text
   colour, classifier text under `defaultFontColor`, sequence/activity arrow
   colours, `xenusu` `<<tag>>` if named, `componentDiagram` selector gap if
   named, anything new).
3. `planning/mission-index.md`: row **SI26** (format: SI24/SI25 rows).
   `planning/next-missions.md`: refresh the standing-signals block; mark §1
   follow-on #2 done, dated.
4. Journal: tasks, decisions flagged, gate results.

Write-set: the four files above. Reversible — docs only.

Commit: `docs(T6): close arrow-label-font-colour — scored bar, SI26 row`
