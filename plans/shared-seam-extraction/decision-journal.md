# Decision journal — shared-seam-extraction

| When | Task | Decision | Why | Evidence |
|---|---|---|---|---|
| 2026-08-17 | 1a+1b | Run batches 1a and 1b as ONE 7-way parallel batch (T1 T2 T4 T5 T6 T7 T8), shared working tree, per-task manifest scoped `--only` to touched engines, full manifest + dot-sync + 4 gates at batch end by orchestrator | README permits it; write-sets verified disjoint at planning; worktree isolation would need 6 gitignored asset dirs linked (`.agent-notes/si24-census-worktree-needs-ignored-assets.md`) | overview files; agents forbidden from checkout/reset/stash/clean (`.agent-notes/g0b-shared-worktree-risk.md`) |
| 2026-08-17 | 1a+1b | INCIDENT: T7's bare `git commit` (orig ccf1e3dd) swept other agents' `git mv`-auto-staged renames/deletions (T2 ×2, T4 ×1, T5 ×3, T8 ×5) into its commit. Orchestrator re-sliced after all 7 landed: `git reset --soft` to 3c93fdc0 + one `git commit -- <write-set paths>` per task with the original messages; result byte-identical to pre-splice tree (backup tag `si27-batch1-presplice`, deleted after gates) | Merge-commit branch: per-task commit IDs are referenced by this journal; misattributed commits would break that | `.agent-notes/si27-t7-shared-index-commit-hazard.md`; final commits ee3c4163 T6 · 2e107fd8 T7 · 497cbd24 T4 · 46b11a22 T2 · 6584addc T5 · 2b0c0891 T1 · d26269f5 T8 |
| 2026-08-17 | T1 | Manifest: 4 fixtures differ (state duzazu-41-telu529, juvagu-33-dupa212, lokija-02-dipe348, vixobo-14-jole910), all `\t` escapes — accepted jar-ward. Orchestrator re-verified lokija: before T1 `<text>` content `\tline2` textLength 37.363; now `line2` textLength 29.662 = jar's glyphs+textLength; only x tab-stop indentation (`skinparam tabSize`, unimplemented) still differs. duzazu/vixobo DOT hash unchanged (SVG-only) | `Display.java:305` `current.append('\t')`; README exit bar 3, push-forward T1 | `.agent-notes/si27-t1-display-newlines-one-port.md` |
| 2026-08-17 | T1 | `fix(T1)` ca884222: `tests/unit/state/layout.test.ts:982,995,1008,1021` hand-built `display` with a real LF; parser stores literal `\n` (probe on sosoxe-55-demi451: label = `A on several lines\nwith\na lot of text`, titleTableHeight 37 = jar HEIGHT). File outside every write-set — stop 1 considered; applied as a gate `fix_and_rerun` because the test encoded exactly the bug T1 retires (2-line input correction, mechanism verified) | Gate rule vs stop 1; a reasonable dev might have stopped | commit ca884222 |
| 2026-08-17 | 1a+1b | `chore(T1,T2,T4,T8)` 54060b49: retired those tasks' KNOWN_DEBT entries now (T0 contract said T10 empties it) — otherwise `npm test` is red between batches by design of D5 | Keeps the gate meaningful per batch; T10 still empties T3's five | commit 54060b49 |
| 2026-08-17 | T7 | State does NOT alias `CoreCommand<ParseState>`: its `execute` takes a 3rd `pass` arg (faithful to `Command.java:44` `execute(D, BlocLines, ParserPass)`) and `passes` is required, local `Pass`; forcing the alias = ~40 type errors outside write-set. Core keeps D4's literal shape (T9 consumes it); class/description/sequence alias exactly; state carries `@see` + rationale + a type test asserting non-extension. FLAG for maintainer at T10: exit bar 2 "ONE port of Command" is 3/4 literal | D4 shape locked; state's divergence is upstream-faithful | `src/diagrams/state/state-commands.ts`, `tests/unit/core/command/Command.test.ts` |
| 2026-08-17 | T8 | Class background/border-rect splice could not move into the engine renderer (D2 prose): both rects need the POST-chrome canvas and `applyChrome` runs between `renderClass` and `assembleSvg` in `src/index.ts`. Kept the finalize logic in `core/assemble-svg.ts` dispatched on `fragment.diagramType`, using core-only primitives; four shell files + four core→engine imports removed; `klimtShell|classShell|stateShell|jsonShell` gone. Also one out-of-write-set 1-line rename in `tests/oracle/svg-conformance/render-fixture-json.ts` (`jsonShell:`→`diagramType:`) | `TextBlockExporter.java:159-232` order | `.agent-notes/si27-T8-assemble-svg-diagram-type.md` |
| 2026-08-17 | T6 | Core `measureLinkNoteDim(text, font, measurer, pureText?)`: shared half only (rose padding via `roseNoteDim`), engines inject their creole `pureText` closure; state omits it (naive split) = former behaviour. `description/link-note-box.ts` kept as wrapper (importer `link-edge-attrs.ts` is T1's) | Unifying the creole engines is T3/leaf-sizing scope | `EntityImageNoteLink.java:61-73`, `ComponentRoseNote.java:81-90` |
| 2026-08-17 | T5 | Both FrontierCalculator ports algorithmically identical to `svek/FrontierCalculator.java:51-167`; stop 5 not triggered. Name `frontierCalculator`; `state-composite-frontier.ts` kept as 68-line `Box`↔`RectangleArea` adapter (folding into geo → 508 lines, hook cap) | — | `.agent-notes/si27-T5-frontier-calculator.md` |
| 2026-08-17 | T2 | `render-atoms.ts` retired ENTIRELY (spec predicted a description-only residue; none existed — every symbol used by class or description sizing) | — | commit 46b11a22 |
| 2026-08-17 | T4 | 2 stale-path JSDoc fixes in `core/style-map-element.ts`, `core/style-cascade-class.ts` (push-forward "JSDoc naming an old path") | — | commit 497cbd24 |
| 2026-08-17 | 1a+1b | GATES (batch end, clean tree): typecheck ✓ lint ✓ build ✓; manifest 4 differ (T1 jar-ward, above); dot-sync class 705 · state 266 · component 259 · usecase 92 · object 78 (unchanged); npm test 600 files / 14587 pass, cov 95.36/90.33/96.93/96.46 | — | /tmp/si27-*.log |
