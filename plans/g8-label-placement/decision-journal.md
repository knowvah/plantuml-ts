# G8 Decision Journal

| Date | Task | Decision | Rationale | Flagged for review? |
|------|------|----------|-----------|---------------------|
| 2026-07-23 | - | Mission start. Branch `feat/g8-label-placement` created off `feat/g7-borderpoint-rank` at 8ab6cfb. Execution plan: strictly serial batches per README (B1: T1 spec+harness, debugger agent; B2: T2 atomic landing; B3: T3 close-out). Single agent per batch, no parallelism (each batch is one task; write-sets are per-task so no ownership conflicts). Orchestrator-verified paths injected into T1 prompt: backlog `oracle/goldens/state/size-backlog.json` (92 entries + _doc), pins `oracle/goldens/svg-state/ratchet.json` (57). | Batches are dependency-chained (T2 consumes T1's spec; T3 consumes T2's landed state) — no parallel opportunity exists. | No |
| 2026-07-23 | T1 | Agent's first pass: conversion formula jar-exact 11/11 labels on beguxu-19 + buniva-95 (flat) + fomusu-59 (both self-loop kinds) + bemena-23 (0.244904-family); NEW mechanism characterized (marginLabel 1/6 + floor-truncated FIXEDSIZE box — SvekEdge withMargin + appendTable (int) cast). Harness committed-ready, reproduces 149/149 baseline (92 backlog + 57 pins), exit 0, ~1.5s. Gates green (10235 tests, typecheck, lint, build). BUT pesita-10 (named fixture) unverified — agent's probe perturbed ONE edge's box, destabilizing the cyclic AA-pass topology (T13 mechanism). Orchestrator did NOT accept the agent's "not a stop condition" self-grading: stop cond. 5 names pesita. Resumed the agent to run the task-specced builder-seam simulation (ALL edges corrected at once, cross-checked against jar svek-N.dot WIDTH/HEIGHT literals) before deciding proceed-vs-stop. | Single-edge perturbation is not the simulation the task specced; the full corrected box set should restore jar topology. If boxes match jar's DOT input and topology still diverges, that is a real divergence → stop cond. 5. | No |
| 2026-07-23 | T1 | STOP — stop condition 5 triggered; mission halted BEFORE T2. Full-seam simulation (all 9 labeled edges of pesita's AA pass given jar-exact FIXEDSIZE boxes at once, verified 9/9 against svek-3.dot WIDTH/HEIGHT literals) still diverges from the jar oracle: rank/Y assignment matches exactly (Idle+Closing both dy=-55.78 to 4+ sig figs) but horizontal mincross ORDER is reversed (port: Idle<Reanimate<Closing; jar: Closing<Reanimate<Idle; per-node dx +549/+314/-396 — non-uniform, the reject signature). Ruled out: the anchor formula (9/9 exact box ground truth, and 11/11 exact anchors on the other 4 fixtures incl. self-loops + 0.244904-family); wrong pass/svek pairing; the single-edge-perturbation artifact; Reanimate's known ~3px body-height gap. Verdict: pre-existing mincross/emission-order sensitivity in this cyclic pass — the SAME open G7 T13/T16 gap (T16 fixed only the minlen===0 bucket), not a G8 formula defect and not a graphviz-ts library finding (order-sensitivity is port-side input ordering, already tracked). T1 deliverables committed in stop-report form: harness (baseline 149/149 reproduced, exit 0), spec.md (formula + 11-label verification table + §5 divergence report), unit tests. Gates green at commit (10235 tests, typecheck, lint, build, harness). | Stop cond. 5's wording is unambiguous: pesita is named and its label positions are not reproduced. The formula itself is validated everywhere the topology matches; the blocker is upstream of G8's scope. Human must choose: (1) fix the T13/T16 mincross-order gap first (its own task/mission), (2) amend the mission to descope pesita from the named set (explicit amendment per G7 T20b precedent), or (3) authorize T2 anyway with pesita expected to remain a backlog entry (harness gate still enforces zero widenings). | YES — awaiting direction |
