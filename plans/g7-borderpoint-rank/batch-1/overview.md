# Batch 1 — Isolation matrix + adjudication

Isolate which single context variable breaks builder-path rank
behavior (G6 attempt 3: bitaxo exact; pesita/kotagu miss badly;
discriminator = i-wrapper / nested child / parent cluster / extra ee
content). Then adjudicate usage-vs-library and write the routing docs.

| ID | Description | Agent | Writes | Depends On | Done |
|----|-------------|-------|--------|-----------|------|
| T1 | Isolation-matrix measurement (3 paths × 5+ cells) | debugger | disposable probes only (deleted) | — | [ ] |
| T2 | Adjudicate; write Round-3 addendum OR issue 09 + PAUSE | debugger (T1 continuation) | see task file (conditional) | T1 | [ ] |

T1 produces no committed code — deliverable is the matrix artifact in
the decision journal. T2's commit is the batch's only commit.
