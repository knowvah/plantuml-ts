# G7 Decision Journal

| Date | Task | Decision | Rationale | Flagged for review? |
|------|------|----------|-----------|---------------------|
| 2026-07-22 | — | Execution plan: strictly serial T1→T2 (batch 1), conditional T3, T4→T5, T6. One debugger agent per task, no parallelism (every task feeds the next). Branch `feat/g7-borderpoint-rank` off main. Orchestrator commits. | Brief mandates serial batches; parallelism.md autonomous-mode exception — plan journaled, not presented. | No |
| 2026-07-22 | T1+T2 | Verdict: USAGE DEFECT, not a graphviz-ts library defect — no issue 09 filed. Mechanism: the isolation matrix (7 cells: rank+i-wrapper, nested child cluster, parent cluster, non-border pseudo-node, individually and in the two compound combinations mirroring pesita/kotagu) shows graphviz-ts's raw layout output byte-exact vs. real dot 15.1.0 across real-dot/text-path/builder-path on every cell, including end-anchor confirmation against pesita's and kotagu's actual cached production DOT. batch-4 retry-3's misses must therefore originate in plantuml-ts's own `addClusters` wiring or the still-unwired `FrontierCalculator`/`materializeCluster` correction pass, not in the library. | Full matrix + ruled-out list + downstream-suspect statement appended as "Round 3" to `plans/g6-cluster-geometry/batch-4/withlabel-derivation.md` — attempt 4 can be paper-derived from that spec without re-measuring. | No |
| 2026-07-22 | T4 | Paper gate PASS: bitaxo C 42×101.72, pesita AA 126×104.72, kotagu CompositeState 289×358 all reproduce exactly from the adjudicated spec (real-dot raw initial + unmodified frontierCalculator + D4 sentinel exclusion). Edit list vs attempt 3 recorded in the derivation doc (6 items: titleTableEligible relaxation, D4 exclusion, Round-3 builder shapes, frontier wiring, child-first order, numeric regression assertion). | D3 gate satisfied; T5 authorized. | No |
| 2026-07-22 | T5 | ATTEMPT-4-MISS — PERMANENT STOP (README cond. 7, D3). bitaxo C exact (42×101.72); pesita AA 55×293.61 vs 126×104.72; kotagu CompositeState 248×398 vs 289×358. Full revert executed and verified (git status clean; 10192 tests, typecheck, lint, build all green post-revert, re-confirmed by orchestrator). No tuning attempted. | Mechanism (T5 diagnosis): pre-existing defect at `src/core/graph-layout-build.ts:154-159` — `addClusters` parent-resolution always nests a child cluster in the parent's OUTER subgraph handle, never inside an active protection wrapper; and jar's "a"/"p0" ancestor-wrapper mechanism (`ClusterDotString.java` protection0/thereALinkFromOrToGroup1) is unimplemented repo-wide. kotagu: jar wraps SubComposite as cluster12a{cluster12p0{cluster12}} (+8pt margin per layer, verified via dot -Txdot on cached svek-1.dot: 191×277→207×293→223×309); the port omits the pair so CompositeState raw initial is 244×412 vs jar 303×358. pesita: AA resolves as sibling to nasreq_auth\'s i/p1 wrapper instead of inside it. Ruled out: frontier math (hand-traced, reproduces wrong output from wrong input; bitaxo exact), D4 (titleTableHeight 28 confirmed), issue-08 naming (isolated repro clean), border-point cluster internal shape (jar-DOT-exact synthetic repro reproduces the same wrong 244×412). Fix path for a human-approved attempt 5: port the a/p0 ancestor-wrapper mechanism and make parent-resolution nest into the active wrapper handle FIRST, then re-attempt border-point wiring. T4\'s paper gate had a structural blind spot: it derived initial bboxes from the JAR\'s cached DOT (which contains the a/p0 wrappers), so it validated the frontier math but not the port\'s own DOT emission — a future paper gate must derive from the PORT\'s emitted DOT. | YES — fifth attempt requires human sign-off |
| 2026-07-22 | — | ATTEMPT 5 AUTHORIZED by human sign-off ("port the a/p0 mechanism first"). Mission reopened; batch 5 added: T7 (a/p0 port + parent-resolution fix) → T8 (paper gate v2 on PORT-emitted DOT) → T9 (border-point wiring, T5 spec unchanged) → T10 (family sweep + close). Hard bars carry over; a SIXTH attempt would need fresh sign-off. | Follow-up list from the T5 stop, executed in the ordered sequence the sign-off specified. | No |
| 2026-07-22 | T7 | a/p0 ancestor wrappers ported + parent-resolution fixed (innermost handle). Firing derived from existing `innerMarginLevels` (a: ===2, p0: defined) — NO new seam fields: jar's protection0/protection1/thereALinkFromOrToGroup1 provably reduce to that value in this port (no swimlanes, no USymbols.NODE composites; 307/307 cached DOTs pair p0/p1, 93/93 pair a/i, zero counterexamples). Nesting a→p0→main→[ee]→i→p1→children verified vs ClusterDotString.java:74-204 and cached jar DOTs. One synthetic unit-test expectation updated (layout.test.ts comp.y 45→50 — asserted pre-fix wrong behavior; not a 57-pin). Gates: 10201 tests, 268/268 parity, 59/59 svg-state pins, typecheck/lint/build clean. | Pre-change gates never covered wrappers (parity comparator only matches ^cluster\d+$; no pinned fixture nested a child under an eligible cluster) — blast radius contained as predicted. RISK carried to T9: border-point clusters can fire a/i with p0/p1 forced off (entityPositionsExceptNormal>0); innerMarginLevels is undefined there, so T9 needs a distinct boolean for that combination. | No |
| 2026-07-22 | T8 | Paper gate v2 PASS on PORT-simulated DOT: 42×101.72 / 126×104.72 / 289×358 all exact. a/i adjudication: thereALinkFromOrToGroup1 gates a+i independently of border-point status; p0/p1 forced off when entityPositionsExceptNormal>0 (ClusterDotString.java:91-204) — pesita AA gets a-only wrap (cluster15a, no p0, per cached jar DOT); bitaxo/kotagu get none. Edit list superseded: 10 items (T4 six + borderPointAncestorWrap field, handlesFor border-point branch with title-table on ee not main, member-placement early-exit, innerMarginLevels guard). Two would-be integration bugs pre-identified: unconditional main.setHtmlAttr and unguarded innerMarginLevels firing on needsZaentPoint clusters. | Gate now derives from what the port emits (T5 lesson applied); T9 authorized. | No |
