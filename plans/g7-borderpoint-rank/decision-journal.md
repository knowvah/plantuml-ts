# G7 Decision Journal

| Date | Task | Decision | Rationale | Flagged for review? |
|------|------|----------|-----------|---------------------|
| 2026-07-22 | — | Execution plan: strictly serial T1→T2 (batch 1), conditional T3, T4→T5, T6. One debugger agent per task, no parallelism (every task feeds the next). Branch `feat/g7-borderpoint-rank` off main. Orchestrator commits. | Brief mandates serial batches; parallelism.md autonomous-mode exception — plan journaled, not presented. | No |
| 2026-07-22 | T1+T2 | Verdict: USAGE DEFECT, not a dot-engine library defect — no issue 09 filed. Mechanism: the isolation matrix (7 cells: rank+i-wrapper, nested child cluster, parent cluster, non-border pseudo-node, individually and in the two compound combinations mirroring pesita/kotagu) shows dot-engine's raw layout output byte-exact vs. real dot 15.1.0 across real-dot/text-path/builder-path on every cell, including end-anchor confirmation against pesita's and kotagu's actual cached production DOT. batch-4 retry-3's misses must therefore originate in plantuml-ts's own `addClusters` wiring or the still-unwired `FrontierCalculator`/`materializeCluster` correction pass, not in the library. | Full matrix + ruled-out list + downstream-suspect statement appended as "Round 3" to `plans/g6-cluster-geometry/batch-4/withlabel-derivation.md` — attempt 4 can be paper-derived from that spec without re-measuring. | No |
| 2026-07-22 | T4 | Paper gate PASS: bitaxo C 42×101.72, pesita AA 126×104.72, kotagu CompositeState 289×358 all reproduce exactly from the adjudicated spec (real-dot raw initial + unmodified frontierCalculator + D4 sentinel exclusion). Edit list vs attempt 3 recorded in the derivation doc (6 items: titleTableEligible relaxation, D4 exclusion, Round-3 builder shapes, frontier wiring, child-first order, numeric regression assertion). | D3 gate satisfied; T5 authorized. | No |
| 2026-07-22 | T5 | ATTEMPT-4-MISS — PERMANENT STOP (README cond. 7, D3). bitaxo C exact (42×101.72); pesita AA 55×293.61 vs 126×104.72; kotagu CompositeState 248×398 vs 289×358. Full revert executed and verified (git status clean; 10192 tests, typecheck, lint, build all green post-revert, re-confirmed by orchestrator). No tuning attempted. | Mechanism (T5 diagnosis): pre-existing defect at `src/core/graph-layout-build.ts:154-159` — `addClusters` parent-resolution always nests a child cluster in the parent's OUTER subgraph handle, never inside an active protection wrapper; and jar's "a"/"p0" ancestor-wrapper mechanism (`ClusterDotString.java` protection0/thereALinkFromOrToGroup1) is unimplemented repo-wide. kotagu: jar wraps SubComposite as cluster12a{cluster12p0{cluster12}} (+8pt margin per layer, verified via dot -Txdot on cached svek-1.dot: 191×277→207×293→223×309); the port omits the pair so CompositeState raw initial is 244×412 vs jar 303×358. pesita: AA resolves as sibling to nasreq_auth\'s i/p1 wrapper instead of inside it. Ruled out: frontier math (hand-traced, reproduces wrong output from wrong input; bitaxo exact), D4 (titleTableHeight 28 confirmed), issue-08 naming (isolated repro clean), border-point cluster internal shape (jar-DOT-exact synthetic repro reproduces the same wrong 244×412). Fix path for a human-approved attempt 5: port the a/p0 ancestor-wrapper mechanism and make parent-resolution nest into the active wrapper handle FIRST, then re-attempt border-point wiring. T4\'s paper gate had a structural blind spot: it derived initial bboxes from the JAR\'s cached DOT (which contains the a/p0 wrappers), so it validated the frontier math but not the port\'s own DOT emission — a future paper gate must derive from the PORT\'s emitted DOT. | YES — fifth attempt requires human sign-off |
| 2026-07-22 | — | ATTEMPT 5 AUTHORIZED by human sign-off ("port the a/p0 mechanism first"). Mission reopened; batch 5 added: T7 (a/p0 port + parent-resolution fix) → T8 (paper gate v2 on PORT-emitted DOT) → T9 (border-point wiring, T5 spec unchanged) → T10 (family sweep + close). Hard bars carry over; a SIXTH attempt would need fresh sign-off. | Follow-up list from the T5 stop, executed in the ordered sequence the sign-off specified. | No |
| 2026-07-22 | T7 | a/p0 ancestor wrappers ported + parent-resolution fixed (innermost handle). Firing derived from existing `innerMarginLevels` (a: ===2, p0: defined) — NO new seam fields: jar's protection0/protection1/thereALinkFromOrToGroup1 provably reduce to that value in this port (no swimlanes, no USymbols.NODE composites; 307/307 cached DOTs pair p0/p1, 93/93 pair a/i, zero counterexamples). Nesting a→p0→main→[ee]→i→p1→children verified vs ClusterDotString.java:74-204 and cached jar DOTs. One synthetic unit-test expectation updated (layout.test.ts comp.y 45→50 — asserted pre-fix wrong behavior; not a 57-pin). Gates: 10201 tests, 268/268 parity, 59/59 svg-state pins, typecheck/lint/build clean. | Pre-change gates never covered wrappers (parity comparator only matches ^cluster\d+$; no pinned fixture nested a child under an eligible cluster) — blast radius contained as predicted. RISK carried to T9: border-point clusters can fire a/i with p0/p1 forced off (entityPositionsExceptNormal>0); innerMarginLevels is undefined there, so T9 needs a distinct boolean for that combination. | No |
| 2026-07-22 | T8 | Paper gate v2 PASS on PORT-simulated DOT: 42×101.72 / 126×104.72 / 289×358 all exact. a/i adjudication: thereALinkFromOrToGroup1 gates a+i independently of border-point status; p0/p1 forced off when entityPositionsExceptNormal>0 (ClusterDotString.java:91-204) — pesita AA gets a-only wrap (cluster15a, no p0, per cached jar DOT); bitaxo/kotagu get none. Edit list superseded: 10 items (T4 six + borderPointAncestorWrap field, handlesFor border-point branch with title-table on ee not main, member-placement early-exit, innerMarginLevels guard). Two would-be integration bugs pre-identified: unconditional main.setHtmlAttr and unguarded innerMarginLevels firing on needsZaentPoint clusters. | Gate now derives from what the port emits (T5 lesson applied); T9 authorized. | No |
| 2026-07-22 | T9 | ATTEMPT-5-MISS - STOP per hard bar; sixth attempt needs fresh sign-off. bitaxo exact; pesita 55x293.61 (byte-identical to the attempt-3/4 miss); kotagu 264x419. Full revert verified (tree clean, 10201 tests green). The T9 border-point wiring itself is CORRECT: captured-input replay shows the port's subgraph nesting tree letter-for-letter isomorphic to jar's cached DOT (cluster0>rank_0, cluster0ee>cluster1a>p0>main>i>p1). Two divergent intermediates, both pre-existing and outside the 10-item edit list: (1) state-composite-cluster.ts resolveClusterComposite feeds titleTableWidth: title.width (title text only, AA=18.72) where jar computes max(title/stereo, attribute-text) (ClusterHeader.java titleAndAttributeWidth; AA attr line "entry / set_timeout()" = 116.46) - corrupts the ee HTML label reservation; explains why the same 55x293.61 broke attempts 3-5. (2) state-composite-pass.ts:126-136 addLevelEdges never consumes Transition.direction: jar reverses [*] -up-> SubComposite to anchor->[*] before graphviz (kotagu svek-1.dot:24); the port emits the un-reversed edge - a corpus-wide gap, outside T9's write-set. | Paper gate v2 asserted titleTableWidth "already computed" without verifying it against the port - same lesson class as T4 (assert nothing about the port; verify). Both fixes are jar-faithful general mechanisms, not tuning; recommended as their own pre-tasks before a paper gate v3 + attempt 6. | YES - sixth attempt requires human sign-off |
| 2026-07-22 | - | ATTEMPT 6 AUTHORIZED by human sign-off ("fix both gaps first, then re-apply T9"). Batch 6: T11 (titleAndAttributeWidth port) -> T12 (Transition.direction edge reversal; write-set expansion to state-composite-pass.ts authorized) -> T13 (paper gate v3) -> T14 (re-apply T9 ten-item wiring) -> T15 (family sweep + close). Strictly serial: both fixes move DOT output and gates are frozen at every commit. A SEVENTH attempt needs fresh sign-off. | Ordered sequence from the T9 diagnosis, per sign-off. | No |
| 2026-07-22 | T11 | titleAndAttributeWidth ported (ClusterHeader.java:73-96): header w = max(mergeTB(stereo,title).w, attr.w), h = sum + MARGIN gate; fed to BOTH labelWidth/Height (unconditional) and titleTableWidth/Height (titleTableEligible). pesita AA labelWidth 18.72->116.4625 / labelHeight 14->28 (oracle cluster15ee WIDTH=116 HEIGHT=28 match); kotagu/bitaxo byte-identical. MECHANISM REFINEMENT of D4: jar GroupMakerState.java:145 hardcodes PortionShower.ALL whose getVisibleStereotypeLabels() returns emptyList - stereo term is ALWAYS 0 for state diagrams (not just the o-o sentinel); the port now hardcodes 0, verified vs AA oracle (height 28 not 42). Blast radius verified: 7 goldens clusters change value, none pinned; parity 268/268, pins 57/57, suite 10205 green. | Write-set deviation journaled as push-forward: new file state-composite-header.ts forced by the 500-line complexity hook (established split pattern, pure move); pass-types untouched. | No |
| 2026-07-22 | T12 | Transition.direction consumed: LEFT/UP invert the link at creation (CommandLinkStateCommon.java:203-206 via Link.getInv(), decor1/decor2 swapped, length untouched); RIGHT/DOWN no-op; bare <-- defaults LEFT. Port swaps from/to in addLevelEdges/sweepOrphanEdges and un-swaps routed points back to semantic order for TransitionGeo. kotagu edge now __zaent_SubComposite->__init_CompositeState, isomorphic to jar svek-1.dot:24. Parity comparator proven direction-blind (undirected degree sequence + minlen multiset) - why 268/268 passed pre-fix; still 268/268. 17 fixtures re-rank per jar; 0 of 57 pins contain reversing hints (grepped). Suite 10219 green. | DOCUMENTED DIVERGENCE flagged for T15/follow-up: reversed edges draw the mathematically identical curve traversed backward - not byte-identical to jar SVG path text (jar keeps DOT-native point order and swaps arrowhead decor side, SvekEdge.java:702-709). Matters only when the 17 fixtures become pin candidates. | No |
| 2026-07-22 | T13 | PAPER-GATE-MISS - STOP before code (mission cond. 6). bitaxo and kotagu exact on paper; pesita AA 152x293.61 vs 126x104.72. Mechanism: graphviz cycle-breaking DFS (dotgen/acyclic.c) roots at the first node encountered in DOT text. AA's pass has a 4-node cycle (Idle->__zaent_AA->Reanimate->Closing->Idle) closed by two T12-reversed edges; jar's svek DOT emits a cycle-closing edge as the first statement (Closing becomes DFS root) while the port's addNodes declares all node shapes before any edge (aa_ok_ex becomes root) - different back-edge choice, materially different ranks, >2.5x taller cluster. Origin: graph-layout-build.ts:64-99 (addNodes nodes-first) x state-composite-pass.ts edge push order. Ruled out: T14 wiring itself (bitaxo/kotagu exact, pesita width term exact once order corrected), dot-engine defect (real dot reproduces the order sensitivity; jar verbatim DOT byte-exact through dot-engine), T11/T12 values (probed exact). T8 10-item edit list unchanged and confirmed sufficient; new item 11 (out of scope): reconcile global node/edge declaration order with jar svek emission order for cyclic passes; item 12 minor DOT-shape note (anchor double-declaration, non-blocking). | Third consecutive pre-existing gap peeled; the paper gate held (defect caught before code this time). Emission-order fix has pin-risk (57 byte-exact pins depend on current order) - needs its own careful task + paper gate v4. | YES - awaiting sign-off for the emission-order task |
| 2026-07-22 | - | T16 AUTHORIZED by human sign-off ("port the emission order, then continue the sequence"). Sequence: T16 (jar svek emission order; pins-must-hold bar) -> T17 (paper gate v4) -> T14 (ten-item wiring) -> T15 (sweep + close). | Continuation of attempt 6 per sign-off; pins are jar oracles, so a faithful order port must keep them byte-exact - that IS the fidelity test. | No |
| 2026-07-22 | T16 | Jar svek emission order ported: DotStringFactory.createDotString:187-198 emits lines0 (length==1, i.e. minLen===0) edges FIRST, before node shapes/clusters; port adds firstEncounterOrder() reordering addNodes' declaration pass so minLen===0 edge endpoints (tail-then-head, first-edge-first) come first - minLen===0 is an exact pre-existing proxy for jar's lines0 bucket (queue="-" for LEFT/RIGHT, CommandLinkStateCommon.java:176-182). Edge registration order deliberately NOT changed (DFS-root selection proven dominant; avoids parity blast radius). pesita AA ranks now EXACT vs real dot on jar DOT (root __zaent_AA, Idle+Closing tied, Reanimate last; was fully mismatched). Residual documented: Cluster.getNodesOrderedTop up-only bump - empty on all targets and 59 pins. Gates: 10225 tests, 268/268 parity, 59/59 pins, clean. | Pins held without exception - the faithful-order hypothesis confirmed. Note for T17: derivation doc mentions a ~7px autonom-sizing residual on AA bbox - T17 must adjudicate it against the paper targets. | No |
| 2026-07-22 | T17 | PAPER-GATE-MISS (pesita only) - STOP before code. bitaxo 42x101.72 and kotagu 289x358 EXACT on paper; pesita AA 126x107.22 vs 126x104.72 (+2.5px height). The v3 ~7px autonom residual disproven (jar node dims substituted into live layout: bit-identical height). Real mechanism, fourth pre-existing gap: real-layout edge-label sizing - graph-layout-build.ts#addEdges never wires the FIXEDSIZE HTML-table label mechanism (svek-dot-emit.ts#labelTable / addClusters title path already use it) and state-composite-edge-label.ts#edgeLabelAttrs under-measures both newline-split and single-line label heights. Fix-and-reverify probe reproduces 126x104.72 EXACTLY. T8 ten items unchanged; NEW items 11/12 correct T8 Round-2 choices (rank-subgraph names __rank_${outerName}_${N} - never cluster-prefixed; ensureMinWidth uses ROUNDED titleTableWidth, oracle width=126 confirmed); item 13 = the edge-label fix, named, general-purpose, out of T14b write-set. | Fourth consecutive gap peeled by gate discipline; wiring spec itself repeatedly confirmed. Awaiting sign-off for the edge-label sizing task (T18) -> paper gate v5 -> T14b -> T15. | YES - awaiting sign-off |
| 2026-07-23 | - | T18 AUTHORIZED by human sign-off ("fix the edge-label sizing, then continue the sequence"). Sequence: T18 (edge-label FIXEDSIZE wiring + height measurement) -> T19 (paper gate v5) -> T14b (ten-item wiring + items 11/12) -> T15 (sweep + close). | Continuation of attempt 6; same pins/parity-must-hold bar as T11/T12/T16. | No |
| 2026-07-23 | T18 | STOP - stop condition 4 (size-backlog would widen); full revert executed by orchestrator, gates re-verified green (10225 tests). The fix itself is jar-EXACT per label: FIXEDSIZE table wiring in addEdges (mirroring addClusters' setHtmlAttr title path) + measureTransitionLabel splitting on the \n token with height lines===1 ? fontSize+1 : lines*fontSize - every pesita AA-pass label and two corpus spot-checks matched jar cached-DOT HEIGHT exactly (14->15 single-line, 14->28 two-line). 57 pins held; parity topology held. BUT 15 pre-existing size-backlog entries widen, all through the FIFTH pre-existing gap: state-composite-autonom.ts#buildPlainAutonomSpec's Math.max(geometry.width, result.width) never folds edge-label ink into geometry.width, so more-accurate label sizes shrink result.width and drift autonom composites further from jar. Six of the 15 are the exact fixtures G5/C1 named when the analogous arrow-font-13pt width fix was reverted (ledger §C1; 5 describe.skip TDD suites preserved in state-composite-pass.test.ts). T18's agent also re-derived and re-reverted that 13pt width finding (byte-exact vs jar textLength on 5 labels). | The autonom pass is adjacent to mission-excluded territory (stop cond. 9 insideAutonomPass); widening the backlog is forbidden; both label fixes (height + width) are blocked behind the same autonom gap. Scope decision belongs to the human. | YES - awaiting scope decision |
| 2026-07-23 | - | T20 AUTHORIZED by human sign-off ("fix the autonom aggregation, then continue the sequence"). Scope extension granted: state-composite-autonom.ts#buildPlainAutonomSpec ink aggregation is IN scope (distinct from the still-excluded insideAutonomPass relaxation). T20 = aggregation fix + re-apply the coupled label fixes (G5/C1 13pt arrow-font width + T18 FIXEDSIZE/height) as ONE task - G5 ledger proves partial landings regress; enable the 5 preserved describe.skip suites. Then T19 (paper gate v5) -> T14b -> T15. T18 agent resumed (holds both reverted fixes). | Coupled changes must land atomically; backlog expected to TIGHTEN once aggregation is jar-faithful. | No |
| 2026-07-23 | T20 | First pass STOPPED per hard bar and fully reverted (gates re-verified at baseline). External box-arithmetic fold (reconstructing ink corners from computeSvekResultGeometry's public output) fixed the 0.244904-family (6 fixtures) and improved pesita/jaxebo/mifuti, but widened beguxu-19 (full extension) or fomusu-59 (half extension) - 14 entries widen either way; 4th/5th confirmation that no geometric scale factor generalizes (G4 S4/S12/S13x3, G5/C1). MECHANISM INSIGHT: all failed attempts operated OUTSIDE the ink walk because layout-ink-extent.ts was out of every write-set; jar's SvekResult#calculateDimension walks real drawU calls where the edge label IS a TextBlock with a measured box (SvekEdge.java:953). S13's parked note prescribes exactly this. ORCHESTRATOR DECISION (push-forward, journaled): one attempt at the correct seam - computeSvekResultGeometry's walk folds each labeled TransitionGeo's label BOX at attachTransitionLabel's placement - write-set expanded to layout-ink-extent.ts; same revert-on-any-widening bar; failure returns to human. Consecutive-fix rule considered: prior failures were all approach A (external arithmetic); approach B (ink-walk box) is architecturally distinct and previously untried. | The T20 sign-off targets this aggregation; the correct seam has never been attempted. | No |
| 2026-07-23 | T20b | STOPPED per consecutive-fix rule (3rd attempt at the same failing check) and fully reverted; baseline re-verified (10225 tests, 268/268 parity, 57/57 pins). Approach B (label box folded inside the native ink walk at attachTransitionLabel's placement, gated labelInk param mirroring includeArrowheadInk) is VERIFIED CORRECT AND COMPLETE: self-loop fomusu-59 resolves for free (delta 0.000069), 0.244904-family fixed, pesita improves to 0.074 - identical numbers to external reconstruction, ruling out implementation location. SIXTH GAP EXPOSED: attachTransitionLabel's LABEL_PERP=12 places labels ~11px further from the edge centerline than jar's real SvekEdge placement for plain vertical short-label edges (jar ~1px); jar's real box is node-dominated so the error is invisible there, but the port's tighter ink frame crosses the boundary. 14 backlog entries widen either way. Jar-faithful label placement is spline-geometry-based (SvekEdge/LabelStrategy) - a substantial port, its own mission. | Consecutive-fix rule: T20 v1 (full extension), T20 v2 (half), T20b (ink walk) all fail the same backlog-widening check; the architecture problem is placement, not aggregation. Human input required: new placement mission, descope pesita from D3's bar (needs explicit amendment), or close G7. | YES - awaiting direction |
| 2026-07-23 | - | OPTION 1 AUTHORIZED by human sign-off: plan the label-placement mission (/plan-mission). G7 PAUSED pending that mission; on its completion resume at T19 (paper gate v5) -> T14b -> T15 per batch-6 table. | The verified-but-reverted stack (T18 FIXEDSIZE/height + 13pt width + T20b ink-walk aggregation) re-lands inside or immediately after the placement mission. | No |
| 2026-07-24 | - | G8 label-placement mission COMPLETE and merged into this branch (merge commit 7ef0134): attachTransitionLabel now consumes graphviz-returned labelX/labelY (LABEL_PERP retired to fallback), T18 FIXEDSIZE edge-labels + T20b ink walk landed, the insideAutonomPass guard relaxed, size-backlog re-tightened (widened=0, 33 improved, pesita-10 -> 0.000519). The edge-label sizing gap that caused T17's pesita +2.5px paper-gate miss is now closed on this tree. Also applied mid-resume (human directive): layout engine dependency renamed graphviz-ts@0.1.26072117 -> @knowvah/dot-engine@1.0.0 (commit 6db6548) -- verified byte-identical layout (full suite 10246 tests, 268/268 parity, 57 pins, harness widened=0 all unchanged); a true drop-in for the port's usage. Resuming at T19 (paper gate v5). | G7 unblocked per the option-1 sign-off; the two blockers (label placement + engine name) are cleared. T19 re-derives the 3 targets on the post-G8 tree, where pesita's +2.5px should now be exact. | No |
| 2026-07-24 | T15 | MISSION CLOSE — family sweep measure-only, no code. Border-point family = 22 stereotype-enumerated state fixtures (+ `[*]`-into-composite members like gojuja/kotagu the grep misses). Fresh `measure-state-size-deltas.ts`: 18/22 size-exact (delta 0), including all T14b spot-checks + kotagu (T14b win, absent=0); 4 residual pre-existing pinned backlog entries unchanged (bitaxo 0.138888, resido 0.138889, nijugi 0.5, pesita 0.000519 sub-pixel) — border-point boxes exact per T19/T14b, residual is a non-border-point node in another svek graph. BACKLOG: harness improved:0/widened:0 → nothing to tighten or remove (G8 re-tighten already folded the wins); nimana-36-veco708 left byte-identical; file unchanged. PINS: none qualified — no size-exact family fixture has a committed golden.svg (creation out of scope); count stays 57. CENSUS: unmoved (0 pins added, 0 floors moved). Gates: 10250 tests pass, 268/268 parity, 57 pins hold, typecheck/lint/build clean, widened=0. Status→COMPLETE. | Measure-don't-guess close-out: the only honest edits are the docs; backlog/pins already tight from G8. Follow-up (out of scope): generate svg-state goldens for the 18 size-exact family fixtures, then pin. | No |
