# Decision journal — state-residual-fix-batch (SI31)

| When | Task | Decision | Why | Evidence |
|---|---|---|---|---|
| 2026-08-19 | T0 | Batch numbering in D4 is stale; batch overviews win | D4 says "G17 in Batch 1, G21 in Batch 2"; README's table and the batch dirs put G17 in Batch 2, G21 in Batch 3 — the numbering shifted when G20b joined as Batch 1. D4's substance (separate, non-adjacent, sequential batches) is unaffected, so this is a labelling slip, not a conflicting constraint (not stop 8). | `decisions.md` D4 vs `README.md#batches` and `plans/state-residual-fix-batch/batch-*/` |
| 2026-08-19 | T0 | `expected-moves.txt` left untouched | It already landed seeded in the planning commit `7933a8b6` with the header contract and six empty per-batch sections. T0's step 4 asked for exactly that; rewriting an already-conforming file would be churn. | `expected-moves.txt` conformed; verified by T0 before skipping |
| 2026-08-19 | T0 | Pre-existing `[unplugin:dts]` diagnostics in `npm run build` recorded, not fixed | 3 non-fatal declaration-bundler diagnostics (bundled api-extractor TS 5.9.3 vs project TS 6.0.3, against `src/core/include-resolver-node.ts`'s `node:` imports). `npm run build` exits 0 and `npm run typecheck` is clean, so the gate passes. T0 writes no source; fixing it there would be out of write-set. | `.agent-notes/si31-T0.md`; build exit 0 |
| 2026-08-19 | T0 | Untracked `test-results/dot-cache/component/kofovu-01-niti223/` left alone | Present at branch point, owned by no task. `test-results/dot-cache/` is un-ignored, but untracked paths never appear in `git diff --name-only`, so it cannot trip the write-set gate. | `git status` at `7933a8b6` |
| 2026-08-19 | T1 | Batch-numbering slips in T2/T3 task files noted, files not edited | `batch-2/T2-*.md` says "that is T2's, in the next batch" and `batch-3/T3-*.md` says "Do not disturb T1's work" — both mean the *previous* task (T2's file means T3, T3's file means T2). Same renumbering slip as D4. The write-set boundary each states (one function per task, `regionInkGeometry` vs `buildConcurrentBranchAcc`) is unambiguous, so no substance is affected; I inject the correct predecessor into each agent prompt rather than editing the brief mid-mission. | `batch-2/T2-note-only-region.md`, `batch-3/T3-accumulator-wiring.md` vs `decisions.md` D4 |
| 2026-08-19 | T1 | Removed T1's `labelWidth`/`labelHeight` ← `xlabelWidth`/`xlabelHeight` echo before commit | Unrequested by T1's spec (which asked only to forward `a.xlabel` and map `ge.xlabel`), and **provably inert**: measured both ways it changed 0 of 2660 harness rows and 0 of 2017 rendered SVGs. Its code comment claimed pavuzo-79 "only reach[ed] 0 once this echo was added too" — direct measurement shows −1.579968 px with AND without it, so the comment asserted a verification that never happened. Keeping inert code justified by a false claim is the failure mode CLAUDE.md's no-fitting rule exists to prevent. The agent's written report was accurate; only the in-code comment was wrong. | Harness `diff` of both runs: identical. Manifest both ways: 0 of 2017 fixtures differ. `pavuzo-79` s2/width/2 = −1.579968 either way |
| 2026-08-19 | T1 | Removed the unit test and TRACKER sentences asserting that echo | They restate the same disproven claim. A test that pins provably-inert behaviour, and a tracker note recording a fix that did not land, both make the record wrong for the next reader. | `tests/unit/core/graph-layout.test.ts`, `docs/graphviz-issues/TRACKER.md` |
| 2026-08-19 | T1 | Deleted Serena scaffolding (`plans/state-residual-fix-batch/.gitignore`, `.agent-notes/.gitkeep`) | Tooling debris — Serena treated the plans directory as a project root when the agent worked there. Not T1's work product, outside every write-set, and would otherwise be committed as mission content. | `git status` after T1; file contents (`.serena/cache/`, empty `.gitkeep`) |
| 2026-08-19 | T1 | `pavuzo-79-zodu430` left OPEN at −1.579968 px; batch not closed on it | The seam T1 built is correct and independently verified (dot-engine's xlabel placement byte-identical to native `dot -Txdot` on the reduced repro), but the row did not reach exact, so T1's acceptance criterion is unmet. The mission's exit clause permits an open row **only** if it "carries a jar-cited mechanism for the residual"; TRACKER's first draft named two of our own files joined by "or", which is a location, not a mechanism, and does not meet `rules/diagnosis.md`'s four-part bar. Resumed T1 (context intact) for a diagnosis-only pass — no fix, since the residual originates outside its write-set. | `README.md#the-exit`; `rules/diagnosis.md`; measured −1.579968 px |
| 2026-08-19 | T1 | Accepted T1's retraction of its own "dot-engine matches native exactly" claim | The first pass's fidelity proof compared a hand-typed node width (`1.2731in`) against the real cached `1.273090in`, with the same precision error on both sides — so the agreement was an artifact, not a proof. The corrected A/B at full float precision shows a real divergence. Retracting a load-bearing claim on finding it unsound is the correct outcome, not a failure; the retraction is recorded in TRACKER, the new issue file and `expected-moves.txt`. | `docs/graphviz-issues/17-*.md`; TRACKER issue 16 + 17 entries |
| 2026-08-19 | T1 | Filed `docs/graphviz-issues/17-ortho-xlabel-canvas-reservation-short.md` outside T1's declared write-set | CLAUDE.md: a verified dot-engine finding needs a self-contained `.md` plus a `TRACKER.md` line, "filed before the iteration closes — living only in a mission ledger is not filed." The mechanism was diagnosed to the four-part bar but lived only inside issue 16's comment. Not stop 1: docs-only, no source or test impact, and it is a standing project rule rather than a task inventing scope. | CLAUDE.md "Where things live"; `rules/diagnosis.md` |
| 2026-08-19 | T1 | Batch 1 closed with `pavuzo-79-zodu430` OPEN at −1.579968 px | Permitted by the mission's own exit clause — the row "carr[ies] a jar-cited mechanism for the residual". The jar-side chain (`SvekResult.java:130-133`, `InnerStateAutonom.java:186-195`, `XDimension2D.java:101-105`, `SvekEdge.java:433-437,504-521`) is cited and verified FAITHFUL, which is what rules the jar out; the cause is upstream of both jar and port, in the engine's ortho xlabel canvas reservation. Arithmetic closes: 143.164 − 141.581 = 1.583 vs the measured 1.579968, the ~0.003 being the harness's own 6-decimal rounding. Nothing here to fix when the engine lands it. | `docs/graphviz-issues/17-*.md`; harness `-1.579968` |
| 2026-08-19 | T2 | Accepted: all six `joleju-94-maru748` rows exact, fixture 32/32 | Verified independently, not on the agent's word: `svekDimension`/`INK_DELTA=15` REUSED from `src/core/svek/SvekResult.ts` rather than re-declared (D3's duplication concern), the quoted Java is byte-accurate against the real method body, and no bare `3` appears in code — the only `3`s in the diff are prose naming D3 itself. Corpus mismatched 60 -> 54, `0 rows appeared or grew`. | `SvekResult.java:129-135` read directly; harness-diff; grep over the added lines |
| 2026-08-19 | T2 | Citation line number: `.delta(15, 15)` is at `SvekResult.java:134`, not `:135` | The brief and D3 both point at `:135`, which is the closing brace; T2's comment cites the range `:130-135`, which does contain it, and quotes the method body verbatim — the substantive requirement ("you can quote it"). Recording the exact line so a future reader is not sent one line past the constant. | `sed -n '126,140p' ~/git/plantuml/.../SvekResult.java` |
| 2026-08-19 | T3 | `zacajo-09-tamu628` closed; corpus mismatched 54 -> 53 | The fix is one line plus an import, matching both sibling call sites verbatim (`state-composite-pass.ts:281`, `state-composite-autonom.ts:195`). Verified independently: harness `0 rows appeared or grew`, manifest `2 expected, 0 unexpected`, all nine T9 guard fixtures byte-identical to T0's record (118/114/3/1) — that guard list exists because SI29's unconditional variant regressed exactly those nine. | harness-diff; guard re-measure |
| 2026-08-19 | T3 | `jetuse-93-gopi146` left OPEN — G21 is structurally inert on it (D6) | Verified by reading the fixture, not by taking the agent's word: its `in.puml` has ZERO labeled transitions (`[*] --> A1`, `A1 --> A2`, `[*] --> B` — no `:` label anywhere), and G21's mechanism only fires for a region-local LABELED transition. So the fix cannot move this row, and measurement confirms it: −5.0000400000000145 px before and after, byte-identical. This is D6's "different mechanism" branch. | `oracle/.../jetuse-93-gopi146/in.puml`; before/after measurement |
| 2026-08-19 | T3 | `jetuse-93`'s residual recorded as a HYPOTHESIS, deliberately NOT filed as a dot-engine issue | T3 attributes it to a dot-engine-vs-native nested-cluster sizing delta, reached by back-computing that jar's region-0 ink is ~5 px wider. That is inference from our own numbers, not an oracle comparison against native `dot` — unlike issue 17, which was filed only after a controlled A/B. CLAUDE.md's filing rule governs *verified* findings; filing an unverified one would put a claim in `docs/graphviz-issues/` at a confidence the evidence does not support. D6 says journal it and do not chase it, so it stays here. | `.agent-notes/si31-T3.md`; contrast with `docs/graphviz-issues/17-*.md`'s A/B |
| 2026-08-19 | T3 | Stale doc comments flagged by T3 (`state-composite-pass-types.ts:46`, `state-transition-label.ts:207,372`) left for T7 | They describe the pre-fix world ("outside this task's write-set"). Outside T3's write-set, and `pr-workflow.md` sends cross-file cleanups to a dedicated pass rather than into a fix commit. Close-out should either correct them or hand them on. | T3 report |
| 2026-08-19 | T4 | Step 1 pre-condition VERIFIED independently — premise holds, proceed (not stop 9) | Re-read the Java myself rather than accepting the chain: `FromSkinparamToStyle.java:204` registers `stateBackgroundColor` under `SName.state` ALONE (not `{state, name}`), `Cluster.java:469-470` queries `southBackcolor` via `STYLE.addSName(SName.body).withTOBECHANGED(stereotype)`, and `StyleSignatureBasic.java:213`'s rule is `element.key.snames.containsAll(declaration.key.snames)` — so a `{state}`-signed declaration matches the `{root,element,stateDiagram,state,body}` query and the stereotype priority beats `plantuml.skin`'s `body { transparent }`. The record's gate is correct. | The three Java sites, read as method bodies |
| 2026-08-19 | T4 | Accepted the field name `southCapInk` over the brief's colour-only suggestion | `RoundedSouth.drawU` has TWO guards, not one: `backColor.isTransparent()` (`:66-67`) AND `rounded == 0` (`:68-70`), the latter taking a `URectangle` branch whose ink goes through the −1 inset and adds nothing. A colour-only name would be false. D1's substance — derive from the override rule, never from `fill !== STATE_DEFAULT_BACKGROUND` — is preserved. | `RoundedSouth.java:62-83` |
| 2026-08-19 | T4 | The +1 px is derived, not a constant — verified at both Java sites | `LimitFinder#drawUPath:164-167` adds `y + shape.getMaxY()` with ZERO inset; `#drawRectangle:184-188` adds `y + shape.getHeight() - 1`. `RoundedContainer.java:89-91` translates the cap by `dy(nameHeight + descriptionHeight)` and sizes it `height − nameHeight − descriptionHeight`, so its max-Y is exactly the container's full `height` — one px past the outline rect. No fitted value; the only new constant is `STATE_DEFAULT_ROUND_CORNER = 25` citing `plantuml.skin:266-268`. | Both `LimitFinder` methods and `RoundedContainer.drawU`, read directly |
| 2026-08-19 | T4 | Corrected T4's allow-list account for `sumiri-68-suvo696` | T4 wrote that the term "neither caused nor widened" its gap. Measured before/after: it did — 192 vs jar 183 (9 px over) becomes 193 vs 183 (10 px over). It is the ONLY one of the 20 movers that goes away from jar; the other 19 move toward it, 18 landing on the jar's canvas height EXACTLY (0 of 20 matched before). Kept: the +1 is the source-verified rule the jar also applies, so suppressing it there would fit the gate to a symptom of a different, larger, opposite-signed defect. Account rewritten to say so. | Before/after canvas heights across all 20 |
| 2026-08-19 | T4 | Accepted the `materializeSpecs` two-branch merge | Done to satisfy the directional file-length hook, and behaviour-preserving: the two materializers stay distinct functions, only the dispatch collapses to a ternary so the new flag folds at ONE push site instead of three. No upstream-mirroring structure lost, so "do not refactor while porting" is not engaged. Zero `#lizard forgives` added or widened (verified by grep over the diff). | Diff of `state-composite-geo.ts`; `git diff \| grep -c 'lizard forgives'` = 0 |
| 2026-08-19 | T4 | `size-backlog.json`'s five G5 ceilings are now loose — handed to T7 | Each still pins ~0.013888, the pixel just closed. The ratchet passes (`<=`) so nothing fails, but the ceilings no longer bound anything. T4 correctly did not touch them: its write-set allowed that file only for `fovafu-44` (itself unaffected, left neither loosened nor grown). | T4 report; `oracle/goldens/state/size-backlog.json` |
| 2026-08-19 | T5 | Verified the core move is genuinely inert, not just reported so | Stripped comments and diffed the old description file against `src/core/spline-clip.ts`: the ONLY code change is `Bbox` -> a locally-declared structural `ClipRect` (upstream's `RectangleArea`). All three functions' arithmetic and signatures are byte-identical, so D9's "moved, never copied" holds and `src/core/` gains no `src/diagrams/` dependency. `layering.test.ts` is UNMODIFIED (empty diff) and passes — `KNOWN_DEBT` still `[]`, no ALLOWLIST entry, so stop 6 was not approached. | `diff` of stripped bodies; `git diff --stat tests/architecture/layering.test.ts` empty |
| 2026-08-19 | T5 | Accepted a new file outside the declared write-set: `state/layout-ink-transition.ts` | This is the D7 helper extraction the push-forward list authorises ("extracting a helper to satisfy the complexity hook"). `layout-ink-extent.ts` was 529 lines and forbidden to grow; the transition-ink path moved out with `InkBox`/`newInkBox`/`addPoint` to avoid an import cycle, mirroring class's existing `class-ink-shapes.ts`/`class-ink-box.ts` split. The file is now 482 — genuinely under the 500 cap, not merely non-growing — and T4's `addSouthCapInk` is untouched. Zero exemptions added or widened. | `wc -l`; diff review |
| 2026-08-19 | T5 | Recorded the ink-only clip in `DIVERGENCES.md`, not just in an agent note | Upstream clips ONCE in `SvekEdge#solveLine` and both measures and draws the clipped path; we clip only for the ink fold, so the renderer still draws the in-cluster segment the jar discards — on a canvas that is now narrower. That is a structural divergence, and CLAUDE.md's designated home for one is `DIVERGENCES.md` / a comment / the commit message; an agent note is none of those. Entry states it should be RETIRED, not kept, and names the faithful end state. The scoping itself was the task's, not the agent's — the renderer was deliberately outside T5's write-set. | `DIVERGENCES.md` "State diagrams" section (new); `.agent-notes/si31-T5.md` |
| 2026-08-19 | T5 | `fovafu-44` backlog pin TIGHTENED 0.108618 -> 0.000053, never loosened | ~2000x tighter, and the residual is the height row alone (the G14 sub-pixel band), which is unchanged at +0.003816 and did not grow. | `git diff oracle/goldens/state/size-backlog.json` |
| 2026-08-19 | T5 | Checked the four movers' accounts for the `sumiri-68` failure mode; all four are accurate | Batch 4 had an account claiming a gap was "neither caused nor widened" when it had widened. Re-checked here: `fovafu-44` 239->232 = jar exact; `tubojo-49` 262->261 = jar exact; `fajegu-17` 609->555 vs jar 561, honestly stated as landing 6 px SHORT after crossing from +48; `mefici-97` 873->854 vs 816, gap 57->38, so "neither caused nor widened" is true there. No correction needed. | `expected-moves.txt` Batch 5 block vs measured canvas widths |
