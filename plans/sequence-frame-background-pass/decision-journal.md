# Decision journal — sequence-frame-background-pass

Append a row for every non-trivial judgement call. "Non-trivial" means: if a
reasonable developer might have chosen differently, log it.

| Date | Task | Decision / finding | Rationale and evidence | Flag for review |
|---|---|---|---|---|
| 2026-08-28 | orch | Branch `feat/sequence-frame-background-pass` cut from `feat/sequence-participant-g-wrapper` @ `1aa15960`. | D7. | |
| 2026-08-28 | orch | Execution plan: B1 (T1 solo) -> B2 (T2–T5 parallel, disjoint write-sets) -> B3 (T6) -> B4 (T7) -> B5 (T8) -> T9 landing. No write-set collisions; every batch gated per README. | `rules/parallelism.md` autonomous exception: log the plan, do not present it. | |
| 2026-08-28 | T1 | Three citation drifts in the brief, corrected before dispatch, verified by `grep -n`. | `cornersize = 10` is `ComponentRoseGroupingHeader.java:64` (brief said :65); the `topRightBottomLeft(1, 30, 1, 15)` padding is `:76` (brief said :74); `theme.ts:347` reads `#666666`, not `#999999` as the brief states. Every `plantuml.skin` line in the README table verified EXACT (:102-103, :117-120, :124-128). The Display rule is exactly `GroupingTile.java:126-127`. | |
| 2026-08-28 | T1 | `theme.colors.frame` dark value (`theme.ts:347`) repointed to black alongside the light one, per D3/T1, although black frame lines in a dark theme are a readability loss. | D3 is locked and states the dark value "is this port's own construct and moves with it"; it has no upstream basis, and the oracle renders light. Flagged rather than silently diverged. | REVIEW |
| 2026-08-28 | T1 | **HALT before commit.** T1's code is complete and confined to its five declared files; the PLAN fails, on two independent defects. Full artefact in `findings/T1-HALT.md`. | (A) stop cond. 1+3: `tests/unit/measurer.test.ts:61,136` pin `colors.frame` to the exact values D3 corrects, and the file is in no task's write-set. D3's premise "appears only at `theme.ts:212,272,347`" is FALSE — measured 7 sites via `grep -rn "colors\.frame" src tests scripts`. (B) making the tab fields REQUIRED breaks `sequence-layout-events.ts:176` (T5's file) and `renderer.test.ts:716,734,751` (T6's), so T1's `typecheck exit 0` gate cannot go green until Batch 3. No fix exists inside T1's write-set. | HALT |
| 2026-08-28 | orch | **Halt resolved by the user.** D3 amended (real 7-site `colors.frame` census; `tests/unit/measurer.test.ts` added to T1's write-set). New **D9**: the three `renderer.test.ts` `FrameGeo` literals move T6 -> T5, and `npm run typecheck` becomes a per-BATCH gate for Batch 1 — T1 commits with exactly four `TS2739` errors, Batch 2 must clear them. | User chose both recommended options 2026-08-28. Keeps the locked required-field contract intact and T2–T5 parallel; cost is one commit that does not typecheck in isolation. Rejected alternatives recorded in D9. | |
| 2026-08-28 | T1 | **DONE** `8a13a6bc`. Batch 1 gate green: lint PASS, `vitest tests/unit` 11638 passed, build PASS. `typecheck` exit 2 with **exactly four** `TS2739` and no other error code — verified independently, matches D9 exactly. Commit touched exactly the six declared files. | Independently re-ran all four gates and `git diff --name-only HEAD~1` rather than trusting the agent's report. | |
| 2026-08-28 | B2 | Dispatched T2–T5 in parallel. Write-sets verified disjoint: T2 `command-grouping.ts`+2 tests; T3 `renderer-frame-blotter.ts`+test; T4 `renderer-frame-header.ts`+test; T5 `sequence-layout-events.ts`, `layout.test.ts`, `renderer.test.ts`. | `rules/parallelism.md` step 3: one writer per file. Only T5 moves rendered output. | |
| 2026-08-28 | B2 | **Hazard to check at the batch gate**: four agents commit to the SAME branch concurrently. A `git add -A`/`git commit -am` in any of them would sweep up another's in-progress files. Verify every commit's `git diff --name-only` against its declared write-set before adjudicating; the tree can still be correct while the commits are mis-attributed. | Not preventable from here once dispatched; detectable and correctable afterwards. Named now so it is not rediscovered as a mystery. | |
| 2026-08-28 | T3 | **DONE** `21fed60a`. Commit verified clean: exactly its two declared files, 374 insertions, nothing swept up. | `git diff --name-only HEAD~1 HEAD`. The concurrency hazard did NOT produce write contamination. | |
| 2026-08-28 | B2 | The concurrency hazard manifested as **measurement** contamination, not write contamination. T3 reported `typecheck exit 0` and one failing test — both readings were of a tree containing T4's and T5's UNCOMMITTED work. Its own commit is clean. Batch gates must therefore be re-run by the orchestrator on the settled tree; a task agent's gate reading in a parallel batch is not trustworthy on its own. | Observed directly: `git status` showed T4/T5 files dirty while T3 measured. | |
| 2026-08-28 | B2 | Golden stroke encoding measured: `kejoke-76-curu931/in.svg` emits `stroke:#000` (18x) and `stroke:#FFA` (6x, the Blotter bands) in STYLE attributes — never the literal token `black`. So `GROUP_LINE_COLOR='black'` must resolve through the colour pipeline before emission. T4's failing test expects `stroke="black"`; the test is wrong, its implementation is right. | `grep -oE 'stroke:#[0-9A-Fa-f]+' on the golden`. | |
| 2026-08-28 | scope | **Stop condition 6 is NOT pre-triggered.** Independently measured `kejoke-76-curu931`'s golden with ElementTree: `svg/g[1]` has **exactly 102** children; the **first 12 are all `rect`**; histogram `polygon 24, line 24, rect 22, g 16, text 10, path 6`. The 6 `path` are the six group tabs and 12 of the 22 `rect` are the 6 band+outline pairs — the README's arithmetic (96 − 6 + 12 = 102) reconciles. | Verified the brief's own measurement rather than inheriting it, per prior-observations §5. | |
| 2026-08-28 | T5 | **DONE** `6d66987c`, typecheck exit 0 (all four deferred `TS2739` cleared). Reported no existing `layout.test.ts` assertion encoded the old post-order, so none was changed — new coverage added as new describe blocks instead. | Verified commit scope: exactly its three declared files. The "changed no existing assertion" claim is the right shape — a contract update that silences an existing test needs justifying; adding new tests does not. | |
| 2026-08-28 | T4 | **DONE** `6cf7b8ba`. Independently reached the same `stroke:#000` finding I measured from the golden, by checking the jar oracle rather than trusting `GROUP_LINE_COLOR='black'`. Also cited `HColorSet.java:127-133` for `#e` -> `#EEEEEE` and `core/svg.ts:83-91` for `font-weight="700"`. | Commit scope verified: exactly its two declared files. | |
| 2026-08-28 | B2 | **Concurrency hazard did NOT bite.** All four commits verified with `git diff --name-only <sha>~1 <sha>`: each touched exactly its declared write-set, nothing swept up. Only measurement readings were contaminated (T3 and T4 both read a tree containing others' uncommitted work); the orchestrator re-runs gates on the settled tree regardless. | | |
| 2026-08-28 | T2 | **D10 issued** — `FrameEvent.branchColors?: (string｜undefined)[]`, write-set widened to `ast.ts` + `sequence-layout-events.ts`. Orchestrator judgement, not escalated: D5 already REQUIRES the per-else fallback (`GroupingTile.java:326-332`); T1's field list merely omitted a field needed to satisfy it, so adding it completes the contract rather than contradicting a locked decision. T1/T5 committed, so no concurrent writer. | Measured 8/1427 corpus fixtures use `else #colour` — in-corpus, so in scope per "the corpus is the work queue". `rules/parallelism.md`: interface change + call sites = one agent's unit. | |
| 2026-08-28 | gate | **The Batch 1 "changes no output" premise is FALSE.** `batch-1/overview.md` ("No adjudicator run — T1 changes no output") and `T1-frame-contract.md` Observability ("changes no rendered output, so it does not move the ratchet") are both wrong. T1 changed `colors.frame` `#999999`->`#000000`, and `renderer.ts` READS it at four live frame-drawing sites (`:243` tab fill, `:244` tab stroke, `:314` frame outline stroke, `:332` separator stroke). Every corpus fixture with a frame changed colour. | Verified by `git show 8a13a6bc -- src/core/theme.ts` plus the four reader sites. Batch 1 was marked done without an adjudicator run on this false premise. | REVIEW |
| 2026-08-28 | gate | **Batch 2 gate will run against `1aa15960`, NOT against Batch 2's parent `8a13a6bc`.** Adjudicating against the batch parent would exclude T1's own colour change from the measurement entirely. `1aa15960` is the mission base (predecessor tip), so the run captures T1–T5 cumulatively. Strictly more conservative, and it is the same ref T7 must use later for the combined landing. | Follows directly from the finding above; a gate that cannot see a change is not a gate. | |
| 2026-08-28 | T2 | **DONE** `0d4afc3b`, commit scope exactly its five declared files (3 original + 2 D10). Confirmed the COLORS index mapping by quoting `CommandGrouping.java:134-135`, and added a D10 end-to-end test that `else #eee` reaches `branchSeparators[0].backColorGeneral` after layout. | Verified with `git diff --name-only HEAD~1 HEAD`. | |
| 2026-08-28 | gate | **First adjudicator run was VOID — my invocation error, caught before it was reported as a result.** I passed `--base` and `--snapshot` together; `main()` (`scripts/sequence-ratchet-adjudicate.ts:552-556`) tests `--snapshot` FIRST and returns immediately, so the run wrote a live-side snapshot and **exited 0 having adjudicated nothing**. The 18-line log with no `JSON_BEGIN` block was the tell. Re-ran with `--base 1aa15960` alone. | The two USAGE forms are alternatives, not combinable. Exactly the prior-observations §2 shape: a clean exit code that measures nothing. Checked the output instead of trusting exit 0. | |
| 2026-08-28 | B2 GATE | **PASSED.** Four gates on the settled tree at `0d4afc3b`: typecheck exit 0, lint exit 0, `vitest tests/unit` 11680 passed, build exit 0. Adjudicator `--base 1aa15960`, 1141 fixtures: **artefact=1 substructure=0 regression=0 inconclusive=17 improved=34 unchanged=1089**. Invariant `regression=0` HOLDS. Σ weightedScore 1241626 -> 1240508, **delta -1118** (fell). | Ran against the mission base, not the batch parent, so T1's colour change is inside the measurement. | |
| 2026-08-28 | B2 GATE | **Skip count: 0.** 1141 adjudicated = 1141 dirs in `test-results/dot-cache/sequence/`. All 17 `inconclusive` error at BOTH refs — pre-existing, and critically **0 error at LIVE only**, i.e. this batch broke no fixture's rendering, and 0 at base only. | prior-observations §2 requires the skip count be reported, not implied. Computed from the JSON rather than the summary line. | |
| 2026-08-28 | B2 GATE | `unchanged=1089` does NOT mean output was unchanged — it means the METRIC could not see the change. Verified directly: our `kejoke` output now carries 12 `stroke="#000"` (T1's `#999999`->black reaching output), yet its verdict is `unchanged` because the comparator short-circuits at top-level childCount (96 vs 102) and a recolour alters no node/attribute COUNT, so the charged units are identical at both refs. | Probed through `renderFixtureSequence` + `DeterministicMeasurer` + explicit `fixtureIncludeStore()` (prior-observations §3), throwaway script reported skip count 0 (§2) and was deleted. Distinguishes "metric blind" from "code inert" — they look identical in the verdict column. | |
| 2026-08-28 | scope | **T6's arithmetic reconciles against live measurement.** Ours: 96 children, 12 `#000` strokes, 0 `#FFA`. Golden: 102 children, 18 `#000`, 6 `#FFA`. T6 adds 6 Blotter bands (`#FFA`) + 6 background-pass outlines (`#000`) = +12 rects, and drops 6 surplus `[cond]` texts: 96 + 12 - 6 = 102. Stop condition 6 remains untriggered. | | |
| 2026-08-28 | T2 | Residual gap, NOT closed by T2: `group Alpha [beta]` stores the title but DROPS the comment — `FrameEvent` has one label slot and no title/comment pair, so T2's own AC4 is only half met. Measured **2 of 1427** corpus fixtures use the form. Neither `kejoke` nor `pixopo` does, so it cannot affect stop condition 6. | Deliberately not fixed mid-batch: closing it needs a THIRD amendment to T1's contract (`ast.ts` + `command-grouping.ts` + `sequence-layout-events.ts`) for 2 fixtures, and T6 is the mission's measurable step. Hand to T8 as a filed spin-off with this mechanism. | REVIEW |
| 2026-08-28 | B3 | Dispatched T6 (sole task). Pre-dispatch, measured BOTH pinned fixtures live at `0d4afc3b` with an XML parser, so T6's targets are measurement rather than inherited claims: `pixopo` ours 20 `{g:2,rect:6,text:8,polygon:2,line:2}` vs gold 20 `{rect:6,g:2,text:7,polygon:2,line:2,path:1}`; `kejoke` ours 96 `{g:16,rect:16,text:16,polygon:24,line:24}` vs gold 102 `{rect:22,g:16,text:10,path:6,polygon:24,line:24}`. | The per-group arithmetic closes exactly: +1 band, +1 bg outline, tab rect->path (count-neutral), -1 surplus `[cond]` text. kejoke: rect 16+12-6=22, text 16-6=10, path 0+6=6, total 96+6=102. | |
| 2026-08-28 | B3 | **`pixopo` needs BOTH halves of the change simultaneously.** Its count already matches at 20/20, so the background rect ALONE would make it 21/20 — introducing a top-level short-circuit that does not exist today and leaving it `inconclusive`, i.e. still blocking. Only dropping the surplus `[cond]` text alongside returns it to 20. This is why D4 puts the header's FOREGROUND half in scope, and it is stated explicitly in T6's prompt so the agent cannot land half the change and read the count as progress. | Derived from the live histograms above, not from the brief. | |
| 2026-08-28 | T6 | **DONE** `0c7abcb7`. Commit scope exactly its two declared files; `renderer.ts` 499 lines (under the 500 hook cap). Self-corrected mid-task: its first attempt exported a constant from `renderer-frame-header.ts` (outside its write-set), then reverted that file and used a module-local `FRAME_ROUND_CORNER` instead — the right call, and it reported it rather than hiding it. | Verified with `git diff --name-only HEAD~1 HEAD` and a clean `git status`. Its throwaway census scripts were genuinely deleted (stale IDE diagnostics referenced them after removal). | |
| 2026-08-28 | T6 | **STOP CONDITION 6: NOT TRIGGERED — verified independently, not taken from the agent's report.** Re-rendered both fixtures through `renderFixtureSequence` + `DeterministicMeasurer` + explicit `fixtureIncludeStore()` (skip count 0) and parsed with ElementTree: `pixopo` 20 children, first child `rect`, histogram `{rect:6,g:2,text:7,polygon:2,line:2,path:1}` — **identical to the golden's**. `kejoke` 102 children, **first twelve all `rect`**, histogram `{rect:22,g:16,text:10,path:6,polygon:24,line:24}` — **identical to the golden's**. | Both fixtures now match their goldens element-for-element in count AND histogram; pixopo's tag ORDER matches too. The predicted arithmetic (+1 band, +1 outline, rect->path, -1 text per group) held exactly. | |
| 2026-08-28 | B3 GATE | Four gates on the settled tree at `0c7abcb7`: typecheck exit 0, lint exit 0, `vitest tests/unit` 11683 passed, build exit 0. Adjudicator running against `1aa15960`. | | |
| 2026-08-28 | B3 GATE | **INVARIANT VIOLATED — `regression=10`.** Adjudicator at `0c7abcb7` vs `1aa15960`, 1141 fixtures: artefact=62 substructure=15 **regression=10** inconclusive=29 improved=77 unchanged=948. Sigma weightedScore 1241626 -> 1223870, **delta -17756** (large net improvement). Batch 3 does NOT pass its gate as written; the regressions go to T7 for diagnosis (stop condition 4 requires a full artefact, not a halt on effort). | Every one of the 10 has a child-count distance that ROSE, i.e. our count moved FURTHER from the golden — not a metric artefact. Worst: `sojufi-84-bexi933` dist 2->8, `cusiro-03-mebe823` 2->5. | HALT-PENDING-T7 |
| 2026-08-28 | B3 GATE | **12 unadjudicated rises alongside the 10 regressions.** Of 29 inconclusive: 17 error at BOTH refs (pre-existing `!include`), **0 error at LIVE only — this mission broke no fixture's rendering** — and 12 that render at both refs with NO top-level childCount record yet scores that rose hard (`menire-56-rase375` 176->452, `rugeco-70-muro754` 277->543). D8 blocks on unadjudicated rises exactly as it blocks on regressions, so these are equally load-bearing for T9. | Computed from the JSON, not the summary line, which reports only totals. | |
| 2026-08-28 | T7 | Dispatched to `debugger` at three refs: `0d4afc3b` (T6 isolated), `1aa15960` (combined landing), `e10c88e9`/main (full two-mission stack). The third is required because the predecessor's CLOSE-OUT **enumerates only 4** of its 10 blocking fixtures (`pixopo`, `kejoke`, `luzapi`, `celego`); its `inconclusive=27` against `main` = 17 include-errors + the 10, so the other 6 are recoverable only by measurement. | T7's task doc says "recover the rest from the predecessor's close-out" — they are not there. Verified by extracting every slug in that file: 4 unique. | |
| 2026-08-28 | T7 | **All three adjudicator runs completed; skip count 17 at every ref, identical on both sides of every comparison.** At `0c7abcb7`: vs `0d4afc3b` artefact=61 substructure=16 regression=10 inconclusive=29 improved=77 unchanged=948, Sigma 1240508 -> 1223870; vs `1aa15960` artefact=62 substructure=15 regression=10 inconclusive=29 improved=77 unchanged=948, Sigma 1241626 -> 1223870; vs `main` artefact=62 substructure=423 regression=10 inconclusive=29 improved=617 unchanged=0, Sigma 1291577 -> 1223870. The regression set and its child distances are IDENTICAL against `0d4afc3b` and `1aa15960`, so all 10 are wholly T6's effect and nothing the predecessor did contributes. | Full census in `findings/adjudication.md`. Computed from the JSON block, not the summary line. | |
| 2026-08-28 | T7 | **Correction: the 17 skipped fixtures are NOT `!include` failures.** Measured through `render-fixture-sequence.ts`: every one is a parser refusal (`sequence refused this source at line N (syntax)`), and only 1 of the 17 (`nuvoja-46-dezu541`) contains an `!include` at all. They are unported sequence syntax — e.g. `dolice-60-copi767`'s inline `[[url]]` on a message. Pinned identically at `main`, `1aa15960` and here. The claim propagated from the predecessor's close-out through two briefs unchecked. | prior-observations §5: verify a ledger claim before acting on it. Repeated in three places before it was measured once. | |
| 2026-08-28 | T7 | **The predecessor's 10 blocking fixtures, recovered by measurement**: `bifuzu-54-gefo004 murori-42-pabo946 panopa-36-muti317 pixopo-04-zitu732 rotila-89-juri637 tusoga-11-gabe405 vesaze-51-ceji187 xegufe-25-paji965 xujoga-62-lacu610 xupozu-13-numi050`. **All 10 are now `improved`** at every ref (e.g. `panopa` 874 -> 385, `pixopo` 307 -> 133). Of the four the close-out names, only `pixopo-04-zitu732` is a member — `kejoke`, `luzapi` and `celego` are evidence fixtures in its narrative, not blockers. | Joined `main`'s snapshot to `1aa15960`'s and re-ran `classify`: improved=557, rose=567, error-at-one-ref=17, and exactly 10 rises carry a null childDistance. Reproduces the close-out's own `27 = 17 + 10` and `567 = 557 + 10`. | |
| 2026-08-28 | T7 | **Root cause of 8 of the 10 regressions AND 11 of the 12 rising inconclusives: one mechanism.** T6's `renderEvent` (`renderer.ts:357-360`) routes EVERY `FrameGeo` into the `ComponentRoseGroupingHeader` port, `frameType` included. Upstream builds a `ref` as a `ReferenceTile` (`TileBuilder.java:174-176`), NOT a `GroupingTile`; its component `ComponentRoseReference` declares only `drawInternalU` (`:83`), so `AbstractComponent#drawU:143-147` runs the EMPTY inherited `drawBackgroundInternalU` (`:139-140`) for it, and `ReferenceTile#drawU:127-136` owns no `Blotter`. A `ref` therefore drew a background outline the jar never draws, a `[label]` comment the jar never draws (`ComponentRoseReference.java:67-78` splits the display at index 1, so the label is BODY text), and its corner before its body rect (`:99` rect, `:118` corner — the reverse of the grouping header). | Ruled out, each with evidence, in `findings/adjudication.md` §3a: Blotter over-emission (sojufi emits exactly 2 bands for its one coloured alt, matching the golden; the surplus 4 equals its ref count); D6 geometry (the only non-size diff record is `svg/g[1][childCount]`); the predecessor's g-wrapper (identical numbers at both base refs); `ElseTile`/`NewpageTile` (`grep -rn "isBackground()"` — `ElseTile:125` is commented out, `NewpageTile:81` early-returns). | |
| 2026-08-28 | T7 | **`fix(T7)` applied at the origin, two files, both in T7's declared write-set** (`renderer-frame-header.ts` = T4's, `sequence-layout-events.ts` = T5's). A `ref` now draws nothing in the background half, carries no tab comment, and emits rect-then-corner. `renderer.ts` was NOT touched: it sits at exactly 499 of the hook's 500-line cap, and the suppression belongs in the component module anyway — upstream's suppression happens inside `AbstractComponent#drawU`, not in the tile. The `Blotter` is deliberately left unguarded: a `ref` never carries a colour out of `command-misc.ts:198,219`, so a band for one cannot occur and `code-principles.md` forbids the guard. | Result vs `1aa15960`: regression 10 -> 2, inconclusive 29 -> 18, improved 77 -> 97, Sigma 1241626 -> 1219837 (delta -21789 vs -17756). All four gates green: typecheck 0, lint 0, `vitest tests/unit` 11683 passed, build 0. | |
| 2026-08-28 | T7 | **Structural divergence, filed for T8, not fixed here.** `ComponentRoseGroupingHeader` and `ComponentRoseReference` are two upstream classes now sharing one port module, distinguished by a `frameType === 'ref'` branch. The faithful structure is a separate `renderer-frame-reference.ts` carrying `ComponentRoseReference`'s own `xMargin`/`heightFooter`/`symbolContextHeader` geometry. That is a NEW file, outside T7's exhaustive write-set (stop condition 1), so it is filed rather than taken. | CLAUDE.md: "a structural divergence IS the bug". Documented in both edited functions' doc comments so the next reader finds it. | REVIEW |
| 2026-08-28 | T7 | **The 12 rising `inconclusive`s: measured, not guessed.** In all 12 it is `baseChildDistance` that is null, and the reason is "the counts MATCHED", not "it short-circuited higher up" — our top-level child count equalled the golden's exactly at `0d4afc3b` for every one (17=17, 26=26, 23=23, 25=25, 29=29, 31=31, 21=21, …), corroborated by base diff lists of 111-177 records, which only a descent can produce. 11 were a REAL loss (the +2 per ref of the mechanism above; `xupufa-46-romu811` has two refs and rose +4) and are now `improved`, most below their pre-mission score. | Also disproves a lead carried in the ledger: `ficiru`/`mupugi`/`xojavo` are NOT "body text parsed but never rendered" — the body text is emitted at both refs (`cekora`'s `short`, `mutuve`'s `v1` appear as the trailing `<text>`); they are ordinary members of the ref set. Stale note; do not carry forward. | |
| 2026-08-28 | T7 | **Three rises survive the fix and are adjudicated BENIGN, with evidence: `fobube-11-nifo424`, `junaxa-14-biko373` (both `regression`), `rugeco-70-muro754` (`inconclusive`).** T6 adds exactly ONE node to each — the frame's background outline `<rect>`, which the golden also carries at the same top-level index. Longest-common-prefix against the golden (an instrument independent of the charge) jumps 0->17, 1->8 and 0->7; for `fobube` and `rugeco` the LCS now equals the golden's FULL length, i.e. the golden's whole top-level sequence is a subsequence of ours. `classify` cannot see this: `artefact` needs the distance to fall and `isSubstructureRise` (`:214-221`) short-circuits on `live.childDistance > base.childDistance` before its arithmetic runs — and adding nodes to a document already larger than the golden is this mission's entire purpose. Residual surplus is pre-existing: `fobube` unported `newpage` pagination (surplus 6 before T6), `junaxa` actor/database glyph decomposition + top-level title text (5 before), `rugeco` one surplus top-level `<g>` for 3 participants (whose base 31=31 match was a COINCIDENCE — the surplus `<g>` cancelled the missing background `rect`). | Not "cleared" by editing the instrument: that would be fitting the classifier to the result, which is what the classifier exists to prevent. | |
| 2026-08-28 | T7 | **Bottom line for T9: the classifier still reports `regression=2` plus one non-error `inconclusive` rise, so a LITERAL reading of D8 blocks the re-pin — but there is no UNADJUDICATED rise left in the 1141.** D8 is locked and orchestrator-only, so T7 does not decide it. If the orchestrator accepts the three adjudications above, T9 may re-pin; if not, the narrowest genuine unblock is closing `rugeco`'s one surplus top-level `<g>`, which is a separate mission. Stop condition 6 re-verified on the fixed tree: `pixopo` 20/20 and `kejoke` 102/102, LCP = LCS = length in both. | Final numbers vs `1aa15960`: artefact=63 substructure=13 regression=2 inconclusive=18 improved=97 unchanged=948, Sigma 1241626 -> 1219837. Vs `main`: artefact=63 substructure=425 regression=2 inconclusive=18 improved=633, Sigma 1291577 -> 1219837 (delta -71740). | REVIEW |
