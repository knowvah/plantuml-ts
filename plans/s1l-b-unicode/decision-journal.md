# Decision journal — S1L-b-unicode

Append one row per non-trivial judgment call during execution (per
`~/.claude/rules/autonomous-execution.md`). Empty at mission start.

| When | Task | Decision | Why |
|------|------|----------|-----|
| 07-27 | T1 | Only `maxLineWidth` decodes per-line in the sizer, NOT `textBlockHeight` (task named both). | Height is fixed purely by `finalizeDisplay` preserving the raw token: `lineCount`/`textBlockHeight` split the raw display, so `<U+000A>` counts as 1 line automatically. HR classification (`isCreoleHrLine`) must stay on the RAW line — upstream classifies stripes BEFORE `AtomText` decode — so decoding inside `textBlockHeight` would be *wrong*. Only width (`maxLineWidth`) needs the decode, for output-neutrality of non-newline glyphs (`<U+221E>`) and to give inline `\n` ~0 width. Renderer decodes in `buildLine` AFTER `classifyStripeLine`, matching. |
| 07-27 | T1 | Write-set expanded to `tests/unit/description/parser.test.ts` (2 tests + block header) — journaled. | The two I4c tests asserted the OLD parse-time-decode contract (`display` decoded). ADR-1's interface contract explicitly reverses this: `node.display` PRESERVES the raw token, decode moves to per-line measure/render. Updating tests that encode the behavior being deliberately changed is a required consequence, not scope creep. Not in any other task's write-set. Stereotype test (2222) unchanged — stereotype path still decodes at parse (T1 left it alone). |
| 07-27 | T1 | Write-set expanded to `oracle/goldens/description/size-backlog.json` — re-pinned gafico 5.680208→3.752777, nujito 3.350521→3.122049. | Batch-1 exit bar: "the 3 targets' HEIGHT components shrink (their pins drop)". The shrink-only ratchet must lock the improvement immediately, else a future regression up to 5.68 goes undetected. lurupu unchanged (its residual is emoji width → T3). junoxu untouched. Measured via `measure-description-size-deltas.ts` (same comparator the ratchet asserts with). |
| 07-27 | T1 | Blast-radius check: junoxu-15 (`<U+00B5>` in a cluster title / DOT label, OUTSIDE the leaf-sizer write-set) verified `unchanged`. | The pre-implementation worry was that the cluster-title path (`measureTitleLabel`, `layout-dot-tree.ts` `cluster.label`) relied on `finalizeDisplay`'s decode and would widen. Empirically it did not — the DOT engine uses explicit dims and the cluster render goes through `buildTextBlock` (now decoding). ADR-1's scoped 3-file fix is sufficient; no extension to `title-label-sizing.ts` needed. |
| 07-27 | **T2** | **decisions.md Rule 2 / ADR-2 premise is CONTRADICTED by evidence** — the quoted `node "$var"` title (node a) is NOT rendered literally; it is creole-processed (`<u:blue>ccc` blue, `<color:green>ddd` green, `<U+000A>` decoded inline), oracle box 2.145×0.611. Instrumented: probe of the oracle SVG (`oracle/corpus-cache/component/gafico-37-cuma657/input.svg`) + per-node DOT dims (`dot-sync-report --slug`). | Planning mis-attributed node **c**'s `<code>` block (the 7.857in `font-family="monospace"` literal `<text>`, `textLength=640.582`) to node a's quoted title. The 7.857in literal is node c (`[<code>$var</code>]`), one line; node a is a separate creole-processed one-line box. Rigor per `rules/diagnosis.md`: mapping confirmed by rank-chain pairing (sh0002/3/4 ↔ sh0006/7/8 = creation order a/b/c) and by SVG content (only node b has "on multi-line context"; only node c is monospace-literal). |
| 07-27 | T2 | gafico's 3.7528in pin is driven by node **c** = the **unported `<code>` creole block** (E2r L2, deferred — `EntityImageDescriptionSupport.ts:393`, and `grep src/core/klimt/creole/` finds no code-block command), NOT quoted-title literalness. → **documented residual** (ADR-2 "document" branch), pin kept at 3.752777, ledger updated. | The real driver is out of T2's "quoted-title" scope AND explicitly deferred. Fixing node a (the quoted title) can't move the pin (node c 3.753 > node a 1.96). No code change → zero regression risk. This is the mission's pre-sanctioned "documented residual = success" outcome; taking it corrects Rule 2 rather than acting on the false premise. Surfaced to the user (Rule 2 correction). |
| 07-27 | **T3** | **ADR-3 premise (emoji glyph width) is CONTRADICTED by evidence** — emoji width is already correct: `Implement` (😁🚀) and `foo` (∞) nodes match the oracle EXACTLY (`WidthTableMeasurer.charWidth` already returns the jar-verified 16-pt astral fallback, `measurer.ts:180`). lurupu's 2.0459in pin is driven by node **bar**. | Instrumented port-vs-oracle per-node dims (probe) + sizer/renderer visible-text (`parseCreole` vs `buildStripeAtoms`). Only `bar` diverges (333px vs oracle 147px). No measurer change is warranted; a fixed emoji fallback already exists and is correct. |
| 07-27 | T3 | lurupu's driver is a **sizer↔renderer creole-lexer divergence**: the SIZER's `parseCreole` (`core/creole.ts`) does NOT strip an unclosed `<b>` or the space-form `<font Segoe UI Emoji>`; the RENDERER's `buildStripeAtoms` (E2r StripeSimple) DOES (probed: renderer visible = "this is also ∞ 🚀☺ long", 22 cps; sizer = 53 cps incl. literal tags). → **documented residual** (ADR-3 "document" branch), pin kept at 2.045912; flagged a scoped follow-on ("sizer↔renderer creole visible-text unification"). | The real fix is in `leaf-sizing.ts`/`creole.ts`, OUTSIDE T3's `measurer.ts` write-set, and high blast radius (re-measures every description leaf via `buildStripeAtoms`) — a STOP-worthy "risk other fixtures" change. Mission pre-sanctioned lurupu as a documented residual. Second ADR premise (after Rule 2) mis-diagnosed by planning — pattern surfaced to the user. |
