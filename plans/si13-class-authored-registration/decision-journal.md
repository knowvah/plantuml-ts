# SI13 Decision Journal

| Date | Task | Decision / Finding | Why | Flagged for review? |
|------|------|--------------------|-----|---------------------|
| 2026-08-04 | plan | Planning phases 2–6 auto-confirmed under the user's "start on mission SI13" directive; scope fixed by the SI13 mission-index row. ADRs 1–4 flagged for review. | Autonomous session; SI15 precedent. | yes |
| 2026-08-04 | T1 | Agent's execution falsified ADR-1's premise: `tests/visual/data/class.json` EXISTS (768 entries), so class merges rather than taking the authored-only branch. 310/314 flat-root slugs collide (the ratchet's corpus goldens share the root); 302 byte-identical, 8 differ by exactly the manifest's `!pragma layout smetana` line. The 8 are BENIGN — survey/dot-cache always measured manifest markup (with pragma), ratchet renders golden markup (without); each pipeline internally consistent. ADR-1 amended with the finding. | Verified against the code/data before repeating (both the agent and the orchestrator re-measured). | no |
| 2026-08-04 | orch | Refined `mergeFixtures`' collision warning post-agent: identical-markup collisions count-only, DIFFERING-markup collisions named. Without it, every class run prints a 310-slug line and the one dangerous case drowns. One agent test asserting the old always-name behavior was rewritten to assert the new split (it was asserting the noise, not a behavior anyone wants). Same-file edit inside T1's write-set. | Warning exists to catch drift; 310 by-design collisions made it unreadable. | no |
| 2026-08-04 | T2 | Pipeline run clean end-to-end: `dot-sync-report class` detected exactly the 4 missing canonicals per-slug and regenerated via jar (stop-6 did NOT fire — 0 silent skips; 55 skips named, incl. `class-actor-bare-no-allowmixing` "not tagged CLASS" because the jar REFUSES that input, an error-page canonical — permanently ineligible via this pipeline BY DESIGN, guarded by its dedicated pinned test). Aggregate: 710 CLASS fixtures, 100% DOT-EQUAL, incl. the three analysable authored fixtures. | Live verification the SI9 ADR-2 fix covers class. | no |
| 2026-08-04 | T2 | ADR-2 drift breakdown (old 718 rows 2026-07-18 → new 721 rows): ZERO dotEqual flips either direction, ZERO verdict downgrades → no stop. 75 verdict upgrades (conformant 271→308, structural-match 63→88, diverged 384→325); 93 maxDelta moved (77 shrank, 16 grew — all 16 within already-diverged fixtures, none ratcheted, named in the scratchpad analysis; measurement drift from 17 days of src commits incl. SI10/SI14/SI15, relevant to A2s not this mission). | ADR-2's mechanical rule: improvements proceed with breakdown. | no |
| 2026-08-04 | T2 | Eligibility (ADR-3), measured: all 4 surveyed authored fixtures dotEqual TRUE. `class-missing-label-URL-SVG-0` — conformant AND already ratcheted (2026-07-16, source dot-cache): its "unknown state" resolves to already-guarded, nothing to add. The three others: structural-match, maxDelta 1 (the pre-existing 1px viewBox/width gap) → fail AC1, not added. `actor-bare`: no parity row possible (jar refuses). **Zero ratchet additions — the expected honest outcome; eligibility now turns solely on AC1, no longer structurally impossible.** | ADR-3 executed. | no |
| 2026-08-04 | T2/orch | Write-set amendment: `test-results/dot-cache/` is COMMITTED oracle data (.gitignore negates it; 2,697 tracked class files), not the gitignored local cache the brief assumed from the SI9 row's "LOCAL cache" wording. The three new authored cache dirs (in.puml == golden, jar svek DOT + svg) are the registration's oracle entries and land in T2's commit. | Same own-unreviewed-brief shape as SI15; the dirs are the deliverable's data. | yes |
| 2026-08-04 | plan | ADR-2 tightens SI9's drift precedent into a mechanical rule: regressions (dotEqual true→false, verdict downgrades) STOP; improvements proceed with the full breakdown journaled. Drift is EXPECTED — parity-class.json predates SI10/SI14/SI15. | SI9's maintainer ruling generalized to a decidable condition. | yes |
