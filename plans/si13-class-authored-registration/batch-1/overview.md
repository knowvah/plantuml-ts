# Batch 1 — enumeration layout (single task)

| ID | Description | Agent | Writes | Depends On | Done |
|----|-------------|-------|--------|------------|------|
| T1 | Per-type golden layout in `dot-sync-fixtures.ts` (class → flat `svg-class` root) + tests | typescript-pro | `scripts/dot-sync-fixtures.ts`, `tests/unit/scripts/dot-sync-fixtures.test.ts` | — | [ ] |

Gates exit 0 at batch close; no committed data changes in this batch.
