# Findings

`audit-table.md` is written by T1 (columns: path · sink `file:line` ·
escaped-where · verdict). `oracles/<name>/{in.puml,jar.svg}` holds the jar
renders T1 produces with `scripts/oracle-render.sh`; tests quote their bytes.
