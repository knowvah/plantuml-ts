# Batch 1 — stop the denominator lying

One task, and nothing else may start until it lands. Four of the 79 pinned
misroutes are not defects: the **jar** produced an error page for them. Every
later batch is scored against the remaining 75, so that split has to exist
before any repair is measured.

Nothing in this batch changes any rendered byte — it changes how the gate
classifies a fixture whose golden is an upstream error image.

| ID | Description | Agent | Writes | Depends On | Done |
|----|---|---|---|---|---|
| T1 | [Jar-error classification](T1-jar-error-classification.md) | typescript-pro | `tests/oracle/svg-conformance/routing-conformance.test.ts`, `oracle/goldens/svg-conformance/routing-baseline.json` | — | [x] |
