# Decision journal

Appended during execution. One row per non-trivial judgment call — if a
reasonable developer might have chosen differently, it belongs here.

| When | Task | Decision | Why | Evidence |
|---|---|---|---|---|
| 2026-08-24 | orchestration | Batch 6 run SEQUENTIALLY (T9 then T8), not in parallel as `batch-6/overview.md` states. | Their write-sets are NOT disjoint: T9's includes `decision-journal.md`, and T8's spec requires enumerating its file list in that same journal before writing any baseline — a same-file write conflict under `~/.claude/rules/parallelism.md`. Independently, T8's re-pin of `routing-baseline.json` cannot be correct until T9's outcome is known: `nuvoja-46-dezu541` becomes `agree` if T9 fixes it and stays `known-misroute` if T9 stops. Running them concurrently would have raced the journal and pinned a fixture whose status was still undetermined. | Both specs read directly; the conflict is in the task files themselves, not inferred. |
| 2026-08-24 | T8 | T8's AC1 as written ('0 known-misroute, 4 jar-error') is NOT achievable and has been restated for the agent. | Two corrections, both already recorded upstream in this journal: jar-error is **8**, not 4 (T1's re-measurement over all 3158 goldens); and known-misroute cannot reach 0 because `component/kokebo-27-vafi688` is a confirmed parse-attempt stop condition that no repair in this mission can move. The achievable target is 8 jar-error, 1 or 2 known-misroute depending on T9, rest agree. | T1 and T4 journal rows above; verified in the Java at T4 close. |
| 2026-08-24 | T6/T7 | Batch 5 closed: SLI 7 -> 2, verified independently at the committed budget (8.97 s, load 3.89). All 5 of T6's fixtures closed, including the inherited `gutute-00-gaki684`. Residual 2 = `CLASS->DESCRIPTION` 1 (`kokebo-27-vafi688`, the recorded parse-attempt stop condition) + `SEQUENCE->NONE` 1 (T9's). | The joint dependency resolved exactly as D2 predicted: `zuvila-56-nuda425` closed only once BOTH landed. T7 measured it still open (correctly, T6 had not landed); T6 measured it `[FIXED]` with T7 already in. Neither task was scoreable alone. | Gate 99/99 exit 0; npm test 635 files/16366 tests; typecheck, lint, build all exit 0; both commits confined to their write-sets. |
| 2026-08-24 | T7 | Recorded T7's re-measured corpus numbers, which CORRECT the brief's: `sequencePlugin.accepts()` was true for **1346** fixtures (not the brief's 1351), **247** of them non-sequence (not 270), before T7; **1343 / 244** after. | The brief's 1351/270 are pre-mission figures from the parent halt; batches 2-4 had already narrowed the plugin before T7 ran. Both counts moved DOWN and neither approached the AC4 ceiling. These are the standing measure of how far `dispatch-by-parse-attempt` still has to go, so the corrected pair is the one to carry forward. | Measured over all 3158 fixtures with the gate's own `collectFixtures`/`diagramTypeOf` logic, git-stashed for a before/after on the same HEAD. |
| 2026-08-24 | T6 | ACCEPTED T6's deliberate EXCLUSION of `stereotype` from the `CommandCreateClassMultilines` TYPE alternation, against my own instruction to 'cover the whole set'. The instruction was wrong; following it literally would have newly misrouted fixtures, which is the mission's primary stop condition. | The alternation (`CommandCreateClassMultilines.java:102-103`) is `interface|enum|annotation|abstract class|static class|abstract|class|entity|protocol|struct|exception|metaclass|stereotype|dataclass|record`. T6 added all siblings EXCEPT `stereotype`, which collides with `<style>` block selectors and note prose. | Verified by the orchestrator, not taken from the report: `sequence/dudeku-78-naju581:10` is `stereotype {` inside a `<style>` block in a SEQUENCE diagram, and `component/jegure-48-cesi766:17` is the prose 'stereotype not working'. Both would have been claimed by class. (The report's third citation, `usecase/lunexo-59-fupo775`, does NOT contain the token — one of three citations did not check out; the decision stands on the other two.) |
| 2026-08-24 | T6 | Accepted `hasMapDeclaration` running on RAW lines, BEFORE the legend/embed narrowing that the same commit introduces. | Reads as inconsistent, and is deliberate: `zuvila-56-nuda425`'s `map` declaration sits inside a `legend … endlegend` that wraps its own `{{ }}`, so routing the widening through the new narrowing would have undone it on the exact fixture the widening exists for. The two changes are separate defects (D3 notes T6 both widens and narrows) and must not share a code path. | Cited `CommandCreateMap.java`; fixture closed `[FIXED]`. |
| 2026-08-24 | orchestration | T6 left a debug file at `tests/tmp-inspect.test.ts` during the run — a location `npm test` COLLECTS, so it briefly inflated the suite to 635 files in the agent's own gate run. It removed the file before finishing; the clean-tree re-run is 635 files legitimately (T7's new `accepts.test.ts`). | Third scratch-file incident this mission. Worth a standing note: agent scratch belongs in the session scratchpad, never under `tests/`, where it silently becomes part of the gate it is supposed to be measuring. | `git status --short` empty at batch close; gates re-run on the clean tree. |
| 2026-08-24 | T5 | Batch 4 closed: all 9 fixtures, SLI 16 -> 7, verified independently at the COMMITTED 120,000ms budget (not a diagnostic one). `SEQUENCE->CLASS` fell 12 -> 3, the 3 being T6's. AC4 — the design-for criterion — held: the gate's 3075-fixture `agree` check reports 0 broken. | Two orthogonal defects, both required, neither closing the other. | Gate 99/99 at exit 0; npm test 634 files/16334 tests; typecheck, lint, build all exit 0. |
| 2026-08-24 | T5 | Accepted the mechanism-1 fix — strip sprite regions BEFORE taking the 20-line window, reusing `stripSpriteRegions` from `descriptive-keywords.ts` — as NOT tripping the task's 'ask first' boundary. | That boundary guards typing on RAW instead of PREPROCESSED lines, inverting the deliberate choice at `block-extractor.ts:242-250`. This keeps windowing on preprocessed lines and merely excludes a specific upstream-defined non-content region; no upstream factory types on a sprite body either. Different change, boundary intact. | Checked for an import cycle rather than assuming: `descriptive-keywords.ts` imports only `sprite-commands.js`, never `block-extractor`, so the new edge is acyclic. |
| 2026-08-24 | T5 | Accepted three added probe signals as D3-authorised widening, each bounded by a SequenceDiagramFactory command and cited inline: `activate`/`deactivate` (`CommandActivate.java:62`), left-pointing arrow dressing `<-`/`<--`/`<<--` (`CommandArrow.java:99-101`, `ARROW_DRESSING1`), and `note over` (`FactorySequenceNoteCommand.java:83,100`). | D3 pre-authorises exactly this widening for T5 and bounds it to that command list. The `note` signal was narrowed to `over` ONLY, with a regression test pinning `note left of X` to stay `class` — `left`/`right` positioning is shared with other diagram families, `over` is not. | Upstream command classes read directly; regression test committed. |
| 2026-08-24 | environment | The routing gate ran in **8.69 s** at load average 5.43, against 1000-2000 s (timeout) at load average 13-44 earlier in this session, with the same committed 120,000ms budget. | Settles the earlier timeouts conclusively as external OS contention (`corespotlightd` at 200-290% CPU), not anything this mission changed, and confirms the budget was never the problem. Recorded so no future reader re-derives it or 'fixes' the budget. | Both timings observed in this session with `uptime` captured alongside; git-stash control run at the contended load also timed out with zero changes. |
| 2026-08-24 | T4 | Batch 3 closed with AC3 **partially unmet**, deliberately and recorded: 34 of 36 fixtures closed, SLI 50 -> 16 (verified independently: `SEQUENCE->DESCRIPTION` empty, 99/99 gate tests, all four gates green). The 2 `CLASS->DESCRIPTION` did NOT close, for two DIFFERENT reasons, each diagnosed separately rather than assumed to be the 34-fixture defect inverted. | The task file explicitly warned these might be a second mechanism; they are, and they are two mechanisms, not one. Neither is fixable inside T4's write-set. | See the two rows below. |
| 2026-08-24 | T4 -> T6 | `component/gutute-00-gaki684` DEFERRED TO T6. Its source carries `protocol X as "INOUT" {`; `protocol` is a native class TYPE keyword (`CommandCreateClassMultilines`'s TYPE alternation: `protocol\|struct\|exception\|metaclass\|...`) that our `UNAMBIGUOUS_CLASS_DECL`/`CLASS_ACCEPTS_PATTERNS` never recognised. | The fix belongs in `src/diagrams/class/class-dispatch.ts`, which is T6's write-set, not T4's. T4 correctly stopped rather than reaching outside its write-set. T6's scope is extended to cover it. | Fixture source read directly; upstream TYPE alternation confirmed in `CommandCreateClassMultilines.java`. |
| 2026-08-24 | T4 | `component/kokebo-27-vafi688` is a CONFIRMED STOP CONDITION — closable only by parse-attempt dispatch, which is the separately-filed `dispatch-by-parse-attempt` mission. Recorded, NOT started, per the brief's stop conditions. This fixture will remain in the residual and the mission's '0 real misroutes' exit bar cannot be fully met without that separate mission. | Source is `component a $a { }` / `component b { }` / `note right of a` / `remove $a` — pure container content with no line-text discriminator. Verified in the Java by the orchestrator rather than taken from the agent's report: `CommandPackageWithUSymbol` is registered on BOTH `ClassDiagramFactory.java:130` AND `DescriptionDiagramFactory.java:96`, and `CommandRemoveRestore` on both (`ClassDiagramFactory.java:114`, `DescriptionDiagramFactory.java:94`). Every line parses under both factories; upstream resolves it by class-tries-first plus whole-document parse success, which D1 forbids copying here. | Upstream factory registration lists, read directly. |
| 2026-08-24 | T4 | Accepted T4 condensing ~40 lines of pre-existing comments in `descriptive-keywords.ts` to hold the file at exactly 527 lines while adding code. | The file was ALREADY 527/500 at HEAD and the complexity hook blocks growth beyond baseline, so some compression was forced. Checked what was lost rather than accepting the summary: all load-bearing citations survive (`PART2CODE`, `vivido-49-nisu863`, `ALL_TYPES`, `DESCRIPTIVE_ONLY_KEYWORDS`, `ALIAS_DECORATED_DISPLAY` all present before and after). Only two breadcrumb pointers were dropped (a `plans/si5b-stdlib/...` path and a `class-dispatch.ts` filename); the mechanisms they annotate survive intact. | `grep -c` before/after per citation; diff of the affected doc block read in full. |
| 2026-08-24 | T2/T3 | Batch 2 closed: routing SLI 75 -> 50, independently verified by the orchestrator (not taken from the agents' reports). The three target buckets (SEQUENCE->JSON, SEQUENCE->YAML, CLASS->YAML) are absent from the bucket table; residual 50 = SEQUENCE->DESCRIPTION 34, SEQUENCE->CLASS 12, CLASS->DESCRIPTION 2, CLASS->SEQUENCE 1, SEQUENCE->NONE 1. | Matches the brief's projected 25 for this batch exactly. The two measured cross-task dependencies both held: `dugeki-47-celo546` fell to yaml under T2 alone and closed once T3 landed (D2), confirming the brief's per-fixture landing measurement rather than the 'engine X stopped claiming it' error that killed the parent mission. | All four gates green at 7ff13413 (npm test 634 files/16309 tests exit 0; typecheck, lint, build all exit 0). Gate run with an explicit reporter: 99/99 pass, `[ROUTING SLI] 50 of 75`. Both commits confined to their declared write-sets (verified via git show --stat). |
| 2026-08-24 | T2 | Accepted T2's addition of a SECOND swimlane pattern for `CommandSwimlane2.java:60-75`'s `swimlane NAME [as LABEL]` keyword form, which the old bare `/^\|.+\|/` never covered. | Reads as a widening, and D3 caps widenings at the two named exceptions — but it is not one: the task's own AC2 requires every form BOTH command classes accept to still be claimed, so omitting it would have left the narrowing incomplete against its own acceptance criteria. Bounded by the upstream command class, cited inline. | `CommandSwimlane2.java:60-75`; no fixture newly misroutes (gate's `agree` check, 0 broken). |
| 2026-08-24 | T3 | Accepted T3's symmetric `title:` colon-form directive-skip in BOTH json and yaml, though only yaml's `rujapu-71-bidi404` strictly required it. | Justified independently of the fixture by `CommandTitle.java:59-67`'s grammar `title(?:[%s]*:[%s]*|[%s]+)` — i.e. upstream's title command accepts the colon form, so skipping it is mirroring the grammar, not fitting the fixture. Applying it to one engine and not the other would have been the arbitrary choice. | Agent verified no existing golden's first content line is a title line; full suite green. |
| 2026-08-24 | orchestration | Two untracked scratch files (`dump-routing.tmp.mjs`, `dump-goldens.tmp.mjs`) appeared in the repo root during batch 2. The T2 agent reported them as pre-existing from prior sessions; they were not — `git status` was clean at session start and their mtimes (08:02/08:04) fall inside this run. They were removed before the batch closed; tree is clean. | Recorded because it is the second instance of the pattern `.claude/memory/verify-agent-claims-si31.md` already documents: a confident provenance claim in a subagent report that measurement contradicts. Cheap to check, so check it. | `git status --short` empty at session start and at batch close; `ls -l` mtimes. |
| 2026-08-24 | T1 | Classified all 8 error-page goldens (not the brief's 4) as `jar-error`: the 4 extra (`state/cagego-53-vemo516`, `state/fugedo-34-fice721`, `state/xacona-99-peze211`, `state/zecivu-62-pagu681`) are version-banner pages pinned `agree` at NONE==NONE, so the 75-defect denominator is unmoved; the split is 3075/75/8, not the brief's 3079/75/4. | AC4 requires the gate to fail any `agree`/`known-misroute` pin whose golden is an error page — the brief's own AC2 numbers (3079/4) would have made AC4 fail on day one against these four. Confirmed with the user before implementing. | Anchored regex over all 3158 goldens (`>PlantUML version...[...]</text>` / `>An error has occurred...</text>`, keyed to `PSystemError.java:148-155` / `Version.java:51-54` / `ReportLog.java:103-108`) finds exactly 8; all 4 extras verified as version-banner pages by reading their `in.svg` heads. |
| 2026-08-24 | T1 | The routing gate's `corpus completeness` test genuinely times out (120s budget) when run solo under this run's system load (`corespotlightd` at 200-290% CPU, sustained load average 13-44 on a 12-core box, WebStorm indexing concurrently). Verified as environmental, not a regression: (a) `git stash` to the unmodified pre-T1 file times out identically (939s) under the same load; (b) a diagnostic-only local bump to 1,800,000ms (reverted before commit, never landed) let the same file run to completion with all 99 tests passing and the exact re-derived counts (3075/75/8); (c) the real gate, `npm test`, passed clean at exit 0 with the committed 120,000ms budget intact — full-suite concurrency is the condition the budget was actually derived for. Committed `CORPUS_BUDGET_MS` is unchanged. | Confirms the class of hazard `.agent-notes/confounded-wall-clock-readings.md` already documents, extended here to an external OS indexer rather than only concurrent `npm test` runs. | `npm test` exit 0, 16283 passed; solo run comparison timings above. |
