# Decision journal — namespace-cluster-box

Appended during execution. Every non-trivial judgement call gets a row:
"non-trivial" means a reasonable developer might have chosen differently.

Also record here, because later tasks are measured against them:

- T1's baseline reproduction (expect 691 / 20685)
- T4's **dipped** numbers — the one expected regression in this mission
- T5's numbers, which must exceed the batch-1 baseline
- the per-fixture outcome for the 11 named residual fixtures
- any residual that survives Batch 3, with its mechanism

| Date | Task | Decision | Why | Alternative rejected |
|---|---|---|---|---|
| 2026-08-14 | Batch 1 | Run T1/T2/T3 as three parallel `typescript-pro` agents | Write-sets are disjoint (`scripts/`, `src/diagrams/class/` + its test, `src/core/` + state) and no task consumes another's output within the batch | Sequential execution — no dependency justifies the wall-clock cost |
| 2026-08-14 | T1 | If the doc-size count reproduces at 691 but the matched-shape total differs from 20685, the agent reports and stops rather than tuning the matcher | Tuning the algorithm to hit a remembered number is fitting, which the port's rules forbid; the gate's job is sensitivity and stability, and the orchestrator judges the delta | Silently accepting whatever the new harness reports as the baseline |
| 2026-08-14 | T5 | **769 / 25403** (+77 doc-size-exact, +961 shapes over post-T4; +78 / +4638 over the batch-1 baseline). Verified by the orchestrator: 112 fixtures moved (110 class, 2 object, **zero state**), **zero regressions** | Reading the polygon is the mission's point and it paid off on both axes | — |
| 2026-08-14 | T5 | **9 of the 11 named residuals are now FULLY exact** — every shape matched, not merely the document size: `dopuzi` 6/9→9/9, `finono` 6/9→9/9, `zomidu` 6/9→9/9, `jinibe` 4/7→7/7, `mucuxi` 5/6→6/6, `ditapa` 4/7→7/7, `repipi` 28/31→31/31, `sugifi` 6/9→9/9, `sumule` 6/9→9/9 | — | The agent reported all 11 as "exact" on document size alone; two of them are not, and the shape counts are what the criterion asks for |
| 2026-08-14 | T5 | **`cidepu-54-bemo048` and `kicolo-81-sidi387` reach exact document size (248x256, 170x352) but stay at 13/28 shapes.** Mechanism, verified not inferred, and it is **OURS**: graphviz sizes an HTML-table node from the table laid out in whole points, so a cell declared `WIDTH="76.44999999999999"` is drawn 76 wide and jar reads THAT box's left edge. `graph-layout.ts#mapNodes:136` instead computes the corner as centre − width/2 from our unquantized 76.45, because `portNodeSize:122` returns the declared width whenever `d.portRows !== undefined` — which both fixtures have (`pack.ClassA::a --> ClassB::b`) and the other nine do not, which is exactly why only these two are affected. The error is half the fractional part: 0.225 for 76.45 and 0.11875 for 77.2375, against measured −0.23 / −0.12 / −0.22. Centres are not in dispute — real graphviz 15.1.1 on jar's own `svek-1.dot` puts the nodes at 40 / 40.5 / 167, jar's rendered 30 / 30.5 / 157 after its −10 shift, and our centres reproduce graphviz's to the digit. Recorded in `.agent-notes/class-html-node-corner-vs-quantized-width.md` | Same family as T15's `portNodeSize` fix, not the namespace box, and mission-sized: the blast radius is every HTML-table node with a fractional width, i.e. most class nodes | **A dot-engine issue was NOT filed, because the first hypothesis was wrong.** It was that the engine keeps a fractional width where graphviz quantizes; an isolated two-node repro run through `render()`+`getLayout()` returns 92/93, identical to real graphviz's `-Tplain`. The engine quantizes exactly as the C does. Also rejected: quantizing our own widths to match, which is fitting |
| 2026-08-14 | T5 | The brief's open question — "+8 in x explained by `CL_OFFSET`, +4 in y NOT explained" — is **answered: there was never a separate y mechanism** | The asymmetry was an artefact of the old approximation itself, which applied a flat `NAMESPACE_SIDE_PADDING` on three sides but `htitle + NAMESPACE_TOP_EXTRA` on top, while modelling `CL_OFFSET` on no side at all. The real polygon carries the offset on all four sides, so both axes went exact together | — |
| 2026-08-14 | T4 | **The predicted dip did not happen — the numbers ROSE, to 692 / 24442** (+1 doc-size-exact, +3677 shapes, +17.7%). Accepted as a real improvement, not a measurement artefact, on independent re-measurement by the orchestrator | The dip was predicted on the assumption that the still-wrong package box would dominate. It does not. The box is 1–3 shapes per fixture; its members are ~29. Adding the wrapper margins moves member NODES into jar's real layout space, and the harness aligns each document under ONE rigid offset — so before T4 a cluster's interior and the rest of the document could not both align at once, and now they can. Verified: exactly 78 fixtures moved (76 class + 2 object), **zero state fixtures** — proving the change is confined to the class engine — and **zero fixtures regressed** | Treating the rise as a broken harness. Ruled out: nothing in the harness changed between the two runs, the run is reproducible, and the type breakdown matches the change's blast radius exactly |
| 2026-08-14 | T4 | All 11 named residual fixtures are byte-identical to the batch-1 baseline | Corroborates the mechanism above rather than contradicting it: these are small single-cluster diagrams where every member sits inside the one cluster, so a uniform wrapper shift is fully absorbed by the rigid alignment and only the box's own error remains. `cidepu-54-bemo048` is still 240x248 against jar's 248x256 — the missing 8px is exactly the `CL_OFFSET` our box does not yet read. That is T5's to close, by construction | — |
| 2026-08-14 | T4 | Accepted a new file outside the declared write-set: `src/diagrams/class/class-namespace-title-table.ts` | `class-dot-graph.ts` hit the 500-line hook cap at 502 lines. The split is mechanical, sits in the same directory, and the new path is in NO other task's write-set, so it cannot collide — the stop condition exists to catch silent edits to files another task owns, which this is not. The brief already blesses exactly this move for T5 | Suppressing the cap with `complexity-ignore` (forbidden) or `#lizard forgives` (wrong tool — this is a file-length cap, not a function one) |
| 2026-08-14 | T4 | Left the 3-line `namespaceTitleFont` duplicated from `class-namespace-shape.ts#titleFont` rather than exporting the original | `titleFont` is unexported, and exporting it would put a write into `class-namespace-shape.ts`, which is **T6's** write-set — a cross-task edit mid-batch. The duplicate carries a doc comment naming the original as its source. Flagged for T6 to consolidate if it touches that file anyway | Exporting now and accepting the write-set collision |
| 2026-08-14 | T1 | **Baseline re-pinned to 691 / 20765.** The document-size count reproduced EXACTLY (691/1073); the matched-shape total came in at 20765, +80 (0.4%) above the pinned 20685 | The 691 half is oracle-backed and confirms the harness sees the same tree, so the delta is an algorithm difference from the planning-time scratch script — which no longer exists and whose tolerance, denominator and tie-breaking were never specified beyond two bullets. Reproducing an unspecified algorithm bit-for-bit is unachievable, and chasing it IS the fitting the brief forbids. Re-pinning is safe here and only here: the tree is unmodified, so no mission-caused regression can hide behind it. Sensitivity is preserved — the matcher's ±0.05 position/size tolerance is far below every residual this mission targets (0.18, 0.29, 0.32, 0.39, 4, 8), and it compares size as well as position, which is the leniency the spec actually warned about | (a) Tuning tolerance/denominator until 20685 appeared — fitting, and it would have silently changed the gate's sensitivity. (b) Halting the mission on a number with no oracle behind it |
