# Decision journal — `activity-edge-draw-order`

Appended during execution. One row per non-trivial judgment call: if a
reasonable developer might have chosen differently, log it. Every riser gets
a row BEFORE its commit (stop 5), and every index-based test assertion that
is reordered gets a per-file count row (push-forward).

Log the execution plan here at startup (autonomous mode replaces the
user-review step with this entry — `rules/parallelism.md`).

| Date | Task | Decision | Rationale | Evidence |
|---|---|---|---|---|
| 2026-09-15 | startup | Execution plan: strictly sequential T1 -> T2 -> T3 -> T4, one task per batch, no parallel dispatch. | D6 makes the batches sequential by construction (two rules share one measurement surface and one re-pin). With one task per batch there is no write-set disjointness to check and no worktree isolation needed; the only concurrent writer risk is the orchestrator itself, so bookkeeping commits happen between tasks, never while an agent is live, and staging is by explicit path (never `git add -A`). | `README.md` Progress (4 batches x 1 task); D6; `~/.claude/rules/parallelism.md` (autonomous mode logs the plan here instead of user review) |
| 2026-09-15 | T1 | Q1 answered WITHOUT amending D2: a lane-less edge cannot occur in a laned diagram, so D2's "first pass it qualifies for" default stands as an unreachable branch rather than a live rule. | The jar's parser latches `SWIMLANE_FORBIDDEN` on the first non-swimlane instruction and errors on any later `\|lane\|`, so content cannot precede the first lane declaration; independently `ConnectionCross#drawU` returns early on a null lane, so the cross pass could not draw one either. A reasonable developer might have picked the cross pass instead (the `Cross` filter does admit null != non-null) — that would be wrong, because the admitted connection then draws nothing. | `ActivityDiagram3.java:80-91`; `ConnectionCross.java:53-61`; `UGraphicInterceptorOneSwimlane.java:96-101`; authored `.puml` rendered via `scripts/oracle-render.sh` -> ERROR diagram; 4 valid laned fixtures -> 0 duplicated elements; 0 of 59 laned baseline fixtures carry an `undefined` edge end |
| 2026-09-15 | T1 | Measured rule (a) and (b) with env-gated patches in ONE scratch tree rather than three sequential edit cycles, and ran a control with both gates off. | Three separate patch/revert cycles cost three full 268-fixture renders and leave no evidence the tree was clean between them. One tree plus a control run proves the gates are inert (52673, every delta 0) and makes the three measured runs parallel-safe, since env is per-process. | `measurements/base.json` and the control run both report aggregate 52673 with 0 non-zero deltas |
| 2026-09-15 | T1 | Rewrote `fixtures.md` keeping the SAME 38 slugs but correcting the pin sum to 11193 and `xarumo-26-zinu467` to a riser (+4). | Membership was reproduced exactly, so the provisional table's slug set was right; its header pin sum (11035) disagrees with re-adding its own rows (11193) and with `diff-baseline.json`, i.e. a transcription error rather than a stale pin. Recording the correction here because T4 re-pins against these rows. | Recomputed from `diff-baseline.json`; `measurements/scratch-{a,b,ab}.json` |
| 2026-09-15 | T1 | Reported the brief's laned-fixture count as 59, not 60. | The brief's 60 comes from a markup regex; `letuke-04-poza319` matches it but parses to `ast.swimlanes === []`, so rule (b) can never move it. Correcting it now prevents T2 from hunting a 60th mover that cannot exist. | Full 268-fixture scan through `assignCoordinatesFull`, keyed on `ast.swimlanes.length` |
| 2026-09-15 | T1 | Corrected my own Q3 arrowhead count mid-measurement (all `<polygon>` -> only fill==stroke). | The first count read 11 arrowheads on `cemipu-87-dinu624` by counting the if-diamond as an arrowhead, which would have manufactured evidence of snake merging that does not exist. Logging it because the corrected figure (9) is what D5's "not needed here" rests on. | `cemipu` 11 polygons / 9 arrowheads; touching pairs = 0 on all 7 slugs |
| 2026-09-15 | startup | Branch `feat/activity-edge-draw-order` cut from `6ff347f8`. | Brief names the branch and that baseline commit; `git rev-parse --short HEAD` confirmed `6ff347f8` before the checkout, and the tree carried only untracked `plans/` dirs. | `git rev-parse --short HEAD` -> `6ff347f8`; `git checkout -b feat/activity-edge-draw-order` |
| 2026-09-15 | T1 | ACCEPTED after independent orchestrator verification of every load-bearing claim. | Q1 unlocks every `src/` edit in the mission (stop 4), and this repo has a standing pattern of confident subagent claims that measurement disproves, so the gating claims were re-checked rather than read more carefully. Verified: both Java barriers quoted verbatim from source; `base.json` aggregate 52673 with zero non-zero deltas; all 38 pins equal to `diff-baseline.json` row for row, all `status:"baseline"`; 268 baseline fixtures total. | `ActivityDiagram3.java` `swimlane()` -> `CommandExecutionResult.error("This swimlane must be defined at the start of the diagram.")` under `SWIMLANE_FORBIDDEN`; `ConnectionCross.java:55-61` early-returns on either null lane; commit `b355127a` (7 files, 0 `src/`) |
| 2026-09-15 | T1 | Brief premise CORRECTED: 59 laned baseline fixtures, not 60. | The brief's `fixtures.md` note said 60 of 268 declare a swimlane. T1 re-derived 59 from `ast.swimlanes.length` and named the 60th; the orchestrator confirmed it independently. `letuke-04-poza319` has no swimlane at all -- its two `\|`-leading lines are a creole TABLE inside a multi-line action (`:a table` / `\|= title \|` / `\| foo\non two lines\|;`), consumed by `readMultilineActionBody` before the swimlane dispatcher sees them. The same false-positive surface `alc-T1` documented one mission earlier. T2 must not hunt a 60th mover under rule (b). | `tests/corpus/activity/letuke-04-poza319.puml` lines 5-7; `.agent-notes/aedo-T1.md` Q1 "Ruled out" |
| 2026-09-15 | T1 | `fixtures.md` pin sum CORRECTED 11035 -> 11193; membership unchanged at 38 slugs. | The provisional header's total was a transcription error, not a stale pin: re-adding the provisional table's OWN rows gives 11193. Orchestrator re-summed the rewritten table (38 rows -> 11193) and cross-checked every row against `diff-baseline.json` -- zero mismatches. So the re-measurement changed the numbers in the `a`/`b` columns, not which fixtures move. | orchestrator sum over `fixtures.md`; `oracle/goldens/svg-activity/diff-baseline.json` |
| 2026-09-15 | T1 | `xarumo-26-zinu467` confirmed a RISER (+4 under b), not a faller. | T1 self-flagged that it first recorded this row as -6 and asked for a second pair of eyes. Independently extracted from `scratch-b.json`: `xarumo` delta=+4 (score 221 vs pin 217), alongside `bumaca` +4, `decudi` +4, `judatu` +2, `maketa` +1, `misiji` -32, `racana` -128 -- all matching the rewritten table. Carry into T4's re-pin as a known riser needing a mechanism row, not a regression. | `measurements/scratch-b.json` |
| 2026-09-15 | B0 | Batch 0 gate GREEN; Batch 0 marked complete. | All four gates run at `b355127a`. Test count verified against collection rather than trusted on exit code alone -- an orphaned `coverage/.tmp` can make `npm test` skip files while still exiting 0 with a clean summary. `rm -rf coverage/.tmp` preceded the run. | `npm test` EXIT=0, `Test Files 710 passed \| 1 skipped (711)`, `Tests 19648 passed \| 2 skipped \| 1 todo`, 0 failures; `npx vitest list --filesOnly` = 711 (match); typecheck/lint/build exit 0 |
| 2026-09-15 | T2 | Rule (b) landed as a stable SORT by pass rank, not as a per-pass gather loop. | A gather loop (`for lane of laneNames { for edge ... }`) drops any edge whose lane is outside `laneNames`, silently shortening the edge list; a stable sort over a rank function can only permute. The two agree on every reachable input -- ranks are `laneNames.indexOf(pass)` in declaration order (D3) with the cross pass ranked last -- so this is a safety property bought for free, not a behaviour choice. | `edge-draw-order.ts#passRank`/`#lanePassOrder`; `Swimlanes.java:328-347` (per-lane passes) then `:350-352` (Cross); unit test "returns a permutation of every index" |
| 2026-09-15 | T2 | Aggregate after (b): **52078**, -595 vs base 52673, 31 fell / 5 rose -- T1's re-measured figure reproduced EXACTLY, and the riser/faller sets match `fixtures.md` row for row. | T1's 52078 was a throwaway scratch shape, so agreement was a cross-check, not a target; nothing was tuned toward it. Every mover is a `fixtures.md` row with a non-`-` `b` delta, and no fixture outside the table moved (stop 6 clear). | `measurements/t2.json` vs `measurements/base.json`; probe `--slugs-file fixtures.md`, subsetSum 10598 over 38 slugs |
| 2026-09-15 | T2 | Five RISERS accepted with mechanisms, none a geometry regression (stop 5). | `bumaca-51-kece901` +4, `decudi-92-bisu741` +4, `xarumo-26-zinu467` +4, `judatu-15-xize591` +2, `maketa-43-juja264` +1. All five are element-COUNT mismatches against the jar (29/35, 72/62, 35/42, 82/105, 55/44), where `compareSvg` pairs elements POSITIONALLY and `compare.ts:404`'s `[childCount]` short-circuit charges the SUM of both sides -- so a reorder moves the number without any coordinate moving. `bumaca` is the proof: 9 elements move, alignment is unchanged at 13, score still +4. `maketa` is the counter-proof that the metric is not fidelity here: alignment IMPROVES 10 -> 12 while the score RISES +1. No node coordinate is read or written by this change. | `.agent-notes/aedo-T1.md` Q5; `compare.ts:404`; `measurements/t2.json` deltas equal `fixtures.md`'s `b` column on all 36 rows |
| 2026-09-15 | T2 | `ALLOWED_NEW_OVERLAPS` re-listed at new indices (9 -> 9) rather than re-derived as a new class, after a HEAD-worktree comparison (stop 8). | The permutation renumbers every edge-derived shape in `shapesOf`'s flat list, so the pinned pairs move index. Re-pinning on a diff alone would have been fitting the list to whatever the run printed; instead the same harness was run in a detached worktree at `d33799dc` and every pair matched byte for byte -- same fixtures, same kinds, same `polygonSkipMode`, identical coordinates, and identical per-fixture shape counts (19/21/20/26/61). `hardViolations` empty throughout. | scratch `head-dump.mts` (worktree `d33799dc`) vs the live harness; per-entry cites now inline in `invariant.test.ts` (`Worm.java:159-168`, `UGraphicCompressOnXorY.java:100-112`) |
| 2026-09-15 | T2 | Reordered index-based assertions: `tile-coordinates.test.ts` **0**, `tests/unit/activity/layout.test.ts` **0**, `compress/invariant.test.ts` **9** (the pinned overlap entries). Nothing deleted (stop 12 clear). | Both edge-order-sensitive suites turned out to assert on edges by SEARCH (`geo.edges.find(...)`, `.filter(...)`) or on single-edge fixtures (`edges[0]` where there is exactly one edge), so a permutation cannot reach them. They are left untouched rather than churned to prove they were inspected. | `npx vitest run tile-coordinates.test.ts tests/unit/activity/layout.test.ts` green with no edit; `grep -n 'edges\[' ` -> 3 hits, all `edges[0]` on one-edge fixtures |
| 2026-09-15 | T2 | The new test's laned-fork fixture sets `tile.swimlane`/`swimlaneOut` explicitly; the first draft did not, and had NO cross-lane edge. | Diagnosed rather than patched around: a directly-constructed `GtileFork` never passes through `tile-layout.ts#withSwimlane`, so its bar and join carried no lane and every connector became a HALF-lane-less edge (`{lane1: undefined, lane2: 'A'}`), which T1 Q1 branch 4 resolves to the known end's lane -- no crossing, so the pass grouping was untestable. Setting the fork's own lane (what `tileFork` does for real markup) yields `[A->A, A->A, A->B, B->A]`. Incidentally the one place branch 4 was observed to fire at all. | scratch `dump-fork.mts` before/after; `tile-layout.ts#withSwimlane`/`#withSwimlaneOut` |
| 2026-09-15 | T2 | Reordered ONE assertion in `tile-layout.test.ts` -- a file in T3's write-set, not T2's -- instead of halting on stop 1 or leaving it red. | Stop 1 halts only when a needed file is outside this task's write-set AND outside every other task's; `tile-layout.test.ts` is named in T3's write-set for exactly this ("ONLY assertions the new order breaks, reordered never deleted"), so the halt condition is not met, while the quality bar does require every non-gate file green. A reasonable developer might have halted instead, hence this row. Batches are strictly sequential (D6), so T3 is not a concurrent writer. The change is order-only: `['X','Y']` -> `['Y','X']`, the same two values, nothing deleted (stop 12 clear), and it is exactly what rule (b) predicts -- lanes declared `A, X, Y`, so branch Y's out-drop (`Y->Y`, lane Y's own pass) precedes branch X's (`X->Y`, the final cross pass). Per-file count: **1**. | `tile-layout.test.ts:395`; `UGraphicInterceptorOneSwimlane.java:96-101`; `Swimlanes.java:178-216,350-352` |
| 2026-09-15 | T2 | Batch 1 gate: `typecheck`/`lint`/`build` exit 0; `npm test` red ONLY on `activity.diff-baseline.ratchet.test.ts`, **5 slugs** (`bumaca-51-kece901`, `decudi-92-bisu741`, `judatu-15-xize591`, `maketa-43-juja264`, `xarumo-26-zinu467`) -- all `fixtures.md` risers journaled above, all inside the D6 red allowance. | The other three activity gates (`.swimlane-baseline`, `.style-baseline`, `.text-baseline`) are GREEN: rule (b) permutes the edge list without moving a coordinate, and those three pin lane extents, style values and text content, none of which a draw-order change can reach. Worth recording because the brief expected all four to be red by design -- the allowance was not needed for three of them. Stop 7 clear: total tests 19670 vs base 19651, a difference of exactly the 19 new tests in `edge-draw-order.test.ts`, and zero non-activity FAIL files. `Test Files 712` = 711 + the new file, so no `coverage/.tmp` under-collect. | `npm test` EXIT=1, `Test Files 2 failed \| 709 passed \| 1 skipped (712)`, `Tests 6 failed \| 19661 passed \| 2 skipped \| 1 todo (19670)`; 6 failures = 5 ratchet slugs + the 1 reordered `tile-layout` assertion (now fixed) |
| 2026-09-15 | T2 | Batch 1 gate, FINAL re-run after the `tile-layout` reorder: one red FILE, `activity.diff-baseline.ratchet.test.ts`, **5 slugs**, all journaled risers. | Recording the corrected figure rather than editing the row above it: the earlier run's 6th failure was the reordered assertion, now fixed, so the red surface is exactly the D6 allowance and nothing else. `Test Files 1 failed \| 710 passed \| 1 skipped (712)`, `Tests 5 failed \| 19662 passed \| 2 skipped \| 1 todo (19670)` -- the +19 over base 19651 is `edge-draw-order.test.ts` alone (stop 7 clear). | `npm test` EXIT=1; typecheck/lint/build exit 0 |
