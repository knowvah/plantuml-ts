# Batch 5 — The wirings (parallel; the ONLY engine-file edits in SI1)

| ID | Description | Writes | Depends On | Done |
|----|-------------|--------|------------|------|
| T11 | Shared `-[single]->` dedup: parsers call the ported semantics (helper over their link lists per ADR-3). Description SWAPS parse-state.ts:168-183's inline version; class + state ADOPT (grep-verified absent). AUTHOR fixtures: class + state `-[single]->` duplicate-link diagrams + deterministic jar oracles (corpus is silent; per the corpus-is-a-starting-point rule) under oracle/goldens/{class,state}/si1-single-dedup-*/ | src/diagrams/description/parse-state.ts, src/diagrams/class/parser.ts (or its command module), src/diagrams/state/parser.ts + new fixtures + tests | T6, T10 | [ ] |
| T12 | Folder un-narrowing: route description folder/package leaves through the faithful create2→BodyEnhanced1 path (leaf-sizing.ts:128-135 gate) and drop the class-side exclusion (class-layout-generic-classifier.ts:70-86) — closes gujigi-63 (pin 0.152778 → conformant, DELETE from class size-backlog + A2s ledger row annotated). Re-measure DESCRIPTION fully: folder-shaped pins that improve are deleted (journal); ANY widening is a STOP | src/diagrams/description/leaf-sizing.ts, src/diagrams/class/class-layout-generic-classifier.ts, oracle/goldens/class/size-backlog.json, oracle/goldens/description/size-backlog.json + tests | T9 | [ ] |
