# Decision journal — usymbol-ink-rule

Appended during execution. Every non-trivial judgement call gets a row:
"non-trivial" means a reasonable developer might have chosen differently.

Also record here, because later tasks are measured against them:

- **T1's measured baseline** — doc-size-exact and matched shapes, with the
  commit sha. NOT the numbers quoted in the brief.
- **T1's family answer** — 5 or 6, with the enumeration and the reason for
  the discrepancy with note (b).
- **T2's 1.5 reproduction** — or, if it does not reproduce, what it
  measured instead and which of the walk or the premise is wrong.
- **T3's `usecase`/`lollipop` finding** — whether the general walk
  reproduces them byte-identically, and what was done about it either way.
- **Every family fixture still not exact at T4**, with its mechanism and
  `file:line`.

| Date | Task | Decision | Why | Alternative rejected |
|---|---|---|---|---|
| | | | | |
