# Decision journal — `activity-oracle-harness`

Append one row per non-trivial judgment call made during execution.
"Non-trivial" means: a reasonable developer might have chosen differently.

| Date | Task | Decision | Rationale | Flagged for review |
|---|---|---|---|---|
| 2026-09-02 | batch-0 | Serialize T0 then T1 instead of running them in parallel | Write-sets are disjoint, but a single working tree shares one git index; two concurrent agents committing race it. Prior mission (`sequence-creole` batch 3) hit exactly this. Wall-clock cost is one small task's duration. | no |
| 2026-09-02 | T0 | Halted on stop condition 1 rather than writing two baseline JSONs outside every task's write-set | Correct call by the task agent; `scripts/repin-sequence-baselines.ts:3-8` reserves baseline writes to the orchestrator. Verified the failure independently (both suites red, 373 unpinned each). | no |
| 2026-09-02 | T0b | Added task T0b (orchestrator-executed) to pin the activity tree into routing- and refusal-baseline additively; recorded as D11 | Both gates' failure text forbids narrowing the walk. Measured first: 350 ACTIVITY->NONE, 23 jar-error, 82 we-error-jar-rendered. All 350 share one cited mechanism (renderer.ts:221-226 vs TextBlockExporter.java:293) that T5 fixes. User ruled on this explicitly. | YES — expands scope into two prior missions' gates |
| 2026-09-02 | T0b | Commit T0b BEFORE T0 | Both gates walk the disk tree, which already holds the cache; pinning first keeps every commit green, whereas committing the cache first lands one red commit. | no |
| 2026-09-02 | T1 | Accepted T1's helper after independently re-verifying its three structural claims, the `dispatcher.ts:124` citation, and determinism over a 40-fixture sample | Agent reports are verified, not taken (`verify-agent-claims-si31`). 34 identical / 0 differing / 6 threw; the 6 are ParseRefusals, which the helper correctly throws on. | no |
| 2026-09-02 | T2 | Added D12: a fixture whose GOLDEN is a jar error page records `status:"jar-error"`, no score | D8 covers OUR parser gaps only; 23 goldens are the jar's own error pages and 15 of those render cleanly here, so a numeric pin would compare a real diagram to an error image. Both sibling gates already exclude this exact set on this exact reasoning — precedent, not invention. | YES — extends the brief's status vocabulary |
| 2026-09-02 | T2 | Corrected D12: the numeric population is 268, not 283 | Orchestrator arithmetic error — `373−23−90+8` is 268. T2 measured it, refused to force the stated number, and flagged it. 283 is a real but DIFFERENT quantity (renders clean here); 15 of those 283 have jar-error goldens. Verified against the committed baseline: 268+82+23=373. | no |
| 2026-09-02 | T2 | Retired the brief's "diff floor is exactly 12, every fixture" premise | Measured: 238 of 268 (89%) sit at diffCount 12 with one identical path set; 30 do not — 9 at 10, 1 at 11, and a tail to 74. The uniform-floor claim in the mission README is false. Recorded rather than worked around; T6's census inherits the real distribution. | YES — a headline premise of the brief was wrong |
| 2026-09-02 | T4 | Identified both extras: A = `svgRoot`'s unconditional bg rect (`src/core/svg.ts:521-524`, reached because activity's fragment carries no `diagramType`), B = `renderActivity`'s unconditional bg rect (`src/diagrams/activity/renderer.ts:199-205`). Both CHROME — full-canvas fills, zero geometry contribution. Artifact in `.agent-notes/aoh-T4-g-children.md`. | Instrumented actual normalized child lists across 5 structurally different fixtures plus a 283-fixture population scan (A+B adjacent in 263; the 20 exceptions are 19 chrome-wrapped + 1 transparent, both confirming the two guards are independent). Java read, not remembered: `SvgGraphics.java:186-192` + `:207-212` + `:805-806`. | no |
| 2026-09-02 | T4 | Extra B is NOT unconditionally deletable — T5 must relocate it behind the jar's `paintBackcolor` guard, not remove it | `SvgGraphics.java:189-191` skips the content rect only for `#00000000`/`#000000`/`#FFFFFF`; for any other solid colour the jar DOES draw it. Three cached goldens prove it: `labala-74-juki864` (#0B58A8), `poraji-17-goke817` (#808080), `levuma-67-cego489` (#1B1B1B) each open their `<g>` with `<rect x="0" y="0" ... stroke:none>`. A naive delete regresses all three; a naive `!== '#FFFFFF'` guard regresses the 5 black-background fixtures. `assemble-svg.ts#SEQUENCE_UNPAINTED_BACKGROUNDS`/`maybeSequenceBackgroundRect` already models exactly this Java — T5 adds an ACTIVITY case reusing it. NOT a halt: B contributes no geometry, so the stop condition (layout-bearing) is not met. | YES — refines D7's "two extras get removed" into "one removed, one relocated behind a guard" |
| 2026-09-02 | T4 | Retired the brief's "+2 on every activity fixture" premise | Measured over the 283 comparable fixtures: the childCount delta histogram is mostly NEGATIVE (`-5`x13, `-10`x12, `-6`x12 ...), with only 55 at exactly +2. The two rects ARE a constant +2, but they sit on top of a per-fixture content deficit (unported activity features), so +2 shows only where content already aligns. Same class of error as T2's retired "diff floor is exactly 12". | YES — a second headline premise of the brief was wrong |
| 2026-09-02 | T4 | Flagged: `routing-conformance.test.ts:586-597` predicts T5 flips 350 fixtures `known-misroute` -> `agree` | That gate's own comment already cites `renderer.ts:221-226` vs `TextBlockExporter.java:293` as the single shared mechanism and names T5 as the fix. Its pins (3133/367/31) must move in T5's commit, not after. | no |
| 2026-09-02 | T4 | Accepted the diagnosis; verified both Java citations verbatim (`SvgGraphics.java:186-192` guard, `:805-806` root style) and re-measured the child-count delta with `compareSvg` | Agent claims are verified, not taken. Both extras are background rects — A from the generic `svgRoot` fallback, B `renderActivity`'s own. `isChrome: true` on both, so T5 is cleared. | no |
| 2026-09-02 | T4 | BINDING AMENDMENT to T5: extra B is RELOCATED behind upstream's `paintBackcolor` guard, not deleted | 3 of 373 goldens legitimately open their root `g` with a background rect (`labala-74-juki864` #0B58A8, `levuma-67-cego489` #1B1B1B, `poraji-17-goke817` #808080) — independently confirmed by the orchestrator. Upstream paints it only when the colour is not #FFFFFF, #000000 or #00000000. Deleting unconditionally would regress those 3. | YES |
| 2026-09-02 | T4 | Added D13: amended the exit bar's `g[1][childCount]` clause | Re-measured over all 268: 168 fixtures have FEWER children than the jar, 20 are already equal, 80 have more (55 at exactly +2). The clause is unachievable by removing 2 rects, and the 20 equal ones become a predicted riser set. | YES — a brief exit criterion was impossible as written |
| 2026-09-02 | T5 | Corrected T4's flag that T5 must move the routing pins | `routing-conformance.test.ts:688` asserts a fall to the jar type is reported `[FIXED]` and NEVER fails, and the derivation counts are computed from the manifest, which T5 does not change. T5 needs no baseline write; T6 re-pins the 350 to `agree`, which that baseline's own `$comment` sanctions. | no |
| 2026-09-02 | T5 | Accepted; re-measured the descent independently rather than taking the report | 154722 -> 108447, **-29.9%**; 268 fell, 0 rose, 0 held. `diffCount` rose on 57 and fell on 211 — the D2 artefact, exactly as predicted. Root attributes: 0 of 268 missing any of the 7. `defs`: 0 of 268 non-empty. | no |
| 2026-09-02 | T5 | D13's predicted riser set did NOT materialise — 0 risers, not the ~20 forecast | The forecast assumed the 20 currently-equal fixtures would acquire a -2 `childCount` diff and short-circuit. They fell instead: removing the two rects improved alignment enough elsewhere to more than offset it. Recorded as a conservative prediction, not a miss — pre-declaring a riser set cost nothing and would have caught a real regression. | no |
| 2026-09-02 | T5 | Found a THEME-RESOLUTION gap while verifying the background guard — census material, not this mission's work | `levuma-67-cego489` (`skinparam mode dark`) resolves `theme.colors.background` to `#FFFFFF` where the jar resolves `#1B1B1B`. The guard is faithful — it correctly declines to paint on our resolved white. T5 reported this honestly and did not special-case it. **CORRECTED by T6:** the orchestrator wrongly added `velodu-59-sada437` as a second instance; its golden is the jar's OWN error page (verified — it carries the `PlantUML version` banner), pinned `jar-error`, contributing no number. Its black root style is `PSystemError`'s black ground, not a theme. `skinparam mode dark` occurs on exactly ONE fixture corpus-wide. | no — single fixture, censused |
| 2026-09-02 | T6 | T6 REFUSED the orchestrator's instruction to re-pin all 350 activity `known-misroute` entries to `agree`, and was right | Only 268 fell. The other 82 still route `ACTIVITY -> NONE` for a DIFFERENT mechanism: our parser refuses those sources, so `renderSync` returns a `PSystemError` page carrying no root attribute at all — T5's shell change cannot reach a document the activity renderer never draws. Verified: the 82 are set-identical to diff-baseline's 82 `status:"error"` entries. Following the instruction as written would have turned the routing gate RED. Counts 3133/367/31 -> 3401/99/31, censused 366 -> 98. | YES — orchestrator instruction was wrong; agent caught it |
| 2026-09-02 | T6 | Accepted; re-verified the re-pin, both corrections, and all four gates independently | Non-activity routing pins are EXACTLY unchanged (3133/17/8, the pre-mission values) — zero cross-contamination, D10 satisfied. The 82 retained `known-misroute` are set-identical to the 82 diff-baseline errors. `npm test` 18198 passed: the -268 vs baseline is exactly the routing gate's per-known-misroute `it()` blocks (`pinnedMisroutes` 367 -> 99), an explained reduction, not lost coverage. | no |
