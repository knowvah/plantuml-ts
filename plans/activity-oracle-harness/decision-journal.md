# Decision journal — `activity-oracle-harness`

Append one row per non-trivial judgment call made during execution.
"Non-trivial" means: a reasonable developer might have chosen differently.

| Date | Task | Decision | Rationale | Flagged for review |
|---|---|---|---|---|
| 2026-09-02 | batch-0 | Serialize T0 then T1 instead of running them in parallel | Write-sets are disjoint, but a single working tree shares one git index; two concurrent agents committing race it. Prior mission (`sequence-creole` batch 3) hit exactly this. Wall-clock cost is one small task's duration. | no |
| 2026-09-02 | T0 | Halted on stop condition 1 rather than writing two baseline JSONs outside every task's write-set | Correct call by the task agent; `scripts/repin-sequence-baselines.ts:3-8` reserves baseline writes to the orchestrator. Verified the failure independently (both suites red, 373 unpinned each). | no |
| 2026-09-02 | T0b | Added task T0b (orchestrator-executed) to pin the activity tree into routing- and refusal-baseline additively; recorded as D11 | Both gates' failure text forbids narrowing the walk. Measured first: 350 ACTIVITY->NONE, 23 jar-error, 82 we-error-jar-rendered. All 350 share one cited mechanism (renderer.ts:221-226 vs TextBlockExporter.java:293) that T5 fixes. User ruled on this explicitly. | YES — expands scope into two prior missions' gates |
| 2026-09-02 | T0b | Commit T0b BEFORE T0 | Both gates walk the disk tree, which already holds the cache; pinning first keeps every commit green, whereas committing the cache first lands one red commit. | no |
| 2026-09-02 | T1 | Accepted T1's helper after independently re-verifying its three structural claims, the `dispatcher.ts:124` citation, and determinism over a 40-fixture sample | Agent reports are verified, not taken (`verify-agent-claims-si31`). 34 identical / 0 differing / 6 threw; the 6 are ParseRefusals, which the helper correctly throws on. | no |
| 2026-09-02 | T2 | Added D12: a fixture whose GOLDEN is a jar error page records `status:"jar-error"`, no score | D8 covers OUR parser gaps only; 23 goldens are the jar's own error pages and 15 of those render cleanly here, so a numeric pin would compare a real diagram to an error image. Both sibling gates already exclude this exact set on this exact reasoning — precedent, not invention. | YES — extends the brief's status vocabulary |
| 2026-09-02 | T2 | Corrected D12: the numeric population is 268, not 283 | Orchestrator arithmetic error — `373−23−90+8` is 268. T2 measured it, refused to force the stated number, and flagged it. 283 is a real but DIFFERENT quantity (renders clean here); 15 of those 283 have jar-error goldens. Verified against the committed baseline: 268+82+23=373. | no |
| 2026-09-02 | T2 | Retired the brief's "diff floor is exactly 12, every fixture" premise | Measured: 238 of 268 (89%) sit at diffCount 12 with one identical path set; 30 do not — 9 at 10, 1 at 11, and a tail to 74. The uniform-floor claim in the mission README is false. Recorded rather than worked around; T6's census inherits the real distribution. | YES — a headline premise of the brief was wrong |
