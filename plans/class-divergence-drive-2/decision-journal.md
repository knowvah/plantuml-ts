# Decision journal — `class-divergence-drive-2`

Append-only. One row per decision, riser, flip, regroup, stop, or
push-forward call.

| # | When | Task | Decision / finding | Why / evidence |
|---|---|---|---|---|
| 1 | 2026-09-24 | T0 | Branch `feat/class-divergence-drive-2` cut from `origin/main` = `18a8e0c50`; four gates green (807/807 test files collected, 22509 pass, 0 fail) | Baseline must be green (T0 step 2); JSON reporter files 807 = `find tests -name '*.test.ts'` 807 |
| 2 | 2026-09-24 | T0 | `b0.json` = 560/86/77, **0 movers** vs `b-plan.json` (pin-diff: 0 transitions; per-row S/N/verdict identical on all 723) | `506fdb4e6` (applySeededDefIds url(#) scan) and `18a8e0c50` (measurer probe-once) changed no class output; no def-id mover |
| 3 | 2026-09-24 | T0 | Oracle record (D5): `oracle/dist/plantuml-oracle.jar -> ~/git/plantuml/build/libs/plantuml-1.2026.8beta1.jar`, MANIFEST `Implementation-Version: 1.2026.8beta1`; `oracle/pin.json` `plantumlVersion: 1.2026.7beta11` (upstreamSha 11ed6720) | Symlink ≠ pin is the known maintainer step (next-missions "Oracle pin (D12)"); not touched (stop 9) |
| 4 | 2026-09-24 | T6 | Spot-checked the HIGH claims fixes depend on, against code: **Q-2** holds (`DotPath.java:206-216 moveStartPoint` moves `x1,y1` AND `ctrlx1,ctrly1`, plus a first-bezier drop branch; `class-edge-geo.ts:160-180` moves only the endpoint, ungated); **Q-7** holds (`class-kal.ts:30` "NOT ported: overlapx/moveX/fixOverlap"); **Q-9** holds (0 `roleLines` hits in `class-ink-box.ts`); **C-1** holds (`class-badge-sized-glyphs.ts:233` `if (letter !== 'C') return undefined`); **R-1** holds (no `assoc-circle` arm in `addClassifierInk`) | T6 step 1; memory "verify agent claims" |
| 5 | 2026-09-24 | T6 | **D-1 DISPROVED** (T4 said dot-engine returns a coarser spline than graphviz for cluster-anchored edges). `dot -Tdot` (graphviz 16.1.0) on runane's cached `svek-1.dot`: edge `sh0044 -> zaent0005` pos `331.9,475.77 324.84,397.63 302.36,148.93 302,145.05` (single cubic, bb h 737). dot-engine raw (`diagnosis/scratch/dump-edge-points.mts`): `321.905,251.227 314.842,329.367 292.36,578.073 292.009,581.954` — identical after the constant (10,10) frame offset (y flipped). Mechanism re-opened as **CLIP-1**, owner this mission (T9), diagnose first | T6 step 1; D-1's own "only remaining variable" inference fails |
| 6 | 2026-09-24 | T6 | **S-9 DISPROVED** (T5 said rezoba/jojime path diffs are dot-engine spline fitting; its evidence was "no pos= in svek-1.dot", which is true of every input DOT). `diagnosis/scratch/raw-edges.mts` vs `dot -Tdot`: rezoba both edges and jojime all three match graphviz point-for-point after constant offsets (48.32,65) / (47.64,65). Every such edge ends on a `zaent<N>` cluster anchor — same edge class as D-1 → merged into **CLIP-1** (T9); guxode's numeric half goes with it | Same probe as row 5 |
| 7 | 2026-09-24 | T6 | Regroup, fixtures per task before → after: T7 8→9 (+xoxuni, +vuresa from T8; −fumalu to T8), T8 9→6 (+fumalu; −xoxuni, −vuresa to T7; −rezoba, −jojime to T9), T9 4→6 (+rezoba, +jojime), **T7b new** 0→1 (lojiga from T17), T11 11→9 (+coxose from T12; −rifuzu, −camuna, −nafiki to T13), T12 8→5 (−coxose; −mucoti, −sefazi settled), T13 4→7 (+rifuzu, +camuna, +nafiki), T15 7→7, T17 11→9 (−lojiga, −xosiza settled). Settled out of mission: 3. Total 62 | Moves follow shared write-set files (`renderer-edge.ts`: S-2/S-4/S-8; `renderer-classifier-box.ts`: T7 S-11 and T8 S-3/S-5) and shared mechanisms (Q-2 is T12's for every Q fixture; a fixture sits with its LAST open mechanism) |
| 8 | 2026-09-24 | T6 | Waves: B1 T7 ∥ T9 then T7b ∥ T8 (T8 and T7b each share a file with T7: `renderer-classifier-box.ts`, `class-namespace-shape.ts`); B2 T11 ∥ T12 then T13 (T13 measured on both; Q-10 on their tree). S.md's `renderer-package.ts` does not exist → `renderer-group.ts` in T7's write-set | T6 step 3 disjointness |
| 9 | 2026-09-24 | T6 | lojiga (R group) → new task **T7b** in batch 1: its first diff is structural (`exp=rect | act=path`), an unported `USymbolStack` (`stack` keyword parsed at `class-container.ts:333` but no shape) — not a canvas term. D2 (structure first) places it in B1; separate task because it is a new shape module | R.md R-8; push-forward "split or merge fix tasks at T6" |
| 10 | 2026-09-24 | T6 | xosiza **settled out of mission**: final `open -> next-missions (layout-epsilon policy)`. R.md R-4 (HIGH, arithmetic reproduced): raw extent 186.999992 floors to the jar's answer; `absorbLayoutEpsilon` (`core/layout-epsilon.ts:33-35`) rounds it up to 187.000 → 188. The absorber compensates our in-process inches round trip (jar scrapes 2-dp `dot -Tsvg`); changing it is the rounding-policy change D6/stop 12 forbid. Filed in `planning/next-missions.md` at T18/T20 | D6; not an ink term |
| 11 | 2026-09-24 | T6 | mucoti, sefazi (Q-8) settled: final `open -> docs/graphviz-issues/19-flat-edge-ignores-html-table-port.md`. Not re-probed by T6: the prior mission's T15 ran a controlled experiment against real graphviz with the same DOT bytes (its row 53); verdicts unchanged since | Already filed; D1 does not require re-filing |
| 12 | 2026-09-24 | T6 | Diagnose-first mechanisms carried into fix tasks (not guessed): S-1b xumofu, S-11 rakuci, S-12 rojoxi (T7); CLIP-1 (T9); Q-3 (T11); Q-6, Q-10 (T13); R-2 (trace), R-5/6/7/9 (T17). S-12: T5 found the brief's rojoxi stderr/no-viewBox lead does not reproduce. No out-of-mission fixture was pulled in by any report | README "Out of mission" unchanged |
| 13 | 2026-09-24 | B1 | Wave 1 dispatched: T7 (sonnet) ∥ T9 in worktrees `.worktrees/plantuml-ts-cdd2-{T7,T9}` (branches `cdd2/T7`, `cdd2/T9` off `fe79c418d`; node_modules, packages/*/generated, oracle/dist, test-results CHILDREN symlinked). T9 routed to **opus** instead of the task file's sonnet | T9 reopens a mechanism two agents mis-attributed; it is a diagnosis before it is a fix (model-routing: multi-path reasoning) |
| 14 | 2026-09-24 | T9 | T9 stopped (stop 1), no commit: CLIP-1 found and probe-verified — (a) upstream applies the package cluster's magnetic border force `(0, htitle=20)` to an end after the compound clip (`SvekEdge.java:927-941`, `Cluster.java:726-757`, `USymbolFolder.java:242-266`; orchestrator re-read all three); (b) `DotPath#moveStartPoint`'s first-bezier drop (`DotPath.java:206-216`) is unported in `applyDecorTrim`. Temporary edits closed all six fixtures (0/0). Fix needs `layout.ts` + `renderer-arrowhead.ts`, in no batch-1 write-set. **Push-forward (re-assign fixtures to another task, write-sets stay disjoint):** CLIP-1's six fixtures moved to T12, which already owns `renderer-arrowhead.ts` and the identical `moveStartPoint` port (Q-2); T12 gains `layout.ts` (clusterRects), `class-shield-helpers.ts`, `core/spline-clip.ts`, `core/svek/Cluster.ts`; T11 ∥ T12 stays disjoint, T13 (also `layout.ts`) already follows T12. Artifact: `diagnosis/CLIP-1.md` | Extending T9's own write-set would be stop 1; moving the mechanism to the task owning the file is the listed push-forward. D2 unaffected (CLIP-1 is link geometry) |
| 15 | 2026-09-24 | T9 | Probe movers (both edits vs b0): lojiga 163→178 N (S 3→2), delasa-80-jusu462 (out of mission) 10664→10746 (S 21→15) — point counts fixed by the bezier drop expose pre-existing offsets as numerics (compareSvg non-monotonic reveal). Zero conformant losses. guxode's Δ0.014 numeric is NOT CLIP-1 (identical with/without probe) → open, left to T8/T10 residual | T9 report; to be re-measured at T14 |
| 16 | 2026-09-24 | T7 | T7 `de0b460e3` cherry-picked as `9980a7917`: S-2 (`AbstractClassOrObjectDiagram.java:134-135` parsed linkType for the couple join), S-4 line colour (`CommandLinkClass.java:368`), S-8 (creole strip + per-line bold, `Display.java:413-419`). Closed pibifa, begico, vuresa (probe); xoxuni 4/0→1/0. Mover outside list: nagega-30-poso418 diverged→structural-match (S 12→0, N 114 unchanged — same S-4 mechanism, `!define` macros expand to trailing `#color`; N identical pre/post via stash). All files inside T7 write-set | T7 report; `git show --stat` |
| 17 | 2026-09-24 | T7 | Five mechanisms fully diagnosed but outside every batch-1 write-set (stop 1 for any edit): **S-1** sugifi/sumule — upstream `AbstractEntityDiagram#packSomePackage` (`:85-106`, gated `ClassDiagram.java:84-85`) marks a single-child group `packed` AFTER uids are minted (`Entity.java:717-741`, skipped at `ClusterDotString.java:76-82`); we collapse at resolve time (`class-namespace-resolve.ts:418`) so one tick is never minted — needs `class-dot-clusters.ts` + `ast.ts`. **S-1b** xumofu — `CommandLinkClass.java:320-333` resolves both endpoints' quarks before creating either leaf; we resolve+create per endpoint — needs `class-ensure-classifier.ts` + `class-command-relationships.ts`. **S-11** rakuci — descriptive-container `[[url]]` never calls `setNamespaceUrl` — needs `class-command-containers.ts`. **S-12** rojoxi — `collapseEmptyNamespace` drops `ns.color`, `renderEmptyPackageLeaf` has no colour — needs `class-namespace.ts` + `renderer.ts`. **S-4t** xoxuni `;text:COLOR` — needs a `class-relationship-ast.ts` field. **Push-forward**: settled as `open -> next-missions` (complete artifacts, separable), filed in `planning/next-missions.md` at T10; not silently widening a write-set. **Flagged for review**: a maintainer may prefer a follow-up task inside this mission | Push-forward "file an out-of-mission mechanism once its artifact is complete and the fix is separable"; stop 1 forbids the edit; details `.agent-notes/cdd2-T7.md` |
| 18 | 2026-09-24 | T7b | T7b `0ae74919e` cherry-picked. R-8's premise corrected by the agent: `USymbolStack` was already ported (`core/decoration/symbol/USymbolStack.ts`) and dispatched generically; the gap was `class-container.ts#closeContainer` never stamping `Namespace.usymbol` for a non-empty container (upstream `CommandPackageWithUSymbol.java:197-198` stamps unconditionally). Plus stack's invisible inner `URectangle` ink (`USymbolStack.java:64-65`, `LimitFinder.java:108-188`). lojiga 3/160 → 1/0 (remaining = CLIP-1b, T12) | Agent first scoped the stamp to `stack` only; orchestrator sent it back — a special case upstream lacks (CLAUDE.md re-mirror rule) |
| 19 | 2026-09-24 | T7b | Risers from the unconditional stamp, both with mechanism: **xadado-92-lazo250** (accepted divergence) 1/344 → 0/352 — structural `g[1]/g[1] childCount 5 vs 3` CLOSED (USymbolComponent#asBig now drawn); numerics are a non-monotonic reveal keyed off the unmeasured embedded `{{ }}` note (`note-layout-measure-rows.ts` seam). **xenere-07-kuji864** (stretch X) 6/196 → 8/212 — the rectangle USymbol path is now reached, and it too lacks the cluster header's displayed stereotype + per-container legend (`ClusterHeader.java:173-180` `getStereoBlock`; `Entity.java:101,551-557`; `CommandPackageWithUSymbol.java:204-206`); the same childCount symptom pre-exists on its folder `pack1`, untouched. Owner: batch-5/T18 pair spec (xenere/sijoba) — needs `class-namespace-usymbol-shape.ts`, `ast.ts`, `parser.ts` | D4: every riser has a mechanism; not a conformant loss |
| 20 | 2026-09-24 | T8 | T8 `2890fe02f` cherry-picked as `7c9f8b4a6` (catalog conflict with T7b resolved by regenerating `docs/catalog.md`). Closed fumalu (S-5, `EntityImageClassHeader.java:93-101`), tuguku (S-7, `VisibilityModifier.java:336-350`, new `core/style-cascade-visibility-icon.ts`), nesivu (S-10, `SkinParam.java:1092`), nisune (S-13, `HColorSimple.java:211-214` opposite(), resolved at render time); gabejo 4/2→0/2 (S-3 via `FromSkinparamToStyle.java:183,187,292-302,396-408` — corrects S.md's `SkinParam#getHtmlColor` citation, unreachable). Split `theme-graph-colors-c.ts` re-exported from `theme-graph-colors.ts` (pre-authorised 500-line split). Object engine: 8 fixtures improved (kagope, kavako, tujasu, zuvila, tusiri, meloxo, guzojo, nitica), shared class renderer, agent-verified vs `parity-object.json`; re-measured in row 23 | T8 report |
| 21 | 2026-09-24 | T8 | **S-6 DISPROVED by T8**: swapping the handler's `color`→`paint` arg is a no-op (`resolveColor('White')` ≡ `resolveColorPaint('White')`, probed). Real mechanism: `class-namespace-folder-outline.ts#renderFolderPolygon` (strictuml sharp-corner folder) emits `stroke` via `shortenColor` with no colour resolution, unlike its `path()`/`line()` siblings. File outside the batch-1 union write-set → guxode `open -> next-missions`. guxode Δ0.014 g[14] also open (not CLIP-1, row 15) | T8 report; stop 1 forbids the edit |
| 22 | 2026-09-24 | T10 | Residual round on merged tree: pibifa, begico, vuresa, fumalu, tuguku, nesivu, nisune 0/0; sugifi 1/0, sumule 1/0, xumofu 3/0, xoxuni 1/0, rakuci 2/0, rojoxi 1/0 (row 17, filed); lojiga 1/0 (CLIP-1b → T12); gabejo 0/2 (Δ1 canvas height, no mechanism → re-assigned to T17, batch 4's canvas task, as a diagnose-first item); guxode 2/2 (row 21). No residual has a stated mechanism inside the union write-set → no residual commit | close-procedure step 1 |
| 23 | 2026-09-24 | T10 | Brief defect: close-procedure step 5 omits `--json`, so `svg-conformance-census.ts class` printed buckets but left `census-class.json` at 2026-09-23/`40bc2ebcf`; the stale census made all 7 new conformants look non-zero. Re-run with `--json tests/oracle/svg-conformance/census-class.json` (step 5 amended in `close-procedure.md`). Also: my T0/T6 `fixtures.md` edits wrote `mechanism` into the `final` column and `final` into an extra column (off-by-one split index) — repaired all 62 rows in this close | Measurement-instrument check before trusting the D3 gate |
| 24 | 2026-09-24 | T10 | Ratchet: 7 pins (`source: cdd2-b1`): begico, fumalu, nesivu, nisune, pibifa, tuguku, vuresa — each survey-conformant AND census 0-diff, `golden.svg`/`in.puml` `cmp`-identical to the cache. Appended, NOT re-sorted: the tamper test (`class.golden.ratchet.test.ts:137`) mutates `fixtures[0]`'s first integer `rect x`, and a localeCompare sort put `bajotu` (no integer rect x) first → a self-inflicted red, reverted | D3 |
| 25 | 2026-09-24 | T10 | Other-engine survey (step 9, all 23 engines re-surveyed to `/tmp/cdd2-b1-eng/`): vs committed pins, activity 1, c4 2, component 3, object 9, sequence 1, state 3, unknown 97, usecase 5 movers (incl. conformant losses in unknown: gemepu, kupofu, ridofi; jititi dotEqual true→false). **Instrument check:** the same 8 engines surveyed on the baseline `18a8e0c50` (worktree `.worktrees/plantuml-ts-cdd2-base`, `/tmp/cdd2-b0-eng/`) show ALL of those movers already present before this mission — the committed pins were stale at mission start (not this mission's to re-pin; close write-set pins class only). Baseline→b1 movers: **5**, all improvements: object kiluja-96-pado371 diverged→structural-match, unknown naluge-13-mefo624 diverged→conformant, vovipa-79-gapu674 diverged→structural-match, zeluge-00-cofi214 diverged→structural-match (non-empty `frame`/`database`/`rectangle` containers now reach their USymbol — T7b's unconditional `usymbol` stamp), unknown padefe-05-bemu901 diverged→conformant (`!define`-expanded trailing `#Indigo` on a link — T7 S-4). T8's "8 object improvements" were measured against the stale pin and are NOT batch-1 movers (all 8 already at baseline). Stale-pin finding filed for T20 → next-missions. 5 ≤ 20 (stop 13 clear) | Memory "measurement artifacts outnumber defects"; stop 4 satisfied by mechanism |
| 26 | 2026-09-24 | T10 | **Batch 1 closed: 560/86/77 → 567/89/67** (survey = render-all b1). 11 class transitions (b0→b1): 7 → conformant (begico, fumalu, nesivu, nisune, pibifa, tuguku, vuresa), gabejo/nagega → structural-match, xadado → structural-match + count 345→352 (row 19, reveal), xenere count 202→220 (row 19, mechanism). 0 dotEqual flips, 0 conformant losses. Census 0-diff 560→567; ratchet 560→567. Gates green, 807/807 files collected | close-procedure |
| 27 | 2026-09-24 | T10 | Routing/refusal baselines: 7 `goldens:svg-class` rows appended to each (clones of their `dot-cache/class` twins: all `agree` / `ok`), `$comment` extended; gate counts refusal 5554→5561 & rendering 5285→5292, routing agree 4402→4409 & manifest 5554→5561, each with a derivation comment. Full suite re-run after pinning: 807/807 files, 0 fail | close-procedure step 10 |
| 28 | 2026-09-24 | B2 | Wave 1 dispatched: T11 ∥ T12 (both opus, per task files) in worktrees off `41bc2628c`. Other-engine comparisons in batch 2 use the b1-tree surveys at `/tmp/cdd2-b1-eng/` as baseline, not the stale committed pins (row 25) | Instrument choice |
| 29 | 2026-09-24 | T11 | T11 `43e7b426c` cherry-picked. **Q-1**: `EntityImageClass.java:100-113,182,238` + `HeaderLayout.java:81-117` — width floors now re-measure the classifier at the final width (`widenMeasuredClassifier`, WeakMap recipe keyed by the measured object to stay inside the write-set). **Q-3** (diagnosed, was LOW): the jar reads a shielded node's corner from graphviz's `PORT="h"` cell polygon (`DotStringFactory.java:390-396`); graphviz truncates HTML `WIDTH`/`HEIGHT` to integers (`htmllex.c:374-382 widthfn`, `doInt` `strtol`) and centres the integer table (`htmltable.c:1914-1917`), so `WIDTH="72.995"` → 72-wide cell at x=8; `shieldCorner` used 72.995 → 7.5025, and the diagram-wide origin shift moved every other node +0.4975. New `htmlCellSize`. T11 fixtures: 8 → 0/1 (only Q-2 left), coxose 249 → 48 (4 Q-2 + 44 on `minlen=0` port edges = dot-engine issue 19, Q-8 family). Movers (all falls): dorafa-63-soba922 → conformant (Q-1 via `applySameClassWidthFloor`; stale "not wired" comment corrected), camuna 147→22, nafiki 152→28, rifuzu 151→27, goloxu 135→14, rilali 181→16, xoxega 191→16, ririlu 449→63, vuzoro 51→10, mucoti 94→12, sefazi 53→12. Non-class: object fonulu-92-libi014 maxDelta 107.599→106.674 (still diverged); state 0. Probe total 568/88/67. DOT parity 721/721 tests | T11 report |
