# Decision journal

| # | When | Task | Decision | Why | Flag for review? |
|---|------|------|----------|-----|------------------|
| 0 | 2026-09-20 | plan | Baseline at `19b0d90b` (main, clean): 735 test files / 21204 tests, coverage 96.14/91.58/97.27/97.11; routing 3424/987/73 over 4484 (censused 986); refusal 4484/73/237/4247, gaps 133, non-activity defect exactly nuvoja; typecheck/lint/build 0. `.claude/settings.autonomous.json` kept as the repo's tailored copy (differs from the template on purpose; permits `npm`, `npx`, `scripts/oracle-render.sh`). `plans/` is committed in this repo, not gitignored. | Phase 8 pre-flight | no |
| 1 | 2026-09-20 | batch-0 | Branch `feat/unknown-bucket-routing-repair` cut at `19b0d90b`; brief committed as `12de2621`. Worktrees `.claude/worktrees/ubrr-T{0,1}` on branches `ubrr/T{0,1}` via `scratchpad/mk-worktree.sh`: links `node_modules`, `assets/stdlib`, `oracle/dist`, `tests/corpus`, `packages/*/{assets,generated}` and the CHILDREN of `test-results/` (never the dir; `dot-cache/` is committed and real). Task commits land on task branches and are cherry-picked onto the mission branch in T-order. | [[batch-parallelism-needs-worktrees]] + its 2026-09-20 addendum | no |
| 2 | 2026-09-20 | batch-0 gate | T0 `56419953` (split `scripts/pin-corpus-tree-measure.ts` out, 452-line main file, 24 tests), T1 `9810bee1` (668 rows, every brief citation verified against the Java, zero corrections). Gates on `9810bee1`: `npm test` 736 files (735+1) / 21228 tests / coverage 96.14/91.58/97.27/97.11, 83.8 s wall; typecheck 0; lint 0; build 0. `pin-corpus-tree.ts unknown --tree <parked> --ledger <dir> --dry`: 538 agree/ok, 26 jar-error, 8 files-untyped-root, 96 no-engine (20 of them known-gap), `unpinned 157` — exactly batch 1's rows. Write-set diff = the 4 declared files. Batch 0 CLOSED. | T0's `--dry` on an EMPTY ledger reports 261 (= 226 misroutes + 35 NONE==NONE refusals); the brief's 157 assumes T1 present — not a defect | no |
| 3 | 2026-09-20 | batch-0 | T0 resolved two spec gaps its own way: `--dry` only COUNTS a defect with no usable reason (`unpinned`) and never throws, while a malformed reason or a slug in two fragments throws even under `--dry`; a `fixed` ledger row that still measures as a disagreement is treated as unpinned rather than silently pinned. Both accepted: batch 1 needs `--dry` to enumerate its work, and the two throws are ledger integrity, not adjudication state. | judgment call on the tool's contract | no |
| 4 | 2026-09-20 | batch-1 | Batch 1 dispatched: T2–T6 on `ubrr/T{2..6}` worktrees, read-only on `src/`. T13 (batch 2, no dependency, write-set disjoint from every batch-1 task) dispatched in parallel on `ubrr/T13` to overlap wall-clock; it is cherry-picked with batch 2 and gated there, not with batch 1. | wall-clock; the brief's batch is a dependency statement, not a scheduling constraint | no |
| 5 | 2026-09-20 | batch-0 fix | `fix(ubrr-T0)` `4595d482`: the generator folded `fix-candidate` rows into `unpinned`, so batch 1's gate (`unpinned: 0`, fix-candidates listed) and batch 2's (zero fix-candidates) were not both checkable from one counter. `--dry` now prints `fix-candidate N` and `unpinned N` (no usable row) on separate lines; the real-run refusal is unchanged. All five batch-1 agents flagged the same contradiction independently and none mis-classified a row to satisfy AC5 as worded. | tool contract, not adjudication | no |
| 6 | 2026-09-20 | batch-1 gate | T2 `2ed5a5a4`, T3 `e2a51c91`, T4 `65f57bb9`, T5 `a5c7f6d9`, T6 `f4685b33`. `--dry` over the parked tree: 825 measured, `unpinned 0`, `fix-candidate 116` (class 66, activity 34, sequence 9, description 6, state 1), pins 709 (T1's 668 + 1 activity-legacy-arrow + 1 class-generic-link + 3 description gaps + 1 preprocessor-function + 35 jar-NONE rows). Gates on `f4685b33`: `npm test` 736 files / 21229 tests / coverage 96.14/91.58/97.27/97.11, 81.8 s; typecheck 0; lint 0; build 0. Write-set diff = the ten declared files. Batch 1 CLOSED. | — | no |
| 7 | 2026-09-20 | batch-1 | PREMISE DISPROVED (T2): none of the 25 `activity-legacy1-example-*` sources use the legacy `(*) -->` syntax (`grep -cE '\(\*\)\|-->'` = 0 for all 25); they are activity3 constructs with port gaps (bullet lists 12, switch/case 5, `backward:` 4, if-family spellings 4, `end while` 2, partition/group 2 + 5). The one true legacy-arrow fixture (`bitabu-34-lota947`) is the ACTIVITY→STATE over-claim, pinned `CommandLinkActivity.java:73` / `ActivityDiagramFactory.java:96`. The brief's expected "25 pins" became 34 activity fix-candidates. | verify-not-skip; `.agent-notes/unknown-bucket-mapping.md` inherited the premise from the cohort name | yes — the note's premise line is wrong; T15 corrects it |
| 8 | 2026-09-20 | batch-1 | MEASUREMENT FINDING (T6): 12 jar goldens are genuine `PSystemError` pages whose signature sits past byte 4096 (offsets 5362–7625) because `PSystemError.java:213-231` prepends the Welcome tutorial (+ a `currentTimeMillis`-gated ad). Both gates and the generator read only the first 4096 bytes (`HEAD_BYTES`), so they read `jarErrored=false`. Decision: the generator keeps 4096 — its contract is to reproduce the gates EXACTLY, and widening the window would re-derive existing rows (stop 4/6 exposure); the 12 are pinned `known-gap` (cohort `jar-error-beyond-head-window`) with the mechanism in the reason. Filed as a follow-on for T15 (`next-missions.md`): widen `readHead` in both gates + generator together, then re-derive. | consistency with the instrument over correctness of the instrument, this mission | yes |
| 9 | 2026-09-20 | batch-1 | T13 landed early on `ubrr/T13` `87854b46` (row 4); held for the batch-2 cherry-pick. | — | no |
| 10 | 2026-09-20 | HALT | STOP 8 (cross-seam) raised by T2: 5 jar-ACTIVITY fixtures (`bezogu-47-vevu307`, `jutesi-96-jemo224`, `legoda-27-cedo414`, `rurufo-83-sadi254`, `tivado-46-vete823`) render DESCRIPTION here because description accepts `rectangle <$sprite{…}>` as a bare display name (no `DISPLAY_WITHOUT_QUOTE` arm matches a leading `<`, `CommandCreateElementFull`); upstream falls through to activity3's `CommandPartition3.java` (catch-all NAME), whose golden is the "You should use a bracket" banner. Narrowing description alone lands them on NONE (a third engine, stop 5); porting `CommandPartition3` alone never runs because description claims first. Both seams at once = stop 8. Also unresolved at this halt: three mechanisms exceed D2(c)'s ~60 NLOC bar by the diagnosing agents' estimates — T2 M2 switch/case/endswitch ≈120 (5 fixtures), T2 M6 `CommandPartition3` ≈90 with an open layout question (2 + the 5 above), T4 A `CommandCreateElementMultilines` wiring ≈90 (37 fixtures, the command is fully ported in description). Batch 2 not dispatched. Orchestrator's recommendation recorded in the session report. | stop 8 is a listed stop condition; the size-bar rulings decide 49 of 116 candidates | yes |
