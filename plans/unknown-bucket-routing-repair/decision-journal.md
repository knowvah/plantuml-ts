# Decision journal

| # | When | Task | Decision | Why | Flag for review? |
|---|------|------|----------|-----|------------------|
| 0 | 2026-09-20 | plan | Baseline at `19b0d90b` (main, clean): 735 test files / 21204 tests, coverage 96.14/91.58/97.27/97.11; routing 3424/987/73 over 4484 (censused 986); refusal 4484/73/237/4247, gaps 133, non-activity defect exactly nuvoja; typecheck/lint/build 0. `.claude/settings.autonomous.json` kept as the repo's tailored copy (differs from the template on purpose; permits `npm`, `npx`, `scripts/oracle-render.sh`). `plans/` is committed in this repo, not gitignored. | Phase 8 pre-flight | no |
| 1 | 2026-09-20 | batch-0 | Branch `feat/unknown-bucket-routing-repair` cut at `19b0d90b`; brief committed as `12de2621`. Worktrees `.claude/worktrees/ubrr-T{0,1}` on branches `ubrr/T{0,1}` via `scratchpad/mk-worktree.sh`: links `node_modules`, `assets/stdlib`, `oracle/dist`, `tests/corpus`, `packages/*/{assets,generated}` and the CHILDREN of `test-results/` (never the dir; `dot-cache/` is committed and real). Task commits land on task branches and are cherry-picked onto the mission branch in T-order. | [[batch-parallelism-needs-worktrees]] + its 2026-09-20 addendum | no |
| 2 | 2026-09-20 | batch-0 gate | T0 `56419953` (split `scripts/pin-corpus-tree-measure.ts` out, 452-line main file, 24 tests), T1 `9810bee1` (668 rows, every brief citation verified against the Java, zero corrections). Gates on `9810bee1`: `npm test` 736 files (735+1) / 21228 tests / coverage 96.14/91.58/97.27/97.11, 83.8 s wall; typecheck 0; lint 0; build 0. `pin-corpus-tree.ts unknown --tree <parked> --ledger <dir> --dry`: 538 agree/ok, 26 jar-error, 8 files-untyped-root, 96 no-engine (20 of them known-gap), `unpinned 157` — exactly batch 1's rows. Write-set diff = the 4 declared files. Batch 0 CLOSED. | T0's `--dry` on an EMPTY ledger reports 261 (= 226 misroutes + 35 NONE==NONE refusals); the brief's 157 assumes T1 present — not a defect | no |
| 3 | 2026-09-20 | batch-0 | T0 resolved two spec gaps its own way: `--dry` only COUNTS a defect with no usable reason (`unpinned`) and never throws, while a malformed reason or a slug in two fragments throws even under `--dry`; a `fixed` ledger row that still measures as a disagreement is treated as unpinned rather than silently pinned. Both accepted: batch 1 needs `--dry` to enumerate its work, and the two throws are ledger integrity, not adjudication state. | judgment call on the tool's contract | no |
| 4 | 2026-09-20 | batch-1 | Batch 1 dispatched: T2–T6 on `ubrr/T{2..6}` worktrees, read-only on `src/`. T13 (batch 2, no dependency, write-set disjoint from every batch-1 task) dispatched in parallel on `ubrr/T13` to overlap wall-clock; it is cherry-picked with batch 2 and gated there, not with batch 1. | wall-clock; the brief's batch is a dependency statement, not a scheduling constraint | no |
| 5 | 2026-09-20 | batch-0 fix | `fix(ubrr-T0)` `4595d482`: the generator folded `fix-candidate` rows into `unpinned`, so batch 1's gate (`unpinned: 0`, fix-candidates listed) and batch 2's (zero fix-candidates) were not both checkable from one counter. `--dry` now prints `fix-candidate N` and `unpinned N` (no usable row) on separate lines; the real-run refusal is unchanged. All five batch-1 agents flagged the same contradiction independently and none mis-classified a row to satisfy AC5 as worded. | tool contract, not adjudication | no |
| 6 | 2026-09-20 | batch-1 gate | T2 `2ed5a5a4`, T3 `e2a51c91`, T4 `65f57bb9`, T5 `a5c7f6d9`, T6 `f4685b33`. `--dry` over the parked tree: 825 measured, `unpinned 0`, `fix-candidate 116` (class 66, activity 34, sequence 9, description 6, state 1), pins 709 (T1's 668 + 1 activity-legacy-arrow + 1 class-generic-link + 3 description gaps + 1 preprocessor-function + 35 jar-NONE rows). Gates on `f4685b33`: `npm test` 736 files / 21229 tests / coverage 96.14/91.58/97.27/97.11, 81.8 s; typecheck 0; lint 0; build 0. Write-set diff = the ten declared files. Batch 1 CLOSED. | — | no |
| 7 | 2026-09-20 | batch-1 | PREMISE DISPROVED (T2): none of the 25 `activity-legacy1-example-*` sources use the legacy `(*) -->` syntax (`grep -cE '\(\*\)\|-->'` = 0 for all 25); they are activity3 constructs with port gaps (bullet lists 12, switch/case 5, `backward:` 4, if-family spellings 4, `end while` 2, partition/group 2 + 5). The one true legacy-arrow fixture (`bitabu-34-lota947`) is the ACTIVITY→STATE over-claim, pinned `CommandLinkActivity.java:73` / `ActivityDiagramFactory.java:96`. The brief's expected "25 pins" became 34 activity fix-candidates. | verify-not-skip; `.agent-notes/unknown-bucket-mapping.md` inherited the premise from the cohort name | yes — the note's premise line is wrong; T15 corrects it |
| 8 | 2026-09-20 | batch-1 | MEASUREMENT FINDING (T6): 12 jar goldens are genuine `PSystemError` pages whose signature sits past byte 4096 (offsets 5362–7625) because `PSystemError.java:213-231` prepends the Welcome tutorial (+ a `currentTimeMillis`-gated ad). Both gates and the generator read only the first 4096 bytes (`HEAD_BYTES`), so they read `jarErrored=false`. Decision: the generator keeps 4096 — its contract is to reproduce the gates EXACTLY, and widening the window would re-derive existing rows (stop 4/6 exposure); the 12 are pinned `known-gap` (cohort `jar-error-beyond-head-window`) with the mechanism in the reason. Filed as a follow-on for T15 (`next-missions.md`): widen `readHead` in both gates + generator together, then re-derive. | consistency with the instrument over correctness of the instrument, this mission | yes |
| 9 | 2026-09-20 | batch-1 | T13 landed early on `ubrr/T13` `87854b46` (row 4); held for the batch-2 cherry-pick. | — | no |
| 10 | 2026-09-20 | HALT | STOP 8 (cross-seam) raised by T2: 5 jar-ACTIVITY fixtures (`bezogu-47-vevu307`, `jutesi-96-jemo224`, `legoda-27-cedo414`, `rurufo-83-sadi254`, `tivado-46-vete823`) render DESCRIPTION here because description accepts `rectangle <$sprite{…}>` as a bare display name (no `DISPLAY_WITHOUT_QUOTE` arm matches a leading `<`, `CommandCreateElementFull`); upstream falls through to activity3's `CommandPartition3.java` (catch-all NAME), whose golden is the "You should use a bracket" banner. Narrowing description alone lands them on NONE (a third engine, stop 5); porting `CommandPartition3` alone never runs because description claims first. Both seams at once = stop 8. Also unresolved at this halt: three mechanisms exceed D2(c)'s ~60 NLOC bar by the diagnosing agents' estimates — T2 M2 switch/case/endswitch ≈120 (5 fixtures), T2 M6 `CommandPartition3` ≈90 with an open layout question (2 + the 5 above), T4 A `CommandCreateElementMultilines` wiring ≈90 (37 fixtures, the command is fully ported in description). Batch 2 not dispatched. Orchestrator's recommendation recorded in the session report. | stop 8 is a listed stop condition; the size-bar rulings decide 49 of 116 candidates | yes |
| 11 | 2026-09-20 | batch-2 plan | USER RULINGS on the halt (row 10), recorded as D11 (cross-seam: fix both seams, judge landing after both are on the branch) and D12 (no size bar on a faithful port; split long Java into hook-sized functions). Fragments split by seam before dispatch (D4, one writer per file): `T2-class.json` (3), `T3-description.json` (1), `T4-sequence.json` (3), `T5-class.json` (3), `T5-sequence.json` (6); the originals keep their other rows. `--dry` unchanged after the split: 825 measured, `fix-candidate 116`, `unpinned 0`. Seam assignment: T7 class 66 (T2 3, T3 17, T4 43, T5 3); T8 description 6 (T3 1, T5 5) + the D11 narrowing of `rectangle <$sprite{…}>` acceptance; T9 state 1 (T6); T10 activity 34 (T2, incl. the 5 cross-seam rows); T11 sequence 9 (T4 3, T5 6); T12 block-extractor 0 rows — SKIPPED. Order: T7, T8, T9, T11 in parallel now; T10 dispatched from the branch AFTER T8 is cherry-picked, so its worktree measures the 5 cross-seam rows with description already narrowed (D3 landing verifiable inside the task). T13 already done. | user decision; D11/D12 are amendments, flagged | yes |
| 12 | 2026-09-20 | batch-2 | T9 STOPPED (stops 4/6/8): its correct narrowing of the state parser (`Command.confirm` before a rule-16 `/[<>]/` match counts as claimed, mirroring `PSystemCommandFactory.java:169-175`) unmasked 5 EXISTING fixtures that STATE had been drawing as empty diagrams: sequence `bomino-39-tipo216` (`CommandStyleImport`), `dolice-60-copi767` (note-on-arrow URL group), `fojomu-60-cuda302` + `zoturo-25-jima978` (`CommandLinkAnchor`), timing `zeboxu-77-xola056` (no engine). USER RULING: re-pin the five honestly. Orchestrator re-pinned them in the same commit as the cherry-pick (routing STATE→NONE, refusal ok→known-gap, reasons name the missing Command and the unmasking), re-derived the refusal gate (237→242 erroring, 4247→4242 rendering, gaps 133→138; routing 3424/987/73 unchanged), regenerated the dashboard (D10). Only the five rows changed (verified row-by-row against HEAD). The three sequence ports + the timing engine are filed for T15 as follow-ons. | user decision amending stops 4/6 for these five | yes |
| 13 | 2026-09-20 | batch-2 | T8 `47e098ce`: 5 title-trim rows fixed; `rozugu-82-pera583` refusal fixed but lands DESCRIPTION vs jar CLASS because class has no `CommandCreateElementMultilines` port — the very command T7 is porting (T4 mechanism A). T8 set it `known-misroute`; the orchestrator re-measures it after T7 merges and flips it to `fixed` if it lands CLASS. T8 also measured the 5 cross-seam rows landing STATE (not NONE) after the description narrowing — state's bare-identifier fallback claimed them; with T9's confirm fix on the branch they should refuse, and T10 reclaims them. | D11 intermediate landings are not stop 5 | no |
| 14 | 2026-09-20 | batch-2 | T11 first commit `30eb2901`: 8/9 fixed (3 T4 over-claims land CLASS, 4 note-participant rows land SEQUENCE, `pacope` lands DESCRIPTION); three pre-existing sequence bugs found and fixed narrowly on the way (participant dispatch pattern swallowing arrow lines, `as` case-insensitivity, colour grammar), two ratchet pins IMPROVED (`seloli` 197→118, `xeroce` 35→23), zero rises. `teoz-ng-001-17` left open as "needs a broader model" — sent back under D12 for a second commit (port `SequenceDiagram.java:154-158` `getLastEventWithNote`). T11 reports `docs/catalog.md` drifts (four new modules): the orchestrator runs `npm run catalog` at the batch-2 merge, not the task. T11 also tripped the Serena-edits-main-checkout hazard once, caught it itself, reverted; main checkout verified clean by the orchestrator. | D12; catalog is a generated merge-time artifact | no |
| 15 | 2026-09-20 | batch-2 | T7 `93644955`: 63/66 fixed (T4 A 37, B 4, C 1, D 1; T3 M1–M5 17; T5 nicere/xipane/vagipu 3 — the last three land DESCRIPTION, their jar type). Six new `ClassifierKind` members (struct/exception/metaclass/stereotype/dataclass/record) supersede `dispatch-by-parse-attempt`'s deliberate non-port (D12). Three split-out modules for the 500-line hook; one carries a circular import with `class-arrow-grammar.ts` (runtime-verified) — flag for review. Two latent defects fixed en route (multiline-element regex stealing `interface X ##color` from the native declaration; bare `0` CIRCLE decor missing from the kind maps). The 3 T2 M8 rows (`coruvi`, `nezaxe`, `tiseze`: class now refuses "Nothing to note to", `CommandFactoryNoteOnEntity.java:293-303`) land DESCRIPTION in T7's pre-T8 worktree — description registers the same command upstream and must refuse too; re-measure after the merge and, if still DESCRIPTION, a small description follow-up commit is required (D11). | D11/D12 | yes (circular import) |
| 16 | 2026-09-20 | batch-2 | T7 cherry-picked as `4f02f458`; re-measured on the branch (T7+T8+T9): `rozugu-82-pera583` still DESCRIPTION (class still refuses — its multiline body closes on the nested `]` inside `{{ }}`, the class twin of T8's part 2) and `coruvi`/`nezaxe`/`tiseze` land DESCRIPTION (description does not refuse a positioned note with nothing to note to; upstream's `CommandFactoryNoteOnEntity.java:293-303` is registered for description as well). Two follow-up agents dispatched off `4f02f458` in parallel with T10/T11: T7b (`src/diagrams/class/**`) and T8b (`src/diagrams/description/**`); neither edits a ledger fragment — the orchestrator flips `rozugu` (T3-description.json) and the three T2-class.json rows at merge from measurement. | D11: both seams; fix at the mechanism's origin | no |
| 17 | 2026-09-20 | batch-2 | T11 second commit `3dce80d0` (branch `3f60aeb4`): `teoz-ng-001-17` lands SEQUENCE after porting `getLastEventWithNote` (`SequenceDiagram.java:154-158`; AbstractMessage/Reference/GroupingLeaf anchors). T11 9/9 fixed. One `[FIXED]` retirement: `sequence/recani-60-licu962` (multi-line `rnote` body now parses). Orchestrator re-pinned via the D5 instruments (`sequence-ratchet-adjudicate.ts --snapshot` at HEAD, then `repin-sequence-baselines.ts`): 14 rows — recani error→baseline 182 + routing agree + refusal ok, and 11 sequence pins FELL (bagexe 228→179, biruge 174→118, bulixe 171→92, butosi 104→68, gifope 163→110, gucare 650→573, h-rnote-style 171→92, kotixe 171→92, note-color 197→118, seloli 197→118, xeroce 35→23); ZERO rises (diffed row-by-row against HEAD). Gates re-derived: routing 3425/986/73, censused 985; refusal 241 erroring / 4243 rendering / 137 gaps. Dashboard regenerated (D10). T7b `409a1e8d` + ledger `6fe9e37c`: rozugu lands CLASS. | [[repin-script-raises-preexisting-red-pin]] check done | no |
| 18 | 2026-09-20 | batch-2 | T10 `6a042495` (cherry-picked `a11418a1`): 34/34 activity rows land ACTIVITY, including the 5 D11 cross-seam rows (description narrowed by T8, `CommandPartition3` ported here). 43 `[FIXED]` retirements in the EXISTING activity tree (routing known-misroute→agree, refusal honest-record `weErrored` true→false) and 145 activity-baseline rows recorded `error` that now render (43 each in diff/style/text, 16 swimlane). `repin-activity-baselines.ts` had no error→baseline path (it re-measures `baseline` rows only), so the orchestrator added one — `scripts/repin-activity-promote.ts` (pure, unit-tested) + `repin-activity-promote-run.ts` (the four live measurers on the gates' own seams) + `repin-activity-style-census.ts` (extracted for the 500-line cap) — then ran `--write`: 145 promotions, 0 rises. Gate rows retired with a scratch script from fresh measurement. Counts re-derived: routing 3468/943/73 (censused 942); refusal 198 erroring / 4286 rendering / 137 gaps, activity queue 82→39, non-activity defect still exactly nuvoja; activity partitions 311/39/23 and swimlane 76/8/8. Activity ratchet Σ 51390→61142 is 43 newly-scored fixtures ([[weightedscore-antimonotone-under-growth]]), not a rise. Catalog + dashboard regenerated (D10). T10's documented rendering divergences (switch reuses `GtileSwitch` not `GtileIfHexagon`; `backward:` parsed but no return-edge geometry; bracket-less partition warning banner not drawn; package/rectangle/card share `GtilePartition`) are follow-ons for T15. | tool gap found by the batch, fixed at its origin | yes (rendering follow-ons) |
