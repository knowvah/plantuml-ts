# Diagnosis notes

Written by batch 1 (T2–T6), one file per task: a table mechanism → count →
upstream cite → our `file:line` → verdict → seam, then one paragraph per
mechanism with the exemplar's refusing line.
