# Decision journal — sequence-creole

Append a row for every non-trivial judgement call: if a reasonable developer
might have chosen differently, log it. Include the mechanism, not just the
choice.

| # | when | decision | why |
|---|---|---|---|
| 1 | 2026-09-02, startup | Cut `feat/sequence-creole` from `feat/sequence-text-and-y-convergence` HEAD (`ebf50d6f`), not from `main`. | The README's branch rule presumes the parent has merged; it has not (parent is halted at batch 4, stop condition 9, unmerged). The mission's starting condition — `descended = 797`, and the census's 440 creole mismatches — was measured at `984da6fe`, a commit on the parent branch. Cutting from `main` would discard the parent's Phase A/B work, so every number in the brief would be unreproducible and stop condition 5 would fire immediately. Merging back stays a merge commit. |
| 2 | 2026-09-02, startup | Execution plan: batch 1 (C1) alone -> batch 2 (C2) alone -> batch 3 (C3 C4 C5 C6) in parallel, write-set-disjoint by C1's type widening -> batch 4 (C7). | The brief's own batching; recorded here per `rules/parallelism.md`, which in autonomous mode logs the plan rather than presenting it. |
| 3 | C1, `f125c3f7` | `TextRun` gained `fontFamily?` and `fontSize?` beyond the brief's five contract fields. | The width table is family-agnostic (`WidthTableMeasurer`), so C1's own `""mono""` acceptance criterion — "whose font is the monospace family" — is unobservable unless the run carries the family, and a `<size:N>` run is unrenderable without the size. Both optional and absent on every pre-creole run, so no existing producer changed. `fontSize` scales in `scaleRun`: `SvgGraphics#format` multiplies an emitted `font-size` by the scale (`SvgGraphics.java:693`), which is why `scaleSequenceTheme` already scales `theme.fontSize`. |
| 4 | C1, `f125c3f7` | A non-`'text'` atom (`'inline'`/`'emoji'`/`'latex'`) produces NO `TextRun` but still advances x by its own measured width. | A `TextRun` is a `<text>` and an image is an `<image>`; sequence geometry has no kind for one, and adding it needs the renderer files C3–C6 own. Advancing keeps the surrounding text runs where the jar puts them instead of sliding them left over the gap. Recorded as a follow-on in `.agent-notes/C1-sequence-creole-seam.md`, per the mission's "atoms with no measured reach" non-goal. |
| 5 | C1, `f125c3f7` | A `HORIZONTAL_LINE` line (bare `----`) falls back to one literal text run rather than dropping the string. | `buildLineAtoms` returns zero atoms for it — upstream draws a `CreoleHorizontalLine` rule instead — and sequence has no rule geometry. The fallback mirrors `class-member-creole.ts#buildMemberAtoms`: a remainder loses a rule, never a string. |
| 6 | C1 gate, `f125c3f7` | C1's "nothing moves" gate verified by the orchestrator, not only the agent. | Cohort line byte-identical to the pre-task baseline: `fixtures=1141 measured=1124 errored=17 descended=797 shortCircuited=327  distance=3855017.293 numericDiffs=60215`. All four gates green; the only red file is the inherited `sequence.diff-baseline.ratchet.test.ts` (93 failures), which the parent mission owns and this mission never re-pins. |
| 7 | C2, `e9d1b4df` | The real-newline `split('\n')` in `messageLabelBlock` STAYS, composed with the escape split. | `Display#getWithNewlines` has no real-newline branch — upstream's `Display` is already a `List<CharSequence>` — but this port's parser lowers the escape to a real newline at `command-note-factory.ts:119` / `command-misc.ts:89` and joins the block-form `ref`/`note` with one at `parser.ts:122`. Dropping the outer split would collapse every multi-line `ref` body and block-form note to one line. |
| 8 | C2, `e9d1b4df` | Sequence does NOT surface the JAWS deprecation warning. | Upstream would (`Display.java:348-353`) with a live `Pragma`, but this port splits at LAYOUT time where no `Pragma` is reachable — the reason `splitDisplayLines` passes `Pragma.createEmpty()` — and emission is gated on `PragmaKey.SHOW_DEPRECATION` with nothing in `src/` reading `Pragma#getWarnings()` back out. A live `Pragma` would render identically today. |
| 9 | C2, `e9d1b4df` | Participant STEREOTYPES do not split, contradicting the task text. | `CommandParticipant.java:174-180` hands the raw stereotype run to `Stereotype.build`, which constructs no `Display` and never reaches `getWithNewlines`. The Java is authoritative over the brief's prose. |
| 10 | C2 close, orchestrator | Regenerated `oracle/goldens/svg-sequence/diff-census.json`, a file no task owns. | It is a CENSUS, not a ratchet: its own header defines regeneration as the maintained workflow and it carries no never-rises semantics. The regeneration was inspected fixture by fixture before committing — ~65 entries moved `missing-element: 1 -> 0` and gained real diffs, which IS the C2 payoff (a subtree that cost 1 unexamined is now compared). This is the brief's "a test constant that moves for a stated, measured reason". The sequence RATCHET was not re-pinned and is not this mission's. |
| 11 | C2 close, orchestrator | The two fixtures read as C2 "overshoots" are not overshoots, and the finding reverses the agent's own reading. | Measured, not inferred: on `kenilu-88-javu563` and `xiceso-64-pelu456` our `<text>` multiset is now identical to the jar's in order, and the entire surplus is ONE element — a full-canvas background `<rect x="0" y="0" ...>` the jar never emits (12 rects to 11, every other rect paired). Pre-existing root chrome, masked before C2 because the two missing text runs cancelled it. Filed in `findings/c2-residuals.md` as a root-chrome follow-on. |
| 12 | C2 close, orchestrator | The one descent LOSS is not fixed here, and is filed with its full mechanism instead. | `gucare-93-petu502`: `parseParticipantName` compiles its three `as` alternatives without `i`, while `Pattern2.compileInternal` gives every upstream command regex `Pattern.CASE_INSENSITIVE` (`Pattern2.java:113`) and `CommandParticipantA3:57` is a bare `new RegexLeaf("as")` — verified in both trees. Uppercase `AS` therefore forks one participant into two, a 5-element surplus that used to cancel the fixture's 5 missing split runs exactly. The fix is one character in `sequence-parse-helpers.ts`, which no task owns (stop condition 1), and the honest fix is a case-insensitivity audit of the whole sequence keyword set rather than one-off-ing `as`. |
| 13 | batch 3 start, orchestrator | C3, C4, C5, C6 run SERIALLY, not in parallel as `batch-3/overview.md` plans. | Three independent reasons, none of which the brief could have known. (1) C2 extracted `displayLines` and `textBlockRuns` into `text-block-geo.ts`, so that file is now shared machinery for the message (C3), participant (C4) and frame-tab (C5) paths — the write-set disjointness C1 was made to buy no longer holds. (2) All four agents share ONE working tree and therefore one git index; four concurrent `git add`/`git commit` cycles race. (3) Hazard 3 in the README — the corpus instruments race the stdlib build — applies to any two concurrent `npm test` runs, not just to chaining. The brief already required C5 and C6 to serialize for reason (1) alone; C2 extended that condition to C3. Serial costs wall-clock only. |
| 14 | batch 3 start, orchestrator | `text-block-geo.ts` is partitioned by FUNCTION across the batch: C3 owns `messageLabelBlock` only; `textBlockRuns`/`displayLines` belong to C5's frame path. | Otherwise C3 creole-ifying the shared `textBlockRuns` would silently do C5's work, and the frame-font findings C5 must honour (`reference` 12, `referenceHeader` 13-bold) would be applied by a task that never read them. |
