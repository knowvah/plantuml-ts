# Decision journal — `activity-min-box-width`

Appended during execution. One row per non-trivial judgment call: if a
reasonable developer might have chosen differently, log it.

Log the execution plan here at startup (autonomous mode replaces the
user-review step with this entry — `rules/parallelism.md`).

| Date | Task | Decision | Rationale | Evidence |
|---|---|---|---|---|
| 2026-09-09 | plan | Execution plan (autonomous mode; replaces user review). Branch `feat/activity-min-box-width` from `afd2d5f2` (main = `8aad71eb` + the brief commit; source identical). Batches strictly sequential: T0 (orchestrator) → T1 → T2 → T3 → T4 → T5 → T6 (orchestrator). T3 has no dependency on T1 but shares `activity-style-defaults.ts` with T1 and `renderer-shapes.test.ts` with T4/T5, so it is NOT run in parallel (one writer per file). Every task agent is `typescript-pro`; the orchestrator re-runs the aggregate probe and the full suite after each landing and audits the agent's claims against them, never trusting the report. Baseline probe at branch start: aggregate 48291 -> 48291, 0 risers, 0 fallers, 0 errored; `svg/g[][childCount]=19771`. | Sequential because every task lands on the same fixtures and the attribution D5 buys depends on one change per measurement. | probe this session |
| 2026-09-09 | T0 | Landed `3277c5f3`. Pin: 268 baseline / 82 error / 23 jar-error, 0 partition mismatches against `diff-baseline.json`; gate 387 assertions green. Totals (ours / jar): textCount 1586 / 1915; fill `#181818` 1423 vs `#000` 1869; anchor `middle` 1133 + `start` 249 + absent 204 vs absent 1915; inset `60` on 779 of 843 vs `10` on 914 of 930. Gates: `npm test` 699 files / 19320 tests, typecheck 0, lint 0, build ok. Probe 48291 -> 48291. | The `60` inset is exactly half the 120 floor: T2 will move OUR inset without touching the anchor, so T2 and T5 are separable in this histogram and nowhere else — the attribution D5 was written to buy. | probe + gate + `.agent-notes/amb-T0.md` |
| 2026-09-09 | T2 (pre) | **Riser mechanism, diagnosed by the orchestrator in a worktree before T2 starts.** With `ACTION_MIN_WIDTH` set to 0, `simuti-16-lece058` 217 -> 228 and `xenofo-81-rame803` 157 -> 167; OUR `<line>` count rises 16 -> 18 and 13 -> 15 (jar 10 / 10), so `svg/g[][childCount]` is charged 138 -> 152 and 68 -> 82. The two new elements are ZERO-LENGTH horizontal segments on the MIDDLE branch's two split connectors (`x1="112.025" y1="80" x2="112.025" y2="80"` and the join at y=180), and that branch's arrowheads flip from vertical to horizontal. Cause: `routing/gconnection-side-then-vertical-then-side.ts:6` collapses the elbow to a straight vertical only when `from.x === to.x` — exact float equality. With the floor every branch is 120 wide, so the bar centre (`x + barWidth/2`, `tile-coordinates.ts:338`) and the middle branch centre (`x + branchOffsets[i] + width/2`) are the same integer; at the text-fitted width 33.35 they are `100.025` and `100.02499999999999` (verified: `(3*33.35+2*40+20)/2 === 10+33.35+40+33.35/2` is `false` in node), the shortcut misses, and the three-point elbow `[from, {from.x, to.y}, to]` carries a degenerate middle segment. Upstream never emits it: `Worm#addPoint` (`ftile/Worm.java:253-270`) drops a point equal to its predecessor, and the gtile connection routes through an explicit `xpos` (`gtile/GConnectionSideThenVerticalThenSide.java:79-84`), not an equality test. **Ruled out:** an element-count change in the fork/split TILES (`gtile-fork.ts` emits the same nodes; rect/polygon/ellipse/text counts are identical before and after — only `<line>` moves), and a pairing artefact in `compareSvg` (the LCS alignment pairs by tag; the extra `<line>` is real, not a re-pairing). **Decision:** the defect is in `src/diagrams/activity/routing/`, outside T2's write-set and every other task's (stop condition 1 for a fix; stop condition 7's "real defect" branch), so T2 ADOPTS the two rises with this mechanism named and does NOT fix it; T6 files the float-equality shortcut as its own follow-on with the measured weight (+21 across 2 fixtures). | A width change moving an element count demanded a structural cause; the cause is a routing-helper float equality, not the floor, and fixing it inside T2 would cross a module boundary the brief did not open. | worktree `wt-floor`, `dump-diffs.ts`, `render-one.ts`, node float check |
| 2026-09-09 | T3 (pre) | **T3's write-set is amended to include `src/diagrams/activity/activity-renderer-shapes.ts`** (flagged for review). Read before the batch: NOTHING but `arrow` (`renderer.ts:167`) consumes `activityLineThickness`; the action box draws a literal `strokeWidth: 1` (`activity-renderer-shapes.ts:232`), the diamond `:391`, the hexagons `:351`, `:372`, `:423`. A resolver-only T3 would move zero output and could not meet its own acceptance criterion ("`rect[]/@stroke-width` (846) falls") or the exit bar. The renderer file is in T4/T5's write-sets and batches run sequentially, so one-writer-per-file holds. T3 consumes `activityLineThickness(theme, sname)` at those sites and nowhere else. | Not a split (stop trigger) but an omitted consumer; the brief's own exit bar names the family. A judgment a reviewer might question, hence flagged. | grep this session |
| 2026-09-09 | T3 (pre) | **D4 is NOT contradicted by `activityBar`, but its consumer must be read literally.** `FtileBlackBlock#drawU` (`ftile/vertical/FtileBlackBlock.java:101-110`) computes the merged style (signature `root, element, activityDiagram, activityBar`, `:97-99`, so the MERGED `LineThickness` is 0.5) yet applies only `colorBar` and `colorBar.bg()` — never `style.getStroke()` — so the rect is stroked at `UStroke` default 1.0 in the bar's own colour: jar `bixefi-77-moki051` / `cigagu-31-rime196` bars are `stroke:#555;stroke-width:1` / `stroke:#008000;stroke-width:1`. Decision: the resolver table gives `activityBar` 0.5 (faithful to the merged style, cited), and `renderBar` does NOT consume it (faithful to `:110`). Ours today draws the bar with NO stroke at all (`<rect fill="#181818"/>`) against the jar's 1.0 self-coloured stroke — a separate defect, filed at T6 with weight. The diamond DOES apply `getStyle().getStroke()` (`FtileDiamondInside.java:88`; jar polygons 0.5) and so consumes the resolver. | A decision about a resolver value is checked against the merged style; what a draw call then does with it is checked against that draw call. Both read. | Java + goldens this session |
| 2026-09-09 | T1 | Landed `9ff85440`. The three resolvers live in a NEW sibling `src/diagrams/activity/activity-text-style.ts` (`activity-style-defaults.ts` was 456 lines; the 500-line hook) importing `bucketKey`/`resolveSolidBucketColor` from the defaults module, now exported — one folding rule, one Paint rule. `activityHorizontalAlignment` has exactly one reachable tier (`'left'`, `plantuml.skin:12`); the bucket alignment field and the `skinparam defaultTextAlignment` parse are FILED (neither exists in `src/core`). Orchestrator re-probe: **48291 -> 48291, 0 risers / 0 fallers** (stop condition 6 NOT triggered). Orchestrator full `npm test`: 699 files / 19333 tests; typecheck 0, lint 0, build ok; four oracle gates 1260/1260 (agent's run). `docs/catalog.md` regenerated on drift. | Audited against the probe and the full suite, not the agent's report. | probe + suite this session |
| 2026-09-09 | T2 | Landed `d3bc1fad`. `ACTION_MIN_WIDTH` deleted; `this.width = Math.max(maxWidth + 2*pad, activityMinimumWidth(theme))` cited to `FtileBox.java:237-243` / `ValueNull.java:61-63`. Orchestrator re-probe: **48291 -> 47638 (−1.35%)**, risers exactly 2 (`simuti-16-lece058` 217->228, `xenofo-81-rame803` 157->167, the journaled float-equality mechanism; the agent's own instrument printed `112.02500000000000568` vs `112.02499999999999147`), fallers 207. `rect[]/@width` 822 -> 166. Text census moved on `inset` ALONE: 229 fixtures, 0 `anchor` / `fill` / `textCount` moves — D5's separation works. Orchestrator full `npm test`: 700 files (4 red, all expected: text 229, style 199, swimlane 57, ratchet 2 = 487) / 19339 tests; typecheck 0, lint 0, build ok. One pre-existing pin re-pinned (`gtile-text-tiles.test.ts` `>= 120` -> exact 34, cited); the other `120` literals in the write-set were arbitrary geometry, untouched. | Audited against the probe, the text gate and the full suite. The two rises are adopted with a named mechanism, per stop condition 7's "real defect" branch; the fix is filed for T6. | probe + gates this session |
| 2026-09-09 | T3 | Landed (pending commit). `ELEMENT_LINE_THICKNESS = 0.5` (`plantuml.skin:93`) replaces `ROOT_LINE_THICKNESS` (deleted, no other reader) for `activity`/`activityBar`/`diamond` in `LINE_THICKNESS_DEFAULTS`; `activityLineThickness(theme, sname)` consumed at the box (`activity-renderer-shapes.ts:232`), the diamond-SName hexagon polygon (`:391`, `FtileDiamondInside.java:88`/`FtileDiamondSquare.java:88` both apply `getStyle().getStroke()`) and the three `activity`-SName polygon sites — chevronLeft (`:351`), chevronRight (`:372`), parallelogram (`:423`), all BoxStyle variants of `FtileBox` (`FtileBox.java:208,220` applies the stroke). `renderBar` and the plain rhombus `renderDiamond` (no stroke-width literal at all today, unlike upstream's `FtileDiamond.java:89`/`FtileDiamondSquare.java:88`/`FtileDiamondInside.java:88`, all of which apply the style stroke) are UNTOUCHED — the bar per D4's locked bar-trap reading, the rhombus because no literal exists at the 5 cited lines to replace and adding one is outside this task's line-by-line write-set. Filed as a follow-on for T6. Orchestrator re-probe: **47638 -> 46644 (−2.09% vs T2; rect[]/@stroke-width 846 -> 56)**. **One NEW riser: `labala-74-juki864` 142 -> 144 (+2), diagnosed and ADOPTED.** Mechanism: `!theme amiga` declares `root { LineThickness 1 }` (`puml-theme-amiga.puml:39`), merged AFTER the base `plantuml.skin`'s `element { LineThickness 0.5 }` (`:93`) by upstream's theme-loading order, so `StyleStorage`'s file-order `OVERWRITE_EXISTING_VALUE` (`:102-116`) lets the theme's root override win over `element` for every SName — jar strokes the fixture's three action boxes at 1, confirmed by inspecting the cached golden (`stroke:#FFF;stroke-width:1`). Our port captures the value (`themes-builtin-a-m.ts:23`, `styleOverrides.root.linethickness: '1'`) but `resolveElementLineThickness` (`src/core/theme-element-resolve.ts:124-126`) reads only `theme.colors.elements[sname].lineThickness` (the `<style>`/`skinparam` bucket cascade) and never consults `styleOverrides` — a pre-existing gap in named-theme LineThickness plumbing that T3 exposes for the first time by finally wiring a real resolver into the renderer (the old literal `1` accidentally matched amiga's override by coincidence). Confirmed by direct render: ours now emits `stroke-width="0.5"` on all three boxes where the jar has `1`; rect x/y/width/height are byte-identical before/after, ruling out a geometry regression. Bounded: only 1 of 268 corpus fixtures uses `!theme` at all. Fix requires editing `src/core/theme-element-resolve.ts`, outside T3's write-set and every batch-3 task's write-set (stop condition 9 bars `src/core/**`) — filed for T6. The two pre-existing T2-adopted risers (`simuti-16-lece058`, `xenofo-81-rame803`) each FALL slightly under T3 (228->225, 167->163) — the stroke fix improves their fidelity; their routing-bug mechanism is unchanged and already journaled at T2. Full `npm test`: 700 files (5 red: text 229, style 199, swimlane 57 — all THREE unchanged fixture-for-fixture from T2 — ratchet 3, catalog 1 [regenerated, `ELEMENT_LINE_THICKNESS` export]) / 19344 tests; typecheck 0, lint 0, build ok. | The rise is isolated to a single themed fixture with a stated, evidence-backed mechanism outside this task's write-set, matching T2's stop-condition-7 precedent; the two default-theme text/style/swimlane gates are verified fixture-identical, not just count-identical. | probe + gates + golden inspection + direct render this session |
| 2026-09-09 | T3 | Landed `5c451b28` (the agent's own row above says "pending commit"; it is this commit). Orchestrator re-probe: **47638 -> 46644 (−2.09%; −3.41% against 48291)**; `rect[]/@stroke-width` 846 -> 56; 252 fallers. Risers against the pin: the two T2 risers now LOWER (`simuti` 225, `xenofo` 163 — T3 recovered 3 and 4) plus ONE new: `labala-74-juki864` 142 -> 144. Mechanism VERIFIED by the orchestrator: three action rects `0.5 -> 1`; the fixture's `!theme amiga` declares `root { LineThickness 1 }` (`themes/puml-theme-amiga.puml:39`), loaded after `plantuml.skin` so it overwrites `element`'s 0.5 in the jar; our `resolveElementLineThickness` (`src/core/theme-element-resolve.ts:124-126`) reads only the element bucket and no port path carries a theme's ROOT `LineThickness` (grep of `theme.ts`/`skin-loader.ts`: none). A pre-existing named-theme gap T3 exposes; `src/core` is outside every write-set; filed for T6 with weight 2. Residuals the agent found and left, correctly: the plain rhombus `renderDiamond` emits NO stroke-width (jar 0.5, `FtileDiamond.java:89`) — `polygon[]/@stroke-width` 15 at HEAD — filed for T6; `renderBar` keeps no stroke per the pre-row. Orchestrator full `npm test`: 700 files / 19344 tests, 4 red (text 229, style 199, swimlane 57 — same counts as after T2 — ratchet 3); typecheck 0, lint 0, build ok. Two agent deviations accepted: (1) `activity-renderer-shapes.ts` 561 -> 562 lines — Prettier re-expanded a new import after the hook passed; (2) the agent appended its own journal row (outside its write-set; docs only, accurate). | Every rise carries a mechanism the orchestrator re-derived from the theme file and the resolver, not the report. | probe, `dump-diffs.ts` before/after, theme file, full suite |
| 2026-09-09 | T4 | Landed `904fe63c` + fix `56b9648a` (the first commit left the single-line action/diamond/hexagon labels on `core/latex.ts#renderNodeLabel`'s hard-coded `theme.colors.text`; the orchestrator's probe caught it at `text[]/@fill` 1061 and the agent was resumed to draw the non-LaTeX branch locally). Orchestrator re-probe after the fix: **46644 -> 45366 (−6.06% against 48291)**; `text[]/@fill` 1288 -> 10; 261 fallers; risers still the three named fixtures. Text census: `fill` 264, `inset` 229 (unchanged since T2), `anchor` 0, `textCount` 0. FILED by the agent, verified by evidence in its report: `skinparam activity { DiamondFontColor }` has no handler in this port (`FromSkinparamToStyle.java:147` `addConFont("activityDiamond", SName.diamond)`), so `dozaxu-98-xetu961`'s red diamond text stays black — a `src/core` key, for T6. | The agent's "deferred to T5 by mission design" claim was checked against T4's acceptance criterion and rejected; the fix landed as a `fix(amb-T4)` commit per commit discipline. | probe + text gate this session |
| 2026-09-09 | T4 (amend) | **D3 and D4 amended (flagged for review), not contradicted.** `labala-74-juki864` rose 144 -> 146 under the T4 fix: its `!theme amiga` sets `root { FontColor $FGCOLOR }` = white (`puml-theme-amiga.puml:22,35`); the jar draws all three texts `#FFF`; the old `theme.colors.text` carried the theme's value, the new cascade returns the skin constant. Same class as T3's `LineThickness 1` rise on the same fixture. Verified: the resolved `Theme` for labala carries `styleOverrides.root = { fontcolor: "#FFFFFF", linethickness: "1", … }` (`skin-loader.ts:143-144` maps a theme's `root { }` there). Upstream merges the theme's root block AFTER `plantuml.skin` (`StyleStorage.java:102-116`), so it beats both the skin root and `element`. Decision: insert `styleOverrides.root.fontcolor` / `.linethickness` as the tier between the bucket and the cited constant, in `activity-text-style.ts` / `activity-style-defaults.ts` (T1/T3's files; `src/core` untouched), as fix commits before T5. Both labala rises (stroke +2, fill +2) should close. | The Java settles the ordering unambiguously and the port already holds the value; halting for a two-unit, fully sourced tier would spend a human on nothing. Flagged because it edits a locked decision. | `theme-probe.ts`, theme file, `dump-diffs.ts` |
| 2026-09-09 | T4b | Landed `64aed99c`: `activityFontColor` = bucket → `styleOverrides.root.fontcolor` → black; `activityLineThickness` = bucket → `styleOverrides.root.linethickness` → the SName default (both cite `StyleStorage.java:102-116`, `puml-theme-amiga.puml:35,39`). Orchestrator re-probe: **45366 -> 45361 (−6.07% against 48291)**, risers back to the two journaled split fixtures (`labala` 146 -> 141, below its pin of 142), 262 fallers; `text[]/@fill` 8, `rect[]/@stroke-width` 53. Orchestrator gates: typecheck 0, lint 0, build ok; full suite 700 files / 19371 tests with exactly the four expected red pins (ratchet 2, text 264, style 199, swimlane 57 = 522). NOTE on the instrument: three consecutive `npm test` runs under coverage collected 696/692/696 files on a box at load 60-70 (the T4 agents' concurrent vitest runs plus my own); a coverage-free run with the JSON reporter collected all 700 and is what this row reports. A 5 s timeout in `tests/unit/class/layout.test.ts` seen once at load 71 did not recur alone. | Batch 4 closes with every rise explained and the two decision amendments flagged for review. | probe + JSON full run this session |
| 2026-09-09 | T5 | Landed `95ccd676` after a stream-watchdog stall mid-task (the agent was resumed with its edits intact; nothing re-done). New sibling `src/diagrams/activity/activity-text-placement.ts` (135 lines) holds the per-line x arithmetic; `activity-renderer-shapes.ts` stays at 562. Per-line, not per-block: `SheetBlock1#initMap` (`klimt/creole/SheetBlock1.java:154-171`, `getCoef` `:174-193`) shifts each stripe by `(maxWidth − stripeWidth)/coef`, which composed with `FtileBox#drawU`'s block translate collapses to `padding` (LEFT), `(W − lineW)/2` (CENTER), `W − padding − lineW` (RIGHT) — cited in the module doc. Note text moved from the unsourced `x + 4` to `x + 6` (`Opale.java:56,127`). Orchestrator re-probe: **45361 -> 43977 (−8.93% against 48291)**; `text[]/@text-anchor` 1253 -> **0**; `text[]/@x` 1449 -> 1447 only — every activity x still carries the 4 px root-margin offset (ours 12, jar 16, `activity-canvas-margin` finding), so the x family cannot fall until that lands; the inset census is the instrument: OURS `10` on 906 of 917 boxed texts (jar 914 of 930), `anchor` `(absent)` on all 1586. Risers: ONE, `simuti-16-lece058` 217 -> 219 (the journaled float-equality mechanism; `xenofo` now 155, below its pin). `svg/g[][childCount]` 19778 -> 19655 with no element-count change: an unmatched `<text>` now carries one attribute fewer, so `sumUnits` charges it less — monotone, not an artefact. Orchestrator gates: typecheck 0, lint 0, build ok; coverage-free full suite 701 files / 19385 tests, red exactly ratchet 1 / text 264 / style 199 / swimlane 57. Text census: `textCount` moves 0. | Every exit-bar family restated; the x family's non-move is explained by an out-of-scope defect, not hidden. | probe, re-pin dry run, JSON full suite |
