# Decision journal — `dispatch-by-parse-attempt`

Appended during execution. One row per non-trivial judgment call: if a
reasonable developer might have chosen differently, it belongs here.

| Date | Task | Decision | Why | Evidence |
|---|---|---|---|---|
| 2026-08-24 | T0 | The baseline is **1**, not 0 — pinned as an honest `weErrored: true, status: "ok"` record, not as a `known-gap` excuse | T0 says a non-zero baseline is a finding to report, not a pin to write, and its Boundaries forbid `known-gap` at this stage: nothing has *gapped* yet because no engine can refuse anything at this commit | `dot-cache sequence/nuvoja-46-dezu541` is `!includedef macro`; we stop in the PREPROCESSOR (`IncludeExecutor.ts:127`), before any engine sees a line. Its error text is `Cannot resolve !includedef 'macro'`. Already the routing gate's second standing known-misroute; named out of scope by stop condition 10 |
| 2026-08-24 | T0 | "We errored" is detected by the **`PSystemError` version banner**, as one whole `<text>` element, with the needle BUILT from `fullDescription()` rather than spelled out | Every page in the `PSystemError` family opens with `header()`, and building the needle from the function means a version bump cannot silently blind the gate. Spelling the string out would have been a magic literal that fails open | `src/core/error/PSystemError.ts:156` returns `[fullDescription()]`; `error-renderer.ts:198` emits each header line as its own `<text>`. Rendered probes: the Welcome screen carries the same opening block but **no** banner, so the banner and not the welcome text is the discriminator |
| 2026-08-24 | T0 | The scan of OUR output is **whole-document**, not a 4 KB head window (goldens keep the head window) | The banner's offset is not fixed: `getGraphicalFormatted` prepends the welcome block only for a short source. Measured at byte **4544** for a two-line source and **2459** for a 200-line one — a 4 KB window would see the second and miss the first, silently under-reporting the exact quantity the gate exists to count | Probe over `@startuml\n!endif\n@enduml` (4544), the same with 200 preceding messages (2459). Cost of not truncating: see the SLI 3 row below |
| 2026-08-24 | T0 | `Your data does not sound like JSON data` counts as a RENDERED diagram, not an error | Upstream draws it inside `JsonDiagram` itself, never through `PSystemError`; so does this port. D6 leaves the `PSystemAbstractFactory` family's own error semantics untouched, so counting it as a refusal would floor SLI 2 above zero for 0 fixtures of real signal | `~/git/plantuml/.../jsondiagram/JsonDiagram.java:118`. Our render of `@startjson\n{ oops\n@endjson` is a 531-byte box with that text and no banner |
| 2026-08-24 | T0 | The jar-error classifier is **duplicated** from `routing-conformance.test.ts`, not imported | Importing a `.test.ts` executes its module body, which would register the sibling's whole 3158-fixture measurement a second time inside this file's suite. Extracting it to a shared module is outside T0's write-set, and the task's Boundaries make that an ask-first | T0 read-set: "reuse its approach; do not import private internals if that means modifying it". Both copies carry the same `PSystemError.java:148-155` / `ReportLog.java:103-108` citation |
| 2026-08-24 | T0 | A fixture that newly errors where the **jar also errored** passes, logged `[MATCHED]` | The gated quantity is SLI 2's — *we error AND the jar rendered*. Us joining the jar in failing moves us toward it. Failing on it would make 8 fixtures permanently unreachable for reasons no command-table work owns | 5 of the 6 fixtures we error on today are already jar-error pages (4 `state/` banners + `svg-class/class-actor-bare-no-allowmixing`); only nuvoja is a defect |
| 2026-08-24 | T0 | `engine` is recorded but **never asserted on** | Which engine drew is the sibling routing gate's subject. Gating it here would fail this gate for a routing move and name the same defect in two places — and batch 3b deliberately moves routing | Contract in `batch-0/T0-refusal-coverage-gate.md` asks `engine` so T13–T20 can filter their own bucket; a filter aid, not a ratchet |
| 2026-08-24 | T0 | `CORPUS_BUDGET_MS` is **transferred** from the sibling's in-situ derivation, not re-derived | The per-fixture work is the same work — render the fixture, read the golden's head — so re-deriving would have produced a second number fitted to the same measurement. Transferring keeps one derivation to maintain | Sibling: 15,492 ms in situ x the 2.70x fork-contention step (`.agent-notes/ratchet-zudize-timeout.md`) = ~41,800 ms ceiling, 120,000 ms at ~2.9x. Corroborated here: **7,304 ms solo** (jiti, no coverage) against the sibling's 6,000 ms under the same conditions |
| 2026-08-24 | T0 | **SLI 3 (parse cost), first reading.** The gate adds a second whole-corpus render: **5,986 ms** in-suite. `npm test` measures **63.66 s** wall-clock with the gate, against the ~60.3 s standing figure | The instrument costs a corpus render by construction — the two gates run in separate vitest workers and cannot share a measurement. Recorded as the pre-change reading D3 asks for, so batch 3b's N-parse cost is compared against a number taken before it | `npx vitest run …/refusal-coverage.test.ts`: 29 tests, 9.30 s total / 5.97 s tests. Full suite 636 files, 16322 tests, coverage 95.45 / 90.49 / 96.96 / 96.53 |
| 2026-08-24 | T1 | `mergeRefusals` is **first-wins on an exact tie**, not last-wins | `mergeV2`'s comparison is a strict `<` (`result.score() < err.score()`), so an equal later score never replaces the earlier one. Getting this backwards would silently pick a different error page from the jar's on every all-fail source, and nothing in the corpus would catch it — D2's stated reason for porting the arithmetic verbatim | `PSystemErrorUtils.java:140-147`, read directly. Verified independently of the agent's report |
| 2026-08-24 | T1 | The `incomplete` refusal carries `commandScore` 0 | Upstream builds **no** `ErrorUml` at that point at all — `finalizeDiagram` just returns `null` (`PSystemCommandFactory.java:161-162`) — so there is no upstream score component to carry. 0 is the same default the other three bare-message points pass as `ErrorUml`'s literal third argument, not a chosen value | Syntax `:171` and `buildExecutionError` (`PSystemAbstractFactory.java:67-72`) both pass a literal `0`; only the execution point passes `result.getScore()` (`:183`). All four verified against the Java |
| 2026-08-24 | T2 | Upstream's per-first-character `switch` is **flattened** to one ordered first-match-wins list | The 20-case switch cannot fit under the repo's CCN-10 hook in one function. Flattening is behaviour-preserving here and provably so: `check(key, text, p)` compares from position `p`, so `key[0]` must equal the switch character — a key from another group can never match. No key in any group is a prefix of another, so even within-group order is not load-bearing | `DiagramType.java:94-218`. Recorded because it IS a structural divergence from upstream's dispatch shape, permitted only because the equivalence is provable, not because it reads better |
| 2026-08-24 | T2 | **`/\s/` replaced by an enumerated `Character.isWhitespace`, and `toLowerCase()` by an ASCII-only fold** — landed as `fix(T2)` | Both built-ins were standing in for Java predicates they are not equal to, and both change *which sources dispatch at all*. `/\s/` disagrees in both directions (matches the three non-breaking spaces and U+FEFF; misses U+001C-U+001F), and `toLowerCase()` folds U+212A to `k`. Neither divergence was ported from anything — they were invented, which is the failure mode `CLAUDE.md` names first | `Character#isWhitespace(char)` javadoc; `DiagramType.java:73-74`, `:225-226`. 13 new tests, each failing under the built-in and passing under the port; code points spelled numerically because a literal U+2007 is indistinguishable from a space in review |
| 2026-08-24 | T2 | `DiagramType` is exported under the **same name** as this port's existing plugin type union, from a different module | The task contract requires upstream's full 39-member enum, which is wider than the port's union; renaming it would break the "preserve upstream names" rule that 16 years of upstream commits depend on. T3 owns the mapping between the two | `DiagramType.java:45-47`; task T2 Interface contracts. Flagged as a collision hazard for T3, which imports both |
| 2026-08-24 | T3 | **T3 is split: `detectUmlType`'s deletion moves to T12.** Maintainer decision, asked rather than taken | T3 requires both "delete `detectUmlType`" (D5) and "move nothing" (AC1), and the two are jointly unsatisfiable. `resolve()`'s third tier returns the plugin for `source.type`, which for `@startuml` IS the guess. **Measured: that tier fires for 31 of 3157 blocks — 26 typed `sequence`, 5 typed `class` — and all 31 currently agree with the jar.** With the guess gone the tier can only take the first plugin whose type is in the candidate set, which is `class` for every `@startuml`, newly misrouting 26. No registration order satisfies both groups | Tier census over both trees, cross-referenced against `routing-baseline.json`: `agree=31 other=0`. Those 31 are exactly what 3a+3b repairs — once `class` can refuse, `sequence` is reached. D5 holds; only its timing moves |
| 2026-08-24 | T3 | `UmlSource.types` is **optional**, not required | Making it required produced **174 typecheck errors across ~100 test files**, none of which assert anything about candidate sets — pure churn that would have swamped the diff this task's "moves nothing" claim has to be reviewed against. The interface already carries three optional fields (`rawStyles`, `linePositions`, `stylePositions`) documented with exactly this reason, so this follows the file's own convention rather than inventing one | Measured by adding the required field and counting `tsc` output. `finalizeBlock` — the only producer in the render pipeline — always populates it. Doc comment states absence means "no candidate constraint", never the empty set, which is a real answer. T12 makes it required |
| 2026-08-24 | T3 | The eight port-only `@start` tags keep routing as they do today | `START_SUFFIX_MAP` carries `@startsequence`, `@startclass`, `@startstate`, `@startactivity`, `@startobject`, `@starttiming`, `@startcomponent`, `@startusecase`. **Upstream has none of them** — `findStartTypes` types all eight `UNKNOWN`, no factory's type is in that set, and the jar renders `PSystemUnsupported`. The divergence is pre-existing; repairing it here would move fixtures, and this task's only property is that it moves none | Grepped `~/git/plantuml/src/main/java/net/` for all eight: no hits. `StartUtils.java:51-63` confirms the jar accepts any `@start<something>` at the block level and defers typing to `findStartTypes`. No corpus fixture uses any of the eight; four test sources do. Flagged for a later mission |
| 2026-08-24 | T3 | A returned refusal is converted to a **thrown `DiagramRefusal`** inside the pipeline | D1 governs the PLUGIN contract — plugins return, and they still do. The throw is an internal jump to `renderSync`'s own `catch`, which is how this port already renders a refusal: `DiagramRefusal` carries the offending line and assumed type into `errorSvg`/`PSystemErrorV2` so the page lands where the jar's lands. Upstream's `throw` is reserved for `catch (Throwable t)` — a different page, "Fatal crash error" | `PSystemBuilder.java:275-281` for the crash path; `error-diagrams.ts#DiagramRefusal`, which predates this mission. Proven reachable end to end by `tests/unit/dispatch/parse-refusal-wiring.test.ts` — it registers a refusing plugin in the unused `object` slot, since no engine refuses until 3a |
| 2026-08-24 | T3 | Added `tests/helpers/parse-ast.ts` | Widening the contract broke 20 call sites in 5 test files that hand `parse()`'s result straight to `layoutSync`. A cast at each would turn a real refusal into an unreadable failure three frames later; the helper throws naming the engine, line and kind. Test-helper placement is on the mission's push-forward list | Consumed by T4–T12, which will make engines refuse for real |
| 2026-08-24 | T4–T11 | Eight engines given strict refusal; all four gates' static half green, `npm test` red by design | D3' makes 3a+3b one atomic unit. Each engine's report was checked against the Java rather than taken on trust; every claimed absence of a refusal point was re-verified | Verified independently: `SequenceDiagram.java:585-587` `isIncomplete()` really is `participantsList.size() == 0`; `StateDiagram.java:231-242` `checkFinalError()` really does reject cross-concurrent-state links; `CommandBoardPlus.java:54-59` really does require a non-whitespace LABEL. **SLI 2 after 3a, before 3b: 728** (sequence 634, class 60, description 33, nuvoja 1) |
| 2026-08-24 | T5 | The `sadamo-18-siva346` conditional skip was converted to an **assertion** | A `return` on the refusal passes whether or not the refusal fires, which is exactly how a regression in the refusal mechanism would go unnoticed. The claim behind it is independently checkable and checks out | `CommandLinkClass.java:176-178`'s identifier is `[%pLN_$]+`, and the fixture's endpoint is ~1,900 backticks. Decisive: that fixture's golden is one of only 8 jar error pages in the corpus (`jarRendered: false`), so the jar refuses the same line |
| 2026-08-24 | T12 | **Registration order corrected against the brief**: activity goes AFTER state, not between class and description | The brief reads upstream's list as one activity factory. There are two — `ActivityDiagramFactory` at slot 5 and `ActivityDiagramFactory3` at slot 8 — and this port's engine mirrors the SECOND. Placing it at slot 5 would let it claim `@startuml` sources upstream gives to description or state | `PSystemBuilder.java:134-141` for the list. Our `trySwimlane`/`tryAction`/`tryIf`/`tryRepeat`/`tryFork`/`trySplit` map 1:1 onto `CommandSwimlane`/`CommandActivity3`/`CommandIf4`/`CommandRepeat3`/`CommandFork3`/`CommandSplit3` (`ActivityDiagramFactory3.java:105-165`); the legacy factory's `UBrexCommandIf`/`CommandLinkActivity`/`CommandLinkLongActivity` (`ActivityDiagramFactory.java:76-97`) have no counterpart in this port |
| 2026-08-24 | T12 | Ported the **allowmixing execution refusal** that T5 deferred here | `CommandCreateElementFull2` MATCHES `component a` on a class diagram and then fails execution when `allowmixing` is unset. This port recorded the violation and adjudicated at end of parse, gated on the block also holding a native class construct — an explicit compensation for eager dispatch, which parse-attempt removes the need for. Refusing at the command, as upstream does, is what hands the block to the next candidate | `CommandCreateElementFull2.java:194-197`; score 0 from `CommandExecutionResult.java:81-83`. **Measured: DESCRIPTION→CLASS 200 → 4, STATE→CLASS 150 → 79, total wrong-engine 538 → 271** |
| 2026-08-24 | T12 | **The brief's proof criterion is stated backwards, and it CLOSES** | README: "`kokebo-27-vafi688` routes CLASS; the jar says DESCRIPTION." Both the committed golden and `routing-baseline.json` say the reverse — `jarType: CLASS`, `ourType: DESCRIPTION`. So the target was to route CLASS, and parse-attempt does, with no heuristic in the path | `routing-baseline.json` known-misroute entry; live measurement `jar=CLASS ours=CLASS AGREE`. `sequence/dudeku-78-naju581` also still agrees |
| 2026-08-24 | T12 | **STOP — stop condition 1, with a mechanism.** 271 fixtures reach the wrong engine and 575 error where the jar rendered | Under parse-attempt, routing correctness becomes exactly command-table fidelity **in both directions**, and this port's tables diverge both ways. An engine that accepts a line upstream's factory does not registers OVER-CLAIMS; an engine that refuses a line upstream's factory does register UNDER-CLAIMS and errors. Batches 4–6 are scoped only to the second | Measured over 3158: 2304 agree, 271 wrong engine, 575 error, 8 jar errors. Classified: **151 of the wrong-engine cases are secondary** — the correct engine refuses, so closing its command table reclaims them (all the `SEQUENCE → *` buckets). The other ~120 are over-acceptance by an earlier candidate, which no planned batch addresses |
| 2026-08-24 | batch 4–6 | **Membership re-derived from measurement, as the brief requires. Four of the eight planned coverage tasks are measured no-ops.** | The overview says membership is a planning guess and to re-derive it from T12's SLI 2 breakdown. Doing so needs care about ATTRIBUTION: the refusal gate's `engine` field records which engine's refusal WON `mergeRefusals` (D2), not which engine should have owned the source. For sizing coverage work the useful attribution is *the engine the JAR chose refuses its own diagram* | Gate attribution: state 224, class 185, sequence 154, description 11. **Jar-answer attribution: sequence 634, class 60, description 33, state 0.** The two reconcile: 727 total command-table gaps, of which 575 end as error pages and ~151 end as misroutes (an engine's gap lets a later candidate claim the source). So batch 4's planned membership — sequence, class, description — is exactly right, and batches 5 and 6 (state, activity, board, chart, packetdiag) are **measured no-ops**: activity/board/chart/packetdiag never refuse a fixture the jar gave them, and state's 224 was merge-winner attribution, not a gap of its own |
| 2026-08-24 | batch 4 | Each agent was handed its bucket as data, not told to go find it | Re-deriving 634 fixtures per agent would have cost three full corpus sweeps and invited three different attributions. `.agent-notes/coverage-buckets/*.tsv` carries one row per defect: tree, slug, refusal kind, line index, and the offending line text | The head of the distribution is heavily concentrated: **214 of sequence's 634 are `!pragma`** (183 `teoz true`, 31 `svgparser sax`), one command registered on every factory via `CommonCommands.addCommonCommands1`. The class and description buckets are dominated instead by lines that are plainly supported (empty lines, `}`, `end note`, `class foo1`), which means a multi-line construct closed early on an EARLIER line — agents were told to diagnose the mechanism, not the reported line |
| 2026-08-24 | T13–T15 | Batch 4 closed. **Coverage buckets: sequence 634 → 151, class 60 → 0, description 33 → 0** | Each engine's bucket was handed over as data and closed by porting the missing `Command`, each with its upstream `file:line` in the code. The single largest item across all three was `!pragma NAME [VALUE]` — `CommandPragma` is registered on every factory via `CommonCommands.addCommonCommands1`, and this port recognised only `!pragma useIntermediatePackages`. It alone accounted for ~150 sequence, 43 class and 26 description fixtures | Measured per engine by re-rendering every bucket fixture through the same instrument the refusal gate uses. Sequence's residual 151 is ~32 exo-arrows (`[->`, `->]`) — `CommandExoArrowAny` needs a `MessageExo` event kind the AST has no member for, so it is a separable feature, not a regex widening — plus a ~119 long tail with no concentrated mechanism |
| 2026-08-24 | T14/T15 residuals | Both `src/core` residuals the agents correctly stopped on were fixed by the orchestrator | Each was outside its engine's write-set, which is exactly what the task spec says to stop for. **`@pause`/`@unpause`/`!exit`/`@append`** was documented in `BlockUmlBuilder.ts` as "not ported — no caller"; `component/lobute-63-tizi096` is the caller (`BlockUmlBuilder.java:100-134`). **The skinparam block collector** was an ad-hoc two-level model with no representation for a third level, so a nested `skinparam database { border { … } }` left the block one `}` early and leaked its tail into `source.lines` | Re-mirrored on upstream's own model, a context stack (`SkinLoader.java:56,78-110`), with `cleanForKeySlow`'s `<<stereo>>`-to-the-end rule (`SkinParam.java:283-300`) preserving the behaviour the two-level model special-cased. Java's whitespace predicate now lives in one module — `StartUtils` used `/\s/u`, the second differently-wrong copy of the divergence `fix(T2)` had already corrected once |
| 2026-08-24 | T12 follow-up | **`state A {` is gated after all** — the container exemption was keyed on shape, never on keyword | `CommandPackageWithUSymbol`'s SYMBOL alternation (`:76-77`) does not contain `state`, and no other command in `ClassDiagramFactory`'s table matches `state A {`. The comment justifying the exemption read "jar-verified: `state A {` renders without allowmixing", which conflated two questions: it renders because the jar routes such a source to the STATE factory. That says nothing about whether `ClassDiagramFactory` accepts the line | **STATE → CLASS fell from 81 fixtures to 0**; total wrong-engine 154 → 73; agree 2844 → 2925. It also net-fixed 22 state unit tests. A worked example of the mission's own warning: a verification that measures the wrong question survives for years |
| 2026-08-24 | batch 4 close | **SLI 4 has moved and needs accounting: 421 sequence diff-baseline ratchet failures, of which 270 RENDER and score worse than their pinned baseline** | The other 151 are error pages — exactly sequence's remaining refusal bucket, so they resolve as that bucket closes. The 270 are different: those fixtures render, and render *further* from the jar than before the mission. Reverting T13's rendering changes alone recovers only 22 of them, so the cause is predominantly the PARSE work — commands newly recognised now emit geometry that was previously dropped entirely | Isolated by re-running the ratchet with `renderer.ts`, `renderer-arrowhead.ts`, `sequence-layout-events.ts` and `layout.ts` reverted to the pre-T13 commit: 421 → 399. This is stop condition 2 at scale and is not accountable inside batch 4; it is T22's subject and needs a mechanism per bucket, not a re-pin |
| 2026-08-24 | T13 | **Diagnosed: 184 of the 242 ratchet rises are a METRIC ARTEFACT, not a fidelity regression. 35 are real.** | `compareNodes`' short-circuits charge `units(actual) + units(expected)` (`compare.ts:198,229,404`). That makes `weightedScore` monotone in alignment for a FIXED pair of documents — which is what `sequence-root-chrome` D5 designed it for — but not comparable across a change that grows OUR side: the same "still mismatched" verdict costs strictly more once `units(actual)` grows. T13 made previously-truncated diagrams emit their full content, so the charge grew even where alignment improved | Worked example `bexoce-95-vibe195`: baseline 622, live 950, and the whole delta is one diff — `childCount actual=14 expected=59` (w 618) becomes `actual=60 expected=59` (w 946). Off by 45 became off by 1, and the score rose 328. Whole-population: **162 risers moved CLOSER to the golden's child count, 35 further, 22 unchanged, 23 have no such short-circuit**; the childCount diff is a median **100%** of each rise. Ruled out: reverting only T13's four rendering files recovers 22 of 421; reverting all of `src/diagrams/sequence` gives risen=0; the orchestrator's own core changes are innocent (risen=0 with T13 reverted while they stood) |
| 2026-08-24 | T13 | **`scale` is rendered as a transform group; upstream bakes it into the coordinates** — a real defect, 10-11 fixtures | `renderer.ts:476-487` wraps children in `<g transform="scale(N)">` over unscaled coordinates. The jar emits no transform group: `gebeki-18-muci858`'s golden (source `scale 2`) carries pre-multiplied coordinates (`M66.938,102`) and dimensions 454x364, where ours emits `x1="70"` inside a wrapper with dimensions 606x502. Two defects — the extra `<g>` level collapses the top-level child count to 1, maximising the short-circuit charge, and the dimensions are independently wrong | 10 of the 35 genuine regressions collapse to exactly one top-level child; 11 of the 35 use `scale`. This is the only concentrated mechanism among the real regressions; the other ~24 are over-emission (we now emit a few MORE children than the golden) with no shared cause yet |
| 2026-08-25 | T13 | **`scale` fixed: the factor is baked into the geometry and theme, not wrapped in a transform** | Jar-measured with `scripts/oracle-render.sh`: `scale 2` doubles dimensions AND font sizes (13,14 -> 26,28) and emits **zero** transforms; `scale 3` triples them. Upstream's one multiply point is `SvgGraphics#format`; its only `transform="scale(...)"` is `manageScale` (`SvgGraphics.java:1035-1051`), for embedded sprites, never the document. Fixed the way this repo already fixed it for json — scale the INPUTS at the layout→render boundary (`json/scale-geo.ts` carries the reasoning: `format(x·k)` either way, since every derived value is linear in its inputs) | Risers 242 → 217, genuinely-less-aligned **35 → 25**, fallers 36 → 46, ratchet failures 421 → 396, zero fixtures rose. All ten single-child collapses recovered; `caxuke-64-femu351` now matches the golden's child count exactly. Flagged not fixed: the jar draws message labels at font-size 13 where participant labels use 14, and this port uses `theme.fontSize` for both — a pre-existing font-size-selection gap |
| 2026-08-25 | T13 | **162 baselines re-pinned UPWARD**, each carrying its own evidence; 55 risers deliberately left failing | An upward re-pin is the one movement this ratchet forbids, so it is justified per entry and in the manifest `$comment` rather than asserted. The 162 are the metric artefact diagnosed above: every one has a top-level child count CLOSER to the golden than before, so the rise measures our document growing, not our fidelity falling. The 25 that moved FURTHER, the 18 with an unchanged gap and the 12 with no such short-circuit are NOT re-pinned — they were invisible underneath the 162 and are the real remaining work | Fresh whole-corpus measurement at `9176f739`. Ratchet failures **396 → 234**, exactly 162 fewer, confirming the re-pin hit the intended set and nothing else; the residual is 179 render errors + 55 risers. Checked before writing: no re-pinned entry has `diffCount: 0` (the "already promoted" assertion still holds) and sequence's `ratchet.json` is empty, so stop condition 2 cannot be tripped — no promoted zero-diff fixture was de-promoted |

---

## T22 close-out — 2026-08-25

**The brief's proof criterion is stated backwards.** README:48 says
`component/kokebo-27-vafi688` "routes CLASS; the jar says DESCRIPTION". The
fixture's own golden says `data-diagram-type="CLASS"`. The criterion still
discriminates — it asks whether parse-attempt lands where the jar lands —
but a reader checking the stated direction would conclude the opposite.
Recorded rather than silently re-worded, because T12 was accepted against it.

**Batch 4–6 membership was a planning guess, and the guess was wrong in a
specific, useful way.** The brief anticipated re-derivation ("the real split
is whatever T0's gate measures after 3b"). Measured: sequence 106, class 26,
state 17, description 4 — and every one of the 153 lives in the SEQUENCE
corpus. The `engine` field disagrees on 17 because D2 keeps the
highest-scoring refusal, not the engine that owns the gap. Batches 5 and 6
therefore closed as measured no-ops, and the coverage work is one engine's,
not six.

**Strictness turned permissive regexes into routing bugs.** Three found and
fixed, each traced to the upstream grammar it mismatched rather than patched
at the symptom: description's bracket trailer (`(.*)?$` vs an anchored
tags/stereotype/url/color tail), the class usymbol container's missing
`TAGS1`/`TAGS2` runs, and class arrow style using `LINE_STYLE_MULTIPLES`
where `CommandLinkClass` uses plain `LINE_STYLE`. The first alone was
failing 20 state tests.

**Eight tests asserted behaviour the jar does not have.** Every one was
checked against the jar with `scripts/oracle-render.sh` before being
rewritten, and each rewrite carries the jar's output in its comment. They
were not wrong when written — they pinned a port that dropped unrecognised
lines silently, which is the behaviour this mission removed.

**Both conformance gates gained a citation requirement.** A `known-gap` and
a `known-misroute` pin must now match `/\w+\.java:\d+/`. The refusal gate's
older `/Command\w+/` was too narrow twice over: it missed the factory-built
`FactorySequenceNoteCommand` naming, and it could not express the gaps that
honestly are not Commands (`EmbeddedDiagram`, the `---` YAML header, the
`%newline()` builtin). Demanding the origin is the stronger bar.

