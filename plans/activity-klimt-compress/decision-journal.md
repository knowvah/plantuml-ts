# Decision journal — `activity-klimt-compress`

Appended during execution. One row per non-trivial judgment call: if a
reasonable developer might have chosen differently, log it.

Log the execution plan here at startup (autonomous mode replaces the
user-review step with this entry — `rules/parallelism.md`).

| Date | Task | Decision | Rationale | Evidence |
|---|---|---|---|---|
| 2026-09-10 | plan | Execution plan: B0 T0 (orchestrator) → B1 T1 ‖ T2 (two `typescript-pro` agents in separate worktrees, disjoint write-sets) → B2 T3 → B3 T4 → B4 T5 → B5 T6 (orchestrator). Probe reused from `activity-parallel-connectors`, extended with counts, family tables and `--removed`. | `rules/parallelism.md` autonomous exception: plan logged here instead of user review. B1 runs in worktrees per `.agent-notes/batch-parallelism-needs-worktrees.md`. | `.agent-notes/akc-T0.md` |
| 2026-09-10 | T0 | Chose `pujozo-36-nino158` as the laned-`if` dump and `nomeco-93-minu967` as the `while` dump. | Smallest-scored fixtures whose source has lanes + `if` (no fork/while/repeat) and `while` only (no lanes/fork/if/repeat), so each dump isolates one mechanism. | probe rows sorted by score |
| 2026-09-10 | T0 | Recorded that `Recentred`, not compression, absorbs the jar's leading 14 px `xMargin` on splits; `simuti`'s left residual after T5 is filed to `activity-canvas-margin`, not a target here. | `SlotSet#reverse()` emits only gaps between occupied slots; `AbstractParallelFtilesBuilder.java:130` `xMargin = 14`; `ActivityDiagram3.java:212`. | `.agent-notes/akc-T0.md` |
| 2026-09-10 | T0 | Predicted a 20 px Y removal on `nomeco-93-minu967` (ours only) — named now so T5 does not treat it as an unexplained riser or reserve it. | The out-edge leaves 20 below the action; only a `ULine` sits in `[144,174]`. | `.agent-notes/akc-T0.md` |
| 2026-09-10 | T1 | Ported `ArrowsRegular` as `arrows-regular.ts` (`arrowDirection`, `arrowHeadPoints`, `arrowHeadExtents`) and made `renderer.ts#arrowTip` draw it, replacing the old continuously-rotated 3-point/8-long/±3.2 triangle. `polygon[]/@points` (whole-attribute, count-mismatch family) fell 1722 -> 230 aggregate, 300 -> 18 subset, exactly as the mission predicted. Per fixture class (whole-attribute + per-index points diffs, before -> after): fork/split (32 fixtures) 300 -> 2262; laned (43) 284 -> 2016; hexagon/if/while/repeat (114) 1006 -> 6277; other (79) 221 -> 1701. Sum of the four deltas is 10445, exactly the aggregate `weightedScore` rise (52956-42511); every non-polygon family (`line[]/@x1`/`@x2`/`@y1`/`@y2`, `rect[]/@x`/`@y`/`@width`, `text[]/@x`/`@y`, `ellipse[]/@cx`/`@cy`, `svg/@width`/`@height`/`@viewBox[]`, `childCount`) is byte-identical before and after -- confirming the entire aggregate movement is this one family. | `ArrowsRegular.java:41-86`; point order and values verified against the four Java methods; `Direction.fromVector` (`utils/Direction.java:110-128`) ported for the cardinal cases. | `src/diagrams/activity/arrows-regular.ts`; scratch `family-by-class.ts` run before (git-stashed) and after |
| 2026-09-10 | T1 | `arrowDirection` diverges from `Direction.fromVector`'s diagonal throw (`Direction.java:128`, `"Not a H or V line!"`): picks the dominant axis (`\|dy\| >= \|dx\|` -> vertical, ties vertical) instead of throwing, because our edges are not always axis-aligned (e.g. `nomeco-93-minu967`'s out-edge `(208.638,164) -> (218.638,184)`), a case upstream's `Worm` structurally cannot produce. For a zero vector, returns `'down'` instead of upstream's `null`; `arrowTip`'s pre-existing `dx===0 && dy===0` guard means this value is never read for a drawn polygon. | Upstream never calls `fromVector` on a non-axis-aligned segment; ours does, from pre-existing, out-of-scope edge-routing imprecision. | `src/diagrams/activity/arrows-regular.ts` JSDoc on `arrowDirection`; `tests/unit/activity/arrows-regular.test.ts` diagonal block |
| 2026-09-10 | T1 | Aggregate `weightedScore` rose 42511 -> 52956 (253 risers, 3 fallers, 0 throws); mechanism stated and verified by an A/B toggle (git-stash the renderer change, re-run the probe: reverted code reproduces the pinned 42511 exactly, confirming the rise is caused solely by this task's change). Before T1, every arrowhead's point count (6, our old triangle) mismatched the jar's `ArrowsRegular` count (8), so `compare.ts`'s `points`/`viewBox` numeric-count-mismatch branch (`compare.ts:426-431`) charged exactly ONE diff per arrowhead regardless of how far off its position was (confirmed: the reverted run shows zero entries under the per-index `polygon[]/@points[]` bucket). After T1, counts match (8==8) on every default-skin edge, so the comparator falls through to its per-index loop (`compare.ts:432-441`) and reports one diff per numeral whose delta exceeds tolerance — up to 8 per arrowhead instead of 1 — for every arrowhead whose absolute tip position does not yet coincide with the jar's, because tip position is inherited from lane/margin/compression geometry this task does not touch (`activity-canvas-margin`, filed; T2-T5, not yet landed). The underlying SHAPE is not wrong: on `bixefi-77-moki051` every one of the 6 dumped arrowheads' 4 points, expressed relative to their own tip, are `(-4,-10),(0,0),(4,-10),(0,-6)` on BOTH sides (byte for byte) -- only the tip translation differs, and by a constant amount per arrowhead (e.g. arrowhead 1: ours `(55.675,51)..(59.675,61)` tip, jar's `(71.675,56.5)..(75.675,66.5)` tip, a constant `(16, 5.5)` offset across all 4 points). This is the documented "weightedScore can rise on a correct fix" pattern (`.agent-notes/weightedscore-can-rise-on-a-correct-fix.md`); not a defect in this port, not fitted, not re-pinned. | A/B via `git stash push -- src/diagrams/activity/renderer.ts docs/catalog.md tests/unit/activity/renderer.test.ts` + probe (reproduced 42511 exactly, 0 risers) then `git stash pop`; `--dump bixefi-77-moki051`; scratch script enumerating `polygon[\d+]/@points$` (whole-attribute) mismatches, all 5 sampled hits were 6-vertex hexagon/diamond shapes where the jar repeats its first point to close the polygon and ours does not (pre-existing, unrelated to arrowheads, out of T1's write-set) | probe run before/after; `--dump bixefi-77-moki051`; scratch `find-mismatch.ts` |
| 2026-09-10 | T1 | The 3 fallers (`perate-09-gale335` 161->126, `ribapo-84-xudu593` 161->126, `rarodo-65-fudu505` 20->16) are the corpus's ONLY three fixtures with `skinparam style strictuml`. `SkinParam.java:1305-1308`'s `arrows()` returns `ArrowsTriangle` (3 points/6 numbers), not `ArrowsRegular`, under `strictUmlStyle()`; `ArrowsTriangle` stays filed/unported (decisions.md D3). Our port always draws `ArrowsRegular` (4 points/8 numbers), so for these 3 the count now MISMATCHES the jar's triangle (was previously an incidental count MATCH: old triangle was also 3 points/6 numbers, coincidentally equal to `ArrowsTriangle`'s count, so it fell through to the same per-index comparison the main mechanism describes). The count-mismatch after T1 collapses back to a single short-circuit diff, lowering these 3 fixtures' scores -- a comparator-granularity artifact in the opposite direction, not a correctness regression; using `ArrowsRegular` unconditionally is exactly what D3 mandates. | `SkinParam.java:1305-1308`; `grep -rl strictuml test-results/dot-cache/activity/*/in.puml` returns exactly these 3 slugs | `--dump rarodo-65-fudu505` (jar polygon `31.638,45,39.638,45,35.638,55`, 3 points) |
| 2026-09-10 | B1 gate | Batch 1 merged: T2 `275e17b1` (ff), T1 `c0ffd177` (rebased; `docs/catalog.md` conflict resolved by `npm run catalog`, no residual drift). Gates: typecheck 0, lint 0, build 0; `npm test` 704 files / 19494 tests with ONE red file, `activity.diff-baseline.ratchet.test.ts`, 253 failed = exactly T1's 253 pre-named polygon risers. Probe `42511 -> 52956`, subset `6752 -> 8714`, risers 253, fallers 3, throws 0. | The T1 quality bar allows the ratchet red; the ratchet is re-pinned at T6 (same shape as `activity-parallel-connectors`). No file outside the two write-sets moved. | `scratchpad b1-*.log`, `b1.json` |
| 2026-09-10 | B1 gate | Stop 6's "42511 exactly" for T2/T3/T4 is read as "the aggregate is unchanged by the task": T2 held 42511 before T1 landed; T3 and T4 must hold 52956 with the identical per-fixture rows (`b1.json`). | Stop 6's intent is "pure porting moves nothing"; T1 was always going to move the polygon family before T3/T4 ran (batch 1 is parallel). Not a decisions.md contradiction. | README stop 6, batch-1 overview |
| 2026-09-10 | B1 gate | Exit-bar risk named now: T1's +10445 is comparator granularity (4-point heads now pair per index, 8 diffs per head instead of 1), and the tips will stay offset from the jar's by the unported root margin (`Recentred`, 12 vs 16) after T5. Σ may not fall below 42511 even if every slot is removed as the jar does. T6 assesses against BOTH 42511 and 52956 and against the x/width families, and halts per its "ask first" if Σ did not fall. | `weightedScore` is anti-monotone under structural convergence (`.agent-notes/apc-T6.md`, memory). | T1 journal row; `b1.json` |
| 2026-09-10 | T2 review | T2 did not port `SlotSet#drawDebugX` (`SlotSet.java:120-125`) or the two `toString()`s; accepted. | Debug-only drawing never reached from the compress path; no consumer in D2's dispatch. Verified `smaller`/`reverse`/`getCompressDelta` line for line against `SlotSet.java:83-118`, `CompressionTransform.java:49-66`. | commit `275e17b1` body |
| 2026-09-10 | T3 | `split-bar`/`split-join-bar` map to NO shape, deviating from a literal reading of this task's own brief (which grouped them with the fork/join bars as `rect, ignoreX`). Read `FtileThinSplit.java:87-96`: `drawU` draws `ULine.hline(last-first)`, not a `URectangle` — our own `renderSplitLine` (`activity-renderer-bars.ts`) already draws a `<line>`, matching. `SlotFinder#draw`'s dispatch (`SlotFinder.java:78-100`) has no `ULine` branch, so it never occupies. Only `FtileBlackBlock#drawU` (fork/join) draws a real `URectangle.ignoreForCompressionOnX()`. | Brief paraphrase conflated split with fork; the Java is the spec. Consequence measured on `simuti-16-lece058`: excluding the split line opens a genuine 1.5px-wide compressible sliver at each split line (`y:[57,58.5]` and `[178.5,180]`, both size 1.5) that a literal-brief `ignoreX` rect would have prevented. | `src/diagrams/activity/layout/compress/shapes-of.ts` `shapeForNode` doc; `t3-debug-simuti.ts` dump |
| 2026-09-10 | T3 | `if`/`if-merge` and `repeat`/`repeat-cond` get NO hexagon reservation, even though the task brief cites `FtileIfDown.java:349,402,440` for it. Read the surrounding methods (`ConnectionElse1`/`ConnectionElse2`/`ConnectionElseHline`, `:304-460`): `FtileIfDown` is upstream's COLLAPSED single-branch-if layout (`diamond1`=split, `diamond2`=merge, ONE `thenBlock`, the "no" side has NO box at all — the reservation sits beside the BYPASS snake that routes around the empty side). Our `GtileIf`/walker (`tile-layout.ts#tileIf`, always builds a `then` AND an `else` `GtileTopDown` branch, even when the source has no `else` keyword) never takes that bypass route — it always walks two symmetric branch positions via `GConnectionSideThenVerticalThenSide`, so `x2`/`xmax`/`y2` (diamond2-relative quantities `FtileIfDown` computes) have no counterpart in our geometry. Confirmed no equivalent `UEmpty(5, hexagonHalfSize)` exists anywhere in `FtileRepeat.java` (`grep -rn hexagonHalfSize`) — repeat never gets this reservation upstream either. | Stop 4 forbids inventing a position without a citable Java `file:line`; the cited sites describe a layout shape ours does not build. Filed as the same pre-existing gap T0 already named for `pujozo-36-nino158` ("the else-branch routing are pre-existing and outside this mission"). | `FtileIfDown.java:74-160,304-460`; `tile-layout.ts:100-121`; `tile-coordinates.ts:183-231` (`gtile-if` case) |
| 2026-09-10 | T3 | `while`/`while-header`'s back-edge DOES get the `UEmpty(5, Hexagon.hexagonHalfSize)` reservation — our `gtile-while` walker's back-edge (`GConnectionVerticalDownThenBack`) structurally matches `ConnectionBackSimple`'s own right-side-detour shape byte for byte (both go from the body's south exit, right to a fixed x, down/up, then left into the header). Placed at `(backFrom.x, Math.max(backFrom.y, bY + body.height) + 12)`, ported as `whileHexagonReservation` (new sibling `layout/hexagon-reservations.ts`, pre-approved name). | Direct structural correspondence, unlike `if`. | `FtileWhile.java:227-273`, cited lines `:264,272`; `Hexagon.java:46` |
| 2026-09-10 | T3 | Divider reservations (`LaneDivider#drawU`'s `UEmpty(x1+x2, 1)`) are computed in `swimlane-placement.ts#computeDividers`'s existing origin loop (one per boundary, n lanes -> n+1 reservations) rather than re-derived later from `SwimlaneGeo` alone: the reservation's LEFT edge is `xpos` (the running accumulator BEFORE this boundary's own `x1` padding), while `SwimlaneGeo.x` (`dividerX[i]`) is `xpos + x1` — the drawn LINE's position, not the reservation's edge. The two are related but not interchangeable, and `SwimlaneGeo` alone (even with `contentX`/`contentWidth`) cannot recover `x1`/`x2` without re-threading `min`/title-overflow inputs, so the reservation is captured where those values already exist. | Avoids a second, drifting implementation of `halfMissingSpace`; verified algebraically (`dividerX[i] = left - x2`, `left = xpos + dividerWidth + centring`, so `dividerX[i] = xpos + x1`) and against a hand-worked no-overflow fixture (`tests/.../swimlane-placement.test.ts`, `x1=x2=5` uniformly). | `LaneDivider.java:85-97`; `Swimlanes.java:337-348`; `swimlane-placement.ts#computeDividers` |
| 2026-09-10 | T3 | `shapesOf`'s `swimlanes` field (required by the interface contract) is accepted but not read: divider and title-band occupancy come entirely from `reservations` (computed upstream where `baseY` is in scope), and no per-lane BACKGROUND rect is emitted at all — `Swimlanes.java:330-338`'s per-lane `back` rect only draws `if (swimlane.getColors().getColor(BACK) != null)`, and no `SwimlaneGeo.backgroundColor` (or any lane-colour) field exists anywhere in the AST/geometry today, so that shape kind is structurally unreachable in this port. | Kept for interface parity per the task contract; wiring it to real per-lane colours is future work, not inventable without a data source. | `Swimlanes.java:330-338`; `activity-layout-types.ts` (`SwimlaneGeo`, no colour field) |
| 2026-09-10 | T3 | Edge-label occupancy is measured with the bounder at `activityFontSize(theme,'arrow')`, deliberately diverging from `renderer.ts#renderEdgeLabel`'s own `label.length * (size * 0.6)` width estimate. NOT changing the renderer (README, "does not change the renderer's edge-label width approximation"). | The slot finder needs the TRUE occupied width (matching `TextLimitFinder`'s contract); the renderer's approximation is a separately filed, pre-existing rendering shortcut. | `shapes-of.ts#edgeLabelShape`; README "What this mission does NOT do" |
| 2026-09-10 | T3 | Dry run (`t3-slots.ts`, 5 T0-dumped fixtures) vs `.agent-notes/akc-T0.md` predictions: `zizaki-04-guvi945` x AGREES exactly (`[19,21][65.7,83.7][128.4,130.4]`, total 22). `simuti-16-lece058` x AGREES (two 18s, total 36); y DISAGREES — T0 predicted "nothing bites" (0), measured total 3 (`[57,58.5]`+`[178.5,180]`, 1.5 each). Wrong prediction, not a bug: T0 predated the split-bar-is-a-`ULine` finding above; excluding the split line's own 1.5px from occupancy is exactly what `FtileThinSplit.java:87-96` requires. `bixefi-77-moki051` x AGREES exactly (230.75+2=232.75). `pujozo-36-nino158` x AGREES (0, "likely 0 once the divider occupies its padding" — confirmed); y DISAGREES — T0 gave no explicit y prediction for pujozo, and I measured total 10 (`[19,23]`+`[184,190]`), the SAME title-band mechanism as below. `nomeco-93-minu967` x AGREES (0); y DISAGREES — T0 predicted 20 removed from the raw `[144,174]` gap; measured 2. Mechanism: the `while` hexagon reservation above (`y1bis=156`, height 12) lands INSIDE that exact gap, splitting it into `[144,156]` (12 wide, shrinks to 2) and `[156+12=168,174]` (6 wide, discarded entirely) — T0 itself flagged this exact possibility unresolved ("if the 20 is in fact our loop-back footprint's bottom band ... that is a T5 finding to name, not a reason to reserve it"); now named. | Every x-axis prediction (all 5) is exact; every y disagreement traces to a mechanism this task's own reservations/exclusions introduce, each independently re-derivable from the printed shape dump. | `t3-slots.ts`, `t3-debug-{nomeco,pujozo,simuti,bixefi}.ts` (scratchpad, not committed) |
| 2026-09-10 | T3 | New finding beyond T0's dump: EVERY multi-lane fixture Y-compresses by a fixed 4px (`[19,23]`) from the title band's own `ignoreForCompressionOnY()` — `URectangle#drawWhenCompressed`'s ON_Y branch reserves only `UEmpty(w,2)` at the band's own top and bottom (2px each), leaving the band's own MIDDLE (band height 18 -> a 14px gap, `[14,28]`) compressible since nothing else occupies it. Confirmed present on both `pujozo` (2 lanes) and `bixefi` (3 lanes). Faithful behavior, not a defect — the same formula the jar's own `URectangle` would apply to an identical-height ignored rect. | Not this mission's to "fix"; it is what porting `Swimlanes.java:358-367`'s ignore flags literally produces. T5 should expect this constant 4px Y-removal on every laned fixture's aggregate. | `URectangle.java:200-206`; `t3-debug-{pujozo,bixefi}.ts` |
| 2026-09-10 | T3 fix (coordinator review) | SUPERSEDES the row above: the "every multi-lane fixture Y-compresses by 4px" finding was INCOMPLETE — it modeled the title band's rect but not what fills its middle in the jar, the lane TITLE. `Swimlanes#drawTitles` draws each title as `ug.draw(new CenteredText(swTitle, width))` (`Swimlanes.java:369-375`). `CenteredText implements UShape` directly (`ftile/CenteredText.java:26`, no `UText` relation), and `SlotFinder#draw`'s dispatch (`SlotFinder.java:78-100`) has no branch for it, so on the ON_X pass (the raw block drawn straight into a fresh `SlotFinder`, `CompressionXorYBuilder.java:60-63`) it never occupies — the X result is unaffected. But `ActivityDiagram3.java:209-210` wraps the ON_X builder inside the ON_Y builder, so ON_Y's `SlotFinder` never sees the raw block directly: `CompressionXorYBuilder(ON_Y).getAffineTransform` calls `onXBuilder.drawU(ySlotFinder)`, and `onXBuilder.drawU` is `rawSwimlanes.drawU(UGraphicCompressOnXorY.create(ON_X, ySlotFinder, xAffine))` — so the raw block draws through an ON_X-mode `UGraphicCompressOnXorY` wrapping `ySlotFinder`. That wrapper's `CenteredText` branch (`UGraphicCompressOnXorY.java:100-112`) does NOT forward the `CenteredText` shape at all: it calls `text.drawU(getUg().apply(getTranslate(x,y))...)`, where `getTranslate` for `mode==ON_X` is `(ct(x), y)` — X is compressed, Y PASSES THROUGH UNCHANGED — and `text.drawU` emits a genuine `UText` straight into `getUg()` (here, `ySlotFinder`). So the title occupies Y exactly like an ordinary `UText`, via `TextLimitFinder`'s `[y-h+1.5, y+1.5]`. Added `CompressShape.kind === 'centeredText'` (`shapes-of.ts#titleShapes`, one per lane when `swimlaneBand !== undefined`, position/font read from `activity-renderer-swimlanes.ts#renderSwimlaneTitles:115-126`, READ ONLY, never edited) and `collectSlots`'s dispatch: skipped on `'x'`, same as `'text'` on `'y'` (`slot-finder.ts#addCenteredTextSlot`). Re-ran `t3-slots.ts`: **`bixefi-77-moki051` y: `0`** (the title's `[y-h+1.5,y+1.5]` fully closes the band's `[14,28]` middle at the default 18pt font — verified: baseline 26, height 18 -> `[9.5,27.5]`, which together with the band's own `[12,14]`/`[28,30]` reservations leaves no survivable gap). **`pujozo-36-nino158` y: `6`** (was 10) — the `[19,23]` band-middle slot is now gone (same closure); only `[184,190]` (the hexagon-to-branch-arrowhead gap, unaffected by this fix) remains. `zizaki`/`simuti`/`nomeco` are unchanged (no lanes). This "0/near-0" outcome is a property of the DEFAULT `SwimlaneTitleFontSize` (18) relative to the band's own height (also 18, `measureSwimlaneTitlesHeight`'s floor); it is not asserted as universal for every possible title-font-size override — a much smaller title font could still leave a survivable gap, and that case is not verified here. | Coordinator-supplied mechanism, verified against the cited Java before implementing (`UGraphicCompressOnXorY.java`, `CompressionXorYBuilder.java`, `CenteredText.java` all re-read in full). | `src/diagrams/activity/layout/compress/shapes-of.ts#titleShapes`; `src/diagrams/activity/layout/compress/slot-finder.ts#addCenteredTextSlot`; `t3-slots.ts`, `t3-debug-{pujozo,bixefi}.ts` (scratchpad, not committed) |
| 2026-09-10 | B2 gate | Batch 2 landed as two commits: T3 `5d0e66d9` + `fix(akc-T3)` `c744c255` (orchestrator review found the Y-pass title occupancy: `CenteredText` has no `SlotFinder` branch, but the ON_Y finder receives the block through the ON_X `UGraphicCompressOnXorY`, whose `CenteredText` branch (`:100-112`) emits `UText`). Gates: typecheck 0, lint 0, build 0; `npm test` 706 files / 19549 tests, the ratchet the only red file (253, identical set). Probe `42511 -> 52956`, per-fixture identical to `b1.json`. | Stop 6 held (unchanged geometry). The fix commit is the one quality-gate fix the rules allow. | scratchpad `b2-*.log`, `b2.json` |
| 2026-09-10 | B2 gate | Accepted two undeclared sibling modules, `layout/walk-while-branch.ts` and `layout/assign-coordinates-full.ts`, plus `tests/diagrams/activity/layout/swimlane-placement.test.ts`, outside T3's literal write-set. | README "Push forward": a sibling module when a file would cross the 500-line hook (`tile-coordinates.ts` was at 478; `swimlane-placement.ts` is now 496). The test file pins a write-set file's new reservations. Not stop 1: nothing outside `layout/`. | `git diff --name-only HEAD~2` |
| 2026-09-10 | B2 gate | Accepted T3's two brief-vs-Java corrections: split lines occupy nothing (`FtileThinSplit.java:87-96` is a `ULine`), and else-less `if`s get no `UEmpty(5,12)` because `FtileIfDown`'s bypass route (`ConnectionElse*`) has no counterpart in our walker (the filed if/switch connector-shape divergence). | Both read from the Java; the brief's grouping was a summary. `simuti` will Y-compress 1.5 per split line (ours starts branch drops at `line + 1.5`, the jar at `line + 1.5` too but with 20 px rows — our 21.5 row is the pre-existing split-builder difference). | T3 journal rows |
