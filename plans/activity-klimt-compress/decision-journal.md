# Decision journal — `activity-klimt-compress`

Appended during execution. One row per non-trivial judgment call: if a
reasonable developer might have chosen differently, log it.

Log the execution plan here at startup (autonomous mode replaces the
user-review step with this entry — `rules/parallelism.md`).

| Date | Task | Decision | Rationale | Evidence |
|---|---|---|---|---|
| 2026-09-10 | plan | Execution plan: B0 T0 (orchestrator) → B1 T1 ‖ T2 (two `typescript-pro` agents in separate worktrees, disjoint write-sets) → B2 T3 → B3 T4 → B4 T5 → B5 T6 (orchestrator). Probe reused from `activity-parallel-connectors`, extended with counts, family tables and `--removed`. | `rules/parallelism.md` autonomous exception: plan logged here instead of user review. B1 runs in worktrees per `.agent-notes/batch-parallelism-needs-worktrees.md`. | `.agent-notes/akc-T0.md` |
| 2026-09-10 | T0 | Chose `pujozo-36-nino158` as the laned-`if` dump and `nomeco-93-minu967` as the `while` dump. | Smallest-scored fixtures whose source has lanes + `if` (no fork/while/repeat) and `while` only (no lanes/fork/if/repeat), so each dump isolates one mechanism. | probe rows sorted by score |
| 2026-09-10 | T0 | Recorded that `Recentred`, not compression, absorbs the jar's leading 14 px `xMargin` on splits; `simuti`'s left residual after T5 is filed to `activity-canvas-margin`, not a target here. | `SlotSet#reverse()` emits only gaps between occupied slots; `AbstractParallelFtilesBuilder.java:130` `xMargin = 14`; `ActivityDiagram3.java:212`. | `.agent-notes/akc-T0.md` |
| 2026-09-10 | T0 | Predicted a 20 px Y removal on `nomeco-93-minu967` (ours only) — named now so T5 does not treat it as an unexplained riser or reserve it. | The out-edge leaves 20 below the action; only a `ULine` sits in `[144,174]`. | `.agent-notes/akc-T0.md` |
| 2026-09-10 | T1 | Ported `ArrowsRegular` as `arrows-regular.ts` (`arrowDirection`, `arrowHeadPoints`, `arrowHeadExtents`) and made `renderer.ts#arrowTip` draw it, replacing the old continuously-rotated 3-point/8-long/±3.2 triangle. `polygon[]/@points` (whole-attribute, count-mismatch family) fell 1722 -> 230 aggregate, 300 -> 18 subset, exactly as the mission predicted. Per fixture class (whole-attribute + per-index points diffs, before -> after): fork/split (32 fixtures) 300 -> 2262; laned (43) 284 -> 2016; hexagon/if/while/repeat (114) 1006 -> 6277; other (79) 221 -> 1701. Sum of the four deltas is 10445, exactly the aggregate `weightedScore` rise (52956-42511); every non-polygon family (`line[]/@x1`/`@x2`/`@y1`/`@y2`, `rect[]/@x`/`@y`/`@width`, `text[]/@x`/`@y`, `ellipse[]/@cx`/`@cy`, `svg/@width`/`@height`/`@viewBox[]`, `childCount`) is byte-identical before and after -- confirming the entire aggregate movement is this one family. | `ArrowsRegular.java:41-86`; point order and values verified against the four Java methods; `Direction.fromVector` (`utils/Direction.java:110-128`) ported for the cardinal cases. | `src/diagrams/activity/arrows-regular.ts`; scratch `family-by-class.ts` run before (git-stashed) and after |
| 2026-09-10 | T1 | `arrowDirection` diverges from `Direction.fromVector`'s diagonal throw (`Direction.java:128`, `"Not a H or V line!"`): picks the dominant axis (`\|dy\| >= \|dx\|` -> vertical, ties vertical) instead of throwing, because our edges are not always axis-aligned (e.g. `nomeco-93-minu967`'s out-edge `(208.638,164) -> (218.638,184)`), a case upstream's `Worm` structurally cannot produce. For a zero vector, returns `'down'` instead of upstream's `null`; `arrowTip`'s pre-existing `dx===0 && dy===0` guard means this value is never read for a drawn polygon. | Upstream never calls `fromVector` on a non-axis-aligned segment; ours does, from pre-existing, out-of-scope edge-routing imprecision. | `src/diagrams/activity/arrows-regular.ts` JSDoc on `arrowDirection`; `tests/unit/activity/arrows-regular.test.ts` diagonal block |
| 2026-09-10 | T1 | Aggregate `weightedScore` rose 42511 -> 52956 (253 risers, 3 fallers, 0 throws); mechanism stated and verified by an A/B toggle (git-stash the renderer change, re-run the probe: reverted code reproduces the pinned 42511 exactly, confirming the rise is caused solely by this task's change). Before T1, every arrowhead's point count (6, our old triangle) mismatched the jar's `ArrowsRegular` count (8), so `compare.ts`'s `points`/`viewBox` numeric-count-mismatch branch (`compare.ts:426-431`) charged exactly ONE diff per arrowhead regardless of how far off its position was (confirmed: the reverted run shows zero entries under the per-index `polygon[]/@points[]` bucket). After T1, counts match (8==8) on every default-skin edge, so the comparator falls through to its per-index loop (`compare.ts:432-441`) and reports one diff per numeral whose delta exceeds tolerance — up to 8 per arrowhead instead of 1 — for every arrowhead whose absolute tip position does not yet coincide with the jar's, because tip position is inherited from lane/margin/compression geometry this task does not touch (`activity-canvas-margin`, filed; T2-T5, not yet landed). The underlying SHAPE is not wrong: on `bixefi-77-moki051` every one of the 6 dumped arrowheads' 4 points, expressed relative to their own tip, are `(-4,-10),(0,0),(4,-10),(0,-6)` on BOTH sides (byte for byte) -- only the tip translation differs, and by a constant amount per arrowhead (e.g. arrowhead 1: ours `(55.675,51)..(59.675,61)` tip, jar's `(71.675,56.5)..(75.675,66.5)` tip, a constant `(16, 5.5)` offset across all 4 points). This is the documented "weightedScore can rise on a correct fix" pattern (`.agent-notes/weightedscore-can-rise-on-a-correct-fix.md`); not a defect in this port, not fitted, not re-pinned. | A/B via `git stash push -- src/diagrams/activity/renderer.ts docs/catalog.md tests/unit/activity/renderer.test.ts` + probe (reproduced 42511 exactly, 0 risers) then `git stash pop`; `--dump bixefi-77-moki051`; scratch script enumerating `polygon[\d+]/@points$` (whole-attribute) mismatches, all 5 sampled hits were 6-vertex hexagon/diamond shapes where the jar repeats its first point to close the polygon and ours does not (pre-existing, unrelated to arrowheads, out of T1's write-set) | probe run before/after; `--dump bixefi-77-moki051`; scratch `find-mismatch.ts` |
| 2026-09-10 | T1 | The 3 fallers (`perate-09-gale335` 161->126, `ribapo-84-xudu593` 161->126, `rarodo-65-fudu505` 20->16) are the corpus's ONLY three fixtures with `skinparam style strictuml`. `SkinParam.java:1305-1308`'s `arrows()` returns `ArrowsTriangle` (3 points/6 numbers), not `ArrowsRegular`, under `strictUmlStyle()`; `ArrowsTriangle` stays filed/unported (decisions.md D3). Our port always draws `ArrowsRegular` (4 points/8 numbers), so for these 3 the count now MISMATCHES the jar's triangle (was previously an incidental count MATCH: old triangle was also 3 points/6 numbers, coincidentally equal to `ArrowsTriangle`'s count, so it fell through to the same per-index comparison the main mechanism describes). The count-mismatch after T1 collapses back to a single short-circuit diff, lowering these 3 fixtures' scores -- a comparator-granularity artifact in the opposite direction, not a correctness regression; using `ArrowsRegular` unconditionally is exactly what D3 mandates. | `SkinParam.java:1305-1308`; `grep -rl strictuml test-results/dot-cache/activity/*/in.puml` returns exactly these 3 slugs | `--dump rarodo-65-fudu505` (jar polygon `31.638,45,39.638,45,35.638,55`, 3 points) |
| 2026-09-10 | B1 gate | Batch 1 merged: T2 `275e17b1` (ff), T1 `c0ffd177` (rebased; `docs/catalog.md` conflict resolved by `npm run catalog`, no residual drift). Gates: typecheck 0, lint 0, build 0; `npm test` 704 files / 19494 tests with ONE red file, `activity.diff-baseline.ratchet.test.ts`, 253 failed = exactly T1's 253 pre-named polygon risers. Probe `42511 -> 52956`, subset `6752 -> 8714`, risers 253, fallers 3, throws 0. | The T1 quality bar allows the ratchet red; the ratchet is re-pinned at T6 (same shape as `activity-parallel-connectors`). No file outside the two write-sets moved. | `scratchpad b1-*.log`, `b1.json` |
| 2026-09-10 | B1 gate | Stop 6's "42511 exactly" for T2/T3/T4 is read as "the aggregate is unchanged by the task": T2 held 42511 before T1 landed; T3 and T4 must hold 52956 with the identical per-fixture rows (`b1.json`). | Stop 6's intent is "pure porting moves nothing"; T1 was always going to move the polygon family before T3/T4 ran (batch 1 is parallel). Not a decisions.md contradiction. | README stop 6, batch-1 overview |
| 2026-09-10 | B1 gate | Exit-bar risk named now: T1's +10445 is comparator granularity (4-point heads now pair per index, 8 diffs per head instead of 1), and the tips will stay offset from the jar's by the unported root margin (`Recentred`, 12 vs 16) after T5. Σ may not fall below 42511 even if every slot is removed as the jar does. T6 assesses against BOTH 42511 and 52956 and against the x/width families, and halts per its "ask first" if Σ did not fall. | `weightedScore` is anti-monotone under structural convergence (`.agent-notes/apc-T6.md`, memory). | T1 journal row; `b1.json` |
| 2026-09-10 | T2 review | T2 did not port `SlotSet#drawDebugX` (`SlotSet.java:120-125`) or the two `toString()`s; accepted. | Debug-only drawing never reached from the compress path; no consumer in D2's dispatch. Verified `smaller`/`reverse`/`getCompressDelta` line for line against `SlotSet.java:83-118`, `CompressionTransform.java:49-66`. | commit `275e17b1` body |
| 2026-09-10 | T3 | `split-bar`/`split-join-bar` map to NO shape, deviating from a literal reading of this task's own brief (which grouped them with the fork/join bars as `rect, ignoreX`). Read `FtileThinSplit.java:87-96`: `drawU` draws `ULine.hline(last-first)`, not a `URectangle` — our own `renderSplitLine` (`activity-renderer-bars.ts`) already draws a `<line>`, matching. `SlotFinder#draw`'s dispatch (`SlotFinder.java:78-100`) has no `ULine` branch, so it never occupies. Only `FtileBlackBlock#drawU` (fork/join) draws a real `URectangle.ignoreForCompressionOnX()`. | Brief paraphrase conflated split with fork; the Java is the spec. Consequence measured on `simuti-16-lece058`: excluding the split line opens a genuine 1.5px-wide compressible sliver at each split line (`y:[57,58.5]` and `[178.5,180]`, both size 1.5) that a literal-brief `ignoreX` rect would have prevented. | `src/diagrams/activity/layout/compress/shapes-of.ts` `shapeForNode` doc; `t3-debug-simuti.ts` dump |
| 2026-09-10 | T3 | `if`/`if-merge` and `repeat`/`repeat-cond` get NO hexagon reservation, even though the task brief cites `FtileIfDown.java:349,402,440` for it. Read the surrounding methods (`ConnectionElse1`/`ConnectionElse2`/`ConnectionElseHline`, `:304-460`): `FtileIfDown` is upstream's COLLAPSED single-branch-if layout (`diamond1`=split, `diamond2`=merge, ONE `thenBlock`, the "no" side has NO box at all — the reservation sits beside the BYPASS snake that routes around the empty side). Our `GtileIf`/walker (`tile-layout.ts#tileIf`, always builds a `then` AND an `else` `GtileTopDown` branch, even when the source has no `else` keyword) never takes that bypass route — it always walks two symmetric branch positions via `GConnectionSideThenVerticalThenSide`, so `x2`/`xmax`/`y2` (diamond2-relative quantities `FtileIfDown` computes) have no counterpart in our geometry. Confirmed no equivalent `UEmpty(5, hexagonHalfSize)` exists anywhere in `FtileRepeat.java` (`grep -rn hexagonHalfSize`) — repeat never gets this reservation upstream either. | Stop 4 forbids inventing a position without a citable Java `file:line`; the cited sites describe a layout shape ours does not build. Filed as the same pre-existing gap T0 already named for `pujozo-36-nino158` ("the else-branch routing are pre-existing and outside this mission"). | `FtileIfDown.java:74-160,304-460`; `tile-layout.ts:100-121`; `tile-coordinates.ts:183-231` (`gtile-if` case) |
| 2026-09-10 | T3 | `while`/`while-header`'s back-edge DOES get the `UEmpty(5, Hexagon.hexagonHalfSize)` reservation — our `gtile-while` walker's back-edge (`GConnectionVerticalDownThenBack`) structurally matches `ConnectionBackSimple`'s own right-side-detour shape byte for byte (both go from the body's south exit, right to a fixed x, down/up, then left into the header). Placed at `(backFrom.x, Math.max(backFrom.y, bY + body.height) + 12)`, ported as `whileHexagonReservation` (new sibling `layout/hexagon-reservations.ts`, pre-approved name). | Direct structural correspondence, unlike `if`. | `FtileWhile.java:227-273`, cited lines `:264,272`; `Hexagon.java:46` |
| 2026-09-10 | T3 | Divider reservations (`LaneDivider#drawU`'s `UEmpty(x1+x2, 1)`) are computed in `swimlane-placement.ts#computeDividers`'s existing origin loop (one per boundary, n lanes -> n+1 reservations) rather than re-derived later from `SwimlaneGeo` alone: the reservation's LEFT edge is `xpos` (the running accumulator BEFORE this boundary's own `x1` padding), while `SwimlaneGeo.x` (`dividerX[i]`) is `xpos + x1` — the drawn LINE's position, not the reservation's edge. The two are related but not interchangeable, and `SwimlaneGeo` alone (even with `contentX`/`contentWidth`) cannot recover `x1`/`x2` without re-threading `min`/title-overflow inputs, so the reservation is captured where those values already exist. | Avoids a second, drifting implementation of `halfMissingSpace`; verified algebraically (`dividerX[i] = left - x2`, `left = xpos + dividerWidth + centring`, so `dividerX[i] = xpos + x1`) and against a hand-worked no-overflow fixture (`tests/.../swimlane-placement.test.ts`, `x1=x2=5` uniformly). | `LaneDivider.java:85-97`; `Swimlanes.java:337-348`; `swimlane-placement.ts#computeDividers` |
| 2026-09-10 | T3 | `shapesOf`'s `swimlanes` field (required by the interface contract) is accepted but not read: divider and title-band occupancy come entirely from `reservations` (computed upstream where `baseY` is in scope), and no per-lane BACKGROUND rect is emitted at all — `Swimlanes.java:330-338`'s per-lane `back` rect only draws `if (swimlane.getColors().getColor(BACK) != null)`, and no `SwimlaneGeo.backgroundColor` (or any lane-colour) field exists anywhere in the AST/geometry today, so that shape kind is structurally unreachable in this port. | Kept for interface parity per the task contract; wiring it to real per-lane colours is future work, not inventable without a data source. | `Swimlanes.java:330-338`; `activity-layout-types.ts` (`SwimlaneGeo`, no colour field) |
| 2026-09-10 | T3 | Edge-label occupancy is measured with the bounder at `activityFontSize(theme,'arrow')`, deliberately diverging from `renderer.ts#renderEdgeLabel`'s own `label.length * (size * 0.6)` width estimate. NOT changing the renderer (README, "does not change the renderer's edge-label width approximation"). | The slot finder needs the TRUE occupied width (matching `TextLimitFinder`'s contract); the renderer's approximation is a separately filed, pre-existing rendering shortcut. | `shapes-of.ts#edgeLabelShape`; README "What this mission does NOT do" |
| 2026-09-10 | T3 | Dry run (`t3-slots.ts`, 5 T0-dumped fixtures) vs `.agent-notes/akc-T0.md` predictions: `zizaki-04-guvi945` x AGREES exactly (`[19,21][65.7,83.7][128.4,130.4]`, total 22). `simuti-16-lece058` x AGREES (two 18s, total 36); y DISAGREES — T0 predicted "nothing bites" (0), measured total 3 (`[57,58.5]`+`[178.5,180]`, 1.5 each). Wrong prediction, not a bug: T0 predated the split-bar-is-a-`ULine` finding above; excluding the split line's own 1.5px from occupancy is exactly what `FtileThinSplit.java:87-96` requires. `bixefi-77-moki051` x AGREES exactly (230.75+2=232.75). `pujozo-36-nino158` x AGREES (0, "likely 0 once the divider occupies its padding" — confirmed); y DISAGREES — T0 gave no explicit y prediction for pujozo, and I measured total 10 (`[19,23]`+`[184,190]`), the SAME title-band mechanism as below. `nomeco-93-minu967` x AGREES (0); y DISAGREES — T0 predicted 20 removed from the raw `[144,174]` gap; measured 2. Mechanism: the `while` hexagon reservation above (`y1bis=156`, height 12) lands INSIDE that exact gap, splitting it into `[144,156]` (12 wide, shrinks to 2) and `[156+12=168,174]` (6 wide, discarded entirely) — T0 itself flagged this exact possibility unresolved ("if the 20 is in fact our loop-back footprint's bottom band ... that is a T5 finding to name, not a reason to reserve it"); now named. | Every x-axis prediction (all 5) is exact; every y disagreement traces to a mechanism this task's own reservations/exclusions introduce, each independently re-derivable from the printed shape dump. | `t3-slots.ts`, `t3-debug-{nomeco,pujozo,simuti,bixefi}.ts` (scratchpad, not committed) |
| 2026-09-10 | T3 | New finding beyond T0's dump: EVERY multi-lane fixture Y-compresses by a fixed 4px (`[19,23]`) from the title band's own `ignoreForCompressionOnY()` — `URectangle#drawWhenCompressed`'s ON_Y branch reserves only `UEmpty(w,2)` at the band's own top and bottom (2px each), leaving the band's own MIDDLE (band height 18 -> a 14px gap, `[14,28]`) compressible since nothing else occupies it. Confirmed present on both `pujozo` (2 lanes) and `bixefi` (3 lanes). Faithful behavior, not a defect — the same formula the jar's own `URectangle` would apply to an identical-height ignored rect. | Not this mission's to "fix"; it is what porting `Swimlanes.java:358-367`'s ignore flags literally produces. T5 should expect this constant 4px Y-removal on every laned fixture's aggregate. | `URectangle.java:200-206`; `t3-debug-{pujozo,bixefi}.ts` |
| 2026-09-10 | T3 fix (coordinator review) | SUPERSEDES the row above: the "every multi-lane fixture Y-compresses by 4px" finding was INCOMPLETE — it modeled the title band's rect but not what fills its middle in the jar, the lane TITLE. `Swimlanes#drawTitles` draws each title as `ug.draw(new CenteredText(swTitle, width))` (`Swimlanes.java:369-375`). `CenteredText implements UShape` directly (`ftile/CenteredText.java:26`, no `UText` relation), and `SlotFinder#draw`'s dispatch (`SlotFinder.java:78-100`) has no branch for it, so on the ON_X pass (the raw block drawn straight into a fresh `SlotFinder`, `CompressionXorYBuilder.java:60-63`) it never occupies — the X result is unaffected. But `ActivityDiagram3.java:209-210` wraps the ON_X builder inside the ON_Y builder, so ON_Y's `SlotFinder` never sees the raw block directly: `CompressionXorYBuilder(ON_Y).getAffineTransform` calls `onXBuilder.drawU(ySlotFinder)`, and `onXBuilder.drawU` is `rawSwimlanes.drawU(UGraphicCompressOnXorY.create(ON_X, ySlotFinder, xAffine))` — so the raw block draws through an ON_X-mode `UGraphicCompressOnXorY` wrapping `ySlotFinder`. That wrapper's `CenteredText` branch (`UGraphicCompressOnXorY.java:100-112`) does NOT forward the `CenteredText` shape at all: it calls `text.drawU(getUg().apply(getTranslate(x,y))...)`, where `getTranslate` for `mode==ON_X` is `(ct(x), y)` — X is compressed, Y PASSES THROUGH UNCHANGED — and `text.drawU` emits a genuine `UText` straight into `getUg()` (here, `ySlotFinder`). So the title occupies Y exactly like an ordinary `UText`, via `TextLimitFinder`'s `[y-h+1.5, y+1.5]`. Added `CompressShape.kind === 'centeredText'` (`shapes-of.ts#titleShapes`, one per lane when `swimlaneBand !== undefined`, position/font read from `activity-renderer-swimlanes.ts#renderSwimlaneTitles:115-126`, READ ONLY, never edited) and `collectSlots`'s dispatch: skipped on `'x'`, same as `'text'` on `'y'` (`slot-finder.ts#addCenteredTextSlot`). Re-ran `t3-slots.ts`: **`bixefi-77-moki051` y: `0`** (the title's `[y-h+1.5,y+1.5]` fully closes the band's `[14,28]` middle at the default 18pt font — verified: baseline 26, height 18 -> `[9.5,27.5]`, which together with the band's own `[12,14]`/`[28,30]` reservations leaves no survivable gap). **`pujozo-36-nino158` y: `6`** (was 10) — the `[19,23]` band-middle slot is now gone (same closure); only `[184,190]` (the hexagon-to-branch-arrowhead gap, unaffected by this fix) remains. `zizaki`/`simuti`/`nomeco` are unchanged (no lanes). This "0/near-0" outcome is a property of the DEFAULT `SwimlaneTitleFontSize` (18) relative to the band's own height (also 18, `measureSwimlaneTitlesHeight`'s floor); it is not asserted as universal for every possible title-font-size override — a much smaller title font could still leave a survivable gap, and that case is not verified here. | Coordinator-supplied mechanism, verified against the cited Java before implementing (`UGraphicCompressOnXorY.java`, `CompressionXorYBuilder.java`, `CenteredText.java` all re-read in full). | `src/diagrams/activity/layout/compress/shapes-of.ts#titleShapes`; `src/diagrams/activity/layout/compress/slot-finder.ts#addCenteredTextSlot`; `t3-slots.ts`, `t3-debug-{pujozo,bixefi}.ts` (scratchpad, not committed) |
| 2026-09-10 | B2 gate | Batch 2 landed as two commits: T3 `5d0e66d9` + `fix(akc-T3)` `c744c255` (orchestrator review found the Y-pass title occupancy: `CenteredText` has no `SlotFinder` branch, but the ON_Y finder receives the block through the ON_X `UGraphicCompressOnXorY`, whose `CenteredText` branch (`:100-112`) emits `UText`). Gates: typecheck 0, lint 0, build 0; `npm test` 706 files / 19549 tests, the ratchet the only red file (253, identical set). Probe `42511 -> 52956`, per-fixture identical to `b1.json`. | Stop 6 held (unchanged geometry). The fix commit is the one quality-gate fix the rules allow. | scratchpad `b2-*.log`, `b2.json` |
| 2026-09-10 | B2 gate | Accepted two undeclared sibling modules, `layout/walk-while-branch.ts` and `layout/assign-coordinates-full.ts`, plus `tests/diagrams/activity/layout/swimlane-placement.test.ts`, outside T3's literal write-set. | README "Push forward": a sibling module when a file would cross the 500-line hook (`tile-coordinates.ts` was at 478; `swimlane-placement.ts` is now 496). The test file pins a write-set file's new reservations. Not stop 1: nothing outside `layout/`. | `git diff --name-only HEAD~2` |
| 2026-09-10 | B2 gate | Accepted T3's two brief-vs-Java corrections: split lines occupy nothing (`FtileThinSplit.java:87-96` is a `ULine`), and else-less `if`s get no `UEmpty(5,12)` because `FtileIfDown`'s bypass route (`ConnectionElse*`) has no counterpart in our walker (the filed if/switch connector-shape divergence). | Both read from the Java; the brief's grouping was a summary. `simuti` will Y-compress 1.5 per split line (ours starts branch drops at `line + 1.5`, the jar at `line + 1.5` too but with 20 px rows — our 21.5 row is the pre-existing split-builder difference). | T3 journal rows |
| 2026-09-10 | T4 | `SwimlaneGeo.contentMinX` is left UNCHANGED by `compressGeometry`, deviating from a literal reading of the batch-3 brief's terse list ("lanes `x`, `x + width`, `contentX`, `contentX + contentWidth`, `contentMinX` through `ct`"). Read `Swimlanes.java:416-431`: `contentMinX` is `swimlane.getMinMax().getMinX()`, measured by drawing the lane's own UNTRANSLATED subtree into a `MinMaxUGraphic` BEFORE `computeSizeInternal` computes `xx` (the one-time translate that places that subtree at its final absolute position, `:423-425`). By the time any `ActivityNodeGeo.x` exists, `contentMinX` has already been fully consumed by that translate — it is an offset in the tile's own PRE-translate local frame, not a point on the page `CompressionTransform` operates on. Feeding a small tile-local number through an absolute-coordinate `ct()` would apply compression from removed slots that have no relationship to it. | Stop 4 forbids inventing behavior without a citable `file:line`; the cited method shows `contentMinX` is pre-layout bookkeeping already baked into other fields' positions, structurally analogous to why `measureLanes` itself stays untouched by compression (stop 12). | `Swimlanes.java:416-431`; `src/diagrams/activity/layout/compress/compress-geometry.ts#transformSwimlane` |
| 2026-09-10 | T4 | `split-bar`/`split-join-bar` need a PER-AXIS split beyond the brief's uniform "the four bar kinds" grouping: on X they get the true rect-style `w' = ct(x+w)-ct(x)` (their `renderSplitLine` reads both `x` and `x+width`, and a horizontal `ULine`'s own transform rule — both endpoints through `ct` — produces the IDENTICAL formula for `dy=0`); on Y they do NOT get `h' = ct(y+h)-ct(y)` like `fork-bar`/`join-bar` (real 2-D `URectangle`s, `FtileBlackBlock.java:101-102`) — a `ULine` with `dy=0` collapses `UGraphicCompressOnXorY#drawLine`'s Y-mode branch (`ct(y), x+dx, ct(y+dy)`) to the SAME `ct(y)` for both endpoints, i.e. a plain translate with the (unread-by-the-renderer) `height` bookkeeping field kept as-is. Implemented as two separate kind sets, `RECT_WIDTH_KINDS` (5 kinds) and `RECT_HEIGHT_KINDS` (3 kinds, excluding the two split kinds). Analogous treatment applied to `Reservation`: only a reservation carrying `ignoreX`/`ignoreY` (a real `URectangle`, currently only the swimlane title band) gets the full recompute on both axes; an unflagged reservation (`UEmpty`, hexagon/divider) is translate-only, size kept. | `UGraphicCompressOnXorY.java:90-96` (rect), `:122-127` (line, both axes), `activity-renderer-bars.ts#renderSplitLine` (height unread); verified with a dedicated unit test pair (X-recompute vs Y-height-kept) on a synthetic split-bar fixture. | `src/diagrams/activity/layout/compress/compress-geometry.ts` (`RECT_WIDTH_KINDS`, `RECT_HEIGHT_KINDS`, `isRectReservation`); `tests/diagrams/activity/layout/compress/compress-geometry.test.ts` "split-bar/split-join-bar geometry transform" |
| 2026-09-10 | T4 | `shapesOf`'s required `swimlaneBand` parameter is NOT added as a new `CompressInput` field, despite the batch-3 brief's Task text reading as though it should be threaded through directly. Per this task's own fuller instructions ("On Y, lanes have no y fields of their own; the band/divider y's (`swimlaneBand`, `swimlaneDividerY`) are computed by `computeSwimlaneChrome` from `maxY` — T5 wires that; you transform only what `CompressInput` carries"), the locked interface contract (`CompressInput`/`CompressResult`, given verbatim twice, both times without `swimlaneBand`) stands as written. `findSwimlaneBand` derives the `SwimlaneBandGeo` `shapesOf` needs from `reservations` instead — the one reservation with BOTH `ignoreX` and `ignoreY` set is structurally always the title band (`withBandReservation`, `assign-coordinates-full.ts`, the only call site that ever sets both; hexagon/divider reservations never carry either flag, `swimlane-placement.ts:110`). `CompressResult` likewise does not return a transformed band — T5 recomputes the band and divider fresh from the transformed `swimlanes` and `bounds.maxY`, the only place their own `y` is produced. | Keeps the two-repetition-locked contract intact instead of silently widening it; the derivation is a single, already-unique discriminator, not an invented rule. | `src/diagrams/activity/layout/compress/compress-geometry.ts#findSwimlaneBand`; `assign-coordinates-full.ts#withBandReservation`; `swimlane-placement.ts:110` |
| 2026-09-10 | T4 | Dry run (`t4-dry.ts`, same 5 T0/T3-dumped fixtures, via `assignCoordinatesFull` -> `compressGeometry`) against this task's own stated targets: `zizaki-04-guvi945` x=22 AGREES (bar `12` width `103.4`, boxes at `24`/`68.7`, both matching exactly), y=0 AGREES. `simuti-16-lece058` x=36 AGREES (boxes `26, 69.35, 112.7`), y=3 AGREES. `bixefi-77-moki051` x=232.75 AGREES (lane 3 width `158.175`, bar width `148.175`), y=0 AGREES. `pujozo-36-nino158` x=0 AGREES, y=6 AGREES. `nomeco-93-minu967` x=0 AGREES, y=2 AGREES. All ten values (5 fixtures x 2 axes) match exactly — no disagreements, because this task's targets already carry T3's two corrections (the split-line-is-a-`ULine` and title-`CenteredText` findings) rather than T0's original, since-superseded predictions. | Full agreement is the expected outcome of a pure geometry-application task over T2/T3's already-verified slot arithmetic; a disagreement here would have meant a transform bug, not a modeling one. | `t4-dry.ts` (scratchpad, not committed) |
| 2026-09-10 | B3 gate | Batch 3 landed: T4 `866999be`, write-set exact. Gates: typecheck 0, lint 0, build 0; `npm test` 707 files / 19563 tests, ratchet the only red file (253, identical set). Probe `42511 -> 52956`, per-fixture identical to `b1.json` (stop 6 held). All ten dry-run values (5 fixtures × 2 axes) agree with T3's slot lists. | Pure transform, unconsumed until T5. | scratchpad `b3-*.log`, `b3.json` |
| 2026-09-10 | B3 gate | Accepted T4's two refinements: `contentMinX` (tile-local, consumed before layout) not transformed; split lines get the rect width formula on X (a horizontal `ULine`'s two transformed endpoints coincide with it, `UGraphicCompressOnXorY.java:127-132`) and keep height on Y. `swimlaneBand` derived from the unique `ignoreX && ignoreY` reservation rather than a new input field. | Each cited; none changes a locked decision. | T4 journal rows |
| 2026-09-10 | T5 | Wired `compressGeometry` into `assignCoordinatesFull`: after `placeSwimlanes`/`computeBounds` (pass-1), build the band reservation from pass-1 chrome (`withBandReservation`, unchanged), call `compressGeometry` with the placed nodes/edges/edgeMeta/swimlanes/reservations/pass-1 bounds, then recompute chrome via `computeSwimlaneChrome(compressed.swimlanes, baseY, titlesHeight, compressed.bounds.maxY)` — baseY/titlesHeight stay the ORIGINAL (unconverted) values, per the task's literal instruction ("the TRANSFORMED lanes and the Y-transformed maxY" names only two of the four `computeSwimlaneChrome` arguments as changing). `totalWidth`/`totalHeight` come from `compressed.bounds` (D6). Added `AssignCoordinatesInput.compress?: boolean` (default `true`, internal-only, not on `ActivityGeometry`) so the invariant test's own "before" snapshot can be taken from the same call site without a second parse path. Split the post-layout half into `compressAndAssemble`/`pass1Assemble` to keep `assignCoordinatesFull` under the 30-NLOC hook. Verified `renderSwimlaneTitles` already re-centres correctly from the transformed `contentX`/`contentWidth` with NO renderer edit needed (confirmed by test, not just by reading). | Task instructions, D1/D6/D7. | `src/diagrams/activity/layout/assign-coordinates-full.ts` |
| 2026-09-10 | T5 | Re-pinned the 5 failing assertions in `tile-coordinates.test.ts` (all in raw-`Tile` fork/split/while tests using a fixed 60×16 bounder) — every new number derived from a scratch dump (`t5-debug-fork.ts`, `t5-debug-fork2.ts`) showing the exact raw `SlotSet` gaps: the GtileWhile reservation's y shrinks by 2 (a bare 12px gap between the body's bottom and the reservation, `12-2*5`); the single-branch fork's hook connectors shift by -2 (one 12px end-reservation gap before the branch box); the 2-detached-branch fork's bar width shrinks by 22 (two 12px end gaps + one 28px inter-branch gap: `2+18+2`) and its Y shrinks by 10 (a 20px branch-region-to-join-bar gap, `20-2*5`); the 3-branch split's bar width and the 1-continuing-of-3 join line both shrink by the two 28px inter-branch gaps (18 each). No other file in `tests/unit/activity/**` or `tests/diagrams/activity/**` went red (`swimlane-placement.test.ts`, `canvas-bounds.test.ts`, `layout.test.ts`, `renderer-swimlanes.test.ts` all stayed green — their fixtures have no compressible gap). | `SlotSet#smaller(5)`/`CompressionXorYBuilder.java:56`, `URectangle#drawWhenCompressed`/`klimt/shape/URectangle.java:193-199`, `AbstractParallelFtilesBuilder.java:129-131`. | `tests/diagrams/activity/layout/tile-coordinates.test.ts`; `t5-debug-fork.ts`, `t5-debug-fork2.ts` (scratchpad) |
| 2026-09-10 | T5 | Wrote the invariant test (`tests/diagrams/activity/layout/compress/invariant.test.ts`) over all 268 baseline fixtures: (a) before/after layout via `assignCoordinatesFull`'s new `compress` flag never throws (stop 10 — GREEN, 0 throws); (b) `shapesOf` yields the same shape count/order before and after (GREEN); (c) `overlaps(shapesOf(after))` ⊆ `overlaps(shapesOf(before))` (stop 11 — **RED, 8 violations across 5 fixtures**). See the next two rows for the full diagnosis. STOPPING before commit per stop 11's unconditional wording (unlike stop 7, it carries no "and its mechanism cannot be stated" escape clause) — the code change itself (wiring + 5 re-pins + invariant test) is left uncommitted in the working tree for review. | README stop 11 (hard stop, no push-forward clause). | `tests/diagrams/activity/layout/compress/invariant.test.ts`; `t5-overlap-debug.ts` (scratchpad) |
| 2026-09-10 | T5 diagnosis | 6 of 8 overlap violations (`bixefi-77-moki051` [6,8], `bugaja-31-jaso630` [7,9], `racana-82-zece676` [7,9] and [13,15], `tobajo-64-mipi810` [31,38]) are two `polygonSkipMode:'x'` cross-lane arrowhead polygons (D5, `Worm.java:159-168` — the fork/join elbow decoration skipped from X occupancy) landing on or within 5px of the SAME collapsed x. Mechanism: all 6 fixtures are fork/split-with-swimlanes; the ALREADY-FILED parser lane-capture defect (`node-dispatch.ts:261,291`'s `swimlaneSpread(ctx)` applied AFTER branches parse, filed in `planning/next-missions.md` since `activity-parallel-connectors`/apc-T6) puts the fork/join bar in the WRONG (last-branch's) lane, leaving an abnormally large unoccupied X span in that lane with nothing but the two skip-x arrowheads inside it. `CompressionTransform.getCompressDelta` (`compression-transform.ts:37-48`, a verified line-for-line port of `CompressionTransform.java:53-66`) maps every point strictly inside one removed slot to that slot's own `start` — by design, upstream's own formula — so both arrowheads, never being occupants themselves, collapse onto the same point. Ruled out: a bug in this task's own wiring (`compressAndAssemble` correctly reproduces T4's own pre-verified dry-run numbers for `bixefi` exactly — lane 3 width 158.175, bar width 148.175, both cited in T0/T4's targets); a bug in T2-T4's transform arithmetic (byte-identical to T4's own accepted dry run). Confirmed by re-deriving all 3 T0 dump targets (`simuti`, `zizaki`, `bixefi`) fresh through the live pipeline — all match exactly (see the measurement row below). Not fixable inside T5's write-set: the fix is either in the parser (`node-dispatch.ts`, not in any task's write-set this mission) or a structural anchor in `shapesOf`/`slot-finder.ts` (T2/T3's modules, explicitly "STOP and report" if a change is needed). | `Worm.java:159-168`; `node-dispatch.ts:261,291`; `compression-transform.ts:37-48`/`CompressionTransform.java:53-66`; `.agent-notes/apc-T6.md` ("Filed... Parser lane capture"). | `t5-overlap-debug.ts` dumps for all 6 fixtures (scratchpad) |
| 2026-09-10 | T5 diagnosis | The remaining 2 overlap violations (`rujuxa-07-neco067` [0,35], `sopape-11-laxo488` [2,8] and [3,9]) are a rect/polygon landing near a lane title (`centeredText`) after a LEGITIMATE, faithful compression of dead space inside a lane's own if/else branches or a fork/split's inter-branch gap (not the lane-capture defect — verified: the moved node and the lane title are BOTH correctly inside the SAME lane on both sides of compression, and the shift amounts reconcile exactly with `contentWidth`'s own before/after values). The reported "overlap" is an artifact of `overlaps()`'s box model (`slot-finder.ts:114-126`, authored by T3 specifically for this test): for `'text'`/`'centeredText'` shapes it uses `shape.y`/`shape.height` as a literal top-left box, but `CompressShape`'s own contract (`shapes-of.ts:37-52`) defines `(x,y)` as the BASELINE for those two kinds — the true glyph box is `[y - height + 1.5, y + 1.5]` (`TextLimitFinder`, applied inside `collectSlots#addTextSlot`, never inside `overlaps()`). Recomputing both cases with the correct glyph box (`rujuxa`: rect y=[35,55] vs title's true box [9.5,27.5] — no overlap; `sopape`: polygon y=[40,50] vs title's true box [9.5,27.5] — no overlap) shows NEITHER is a real visual collision. Ruled out: a modeling error in `shapesOf`'s `titleShapes`/`edgeLabelShape` (their stored `x`/`y` match the renderer exactly, `activity-renderer-swimlanes.ts:115-126`); a bug in the compression transform (the lane's `contentX`/`contentWidth` recompute is exactly `ct(contentX+contentWidth)-ct(contentX)`, the documented D7 formula). Not fixable inside T5's write-set: `overlaps()` lives in `slot-finder.ts` (T3's module). | `shapes-of.ts:37-52` (`CompressShape` doc, text baseline contract); `slot-finder.ts:45-51,114-126` (`addTextSlot` vs `overlaps`); `activity-renderer-swimlanes.ts:115-126`. | `t5-overlap-debug.ts` dumps for `rujuxa`/`sopape` (scratchpad) |
| 2026-09-10 | T5 | Measured (`t5.json` vs `b1.json`): aggregate `weightedScore` 52956 → 52954 (net -2, essentially flat — the B1-gate risk note was right: "Σ may not fall below 42511... the tips will stay offset from the jar's by the unported root margin after T5"); subset 8714 → 8709. Against the ORIGINAL pins (42511/6752), still 253 risers (T1's pre-named polygon set, unchanged) + 3 fallers (T1's `strictuml` set, unchanged) — T5 itself moves only 6 fixtures up and 14 down relative to `b1` (248 unchanged; most of the 268 have no fork/split/lane structure to compress). x/width families vs `b1`: `rect[]/@x` 920→920, `text[]/@x` 1455→1455, `line[]/@x1` 2705→2705, `line[]/@x2` 2705→2704, `svg/@width` 266→265, `polygon[]/@points` 230→230 (all near-flat — most pre-T5 mismatches were already off by the SAME small residual, mostly the un-ported root-margin difference, so compression converges the geometry without crossing enough tolerance boundaries to move the SCORE); `rect[]/@width` 149→132 (-17, the family compression is MOST directly built to fix) fell cleanly. 0 throws. `removed` totals (`t5-removed.ts`, all 268): x=5827.175 (85 fixtures nonzero), y=2117.000 (160 fixtures nonzero). `zizaki-04-guvi945`, `simuti-16-lece058`, `bixefi-77-moki051` all match T0's targets EXACTLY through the live pipeline (bar 12/103.4, boxes 24/68.7; boxes 26/69.35/112.7, top line span 86.7 matching the jar's 86.7; lane-3 width 158.175, both bars 148.175). 6 risers vs `b1` (all Y-family, comparator-granularity noise from a real, small, correctly-computed Y-compression crossing a tolerance/pairing boundary — same documented class as `.agent-notes/weightedscore-can-rise-on-a-correct-fix.md`): `racana-82-zece676` +9 (`line[]/@y1`+5/`@y2`+4 — same lane-capture-defect fixture as the overlap finding above), `leduvi-16-voli986` +5, `cifafo-49-jazi415` +3, `fatuzu-07-cevu894` +2, `pujozo-36-nino158` +2 (the ALREADY-NAMED T3/T4 title-band-closure mechanism, `y=6` predicted and confirmed, not new), `sopape-11-laxo488` +1 (the same fixture as the text-overlap finding above). 17-fixture laned-subset divider census (`t5-laned-census.ts`, Euclidean distance of `dividerXs` to the jar's): 14 CLOSER, 1 SAME (`begivo`), 2 FARTHER — both farther fixtures (`maketa-43-juja264`, `noxasi-06-nejo322`) are ALSO fork/split-with-lanes fixtures in the same filed parser lane-capture defect class. Timed the 4 activity oracle gates (`activity.diff-baseline.ratchet`, `activity.style-baseline`, `activity.swimlane-baseline`, `activity.text-baseline`) 3×: 4.771s/5.003s/5.646s, median **5.003s** vs T0's 4.40s (+13.7%, no single test approaches the 5000ms default — max observed 419ms). `npm test`: 708 Test Files, 5 red (`activity.diff-baseline.ratchet` 253, `activity.style-baseline` 176, `activity.swimlane-baseline` 48, `activity.text-baseline` 20 — all EXPECTED, T5 moves geometry and T6 re-pins these baselines per the mission's own design; PLUS `invariant.test.ts` 1 red — the stop-11 finding above, NOT expected, blocking the commit). typecheck/lint/build all exit 0. | `t5.json`, `t5-removed.ts`, `t5-laned-census.ts`, gate logs (all scratchpad, not committed). | `/tmp/gate2-akc-t5-*.log`, `/tmp/akc-t5-fulltest.log` |
| 2026-09-10 | T5 halt | **Stop 11 triggered — HALTED for human input.** T5's invariant test found 8 new post-compression overlaps (5 fixtures); T5 halted without committing. Its work is parked at `e925560e` on branch `akc/T5-wip` (assign-coordinates-full wiring, `compress` flag, the invariant test, 5 re-pinned tile-coordinates assertions, its journal rows). The mission branch stays at batch 3 + one fix. | `rules/autonomous-execution.md`: a brief stop condition is a hard stop; precedent SI32 (human amended D1/D2 after a stop-9 halt). | `git log akc/T5-wip` |
| 2026-09-10 | T5 halt | Orchestrator verification of T5's diagnosis: (1) CONFIRMED an instrument bug — `overlaps()` read every `y` as a box top while text kinds carry the baseline (`TextLimitFinder.java:82-90`); fixed and pinned as `fix(akc-T3)` `99c473c4`, which removed the two `rujuxa`/`sopape` false positives and exposed two real `maketa` ones. (2) With the fixed instrument, 7 violations remain, and EVERY pair has a shape that does not occupy on the axis that moved it: 5 `polygon×polygon` pairs (`bixefi`, `bugaja`, `racana`×2, `tobajo`) are both `polygonSkipMode:'x'` cross-lane heads (`Worm.java:159-168` sets `ON_X` precisely so compression may move the decoration); 2 `empty×centeredText` pairs (`maketa`) are the lane title, which `SlotFinder#draw` never dispatches on X, re-centred over a lane whose content shrank (`UGraphicCompressOnXorY.java:100-112`; `pos` may go negative). | Stop 11's premise — "a new overlap after compression is a defect" — is false for non-occupying shapes; the jar's own design moves them. The input geometry that makes ours collide is the filed parser lane-capture defect (`node-dispatch.ts:261,291`): the bar and the elbows sit in one lane and the branch content in another, leaving a span the pass faithfully collapses. The jar's `maketa` lane 1 is 123 wide with the title inside because its content IS there. | scratchpad `t5-verify.ts` output; `test-results/dot-cache/activity/maketa-43-juja264/in.svg` |
| 2026-09-10 | T5 halt | Recommendation for the human: amend stop 11 to the invariant the Java actually guarantees — "no new overlap between two shapes that BOTH occupy on the moved axis" (equivalently, allow pairs where one shape is `polygonSkipMode === axis` or `centeredText` on X) — then resume from `akc/T5-wip`: refine the test, journal the 7 pairs as the pre-named class, commit T5, run T6. Alternative: fix the parser lane-capture defect first (outside every write-set: stop 1) and re-measure. Not recommended: exempting the 5 fixtures by name. | The refined invariant is a statement about the port; the by-name exemption is a fit to the corpus. | this row |
| 2026-09-10 | T5 halt | Exit-bar status at the halt (T5's measurement, WIP branch): aggregate 52956 → 52954 (−2) against the post-T1 reference, i.e. **52954 vs the brief's 42511 — Σ did not fall**; subset 8714 → 8709 (vs 6752). `rect[]/@width` 149 → 132 fell; `line[]/@x2` −1, `svg/@width` −1; every other x family flat. `removed` over 268: x 5827.175 (85 fixtures), y 2117.0 (160 fixtures), 0 throws. The three targets match exactly (zizaki bar 103.4, boxes 24/68.7; simuti 26/69.35/112.7; bixefi lane 3 158.175). Laned census 14 closer / 1 same / 2 farther (both the parser-defect class). Gate median 5.003 s vs T0 4.40 s, max test 419 ms. | The comparator artifact named at the B1 gate: after T1 the heads pair per index and stay offset by the unported root margin (`Recentred`, 12 vs 16), so a geometrically converging pass cannot move the score. T6's "ask first if Σ did not fall" would have halted here regardless of stop 11; the human decides the exit bar's reference (42511 vs 52956, or the families/targets). | T5 journal rows on `akc/T5-wip` |
| 2026-09-10 | T5 halt | Final gates on the mission branch at `99c473c4`: typecheck 0, lint 0, build 0; `npm test` 707 files / 19564 tests, the activity ratchet the only red file (253 = T1's pre-named risers; the other three activity baselines are green because T5 is not on this branch). Probe `42511 -> 52956`, subset `6752 -> 8714`, 0 throws. Sibling baseline at `fa578b8a` for T6 already recorded: 23 files / 2167 passed / 1 pending (scratchpad `sibling-base.txt`). | Session-end gate per `rules/autonomous-execution.md`. | scratchpad `halt-*.log` |
| 2026-09-10 | stop 11 amended | Human decision (after the halt): stop 11 now reads "the invariant test finds a new overlap between two shapes that BOTH occupy on the moved axis" — pairs where one shape contributes no slot on that axis (`polygonSkipMode === axis`, `centeredText` on X, an ignored rect's middle) are the class the jar moves by design and are pinned, not forbidden. Resuming T5 from `akc/T5-wip` (`e925560e`, cherry-picked onto the mission branch). | `Worm.java:159-168`, `UGraphicCompressOnXorY.java:100-112`, `URectangle.java:193-206`; halt rows above. | this row |
| 2026-09-10 | T5 resume | Added `occupiesOn(shape, mode)` to `slot-finder.ts` (write-set extended per the amendment): false for a `rect` carrying the matching ignore flag (only its 2px ends occupy, `URectangle#drawWhenCompressed`, `klimt/shape/URectangle.java:193-206`), false for a `polygon` whose `polygonSkipMode === mode` (`Worm.java:159-168`), false for `centeredText` on `'x'` (`CenteredText` has no `SlotFinder#draw` branch, `SlotFinder.java:78-100`, and only re-emits as a real `UText` on Y through `UGraphicCompressOnXorY.java:100-112`), true otherwise. 8 new unit tests in `slot-finder.test.ts`, one per branch plus the three default-true kinds (text/empty/ellipse). | README stop 11 amendment. | `src/diagrams/activity/layout/compress/slot-finder.ts`; `tests/diagrams/activity/layout/compress/slot-finder.test.ts` |
| 2026-09-10 | T5 resume | Refined `invariant.test.ts`'s third case: a new pair is a HARD violation only when both shapes `occupiesOn` BOTH axes; every other new pair is collected into a REGRESSION PIN (`ALLOWED_NEW_OVERLAPS`, sorted, asserted with `toEqual` not a count) so a future change to the set is examined, not silently absorbed. Re-ran against the live corpus: 0 hard violations, and the allowed set matched the coordinator's predicted 7 exactly — `bixefi-77-moki051` [6,8], `bugaja-31-jaso630` [7,9], `racana-82-zece676` [7,9] and [13,15], `tobajo-64-mipi810` [31,38] (all `polygon×polygon`, the X-skipped cross-lane heads collapsing onto one point because the filed parser lane-capture defect leaves the fork/join bar and its elbows in one lane), and `maketa-43-juja264` [22,26]/[23,26] (`empty×centeredText` — the same fixture flagged FARTHER in the T5-halt laned-divider census; its lane title re-centres over content whose compressed extent no longer reaches where the title's own reservation-derived slot sits, per `UGraphicCompressOnXorY.java:100-112`'s `CenteredText` re-centring rule). Kept the no-throw and count/order cases unchanged. | Coordinator's amendment message; verified against the live 268-fixture corpus, not merely restated. | `tests/diagrams/activity/layout/compress/invariant.test.ts` |
| 2026-09-10 | T5 resume | Re-measured after the `overlaps()` fix landed underneath (`99c473c4`, orthogonal to T5's own wiring): probe `42511 -> 52954`, subset `6752 -> 8709`, per-fixture scores byte-identical to the halt's own `t5.json` (0 mismatches) — the invariant/`occupiesOn` change touches only the TEST, never rendered geometry. `npm test`: 708 Test Files, 4 red — the four `tests/oracle/svg-conformance/activity.*` baselines (`diff-baseline.ratchet` 253, `style-baseline` 176, `swimlane-baseline` 48, `text-baseline` 20, all EXPECTED and T6's re-pin job); `invariant.test.ts` now GREEN. `docs/catalog.md` regenerated (drifted on the new `occupiesOn` export). typecheck/lint/build all exit 0. | `t5-final.json`; `/tmp/akc-t5-final-test2.log`. | scratchpad |
| 2026-09-10 | B4 gate | Batch 4 landed: T5 `e4c62734` (after the stop-11 amendment `09dda945`). Write-set exact (+ `slot-finder.ts`/its test for `occupiesOn`, extended at the resume). Gates: typecheck 0, lint 0, build 0; `npm test` 708 files / 19575 tests, red ONLY on the four activity oracle baselines (ratchet 253, style 176, swimlane 48, text 20 — T6's re-pin). Invariant test green: 0 hard violations, 7 allowed pairs pinned. Probe `42511 -> 52954`, subset `6752 -> 8709`; vs the post-T1 reference 6 rose / 14 fell / 248 unchanged. Siblings at HEAD: 23 files / 2167 passed / 1 pending, identical per file to `fa578b8a`. | Stop 8 clear; stops 10–13 clear (0 throws, 0 hard overlaps, `measureLanes` untouched, median 5.003 s). | scratchpad `b4-*.log`, `sibling-head.txt` |
| 2026-09-10 | B4 gate | Magnitude measurement (the score is blind to it, `.agent-notes/activity-canvas-bounds.md`): mean absolute distance to the jar over the 32 fork/split fixtures, positional pairing, before T5 (`38cdfb41`) → HEAD: `rect@x` 57.65 → 22.04 px, `rect@width` 78.59 → 56.01, `line@x1` 170.03 → 108.03, `line@x2` 167.06 → 104.20, `text@x` 95.04 → 38.57, `svg@width` 106.72 → 38.56; y families flat (`rect@y` 19.71 → 19.57, `line@y1` 65.97 → 65.89, `svg@height` 33.0 → 32.59). | The exit bar's "families fall" cannot register a 57 → 22 px convergence while the tips still miss by the unported root margin; this is the fidelity evidence the close-out states. | scratchpad `magnitude.ts` |
