# Decision journal — S1L-b

Append one row per non-trivial judgment call during execution (per
`~/.claude/rules/autonomous-execution.md`). Empty at mission start.

| When | Task | Decision | Why |
|------|------|----------|-----|
| 2026-07-27 | Batch 1 | Execute T1→T2→T3→T4 directly (no parallel agents) | Tasks are tightly coupled (T3⇐T2), center on a render path needing deep reading, and parallel agents share one worktree (git-mutation hazard). Single-agent is cheaper and safer here. |
| 2026-07-27 | Batch 1 | Branch `feat/s1l-b-display` off main; will merge-commit | Mission-brief branches use merge commits (per-task commit IDs preserved). |
| 2026-07-27 | T1/T2 | Implement T2 (parser body) in working tree FIRST to reproduce the HR crash, then build+verify T1, then commit in order T1→T2 | The crash is unobservable until the body reaches the renderer (T2). Diagnosis discipline needs a live repro. Commit order stays T1→T2 so each commit is green (6 HR-body fixtures would error-diagram if T2 landed before T1). |
| 2026-07-27 | T2/T3/T4 | Land T2+T3+T4 as ONE green commit (referencing all three task IDs) instead of three separate commits. | The description-parity ratchet couples them: T2-alone reds it (body reaches the sizer, HR sized as a full text line without T3); T2+T3 without T4 still reds it (fariba-82 widens vs its old pin until re-baselined). No ordering of {T2,T3,T4} has a green intermediate, so separate commits would mean knowingly committing red WIP — violating the protocol's "each commit green / no WIP". One combined commit keeps history green and bisectable; task IDs are preserved in the message + this journal. (T1 stayed its own green commit — it is genuinely independent.) |
| 2026-07-27 | T3 | Detect HR lines in the sizer via the render-side `classifyStripeLine(...).type === 'HORIZONTAL_LINE'` instead of ADR-4's hand-rolled `CREOLE_HR_RE`. | Refinement of ADR-4 (same 8px height; still "no per-style split"), logged because it deviates from the ADR's prescribed regex. The WIP regex treated `____` as an HR, but the renderer (`classifyStripeLine`) does NOT — `____` renders as literal text. A sizer/renderer disagreement on HR-ness would mis-size any `____`/`-----` body. Reusing the canonical classifier makes the sizer size exactly what the renderer draws (the sizer↔renderer sync invariant). Targets use only `====` (HR under both), so the DoD is unaffected; verified empirically via the size harness. |
| 2026-07-27 | T1 | **Root cause is `USymbolCloud`, NOT `renderer-cluster.ts`. Fix in `src/core/decoration/symbol/USymbolCloud.ts` (outside T1's declared write-set).** Write-set correction, logged per scope-escalation rule. | Diagnosis (evidence, not guess): plain `====`/`----` bodies on component/node/etc render fine — every symbol's `asSmall` already wraps the label draw in a stencil interceptor (`UGraphicStencil` or `MyUGraphic*`). Bisected the one error-diagram fixture (codabo-50) to the minimal repro `actor` + `cloud [ … ---- … ]`. **Mechanism:** `USymbolCloud.asSmall.drawU` dropped upstream's `ug = UGraphicStencil.create(ug, dim);` (verified present+active in `~/git/plantuml/.../USymbolCloud.java`) and added a false comment claiming upstream omits it. **Origin:** `USymbolCloud.ts:305-321`. **Causal chain:** cloud HR body → 2+ element layout runs the ink-extent `LimitFinder` pass → cloud label's `UHorizontalLine` reaches `LimitFinder` raw (no stencil) → `unsupported shape UHorizontalLine` throw → error diagram. **Ruled out:** all other `asSmall` wrap correctly (audited 22 symbols vs upstream); Hexagon's missing wrap is faithful (upstream commented it out); the mission's named write-set `renderer-cluster.ts` is the *container* renderer and never touches leaf HR. **Fix:** restore the one dropped line + delete the false comment; matches ADR-1 (interception, no LimitFinder branch). Bounded (1 line, 1 file), reversible, required for DoD ("6 HR-body fixtures render"). Proceeding per repo policy ("crossing a module boundary to reach the fix is the work; escalate with a measured plan, strong default is to do it"); flagged for human review in the mission summary. |
