# Decision journal — `activity-style-defaults`

Appended during execution. One row per non-trivial judgment call: if a
reasonable developer might have chosen differently, log it.

Log the execution plan here at startup (autonomous mode replaces the
user-review step with this entry — `rules/parallelism.md`).

| Date | Task | Decision | Rationale | Evidence |
|---|---|---|---|---|
| 2026-09-08 | — | Execution plan: batches run in brief order 0→4; T3/T4 and T5/T6 are the only parallel pairs, each executed sequentially by the orchestrator rather than as parallel agents unless their write-sets are disjoint at the time. | `rules/parallelism.md` requires the plan be logged in autonomous mode rather than presented for review. | `plans/activity-style-defaults/README.md` batch index |
| 2026-09-08 | T0 | Generated the pin with a THROWAWAY scratchpad script that re-implements the census walk, rather than sharing code with the gate. | The gate is the authoritative implementation; a deliberately independent second implementation means a disagreement between them fails the gate on the pin it wrote, which is the check we want. The script is not committed — the write-set is three files. | `npx vitest run …/activity.style-baseline.test.ts` → 383 passed against a pin written by the other implementation |
| 2026-09-08 | T0 | Pinned the JAR side as well as ours, and gated it. | A jar-side move means the committed golden changed under the pin — a corpus event, not a port change — and it is worth failing loudly on. Cost is one extra `normalizeSvg` per fixture, no extra render. | `style-baseline.json` `jar` field; gate message names the side |
| 2026-09-08 | T0 | Recorded `textCount` as a stability tripwire, NOT a convergence target. | The two sides are already 327 elements apart (1588 ours / 1915 jar) before any change lands; that gap is owned by `activity-embedded-diagram-labels` and the creole families. D6 should leave it approximately fixed. | `.agent-notes/asd-T0.md` |
| 2026-09-08 | T0 | Recorded that **0 of 268** fixtures currently match the jar canvas on both axes. | Makes any fixture that reaches canvas parity after T3–T6 unambiguous evidence rather than noise. | `.agent-notes/asd-T0.md` |
| 2026-09-08 | T1 | Added the four SNames and `ElementColors.roundCorner`; did NOT add `padding?: number`. | D2 puts the DEFAULT tier in `activity-style-defaults.ts`, so `Padding 10` reaches T4's derived box height from that module, not from the bucket — a bucket `padding` field would today have no writer and no reader, which is the speculative field `code-principles.md` forbids. If a fixture ever sets `<style> activity { Padding N }`, add it then. | `src/core/theme-graph-colors.ts`; `tests/unit/core/skinparam-element-buckets.test.ts` 14 passed |
| 2026-09-08 | T1 | Put T1's assertions in NEW files at the brief's paths rather than in `tests/unit/core/style-map-element.test.ts` / `tests/unit/skinparam.test.ts`. | Those two are the project's existing homes for this machinery but neither is in T1's write-set. The new files defer to them for everything predating the mission. | write-set check: `git status` shows only T1's four files |
| 2026-09-08 | T1 | Blast-radius proof for D3: the whole 691-file suite is green, and `resolveTheme('default').colors.elements` is `undefined`. | Adding a name to an ALLOWLIST cannot change a resolved value unless a source actually declares that name; the default theme declares none, so every other engine's default is untouched by construction, and the class/state/description/sequence/json conformance ratchets passing is the empirical half. | `npm test` 690 passed / 1 skipped (691), 18677 tests |
| 2026-09-08 | T1 | **HALT — stop condition: required file outside every task's write-set.** T1 AC1 (`<style> activityDiagram { activity { FontSize 20 } }` -> 20) CANNOT pass with T1's declared write-set. | Mechanism: `parseStyleBlock` yields the selector `activitydiagram.activity`; `style-map-element.ts:76-85#resolveElementBucketSelector` only strips a `<diagramType>.` prefix drawn from `DIAGRAM_TYPE_SELECTOR_NAMES` (`style-map-element.ts:52-58` = classdiagram, componentdiagram, usecasediagram, statediagram, objectdiagram — **no activitydiagram**), so the selector matches neither a bare bucket name nor a recognized prefix and is dropped. Same file is the only place a `<style>` block's `RoundCorner` could populate the field D4 just added. `src/core/style-map-element.ts` is in NO task's write-set (T1–T7 all checked). Per `rules/autonomous-execution.md` this is a stop, and it is not merely mechanical: adding `activitydiagram` to that list also makes `activityDiagram { note { … } }` resolve to the SHARED `note` bucket, which is exactly the cross-engine effect D3 exists to bound. | probe: bare `activity { FontSize 21 }` -> 21 and `skinparam DiamondFontSize 40` -> 40 both PASS; nested form -> `undefined` |
| 2026-09-08 | T1 | **Halt resolved:** widened T1's write-set with `src/core/style-map-element.ts`; added `activitydiagram` to `DIAGRAM_TYPE_SELECTOR_NAMES` and a `roundcorner` populator to `collectElementStyleBuckets`. D3 amended as D3a. | Approved by the human at the halt. Verified against upstream first: every activity style signature has the `of(root, element, activityDiagram, <sname>)` shape, so the nesting our other five diagram types already implement is the faithful one here. The `note`-bucket widening it implies is upstream behavior (`FtileWithNoteOpale.java:89`), and is now an explicit assertion rather than an accident. | AC1 probe `undefined` -> `20`; four gates green, 18682 tests |
| 2026-09-08 | T2 | Corrected four `plantuml.skin` line citations written in T1 before porting the values. | Read the skin file with line numbers rather than trusting the README's table, per CLAUDE.md. The README's `:358-385` range and the citations I wrote in T1 were each off by a few lines: activity is `:360-362`, diamond `:370`, activityBar `:387`, swimlane `:309-314`. A drifted citation is worse than none — the next reader trusts it. | `sed -n '306,392p' plantuml.skin` |
| 2026-09-08 | T2 | Sourced the `activity`/`activityBar`/`diamond` LineThickness default and every non-activity RoundCorner default from the ROOT block (`plantuml.skin:15`, `:13`) rather than asserting "upstream's Style default". | My first draft wrote `1` and `0` with a prose justification and no `file:line`. The root block declares both outright; an uncited constant is unfinished work (CLAUDE.md). Exported as `ROOT_LINE_THICKNESS` / `ROOT_ROUND_CORNER` so the inheritance is visible rather than a bare literal. | `plantuml.skin:13,15` |
| 2026-09-08 | T2 | Included `swimlaneFontSize`/`swimlaneLineThickness` as their own functions rather than adding `swimlane` to the `ActivitySName` union. | A swimlane is lane chrome, not an FTile kind — it is resolved by `renderer.ts` on a different axis from every union member. Keeping it out of the union preserves the union's meaning; keeping its constants in this module preserves the "one place an activity skin number is written" property. | `activity-style-defaults.ts` |
| 2026-09-08 | T2 | Investigated the jar's `fill="#222"` vs our `resolveColorToSvgHex('#2')` -> `#222222`, and found NO divergence. | `XColor#toSvg()` (`klimt/awt/XColor.java:111-125`) does emit 6-char uppercase, so our port is faithful there; the 3-char spelling comes from `SvgGraphics#shortenColor` (`klimt/drawing/svg/SvgGraphics.java:545-554`) at the EMISSION layer, which this port already carries and already applies to `fill`/`stroke` (`src/core/svg.ts:170-173`). Recorded because it looked like a finding and is not — T5 must not "fix" it, and must not fit `'#222'`. | `src/core/svg.ts:166-173`; `usymbol-shapes.ts:50` |
| 2026-09-08 | T2 | Regenerated `docs/catalog.md` (outside T2's write-set). | The catalog is generated and drift-gated; a new `src/` module fails `npm test` until `npm run catalog` runs. Mechanical and gate-mandated, not scope expansion. | `tests/architecture/catalog.test.ts` red -> green |
| 2026-09-08 | T2 | Deleted a `expect(DIAMOND_FONT_SIZE).not.toBe(ACTIVITY_FONT_SIZE - 1)` assertion I had written, after it failed. | 11 IS 12-1; the inequality was simply false. The real guard against a derived spelling is the literal constant with its own citation plus a test that an `activity` override does NOT move the diamond — which is what replaced it. | test run: 1 failed -> 26 passed |
| 2026-09-08 | T3 | **HALT — stop condition: task mis-scoped. `activity-layout-helpers.ts` is not on the render path.** T3's edits landed and moved the aggregate weightedScore 61677 -> 61677, 0 of 268 fixtures. | **Mechanism:** `activityPlugin.layoutSync` calls `layoutActivity` from `layout/tile-layout.ts` (`index.ts:10,25`), which builds the `Gtile*` classes in `src/diagrams/activity/tiles/`; each sizes itself in its own constructor (`tiles/gtile-action.ts:19-37`). `activity-layout-helpers.ts` is imported only by `layout.old.ts` and its nine `activity-layout-*.ts` siblings (~2049 LOC), and the live path imports `layout.old.ts` for TYPES ONLY (`layout/tile-layout.ts:30`, `layout/tile-coordinates.ts:2`, both `import type`). That cluster is a superseded engine kept alive solely by `tests/unit/activity/layout.test.ts:9`. **Ruled out:** measurement error (the probe reproduces diff-baseline's 61677 exactly over the same 268), a stale build (jiti reads source; typecheck passed), and the change being genuinely size-neutral (14->11 on the diamond and 1.4x->1.0x on the advance cannot both be size-neutral if executed). **The defect is real but lives elsewhere:** the 1.4x is at `tiles/gtile-action.ts:28` and the root font is read at 8 sites across `tiles/gtile-{action,diamond,note,label,group,spot}.ts`, three of them as the same unsourced `theme.fontSize - 2`. None of those files is in any task's write-set. | probe: aggregate 61677 -> 61677, risers 0, fallers 0, unchanged 268 |
| 2026-09-08 | T3 | **Halt resolved:** re-targeted T3 at the LIVE sizer, `src/diagrams/activity/tiles/{gtile-action,gtile-diamond,gtile-note,gtile-label,gtile-group,gtile-spot}.ts`, and reverted the edits to the dead `activity-layout-helpers.ts`. | Approved by the human at the halt. The ~2049-LOC `layout.old.ts` cluster is filed for a separate mission rather than deleted here. | aggregate 61677 -> 61644, ZERO risers, 16 fallers, 120 of 268 censuses moved |
| 2026-09-08 | T3 | Assigned `repeatCondSize`/the hexagon condition to `diamond`, NOT `activity` as the brief's task list guessed. | `gtile/GtileIfHexagon.java:184`, `gtile/GtileHexagonInside.java:64` and `gtile/GtileRepeat.java:89` all resolve `of(root, element, activityDiagram, activity, diamond)` — and they are this port's three condition call sites one for one. The brief said "verify each against upstream's ftile"; verification disagreed with its own guess. | the three Java sites |
| 2026-09-08 | T3 | Assigned `gtile-group` -> `composite`, `gtile-spot` -> `circle`, `gtile-label` -> the bare root font. | `ftile/vcompact/FtileGroup.java:89-92` (composite), `ftile/vcompact/VCompactFactory.java:103-105` + `gtile/GtileCircleSpot.java:66` (circle, spot), `ftile/Swimlanes.java:248,270` (goto). All three had an unsourced `theme.fontSize - 2`. `goto` reads the theme DIRECTLY rather than through the resolver: `plantuml.skin` carries no `goto` block at any scope (verified by grep), so routing it would invent a scope upstream does not have. | `plantuml.skin` grep; the four Java sites |
| 2026-09-08 | T3 | **Amended T2's module:** a kind that declares no `FontSize` upstream now falls back to `theme.fontSize`, not to a copy of the action box's 12. | Found while mapping `gtile-group`. `composite` (`plantuml.skin:364-368`), `circle` (the bare block at `:331-332` is EMPTY) and `activityBar` (`:387`) declare no FontSize anywhere, so they inherit the ROOT `FontSize 14` (`:10`) — which the theme already carries and which a user's `skinparam defaultFontSize` moves. My first draft handed them 12, a fabricated number, with a comment claiming it avoided fabricating one. | `activity-style-defaults.test.ts` +1 test, 27 passed |
| 2026-09-08 | T3 | Replaced the `{ fontSize, fontFamily } as unknown as Theme` stub in 11 files under `tests/diagrams/activity/` with a real `resolveTheme('default')` spread. | Those stubs have no `colors`, so `resolveElementFontSize`'s `theme.colors.elements?.[...]` threw the moment the tiles started resolving per-element style; 6 suites failed to collect. The fix is the stub, not a defensive `theme.colors?.` — a real Theme always has `colors`, and guarding a state that cannot occur is what `code-principles.md` forbids. `fontSize` stays 13 so every root-font assertion is unchanged. Outside T3's write-set, but it is breakage T3 caused. | 693 files, 18718 passed |
| 2026-09-08 | T3 | **Amended T0's "do not re-pin per task" note:** `style-baseline.json` is now re-pinned by each of T3-T6. | T3-T6 each break that equality gate by design; leaving it red across four commits would make an UNINTENDED break indistinguishable from the intended one, which is the safety the suite buys. Attribution is preserved — better than before — by recording each task's delta in this journal and in the pin's own `$comment`, with the T0 floor recoverable at commit `e7f33681`. | `style-baseline.json` `$comment` |
| 2026-09-08 | T3 | Recorded the T3 delta: font-size histogram UNCHANGED, textCount UNCHANGED at 1588, 120 of 268 canvases moved. | Exactly right for a SIZER-only change: the renderer still writes `theme.fontSize` into the `font-size` attribute (that is T5/T6), so what moves is geometry, not the histogram. `textCount` holding at 1588 is T0's tripwire firing correctly — D6's line-advance change moves positions and box heights, never element counts. | `.agent-notes/asd-T3.md` |
