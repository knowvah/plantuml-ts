### nimise-04-jove070

- **bucketLabel:** other
- **rows:**
  | scope | axis | idx | ours (in) | jar (in) | Δpx |
  |---|---|---|---|---|---|
  | 2 | width | 1 | 2.549670 | 2.602948 | -3.836016 |
  | 2 | height | 1 | 3.555555 | 3.555556 | -0.000072 |
- **status:** unresolved
- **mechanism:** NOT the self-loop label (see `ruledOut` — structurally
  disproven on both sides). Direct hand-reconstruction of jar's OWN drawn
  child content (`Stopped`/`Running`/`__init_Active`/both `EvStartStop`
  labels/arrowheads, read from `in.svg`'s real coordinates) gives a content
  ink SPAN of `170.412 - 21.846 = 148.566`px — matching **our own port's**
  computed span (`148.57625`px, i.e. `computeSvekResultGeometry`'s
  `maxX(148.57625) - minX(0)`) to within 0.01px. Feeding that span through
  the (Java-confirmed-correct, see `javaRef`) `SvekResult.calculateDimension`
  recipe (`span + INK_DELTA(15)`) then `InnerStateAutonom`'s own wrapper
  delta (`MARGIN*2 + 2*MARGIN_LINE = 20`, `marginForFields=0`, no body text)
  predicts a declared composite width of `148.566 + 15 + 20 = 183.566`px —
  matching **our port's actual declared value** (183.57625px) almost
  exactly. Jar's REAL cached declaration (`svek-2.dot`) is `187.4122`px, a
  further `~3.836`px on top of that prediction. The gap is real and
  unexplained by anything in `layout-ink-extent.ts`/
  `state-composite-edge-label.ts`/`state-transition-label.ts` — every
  formula in this port's write-set reproduces jar's OWN drawn geometry
  exactly; the missing `3.836px` must be added somewhere between
  `InnerStateAutonom.calculateDimensionSlow()`'s `img`/`text`/`attr` inputs
  and the final DOT declaration, a step this task could not observe from the
  SVG/DOT cache alone (see `nextStep`).
  **Cross-fixture clue (new this task, not in the prior other.md record):**
  `nimana-36-veco708` (SI28 T2's `composite-a.md`, "resolved by adoption",
  re-examined this task) has the BYTE-IDENTICAL delta `-3.836`px on its own
  composite `yes` (scope 2, width, idx 1: ours 2.563594in / jar 2.616871in)
  despite having ZERO self-loops and COMPLETELY DIFFERENT child content
  (`yesno`/`yesyes` leaves with description text, vs `Stopped`/`Running`
  with none). The one thing `yes` and `Active` share structurally: BOTH are
  `autonom` composites that are THEMSELVES a direct endpoint of an edge
  declared OUTSIDE their own subtree (`no --> yes`/`yes --> no` for
  `yes`; `Active --> Active` for `Active`) — `Entity.isAutarkic()` does
  not disqualify either from `autonom` classification, and
  `GroupMakerState.getPureInnerLinks()`'s `isPureInnerLink12` excludes
  both edges from the composite's own `im` (see `ruledOut`), yet BOTH
  composites are still exactly `3.836px` narrower than jar. The identical
  magnitude despite unrelated content strongly suggests a FIXED,
  content-independent term jar adds when the composite is a direct edge
  endpoint — not yet isolated. `bunade-42-fudu910`'s `NotShooting` composite
  has the SAME "composite is a direct edge endpoint" shape
  (`NotShooting --> Shooting`/`Shooting --> NotShooting`) but only a
  `0.164448`px residual — see that fixture's own record below: the
  hand-reconstruction there closes almost exactly, so the "direct endpoint"
  property is not sufficient by itself to predict the FULL 3.836px gap;
  something else distinguishes {nimana, nimise} from {bunade} that this
  task did not isolate.
- **originFileLine:** UNRESOLVED — everything in `layout-ink-extent.ts`
  (`:386-392`, `addTransitionInk`'s label-box fold) and
  `state-transition-label.ts` (`labelBoxFields`, the anchor→inkBox formula)
  was directly re-derived from `in.svg` this task and matches jar's own
  drawn geometry to sub-0.02px; the missing term is not in either file.
  Candidate origin (unverified): `state-composite-autonom.ts:257-259`
  (`buildPlainAutonomSpec`'s `inkTransitions`/`inkStates` construction) is
  structurally faithful to jar's `getPureInnerLinks()` (verified this task,
  see `ruledOut`), so if a term is missing it is upstream of what any of
  these three files can see — possibly a jar-side reservation this port has
  no analogue for at all (see `nextStep`).
- **javaRef:** `~/git/plantuml/src/main/java/net/sourceforge/plantuml/svek/InnerStateAutonom.java:186-197`
  (`calculateDimensionSlow`: `dim = text.mergeTB(attr, img); result =
  dim.delta(MARGIN*2 + 2*MARGIN_LINE + marginForFields)` — read directly
  this task, confirms `state-composite-sizing.ts#measureAutonomWrapper`'s
  formula is a byte-faithful port, MARGIN/MARGIN_LINE=5 confirmed at
  `svek/IEntityImage.java:45-46`);
  `~/git/plantuml/src/main/java/net/sourceforge/plantuml/svek/SvekResult.java:130-135`
  (`calculateDimension`: `minMax.getDimension().delta(15, 15)`, matches
  `core/svek/SvekResult.ts#svekDimension`/`INK_DELTA=15` exactly);
  `~/git/plantuml/src/main/java/net/sourceforge/plantuml/svek/GroupMakerState.java:101-108,142`
  (`getPureInnerLinks`/`isPureInnerLink12`, read directly this task — see
  `ruledOut`); `~/git/plantuml/src/main/java/net/sourceforge/plantuml/abel/EntityUtils.java:40-74`
  (`isParent`/`isPureInnerLink3`/`isPureInnerLink12`, read directly this
  task to confirm the self-loop/cross-edge exclusion).
- **causalChain:** `(2.549670 - 2.602948) * 72 = -3.836016`px, byte-identical
  to the pinned ratchet (SI28 RE-PIN note, 2026-08-15) and to today's
  re-measurement (`npx jiti scripts/measure-composite-declared-size.ts
  nimise-04-jove070`) — the residual is UNCHANGED, not a new regression.
  Hand arithmetic above (span 148.566 → predicted 183.566 vs jar's actual
  187.412) accounts for the exact same `~3.836`px as a single unexplained
  jump between the content-only prediction and jar's cached declaration.
- **ruledOut:**
  - **Self-loop label folded into `Active`'s own content** — DISPROVEN two
    ways. (1) This port: `buildPlainAutonomSpec`'s `localTransitions = s
    .transitions.filter((t) => t.from !== s.id && t.to !== s.id)`
    (`state-composite-autonom.ts:218`) excludes it (the self-loop is a
    TOP-LEVEL transition, not in `s.transitions` at all — it never reaches
    the filter). (2) Upstream: `GroupMakerState.getPureInnerLinks()` uses
    `isPureInnerLink12(Active, selfLoop)` →
    `isParent(Active.getParentContainer(), Active) &&
    isParent(Active.getParentContainer(), Active)`; `Active`'s parent is
    the diagram ROOT, and `EntityUtils.isParent`'s `while
    (groupToBeTested.isRoot() == false)` guard means `isParent(ROOT, *)`
    returns `false` unconditionally (loop body never runs) — so both
    conjuncts are `false`, `isPureInnerLink12` returns `false`, and the
    self-loop is excluded from `im`'s own links before jar ever computes
    `Active`'s size. Verified by reading `EntityUtils.java`/
    `GroupMakerState.java` directly this task, not inferred.
  - **`@knowvah/dot-engine` layout arithmetic vs. real graphviz** —
    DISPROVEN by direct experiment: fed jar's OWN verbatim `svek-1.dot`
    (nimise-04's inner pass) through both `dot-engine`'s `renderSvg(dot,
    'dot')` and a local `dot -Tsvg` (graphviz 15.1.1, same binary
    `scripts/oracle-render.sh` uses). Both produced BYTE-IDENTICAL label
    polygon coordinates (`points="74,-68 74,-83 145,-83 145,-68 74,-68"`)
    for the `Running->Stopped` label. No engine-arithmetic divergence on
    this graph.
  - **Node/edge declaration order** (the known, documented `__init_*`
    ordering divergence T0/D4 flagged as a SEPARATE, out-of-scope issue) —
    DISPROVEN by controlled experiment: captured our own scope-1
    `DotInputGraph` via `setLayoutInputObserver`, ran it through
    `layoutGraph()` twice — once as emitted (`Stopped, Running,
    __init_Active`) and once reordered to jar's exact order (`__init_Active,
    Stopped, Running`, matching `svek-1.dot`'s node/edge sequence). Both
    produced the IDENTICAL `labelX` (`112.76618196920305`) for
    `Running->Stopped`. Order has zero effect on this graph's layout.
  - **Text measurer precision** (`WidthTableMeasurer` vs. jar's real AWT
    font metrics) — DISPROVEN: our computed `measuredWidth` for
    "EvStartStop" (13pt) is `69.30625`px, jar's own SVG `textLength`
    attribute for the identical text/font is `69.306`px — matching to
    within rounding of the 3-decimal SVG attribute.
  - **`toSvekDot` structural mismatch** — DISPROVEN: our emitted scope-1 DOT
    text (`toSvekDot`) is structurally identical to jar's `svek-1.dot`
    (same `remincross=true; searchsize=500;`, same shapes/sizes/
    `arrowtail=none,arrowhead=none,minlen=1`, same `WIDTH="71"
    HEIGHT="15"` label tables ×2) modulo node/edge declaration order,
    already ruled out above.
- **pairingRisk:** none — scope 2 has 2 real nodes (0.277778 circle,
  2.602948 composite), far apart in size.
- **sharedCauseWith:** `nimana-36-veco708` (byte-identical `-3.836`px,
  re-verified this task, see `mechanism` — a stronger claim than
  `composite-a.md`'s own "adopted, not independently re-derived" caveat:
  this task independently re-derived the magnitude match and additionally
  found the "composite is a direct edge endpoint" structural commonality).
  `bunade-42-fudu910` shares the BUCKET and the "composite is a direct edge
  endpoint" shape but NOT the magnitude (0.164448 vs 3.836016) — see that
  fixture's own record; NOT claimed as the identical mechanism, only a
  related family pending disambiguation.
- **proposedWriteSet:** UNKNOWN — every file this task's write-set could
  touch (`layout-ink-extent.ts`, `state-composite-edge-label.ts`,
  `state-transition-label.ts`) was verified faithful to jar's own drawn
  geometry; a source edit there would ADD ink that jar's `in.svg` does not
  actually draw, which is exactly the "never fit a value" trap the mission's
  CLAUDE.md's stop 7 names. The real origin is either (a) further inside
  `InnerStateAutonom`/`SvekEdge`'s Java that this task did not fully trace
  (a jar-side reservation with no port analogue yet), or (b) a
  `state-composite-autonom.ts`/`state-composite-classify.ts`-level omission
  (outside this task's write-set) in which entities/links feed
  `computeSvekResultGeometry`.
- **sizeEstimate:** unknown until the real jar is instrumented (see
  `nextStep`) — could be a single missing constant (low blast radius,
  shared by `nimana-36`/`nimise-04` and possibly every "composite is a
  direct edge endpoint" fixture) or a genuinely new ink term requiring a
  `StateNodeGeo`/`TransitionGeo` field (unknown blast radius).
- **confidence:** medium — the mechanism class ("composite as direct edge
  endpoint" reserves extra width jar's declared size shows but its drawn
  content does not") is evidenced by two independently-shaped fixtures
  sharing a byte-identical magnitude; the exact originating Java
  computation is not isolated.
- **nextStep:** Instrument the REAL jar, not the SVG cache: add temporary
  `System.err.println` calls in `InnerStateAutonom.calculateDimensionSlow()`
  (print `img.getWidth()`, `text.getWidth()`, `attr.getWidth()`) and in
  `GroupMakerState.getPureInnerLinks()` (print the resolved `links` list),
  rebuild locally (`~/git/plantuml`, gradle), and run on `nimise-04-jove070`
  and `nimana-36-veco708` via `scripts/oracle-render.sh`. This directly
  answers whether `img.getWidth()` really is `167.4122`px (as the
  DOT-declaration subtraction implies) — if so, the extra `~3.836px` is
  INSIDE `im`'s own content ink walk and this task's SVG-based
  reconstruction is missing an invisible-but-ink-contributing element
  (candidate: an entry/exit-action `attr` reservation this task assumed was
  empty but jar may size differently for a group vs. a leaf); if
  `img.getWidth()` is closer to `163.57`px (matching this port), the extra
  term is added AFTER `im.calculateDimension()`, somewhere between
  `InnerStateAutonom`/`GroupMakerState`/`SvekEdge` and the final DOT
  `width=` attribute this task did not locate. Also re-run the SAME
  reconstruction on `bunade-42-fudu910` inside the jar (not just the SVG)
  to see whether its own `img.getWidth()` differs from the "composite is a
  direct edge endpoint" fixtures by the SAME missing mechanism at a smaller
  magnitude, or by an unrelated one.

### nimana-36-veco708

- **bucketLabel:** composite
- **rows:**
  | scope | axis | idx | ours (in) | jar (in) | Δpx |
  |---|---|---|---|---|---|
  | 2 | width | 1 | 2.563594 | 2.616871 | -3.836 |
  | 2 | height | 1 | 3.152777 | 3.152778 | -0.000 |
- **status:** unresolved
- **mechanism:** See `nimise-04-jove070`'s record above — this task
  independently re-measured nimana-36 (`npx jiti
  scripts/measure-composite-declared-size.ts nimana-36-veco708`) and
  confirms the row is byte-identical to nimise-04's own residual
  (`-3.836016`px vs `-3.836`px to the harness's own reported precision).
  `composite-a.md`'s prior record for this fixture (SI28 T2, "resolved by
  adoption") explicitly caveats it was "not independently re-derived" via
  its own probe; this task supplies that independent re-derivation and
  additionally finds the shared structural property (composite is a direct
  edge endpoint of `no --> yes`/`yes --> no`, both excluded from `yes`'s
  own `im` content by `isPureInnerLink12`, exactly as nimise-04's self-loop
  is excluded) — see the shared `mechanism` prose above rather than
  duplicating it. `composite-a.md`'s own status of "resolved" for this
  fixture is therefore SUPERSEDED by this record: the mechanism was
  adopted from a T10 record that was itself marked `unresolved` with a
  `nextStep`, so "resolved by adoption" only meant "same magnitude as a
  cross-referenced fixture," not "cause isolated." This task did not find
  the isolating mechanism either.
- **originFileLine:** UNRESOLVED — see `nimise-04-jove070`'s record;
  `state-composite-autonom.ts:257-259`'s `inkStates`/`inkTransitions`
  construction for `yes` was verified structurally faithful to jar's
  `getPureInnerLinks()`, same as `Active`'s.
- **javaRef:** Same as `nimise-04-jove070`'s record.
- **causalChain:** `(2.563594 - 2.616871) * 72 = -3.836$`px (harness's own
  3-decimal report), matching nimise-04's `-3.836016`px to the precision
  both fixtures report at.
- **ruledOut:** Same four items as `nimise-04-jove070`'s record, re-verified
  applicable here: `yes` has zero self-loops so the self-loop-specific
  ruling-out doesn't directly apply, but the STRUCTURALLY ANALOGOUS
  "composite is a direct edge endpoint, excluded from `im` by
  `isPureInnerLink12`" argument does (checked directly: `e1=yes,
  e2=no` or `e1=no,e2=yes`; either way one of `group1`/`group2` is the
  diagram ROOT, `isParent(ROOT, yes)` is unconditionally `false` per
  `EntityUtils.isParent`'s root guard, so `isPureInnerLink12` is `false`
  and the edge is excluded from `yes`'s own `im`). Declaration order and
  measurer-precision rulings from `nimise-04-jove070` were not re-run
  fixture-specific for `nimana-36` (no reason to expect a different
  result given the shared engine/pipeline) — flagged as NOT independently
  re-verified for this specific fixture, unlike the self-loop-exclusion
  argument above which was re-checked directly for `yes`.
- **pairingRisk:** none — scope 2's two nodes (`no`: 0.945833..1.045833in
  range per composite-a.md, `yes`: 2.6x) are far apart in size.
- **sharedCauseWith:** `nimise-04-jove070` (byte-identical magnitude,
  independently re-derived this task — see above).
- **proposedWriteSet:** Same as `nimise-04-jove070`'s record — unknown
  pending jar instrumentation.
- **sizeEstimate:** Same as `nimise-04-jove070`'s record.
- **confidence:** medium — same basis as `nimise-04-jove070`.
- **nextStep:** Same jar-instrumentation nextStep as `nimise-04-jove070`;
  run BOTH fixtures through the same instrumented local jar build in one
  pass since they are the two-node group.

### bunade-42-fudu910

- **bucketLabel:** other
- **rows:**
  | scope | axis | idx | ours (in) | jar (in) | Δpx |
  |---|---|---|---|---|---|
  | 2 | width | 2 | 2.144340 | 2.146624 | -0.164448 |
  | 2 | height | 2 | 3.555555 | 3.555556 | -0.000072 |
- **status:** unresolved
- **mechanism:** Re-measured this task (`npx jiti
  scripts/measure-composite-declared-size.ts bunade-42-fudu910`): residual
  unchanged since the 2026-08-15 RE-PIN (`0.002284in*72=0.164448px`
  exactly). Hand-reconstruction of jar's own drawn content
  (`Idle`/`Configuring`/`__init_NotShooting`/both `EvConfig`
  labels/arrowheads, read from `in.svg`) gives content SPAN
  `145.277 - 25.884 = 119.393`px; the `span + 15 + 20` formula then
  predicts a declared width of `119.393 + 35 = 154.393`px against jar's
  actual declared width of `154.557`px — a `0.164`px gap, matching the
  row's own residual almost exactly (this task's own from-scratch SVG
  reconstruction reproduces the residual directly, rather than the prior
  `other.md` record's own hand arithmetic, which independently landed on
  the same `~0.164`px gap via `154.557 - 35 = 119.557` vs its own measured
  `119.5565`). This is internally consistent: unlike nimise-04/
  nimana-36 (a `3.836`px gap between predicted-from-content and
  jar's-actual-declaration), bunade-42's gap between "content span I can
  find in the SVG" and "content span jar's declared width implies" is only
  `0.164`px — the SAME order of magnitude as the row's own residual, i.e.
  this fixture's ink-walk formula is nearly self-consistent and the
  remaining `0.164px` is plausibly a much smaller, likely rounding-class
  term (sub-pixel corner selection, e.g. which of two near-tied candidate
  ink points `Math.max` picks) rather than a whole missing element — a
  DIFFERENT scale of problem than nimise-04/nimana-36's `3.836px`, even
  though `SYNTHESIS.md` groups all three in one G13 bucket by
  `originFileLine`.
- **originFileLine:** UNRESOLVED — `layout-ink-extent.ts:386-392` remains
  the fold site (unchanged from the pre-existing record), but as with
  nimise-04 this task could not confirm the VALUE feeding it is wrong
  there rather than upstream.
- **javaRef:** `~/git/plantuml/src/main/java/net/sourceforge/plantuml/klimt/shape/TextBlockMarged.java:79-87`
  + `~/git/plantuml/src/main/java/net/sourceforge/plantuml/klimt/drawing/LimitFinder.java:159-162`
  (unchanged from the pre-existing `other.md` record — this task did not
  re-derive a different site for the sub-pixel-scale residual).
- **causalChain:** `(2.144340 - 2.146624) * 72 = -0.164448`px, matching the
  pinned ratchet exactly; unchanged since the RE-PIN.
- **ruledOut:** Self-loop involvement — moot, `NotShooting` has no
  self-loop; its extra transitions (`NotShooting --> Shooting`/
  `Shooting --> NotShooting`) are cross-composite, structurally the SAME
  "composite is a direct edge endpoint" shape as `yes`/`Active`, and
  `isPureInnerLink12` excludes both from `NotShooting`'s own `im` by the
  identical argument (one of `group1`/`group2` resolves to the diagram
  ROOT, `isParent(ROOT, NotShooting)` unconditionally `false`). This task
  did NOT re-run the dot-engine-vs-real-graphviz or declaration-order
  controlled experiments specifically for bunade-42 (only for nimise-04) —
  flagged as not independently verified here, given the residual's much
  smaller magnitude made those experiments a lower priority within this
  task's time budget.
- **pairingRisk:** none — scope 2's three nodes (0.277778, 1.045833,
  2.14x) are far apart in size.
- **sharedCauseWith:** Structurally related to `nimise-04-jove070`/
  `nimana-36-veco708` (same "composite is a direct edge endpoint" shape)
  but NOT the same magnitude — see `mechanism` above. Do not merge into one
  fix without separately verifying bunade-42 closes; the two residual
  SCALES (`3.836px` vs `0.164px`) may be different mechanisms that happen
  to originate near the same fold site.
- **proposedWriteSet:** Unknown, same caveat as `nimise-04-jove070`.
- **sizeEstimate:** Likely small (sub-pixel-class term) if the mechanism is
  genuinely distinct from nimise-04/nimana-36's; verification cost is low
  once isolated (one fixture, no cross-shape blast radius evidenced yet).
- **confidence:** low — this task did not isolate a specific missing Java
  term for the `0.164px` scale, only reproduced that the gap is
  self-consistent with a from-scratch SVG reconstruction.
- **nextStep:** Same jar-instrumentation approach as `nimise-04-jove070`'s
  `nextStep`, run on `bunade-42-fudu910` too, specifically to determine
  whether its `0.164px` gap and nimise-04/nimana-36's `3.836px` gap share
  one Java-side term at different magnitudes or are two unrelated
  mechanisms that happen to share a fold site.
