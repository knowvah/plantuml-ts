# Decision journal — edge-label-box-followups

Appended during execution. One row per non-trivial judgment call: if a
reasonable developer might have chosen differently, it belongs here. Every
count comes from a command run on a clean tree (SI23 method note: the census
is invalid while any parallel agent has uncommitted work on disk).

| Date | Decision / finding | Evidence / why |
|---|---|---|
| 2026-08-16 | **Batch 1 execution plan.** T1 (`typescript-pro`) and T2 (`typescript-pro`) launched in parallel; write-sets disjoint (T1: `edge-label-box.ts` + its test + class backlog; T2: style-cascade/theme/skinparam files + new `arrow-label-font.ts`). Baseline census (shape-match + DOT for class/state/component/usecase/object/description) captured in a detached worktree at `9c4ed349` (`scratchpad/wt-base`, ignored dirs symlinked) so agents' uncommitted edits cannot contaminate it. Post-batch census runs the same way at the batch's final commit. | Starting state verified: backlogs 11/9/2/0 as README states; tree clean apart from an untracked `test-results/dot-cache/component/kofovu-01-niti223/`. Agents forbidden from `git checkout/reset/stash/clean` (`.agent-notes/g0b-shared-worktree-risk.md`). | No |
| 2026-08-16 | **T1 (`151b5317`) — mechanism correction: the quantifier strip is `CharHidder`'s creole escape, NOT the visibility strip `decisions.md#quantifier-visibility-strip-focaci` cites.** Agent read the call path and orchestrator re-verified: `Display#manageGuillemet(manageVisibilityModifier)` (`Display.java:410-419`) has exactly one caller, `abel/LinkArg.java:71` (main label only); the quantifier arm (`SvekEdge.java:329-351`) never touches `LinkArg`. What does run on every creole line is `StripeSimple#analyzeAndAdd` → `CharHidder.hide` (`StripeSimple.java:150`, `utils/CharHidder.java:59-90`): `~` followed by one of `_-"#][*./<` is consumed as an escape and the next char survives literally. Same 53 for `focaci` (`~* initiators` → `* initiators`), but different behaviour elsewhere — solo oracle renders: `+ initiators`/`# initiators` UNSTRIPPED (56/62), `~initiators` 56 (tilde kept), `~*initiators` 53, bare `*initiators` 60 (creole list bullet, not ported). Implemented as `stripLeadingEscapedChar` (leading position only; list-bullet path out of scope, documented). | A literal visibility strip would have wrongly narrowed `+`/`-`/`#`-prefixed quantifiers. `decisions.md`'s mechanism section is wrong and T8 corrects it. Result: focaci 61x13 → 53x13, class DOT 699→700, class backlog 11→10. | Yes — brief evidence was wrong; agent was right to disobey. |
| 2026-08-16 | **T2 (`48c572b3`) — `<style> arrow { FontSize }` deliberately NOT merged into `graphOverride.arrowFontSize`** (`style-map-theme.ts`, commented at the site); `arrowFontFamily`/`arrowFontStyle` are merged. Reason: `description/layout.ts:354` is a pre-existing skinparam-only reader of `graph.arrowFontSize`; merging the cascade there moved `zosuje-43-zebi775`'s DOT box with the SVG unchanged — stop 8 (T2 zero movement) and D4's exact anti-pattern. `computeArrowFontOverride` still resolves size; `resolveArrowLabelFont` reads it. **Consequence for Batch 3:** lifting the exclusion requires `src/core/style-map-theme.ts`, in no Batch-3 write-set. Orchestrator amends: T6 gains `style-map-theme.ts` (exclusion-lift only) since T6 retires the `:354` read; **T6 runs before T5** (T5's `camuna` needs the cascade size). `theme.ts` untouched (`mergeGraphColors` is a blanket spread). `FontStyle` mapping: substring bold/italic per `FontStyle.java#mutateFont` (independent axes), same as `parseFontStyleFlags`. `style-cascade-class.ts` at 484/500 lines forced a compact function body. `tests/unit/skinparam.test.ts` gained the first direct `arrowfontsize` key test. | Zero movement verified: DOT class 700 (T1's), state 266, component 257, usecase 89, object 78; census 783/26,206 byte-identical to baseline. | Yes — sequencing change to Batch 3. |
| 2026-08-16 | **Batch 1 gates.** `npm test` 595 files / 14,450 tests, coverage 95.36/90.34/96.92/96.46; typecheck/lint/build exit 0. Write-sets: T1 3 files, T2 11 files, all declared. Census at `48c572b3` in a detached worktree: only `class/focaci-80-suzu938` changed (doc-size 142x178 → 137x178, jar 135x178 — toward jar); no fixture rose; TOTAL 783/1074, 26,206. Method note: `dot-sync-report.ts description` is not a type — description = `component` + `usecase`; both agents reported "no manifest" for it, harmless. | Batch exit met (class ≥ 699; T2 byte-identical). | No |
