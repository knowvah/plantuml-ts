# Decision journal — edge-label-box-followups

Appended during execution. One row per non-trivial judgment call: if a
reasonable developer might have chosen differently, it belongs here. Every
count comes from a command run on a clean tree (SI23 method note: the census
is invalid while any parallel agent has uncommitted work on disk).

| Date | Decision / finding | Evidence / why |
|---|---|---|
| 2026-08-16 | **Batch 1 execution plan.** T1 (`typescript-pro`) and T2 (`typescript-pro`) launched in parallel; write-sets disjoint (T1: `edge-label-box.ts` + its test + class backlog; T2: style-cascade/theme/skinparam files + new `arrow-label-font.ts`). Baseline census (shape-match + DOT for class/state/component/usecase/object/description) captured in a detached worktree at `9c4ed349` (`scratchpad/wt-base`, ignored dirs symlinked) so agents' uncommitted edits cannot contaminate it. Post-batch census runs the same way at the batch's final commit. | Starting state verified: backlogs 11/9/2/0 as README states; tree clean apart from an untracked `test-results/dot-cache/component/kofovu-01-niti223/`. Agents forbidden from `git checkout/reset/stash/clean` (`.agent-notes/g0b-shared-worktree-risk.md`). | No |
| 2026-08-16 | **T1 (`151b5317`) — mechanism correction: the quantifier strip is `CharHidder`'s creole escape, NOT the visibility strip `decisions.md#quantifier-visibility-strip-focaci` cites.** Agent read the call path and orchestrator re-verified: `Display#manageGuillemet(manageVisibilityModifier)` (`Display.java:410-419`) has exactly one caller, `abel/LinkArg.java:71` (main label only); the quantifier arm (`SvekEdge.java:329-351`) never touches `LinkArg`. What does run on every creole line is `StripeSimple#analyzeAndAdd` → `CharHidder.hide` (`StripeSimple.java:150`, `utils/CharHidder.java:59-90`): `~` followed by one of `_-"#][*./<` is consumed as an escape and the next char survives literally. Same 53 for `focaci` (`~* initiators` → `* initiators`), but different behaviour elsewhere — solo oracle renders: `+ initiators`/`# initiators` UNSTRIPPED (56/62), `~initiators` 56 (tilde kept), `~*initiators` 53, bare `*initiators` 60 (creole list bullet, not ported). Implemented as `stripLeadingEscapedChar` (leading position only; list-bullet path out of scope, documented). | A literal visibility strip would have wrongly narrowed `+`/`-`/`#`-prefixed quantifiers. `decisions.md`'s mechanism section is wrong and T8 corrects it. Result: focaci 61x13 → 53x13, class DOT 699→700, class backlog 11→10. | Yes — brief evidence was wrong; agent was right to disobey. |
| 2026-08-16 | **T2 (`48c572b3`) — `<style> arrow { FontSize }` deliberately NOT merged into `graphOverride.arrowFontSize`** (`style-map-theme.ts`, commented at the site); `arrowFontFamily`/`arrowFontStyle` are merged. Reason: `description/layout.ts:354` is a pre-existing skinparam-only reader of `graph.arrowFontSize`; merging the cascade there moved `zosuje-43-zebi775`'s DOT box with the SVG unchanged — stop 8 (T2 zero movement) and D4's exact anti-pattern. `computeArrowFontOverride` still resolves size; `resolveArrowLabelFont` reads it. **Consequence for Batch 3:** lifting the exclusion requires `src/core/style-map-theme.ts`, in no Batch-3 write-set. Orchestrator amends: T6 gains `style-map-theme.ts` (exclusion-lift only) since T6 retires the `:354` read; **T6 runs before T5** (T5's `camuna` needs the cascade size). `theme.ts` untouched (`mergeGraphColors` is a blanket spread). `FontStyle` mapping: substring bold/italic per `FontStyle.java#mutateFont` (independent axes), same as `parseFontStyleFlags`. `style-cascade-class.ts` at 484/500 lines forced a compact function body. `tests/unit/skinparam.test.ts` gained the first direct `arrowfontsize` key test. | Zero movement verified: DOT class 700 (T1's), state 266, component 257, usecase 89, object 78; census 783/26,206 byte-identical to baseline. | Yes — sequencing change to Batch 3. |
| 2026-08-16 | **Batch 1 gates.** `npm test` 595 files / 14,450 tests, coverage 95.36/90.34/96.92/96.46; typecheck/lint/build exit 0. Write-sets: T1 3 files, T2 11 files, all declared. Census at `48c572b3` in a detached worktree: only `class/focaci-80-suzu938` changed (doc-size 142x178 → 137x178, jar 135x178 — toward jar); no fixture rose; TOTAL 783/1074, 26,206. Method note: `dot-sync-report.ts description` is not a type — description = `component` + `usecase`; both agents reported "no manifest" for it, harmless. | Batch exit met (class ≥ 699; T2 byte-identical). | No |
| 2026-08-16 | **Batch 2 execution plan.** T3 (`typescript-pro`, opus — largest task, parse→layout→render) and T4 (`typescript-pro`, sonnet) in parallel; write-sets disjoint except `oracle/goldens/class/label-size-backlog.json` (T4 only in this batch — T3 owns the description backlog). `src/core/edge-label-box.ts` is at exactly 500/500 lines after T1 (T1 already trimmed prose to fit). T3 is instructed: add `roseNoteDim` there by trimming redundant prose only (never a `file:line` citation); if it will not fit, put `roseNoteDim` in a NEW `src/core/rose-note-dim.ts` and report — orchestrator pre-authorises that write-set amendment (a new file with no owner is not stop 2). Post-batch census again in a detached worktree at the batch's last commit. | Both agents forbidden from git state mutations; both told not to run the census. | Yes — conditional write-set amendment. |
| 2026-08-16 | **T4 (`d24fd288`).** `hasSeveralGuideLines` (`Display.java:729-747`, orchestrator re-read) has FOUR conditions — `startsWith("< ")`, `startsWith("> ")`, `endsWith(" <")`, `endsWith(" >")` — not the two `decisions.md#d6` summarises; all four ported (`class-magic-arrow.ts#hasSeveralGuideLines`). Per-line path reuses `parseMagicArrowLabel` (no second token parser); `TextBlockArrow2` 13×13 in the box, `.80` never. **Stop 10 does not fire:** `StringBounderFromWidthTable#calculateDimension` (`:67-69`) uses only `font.getSize2D()` + the width table — no bold/italic branch. Results: vuresa 142×41→128×41, gobuco/lapoma 24×54→29×54; class DOT 700→703; class backlog 10→7. Judgment calls: `hasSeveralGuideLines` evaluated on pre-guillemet lines (no combined fixture); `mergeLR/TB` arithmetic duplicated inline rather than exporting `edge-label-box.ts`'s private `mergeTB` (file outside write-set and at 500/500). **Step 3 (per-line SVG glyph draw) STOPPED, not inlined:** `EdgeGeo.arrowGlyph` (`class-geo-types.ts:309`) is a single optional glyph and `labelLines` has no glyph slot; extending it needs `src/diagrams/class/class-geo-types.ts`, in no task's write-set (brief stop 2). Orchestrator did NOT amend the write-set: the change would also touch `class/renderer-edge.ts` (T5's file). DOT box and backlog results stand; the SVG for gobuco/lapoma draws no per-line glyph (text centred in the 29-wide box). Carried to the close-out as an explicit follow-up needing a human write-set decision. | Census at `608a0c32`: gobuco 6/22→14/22 (doc-size 163→168 = jar), lapoma 14/22, doc-size 65→68 (jar 72), vuresa 14/49→48/49 (576→562 = jar); no fixture rose. | Yes — sub-step stop. |
| 2026-08-16 | **T3 (`608a0c32`).** `roseNoteDim` landed in NEW `src/core/rose-note-dim.ts` (pre-authorised; `edge-label-box.ts` at 500/500 could not take ~35 lines without deleting cited prose). Position captured in `note-grammar.ts#matchOnLink`, BOTTOM default in `parse-state.ts#attachNoteToLastLink` (mirrors `CommandFactoryNoteOnLink#executeInternal`); colour NOT captured — its only upstream consumer is the note polygon fill (D2's other mission). `Link#addNote` is `this.note = note` (`abel/Link.java:332-334`, orchestrator re-read) — replace, mirrored. `buildNoteBody` (`leaf-sizing.ts:297`) is the un-margined operand (`measureNote` adds 21/10 afterwards) — `+31/+20` with no double count. D2 plumbing: `layout.ts#withLinkNoteDrawn` maps links to display-only copies whose `label` carries the note lines for `buildEdgeGeos` (spec's `noteLines` field would have needed `layout-helpers-types.ts`/`layout-geo-post.ts`, outside the write-set); `renderer-edge.ts` unchanged. `theme` reaches `link-edge-attrs.ts` via `EdgeFontSpecs.noteTheme` (5-param cap on `buildLinkEdgeAttributes`). Named gap: `computeLinkDzeta` still flat-measures `mainLabelText`; upstream eats the merged block (`SvekEdge.java:1169-1171,1193-1194`) — inert on the corpus (all four fixtures at the nodesep/ranksep floors). **Write-set expansion (journalled):** `tests/unit/scripts/measure-description-size-deltas.test.ts` pins the description backlog size — lowered 9→5 per its own comment. All four slugs cleared: fogiku/jafuke 51×15→80×33, dikexa 46×28/125×41→75×48/174×46, zavitu 239/243/364×28→268/272/393×48. Component 257→258, usecase 89→92, description backlog 9→5. | Stop 9 never approached — note text stays visible and no fixture rose. | Yes — write-set expansion + spec deviation on the D2 field site. |
| 2026-08-16 | **Batch 2 gates.** `npm test` 595 files / 14,489, coverage 95.36/90.35/96.92/96.46; typecheck/lint/build exit 0. Census at `608a0c32`: TOTAL 785/1074 exact, 26,248 shapes; only the three T4 class fixtures changed, all toward jar. DOT class 703, state 266, component 258, usecase 92, object 78. Backlogs now class 7 / description 5 / state 2 / object 0 = **14**. | Batch exit met (class ≥ 700, component ≥ 257, usecase ≥ 89, no rise). | No |
| 2026-08-16 | **Batch 3 execution plan (amended sequencing).** T6 runs FIRST and alone, with `src/core/style-map-theme.ts` + `tests/unit/style-map-theme.test.ts` added to its write-set for exactly one purpose: lift T2's `arrowFontSize` exclusion (merge the `<style> arrow { FontSize }` cascade into `graphOverride`) in the same commit that retires `description/layout.ts:385-387`'s direct read in favour of `resolveArrowLabelFont` — D4 in one commit, no transient layout/ink disagreement. Then T5 (class) and T7 (state) in parallel — T5 needs the lifted cascade for `camuna`; T7's write-set is disjoint. Corpus scan (python, both `skinparam *ArrowFont*` and `<style> arrow { Font* }`): state has ZERO fixtures with an arrow-font override, so T7's zero-movement bar is safe by construction; class has camuna/nafiki-56-jixu680/ticuxa; component has 10 (incl. zosuje, figika-36-sola271 with `ComponentArrowFont{Name,Style,Size}`), usecase 2 (jecici skinparam arrowFontSize 10, xozabi). Fixtures WITH an override may legitimately move under T5/T6 (that is D4 working); each must move toward jar and be journalled — a rise stops. | T6 sonnet, T5 sonnet, T7 sonnet. Census after T6 and after T5/T7, each in a detached worktree. | Yes — write-set amendment + ordering. |
