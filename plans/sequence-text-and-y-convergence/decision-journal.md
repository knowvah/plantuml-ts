# Decision journal — sequence-text-and-y-convergence

Append a row for every non-trivial judgement call: if a reasonable developer
might have chosen differently, log it. Include the mechanism, not just the
choice.

| # | when | decision | why |
|---|---|---|---|
| 1 | 2026-09-01, A1 | Widened `SequenceTextSpec.fontWeight` from the contract's `'bold'` to `'bold' \| '700'`. | The jar's deterministic-text SVG emits the numeric weight, never the keyword — `renderer-frame-header.ts#boldFontWeight` already encodes this, jar-verified on `bepipo-37-fego336`, and `renderer.ts#renderDividerLabel:262` does the same for dividers. A4/A5 would otherwise have to reopen the emitter. The widening is a superset, so every consumer written against the contract still typechecks. |
| 2 | 2026-09-01, A1 | Regenerated `docs/catalog.md`, which is outside A1's declared write-set. | The catalog is generated and drift-gated (`tests/architecture/catalog.test.ts`); adding any module makes it fail. `npm test` caught it, exactly as the brief's gate note predicted. Mechanical, not a design choice. |
| 3 | 2026-09-01, A1 | **HALTED** before adding the geometry metric fields. Emitter half landed and gate-verified; geo half not attempted beyond a reverted probe. | Four measured defects in the field contract, recorded in `.agent-notes/A1-sequence-geo-text-metric-fields.md`: `DividerGeo.textWidth` is an existing field with a different jar-derived meaning (TS `Duplicate identifier`); `NewpageGeo` has no text at all (upstream `ComponentRoseNewpage` draws one `hline`); a per-geo scalar cannot describe a multi-line note/divider/stereotype; and REQUIRED fields force edits to A3's and A4's files plus six test files, violating A1's own `git diff --name-only` gate. The fix — per-RUN metrics on `TextRun` rather than per-GEO scalars — changes batch 2's interface contract, so it is the maintainer's call (mis-scope stop, `~/.claude/rules/autonomous-execution.md`). |
| 4 | 2026-09-01, A1 (resumed) | Split `ast.ts` into `ast.ts` + `geo.ts` with a `export type * from './geo.js'` barrel, rather than four per-task geometry modules or 31 rewritten import statements. | The barrel changes no import site, so the split is provably behaviour-neutral — which is A1's whole gate. Four per-task modules would have bought batch-2 disjointness on a three-line contention; priced and rejected in D8. The ast/geo edge is a type-only cycle, erased at compile time, and verified by a green build plus the full suite. |
| 5 | 2026-09-01, A1 (resumed) | `TextRun.y` left documented as "in whatever convention its producer documents" rather than declared a baseline. | A2 owns the block-top -> baseline change and its `sequence-page.ts` fallout. Declaring it a baseline in A1 would have been a doc comment that lies until A2 lands. A2's contract now names the wording it must replace. |
| 6 | 2026-09-01, A1 (resumed) | `messageLabelBlock` probes the descent with `'M'`, the same string it already probes the line height with. | Every `StringMeasurer` in the port ignores `getDescent`'s `text` argument (`_text` in all five implementations), so the probe string cannot matter; matching the existing line-height probe keeps the two consistent if one ever starts to. |
| 7 | 2026-09-01, brief rewrite | Corrected the text-site inventory from eight to nine, moved `ref` bodies from A5 to A4, gave A5 the box group label, and deleted "newpage titles" as a non-existent site. | Measured by grep on `da67c704`, not carried forward: `renderer.ts:374`'s box label was in no task; `renderer.ts:257` is the divider label, which the old A5 read as a newpage title; `NewpageGeo` has no text and neither does `ComponentRoseNewpage`. `ref` bodies are frame text and A4 already owns the frame's other runs. |
| 8 | 2026-09-01, brief rewrite | Batch 2 is no longer write-set-disjoint: `geo.ts` is written by A3/A4/A5 and `sequence-layout-events.ts` by A4/A5. Documented rather than engineered away. | Disjointness was a means to safe parallelism, not an end. The contention is one field per task in a type-only file, and `renderer.ts`/`sequence-layout-events.ts` split cleanly by function. The overview now says to serialize A4 and A5 if they are actually run in parallel. |
| 9 | 2026-09-01, A2 | Ported upstream's LEFT message-label placement rather than only the baseline and `textLength`. | A2's own acceptance criterion names the jar's `x="32.894"`, which the centring divergence `text-block-geo.ts` documented could not reach. The rule is `getOldPaddingX1()` + a head clearance, both cited; verified on `bosedo-77-loge384`'s three arrows, two rightward and one leftward. |
| 10 | 2026-09-01, A2 | Left exo-message x alone while changing every other message's. | `MessageExoArrow` is a different upstream component with its own placement. `exoLabelLeft` already computed a left edge; the `+ blockWidth / 2` at its call site existed only to feed the old centre-taking signature. |
| 11 | 2026-09-01, A3 | Moved `labelCy` and `badgeGeo` out of the renderer into `sequence-layout-participant-sizing.ts` rather than duplicating them. | Both halves need them once layout places the runs — the renderer still has to translate the head block to the footer row. Two copies is the sizer/renderer split `planning/sizer-renderer-parity.md` names. |
| 12 | 2026-09-01, A3 | Stored ONE run array for the head and derived the footer by translation, rather than storing two. | The text, its widths and its metrics are identical; only y differs. The offset is a difference of two `participantLabelCy` values, not of two block tops, because a glyph-bearing kind flips label and glyph between the rows. |
| 13 | 2026-09-01, A4 | Fixed the `ref` body and header FONTS (12 and 13-bold) inside A4 rather than filing them. | Emitting a `textLength` computed at the wrong size distorts glyphs, which is worse than the displacement it replaced — the defect A4 would otherwise have shipped. Both are cited skin values, not fitted. |
| 14 | 2026-09-01, A4 | Regenerated `oracle/goldens/svg-sequence/diff-census.json` at each task instead of holding it to C4 with the ratchet. | It is a descriptive bucket tally, not a ratchet: it has no pass/fail direction, so re-pinning cannot adopt a regression. Leaving a SECOND gate permanently red for the mission would have masked the seven-file stdlib race A4 hit. |
| 15 | 2026-09-01, A5 | Kept left-alignment for note bodies despite 58 fixtures rising. | The alignment is jar-verified; the rise is a `note right` POSITION defect that centring had been masking. Reverting would restore an accidental compensation and hide the real bug from Phase B. |
| 16 | 2026-09-01, A5 | Did NOT fix the note text inset (10 vs the jar's 6) or the box label font (11 plain vs 13 bold). | The first needs `diffX` derived from `ComponentRoseNoteBox#getPreferredWidth`, which this port has not modelled — picking 6 would be fitting a constant. The second moves the label's x and reopens nested-box layout, which the only fixture exercising it does not yet render. Both recorded with citations. |
| 17 | 2026-09-01, A6 | Reported acceptance criterion 2 as FAILED rather than reinterpreting it to fit. | `text@x + text@y` fell 15.6%, not >90%. The criterion assumed all 423 064 was the centre-versus-left-edge artefact; only the x half was, and the jar-comparison worked example behind the estimate (`jobadi-87-jegi648`, charge 12.469) is an x charge. Restating the target to match the result would have hidden the thing Phase C most needs to know. |
| 18 | 2026-09-01, A6 | Wrote a one-off element-scoped measurement rather than extending the distance instrument. | The instrument aggregates by attribute across every element kind and its snapshot format is what `--compare` and the baseline JSON are built on; adding an element dimension would change that contract for one report. The scratch script reuses the instrument's own `listFixtureSlugs`/`attributeOf`/measurer/store so membership and precautions are identical, and was deleted after use. |
| 19 | 2026-09-01, A6 | Re-ran the adjudication after discovering the first run compared the base ref against itself. | The Bash working directory persisted from an earlier `cd` into the base worktree. The first run reported `improved=0 unchanged=1124`, which contradicted a ratchet failing 79 tests — the contradiction, not the number, is what caught it. Recorded in `.agent-notes/`. |
| 20 | 2026-09-01, B1 | Halted batch 4 after B1 WITHOUT running B2 or B3, though both are scoped and write-set-disjoint from it. | Stop condition 9 fired: six features, not three. B2 and B3 are pre-approved and were both corroborated by the measurement, so running them was tempting — but the condition's stated purpose is to re-plan Phase B before more of it lands, and the maintainer wrote it knowing B2 and B3 shared the batch. Interpreting a stop condition liberally because the work looked safe is the failure the condition exists to prevent. |
| 21 | 2026-09-01, B1 | Attributed features by diffing ROOT-child multisets per fixture rather than by reading sources. | The element census says which class is short, never why. The root diff produces a per-fixture signature (`line+3,path-1`) that clusters mechanically, and it is the same quantity `compareSvg` short-circuits on — so a family that closes to zero is exactly a fixture that would enter the descended cohort. 41 signatures fell out, which is itself the stop-condition-9 evidence. |
| 22 | 2026-09-01, B1 | Recorded the `<image>` deficit as unexplained rather than guessing at sprites. | 42 elements across 16 fixtures, counted but not traced to a mechanism. B1's acceptance criteria allow an explicit "unexplained"; inventing an attribution would have made the feature count look tidier and the re-plan worse. |

