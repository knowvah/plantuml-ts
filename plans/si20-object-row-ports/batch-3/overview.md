# Batch 3 — close-out

Nothing here changes rendering behavior; this batch makes the record true.

| ID | Description | Agent | Writes | Depends On | Done |
|---|---|---|---|---|---|
| [T4](T4-close-out.md) | Create the SI20 index row, file the findings, state the real number | general-purpose | `planning/mission-index.md`, `.agent-notes/**`, `../README.md`, `../ledger.md`, `DIVERGENCES.md` (conditional) | T3 | [ ] |

## Batch exit

- `planning/mission-index.md` has a **new SI20 row** — it does not exist yet
  — carrying the real measured number, including that the mission closed at
  **78/80** with the two `no-candidate` fixtures named.
- `.agent-notes/si17-rozuxo-object-row-port-producer.md` is updated to record
  that the mechanism it predicted has landed, with the commit — and that its
  own "scoped by precedent" framing was **incomplete**, because the object
  sizing path is not the class one. Preserve the original note as history.
- Any deliberate remaining divergence has a `DIVERGENCES.md` entry naming its
  mechanism. An effort excuse is not a divergence, and a tracked defect with
  a named owner is a backlog item rather than a divergence.
