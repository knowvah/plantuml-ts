# Decision journal — state-anchor-clip-retire (SI32)

| When | Task | Decision | Why | Evidence |
|---|---|---|---|---|
| 2026-08-19 | T0 | Baselines pinned at branch point `4895623b`; tracked copy added | Fixes the methodology gap SI31 recorded at its close-out (`plans/state-residual-fix-batch/README.md:217-230`): both `test-results/*` pins are gitignored and re-pinned by every batch gate, so SI31's mission-wide delta had to be reconstructed from a journal chain instead of byte-diffed. | harness sha256 `fdff4e2c…4114`, summary `273/2660/2576/47/37/0/39` (matches the brief's expected value); manifest sha256 `73fe5db5…003b`, 2017 fixtures; `tests/fixtures/si32-harness-baseline.jsonl` byte-identical (`cmp` clean) and not gitignored (`git check-ignore` exit 1). Four gates green, `npm test` 56 s, coverage 95.38/90.28/96.93/96.47. Orchestrator re-verified every number independently. |
| 2026-08-19 | T0 / orchestrator | **SI31's mover accounts for `fajegu-17-joba577` and `mefici-97-tudu030` are numerically wrong; all four SI31-T5 movers are canvas-EXACT against the jar today.** Recorded as a correction to SI31's record; SI31's own file is NOT edited (outside every SI32 task's write-set — stop 1). T4 carries the correction forward. | T0 measured all four movers canvas-exact and flagged it as undiagnosed. It is now diagnosed, by measurement at the exact SI31 commits rather than by inference. `plans/state-residual-fix-batch/expected-moves.txt:101` claims `fajegu-17` went "609 -> 555 against jar's 561 … lands 6px SHORT"; `:102` claims `mefici-97` went "873 -> 854 against jar's 816 … remaining 38px". Neither residual exists. This matters to T2: SI31's record implies 6 px and 38 px of headroom to move into, so a T2 canvas move on either fixture would look jar-ward when it is in fact **away** from the jar (D6). | Git worktrees at the two SI31 commits, rendered through the repo's own `renderSync` + `DeterministicMeasurer` + `buildStdlibAssetsStore` wiring (`scripts/render-manifest.ts:108-112`), reading `<svg width/height>`: **before T5** (`9bc2b7eb`) `fovafu-44` 239×230, `tubojo-49` 262×256, `fajegu-17` **614×846**, `mefici-97` **835×673`; **at T5** (`235ddbe2`) all four already 232×230 / 261×256 / **561×846** / **816×673** — every one byte-exact to its jar `in.svg`. Today at `4895623b`: identical to T5. SI31's `fovafu-44` (239→232) and `tubojo-49` (262→261) accounts are correct; only the other two are wrong, and independently corroborated for `fajegu-17` by `plans/note-leaf-model/baseline/shape-match.txt:842` (`614x846 561x846`). No `src/` change between `235ddbe2` and `4895623b` affects them, so this is a bookkeeping error in SI31's account, not a drift in this port. |
| 2026-08-19 | T1 | **STOP (stop 9) — D1's premise "run once after node geometry is final and before any consumer" is false in this port. No workaround proposed.** The ink walk is not a terminal consumer: two of its three call sites run mid-assembly and their output *is* an input to node geometry. | `layout-ink-extent.ts#buildInkBox` (`:369-373`) is what applies today's clip (`layout-ink-transition.ts:147` -> `clipTransitionPoints`, `:116-127`). It is called from `state-composite-concurrent.ts:190` (region stacking extent + `dx/dy`, consumed `:434-444`) and `state-composite-autonom.ts:268` (-> `childImg` `:269-272` -> `measureAutonomWrapper` `:274` -> the composite's own width/height in the CONTAINING pass), as well as the terminal `layout.ts:262-263`. Clipping only in a single post-assembly pass leaves the first two measuring unclipped splines — changing the numbers that size composite nodes. Upstream does the opposite: every autarkic group gets its own `GroupMakerState.getImage()` (`dot/CucaDiagramSimplifierState.java:61-66`) -> `GraphvizImageBuilder.buildImage` runs `dotStringFactory.solve(svg)` **then** `new SvekResult(...)` (`svek/GraphvizImageBuilder.java:287-289`), so the clip loop (`DotStringFactory.java:456-457`) precedes *that level's* `calculateDimension`. Reachability is not hypothetical: `state-composite-classify.ts:158` classifies a non-autarkic composite `'cluster'` and `resolveEndpoint` (`:254-256`) rewrites its endpoints to `zaentId` (`:246-248`); running `state Outer { state Inner { I1; I2; I1-->I2 } state P; I1-->P; P-->Inner }` through the public `layoutState` gives `Outer.transitions = [I1->I2, I1->P, P->__zaent_Inner]` with `Outer.children = [Inner, P]` — the `__zaent_` edge and `Inner`'s rectangle are both in Outer's own pass, so `state-composite-autonom.ts:268` clips it today, before Outer's size is fixed. Full per-site evidence in `.agent-notes/si32-T1.md`. |
| 2026-08-19 | T1 | Q1/Q2 recorded per site: node geos are materialized and single-framed at 4 of the 7 entry points (`layout.ts:187`, `state-composite-concurrent.ts:189`, `state-composite-geo.ts:498`, `state-composite-autonom.ts:267`); the other 3 (`state-composite-concurrent.ts:438`, `state-composite-cluster.ts:271`, `state-composite-autonom.ts:285`) emit `GeoSpec` + raw `DotLayoutResult` nodes with no `StateNodeGeo` at all. Coordinate frames agree at every site and after assembly. | The two "shifted result" sites are frame-safe: nodes and transitions receive the identical translation (`concurrent.ts:435-440`; `autonom.ts:275,284-285`), and `materializeAutonom` later applies one `dx/dy` (`state-composite-geo.ts:160-161`) to both children (`:181,:187`) and transitions (`:182,:191`), as does `layout.ts#applyStateDocumentMargin` (`:261-268`). A clip is translation-commutative, so frame is not the risk — availability is, and D1 is right that per-site clipping is impossible at sites 3/4/7. | D1's *rejection* of option A/B stands on its own evidence; only its "and therefore a single late pass is safe" half fails. |
| 2026-08-19 | T1 | **D4 confirmed** — nothing in the state engine retains an unclipped path; every `TransitionGeo.points` reader is clipped-safe. | Readers: `state-renderer-transitions.ts:186` (drawn path — must be clipped, `SvekEdge.java:671`); `renderer-arrowhead.ts:105,148,179,200` (D3, `SvekEdge.java:680,683`); `layout-ink-transition.ts:122-123,147` (the clip itself); `state-transition-label.ts` via `attachTransitionLabel` (construction-time only); `state-composite-pass.ts:325` (construction-time reverse); four pure-translation shifts (`layout.ts:256`, `state-composite-geo.ts:109`, `state-composite-concurrent.ts:370,381`). `renderer-note.ts:142`, `state-composite-cluster.ts:208`, `state-composite-autonom.ts:94`, `core/graph-layout.ts:*`, `core/svek/SvekEdge.ts:*` read `DotLayoutResult` edge points, a different type. No `dotPathInit` analogue; `Kal` exists only as the class/description stub (`core/abel/Kal.ts`, `LinkArg.ts:25-26`, `Extremity.ts:24-28` "never read"). |
| 2026-08-19 | T1 | **D5 confirmed in conclusion, corrected in reason.** Labels do not move under a post-construction clip — but not because the position always comes from `lp`. | Upstream's label position is `getXY(fullSvg, noteLabelColor)` (`SvekEdge.java:742-746`, drawn `:951-954`), independent of `dotPath` — D5's reason is right upstream. In this port `attachInlineTransitionLabel` uses `edgeResult.labelX/labelY` only when `labelX !== undefined` AND a measurer exists (`state-transition-label.ts:386-389`); otherwise it falls back to `perpendicularOffsetLabel(points)` (`:394`, formula `:161-175`), which IS point-derived — and the measurer-less path is real (concurrent-region accumulators, documented `:369-375`). `attachNoteOnLinkLabel` has the same two arms (`resolveNoteAnchor`, `:326-333`). The guarantee is therefore **temporal**: `attachTransitionLabel` runs inside `buildFlatTransitionGeos` (`layout.ts:156`) and `buildLevelTransitionGeos` (`state-composite-pass.ts:347`), so any clip placed after construction cannot reach it. A clip moved *ahead of* label attachment would move labels on that path. |
| 2026-08-19 | orchestrator | **D1 and D2 amended, mission resumed.** D1' places the clip per LAYOUT RESULT (inside `buildLevelTransitionGeos`), D1'a keeps label attachment on pre-clip points, D2' takes clip rects from `result.clusters`. Originals left unedited in `decisions.md`; amendments appended as superseding entries. | T1's STOP was independently re-verified before amending, not accepted on report: `dot/CucaDiagramSimplifierState.java:57-71` builds each autarkic group with its own `GroupMakerState.getImage()`, and `svek/GraphvizImageBuilder.java:287-289` runs `solve(svg)` then `new SvekResult(...)` — so upstream's clip loop is per layout result, and upstream shares this port's "the ink walk sizes the parent" structure. D1's global placement was therefore never the faithful port, independent of whether this port could host it. The user ruled 2026-08-19 to amend both decisions and continue rather than hand back for re-planning. | D2's isolation rationale is knowingly spent (provenance + timing move together); the standing mitigation is that T2 diagnoses every mover to a mechanism instead of inferring one. Implementability of D2' verified by probe before amending: `result.clusters` is populated and keyed by `DotInputCluster.id` (`state-composite-geo.ts:49-51` already consumes it), and `acc.clusters[].nodeIds` carries the `__zaent_` anchor (`fovafu-44-mifu394` → `cluster0: ["X","Y","__zaent_A"]`). |
| 2026-08-19 | orchestrator | **D2'a appended — D2' was written from a call site, not a method body, and is corrected.** General provenance claim survives; a border-point-composite exception is added and handed to T2 as a scoped finding, not a fix. | The user asked whether the Java had actually been read for D2. It had not, beyond `SvekEdge.java:671`'s call site. Reading it: `DotStringFactory.java:425-434` + `Cluster.java:511-512` confirm the rect is the raw graphviz cluster bbox at clip time (D2' right); `SvekEdge.java:660-663` + `Cluster.java:410-430` + `ClusterDotString.java:101-105` show `solveLine` reassigns that rect via `FrontierCalculator` first, but only for composites with border-point children (`entityPositionsExceptNormal().size() > 0`). | Correction was sent to T2 mid-flight rather than after it landed, so it folds the finding in rather than redoing work. `FrontierCalculator` is explicitly NOT to be ported by T2 (second port of upstream arithmetic, out of scope). Order-dependence noted: `manageEntryExitPoint` mutates shared cluster state inside the per-line loop, so a later line sees the adjusted rect. |
| 2026-08-19 | T2 | **The clip landed: 41 movers, all state, all SVG-hash-only, all TOWARD the jar; no canvas moved and the declared-size dump is byte-identical to T0's pin.** Clip sites: `state-composite-pass.ts:370` (`buildLevelTransitionGeos`, the seam all seven composite entry points funnel through) and `layout.ts:178` (flat path, structurally a no-op — `buildDotGraph` emits no clusters). New wiring-only module `state-transition-clip.ts`; `clipTransitionPoints`, `buildCompositeAnchorRects`, `collectAnchorRects` and all `anchorRects` threading deleted. | Orchestrator re-verified rather than accepting the report. Independent pre/post manifest built from a worktree at `f1720a14`: exactly 41 moved, `Counter({'state': 41})`, 41 SVG-hash / **0 DOT-hash** — and the 41 match `expected-moves.txt` exactly in both directions (0 measured-not-listed, 0 listed-not-measured). Direction measured independently on five fixtures by summed nearest path-endpoint distance to the jar's own `in.svg`: `fovafu` 65.730 → **0.018**, `mefici` 184.078 → **0.117**, `tilili` 661.618 → **47.736**, `viroxo` 121.133 → **77.401**, `pesita` 761.735 → **663.541**. All toward. `fovafu`'s drawn path now matches the jar's to ~0.002 px per coordinate. | Harness `cmp` against the TRACKED T0 pin is byte-identical (sha `fdff4e2c…4114` both) — the tracked-copy mechanism paying off one batch early, not just at T4. Gates: `npm test` exit 0, **56 s** (ceiling 60.3), coverage 95.42/90.33/96.94/96.51; typecheck/lint/build 0; ratchets 270/721/357/80; layering green with `KNOWN_DEBT []`; catalog not drifted. |
| 2026-08-19 | T2 / orchestrator | **T2's `mefici-97-tudu030` "820x673, NOT canvas-exact" observation is wrong and is corrected in both records.** All four no-headroom fixtures are canvas-exact. | T2 recorded it at **High** confidence, and its stated impact — "a future task must not treat mefici's width as a locked-exact row" — is the exact inverse of the truth and would have invited a later mission to "improve" a row already sitting on the jar. Re-measured via two independent render paths on this tree (bare `renderSync`, and the `render-manifest.ts#renderFixture` wiring with `withStdlib`): both emit `width="816px" height="673px"`, byte-identical to the jar's `in.svg`; also 816x673 at `4895623b` and at SI31's `235ddbe2`. | Corrected in `.agent-notes/si32-T2.md` and `expected-moves.txt`, marked as an orchestrator correction with the method. Second time this mission that a confident subagent measurement did not survive re-measurement (cf. T0's undiagnosed canvas flag) — re-measure before a number enters the close-out. |
| 2026-08-19 | T2 / orchestrator | `docs/catalog.md` written outside T2's declared write-set — **accepted, not a stop 1.** | It is generated (`npm run catalog`) and drift-gated by `tests/architecture/catalog.test.ts`; T2 added a real module (`state-transition-clip.ts`), so regenerating is mandatory for `npm test` to be green. No other task owns the file, so the stop-1 condition ("a file no task owns" needing a write) is not what happened here — the write was forced by the gate, not chosen. | Journaled per the brief's push-forward rule rather than waved through silently. |
| 2026-08-19 | T2 | **D2'a's border-point family is reachable: 2 of the 41.** `pesita-10-dene726`/`AA` and `viroxo-69-fito663`/`comp1` clip against the raw `result.clusters` box where upstream would use the `FrontierCalculator`-adjusted one. Recorded as a scoped finding; `FrontierCalculator` deliberately NOT ported. | Measured with the port's own `hasDirectBorderPointChild`, not inferred. Rect deltas (shift-invariant): `AA` 156×118.720 raw vs 126×104.720 adjusted; `comp1` 123×277.000 vs 109×277.000. Both fixtures still move toward the jar (orchestrator-measured: `pesita` −98.2 px, `viroxo` −43.7 px), so the gap narrows the error without closing it. | `pesita` retains the largest residual of any fixture spot-checked (663.5 px), which is consistent with the unported adjustment being a real remaining term there. This is a `DIVERGENCES.md` candidate for T3 — it must not be filed as "resolved" or silently dropped. |
| 2026-08-19 | T3 | **The divergence is retired and a narrower, honest one is filed in its place.** Old `DIVERGENCES.md` entry deleted in full; `## State diagrams` heading kept and now carries the border-point clip-rect entry. `docs/architecture/overview.md` pin corrected `^1.5.0` → `^1.6.0`; three candidates filed in `planning/next-missions.md`. | Deletion premise verified in the CODE by both T3 and the orchestrator independently, not from T2's report: `clipTransitionPoints` has no `src/` match; the ink walk folds `transition.points` directly (`layout-ink-transition.ts:66`); the renderer reads the same array (`state-renderer-transitions.ts:186`); all four arrowhead readers likewise (`renderer-arrowhead.ts:124,167,198,219`); points are populated once, at `state-composite-pass.ts:370` and `layout.ts:178`. State's clip exists at exactly one site (`state-transition-clip.ts:123-124`) consuming the single core port — stop 13 clear. | T3 verified all seven Java citations resolve; the orchestrator re-checked two it had not personally opened (`SvekEdge.java:1205` `manageCollision`, `DotStringFactory.java:461-463` `alignEdgesAtLabelNodes`) — both correct. T3 also caught a second staleness in the same paragraph its task file told it to check: the ASCII diagram labelled dot-engine `(file: tarball dep)` while the prose said "not a local tarball" — now `(npm dep)`. Gates: test exit 0, 56 s, coverage 95.42/90.33/96.94/96.51; typecheck/lint/build 0. Harness/manifest gates not re-run: `git diff --name-only -- src/ tests/` is empty against the T2 commit, so they cannot differ from the run that passed there. |
