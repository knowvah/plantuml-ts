# Decision journal — `doteq-regression-bisect`

Append one row per non-trivial judgment call. "Non-trivial" means a
reasonable developer might have chosen differently.

| When | Task | Decision | Why |
|---|---|---|---|
| 2026-09-03 | T0 | Executed T0 directly (no `debugger` agent) and did not launch Batch 1. | T0 is a sequence of checkouts and predicate runs whose value is in the verdict table, and its stop clause fired; spawning three bisect agents against a window with no flip would have spent ~45 min to re-find a test commit. |
| 2026-09-03 | T0 | Measured the good end at both `4758f08a` (the brief's literal "last commit of 2026-08-12") and `03467532` (the merge that introduced the pin, `generatedAt` 13:04Z). Contract records `03467532`. | The pin timestamp is 12 hours before `4758f08a` with ~60 commits between, including class/object port work that plausibly moves the class fixture. Both were `false`, so the choice did not matter — but it would have if the flip had been same-day. |
| 2026-09-03 | T0 | Ran the predicate from the main tree's `test-results/dot-cache` by absolute path rather than the worktree's copy. | The cache turned out to be tracked in git (10531 files), so a worktree checkout of an old commit could in principle change the oracle under the predicate. `git log` proved the three dirs unchanged since `d42b9687`; the absolute path makes that a non-issue regardless. Comparison logic untouched (push-forward 5). |
| 2026-09-03 | T0 | **STOP — condition 1.** All three `false` at the good end under the pinned predicate. Before stopping, ran two extra discriminating experiments (native predicate at good; old predicate stack against current `src/`) and one two-commit predicate bisect (`d3ff29be^` vs `d3ff29be`). | Stop condition 1 says do not widen and continue — it does not say do not diagnose. The four extra runs turn "premise wrong" into a stated mechanism: `labelSizeOk` added to `tests/oracle/svek-dot.ts` at `d3ff29be`; all three are pre-existing label-box divergences already carried in `oracle/goldens/*/label-size-backlog.json`. No `src/` flip exists in the window under either comparator. |
| 2026-09-03 | T0 | Committed `plans/doteq-regression-bisect/` alongside the declared write-set (`.agent-notes/bisect-doteq-T0.md`). | The brief was untracked; a stopped mission whose journal and summary exist only on one machine is not a record. Gate 5's "declared write-set only" is read as "no source outside the write-set" — plans/ is progress tracking, which every mission here commits. |
| 2026-09-03 | T0 | Left the three worktrees in place; did not switch the main checkout back to `feat/linetype-ortho-routing`. | Both are reversible and the human may re-scope onto the same window. Removal commands are in the artifact. |
