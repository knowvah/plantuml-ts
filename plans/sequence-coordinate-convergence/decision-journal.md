# Decision journal — sequence-coordinate-convergence

Branch `feat/sequence-coordinate-convergence`, cut from `main` @ `ebbd1f41`.

| # | when | decision | why |
|---|---|---|---|
| J1 | Batch 0 | The brief was found at `~/git/plantuml/plans/sequence-coordinate-convergence/` — the upstream JAVA clone, not this repo. Copied into `plans/sequence-coordinate-convergence/` here and executed from there. | The Java clone is a read-only reference checkout; a mission brief for this port belongs in this port's `plans/`. No content changed. |
| J2 | T1.2 | Baseline recorded as BOTH `findings/baseline.md` (the brief's write-set) and `findings/baseline.json` (the machine-readable snapshot the instrument's `--compare` mode reads). | Every later batch's gate is "distance fell against Batch 1's baseline". Re-deriving that from a prose table by hand is exactly the kind of transcription that fits a constant. The JSON is additive, lives inside this mission's own plan directory, and is owned by no other task. |
| J3 | T2.2 | Updated seven test files outside T2.2's declared write-set: `tests/unit/theme.test.ts`, `tests/unit/measurer.test.ts`, `tests/unit/sequence/layout.test.ts`, `tests/unit/sequence/renderer.test.ts`, `tests/diagrams/activity/tiles/gtile-text-tiles.test.ts`, `tests/unit/chart/layout.test.ts`, `tests/unit/chart/renderers/bar.test.ts`. | Not a scope change: T2.2 owns removing `participantMinWidth`, and a test that asserts the removed field exists is part of that removal, not separate work. Five of the seven merely name the field in a theme literal or use it as an arbitrary sibling in a merge test; two assert geometry the floor produced. Stop condition 1 covers a file "no task owns" — these are owned by the deletion itself. |
| J4 | T2.2 | `tests/unit/sequence/renderer.test.ts`'s TO_RIGHT anchor test now compares with a tolerance instead of exactly. | Diagnosed, not loosened. The test computes the box's right edge as `x + width` READ BACK FROM THE SVG, and both attributes round to 3dp: `Carol` at x=88.9375 w=47.5125 serialises as 88.938 + 47.513 = 136.451 where the true edge is 136.450. The head tip is emitted from the unrounded value and is correct. The 80px floor was an integer, so the two halves used to round exactly and the artefact was invisible. |
| J5 | T2.2 | `?-> Bob`'s body now starts at x = -4.194, off the left of the canvas. Recorded, not fixed here. | Mechanism: `getPoint1() = posC - preferredWidth` = 49.469 - 53.663. The arrow's width is unchanged; the lifeline moved left by 20.531 when the box lost its floor, and this port's constant `LEFT_MARGIN = 30` no longer absorbs the overhang. Upstream has no such constant — it solves `xorigin.addAtLeast(0)` over every tile (`SequenceDiagramFileMakerTeoz.java:89-110`), which is exactly Batch 5's T5.1. This is a pre-existing origin defect that Batch 2 unmasked, not one it introduced. **Input to T5.1.** |
| J6 | T3.2 | `renderLifeline` gained a `headHeight` parameter and now starts the line there instead of at `p.y + p.height`. Signature change, `renderer-lifeline.ts` + `renderer.ts` + its unit test, outside T3.2's stated write-set ("`sequence-layout-participants.ts`, tests"). | The `+1` is unobservable without it. Upstream draws every lifeline from body-coordinate 0 and translates the body by `getHeadHeight()` (`PlayingSpaceWithParticipants#drawU:213`), so all lifelines start at the head ROW's bottom, not at each box's own bottom — a distinction that only exists once the reserved area stops equalling the painted box. Owned by T3.2: it is the consumer of the term T3.2 introduces. |
| J7 | Batch 3 gate | `@y1` ROSE 108641 -> 117080 while the batch total fell by 38012 and both gated attributes (`@height`, `@y`) fell. Not a stop-condition-4 event (that is a TOTAL rise), but the mechanism is stated rather than left open. | Two errors were partially cancelling. The jar's first lifeline sits at 39 = a 10px document top margin plus a 29px head row. This port has no top margin, so before Batch 3 its lifeline was at 34 (`\|34-39\| = 5`) and after it is at 29 (`\|29-39\| = 10`). Correcting the head row alone moves the y axis further out until the origin is corrected too — which is Batch 5's T5.1, the same term J5 already points at. Measured directly on `jobadi-87-jegi648`, not inferred. |
| J8 | T4.2 | No code change. The audit found all seven glyph kinds already exact, so T4.2 is a no-op and T4.3's constant is confirmed rather than replaced. | Not an effort excuse — the evidence is in `findings/participant-symbols.md` and pinned in `tests/unit/sequence/participant-symbol-sizing.test.ts`: the head row lands on the jar's to the pixel on four fixtures covering all seven kinds, every consecutive lifeline spacing on `kibave-01-tafo463` is off by the SAME +10 (which forces every width error to zero across six different kinds), and every glyph's drawn shape is dimensionally identical to the jar's. |
| J9 | T4.1 | Filed, not fixed: `ActorBorderColor` is not applied to the actor stickman's stroke (`kibave-01-tafo463` sets it to blue; the jar strokes `#00F`, this port `#181818`). | A colour, not a coordinate — invisible to this mission's gate and outside its write-set. `.agent-notes/coverage-buckets/sequence.tsv:303` already tracks a second fixture in the same bucket, so it is a known-shaped gap. Recorded here so finding it a second time is not the only way it resurfaces. |
| J10 | T5.2 | Implemented the `dx(-min1)` origin solve (a two-pass layout), not just the two margin constants T5.2 names. Touches `layout.ts`, `sequence-layout-participants.ts`, `sequence-layout-exo.ts`, `sequence-layout-events.ts`. | Setting `LEFT_MARGIN` to 10 alone put content off the left of the canvas on 87 fixtures — the defect J5 predicted, now unmasked at scale. T5.1's own brief text names `xorigin.addAtLeast(0)` as part of the derivation, so the solve is the deliverable, not an extension of it. Shipping the constant without it would have been shipping a known clipping bug. |
| J11 | T5.2 | Corrected two frame constants found while measuring the overhang: `GroupingTile.MARGINX` 20 -> 16 (`:89`) and added `EXTERNAL_MARGINX1 = 3` (`:82`) to a group's leftward footprint. | Both were in the causal chain of the gate: an uncited 20 made every group frame overhang the participant row far enough to shift the whole document's origin, and 16 fixtures were short by exactly 3.000 without the second. Both carry an upstream `file:line`; the 16 is jar-verified on `bovugo-63-lazo401` (`x="13.469"` against a lifeline centre of 29.469). |
| J12 | T5.2 | Message LABEL extents are deliberately EXCLUDED from the origin walker, leaving 33 fixtures with clipped content. Measured both ways rather than assumed. | Including them: negatives 33 -> 7, but leftmost-box-exact 707 -> 687 and total distance +6236. Upstream absorbs a wide label either by moving the origin or by widening a gap, and which one is the `Real` solver's decision — **D6**. Stop condition 6 forbids settling D6 by an edit, so the trade-off is measured and handed to T7.1 instead of being resolved here. |
| J13 | T6.2 | `participantGap` 20 -> 10, one constant, no structural change. The englober and self-overflow margins in `posA`/`posE` are recorded as unmodelled rather than added. | The constraint is `nextA >= prevE + 10` with both margins zero for an ordinary participant (`LivingSpaces:61-71`, `LivingSpace:292-298`), so the ordinary case needs only the number. The two margins have exactly two callers upstream (`Doll.java:220-221`, `CommunicationTileSelf.java:208-213`) and both are the same question D6 asks — does a wide thing move its neighbours, pairwise or globally — so they belong to T7.1, not here. |
