# Decision journal — `activity-if-tile-port`

Appended during execution. One row per non-trivial judgment call: if a
reasonable developer might have chosen differently, log it. Every riser at
any task, every write-set deviation, every gate result, every D-amendment.

| Date | Task | Decision | Why | Evidence |
|---|---|---|---|---|
| 2026-09-15 | plan | Execution plan: seven sequential batches T1..T7 exactly as the brief's overviews list them; no parallel batch exists because every src task shares `conditional-builder.ts` or `tile-coordinates.ts`. T1 runs on `debugger`, T2–T6 on `typescript-pro`, T7 on the orchestrator. Each agent is named `aitp-Tn` so follow-ups resume it via SendMessage rather than re-spawning. | The parallelism rule's autonomous exception: plan logged here instead of presented. The brief itself was generated in-session because the filed follow-on had no brief and a false premise (see `.agent-notes/aicdo-planning.md`); the maintainer confirmed the re-scope, D1–D9, the task sequence, the stops and the name. | Brief commit `c1d8fce9`; baseline gates green at `b79502b5` (711 files passed, 1 skipped) |
| 2026-09-15 | T1 | Rejected 6 of the brief's 9 suggested Q1 representative slugs (`carapo-31-bisi880`, `rerovo-62-nazo755`, `vimako-25-mega336` as `with-links`; `nijipa-25-pede639` as `down`) and substituted verified replacements (`rerovo`/`vimako` moved to `down`, where they DO belong; `suzuci-53-biku826`/`feceme-58-xodo415`/`copisa-69-xisi273` as the real `with-links` set). | The spec's own instruction ("verify each with Q0 first") caught real errors: 3 slugs were misclassified builders under the ported predicate, `carapo-31-bisi880` sets `skinparam ConditionStyle InsideDiamond` (D8 out of scope), and `nijipa-25-pede639` has a note floating inside the `if` branch (also D8 out of scope, "notes attached to an if"). | `.agent-notes/aitp-T1.md` §Q1; `plans/activity-if-tile-port/fixtures.md`'s "Q1 representative slugs" section |
| 2026-09-15 | T1 | Split `--align`'s per-tag-count/alignment logic into a new sibling module `scripts/activity-probe-align.ts` instead of adding it inline to `scripts/activity-probe.ts`. | `activity-probe.ts` was 442 lines before the flag; the new logic would have crossed the file's 500-line cap (`~/.claude/rules/code-principles.md`), which the mission prompt pre-authorised as a push-forward. Widened `flattenElements` from private to exported so the sibling could reuse the SVG-tree walk instead of duplicating it. | `scripts/activity-probe.ts` (468 lines), `scripts/activity-probe-align.ts` (82 lines), `tests/unit/scripts/activity-probe-align.test.ts` |
| 2026-09-15 | T1 | `fixtures.md`'s `builders` column encodes `swapped`/`optionalStop` as suffix tags (`(swap)`, `(opt)`, `(swap,opt)`) on the bare `down` builder name, rather than as separate columns. | Keeps the table's column shape (`slug \| builders \| pin \| laned`) exactly as the interface contract requires while still surfacing the two `down` sub-variants Q1's template needed to distinguish (diamond2 drawn vs omitted). | `plans/activity-if-tile-port/fixtures.md` |
| 2026-09-15 | T1 | Q3 finding overrides the mission brief's own framing: `midArrow`/the "longest segment" rule is dead code today (only set by the unreachable `layout.old.ts` engine), so D6's "T1 verifies pins cannot move" concern doesn't apply to any EXISTING behaviour — the real safety question (does `emphasize:'up'` land on the same segment the jar emphasizes) was answered structurally instead, via `GConnectionDownThenUp`'s fixed 4-point construction. Not a D1–D9 contradiction (D6's actual decision — replace `midArrow` with `emphasize: Direction` — is still correct), so no stop-3 halt. | Import-graph analysis (`src/diagrams/activity/index.ts` wires the live engine from `tile-layout.ts`, never `layout.old.ts`'s `layoutActivity`) plus a direct render of `biguku-39-voxu233` showing zero mid-arrow polygons in our current output. | `.agent-notes/aitp-T1.md` §Q3 |
| 2026-09-15 | B0 gate | Batch 0 closed on T1 `563d7ebb`. Orchestrator re-verified: commit = write-set exactly, no `src/` change, worktree removed, typecheck + lint green, `--align rerovo-62-nazo755` reproduces the note's figures (3/6, 4/5, 2/4, 1/1, 4/16). `npm test` NOT re-run by the orchestrator: load average 135 from `mediaanalysisd`/Spotlight; T1's own clean run (713 files, 19677 tests, exit 0) is accepted as the gate result. | Re-running a 1-min suite under a 135 load would stall for the third time today and prove nothing about the code. | `uptime` at close; T1 report |
| 2026-09-15 | B0 gate | D6 AMENDED (flagged for review): the only `midArrow` writer is the DEAD old engine (`activity-layout-repeat.ts:105`); the live repeat back-edge draws no mid-arrow, while the jar's golden does (`biguku-39-voxu233`, T1 Q3). T2 renames the field everywhere it compiles (including that dead line, outside its declared write-set — pre-authorised here as a mechanical rename) and does NOT add `emphasize` to the live repeat edge; T7 files the missing repeat mid-arrow under `activity-repeat-connector-draw-order`. | Adding the jar's mid-arrow would put a new polygon on 43 repeat fixtures outside `fixtures.md` at T2 (stop 6) and is repeat work, not if work. D6's approved outcome (pins unmoved at T2; `emphasize` semantics per `Worm.java:138-139`) is unchanged; only its premise was wrong. Not halted (stop 3) because the amendment narrows scope and changes no approved behaviour — a reviewer may disagree, hence the flag. | `.agent-notes/aitp-T1.md` §Q3 |
| 2026-09-15 | B0 gate | T1 replaced 6 of the brief's 9 suggested representative slugs (3 misclassified by the markup guess, `carapo-31-bisi880` uses a D8 `ConditionStyle`, `nijipa-25-pede639` has a note inside the if). T3–T5 use T1's nine, not the brief's. | The brief's list was a planning guess; the note's is predicate-verified. | `.agent-notes/aitp-T1.md` §Q1 |
| 2026-09-15 | T2 | `if-label` (`renderNode`) draws its `<text>` directly (via `activity-renderer-shapes.ts`'s own `text`/`textLines`/`ASCENT_FRACTION` helpers) instead of literally calling `renderLabel` with `{ sname: 'arrow', ... }` as the task's operating notes worded it. | `ActivityTextOpts.sname` (`activity-text-placement.ts`, outside T2's write-set) is a closed `'activity' \| 'diamond'` union; widening it to `'arrow'` and giving `activityTextLineX` a LEFT-fixed branch would touch a file no task in this mission's batches owns, and using the EXISTING `'activity'` branch instead would apply that bucket's padding constant and the user's global `HorizontalAlignment` skinparam to a label the jar places unconditionally LEFT (`ConditionalBuilder.java:280`) — a real correctness bug on any diagram with a non-default alignment, not just a style deviation. The direct-`text()` version produces byte-identical output to what a correct `renderLabel` call would (same font size, same fill bucket, same baseline formula) without touching the file. Flagged for T3 (or a T7 follow-on) to reconcile if `activity-text-placement.ts` later grows an `'arrow'` case anyway. | `src/diagrams/activity/activity-text-placement.ts` (`ActivityTextOpts`, `activityTextLineX`); `src/diagrams/activity/activity-renderer-if-shapes.ts` |
| 2026-09-15 | T2 | Split `activity-renderer-shapes.ts` twice: moved `renderIfMerge`/`renderIfLabel` into a new `activity-renderer-if-shapes.ts`, and moved `renderSignalLabel`/`renderChevronLeft`/`renderChevronRight`/`renderParallelogram` into a new `activity-renderer-signal-shapes.ts` (re-exported from the original module so `tests/unit/activity/renderer-shapes.test.ts`'s existing import path keeps working). | The file was already at 556 lines (over the complexity hook's 500-line cap) before T2 touched it; adding the two new renderers alone pushed it to 605 and the hook blocked the write. One extraction (if-shapes, ~70 lines) was not enough headroom on its own, so a second, unrelated-to-if but self-contained group (the SDL signal shapes) was also moved, following the exact circular-import pattern `activity-renderer-bars.ts` already established and documents as safe. Pre-authorised by the mission's stop condition 1 / README "Push forward" ("a pure-move extraction plus re-export at the 500-line hook"). | `activity-renderer-shapes.ts` (473 lines after), `activity-renderer-if-shapes.ts` (71 lines), `activity-renderer-signal-shapes.ts` (125 lines); hook message "activity-renderer-shapes.ts has 605 lines (was 556) (max 500)" |
| 2026-09-15 | T2 | `renderIfMerge`'s polygon carries an explicit `strokeWidth: activityLineThickness(theme, 'diamond')`, added after the initial commit-candidate diff was reviewed. | `FtileDiamond.java:89`'s `.apply(getStyle().getStroke())` resolves the diamond style's own line thickness; `renderHexagon` already resolves that same bucket explicitly for its condition polygon, so leaving `renderIfMerge`'s rhombus on the SVG default would silently drop a `diamond { LineThickness }` skinparam override on the merge shape only. Pinned with a new renderer unit test asserting `stroke-width="0.5"` (`ELEMENT_LINE_THICKNESS`, the diamond bucket's default). | `src/diagrams/activity/activity-renderer-if-shapes.ts`; `tests/unit/activity/renderer.test.ts` |
| 2026-09-15 | T2 gate | `npm test`'s first run under load (`mediaanalysisd`/Spotlight) collected only 707 test files (706 passed, 1 skipped) — below the mission's 709 floor — and vitest's coverage-threshold check failed branches at 89.99%. `rm -rf coverage/.tmp` + rerun (per README's documented remedy) was in flight when the orchestrator supplied its own clean run instead: 712 passed / 1 skipped (713 files), 19683 tests, exit 0. Targeted rerun after the stroke-width fix: `tests/unit/activity/renderer.test.ts` 70/70 passed; `npm run typecheck`, `npm run lint`, `npm run build` all exit 0 (`build`'s pre-existing `include-resolver-node.ts` NodeJS-namespace errors are unrelated to this task's write-set and do not fail the build). Probe: `measurements/t2.json` aggregate 52067, zero movers vs `base.json` (acceptance bar met). | Two separate `nohup ... &` backgrounding attempts detached from the Bash tool's own process tracking, so their "completed" notifications only reported the launcher shell exiting, not the actual `npm test` run finishing -- confirmed via `ps`/`kill -0` polling before trusting either result. | `plans/activity-if-tile-port/measurements/t2.json`; scratchpad `t2-npm-test.log`, `t2-npm-test-2.log`; orchestrator's `orch-t2-test.log` |
