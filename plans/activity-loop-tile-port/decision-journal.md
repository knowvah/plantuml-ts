# Decision journal — `activity-loop-tile-port`

| Date | Task | Decision | Why | Evidence |
|---|---|---|---|---|
| 2026-09-16 | B0 | Execution plan: T0 in the orchestrator on the main tree; T1 dispatched to `typescript-pro` in an isolated git worktree (`isolation: worktree`) concurrently; T1's commit is merged (fast-forward/cherry-pick) after T0 lands and re-verified with the committed tools | Disjoint write-sets; memory `batch-parallelism-needs-worktrees` says parallel agents need separate worktrees; T0's render-all must read a tree T1 is not editing | `batch-0/overview.md` |
| 2026-09-16 | T0 | Committed `7dcb0726`. Scan at HEAD: 268 scanned, 0 with diagonal; calibration at `6f1c04f7` (script copied into a throwaway worktree with `test-results/` + `node_modules` symlinked): 19 fixtures / 21 segments = awrl's count; render-all twice: 0 of 268 differ; probe `--json`: aggregate 49658, 0 risers, 0 fallers, per-slug scores and `families` identical to `measurements/base.json`; catalog: no drift (scripts are not catalogued) | T0 acceptance criteria; `.agent-notes/awrl-T3.md` says to calibrate a detector against the brief's count before trusting it | `scratchpad/scan-head.json`, `scratchpad/t0-probe.json` |
| 2026-09-16 | T0 | Gates: typecheck 0, lint 0, build 0 (first run printed 3 `error TS2591/2503` lines for `src/core/include-resolver-node.ts` node types with exit 0; not reproduced on rebuild at HEAD nor at `3651a1ec`, 0 lines each). Treated as transient, not a defect; noted in `.agent-notes/altp-T0.md` | Matches aitp-T7's "agents reported build TS errors the orchestrator never reproduced" -- now seen once by the orchestrator too | `/tmp/build.log`, `/tmp/build2.log`, `/tmp/build-base.log` |
| 2026-09-16 | T1 | `tests/unit/activity/parser-lane-capture.test.ts` (outside T1's write-set, in no other task's) updated in the orchestrator: `repeatNode.body[0]` -> `repeatNode.entry` with the `InstructionRepeat.java:51` / `FtileRepeat.java:77-80` cite, plus `body` asserted `[]`. Read as the pre-authorised class of stop 1 ("the test files named per task"): a test pinning the contract T1 supersedes, updated not deleted (stop 9) | The agent correctly stopped rather than edit outside its write-set; halting the mission over a 3-line assertion re-point would be disproportionate | agent report; `git diff` of the file |
| 2026-09-16 | T1 | Agent-verified claim: Lizard 1.23.0 reports `tileNode@82-208` (HEAD) as one blob to end-of-file, so ANY line added below 82 in `tile-layout.ts` trips the hook's name-keyed ratchet ("worsened: NLOC 96->99, CCN 16->17"). Reproduced with `lizard -l typescript`; a scratch copy with `tileNode` moved to the file's end parses every function at its true size (`tileNode` NLOC 37 / CCN 15 pre-existing, `tileRepeat` 18/1, `tileWhile` 12/1). Decision: after T1 lands, one `chore` commit pure-moves `tileNode` to the end of the file (precedent: `node-dispatch.ts:1-14`, the same tool bug sidestepped by splitting) so T2/T5 can edit `tile-layout.ts` through the hook. Render-all + `cmp` must show 0 movers | The alternative -- an entry in `~/.claude/hooks/complexity-ignore` -- is user config, and a global waiver hides a real ratchet for a tool artifact; the move is the smaller change | `scratchpad/tile-layout-moved.ts` lizard rows |
| 2026-09-16 | T1 | The main tree's `node-dispatch.ts` carried an intermediate version of the agent's `tryRepeat` rewrite (second hunk only) when the worktree patch was applied; `git stash list` was empty, so it was a direct write to the main tree, not a shared-stash pop. Restored from HEAD, `git apply` of the worktree diff then applied cleanly (5 files) | An `isolation: worktree` agent can still reach the main tree by absolute path; verify `git status` of the main tree before applying a worktree patch | `git status` before/after |
| 2026-09-16 | T1 | Verification on the applied tree: targeted tests 3 files / 132 passed; render-all vs `7dcb0726`: 0 of 268 differ; scan 0; probe aggregate 49658, per-slug and families identical to `base.json`; catalog no drift | T1 acceptance criteria (byte-identical output) | `scratchpad/ra-t1`, `scratchpad/t1-probe.json` |
| 2026-09-16 | T1 | Committed `92a7fcb2` (6 files: the 5 in the write-set + `parser-lane-capture.test.ts`, journaled above). Gates on that tree: `npm test` exit 0, 722 collected = 722 on disk, 19837 passed / 0 failed; typecheck 0; lint 0; build 0 (0 `error TS`) | T1 quality bar + batch gates | `scratchpad/t1-test.json` |
| 2026-09-16 | T1 | `chore(altp-T1)` committed as the `tileNode` pure move: lizard `tileNode` 37/15, `tileRepeat` 18/1, `tileWhile` 12/1; 28 test files / 676 passed under `tests/diagrams/activity/layout` + `tests/unit/activity`; render-all vs `92a7fcb2`: 0 of 268 differ; typecheck/lint/build 0 | Journaled decision above; one commit per task plus the tooling fix it needed | `scratchpad/ra-move` |
| 2026-09-16 | B0 | Batch 0 closed: T0 `7dcb0726`, T1 `92a7fcb2` + chore. Expected movers: none -- confirmed 0 of 268 across both tasks. Next: Batch 1 (T2) | README progress | this journal |
| 2026-09-16 | T2 | Agent report verified: movers = exactly the 60 `fixtures.md` rows (0 outside); scan 0; `cemagu` hexagon width 187.35 = jar, text 6=6; `bareka` text 9=9; `GtileDiamond` readers = `gtile-switch.ts` + the generic `'gtile-diamond'` case + its own tests (kept, D1). Aggregate 49658 -> 49789 (16 risers / 14 fallers) -- pre-fix numbers, re-measured after the fix below | T2 acceptance criteria | `measurements/t2.json` (pre-fix), `scratchpad/t2-movers.txt` |
| 2026-09-16 | T2 | DEFECT found by byte-diff, not by the score: `kudedo`/`kasadu` (laned repeats, no labels) rose +27/+13 because the condition hexagon changed LANE. Mechanism: the agent's `pushRepeatCondition`/`pushWhileHeader` push with the parent's `myLane`, bypassing `walkTile`'s `laneAt(tile, lane)` (`tile-coordinates.ts:117-118`) that lets the hexagon's own `swimlane` (`tileRepeat`'s `outLane`, `FtileRepeat.java:149,152`) win. Goldens agree with the old lanes. Agent resumed to fix with `laneAt` + a lane test | A no-label fixture cannot rise from label growth; every riser must have a class (aitp T7) -- the two that had none were the defect | SVG diffs `scratchpad/ra-move` vs `ra-t2` |
| 2026-09-16 | T2 | `tests/unit/activity/layout.test.ts` (named in T2's write-set) imports `layout.old.ts`, the dead engine; nothing there changes. Brief's expectation was wrong; no edit | Agent finding, verified by the import | `layout.test.ts:1-20` |
| 2026-09-16 | T2 | Lane fix verified: both helpers now use `laneAt(hexagon, myLane)`; regression test added (`tile-coordinates.test.ts`, fails on revert). Re-measured: movers still exactly the 60 rows; scan 0; aggregate 49658 -> 49740; 13 risers / 13 fallers; `kudedo`/`kasadu` no longer rise | Byte-diff, probe | `measurements/t2.json` (final), `scratchpad/ra-t2` |
| 2026-09-16 | T2 | Accept-rises by class (D1 "known riser class", aitp T7 format): (1) element growth under positional pairing -- every labelled loop fixture gains `if-label` texts the jar draws: bareka +56, cemagu +5, cutabu +3, foludi +19, guceja +4, jupivo +27, jupoxe +7, nafaxo +12, tepivu +2, vamazo +21, xovano +6 (all have `is (`/`endwhile (` labels; text counts now equal the jar's on cemagu 6=6 and bareka 9=9); (2) intermediate loop dimension until T3/T5 (D4): levuma +2 = `svg/@height` + `viewBox[3]` (the hexagon is 16 px shorter, the repeat's stacking height is still awrl's, so a height that matched by coincidence at 274 now reads 258); (3) pre-existing divergence made visible: reluvi +2 = `polygon[4]/@points[0]`,`[6]` (empty-condition hexagon grew 20 -> 24 wide = the jar's 24, but the jar draws a 4-point diamond and ours a 6-point hexagon, so two x's newly mismatch). No unexplained riser | Stop 4: every rise has a stated mechanism; comparator paths from `compareSvg` per slug | `scratchpad/diffpaths.mts` output |
| 2026-09-16 | T2 | `invariant.test.ts` (outside T2's write-set; the exit bar's own exemption lists): `hardViolations` had ONE entry, `tobajo-64-mipi810 [16,17] polygon×text` = a repeat hexagon and its OWN east label. Attributed with `FtileDiamondInside.java:102` (east drawn at `dimTotal.getWidth()`, touching by design; the golden's `ja` sits at its hexagon's right edge x=330.838) and the existing ulp class (`before` touches exactly at 639.5875; `after` overlaps by 5e-14 at 338.01875 vs 338.01874999999995). Added to `ALLOWED_HARD_OVERLAPS` with the cite; the three `ALLOWED_NEW_OVERLAPS` tobajo entries re-indexed +2 (the two label texts precede them; coordinates unchanged, a listed push-forward) | Stop 7 reads "non-empty OR cannot be attributed with a Java cite"; this one is attributed and is the mechanism the list exists for | temporary probe log in the test (reverted) |
| 2026-09-16 | T2 | Committed. Gates on the tree: `npm test` 723 collected = 723 on disk, red ONLY on the four activity oracle gates (ratchet 13 = the 13 risers, style 60 = the 60 movers, swimlane 19, text 23; every red test names a mover), exit 1 under the red allowance; `svg-conformance` 27 files / 3428 tests, count unchanged (stop 6 ok); typecheck 0, lint 0, build 0 (0 `error TS`) | T2 quality bar + red allowance | `scratchpad/t2b-test.json` |
| 2026-09-16 | B1 | Batch 1 closed. Finding for T3/T4: `back1` is `nextLinkRenderer()` consumed at `endwhile` (`ActivityDiagram3.java:403-407` `incoming(nextLinkRenderer())`, `InstructionWhile.java:208-213`), i.e. a `-> text;` line immediately before `endwhile`; ours tiles every `arrow-label` node to `null` (`tile-layout.ts` `case 'arrow-label'`) and NO baseline while fixture has one before `endwhile`, so `labelHeight = 0` corpus-wide. T3 models `labelHeight` from that source with the cite; the dropped arrow-label is a pre-existing gap to file, not this mission's | Read the Java before instructing T3 rather than repeating the brief's "measure it with the arrow font" | grep over the 23 while fixtures |
| 2026-09-16 | T3 | Agent report verified: `gtile-while.ts` is a verbatim port (`left = geo.left + 24`, `width = geo.w + 36`, `height = geo.h + 48 + labelHeight`, body y centred, `FtileWhile.java:575-596,622-644`); `labelHeight` fixed at 0 with the `back1` cite (`ActivityDiagram3.java:403-407`); `walk-while-branch.ts` needed no edit. Movers = exactly the 23 `while` rows (0 parent re-centres, 43 repeat-only rows byte-identical); scan 0; aggregate 49740 -> 49740: bareka -2 (`svg/@height`, `viewBox[3]` now match the golden), ruzica +2 (`line[28]/@y2`, `line[29]/@y2`: the OLD back-edge route drawn from the new offsets -- T4 replaces those edges; class: intermediate geometry) | T3 acceptance criteria; stop 4 | `measurements/t3.json`, `scratchpad/t3-movers.txt` |
| 2026-09-16 | T3 | Agent claim "3 pre-existing `error TS` lines in `npm run build`" -- the orchestrator's own build on the same tree: see the gate row below. Filed once already in `.agent-notes/altp-T0.md` (transient, exit 0) | verify-agent-claims | `/tmp/build4.log` |
| 2026-09-16 | T3 | Committed. Gates: `npm test` 723 collected = 723 on disk, red only on the four activity oracle gates and only on movers, exit 1 under the red allowance; typecheck 0; lint 0; build 0 (0 `error TS`) | T3 quality bar | `scratchpad/t3-test.json` |
| 2026-09-16 | T4 | Pre-decision on stop 9: assertions on `backEdgeRightX` and on `GConnectionVerticalDownThenBack` go away WITH the retired symbol (D8), cited in the test file; stop 9 governs assertions on live behaviour, which are updated never deleted | D8 is explicit; a stop on every retired-field assertion would make D8 unexecutable | `decisions.md` D8 |
| 2026-09-16 | T4 | Prompt corrections vs the brief, from the Java: BOTH `ConnectionOut` snakes are undecorated (`Snake.create(skinParam, color)` two-arg form, `FtileWhile.java:485-486,504`), so `arrowhead: false` on both, `emphasize: 'down'` on the first only; the brief said only the second | READ THE JAVA FIRST | `FtileWhile.java:482-511` |
| 2026-09-16 | B3-pre | STOP-11 PRE-FINDING (not yet triggered -- nothing draws `Complex1` until T6): parsing the 43 repeat rows with our parser shows SIX with `swimlane != swimlaneOut`: `becanu-19-diti597` (col1->col2), `givanu-33-kire967` (col1->col2), `kasadu-53-tuki533` (Author->Reviewer), `kudedo-31-pafi082` (Lane2->Lane1), `mafete-03-rapa918` (Swimlane1->Swimlane2), `manata-12-rido730` (actorA->actorC). `FtileRepeat.java:186-199` routes those to `ConnectionBackComplex1`, which the README lists under "does NOT do" and D5 calls a stop. T4 (while) and T5 (repeat dimension/entry) do not depend on the back-edge selection; the mission continues through T5 and halts at the T6 boundary with the slug list filed and the D5 question for the human | Stop 11 + stop 3 (a locked scope decision needs amending); finishing the unaffected tasks first is the "finish every other part in full" rule | `scratchpad/lanecheck.mts` output |
| 2026-09-16 | B3-pre | T5 note from the Java: under `INSIDE_HEXAGON` the `FtileRepeat` is built with `tbTest = TextBlockUtils.empty(0, 0)` (`FtileRepeat.java:152`), so `calculateDimensionInternal`'s floor is `max(left + right, 0 + 24) + 24` (`:706-712`); the brief's "tbTest.w + 24" is 24 on every fixture | Read before instructing | `FtileRepeat.java:141-152,700-715` |
| 2026-09-16 | B3-pre | `ConnectionBackComplex1` sized: ~70 lines, two snake shapes (`FtileRepeat.java:333-404`); filed with the six slugs and three options in `stop-11-complex1.md`. The predicate's lane set is `repeat.getSwimlanes()` = the BODY ftile's lanes (`:187`), not `FtileRepeat#getSwimlanes` (`:109-116`, which adds the diamonds) | "Hard / out of scope are triggers to VERIFY" | `stop-11-complex1.md` |
| 2026-09-16 | T4 | Agent report verified; deviation accepted: `loop-routing.test.ts` kept (it also covers `GConnectionDownThenUp`, still live until T6) with only the retired block removed. Agent's own diagnosis (both `ConnectionOut` snakes undecorated) matches the orchestrator's pre-read | verify-agent-claims | agent report |
| 2026-09-16 | T4 | DEFECT found by segment comparison against the golden (`scratchpad/eldump.py`, anchored on a body action): the `while-header` polygon was 35 px tall on `cemagu` where the jar draws 24. Mechanism: T2's `pushWhileHeader` used `header.height` (= `calculateDimensionFtile`, hexagon + north label, `FtileDiamondInside.java:119-124`) but `drawU` draws `Hexagon.asPolygon(dimTotal)` with `dimTotal = calculateDimensionAlone` (`:87-89`). Fix in the orchestrator (`fix` folded into the T4 commit, journaled): `height: header.getCoord(SOUTH_HOOK).y` (the alone height, no new export from `gtile-diamond-inside.ts`), regression test added. `repeat-cond` has no north label so `condition.height` equals the alone height there (T5/T6 may read `SOUTH_HOOK.y` for parity; zero output change). `if-split` is unaffected (`FtileIfDown` never calls `withNorth`) | The magnitude-blind score and `--align` counts both passed; only element geometry vs the golden showed it | `scratchpad/ra-t4b`, `tile-coordinates.test.ts` new block |
| 2026-09-16 | T4 | After the fix: movers = exactly the 23 `while` rows; scan 0; aggregate 49740 -> 50226 (+486), 24 risers / 9 fallers on the probe. Segment comparison (best-of alignment, `scratchpad/segcmp2.py`, matched/ours/golden): `cemagu` 10/10/10 (rose +35!) and `fovaja` 22/22/22 (rose +111!) match the golden to 0.01 -- the rises are pure positional-pairing/reorder (elements added: 4 lines + 2 polygons per while); `foludi` 8/9/8 (one extra segment: the jar's snake merge, `UGraphicForSnake.java:146-165`); `bareka` 4/17/13: the while's own back edge is 6 px left of the jar's because the merged body (the nested `if`) is 6 px narrower (aitp's `FtileIfDown` domain) and the welding starts 10 px left because `GtileBreak` is 20x20 vs `FtileBreak` 0x0 (`FtileEmpty.java:74-76`, agent note `.agent-notes/altp-T4-gtile-break-dimension.md`); `tepivu`/`vamazo`/`reluvi`/`novata`/`perate` carry `skinparam conditionStyle` (EMPTY/INSIDE_DIAMOND, the README's "does NOT do", `activity-condition-style-variants`); laned rows (`ruzica` 3/46/45, `kijazo`, `judatu`, `gesogi`, `xovano`, `bulasi`, `camavo`, `vupuse`, `zepima`) -- see the next row | Stop 4 (every rise classed) and the exit bar's "residual names its element and Java cite" | table in this row |
| 2026-09-16 | T4 | GAP FOUND (not in the brief, file for T7/human): with swimlanes the jar draws `ConnectionTranslatable` connections through `drawTranslate(ug, translate1, translate2)` when the two ftiles sit in different lanes; `ConnectionIn.drawTranslate` (`FtileWhile.java:200-214`) inserts a dog-leg at mid-y, and `ConnectionBackSimple.drawTranslate` (`:276-308`) has a DIFFERENT shape: `xx = max(translate1.dx, translate2.dx) + width`, elbow at `y1 + 12`, a separate `asToUp` arrow at `(xx, (y1+y2)/2)` (`:307`), `MergeStrategy.LIMITED`. T4 ported only `drawU`. Every laned while row's residual is this class until ported (a separable follow-on: `activity-loop-lane-translate`) | Exit bar names `ruzica` as representative; its residual is now named with the cite | `FtileWhile.java:200-214,276-308` |
| 2026-09-16 | T4 | `cutabu` (2/10 matched, no lanes, `while ()` + `end`): the golden's `end` cross sits under the EXIT column (x = 12) with no bottom horizontal and a shorter exit -- an `end` directly after `endwhile` is placed differently by the jar; mechanism not isolated in this task (not a while-connector shape: our five edges reproduce the ported formulas). Filed for T7's residual list | Stop 4 needs a mechanism per RISE; cutabu's +16 is positional pairing (4 lines + 2 polygons added) like cemagu; the placement question is separate | `segcmp2.py cutabu-59-cilo276` |
| 2026-09-16 | T4 | Gates: `npm test` 723 collected = 723 on disk, red only on the four activity oracle gates (ratchet 24, style 60, swimlane 19, text 22; every red test names a mover), exit 1 under the red allowance; typecheck 0; lint 0; build 0 (0 `error TS`). Committing | T4 quality bar | `scratchpad/t4-test.json` |
| 2026-09-16 | B2 | Batch 2 closed: T3 `adf84310`, T4 committed above. Next: Batch 3 T5, then HALT at T6 (see `stop-11-complex1.md`) | README progress | this journal |
| 2026-09-16 | T5 | Phase 1 (pure move) committed after the orchestrator's own check: 0 of 268 differ vs `256ad510`'s render; typecheck 0, lint 0, build 0 (0 `error TS` -- the agent again reported 3 "pre-existing" lines it saw via `git stash`; not reproduced here). The move decomposed the case into three helpers to pass the hook once it left `walkTile`'s `#lizard forgives`; journaled as the listed pure-move push-forward | D10 | `scratchpad/ra-t5a` |
| 2026-09-16 | T5 | Agent finding, not verified by the orchestrator: lizard 1.23.0 attributes a trailing `interface` block to the PRECEDING function's NLOC; rule applied in `walk-repeat.ts` (interfaces before functions). Worth an `.agent-notes/` entry if T6 hits it | tool artifact, recorded | agent report |
| 2026-09-16 | T5 | Phase 2 verified: movers = exactly the 43 `repeat` rows (23 while-only rows byte-identical); scan 0; aggregate 50226 -> 50108 (48 risers / 11 fallers vs t4). Acceptance numbers asserted (left 25, width 79, height 220, bodyOffsetY 72, conditionOffsetY 180, entryOffsetX 13); `repeat :R1;` -> `children[0]` is the action tile; bare `repeat` -> `GtileRepeatEntry` + one `repeat-start` node | T5 acceptance | `measurements/t5.json`, `scratchpad/t5-movers.txt` |
| 2026-09-16 | T5 | Rises by class: (1) element growth under positional pairing -- every bare repeat gains the entry diamond polygon: the +4 rows (becanu, bulasi, bumaca, camavo, cixave, cufega, dacuga, gesogi, givanu, gofebi, judatu, jupoxe, kasadu, katopo, levuma, loxija, mafete, megara, perate, reluvi, ribapo, xabesu, zepima; +6..+12 where two repeats or labels reorder: biguku, boxoto, guceja, ziboco, dixiku, doziki, nivese, navene, kudedo, tobajo, xekame, bizono); (1b) the comparator's child-count charge (`compare.ts:404`, memory `weightedscore-antimonotone-under-growth`): bozuro +227 and vupuse +113 rose while their `compareSvg` diff LISTS shrank (155 -> 90, 109 -> 73), and gacaja -218, gelono -140, manata -290 fell the same way. No unexplained riser | Stop 4 | `scratchpad/diffpaths.mts` on bozuro/vupuse |
| 2026-09-16 | T5 | Agent deviation accepted: the `backward` child slot of `GtileRepeat` was DROPPED, not kept. No parser path produces a backward body (grep of `ast.ts`/`node-dispatch.ts`: zero matches) and `tileRepeat` always passed `null` (HEAD `tile-layout.ts:164`), so awrl T2's "stacking" had no live input; the README's "stays the interim" described dead code. `activity-loop-backward` starts from the jar's side placement (`FtileRepeat.java:750-757`) | D8 spirit; verified by grep, not by the agent's word | `git show 7163ae94:src/diagrams/activity/layout/tile-layout.ts` |
| 2026-09-16 | T5 | `invariant.test.ts` re-indexed by the agent: `ALLOWED_HARD_OVERLAPS` tobajo/boxoto +1 (coordinates unchanged, the entry diamond precedes them); `ALLOWED_NEW_OVERLAPS` tobajo three entries +3 with coordinates moved (y 507 -> 591: the repeat is taller by D4's 8*12 gutters) -- same pair, same `polygonSkipMode 'x'` class. Accepted as the listed push-forward; not a new pair | README push-forward list | agent report; full suite green on that file |
| 2026-09-16 | T5 | Representative repeat slugs at T5 (segments matched/ours/golden): biguku 1/5/6, katopo 2/10/11, felega 1/9/12 -- expected: T5 places the entry tile but T6 owns every repeat connection (no entry->body `ConnectionIn` yet, back edge still awrl's left route). Recorded for the halt report, not a T5 residual | T5 spec: "edges unchanged" | `segcmp2.py` |
| 2026-09-16 | T5 | Committed `bc2c4194`. Gates: `npm test` 724 collected = 724 on disk (one new test file), red only on the four activity oracle gates (ratchet 48, style 60, swimlane 19, text 23; every red test names a mover), exit 1 under the red allowance; typecheck 0; lint 0; build 0 (0 `error TS`) | T5 quality bar | `scratchpad/t5-test.json` |
| 2026-09-16 | T6 | HALT (stop 11 + stop 3). T6 as written throws on `ConnectionBackComplex1`, which six baseline repeat fixtures reach (`stop-11-complex1.md`); D5 and the README's "does NOT do" list are locked, so the port-vs-interim choice is the human's. Not dispatched. T7 (re-pin) not run: the four activity oracle gates stay red on the 60 loop movers by design until T6 lands and T7 re-pins | autonomous-execution stop rules | `stop-11-complex1.md` |
