# Decision Journal — si12-eager-module-removal

Append one row per non-trivial judgment call. "Non-trivial" means: a
reasonable developer might have chosen differently.

Also log here: quality-gate results per batch, any brief line-number
correction, every measurement the brief asks you to record, and every STOP
with its full output.

**T4 must record the measured unpacked size of both packages and the ceilings
it set.**
**T5 must record the re-measured reduction — T6 and T7 quote it rather than
SI11a's 99.702%.**

| Date | Task | Decision | Rationale |
|---|---|---|---|
| 2026-08-01 | planning | Mission brief created | SI12 registered when the eager-module decision was settled; planned the same day |
| 2026-08-01 | planning | Eager dropped for aws + tupadr3 ONLY | Measured: `-tupadr3` 40.8 MB unpacked = eager 20.49 + assets 19.9 + manifest 0.43, so a remote consumer installs 40.8 MB to use 0.43. `-stdlib` is 2.9 MB total and carries the bundles most likely to be wanted offline, so it keeps eager |
| 2026-08-01 | planning | Assets were never a candidate for removal | `docs/stdlib-remote.md`'s pinned-CDN recipe points `baseUrl` at `.../plantuml-stdlib-tupadr3@<v>/assets/tupadr3/` — jsDelivr serves them out of the published tarball, and SI11b's per-sprite loading reads them too |
| 2026-08-01 | planning | T1 runs alone rather than in parallel with its consumers | The generated tree is built once in vitest `globalSetup`; four agents editing consumers while the tree changes underneath them is the shared-worktree hazard SI11b paid for twice |
| 2026-08-01 | T1 | `remoteReExportLine` duplicates `emit-remote-manifest.ts#remoteExportName` rather than importing it | That helper is private and its file is outside T1's write-set. Same precedent `emit-remote-manifest.ts` itself sets by duplicating `read-bundle.ts#isPumlFile`. Verified against a real build: the emitted `index.js` names (`awslib14Remote`, `awslibRemote`, `tupadr3Remote`) match ADR-1 exactly |
| 2026-08-01 | T1 | Test reads the globalSetup-built tree instead of calling `buildStdlibPackages()` | `buildStdlibPackages` is a WRITER of `packages/*/generated/` and vitest runs files in parallel workers — the race `stdlib-packages.test.ts` documents and SI11a lost an escalation to |
| 2026-08-01 | T1 | **STOP raised (condition 1), maintainer-approved:** `tests/unit/sprite-package-files.test.ts` added to T4's write-set | `spec.modules.map(...)` at line 179 stops typechecking under ADR-2 (`error TS18048: 'spec.modules' is possibly 'undefined'`). SI11b file, in no SI12 task's write-set. Options offered: extend T4 / new T4b / orchestrator gate-fix. Maintainer chose extending T4 — it is the identical narrowing error T4 already fixes in the sibling `stdlib-package-files.test.ts:241`, so one agent, one coherent commit. Narrowing only; nothing else in that file is in scope |
| 2026-08-01 | T2 | `emit-all-index.ts` uses nine explicit named re-exports instead of `export *` | ADR-4 requires the surface be auditable and pinned; `export *` is what would silently export less than documented. Also replaced the README's `Object.values(stdlib)` + `stdlibStore(...)` idiom with named imports — a manifest is structurally indistinguishable from a `BundleData` at runtime, so the old idiom would silently MISREGISTER the two manifests as bundles |
| 2026-08-01 | T3 | `stdlib-tupadr3/package.json` needed no edit | Its `exports` already matched the batch-2 contract before the task started; step 2 was "confirm", and confirmation is what happened. Both READMEs' opening claims ("packaged as `BundleData` values") were made FALSE by T1, so they were corrected alongside Usage rather than left contradicting it |
| 2026-08-01 | T3 | **MEASURED unpacked (npm pack --dry-run --json):** stdlib-aws **8,346,761 B** (904 files) · stdlib-tupadr3 **20,292,674 B** (6,858 files) | Brief targets were 8.3 MB / 20.3 MB — both hit. `files` keeps `assets/` in both (stop condition 5 holds); asset paths resolve in the packed list, verified by resolving it rather than reading the `files` array |
| 2026-08-01 | T4 | **CEILINGS LOWERED:** stdlib-aws 18 → **10 MB** (measured 8,346,761 B, ~26% headroom) · stdlib-tupadr3 45 → **24 MB** (measured 20,292,598 B, ~24% headroom) | The mission's only automated regression signal. At the old values they passed vacuously at half the size. Headroom sized so ordinary vendored-asset growth does not trip them but a returned eager module would |
| 2026-08-01 | T4 | Rebuilt the "alias resolution" describe against shipped bytes rather than deleting it | Not named in the task's "unchanged" list but it closed over the removed eager vars — the two-level cascade the method rules warn about. Dropped one sub-test that literally duplicated acceptance 4 in the same file |
| 2026-08-01 | T5 | **RE-MEASURED REDUCTION: 99.693%** — baseline **19,850,300 B** (6,849 files under `assets/tupadr3/`), manifest gzip-9 51,415 B, 4 resources fetched 9,564 B, **60,979 B total over the wire** | ADR-3. **T6 and T7 quote THIS, never SI11a's 99.702%.** New baseline is 3.11% below the 20,488,276 B eager module it replaces — same content, two encodings, so the gap is re-basing not missing content; stop condition 8 not triggered. Verified three independent ways (`find`+`stat`, standalone Node script, the test's own sum) — all agree exactly |
| 2026-08-01 | batch 2 | Gates: `npm test` **470 files / 11,349 tests** green (baseline 468/11,317; +2 files, +32 tests from T1's and T2's new suites) · typecheck 0 · lint 0 · build 0 · `vendor-stdlib --verify` **34,587 verbatim** · size deltas **320/351, widened 0** · write-set containment exact (every changed file in precisely one task's set, nothing outside) | All four tasks committed separately: T2 `b9dc6912`, T3 `ddda6d8e`, T4 `fc883d3c`, T5 `e2374914` |
| 2026-08-01 | batch 2 | **DEFECT FOUND AFTER GATES PASSED — batch 5 / T8 created, maintainer-approved under stop condition 1** | The warm-tree gates above are green but CI is COLD. `packages/*/assets/` is gitignored and populated only by `copy-assets.mjs` (`prepack`); before SI12 exactly one test file touched it, and batch 2 added a second writer (T4's `beforeAll`) plus a reader (T5's baseline sum) across parallel workers. **Mechanism:** `copy-assets.mjs#copyBundleAssets` `rmSync`s then incrementally copies, and its `isUpToDate()` guard is file-COUNT based so it short-circuits only when ALREADY complete — during any partial state it returns false and deletes a tree another worker is mid-copy or mid-read of. **Reproduced 3/3 cold:** two files fail and T5's baseline reads 8,195,997 / 7,399,179 / 8,315,921 B instead of 19,850,300 B (99.256% / 99.176% / 99.267% instead of 99.693%). **Ruled out:** T5 arithmetic (warm figure verified 3 ways), T4's ceilings (pass warm), `generated/` (already serialized in `globalSetup`). Same failure class, same fix, as the SI11a T8 race that `build-stdlib-globalsetup.ts`'s own header documents. Options offered: new task / orchestrator gate-fix / revert T4's `beforeAll` and read the vendored tree / halt. Maintainer chose the new task |
| 2026-08-01 | batch 1 | Gates: typecheck RED by design (2 errors, both `spec.modules` narrowing — one is T4's expected breakage, one is the escalation above). lint 0 · build:stdlib 0 · `stdlib-eager-omission` 23/23 · `vendor-stdlib --verify` **34,587 files, all verbatim** · `packages/stdlib` byte-identical (sha256, 12/12) · 395 svg class/object/state goldens + the 54-fixture description ratchet all zero-diff | Batch-1 exit criteria met. The brief's "389 goldens" figure measures 395 today (312 class + 24 object + 59 state) — a drift correction, not a regression; nothing was re-pinned |
