# G6 Decision Journal

| Date | Task | Decision | Rationale | Flagged for review? |
|------|------|----------|-----------|---------------------|
| 2026-07-21 | Batch 1 | Execution plan: T1 (debugger, sync) → T2 (typescript-pro), strictly sequential; no parallelism (T2's write-set is conditional on T1's verdict per D1). Branch `feat/g6-cluster-geometry` created off main. | Batch has a hard T1→T2 dependency; nothing to parallelize. | No |
| 2026-07-21 | T1 | Fixture-location note: the 6 named cluster fixtures live under `tests/corpus/class/` (corpus classifier), while their goldens are `oracle/goldens/state/`. Passed to agents to avoid re-discovery. | Corpus classification diverges from golden layout; costs agents time otherwise. | No |

## T1 mechanism artifact (2026-07-21, debugger agent, CONFIRMED)

- **mechanism**: The seam emits the cluster HTML title table with `HEIGHT="3"` (`CLUSTER_TITLE_TABLE_HEIGHT = 3`) into graphviz-ts layout via `DotInputCluster.titleTableHeight`; jar's real DOT emission uses `HEIGHT="9"` — a verified constant, content-independent across 70+ cached svek-*.dot samples. graphviz-ts's cluster-label border formula (`border[TOP_IX] = label.dimen.y + 2*GAP`, GAP=4 — graph-label.ts:113, faithful port of `lib/common/input.c:885-892`) is fed dimen.y=3 instead of 9, so the TOP-only cluster margin (`position.c:714`, `ht2 += GD_border[TOP_IX].y`) is short by exactly 6pt per titleTableEligible cluster, compounding by nesting depth (6 × (1 + titleTableEligible descendants on direct chain)).
- **origin**: `src/diagrams/state/state-composite-cluster.ts:96` (`CLUSTER_TITLE_TABLE_HEIGHT = 3`), consumed at :316 and `src/core/graph-layout-build.ts:174-178`.
- **fixLocation**: `seam`
- **formula**: `CLUSTER_TITLE_TABLE_HEIGHT` 3 → 9. Pure DOT-layout-input correction; independent of `CLUSTER_HEADER_HEIGHT = 19` (renderer-side, unrelated, no change).
- **ruledOut** (evidence in T1 run, agent af020abad7fd2ec1d):
  1. graphviz-ts layout/rank-sep diverging from C — REFUTED: feeding jar's literal svek-N.dot reproduces real dot 15.1.0 byte-for-byte (gojuja A: 85 both).
  2. jar adding vertical margin outside graphviz layout — REFUTED: real dot on cached svek-N.dot alone reproduces jar oracle heights (85/181/240/99).
  3. position.c:780 cluster-sep hypothesis (the prior suspect) — REFUTED as primary cause: minimal no-label repro gives identical symmetric 24pt margins in both engines.
  4. builder-API HTML mishandling — REFUTED: doGraphLabel correctly measures what it is given; the input value was wrong.
  5. cakaxu's residual: separate edge-label internal-rank-spacing divergence (graphviz-ts 1.5pt more than dot for one labeled edge) — DIFFERENT mechanism, NOT part of this fix; expect cakaxu at −1.5 after fix.
  6. Nested-cluster margin composition (kideju-07-gero206 B/A residuals 48/96 after fix) — separate, additive, confirmed still present, OUT OF SCOPE here.
- **coverage** (predicted → verified by monkey-patch replay): gojuja A 79→85 (jar 85, resid 0); fevida example 175→181 (jar 181, resid 0); cakaxu AbstractState 235.5→241.5 (jar 240, resid −1.5, mechanism 5); decede E →99 (jar 99, resid 0; prior svg/@height gap-1 was sibling A which never reaches the titleTable path); zaloga/zumuje comp2 raw +6 verified at graphviz-ts layer (full pipeline blocked by separate class="entity"-routing gap — Batch 2/D3 territory); kideju C/B/A deltas +6/+12/+18 exact per compounding rule, C resid 0, B/A retain separate mechanism-6 residual.

| 2026-07-21 | T1 | Verdict: fixLocation=seam, one-line constant fix. Note: this does not conflict with D2 — HEIGHT="9" is jar's actual DOT emission (not a recalibration); D2's Batch-3 formula work concerns the multi-line/action-text title-table cases and remains open. | Jar emission is the spec; matching it is faithful porting. | No |
| 2026-07-21 | Batch 1 gates | ALL PASS: npm test (380 files, 10152 tests, exit 0), typecheck, lint, build. Write-set: state-composite-cluster.ts + layout.test.ts (T2) + graph-layout.types.ts doc-comment correction (orchestrator; conditional write-set member, non-behavioral, T2-flagged stale claim). size-backlog untouched (metric is rect-node dims only; no entry moved). Commits: 9044759 (brief), 729fef1 (T2 fix). | Gates green; doc fix prevents future misleading of readers per pr-workflow 1-3-line rule. | No |
| 2026-07-21 | Batch 1 | Known residuals carried forward: cakaxu −1.5 (edge-label internal-rank-spacing in graphviz-ts, unfiled — NOT yet a verified library finding at fix-grade detail; candidate for future diagnosis), kideju B/A −48/−96 (nested-cluster margin composition), zaloga/zumuje comp2 blocked by class="entity" routing (Batch 2 / D3 will address emission classification). | Each is a distinct mechanism per T1's ruled-out list; none in Batch 1 scope. | No |
| 2026-07-21 | Batch 2 | Execution plan: T3 (typescript-pro) ∥ T4 (typescript-pro) in parallel — disjoint write-sets (renderer-group/composite-box vs skinparam/state-render-colors; both add tests in different files). T5 (general-purpose) strictly after both. Orchestrator commits each task separately after both return + gates. | Per overview.md; no write conflicts; shared-worktree rule means agents make no git mutations. | No |
| 2026-07-21 | T3 | WRITE-SET AMENDMENT: T3 additionally changed `src/diagrams/state/renderer.ts` (one dispatch call site `wrapEntity`→`wrapCluster` + import in `renderClusterSiblingMarkup`, the G5 C3 flat-sibling function). The brief's write-set mislocated the wrap decision (it is made in renderer.ts, not renderer-group.ts/renderer-composite-box.ts). Treated as in-write-set per parallelism.md's logical-unit rule (interface change + call site = one unit); D3 authorizes exactly this behavior. Batch overview write-set column amended. Alternative (globally flag-changing wrapEntity) rejected as worse blast radius. | Stopping the mission over a brief-authoring geography error would serve no protective purpose; change is one line + import, jar-verified, zero pinned-golden churn (54/54 ratchet byte-identical), no overlap with T4's files. | **Yes — review the write-set amendment** |
| 2026-07-21 | T3 | Classification threading design: reused the existing `node.clusterHeaderHeight !== undefined` gate (already the exact real-cluster dispatch signal from G5 C3) instead of adding a new pass-context field. 'cluster'-classified composites that the port's eligibility gate excludes (multi-line title etc.) still fall back to class="entity" — pre-existing named gap, out of T3 scope (no sizing/classification changes allowed). | Avoids a new type field duplicating an existing signal; keeps write-set minimal. | No |
| 2026-07-21 | T4 | Mechanism (diagnosed before fix): bare `statediagram{}` selector parsed into StyleMap key "statediagram" but no consumer read it (applyStyleMap reads only "statediagram.arrow"; collectElementStyleBuckets only "state"/"statediagram.<bucket>") → renderer fell to hardcoded defaults. Fix: cascade-alias bare-selector props into the nested keys in parseStyleBlock + new border/font bucket tiers in state-render-colors.ts. | Origin-level fix through existing machinery per D4. | No |
| 2026-07-21 | T4 | CONTINUATION + WRITE-SET AMENDMENT: two style-attributable residuals (cluster-title fontcolor hardcoded #000000 in renderer-composite-box.ts:142-148; RoundCorner rx/ry hardcoded STATE_BOX_RX=12.5 in renderer-box.ts:42 + renderer-composite-box.ts:92) were blocked only by T3's parallel file ownership, now released. Resuming T4 with write-set extended to renderer-composite-box.ts (already in batch write-set via T3) and renderer-box.ts (NEW — outside batch write-set, amendment flagged). Bar: 54 pinned ratchet goldens stay byte-identical (defaults must resolve to current values). | T4's acceptance criterion (every style-attributable attribute matches jar) unmet without these; artificial concurrency boundary, not scope boundary. | **Yes — review renderer-box.ts amendment** |
| 2026-07-21 | T4 | Round-2 landed: cluster-title FontColor + RoundCorner wired through existing resolution machinery. Additional WRITE-SET AMENDMENT: `src/core/theme.ts` + `src/core/style-map-theme.ts` (new optional `stateCascadeRoundCorner` field + its applyStyleMap population) — required because Theme.colors.graph is strictly typed (no index signature); mirrors the existing stateArrowLineColor pattern, additive-only. decede now matches jar on EVERY style attribute; 54/54 pins byte-identical; 3019 cross-family tests green. | Typed seam is the only non-special-case route from StyleMap to state-render-colors; per D4 no renderer parser allowed. | **Yes — review theme.ts/style-map-theme.ts amendment** |
| 2026-07-21 | T4 | decede post-T4 residuals are geometry-only: canvas 393×279 vs jar 396×290, and composites A/C render via the autonom shape (fail the G5 C3 cluster eligibility gate) while jar renders real clusters. Batch-2 premise ("decede blocked only by class-attr + style cascade") was optimistic — T5's byte-compare will adjudicate; expect decede NOT pinnable this batch. | Pre-existing named gaps (eligibility gate, mechanism-16 leftovers), not style. | No |
| 2026-07-21 | Batch 2 gates | ALL PASS: npm test (10180 tests), typecheck, lint, build. Write-set = declared sets + three journaled amendments (renderer.ts, theme.ts/style-map-theme.ts, renderer-box.ts). Commits: 6775601 (T3), 15b9fc7 (T4), 2c5b18c (T5). Census: svg-state pins 52→57 (brief's "54" and backlog "93" were off-by-recordkeeping; actual on-disk 52/92). Backlog 92→92, widened none. | Gates green; census floor raised. | No |

### Batch-2 residual mechanisms (T5 byte-compare, for future missions)

- **gojuja-90-pune699** (55 diffs): (1) uniform −5px vertical shift on everything below the first composite's title band (canvas 334 vs 339) — ~50 diffs are this one offset; (2) `*start*-to-A` edge structurally different: single spline run to y≈89.9 vs jar's 3-segment spline ending y≈44.73 (arrowhead −45.13), and edge id `*start*-to-__zaent_A` leaks the cluster-entry alias (jar: `*start*-to-A`).
- **decede-10-buvu414** (44 diffs): half-pixel band offsets (upper-band y 23/56 vs jar 23.5/56.5; lower band −1), one missing child element in g[1]/g[3] (6 vs 7), final-edge drift 0.57→1.18. Canvas 279 vs 280.
- **cakaxu-97-nexe753** (37 diffs): cluster now 1.5 TOO TALL (241.5 vs 240 — T1's predicted edge-label internal-rank-spacing divergence in graphviz-ts, still unfiled pending fix-grade diagnosis); plus `AbstractState-to-InnerState1` edge-label styled with default state font instead of jar's 13pt #000000 edge-label style (fill #181818 vs #000000, size 14 vs 13, missing lengthAdjust/textLength), id `__zaent_` prefix leak.
- Cross-cutting candidate: the `__zaent_` id leak appears in both gojuja and cakaxu edge ids.
| 2026-07-21 | Batch 3 | Execution plan: T6 (debugger, done) → T7 (typescript-pro), sequential. T6 derived formula from jar source (ClusterHeader.java:73-96, ClusterDotString.java:124 getTitleAndAttributeHeight()-5, IEntityImage.java:45 MARGIN=5, Entity.java:610-633 getStateDescription): titleTableHeight = (stereoLines+titleLines+attrLines)*14 + (attrLines>0?5:0) - 5. Reduces to 9 single-line; 42 Run; 37 multi-line-A (render gap 47 = C5's datum). 6/6 verification exact incl. literal cached-DOT HEIGHT reads. Relaxable: lineCount===1 only, plus NEW attrLines plumbing from s.description; fontSize===14 / hasBorderPointChildren / insideAutonomPass gates stay. stereoLines term source-derived but oracle-unverified (no fixture found) — T7 must watch it in re-measurement. | Formula from source, oracle verifies (D2 discipline). | No |
