# Decision Journal — svg-sprite-nanoparser

Append one row per non-trivial judgment call. "Non-trivial" means: a
reasonable developer might have chosen differently.

Also log here: quality-gate results per batch, any brief line-number
correction, any measured before/after figure the brief asks you to record,
and every STOP with its full error output.

| Date | Task | Decision | Rationale |
|---|---|---|---|
| 2026-07-30 | planning | Mission brief created | Diagnosis was already jar-verified in `plans/s1l-leaf-sizing/ledger.md`; planning started near Phase 2 rather than cold |
| 2026-07-30 | planning | ADR-5's stated reason for inlining sprite declarations was WRONG and is corrected in place | It said `renderFixture` "runs the preprocessor with no include resolver", which reads as though the port cannot resolve `!include <bootstrap/bootstrap>`. It can, and has since SI5b closed 2026-07-14. The real cause is narrower: `render-fixture.ts` calls `buildBlockUmls(markup)` with no options, while `svg-conformance-census.ts:148-155` — which its own doc comment claims it mirrors "exactly" — does build a store. Inlining stays right for this mission; filed as SI8, which owns reverting the three fixtures to the `!include` form. Also corrected two stale `.claude/catalog.md` entries this surfaced ("No stdlib asset is vendored"), plausibly the source of the wrong framing. Commit `6c10139d` |
| 2026-07-30 | batch 1 | Execution plan logged rather than presented (autonomous mode, `parallelism.md`) | Four parallel `typescript-pro` agents, one per task. Write-sets disjoint: T1/T2/T3 create new files, T4 modifies `creole-atoms.ts` alone. Agents forbidden from git mutations (shared worktree) and from running the full suite (siblings mid-edit → false failures); orchestrator commits after the batch |
| 2026-07-30 | T4 | **STOP raised and resolved by the maintainer: T4's acceptance criteria were impossible as written.** T4 extended to add the discriminant across producers and draw sites | The task promised "no consumer modified" / "existing producers unchanged". Against ADR-2's own shape that cannot hold: a required `kind` literal means neither producer satisfies the type, and both draw sites read `.href` unconditionally, which cannot compile once a variant without `href` exists. 10 typecheck errors, all outside T4's write-set, all in files owned by T7/T9/T10. Options put to the maintainer were (A) extend T4 with the mechanical discriminant plumbing, (B) defer T4 into batch 2 with T7. **Maintainer chose A.** Behaviour preserved — every path still emits `image`; size-deltas held at 320/351 widened 0. Commit `89fbb4c4` |
| 2026-07-30 | batch 1 | **Write-set correction: the three new test files were relocated from `src/` to `tests/unit/core/klimt/`** | The brief's write-sets specified colocated `src/**/*.test.ts` paths. `vitest.config.ts`'s `include` is `['tests/**/*.test.ts']` and the repo contains **zero** pre-existing colocated `src` tests — so as authored, all 54 new tests were invisible to `npm test` and the batch would have reported green having run none of them. Three agents flagged this independently. Repo convention wins over the brief; no config change. Test count confirms it: 449→452 files, 11,029→11,083 tests |
| 2026-07-30 | T2/T3 | Type-seam mismatch recorded for T6/T8, not fixed here | T3's locally-declared structural `ColorResolver` returns `Paint`; T2's actual `ColorResolver` returns `ResolvedColor`. T3 could not import T2's file (parallel tasks, and creating it was outside T3's write-set). Whoever wires `SvgNanoParser` to both in T6/T8 must reconcile them — this is a real mismatch, found by T3 verifying its own assumption against the landed sibling rather than trusting the batch's "no interdependency" claim |
| 2026-07-30 | T1/T3 | `XAffineTransform` now exists in `UGraphicWithScale.ts`; T1 worked around its absence | Both agents independently confirmed no `XAffineTransform` existed. T1 ported the affine overload's *logic* with an identity scale rather than the type; T3 ported the type inline. Not a conflict today, but T6/T8 should consume T3's rather than add a third path. T1 also did not port `toSvg()`, `drawMe`, or the literal `toUPath` overloads — no caller, and its interface contract names only `parseSvgPath` |
| 2026-07-30 | T4 | Dead test block flagged, not deleted | `tests/unit/creole-img-render.test.ts` still carries `describe('AtomImageResolver — optional ink fields (T3-seams, ADR-2)')` exercising the `inkX/inkY/inkWidth/inkHeight` fields that `sizer-footprint-parity` T2 deleted. They survived the deletion because excess properties are not checked in function-return position, so they assert against a channel the type no longer declares and cannot fail. Minimal `kind: 'image'` added to keep them compiling; **deleting them is a separate decision for the maintainer**, and ADR-2's non-goal is the argument for it |
| 2026-07-30 | batch 1 | **Batch 1 gates: all green.** `npm test` 452 files / 11,083 tests · `typecheck` 0 · `lint` 0 · `build` 0 · `measure-description-size-deltas.ts` **320/351 (91.2%), widened 0** — identical to the pre-batch baseline | Exit criterion "no rendered output changed" met: the size-delta summary and all 389 SVG goldens are byte-identical. Commits `6a5d3e7d` (T1), `75b2baaf` (T2), `4de739aa` (T3), `89fbb4c4` (T4) |
| 2026-07-30 | T5 | ADR-1's equivalence verified, no divergence | `pathBBox` now wraps `parseSvgPath` + `UPath`'s minmax. Existing tests pass **unmodified** (46/46) — the evidence ADR-1 asked for. `bi-globe` bit-identical at `{0, 1.077, 16, 14.923}`. **Brief inaccuracy for the record:** stop condition 7 and T5's spec both refer to "the existing `pathBBox` test suite", but no `svg-path-bbox.test.ts` exists; the coverage lives in `SvgPath.test.ts` and `sprite-commands.test.ts`. The proof stands, it just isn't where the brief said |
| 2026-07-30 | T6 | ColorResolver seam resolved with a local adapter, neither sibling file edited | `UGraphicWithScale`'s structural interface returns `Paint`; `ColorResolver` returns `ResolvedColor`. A private `PaintColorResolver` in `SvgNanoParser.ts` converts via `colorResolverToSvgHex`. Widening either sibling was rejected — both conventions are right for their own other callers |
| 2026-07-30 | T7 | `EntityImageDescriptionDelegates.ts#drawU` keeps T4's guard | Same draw site, outside T7's write-set. Class/object/state sprite atoms need it moved in step; scheduled for planning alongside T9 rather than left silent |
| 2026-07-30 | batch 2 | **Batch 2 gates: all green.** `npm test` 454 files / 11,106 tests · typecheck 0 · lint 0 · build 0 · size-deltas **320/351, widened 0** — unchanged | "Still no rendered output change" holds. Commits `dacdc82d` (T5), `68260ccb` (T6), `214f29ae` (T7) |
| 2026-07-30 | orchestrator | **Process error: a persisted `cd` into `~/git/plantuml` put the T7 commit in the Java reference repo.** Reset `--mixed`, restoring its pre-existing uncommitted `.gitignore` edit exactly; re-committed in `plantuml-ts` with `git -C` | The misplaced commit contained only that one unrelated `.gitignore` modification it swept up. Second occurrence of persisted-`cd` on this mission line — use `git -C <abs>` for every git call, never a bare `git` after any `cd` |
| 2026-07-30 | T6/T8 | **STOP — the `<g transform>` stack cannot reach path geometry. Escalated to the maintainer; blocks T8.** | T6 accumulates the transform matrix correctly, but nothing applies it. Two causes: (1) `parseSvgPath(d, translate: UTranslate)` takes a translate, while the overload upstream actually consumes is `toUPath(XAffineTransform at)`, which bakes the full matrix in at parse time; (2) `UPath.affine`/`rotate` and `USegment.affine`/`rotate` are unported — **deferred solely because `XAffineTransform` did not exist, which T3 has since created**, so the stated blocker is stale (method rule 2 applied to a deferral note). ~40 Java lines. Reach: archimate 63 `transform=` **including this mission's own `sprite-svg-archimate-0` golden**; bootstrap 1, so `bootstrap-0`/`ruziru-69` — the mission's actual objective — are unaffected; stdlib-wide 18,241. `src/core/klimt/shape/UPath.ts` is in **no** task's write-set, so this is stop condition 1. Note when porting: upstream's `USegment.affine` throws `UnsupportedOperationException` for any non-ARCTO segment with `coord.length != 2`, i.e. cubics cannot be affine-transformed — that is exactly why `drawPath` bakes the transform in at parse time instead of calling `affine`, and the throw must be preserved |
| 2026-07-30 | T13 | **Maintainer approved the escalation; task inserted between batches 2 and 3 and landed.** `USegment.rotate/affine`, `UPath.rotate/affine`, `XAffineTransform.getRotateInstance` + point transform, and the transform threaded into `drawPath` | Commit `ff50f844`. `parseSvgPath`'s transform is an OPTIONAL third param defaulting to identity, so `svg-path-bbox.ts`'s two-arg call compiles untouched — ADR-1's evidence is intact, and `SvgPath.test.ts` changed by **63 insertions / 0 deletions** (verified with `git diff --numstat`, not taken on the agent's word). `USegment.affine`'s throw for non-ARCTO segments with `coord.length != 2` is ported and pinned by a test. Gates: 455 files / 11,123 tests, typecheck/lint/build 0, size-deltas 320/351 widened 0 — rendered output unmoved, as required while nothing emits `drawable` |
| 2026-07-30 | method | **Generalised lesson: a deferral note's stated blocker can go stale silently.** `UPath.ts:31`/`:91` deferred `affine`/`rotate` "because `XAffineTransform` is not part of this port's geometry surface" — correct when written, false the moment T3 landed | This is method rule 2 applied to a *deferral* rather than a fix. Worth applying to every "not supported / out of scope / deferred" note in this codebase: check whether its reason still holds before believing it. T13's agent was briefed to do exactly that going forward |
