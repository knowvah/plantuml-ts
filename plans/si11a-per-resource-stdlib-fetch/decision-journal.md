# Decision Journal — si11a-per-resource-stdlib-fetch

Append one row per non-trivial judgment call. "Non-trivial" means: a reasonable
developer might have chosen differently.

Also log here: quality-gate results per batch, any brief line-number correction,
every measurement the brief asks you to record, and every STOP with its full
output.

**T4 must record its concurrency primitive and, if bounded, the bound and its
reasoning.**
**T8 must record the measured payload — T9 quotes it rather than the brief's
projection.**

| Date | Task | Decision | Rationale |
|---|---|---|---|
| 2026-07-31 | planning | Mission brief created | SI11 raised as a tracked row when SI8 closed; planned the same day |
| 2026-07-31 | planning | **SI11 SPLIT into SI11a and SI11b on maintainer direction** | Two distinct mechanisms (per-resource fetch; per-sprite transform), a build-pipeline change, a licensing carve-out and a hosting decision in one mission was too much. The split also fixed a sequencing problem: SI11b's sprite index rides on the manifest/asset shape SI11a establishes, so planning it first would have built on an unbuilt foundation |
| 2026-07-31 | planning | Mechanism = runtime HTTP fetch, NOT per-resource static modules | Maintainer ruling before planning. 6,849 modules for tupadr3 (29,101 tree-wide) is a bundler-explosion risk and would substantially change the generated package shape |
| 2026-07-31 | planning | **The three published packages have THREE different shapes** — measured, and it reshaped the mission | `bootstrap1.13.1` is 1.06 MB in **3 files** (2,078 sprites in ONE 1,059.9 KB file), so per-resource splitting is a NO-OP for it; `awslib14` is 7.93 MB / 891 resources; `tupadr3` is 19.54 MB / 6,849. "Per-resource splitting" as ADR-2 of SI8 framed it solves the two giants and does nothing for bootstrap — which is what justified the split |
| 2026-07-31 | planning | ADR-3 (key→path map, not convention) rests on a measurement | **890 of 891 `awslib14` paths contain uppercase** while `derivePumlKey` lowercases. Path-by-convention would require renaming vendored files; the map costs ~19 KB gzip more for tupadr3 (49.6 vs 31.0) and keeps SI11a a pure checksummed file copy — which matters because `awslib14` is the CC BY-ND bundle |
| 2026-07-31 | planning | ADR-7 was written only after extensibility was **verified working today** | Probed a non-`@plantuml-ts` bundle: registers under its own name and resolves eagerly, lazily, and through nested cross-includes. Nothing in `src/` hardcodes a package list or closes the bundle namespace. So the gap SI11a could introduce is not "third parties can't register" but "third parties can't get per-resource" — which is what ADR-7 closes |
| 2026-07-31 | planning | The sprite crux was verified, not assumed — and it is SI11b's, not SI11a's | `SpriteRegistry` is a per-diagram `Map` read SYNCHRONOUSLY at measure/render (`getSprite`, `spriteDimsLookupFor`, `Stereotype#getSprite`). No await is available at lookup and `renderSync` must stay sync, so per-sprite loading must be prefetch-driven off a `<$name>` scan. Recorded here so SI11b does not re-derive it |
| 2026-07-31 | planning | `assets/stdlib/stdlib` is a **self-referential symlink** | Discovered while measuring; a naive recursive `find` double-counts or loops. The 29,101 figure excludes it and is correct. The tree is gitignored and regenerable, so this is a note for tooling, not a defect to file |
| 2026-07-31 | batch-1 plan | Executed T1 ∥ T5 as two `typescript-pro` agents; write-sets disjoint (`src/core/tim/StdlibRemote.ts` + `tests/unit/stdlib-remote.test.ts` vs `scripts/build-stdlib-packages/**` + `tests/unit/stdlib-packages.test.ts`). Agents forbidden from all state-mutating git; orchestrator commits per task | The brief's batch-1/overview.md declares the two independent and fixes the manifest shape so neither blocks the other. Agents share one worktree, so a git mutation by either would entangle the other's diff |
| 2026-07-31 | batch-1 gate | Agents run typecheck + lint + their own targeted vitest only; orchestrator runs the full four-gate set after both return | Three concurrent full `npm test` runs would thrash the machine and make failure attribution harder, not easier. Baseline `npm test` was confirmed exit 0 before either agent wrote a file |
| 2026-07-31 | T1 | Memoized via a `.catch()`-chained promise rather than `async`/`await` inside `fetch()` | Keeps the whole synchronous portion (manifest lookup, cache check, `inflight.set`) before any microtask boundary, so concurrent calls share one in-flight promise without depending on await-ordering. `StdlibRegistry`'s `async` + `await pending` style would also work; this has fewer suspension points to reason about |
| 2026-07-31 | T1 | Added a test beyond the six criteria asserting the DEFAULT fetcher really is `fetchInclude` | Method rule 2 — the ADR-5 "CORS/CSP differentiation for free" claim was verified against the current call graph by spying on `globalThis.fetch`, not taken from the doc comment. It holds |
| 2026-07-31 | T5 | `PackageSpec.remoteModules` reuses `GeneratedModule` rather than a parallel type | The remote manifest is a pure function of the same `(exportName, bundleName, assetFolder \| aliasOf)` metadata the eager export already carries; a new type would duplicate declarative data for no benefit |
| 2026-07-31 | T5 | Remote export identifier is `${exportName}Remote`; only `stdlib-aws` and `stdlib-tupadr3` got `remoteModules` | Matches the brief's sample verbatim and keeps `package-specs.ts` free of a second naming axis. `stdlib`/`-all` are out of T6's stated packaging scope; `remoteModules` is optional so they can be added later |
| 2026-07-31 | T5 | **MEASURED, not cited: the tupadr3 manifest gzips to 52,118 B (50.9 KiB)**, under the ≤60 KB bound | Brief projected 49.6 KB. The delta is vendored-asset drift since that measurement, not a defect. Method rule for T5 required re-measuring against current assets rather than repeating the brief's number |
| 2026-07-31 | T5 | **Non-obvious test constraint discovered:** a test that `import()`s a generated file must write it INSIDE the repo root (used `node_modules/`), not `os.tmpdir()` | Vitest's Vite dev server denies `import()` outside its `fs.allow` root, which defaults to the workspace root; macOS `os.tmpdir()` (`/private/var/folders/...`) is outside it and every load 404'd. Recorded for any future task that dynamic-imports generated output |
| 2026-07-31 | batch-1 gates | **All green.** `npm test` 460 files / 11,235 tests (baseline 459/11,219: +1 file, +16 tests = 11 T1 + 5 T5); typecheck/lint/build exit 0; `vendor-stdlib --verify` 34,587 files verbatim; size-deltas 320/351 **widened 0**; ratchet + svg-conformance 873 tests pass | Gate set run by the orchestrator after both agents returned, so a failure could be attributed before either commit landed. No vendored file changed; working tree matched the two declared write-sets exactly |
| 2026-07-31 | **STOP — condition 12** | **`scripts/build-stdlib-packages.ts` is in NO task's write-set, but the mission cannot emit a manifest without it.** `buildPackage()` (build-stdlib-packages.ts:44-54) iterates only `spec.modules`; `spec.remoteModules` is never read, so T5's emitters are unreferenced and `npm run build:stdlib` produces no manifest. Verified directly against the call graph, not taken from the agent's report. T5's write-set lists four files and excludes this one; T6's spec says "Do NOT modify the generator"; all nine tasks' write-sets were checked. **Escalated to the maintainer rather than self-approved** | The fix is a ~4-line loop mirroring the existing one, and T5's own task TITLE ("the generator emits a remote-manifest module per bundle") implies it — but stop condition 12 is explicit and this mission line has a history of costly scope errors, so the boundary call is the maintainer's. T6 is blocked on it; T2 and T7 are not |
| 2026-07-31 | **maintainer ruling** | **Stop condition 12 resolved: the generator wiring was authorized as a `fix(T5)` commit** | Presented as a sized decision (a ~4-line loop, build-time only, no `src/` or vendored impact) against two alternatives (widen T6's write-set; halt for replanning). The maintainer chose the T5 fix. Emitted filename `<fileBaseName>.remote.{js,d.ts}` was part of that authorization, so T6 can match its `exports` subpaths to it |
| 2026-07-31 | fix(T5) | Emitted the manifest as a `.remote` SIBLING of the eager module, not a replacement | ADR-1: the eager module must stay byte-identical for offline consumers. Packages with no `remoteModules` (`stdlib`, `stdlib-all`) are untouched by the loop |
| 2026-07-31 | fix(T5) | **MEASURED on real generator output:** `tupadr3.js` 20,488,276 B → `tupadr3.remote.js` 433,420 B (51,803 B gzip -9); `awslib14.js` 8,319,265 B → 71,848 B (11,116 B gzip -9) | The brief's per-bundle projections are now superseded by measurements taken from the emitted files. **ADR-6's awslib14 figure of 8.3 KB gzip is low — it measures 11.1 KB.** T7's docs had already flagged that number as un-remeasured; T8/T9 should quote 11.1 KB |
| 2026-07-31 | **T2 — brief correction with teeth** | **A bare `StdlibRemoteManifest` is structurally INDISTINGUISHABLE from a `BundleData` at runtime**, so T2's task item 1 ("teach `harvest` to recognise a manifest-shaped export") is not achievable as written | Both are `{name, aliasOf?, files: Record<string,string>}`; only the SEMANTICS of the values differ (paths vs content). Registering a `.remote` module directly would make the registry serve file PATHS as file CONTENT — silent corruption. Converting one needs a `baseUrl`, which ADR-4 forbids defaulting and `stdlibRegistry`'s frozen signature cannot supply. The functional path is a **`RemoteBundle`-shaped export, pre-wrapped via `remoteStdlib()` in the registrant's own thunk**. Documented at `isBundleData` rather than auto-wrapped. **Every downstream consumer recipe (T6 README, T8 e2e, T9 summary) must use the wrapped idiom** |
| 2026-07-31 | T2 | `resolve()` and `resolveResource()` both route through one internal `resolveEntry()`, branching once on the RESULT rather than on how the bundle was registered | Gives T3 a single code path — the stated point of ADR-2. Verified the alias chain through the REAL `bundlesFor`/`prefetchInner` via `prepareIncludeStore` + `renderSync`, by test rather than by reading (method rule 2). Exactly one implementer of `StdlibRegistry` exists repo-wide (the factory's own object literal); no structural test double needed updating |
| 2026-07-31 | T2 | `harvest` now also inspects the top-level resolved thunk value, not only its named exports | Supports the `() => import(x).then((m) => remoteStdlib({…}))` idiom, where the thunk resolves directly to one `RemoteBundle` rather than a namespace. The symmetric top-level check for `BundleData` is harmless — no existing thunk resolves to a bare top-level `BundleData`; verified no regression across the 34 pinned registry tests |
| 2026-07-31 | T7 | `src/index.ts` landed at exactly 500 lines by removing a stray double-blank line and tightening one `#lizard forgives` comment — the DIRECTIVE preserved, only its prose reworded | Extracting a helper for the `render`/`chrome`/`assemble` tail would have needed its own doc comment plus a 7-param exemption comment, netting MORE lines than it saved. Orchestrator reviewed the diff to confirm nothing load-bearing was dropped |
| 2026-07-31 | T7 | Docs deliberately document NO generated-package subpath as currently usable | At the time T7 ran, `build:stdlib` emitted no manifest (the stop-12 gap). Rather than document a path that 404s, the docs carry a "what doesn't exist yet" section and build every recipe on a consumer-supplied manifest object. **T9 should revisit this now that the wiring has landed** |
| 2026-07-31 | batch-2 gates (T2, T7) | **All green.** `npm test` 460 files / 11,245 tests (+10 from T2); typecheck/lint/build exit 0; size-deltas 320/351 **widened 0**; `vendor-stdlib --verify` verbatim. Re-run after the `fix(T5)` wiring: unchanged | T7 reported a lint error and a `TS2741` mid-run, both in `StdlibRegistry.ts` — T2's concurrently-unfinished file, outside T7's write-set. Both cleared once T2 landed. Correctly diagnosed by T7 as cross-agent interference rather than chased |
| 2026-07-31 | T6 | Assets ship via each package's OWN `prepack` script (`packages/<pkg>/scripts/copy-assets.mjs`), not via the shared generator | Kept the mechanism reproducible AND inside T6's write-set, avoiding a third write-set escalation. Verified empirically that `npm pack --dry-run --json` really does run `prepack`. Byte copy via `copyFileSync` with no read-and-rewrite — `awslib14` is CC BY-**ND** and a re-encode would void the grant |
| 2026-07-31 | T6 | Assets live at package-root `assets/`, not under `generated/`, and are gitignored per-package | The generator's `rmSync(generatedDir)` would otherwise delete them on every rebuild; keeping the two regeneration steps independent. Gitignoring matches `generated/`'s established "regenerable, never committed" convention, so the root `.gitignore` stayed untouched |
| 2026-07-31 | T6 | **Portability note:** `copy-assets.mjs` uses `readdirSync(..., {recursive:true})` + `entry.parentPath`, which needs Node ≥20.12 | This repo declares no `engines` field and runs Node 25.2.1, where `entry.path` no longer exists at all. Only ever executed at pack/publish time in the maintainer's environment, so the exposure is bounded — recorded rather than fixed |
| 2026-07-31 | **T6 STOP → resolved by the orchestrator** | **`PACK_CEILINGS` failed: stdlib-aws 16,665,500 B vs a 15 MB ceiling, stdlib-tupadr3 40,780,091 B vs 35 MB.** Raised to 18 MB / 45 MB | **Not stop condition 12** — `tests/unit/stdlib-packages.test.ts` IS in T5's write-set, just not T6's, so the mission's own rule left this resolvable without escalation. The overrun is a deliberate consequence of two approved ADRs, not bloat: ADR-1 keeps the eager inlined module byte-identical for offline consumers while ADR-4 additionally ships the raw `.puml` files, so each bundle's content exists twice in two encodings **on purpose**. Ceilings keep ~2 MB / ~6 MB headroom — enough for asset drift, tight enough to still catch an accidental third copy. **The packages roughly DOUBLE in size; flagged to the maintainer as a product consequence worth revisiting** |
| 2026-07-31 | **T3 STOP → maintainer ruling** | **The `Stdlib.java` key transform was unreachable: it lived only inside the PRIVATE `resolvePumlResource` (`StdlibStore.ts:97`), and `StdlibStore.ts` is in no task's write-set.** Maintainer chose a new shared module | Independently verified before escalating: only `BundleData`/`StdlibStore`/`stdlibStore`/`withStdlib` are exported, and the build-time `derivePumlKey` imports `node:fs` at module scope so it cannot cross into browser-safe `src/` (and takes a bundle-relative path, so it has the wrong shape regardless). Duplicating the transform was forbidden — two answers to that question is how the port drifts from the jar. **T3 stopped correctly rather than improvising a Proxy-based workaround it had considered** |
| 2026-07-31 | fix(T3) | Created `src/core/tim/stdlib-path.ts` exporting `splitStdlibPath`; `resolvePumlResource` now calls it | One implementation, two runtime callers. Purely additive — `BundleData`, `stdlibStore`, `withStdlib` and `resolve()` signatures are all unchanged, so mission stop condition 4 is untouched despite the file being named in it. Alias resolution deliberately stayed in `StdlibStore#resolveBundle` with its cycle guard, per ADR-2 |
| 2026-07-31 | fix(T7) | **Two real doc defects found, beyond staleness** | (a) Every example imported from `@plantuml-ts/core`, **which does not exist** — the package is `plantuml-ts`. (b) The registry example hand-rolled `{ resolve }` only, which no longer satisfies the interface after T2 added a required `resolveResource`, and would have resolved NOTHING anyway since `asBundleData()` returns empty `files` by design. Both would have shipped a recipe that cannot work. Found by checking the doc against the real API rather than reading it for staleness alone |
| 2026-07-31 | **T3 — brief factually wrong** | The task asserted "a tupadr3 icon `!include <tupadr3/common>`". **`assets/stdlib/tupadr3` contains ZERO `!include` directives** — verified by grep across the whole tree | `ban.puml` uses the `ENTITY()` macro `common.puml` defines; there is no include edge. The transitive-walk criterion was re-based on a VERIFIED real instance instead: `assets/stdlib/C4/C4_Context.puml`, whose first line is `!include <C4/C4>`. Confirmed independently by the orchestrator, not relayed on the agent's word. Method rule 2 working exactly as intended — the claim was checked against a real bundle file rather than against the design |
| 2026-07-31 | T3 | Asserted the derived key by spying on `resolveResource` directly, rather than inferring it from the fetch URL | A stronger proof of criteria 1 and 5: it pins the exact `(bundle, key)` pair the walk asks for, independent of how the manifest happens to map that key to a path |
| 2026-07-31 | **T6 gate failure — DIAGNOSED, not patched** | `npm pack --dry-run --json` failed for stdlib-tupadr3 in the full suite. **Mechanism: a timeout, not a size breach.** Raised the pack tests' budget 30 s → 120 s | Standalone the command SUCCEEDS in 12.1 s and reports 40,780,091 B, under the new 45 MB ceiling. T6's `prepack` hook now byte-copies 6,849 `.puml` files before packing; under the full suite's parallel worker load that overran the 30 s budget, and `execFileSync` surfaced the kill as a bare `Command failed: npm pack --dry-run --json` — a timeout wearing the costume of a packaging failure. Reproduced standalone before changing anything, per the repo's diagnosis rule; the ceiling assertion itself was never the thing failing |
| 2026-07-31 | batch-3 + T6 gates | **All green.** `npm test` 462 files / 11,258 tests; typecheck/lint/build exit 0; `vendor-stdlib --verify` verbatim; size-deltas 320/351 **widened 0** | Baseline was 459/11,219. Net +3 files, +39 tests across T1, T2, T3, T5, T6 |
| 2026-07-31 | pre-flight | **The new `vendor-stdlib --verify` gate failed at BASELINE — fixed before handoff** | `bootstrap1.13.1/.gitignore` had `.serena/cache/` appended to it by Serena MCP tooling, so the gate this mission promotes to a stop condition was already red. Restored the exact vendored 55 bytes by hashing every line-boundary truncation against the manifest's `sha256:3c315ddb…` rather than guessing (a first guessed trim cut one byte too many and did not match). Gate now reports `all files verbatim (sha256 match)` across 34,587 files / 34 bundles. **If `--verify` flags this same file mid-mission, it is Serena drift in a gitignored regenerable tree — NOT a stop-condition-5 violation.** Any OTHER file is a real violation |
