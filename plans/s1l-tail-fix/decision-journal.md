# Decision Journal — S1L Tail Fix

Append one row per non-trivial judgment call. "Non-trivial" means a reasonable
engineer might have chosen differently. Append during execution; never rewrite
an existing row.

Log here in particular:
- any SYNTHESIS mechanism that turns out wrong (push-forward 2 — expected)
- every pin set reported to the orchestrator for deletion (ADR-1)
- the `fariba-82` residual outcome, resolved or not (push-forward 4)
- the sprite licence review's per-set verdicts (ADR-9)

| Date | Task | Decision / observation | Rationale | Needs review? |
|------|------|------------------------|-----------|---------------|
| 2026-08-07 | orchestrator | Branched `feature/s1l-tail-fix` off `feature/s1l-tail-diagnosis` HEAD (4a449dfd), not off `main` as README says | The diagnosis branch is 13 commits ahead of `main` and is unmerged; the mission brief, the nine ADRs and all of `findings/` live only there. Branching off `main` would have dropped every input the mission depends on. | No |
| 2026-08-07 | orchestrator | Baseline re-measured before Batch 1: 351 total, 321 conformant (91.5%), widened 0, improved 0 | Confirms README's starting figure on this tree; gives the batch gates a real before-picture rather than a quoted one. | No |
| 2026-08-07 | F1-c | Split `RAW_GLYPHS`'s 223-entry data table into a new file, `src/core/openiconic-glyphs-data.ts` (exporting `RAW_GLYPHS` + the `RawGlyph` type), imported by `src/core/openiconic-glyphs.ts` -- one file beyond the task's declared 2-file write-set. | The full upstream OpenIconic set (223 icons) cannot fit in `openiconic-glyphs.ts` under this project's enforced 500-line-file cap (the local `check-complexity.py` hook blocked the edit at 503 lines, before the table was even added) -- the same size problem the project already solved by precedent (`svg.ts`→`svg-markers.ts`, `style-map-theme.ts`→`style-map-element.ts`, both re-exporting from the original to keep the public API stable). Zero behavior change; zero overlap with F1-a's (`leaf-sizing.ts`/`renderer-entity.ts`/`CreoleParser.ts`) or F1-b's (`parser.ts`/`parse-helpers.ts`/`parse-state.ts`) write-sets; `isKnownOpenIconicGlyph`/`buildOpenIconicPathD`'s public signatures are unchanged. Logged rather than a hard stop per parallelism.md's "purely stylistic, doesn't affect behavior" push-forward criterion. | Yes -- flagging for maintainer visibility since it is technically outside the task file's literal write-set, even though it introduces no write conflict. |
| 2026-08-07 | F1-b | **Task-file mechanism corrected (push-forward 2):** G8's fix is NOT "the minimum leading whitespace across the accumulated body lines". Upstream `BlocLines#trimSmart(1)` (`utils/BlocLines.java:305-316`) strips the leading space/tab run of the block's **reference line** — block line index 1, i.e. the FIRST BODY LINE — from that line and every line after it, capped per-line by `removeStartingSpaces` (`:330-343`). A minimum would under-strip whenever the first body line is the least-indented one and over-strip nothing, but it is a different function and diverges the moment a later line is indented less than the first. Implemented reference-line-based; `vixeni-34-nici683` measures exact (pairMax 0). | The task file's "common base indent (minimum)" wording was a plausible paraphrase, not the Java. CLAUDE.md: upstream is authoritative; the reference-line rule is also what makes the deepest `|_` reach level 3 rather than a min-derived level. | No |
| 2026-08-07 | F1-b | **Upstream quirk recorded, not a defect:** TYPE0's CLOSING line is exempt from `trimSmart` in effect — upstream reads its pre-quote prefix off `lines.getLast().getTrimmed()` (`CommandCreateElementMultilines.java:184`), so a terminator line's own indentation never reaches the display even though `trimSmart` ran over it. Same for TYPE1's `]` line. A co-located test asserts this rather than the intuitive "all body lines keep relative indent". | Found when the first draft of the TYPE0-indent test failed against the (correct) implementation. Instrumented against the Java before changing either side; the test was wrong, the code was right. | No |
| 2026-08-07 | F1-b | `ParseState`'s `inElementBlock`/`elementBlockNode`/`elementBlockBody` trio replaced by ONE `pendingElement: PendingElementState \| undefined`, discriminated by a `terminator: 'bracket' \| 'quote'` field. Deliberately NOT the two-variant `kind`-keyed union the task file suggested. | `PendingNoteState`'s `kind` and `terminator` are genuinely independent (three kinds × several terminators); here they would be strictly 1:1, so a second tag is duplicated state two writers can disagree about. The three old fields are read ONLY by `parser.ts` (grep-verified across `src/` and `tests/`) and by `startNewPage`, so nothing outside this task's write-set sees the change — the F2-a contract (do not disturb `resolveStillUnknown`, `nodesById`, `qualifiedNodesById`) is untouched. | No |
| 2026-08-07 | F1-b | **TYPE1's colour is still dropped** (upstream `executeNow` calls `result.setColors(colors)` for both phases; this task wires colour on TYPE0 only). Filed as a follow-up, not fixed. | Colour is size-neutral, so it cannot be verified by the size ratchet — its only observable is SVG, and `titona-45-jile471`-style `node B #red\|green;line.dashed [` fixtures would change SVG output with no golden to validate against and a standing prohibition on regenerating goldens (stop condition 7). TYPE0's colour, by contrast, is explicitly required by the task (`ColorParser.exp1()` sits before `as`) and was jar-verified size-neutral by the `multiline-display` finding's own controlled experiment, which included `#red`. | Yes — a real fidelity gap, deliberately deferred. |
| 2026-08-07 | F1-b | **Pre-existing divergence found, not fixed:** `parseDescription`'s top-level loop skips blank lines (`parser.ts`, `if (lines[i]!.trim() === '') { i++; continue; }`) BEFORE any block phase runs, so a blank line inside a TYPE0/TYPE1 body — which upstream keeps as a real display row via `subExtract` — can never reach the block accumulator at all. Both phases therefore drop blank body rows. | Changing that skip is upstream of note accumulation as well as element bodies (notes push through the same loop), i.e. exactly the disproportionate blast radius G8's finding warns about, and no fixture in the 351 exercises it. Recorded so it is findable rather than rediscovered. | Yes — follow-up. |
| 2026-08-07 | F1-b | The TYPE1 opener's inline body now comes from a captured regex group (`\[([^\[]*)$`) instead of `line.slice(line.indexOf('[') + 1)`. | A 1-line correctness fix in code this task rewrites anyway (pr-workflow.md "pre-existing violations"): with a URL decoration, `component c [[url]] [` made `indexOf('[')` find the URL's bracket and pushed `[url]] ` into the display. The regex already guarantees the body `[` is the last one on the line, so the captured tail is exact. Full 351 sweep re-measured: widened 0. | No |
| 2026-08-07 | F1-b | **Pins reported closed to the orchestrator (ADR-1, not written here):** `pecupa-75-zote612`, `tajadu-40-juro990`, `nixura-77-bina738`, `vixeni-34-nici683` — all four at `maxSizeDeltaIn` 0 under BOTH the gate metric and `audit-size-metric-identity.ts`'s exact bottleneck matching. `fariba-82-xolu802` is explicitly NOT closed: 0.388889 → 0.194444in, exactly the 14px E1 stereotype row, leaving E2's 14px (F4-c) plus the undiagnosed `sh0006` +2px residual. | METRIC-AUDIT's corrected targets were used, not the stale pins (`nixura-77` true 1.5403 not 1.2731; `vixeni-34` true 0.2222 not 0.2033). A pairMax of 0 is a constructive proof of an exact node-preserving bijection, which is the acceptance bar the corrected targets set. | No |
| 2026-08-07 | F1-a | **Reused the exported `buildDesc` (`EntityImageDescriptionDelegates.ts`) as the note body's `create3` call site** instead of reconstructing `buildLocalSkinSimple`/`descAtomOps` locally (the `leaf-sizing-folder-title.ts` precedent for `create2`). Two adaptations, both reported in `buildNoteBody`'s doc comment: `symbol` is passed `USymbols.RECTANGLE` as an INERT argument (`buildDesc` reads it only for `getSNames()[0] === 'package_'`, and a note has no `USymbol` upstream at all), and `codeName === displayText` selects the `fontTitle` slot. | Reconstructing the wiring would have meant TWO copies (sizer + renderer) of the `ISkinSimple`/`AtomOps`/`Display`/`LineBreakStrategy` construction — precisely the parallel-model drift this group exists to end — or a new shared file outside the declared write-set. `buildDesc` is fully parameterized, already jar-verified, and gives sizer and renderer ONE `create3` construction by force. | No |
| 2026-08-07 | F1-a | **Narrowing recorded, not silent:** `buildDesc`'s blank-display branch tests `displayText.trim().length === 0`; `EntityImageNote.java:114` tests `strings.size() == 1 && strings.get(0).length() == 0`. They agree on the reachable case (empty note body → 0×0 block → 21×10 box) and differ only for a body that is entirely WHITESPACE across one or more lines, which upstream measures as real blank text lines. | No fixture in the 351 exercises that shape; the alternative was duplicating the whole `create3` wiring to change one predicate. Documented on `buildNoteBody` so it is findable. | Yes — a real (unreachable-today) fidelity gap. |
| 2026-08-07 | F1-a | **`measureNote` now receives `opts.wrapWidth`/`opts.guillemet` as well as `opts.fontSize`.** ADR-4 only required the font size; the other two ride the same parameter and close `sizer-renderer-parity.md`'s jar-proven `wrapWidth`/`guillemet` GAP rows for the `measureNote` path (upstream passes `style.wrapWidth()` straight into `create3`, `EntityImageNote.java:117`, and the RENDERER already applied both). | Withholding them would have re-created the sizer/renderer split on the very function this task exists to unify. Full 351 sweep re-measured after: widened 0. | No |
| 2026-08-07 | F1-a | **Renderer stencil now built from the NODE box, not the block dimension.** Was `UGraphicStencil.create(ug.apply(translate), blockDim)`; now `UGraphicStencil.create(ug, node box).apply(translate)`, mirroring `EntityImageNote.java:207`'s `UGraphicStencil.create(ug, this, …)` which stencils the whole note BEFORE `drawNormal` applies the margin translate. | A block-sized stencil clipped every separator rule to the text width; the jar draws them across the full note (`x1="7" x2="90.4313"` on a 6..91.43 box). SVG diff-baseline ratchet: no rise anywhere, `xufexu-38-fola855` fell 3 → 1. | No |
| 2026-08-07 | F1-a | **Discrepancy diagnosed and ruled OUT of G2 (push-forward 2):** a note's last drawn baseline overhangs its (correctly measured) box. Mechanism: the creole DRAW path advances each line by `fontSize × 1.17773` while `calculateDimension` bills `fontSize` exactly. Origin is the shared `Sea`/`SheetBlock` draw-vs-measure seam reached from `descAtomOps`, NOT the note path — reproduced identically on the ENTITY `desc` route (`rectangle R [ aa/bb/cc ]` steps 16.4883px at font 14 = 14 × 1.17773 vs the jar's 14; a note steps 15.3105px at font 13 = 13 × 1.17773 vs the jar's 13), which has been on `BodyFactory.create3` since `bodyenhanced-atom-seams` T4 and is untouched here. **Ruled out:** the note block dimension (jar-exact on all 6 fixtures), the 6/15/5 note margins (jar-exact), `decorate`'s separator geometry (jar-exact), the note font size (jar-exact at 13 and at the `FontSize 10` override), and anything F1-a introduced (the ratio reproduces with no note involved). F1-a strictly REDUCED the overhang on `xufexu-38-fola855` note 1, 28.15px → 2.12px. Filed, not fixed. | Fixing a shared creole line-advance constant is a different mechanism, a different owner and a cross-engine blast radius; G2's own bar (jar-exact DOT boxes) is met. The co-located test asserts the renderer's block model structurally (5 text runs + 8 rules — the jar's own counts — plus the first baseline's position) rather than strict containment, so it neither hides the residual nor bakes it in as an expectation. | Yes — filed for the `Sea`/`SheetBlock` seam owner. |
| 2026-08-07 | F1-a | **Renderer note font fallback changed from `theme.fontSize` (14) to `NOTE_FONT_SIZE` (13).** `textFont(theme,'note')` defaulted to the diagram font, so notes DREW at 14 while the sizer MEASURED at 13 — the renderer half of cause C3, a standing 1px-per-line box/ink disagreement. | Jar-refuted directly: `test-results/dot-cache/component/xufexu-38-fola855/in.svg` emits `font-size="13"` on every note line and `font-size="14"` on the use-case label beside it. `resolveElementFontSize(theme,'note','title')` is the same call `ClassifyCtx.fontSizeFor` makes into `BoxSizingOpts.fontSize`, so an override now moves box and ink together. | No |
| 2026-08-07 | F1-a | **Pins reported closed to the orchestrator (ADR-1, not written here):** `xufexu-38-fola855`, `pivudu-29-pele178`, `tijexo-10-zipo222`, `kovaxi-11-reti348`, `zidebi-71-nocu387` — all five at `maxSizeDeltaIn` 0 under BOTH the gate metric and `audit-size-metric-identity.ts`'s exact bottleneck matching (`pairMax` 0). `nobiza-91-fimo741` is explicitly NOT closed: its C4 note residual IS closed (note node now 695.625×37, jar-exact) but the fixture still measures 0.27691 — `sortedMax == pairMax == 0.27691, hidden 0`, i.e. the entire remaining error is the `First` node's G3 stereotype-as-sprite delta, due in F2-b. | METRIC-AUDIT's corrected targets were used, not the stale pins (`kovaxi-11`/`zidebi-71` true 0.9470, not the pinned 0.7720). ADR-3's independent verification of C1/C2/C3/C4 was done per cause, not inferred from the fixture count. | No |
| 2026-08-07 | orchestrator | **Batch 1 gates green on the combined tree**, run unpiped with `$?` captured per gate: `npm test` 0 (547 files / 12305 tests), `typecheck` 0, `lint` 0, `build` 0, `measure-description-size-deltas` 0, `audit-size-metric-identity` 0. Result **321 → 330/351 (94.0%), widened 0, falseConformant 0**, understated 8 → 3. | Each agent's own run saw the other two mid-flight (F1-b observed 5 test failures and 3 lint errors that were F1-c's in-progress edits). Only the orchestrator's post-batch run on the settled tree is authoritative, which is why it is re-run here rather than inferred from the three completion summaries. | No |
| 2026-08-07 | orchestrator | **Deleted 9 closed pins from `oracle/goldens/description/size-backlog.json`** (ADR-1 — the orchestrator's write, not the tasks'): `xufexu-38-fola855`, `pivudu-29-pele178`, `tijexo-10-zipo222`, `kovaxi-11-reti348`, `zidebi-71-nocu387` (F1-a); `pecupa-75-zote612`, `tajadu-40-juro990`, `nixura-77-bina738`, `vixeni-34-nici683` (F1-b). Retained `nobiza-91-fimo741` (0.27691), `fariba-82-xolu802` (0.388889) and `vivido-49-nisu863` (0.157407) — all three reported explicitly NOT closed. | Deleting a pin tightens the ratchet, so the four engine ratchet tests and the description sweep were **re-run after** the deletion, not before: 1418/1418 pass, 330/351, widened 0. Gates run before the deletion would not have exercised the tightened bar. | No |
| 2026-08-07 | orchestrator | The three retained pins still carry their **pre-batch** values even though two of them measurably shrank (`fariba-82` 0.388889 → 0.194444, `vivido-49` 0.157407 → 0.051282); they now report `improved`, not `unchanged`. | ADR-8 forbids re-basing pins this mission. A pin only shrinks by being deleted when its fixture closes, so leaving the stale-but-loose value is correct and is not a missed ratchet tightening. | No |
| 2026-08-07 | F2-b | **Task-file/SYNTHESIS mechanism corrected (push-forward 2):** G7's fix is NOT "swap in `Stereotype#getMultipleLabels`'s `<<\s?((?:<&\w+>\|[^<>])+?)\s?>>` pattern". `EntityImageDescription` never calls `getMultipleLabels`; its label list comes from `portionShower.getVisibleStereotypeLabels` (`java:193`) → `CucaDiagram#getVisibleStereotypeLabels` (`net/atmp/CucaDiagram.java:590-602`) → `Stereotype#getLabels(DOUBLE_COMPARATOR)` → `StereotypeDecoration#cutLabels` (`java:185-196`), whose pattern is `\<{2,3}.*?\>{2,3}` — it does NOT exclude `<`/`>`; it DROPS any run that opens with three brackets. `<<<U+00B5>Service>>` closes because it is a `<<<...>>>` run, not because angle brackets are excluded. Implemented as `Stereotype.build(run) → getLabels(GUILLEMET_NONE)`. | The distinction is observable and would have been a fresh divergence: on `<<a<b>>` the proposed `getMultipleLabels` regex yields `[]`, while upstream's real description path yields `a<b`. Both routes close `junoxu-15`; only one matches the jar on the neighbouring case. `net/atmp/` was reached by grepping `src/main/java/net/`, per CLAUDE.md's own warning. | No |
| 2026-08-07 | F2-b | **ADR-6 verified by measurement, not inferred:** routing through `buildComplex` DOES subsume G7. `junoxu-15-gori632` 0.247743 → **0** (`audit-size-metric-identity`: `sortedMax = pairMax = hidden = 0`) with no G7-specific code. No explicit second fix was needed. | ADR-6 required this be measured against `junoxu-15` directly rather than deduced from the regex change. | No |
| 2026-08-07 | F2-b | **`DescriptiveNode.stereotype`'s "never an empty array" invariant is retired**; an empty array now means "a `<<...>>` run was consumed but contributed no visible label" (upstream's own outcome for `<<<...>>>` and for a sprite-only `<<$name>>`). Doc comment rewritten in `ast.ts`; `absent` still means "no run at all". | Preserving the old invariant would have required editing `parse-helpers.ts`, `parser.ts` and `element-grammar.ts` (F2-a's file) to translate `[]` → `undefined` — three files outside the write-set, one of them contended. Every existing consumer already guards on `length > 0` or is set-membership (`includes`), verified by grep; full suite green. | No |
| 2026-08-07 | F2-b | **G3-M1's `getSprite()` branch NOT implemented — write-set overrun, escalated (stop condition 1).** The empty-label half of G3-M1 landed (it lives entirely in `extractNodeStereotype`) and closes `nobiza-91-fimo741` 0.27691 → 0. The sprite-REPLACES-the-block half cannot: `buildStereo`'s only caller is `EntityImageDescription.ts:294`, its input contract is `EntityImageDescriptionLabels` (same file, `:159`), and the two producers are `leaf-sizing-entity.ts:190` (in write-set) and `renderer-entity.ts:198` (**not**). Carrying `spriteName` from the parser also needs `parse-helpers.ts`, `parser.ts` and `element-grammar.ts` — the last owned by F2-a this batch. Measured: 4–5 files outside the write-set, one contended. | Threading the sprite name into the sizer alone would create exactly the sizer/renderer split `planning/sizer-renderer-parity.md` exists to prevent, and adding a param `buildStereo` no caller can supply is dead code. Nothing in the 351 regresses by deferring: all four dependent fixtures (`turasu-73`, `lesori-32`, `ravodu-50`, `tuliba-37`) need F4-a's asset bundle before the branch has any observable effect, and all four IMPROVED here anyway (1.2248→0.2101, 0.2429→0.0694 ×2, 0.5210→0.2639). | **Yes — F4-a's stated consumer does not exist yet. Either widen a follow-up task's write-set to `EntityImageDescription.ts` + `renderer-entity.ts` + the three parser files, or fold it into F4-a.** |
| 2026-08-07 | F2-b | Corrected one existing assertion in `tests/unit/description/parser.test.ts` (`'resolves a <U+XXXX> … in a stereotype -- junoxu-15-gori632'`, was `toEqual(['µService'])`) — a test file F2-a was concurrently appending to. | The test codified the G7 defect itself; leaving it would have failed the batch gate. The edit is a surgical 3-line replacement ~480 lines above F2-a's additions, in a file neither task's declared write-set claims (both authored tests in it). Full suite re-run green afterwards. | Yes — co-authored test file, flagged for merge awareness. |
| 2026-08-07 | F2-a | **Recorded mechanism corrected (push-forward 2): the bare `User << Human >>` leaf is NOT `STILL_UNKNOWN`.** `element-font.md`'s gogamo-72 record, the task file's Interface Contract and acceptance criteria 1-2 all specify emitting a `stillUnknown` leaf for `resolveStillUnknown` to mute to actor-or-interface. Jar-refuted: `@startuml / User << Human >> / @enduml` (no other leaf at all) renders a STICKMAN, not the INTERFACE circle a muted still-unknown leaf would get. `CommandCreateElementFull.executeArg` never produces STILL_UNKNOWN — with SYMBOL omitted and a bare CODE, `symbol == null` and java:271-274 assigns `LeafType.DESCRIPTION` + `actorStyle().toUSymbol()` DIRECTLY; STILL_UNKNOWN is `CommandLinkElement.getDummy`'s business alone. Implemented as a plain `'actor'` emission, identical to rule 15's existing quoted-form handling. Acceptance criterion 2's expected `'interface'` is therefore wrong and its test asserts `'actor'` + `stillUnknown === undefined` instead. | The two differ observably the moment the diagram has no usecase/actor leaf — exactly criterion 2's case — so following the brief would have shipped a fresh divergence on the one input it was written to protect. Side effect: `parse-state.ts` needed no change at all, so F2-a's write-set shrank rather than grew. | No |
| 2026-08-07 | F2-a | **New rule ordered in `command-table-shorthand.ts` (rule 8c), NOT after rule 11b in `command-table-containers.ts` as the task file directs.** Rule 10's `^\[([^\]]+)\](.*)?$` trailer is greedy and its `RE_BRACKET_ALIAS` alias stops at the first space, so `[Comp] as "Big component"` is consumed and mis-parsed (id `"Big`, display `Comp`) before ANY containers-table rule after it can run — an after-11b placement cannot satisfy acceptance criterion 5. Verified the earlier slot steals nothing: rules 5-8b all require a `:`/`(`/`()` PREFIX and reject a quoted tail; rule 9 (links) has no bare `as` in that position; rule 11's own reachable case (`(Use) as "X"`, no space in the quoted RHS) already resolves to the SAME id/display through `parseNameSection`'s `RE_ID_AS_DQ`, so nothing existing changes result. One rule, not the two the task file proposed — the shorthand slot covers every CODE_CORE notation, so a second containers-table copy would be duplicate logic. | The task file's ordering instruction was aimed at not intercepting a QUOTED LHS (`"a" as (uc4)`, rule 11b); this pattern's CODE alternation has no quoted branch (upstream's `CODE_CORE` has none either — `CODE_WITH_QUOTE` adds it, and only for CODE1), so the hazard is structurally excluded regardless of position. Five ordering-guard tests pin rules 10/11/11b/15 plus `"a" as "b"` matching nothing. | Yes — deviates from the task file's literal ordering instruction. |
| 2026-08-07 | F2-a | **Split `element-grammar.ts` into `element-grammar.ts` + a new `element-grammar-nosymbol.ts`** — one file beyond the declared write-set. The keyword-LESS declaration family (`RE_BARE_AS_DECORATED`/`parseBareAsDecorated`, `RE_BARE_QUOTED_DECL`, and the two new patterns) moved verbatim; `element-grammar.ts` keeps the bracket shorthand and the `remove`/`hide`/`show` helpers. | `element-grammar.ts` was at 478 lines with a hard 500-line `check-complexity.py` hook, which BLOCKED the edit at 605 lines. Same forced split, same repo precedent as F1-c's `openiconic-glyphs-data.ts` (also `command-table.ts` → `command-table-*.ts`, `parse-helpers.ts` → `parse-helpers-strings.ts`). The four moved symbols have exactly two importers, both F2-a's own command-table files (grep-verified across `src/` and `tests/`), so the new file collides with no concurrent agent and changes no behavior. | Yes — outside the literal write-set, though with zero write conflict. |
| 2026-08-07 | F2-a | **Three fixtures authored + jar oracles generated (ADR-7):** `alias-quoted-display-bare-0` (`Admin as "Main Admin"`), `alias-quoted-display-colon-0` (`:A: as "Actor name"`), `alias-quoted-display-bracket-0` (`[Comp] as "Big component"`) under `oracle/goldens/description/`, each with a link to a `(Use)` ellipse so graphviz actually runs and a `svek-1.dot` is dumped. Generated one-fixture-per-dump-dir with `-DPLANTUML_DETERMINISTIC_TEXT=true`. All three pass the description ratchet (structural EQUAL + size-conformant) on first run. Corpus 351 → 354. | The three shapes were found during diagnosis with no golden covering them; the bracket one MIS-parsed rather than merely no-matching, which a synthetic-only check would have under-tested (it produced a plausible node, just the wrong one). | No |
| 2026-08-07 | F2-a | **Pins reported closed to the orchestrator (ADR-1, not written here):** `gogamo-72-pibo470` and `dopova-50-digo290`, both at `maxSizeDeltaIn` 0 under the gate metric AND `pairMax` 0 / `unambiguous: true` under `audit-size-metric-identity.ts`'s exact bottleneck matching. | METRIC-AUDIT's corrected targets were the bar, not the stale pins (`gogamo-72` true 0.4604 vs pinned 0.2148 — more than 2×; `dopova-50` true 0.9371 vs pinned 0.8827). A unique sub-tolerance bijection at pairMax 0 is a constructive proof that every node matches, which is what the corrected targets demand. | No |
| 2026-08-07 | F2-c | **Recorded write-set narrowed by measurement (push-forward 2).** `sprite.md`'s `proposedWriteSet` for M3 assumed the url flag would have to be honoured in `creole-atoms.ts#scanLineForAtoms` as well as on the `StripeSimple` token. Instrumented `spriteScale` with a stack trace on the `bivira-53` shape: the ONLY sizing and rendering call sites reached are `leaf-sizing-entity.ts:80` (`sizingAtomImageResolverFor`) and `render-atoms.ts:274/279` (`resolveSpriteAtom`), both fed from `Sea.add`/`SheetBlock1.initMap` — i.e. exclusively from `buildLineAtoms`'s token stream. `scanLineForAtoms` is not on the leaf path at all, so it was left untouched and no url grammar is duplicated into `creole-atoms.ts`. | The alternative (a second url-span scanner inside `scanLineForAtoms`) would have duplicated `CommandCreoleUrl`'s grammar in a file shared by four engines to serve a path that measurement shows is never taken. `measureLineWithAtoms`/`lineAtomHeightExcess` DO still use the raw scan, but their only caller is `link-edge-attrs.ts`, which passes text `resolveInlineLinks` has already stripped of `[[…]]` markup — the provenance is gone before creole sees it, a different mechanism (same family as F4-d's). | No |
| 2026-08-07 | F2-c | **ADR-7 fixture authored but staged OUTSIDE the ratchet's scan path:** `oracle/goldens/pending/f4d-url-label-first-line/{input.puml,svek-1.dot}` (+ a `pending/README.md` stating the convention), not `oracle/goldens/description/<slug>/`. Jar oracle generated with the mandatory `-DPLANTUML_DETERMINISTIC_TEXT=true`, one fixture per dump dir. Four nodes, all four jar values reproduced: `0.591319` vs ours `2.663368`; `2.242882` vs `2.349826`; `0.944444 x 0.944444` vs `3.067775 x 0.995726`; and `1.316840 x 1.138889` **exact** (the F2-c regression guard). | Landing it as a real golden was a direct conflict: a new slug with no `size-backlog.json` pin classifies as `widened`, which is stop condition 4/5, and ADR-1 forbids a task from adding the pin itself. F4-d (batch 4) already owns this defect's fix, fixture and pin, so staging preserves ADR-7's "oracle lands with the task that authored it" without manufacturing a stop condition. Every ratchet joins to its own exact engine directory (`measure-*-size-deltas.ts`, `tests/oracle/*.ratchet.test.ts` — grep-verified), so `goldens/pending/` is invisible to all of them. | Yes — F4-d must promote the directory and add its pin. |
| 2026-08-07 | F2-c | **New defect found and reported, not fixed (outside write-set):** `CommandCreoleUrl.ts#resolveLabel`/`resolveUrlAndTooltip` strip `{…}` GLOBALLY from a `[[…]]`'s inner text to remove an optional tooltip, so a sprite's own `{scale=N}` block inside the LABEL is eaten — `[[u <$s{scale=0.31}>]]` yields `scale: 1` where `<$s{scale=0.31}>` outside a link yields `0.31`. Upstream's tooltip is POSITIONAL (`UrlBuilder.java:76-79`: link, then `\{([^{}]*)\}` immediately after it, then a label whose first char may not be a brace but whose remainder may), so upstream keeps the label's braces. Recorded as an `it.todo` in `tests/unit/creole-url-sprite-scale.test.ts` rather than pinned at its wrong value. | Found while writing F2-c's own provenance test. `CommandCreoleUrl.ts` is not in this task's write-set and the `*N` scale form (`<$s*0.31>`, which `vivido-49-nisu863` node 0 uses) is unaffected, so no measured fixture regresses by deferring. Pinning the current value in a test would have converted a bug into an expectation. | Yes — needs an owner. |
| 2026-08-07 | F2-c | Trimmed 6 lines of stale doc from `creole-atoms.ts` to fit the new `SpriteAtomToken.insideUrl` field under the enforced 500-line file cap: the file was at exactly 500. The trimmed text is T4's own "FLAG for orchestrator reconciliation: confirm the registry's `get()` return type is structurally assignable here once T4 lands" paragraph, whose condition is verifiably satisfied — `sprite-commands.ts#spriteDimsLookupFor` returns `SpriteDims` directly. Its live content (what the type is, why only width/height) is preserved verbatim; only the resolved-FLAG sentence and the "had not landed when this file was written" aside were dropped. | The alternative was F1-c's data-file split precedent, i.e. a NEW `src/` module outside the write-set for a one-field addition — disproportionate. `pr-workflow.md` sanctions a 1–3-line fix of a pre-existing violation in a file you are already editing; a self-declared-resolved TODO is that. No behaviour, no exported symbol and no `@see` reference was touched. | No |
| 2026-08-07 | F2-c | **Write-set overrun identified and NOT taken (stop condition 1) — one-line follow-up in the CLASS engine.** `src/diagrams/class/class-member-atom-resolve.ts:75` still calls `spriteScale(atom.scale, baseFont.size)` to scale the rasterized sprite PNG, while `:70`'s `measureInlineAtom` on the same atom now honours `insideUrl`. For a `<$sprite>` inside an inline `[[url …]]` in a class MEMBER (reachable — `renderer-classifier-box.ts#renderRowAtoms` drives `analyzeAndAddInlineWithUrl`, G2 N40), the two disagree. Change needed: `spriteScale(atom.scale, baseFont.size)` → `spriteAtomScale(atom, baseFont.size)`, one line plus the import. | The file is in no task's write-set this batch and the orchestrator's rule is explicit: report rather than expand. The inconsistency is bounded — the returned `width`/`height` come from `dims`, so DOT/SVG geometry is correct on both sides; only the embedded PNG's raster resolution is off, i.e. a resampling-quality artifact, not a size drift. Class ratchet re-measured after the change: 711/712, widened 0, improved 0, and the full suite is green, so nothing observed regresses. | **Yes — 1-line follow-up, `class-member-atom-resolve.ts:75`.** |
| 2026-08-07 | orchestrator | **Batch 2 gates green, verified on a COLD tree.** Deleted `packages/{stdlib,stdlib-aws,stdlib-tupadr3}/assets` and ran `npm test` twice: both exit 0, 549 files / 12363 tests. `typecheck` 0, `lint` 0, `build` 0. Description **330 → 339/354, widened 0**; audit `agree 354, falseConformant 0, understated 0, maxHidden 0`; class 711/712 widened 0; state 148/148 widened 0. | F2-c's own run hit `tests/unit/split-sprite-bundle.test.ts` seeing 190 of 2078 sprite fragments — a warm-tree `packages/*/assets` rebuild race between three concurrent agents, and F2-b explicitly deferred the destructive cold-tree run to the orchestrator. A warm pass would not have distinguished "race" from "real breakage", so the assets were deleted and the suite run twice. | No |
| 2026-08-07 | orchestrator | **The corpus denominator moved 351 → 354.** F2-a authored three new fixtures with jar oracles under ADR-7 (`alias-quoted-display-{bare,colon,bracket}-0`), all conformant on first measurement. The mission's headline arithmetic (321/351 → 347/351) was written against 351. | ADR-7 explicitly approves authoring fixtures + oracles, and CLAUDE.md calls the corpus a starting point rather than a ceiling, so growth is the intended behaviour, not scope creep. Recorded so the final count is not misread as a shortfall: batch 2's +6 closures plus 3 new conformant fixtures is 330/351 → 339/354. | No |
| 2026-08-07 | orchestrator | **Deleted 6 closed pins** (ADR-1): `gogamo-72-pibo470`, `dopova-50-digo290` (F2-a); `junoxu-15-gori632`, `nobiza-91-fimo741` (F2-b); `bivira-53-boja685`, `vivido-49-nisu863` (F2-c). 15 pins remain. Ratchets re-run AFTER deletion: 1421/1421 pass, widened 0. | Same discipline as batch 1 — deletion tightens the bar, so the gate must follow the deletion. | No |
| 2026-08-07 | orchestrator | **Carried into Batch 4 planning: F2-b's `buildStereo` sprite-replacement escalation.** F2-b closed G3-M1's label half but could not reach the `getSprite()` branch — its input contract lives in `EntityImageDescription.ts` and a second producer in `renderer-entity.ts`, and carrying `spriteName` from the parser also needs `parse-helpers.ts`, `parser.ts` and `element-grammar.ts` (F2-a's file that batch). Measured at ~6 files / ~40 lines, no algorithmic risk. Four fixtures now sit with a residual that is purely the missing 19px sprite block: `turasu-73` 1.2248→0.2101, `lesori-32` and `ravodu-50` 0.2429→0.0694, `tuliba-37` 0.5210→0.2639. | Stop condition 1 raised correctly rather than half-threading the sizer alone, which would have created exactly the split `sizer-renderer-parity.md` forbids. F4-a vendors the sprite assets but has no consumer for them, so this threading is what makes those four closable — it is in scope per CLAUDE.md ("bounded but in another file is still in scope"), not a deferral. | Yes — scope decision due at Batch 4. |
| 2026-08-07 | orchestrator | **Carried into Batch 4: F2-c staged its ADR-7 fixture OUTSIDE the ratchet scan path**, at `oracle/goldens/pending/f4d-url-label-first-line/`, with a README explaining why. F4-d must promote the directory into `oracle/goldens/description/` and add its pin. | Landing a reproduced-but-unfixed defect as a live golden would classify `widened` for want of a `size-backlog.json` pin that ADR-1 forbids a task from writing — a manufactured stop condition. Staging keeps the jar oracle (generated correctly, with `-DPLANTUML_DETERMINISTIC_TEXT=true`) without tripping a gate. Grep-verified every ratchet joins its own engine dir, so `goldens/pending/` is invisible to all four. | Yes — F4-d must promote it. |
| 2026-08-07 | orchestrator | **F3-lic dispatched to a fully-tooled agent, not the `legal-advisor` agent the batch table names.** | `legal-advisor` carries only Read/Write/Glob. F3-lic's own acceptance criteria require checking `~/git/plantuml`'s commit history for the sprite set's introduction (needs read-only `git log`) and researching whether The Open Group or the Archi tool publishes the icons under an open licence, plus ArchiMate trademark/redistribution terms (needs web research). Neither is reachable with Glob alone, so the named agent could not have satisfied the task as written — it would have had to default every set to `provenance unknown` for want of tools rather than for want of evidence, which is precisely the wrong reason to reach that verdict on a blocking gate. Agent choice is a planning suggestion, not an ADR, so this is push-forward rather than a stop. | Yes — brief's agent assignment diverged. |
| 2026-08-07 | F3-lic | **VERDICT: `archimate/` (139 files) is MIT-compatible — F4-a is cleared, stop condition 10 NOT triggered, and README's "−4 (→343)" risk row does not fire.** All five dependent fixtures (`lesori-32-zeve057`, `ravodu-50-siso430`, `tuliba-37-liza126`, `kofuca-08-pafi749`, `turasu-73-zoni468`) stay in scope; no documented gap on licence grounds. | Copyright chain closed on primary sources on BOTH halves of the set, which turned out to have different provenance (see next row). PNGs: derived from the Archi modelling tool, imported 2016-01-09; Archi's root `License.txt` postdates the import by 5 months, so the agent checked the last Archi commit BEFORE the import date and found the owning plugin `com.archimatetool.editor` already carried its own MIT licence — clean at time of taking, not just today. SVGs: original contributor drawings, no CLA/DCO in `CONTRIBUTING.md` so inbound=outbound; and upstream's `plantuml-mit` subproject demonstrably ships `src/main/resources` (`build.gradle.kts` srcDir wiring), so the MIT jar and `@plantuml/core` npm package already redistribute these exact bytes under MIT. | No |
| 2026-08-07 | F3-lic | **Task-file framing corrected:** the set is not one artifact. It is **116 `.svg` + 23 `.png` with entirely different provenance** — the task file's "a mix of `.svg` and `.png`" hides the split. Auditing it as a single corpus would have covered only half. | The two halves needed independent chains: the PNGs trace to a 2016 bulk import from a third-party tool, the SVGs to 2025 contributor-original work. A single verdict derived from either half alone would have been unsupported for the other. | No |
| 2026-08-07 | F3-lic | **Trademark binds independently of copyright but does NOT block vendoring.** ArchiMate® is a registered Open Group mark restricting use of the *mark* with products/services and imposing a notice obligation; it says nothing about redistributing artwork. Spec-*document* redistribution is prohibited but not engaged — no document text is shipped. | Strongest primary signal: The Open Group is itself a named copyright holder on Archi's MIT licence, i.e. the mark owner grants MIT over a codebase of ArchiMate icons. The "fair use for implementers" spec sentence was rated MEDIUM only (`pubs.opengroup.org` now redirects to an SSO gate, which the agent declined to authenticate through) and is deliberately not load-bearing in the verdict. | No |
| 2026-08-07 | F3-lic | **Five obligations transferred to F4-a — the verdict is conditional, and vendoring bare would turn a clean ruling into a real violation.** (1) carry BOTH MIT notices (Archi lineage + PlantUML lineage); (2) reproduce upstream's own Archi attribution line; (3) carry the ArchiMate trademark notice; (4) vendor **byte-verbatim** — re-drawing forfeits the provenance chain (SI5b D3); (5) pin upstream SHA `de1f986f092`. | ADR-9(a) makes the review blocking precisely because the decision it informs is not cheaply reversible. A conditional clearance whose conditions are not carried into the acting task is equivalent to no review at all. | Yes — F4-a must satisfy all five. |
| 2026-08-07 | F3-lic | The task's stated quality bar (`git diff --name-only` shows no `src/` path) was **not literally satisfiable** in a shared worktree; F3-lic reported that honestly rather than claiming a clean tree. Its own footprint is exactly one file, `plans/s1l-tail-fix/findings/sprite-licence-review.md`; the `src/` paths in the diff belong to concurrent F3-fix and F3-seam. No `assets/**` path, no sprite bytes staged. | The bar was written as though the task ran alone. Verified independently by the orchestrator at batch close via per-file attribution rather than a whole-tree check. | No |
| 2026-08-07 | F3-diag | **VERDICT: `resolved` (push-forward 4 not engaged).** `fariba-82`'s `sh0006` +2px is a stereotype-font-size defect, not a sprite defect: `awslib/AWSCommon.puml:35-37` sets `skinparam rectangle { StereotypeFontSize 12 }`; the jar measures the `«User»` row at 12pt, we measure at 14pt. Jar 20+12+92 = 124 = 1.722222in; ours 20+**14**+92 = 126 = 1.750000in. One row, so width-invariant — which matches T1's original observation. | Two independent defects each force the 14, and BOTH must be fixed. M2 is G4, already in flight as F3-fix (`leaf-sizing-entity.ts:146`, `fontStereo: font` with `ClassifyCtx.fontSizeFor` resolving role `'title'` only). M1 is new — see next row. | No |
| 2026-08-07 | F3-diag | **T1's own recorded `nextStep` was WRONG and is corrected.** The residual is not the `$User [64x64/16z]` sprite + label stack. Disproved by construction: a sprite-free, img-free repro (`skinparam rectangle { StereotypeFontSize 12 }` + `rectangle "Trusted user" <<User>>`) reproduces the same +2px — jar 0.638889in vs ours 0.666667in — with no sprite present at all. Dropping the skinparam collapses the delta to 0. | This is the fifth-plus time a recorded mechanism has proven wrong this mission (push-forward 2). Recording it because the sprite hypothesis is the one F4-c would otherwise have inherited, and it would have sent F4-c into the awslib sprite stack for a defect that lives in the theme layer. | No |
| 2026-08-07 | F3-diag | **NEW BLOCKER — M1 is outside EVERY task's write-set in this mission.** Origin `src/core/style-map-theme.ts:141` (`buildStyleMapPartialTheme`'s `...(extras.hasElements ? { elements: extras.elements } : {})`; `deepMergeTheme`'s `colors` spread at `theme.ts:428-430` is shallow, so this REPLACES rather than merges). Any element-scoped `<style>` selector **deletes** the skinparam-derived `rectangle` bucket before the sizer can read it. Upstream cannot do this — `StyleBuilder.java:86-104` merges per-signature (`OVERWRITE_EXISTING_VALUE`), so a `file` signature cannot evict `rectangle`. Bisection pinned the trigger exactly: skinparam alone → bucket present; `+ wrapWidth` → present; `+ skinparam rectangle<<stereo>> {…}` → present; `+ <style> file {…}` → **gone**. | Batch 3's write-set lists `style-map-element.ts`, a DIFFERENT file — `style-map-theme.ts` is confirmed untouched by `git diff`. Consequence: after F3-fix (M2) + F4-c (E2), `fariba-82` still measures **0.027778in** and does not close. Stop condition 1 applies; the mission lands at 346 unless M1 is scheduled. Blast radius is cross-engine, and 4 further description goldens match the trigger shape by literal grep (`cusubu-18`, `kagapo-72`, `revusu-28`, `vajaxu-62`) — an undercount, since `fariba-82`'s own skinparams arrive via `!include` and so do not grep. | Yes — mission-scope decision. |
| 2026-08-07 | F3-diag | Guidance to F4-c: **proceed** (E2 is correctly diagnosed and worth 0.194444in) but **do not book the +1 and do not delete `fariba-82`'s pin.** | An honest partial is the correct outcome here; booking the fixture as closed on the strength of E2 alone would overstate the mission's result by exactly the amount M1 still costs. | No |
| 2026-08-07 | F3-seam | ADR-2 seam built and additive: `RenderOptions.assetStore?: AssetStore`, with `AssetPayload { bytes, mimeType }` and `combineAssetStores(...)` (first-match-wins, mirroring `withStdlib`'s composition). Sync-only by design — mirrors `includeStore`, not `stdlibRegistry`. No key scheme baked in; F4-a and F4-b each layer their own. | Stop condition 8 (default bundle must not grow) held, but the raw before/after was contaminated by F3-fix's concurrent uncommitted edits in the shared worktree. Rather than report a misleading delta, F3-seam isolated its own contribution: the interfaces are fully type-erased under `verbatimModuleSyntax`, leaving one ~230-byte function with zero new imports, confirmed in the built output. Orchestrator re-verifies cleanly at batch close. | No |
| 2026-08-07 | F3-fix | **ADR-5's premise CONFIRMED by controlled measurement, and it is worse than recorded.** With tier 2 disabled behind a temporary probe flag (removed before commit), tier 1 alone measures `loroto-06`/`toxine-81` `nodefoo` **92×64 = exact** but `nodebar` **93.125×64** against the jar's **91.363×54** — a +10px height error (was +4px) *and* a new +1.762px width error the ADR does not mention (tier 1 sizes `«bar»` at 20, which then also widens the block). Tier 1+2 together: all four fixtures exact on every node. `kofuca-08` and `revusu-28` measure exact on **tier 1 alone**, so the acceptance criterion that `kofuca-08` closes without tier 2 is satisfied by measurement, not inference. | The split was never proposed; this is the evidence that shipping it would have tripped stop condition 4. Recorded because the width half of the tier-1-only regression is new information — ADR-5 and `element-font.md` both describe the intermediate state as height-only. | No |
| 2026-08-07 | F3-fix | **`preprocessor.ts` needed NO change — the task file's tier-2 bullet for it is void.** Probed both front-ends directly: `skinparam node { StereotypeFontSize<<bar>> 10 }` already normalizes to the single flat key `nodestereotypefontsize<<bar>>` (`collectSkinparamBlockEntry`'s `name.includes('<<')` branch, which deliberately does not double the block's own stereotype suffix), and `<style> node { stereotype { .bar { FontSize 10 } } }` already yields the selector `node.stereotype..bar`. Both values were being **parsed and then discarded downstream**, not lost at intake. | The task file flagged this bullet as unverified ("a direct grep found no existing hit, so confirm the real hook") — confirmed absent. Also load-bearing for the design: because `normaliseKey` lowercases and strips `[_.]` from the WHOLE skinparam key, its label arrives already `StyleSignatureBasic#clean`-normalized, which is why one cleaned map can serve both front-ends instead of two parallel fields. | No |
| 2026-08-07 | F3-fix | **Interface contract deviated: `ClassifyCtx.stereotypeFontSizeFor` takes `(sname, stereotypes)`, not the specified `(sname) => number \| undefined`.** `resolveElementFontSize` likewise gained an optional 4th `stereotypes` parameter. | The specified sname-only shape cannot express tier 2 at all: `loroto-06` declares `node nodefoo <<foo>>` and `node nodebar <<bar>>` — same USymbol, sizes 20 and 10. Mirroring `fontSizeFor` "exactly" and delivering tier 2 are mutually exclusive; the contract was written before that was noticed. `lineThicknessFor` keeps the specified shape. The labels passed are the **filtered** (`sizedNode.stereotype`) list, so a `hide stereotype`d label cannot resize a box it no longer draws. | No |
| 2026-08-07 | F3-fix | **The INK half of tier 2 is NOT fixed — write-set overrun, reported not taken (stop condition 1).** `renderer-symbol.ts#textFont` cannot resolve the name-scoped tier: it has no access to the entity's stereotype labels and already sits at the project's 5-parameter ceiling, so delivering them means bundling `role`+`stereotypes` into one options argument and updating **five** call sites — `renderer-entity.ts:189-190`, `renderer-cluster.ts:92,101`, `class/renderer-usymbol-entity.ts:82-83` — four of them outside this task's write-set. `loroto-06` still draws `«bar»` at font-size 20 where the jar draws 10 (verified in our own SVG). No gate observes it: neither slug is in `oracle/goldens/svg-description/ratchet.json`. | Adding the parameter to `textFont` alone was implemented and then reverted: with no caller able to pass it, it was dead capability that also forced a `#lizard forgives` on a 6-parameter public function. A precise `> **Open, escalated**` block naming all five call sites was left in `textFont`'s doc instead. Size measured, not estimated: 3 files, 5 call sites, ~12 lines, no algorithmic risk. | **Yes — 3-file follow-up, `textFont` + its 5 call sites.** |
| 2026-08-07 | F3-fix | **Edited a pre-existing test outside the "test files you author" clause: deleted `resolveElementLineThickness` from `tests/architecture/sizer-renderer-parity.test.ts`'s `KNOWN_GAPS`.** That guard failed `npm test` with its own instruction — "its Batch-4 fix appears to have landed. Delete its KNOWN_GAPS entry in the SAME commit as the fix". | The entry is the shrink-only ratchet this task's G5 retires; leaving it stale is a red gate, and the file's doc explicitly forbids the alternative (moving it to `SIZE_NEUTRAL`). `resolveElementShadowing` was left in place — the sibling `deltaShadow` slot is still un-threaded and still a real gap. `planning/sizer-renderer-parity.md`'s own `LineThickness` GAP row is now stale too, but that file is in no task's write-set. | Yes — `sizer-renderer-parity.md` row needs updating. |
| 2026-08-07 | F3-fix | Two files hit the 500-line cap mid-change and were kept under it by **shortening doc comments I was adding** plus one mechanical dedup: `layout.ts` had 4 lines of headroom for a 2-field + 2-construction change, so its two merged single-line `import … from '../../core/theme.js'` statements were collapsed into one and the new `ClassifyCtx` fields carry a one-line pointer to the fully-documented `BoxSizingOpts` fields instead of their own prose. `style-map-element.ts` was at 469 and its new branch is split into `collectStereotypeTagFontSize` rather than inlined. | The sanctioned answer to the cap is a module split, but that means a NEW `src/` file — outside the write-set, and disproportionate for two fields. No pre-existing comment, behaviour or exported symbol was touched; the rationale those fields would have carried is written out in full at `BoxSizingOpts.stereotypeFontSize`/`.lineThickness`. Both files now sit at exactly 500 — flagged so the next task touching either knows it has zero headroom. | Yes — `layout.ts` and `style-map-element.ts` are both at 500/500. |
| 2026-08-07 | F3-fix | **Object cross-engine check run via the task file's local uncommitted copy, not the README's `object-dot-parity.test.ts` route** (the two documents disagree; the README says no object script is needed, the task file says to make one). Result: 78 fixtures, **widened 0**, improved 21. The 21 are pre-existing stale pins, not my doing — verified by grep: **zero** object goldens contain either trigger syntax (`stereotypefontsize<<` or a `stereotype {` selector), so none of my new resolution paths can fire there. `object-dot-parity.test.ts` also passed inside `npm test`. | Both routes were exercised rather than picking one, since the two documents conflict and `widened > 0` on object is a stop condition. The throwaway script was written as a gitignored `inspect-*.ts` at the repo root and deleted; it is not in the diff. | No |
| 2026-08-07 | F3-fix | **ADR-5's premise CONFIRMED by controlled measurement, and understated.** With tier 2 disabled behind a temporary probe flag (removed before commit), tier 1 alone leaves `nodebar` at **93.125×64** vs oracle **91.363×54** — the +10px height ADR-5 predicted **plus a +1.762px width error neither ADR-5 nor `element-font.md` mentions**. Tier 1+2 together: every node exact on all four fixtures. | ADR-5 overrode the task-granularity rule on the strength of a predicted widening; that prediction is now measured rather than asserted, and the real cost of splitting was larger than the ADR claimed. Also confirms the naive one-slot trap was not reproduced (`nodefoo` 92×64, not the 112.250×70 the finding asserted). | No |
| 2026-08-07 | F3-fix | **G4 and G5 verified independently, not jointly.** G4: `kofuca-08` (`skinparam rectangle { StereotypeFontSize 12 }`, no name tier) measures exact **on tier 1 alone** — 44.363×74 / 142.238×152 / 154.15×152. G5: `revusu-28` 55×76 → 62×83 (jar exact), with a unit test asserting both dimensions move by exactly `2×(t−0.5)`; `ActorAwesome` untouched. | The task carried two mechanisms through one thread; passing fixtures is not proof each was addressed. | No |
| 2026-08-07 | F3-fix | **NEW ESCALATION — the INK half of tier 2 is not fixed** (stop condition 1). `«bar»` still DRAWS at font 20 against a box now correctly measured at 10. `textFont` cannot reach the entity's stereotype labels without bundling `role`+`stereotypes` and updating **5 call sites** (`renderer-entity.ts:189-190`, `renderer-cluster.ts:92,101`, `class/renderer-usymbol-entity.ts:82-83`), 4 of them outside the write-set. Measured at 3 files / ~12 lines. | **No gate observes this** — neither slug is in the SVG ratchet, so the mission's own instrumentation would report success while the rendered output stays wrong. That is precisely the failure mode `planning/sizer-renderer-parity.md` exists to prevent, so it is recorded as a live sizer/renderer divergence rather than a cosmetic follow-up. | Yes — scope decision. |
| 2026-08-07 | F3-fix | `revusu-28-pexi248` **closed**, despite matching F3-diag's M1 trigger shape by grep. | Direct evidence that M1's real blast radius is narrower than the 4-golden literal-grep estimate (`cusubu-18`, `kagapo-72`, `revusu-28`, `vajaxu-62`) — that grep matches the shape, not the defect. Narrows, but does not eliminate, the case for scheduling M1. | No |
| 2026-08-07 | orchestrator | **Stop condition 8 ("the default bundle grows") read as scoped to vendored asset payloads, not to ordinary code.** Measured at batch 3 close: 2,702.06 → 2,704.62 kB raw, 711.53 → 712.50 kB gzip (+2.56 kB / +0.97 kB gzip), all of it F3-fix's ~180 lines of real cascade code; F3-seam's contribution is type-erased plus one ~230-byte function. | ADR-9(b)'s own wording scopes the rule to the Twemoji artwork channel: "Twemoji artwork ships behind the optional/lazy channel from ADR-2 — the default bundle must not grow." A literal whole-bundle reading would make every code-adding fix in the mission a stop condition, which cannot be the intent. Recording the interpretation because it is the orchestrator's call, not the ADR's explicit text. F4-b remains bound by the strict reading for the artwork itself. | Yes — interpretation of a stop condition. |
| 2026-08-07 | orchestrator | **Batch 3 gates green on a COLD tree** (`packages/*/assets` deleted): `npm test` 0 (551 files / 12389 tests), `typecheck` 0, `lint` 0, `build` 0. Description **339 → 343/354 (96.9%), widened 0**; audit `agree 354, falseConformant 0, understated 0, maxHidden 0`; class 711/712 widened 0; state 148/148 widened 0. Deleted 4 pins (`loroto-06-fano471`, `toxine-81-xofo986`, `kofuca-08-pafi749`, `revusu-28-pexi248`); 11 remain. Ratchets re-run AFTER deletion: 1421/1421. | Same discipline as batches 1 and 2. F3-fix's own single test failure was correctly identified as its own (`sizer-renderer-parity` `KNOWN_GAPS` ratchet firing per its own instruction), not a concurrency artifact — and it fixed it rather than attributing it away. | No |
