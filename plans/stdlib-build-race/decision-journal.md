# Decision journal — stdlib-build-race

| When | Task | Decision | Why | Evidence |
|---|---|---|---|---|
| 2026-08-21 | orch | Execution plan logged rather than presented (autonomous mode). Batches run 0 -> 1 -> 2 (T2 \|\| T3) -> 3 -> 4; agents run no git, orchestrator commits by pathspec. | `rules/parallelism.md` autonomous exception; brief's batch table. | Write-sets disjoint only in batch 2: T2 owns `scripts/build-stdlib-packages.ts`, T3 owns `tests/helpers/build-stdlib-globalsetup.ts`. T2 and T4 share the script, so batch 3 is serial after 2. |
| 2026-08-21 | orch | Branch `fix/stdlib-build-race` cut from `main` at `0e6293ef`. | README "Branch" line. | `git log --oneline -1` = `0e6293ef refactor(oracle): move the include seam to tests/helpers`. |
| 2026-08-21 | orch | `scripts_scratch/` is NOT gitignored; T0/T1 scratch must be excluded by pathspec at commit time, not assumed ignored. | Avoids scratch landing in a commit. | `git check-ignore -v scripts_scratch/T0/x.ts` -> no match. |
| 2026-08-21 | T0 | PIVOT GATE PASSED. Hypothesis confirmed, not softened. Orchestrator independently re-ran the primary repro: FAIL at attempt 885, same signature. 6/6 counting T0's five. | D1 requires a reliable repro before any fix; a 1-in-7 bug cannot be verified by "it stopped failing". | Agent 5/5 (attempts 1297/334/541/530/801, mean 700.6) + orchestrator 1/1 (885). Secondary `worker_threads` pin 5/5, first failure at attempt 54-60. `.agent-notes/sre-T0.md`. |
| 2026-08-21 | orch | Batch 0's `npm test` gate accepted on T0's measurement (57.687 s at load1 4.56) plus a structural argument, rather than re-run by the orchestrator. | `vitest.config.ts:6` `include: ['tests/**/*.test.ts']` does not match `scripts_scratch/T0/*.test.ts`, and coverage `include: ['src/**/*.ts']` is unchanged. Batch 0 therefore CANNOT move the suite. Re-running it concurrently with T1's agent would have been two concurrent `npm test` runs — the very race under investigation — and confounded both. | typecheck/lint/build re-run by the orchestrator: all exit 0, 3 pre-existing dts notes only. `git diff --name-only -- src/` empty. render-manifest: "OK: 0 expected moves, 0 unexpected". |
