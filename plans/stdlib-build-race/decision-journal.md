# Decision journal — stdlib-build-race

| When | Task | Decision | Why | Evidence |
|---|---|---|---|---|
| 2026-08-21 | orch | Execution plan logged rather than presented (autonomous mode). Batches run 0 -> 1 -> 2 (T2 \|\| T3) -> 3 -> 4; agents run no git, orchestrator commits by pathspec. | `rules/parallelism.md` autonomous exception; brief's batch table. | Write-sets disjoint only in batch 2: T2 owns `scripts/build-stdlib-packages.ts`, T3 owns `tests/helpers/build-stdlib-globalsetup.ts`. T2 and T4 share the script, so batch 3 is serial after 2. |
| 2026-08-21 | orch | Branch `fix/stdlib-build-race` cut from `main` at `0e6293ef`. | README "Branch" line. | `git log --oneline -1` = `0e6293ef refactor(oracle): move the include seam to tests/helpers`. |
| 2026-08-21 | orch | `scripts_scratch/` is NOT gitignored; T0/T1 scratch must be excluded by pathspec at commit time, not assumed ignored. | Avoids scratch landing in a commit. | `git check-ignore -v scripts_scratch/T0/x.ts` -> no match. |
| 2026-08-21 | T0 | PIVOT GATE PASSED. Hypothesis confirmed, not softened. Orchestrator independently re-ran the primary repro: FAIL at attempt 885, same signature. 6/6 counting T0's five. | D1 requires a reliable repro before any fix; a 1-in-7 bug cannot be verified by "it stopped failing". | Agent 5/5 (attempts 1297/334/541/530/801, mean 700.6) + orchestrator 1/1 (885). Secondary `worker_threads` pin 5/5, first failure at attempt 54-60. `.agent-notes/sre-T0.md`. |
| 2026-08-21 | orch | Batch 0's `npm test` gate accepted on T0's measurement (57.687 s at load1 4.56) plus a structural argument, rather than re-run by the orchestrator. | `vitest.config.ts:6` `include: ['tests/**/*.test.ts']` does not match `scripts_scratch/T0/*.test.ts`, and coverage `include: ['src/**/*.ts']` is unchanged. Batch 0 therefore CANNOT move the suite. Re-running it concurrently with T1's agent would have been two concurrent `npm test` runs — the very race under investigation — and confounded both. | typecheck/lint/build re-run by the orchestrator: all exit 0, 3 pre-existing dts notes only. `git diff --name-only -- src/` empty. render-manifest: "OK: 0 expected moves, 0 unexpected". |
| 2026-08-21 | T1 | REJECTED on first return; agent resumed (not re-spawned) to fix. The committed test resolved its writer/reader from `scripts_scratch/T0/`, which the brief has the orchestrator delete after this batch. | A committed test depending on uncommitted scratch breaks on deletion and on any fresh checkout. Batch 1's overview requires the harness become "a committed artifact". | `tests/integration/stdlib-build-race.test.ts:44-45` pointed at `scripts_scratch/T0/{writer,reader}.ts`; `git status` shows `scripts_scratch/` untracked. |
| 2026-08-21 | orch | T1's write-set extended to add a committed home for the harness under `tests/helpers/`. Not a stop-4: the file is new, owned by no other task, and required by T1's own acceptance. | `rules/naming-conventions.md` puts shared test utilities in `tests/helpers/`; `vitest.config.ts:6` collects only `tests/**/*.test.ts`, so non-test files there are not run as suites. | Journaled rather than halted, per push-forward "harness shape and filenames". |
| 2026-08-21 | T1 | Accepted on re-report. Orchestrator verified self-containment by DELETING `scripts_scratch/` and re-running the guarded test: still reproduces (attempt 905), require stack now names `tests/helpers/stdlib-build-race-reader.ts`. | The whole point of the rejection was that the test must survive scratch removal. Verifying by inspection would not have proven it. | Harness code bodies byte-identical to T0's proven probe (comment-only diff, verified with a comment-stripped `diff`). Remaining `scripts_scratch` mentions in `tests/` are provenance comments only. |
| 2026-08-21 | orch | Batch 1 `npm test` = **57.00 s** (`real` 58.15 s), 624 passed / 1 skipped file, 16006 passed / 2 skipped / 1 todo. Under the 60.3 s ceiling with 3.3 s headroom. | The guarded test adds one SKIPPED file and costs nothing measurable. | Reported as CONSERVATIVE, not clean: `load1` read 46.00 at start, still carrying the immediately-preceding gate run's spike (1-min average decay), though all Spotlight daemons were 0.0. The run came in FASTER than both prior settled readings (T1 57.05 s, T0 57.687 s), so the adverse condition did not inflate it. |
| 2026-08-21 | orch | `scripts_scratch/` deleted, per push-forward "probes under `scripts_scratch/T<N>/`, deleted before commit". Never committed. | Its content now lives in `tests/helpers/`. | `git status` after removal shows only T1's four intended files. |
| 2026-08-21 | orch | Batch 2's parallelism carries a self-inflicted hazard: ANY `vitest` invocation runs `globalSetup` -> `buildStdlibPackages()`, so T2's and T3's concurrent test runs can trigger the very race under repair. Both agents were warned explicitly and told to re-run at settled load rather than "fix" a `Cannot find module .../generated/` failure that is not theirs. | Left unwarned, an agent would have attributed a sibling-induced failure to its own change and chased a phantom. T3's change is a comment and CANNOT cause a runtime failure, which makes the misattribution risk especially high there. | The mechanism is exactly `.agent-notes/sre-T0.md`'s: two independently-scheduled processes calling the same builder against one fixed path. |
| 2026-08-21 | orch | Orchestrator runs the AUTHORITATIVE gate suite once after both tasks land, rather than trusting either agent's in-flight `npm test`. | Any agent-side `npm test` during this batch is by construction contended. Agent readings are indicative; the post-batch run at settled load is the number of record. | Serialising the batch to avoid this was rejected: it would forfeit the only parallel batch for a hazard that a warning plus one authoritative re-run handles. |
| 2026-08-21 | T3 | Accepted. Orchestrator verified comment-only independently by diffing the file against `HEAD` with comment lines stripped: code byte-identical. | The task's central claim was "no behaviour change"; asserting it from the agent's word would not have tested it. | Guarantee now scoped to one vitest process; mechanism named; si11a/SI12 history preserved; closes by telling the reader not to take the opening claim as absolute. |
| 2026-08-21 | T2 | REJECTED on first return; agent resumed. Its report called `buildSpriteSplits` -> `splitSpriteBundle`'s unconditional `rmSync` "an unfixed, out-of-scope race". Orchestrator verified the claim instead of repeating it: it is LIVE and has a concurrent reader, so it defeats D3. | D3's design is that the second lock holder re-checks and SKIPS, performing no destructive op, so run A's workers see an intact tree. An unconditional `rmSync` left inside the locked region breaks that for the sprites tree. The lock cannot cover it: per D3 the lock is released before A's workers run, so B deletes underneath them legally. CLAUDE.md: "'out of scope' is a trigger to VERIFY, not skip." | `scripts/split-sprite-bundle/split.ts:144` `rmSync(opts.outDir)`; `buildStdlibPackages()` calls `buildSpriteSplits()` unconditionally, outside T2's skip; `tests/unit/sprite-package-files.test.ts:200` `readdirSync` on that tree, `:98` reads `sprites.json`, `:138-149` `npm pack`s it — all in workers. Precedent: si11a T8 fixed `generated/`, SI12 T8 then had to fix the same class in `assets/`. |
| 2026-08-21 | T2 | Extra-file residual accepted as DOCUMENTED, not closed in code. `isGeneratedDirUpToDate` iterates `freshOutputs` and cannot see a leftover file a fresh build would not write; on skip it survives, and `generated/` ships via each package's `files:`. | Closing it would need a `readdirSync` set-comparison, which risks being read as the count-based staleness signal D4 forbids (stop 5). A narrow, documented leak beats a D4-adjacent mechanism. | Leak window verified narrow: `emit-index.ts#indexLines` enumerates modules by `fileBaseName`, so dropping/renaming one changes `index.js` -> hash mismatch -> rebuild -> `rmSync` cleans. T2 asked to confirm this independently rather than accept the orchestrator's reasoning. |
| 2026-08-21 | T1/T2 | **Latent harness defect found by T2, verified by the orchestrator.** `tests/integration/stdlib-build-race.test.ts` can never PASS on any tree: `READER_ATTEMPTS = 100_000` cache-busted `import()`s cannot finish inside `RACE_TEST_TIMEOUT_MS = 120_000`. Pre-fix this was invisible because the race killed the reader at ~900 attempts. T1 resumed to bound the reader by the writer's lifetime. | T4's acceptance requires the repro to flip GREEN. As written it would time out instead, so a fixed tree would be indistinguishable from a broken one — the mission's central evidence would be worthless. | Orchestrator ran it post-T2: `Error: Test timed out in 120000ms`, `Duration 120.98s`. Pre-fix runs failed fast at attempts 1036 (T1), 905 (orch), 885 (orch, pre-T1). |
| 2026-08-21 | orch | Batch 2's overview states "Do not expect T1's guarded repro to pass after this batch; it should still fail." Reality: it is expected to PASS post-T2. Recorded as a task-file expectation corrected by evidence, NOT a D-conflict (no stop 7). | D3's "neither half closes the race alone" is a claim about the general race. This harness only exercises the UNCHANGED-INPUTS case, which T2's skip genuinely does close — and which is what the original 1-in-7 failure was (two runs over identical source). D3's residual is the CHANGED-INPUTS case, which the harness deliberately does not simulate. | T2 independently reached the same conclusion: "D3's own residual note assumes genuinely-changing inputs, which this harness doesn't simulate." Precedent for resolving rather than halting: the field-name reconciliation in `.agent-notes/g1h-T2.md`. |
| 2026-08-21 | orch | Pre-fix red state will be re-verified BY THE ORCHESTRATOR against the old builder, not by T1. | Agents run no git; proving the re-tuned harness still catches the race needs the pre-T2 builder restored temporarily. | Instructed T1 to report clean-run numbers only and not to attempt the revert. |
| 2026-08-21 | T1 | Orchestrator DISPROVED its own suggested mechanism, and the agent was right to test it. My SIGTERM proposal cannot work: the reader's cache-busting `import(url?t=N)` registers a permanent, never-evicted ESM loader entry per attempt, so per-call cost grows unboundedly and a JS-level handler cannot interrupt an `import()` already in flight. Agent switched to `SIGKILL`. | An unbounded in-flight import made the cooperative stop-flag useless; `kill -TERM` had zero effect for 2+ minutes on a live reader. | Agent measured kill-to-close at 2-3 ms with SIGKILL. It also found a second real defect unprompted: timed-out runs orphaned writer/reader OS processes, since vitest's per-test timeout does not cancel the in-flight promise. Fixed with an unconditional `afterEach` force-kill. |
| 2026-08-21 | T1 | Constants: `WRITER_ITERATIONS` 300 -> 275, `RACE_TEST_TIMEOUT_MS` 120_000 -> 180_000 (NOT the 500_000 first proposed). Clean run measured at 122,933 ms, leaving ~46% margin. | A 500 s timeout would have hidden a slowdown rather than reflected a need. The agent measured instead of guessing, and the timeout now follows from the measurement. | Orchestrator challenged the 500 s figure with a specific hypothesis (T2's per-check SHA-256 of every generated file dominating runtime) and told the agent to confirm or refute rather than accept it. |
| 2026-08-21 | orch | **Pre-fix red state re-verified by the orchestrator against the restored old builder, with the RE-TUNED harness** — the check T1 could not perform. Result: `FAIL at attempt 1409`, correct `Cannot find module` signature, `Duration 106.31s`. Builder then restored. | 275 iterations were chosen from historical attempt numbers, not from a fresh measurement. Unverified, the test might have been green everywhere and worthless as a gate. | Both directions now sit under the 180 s timeout: pre-fix RED at 106.31 s, post-fix GREEN at 127.44 s. The test discriminates a fixed tree from a broken one. |
| 2026-08-21 | orch | Batch 2 gates GREEN at a genuinely settled machine: load1 4.54, all Spotlight daemons 0.0. `npm test` **55.75 s** (625 passed / 1 skipped files; 16026 passed / 2 skipped / 1 todo), coverage 95.44/90.47/96.95/96.53. typecheck, lint, build exit 0 (3 pre-existing dts notes). render-manifest "0 unexpected". `git diff -- src/` empty. | Every agent-side reading during this batch was contended by construction; this is the number of record. | Faster than the batch-1 baseline (57.00 s), so the skip costs nothing measurable. 5.5 s headroom under the 60.3 s ceiling. |
| 2026-08-21 | orch | **T4's acceptance clause "T1's guarded repro now PASSES" is ALREADY SATISFIED before T4 begins**, so it cannot serve as evidence the lock works. T4 must supply its own proof. | The harness exercises only the unchanged-inputs case, which T2's skip closes. A pre-satisfied checkbox standing in for verification is exactly how an unproven fix ships. | Post-T2 repro: PASS in 127.44 s. T4's real evidence must be concurrent builders and a partial-tree observation, plus stale-lock reclaim and a bounded wait (stop 6). |
| 2026-08-21 | T4 | **STOP-6 DEFECT found by orchestrator verification: an unparseable lock file wedges every future run.** `build-lock.ts:177` guards reclaim with `contents !== undefined && isStale(...)`, so a lock whose JSON does not parse is never reclaimed. Agent resumed to fix. | Converting a 1-in-7 flake into every-run failure is strictly worse than the bug, which is exactly what stop 6 forbids. A crash or full disk mid-`writeFileSync` leaves precisely this state. | Planted `{"pid":12345,"acqui` (truncated). Measured: 30 s stall, then `Timed out after 30000ms waiting for the stdlib build lock`, lock file still present. Every later run repeats it until a human deletes a file under `/var/folders/`. |
| 2026-08-21 | orch | Fix left to the agent's judgement, with the counter-hazard named: `writeFileSync(..., {flag:'wx'})` creates then writes, so naively treating unparseable as stale lets a racing process steal a lock from a holder that just acquired it. Must not trade a rare wedge for a common race. | Two acceptable mechanisms offered (mtime grace period, or temp-file + atomic `rename`); the agent picks and justifies. Using the LOCK FILE's mtime is not a D4 violation — D4 governs build-OUTPUT staleness, not holder liveness. | Both directions must be pinned by tests: corrupt lock reclaimed without hanging, AND create-then-write window not stolen. |
| 2026-08-21 | orch | Three earlier "failures" I measured were MY error, not T4's: I planted locks in `/tmp`, but `os.tmpdir()` on macOS is `/var/folders/.../T`. Corrected before reporting anything. | Nearly filed a false defect against working stale recovery. Recorded so the next person probing this lock does not repeat it. | At the real path, dead-PID reclaim works: `reclaiming stale lock ... (holder pid 99999, age 169ms)`, build 510 ms, released, no hang. |
| 2026-08-21 | orch | Lock-timing constants VERIFIED against a real measurement rather than accepted: cold full build = **0.91 s**. `staleAgeMs` 60 s is ~66x margin; `maxWaitMs` 30 s is ~33x. | The OR-staleness (dead PID OR age > 60 s) can steal from a LIVE holder, which would reintroduce the race. Only safe if builds cannot approach 60 s. | Ordering is defensive: `maxWaitMs` (30 s) < `staleAgeMs` (60 s), so a waiter arriving at a fresh lock times out loudly rather than stealing. Stealing needs arrival when the lock is already >30 s old — 33x the measured build. |
| 2026-08-21 | T4 | Stop-6 fix ACCEPTED after orchestrator re-verification at the real lock path. Corrupt lock now reclaims after its 2 s grace (`unparseable content, age 2003.8ms`), build completes in 2473 ms — previously a 30 s stall then permanent failure on every future run. Dead-PID reclaims at age 0 ms in 421 ms, and the misleading "waiting" log no longer fires when no wait occurred. | Stop 6 is the one failure mode strictly worse than the bug; it had to be tested, not reviewed. | Agent's rationale for mtime-grace over write-then-`rename()` is sound and independently checked: `rename()` silently replaces its destination so it cannot deliver "exactly one caller wins"; `wx` already does, and the defect was in the stale-check's blind spot, not acquisition. 2 s is ~1000x the sub-millisecond `open->write->close` window. |
| 2026-08-21 | orch | **Three concurrent REAL builders against a cold tree — the lock's actual evidence.** builder3 acquired and rebuilt all 5 targets; builders 1 and 2 waited, re-checked inside the lock, and SKIPPED all 5 (zero `rmSync`). All exit 0, no lock left, tree valid (4 generated files, 2078 sprites). | Exactly D3's predicted sequence: A builds under the lock and releases; B and C acquire, re-check, skip; nothing is deleted under a live reader. | This is the evidence the guarded repro CANNOT provide, since that repro went green after T2 with no lock in existence. |
| 2026-08-21 | orch | Batch 3 gates GREEN at settled load (load1 4.46, Spotlight daemons ~0): `npm test` **56.46 s** (626 passed / 1 skipped files; 16034 passed / 2 skipped / 1 todo), coverage 95.44/90.47/96.95/96.53. typecheck, lint, build exit 0. render-manifest "0 unexpected". `git diff -- src/` empty. | The lock is taken on EVERY run, so its uncontended cost had to be measured, not assumed. | 56.46 s vs the 55.75 s batch-2 baseline and 57.00 s batch-1 baseline — within run-to-run spread, so the lock costs nothing measurable. 3.8 s headroom under the 60.3 s ceiling. |
