# Decision journal — `activity-swimlane-rendering`

Appended during execution. One row per non-trivial judgment call: if a
reasonable developer might have chosen differently, log it.

Log the execution plan here at startup (autonomous mode replaces the
user-review step with this entry — `rules/parallelism.md`).

| Date | Task | Decision | Rationale | Evidence |
|---|---|---|---|---|
| 2026-09-09 | plan | Execution plan: batches 0–7 strictly sequential (each depends on the previous); T0 and T7 orchestrator-executed; T1–T6 one agent each, no parallelism within a batch. Branch `feat/activity-swimlane-rendering` from `d0a7e1bd`. | Every batch has one task and every task depends on the previous batch; there is nothing to run in parallel. | README Index; batch overviews |
| 2026-09-09 | T0 | Census core split into `tests/oracle/svg-conformance/swimlane-census.ts` (pure, no vitest import), with the gate importing it. Not in the write-set as written. | A `.test.ts` cannot be imported by a plain script (vitest's `describe` throws outside a runner), and the re-pin generator must use the SAME instrument as the gate or it is a second instrument. Push-forward item "file organisation". | `swimlane-census.ts` doc comment |
| 2026-09-09 | T0 | Divider rule: vertical line that is TOP-ANCHORED (starts at the smallest y any vertical line reaches) AND spans ≥ 50% of the canvas. The first cut (span only) mis-counted 22 of 25 anomalies. | Upstream draws dividers without the title translate every edge is drawn under (`Swimlanes.java:342` vs `:338-340`), so a divider's top is the drawing's top. | `.agent-notes/asr-T0.md` |
| 2026-09-09 | T0 | `letuke-04-poza319` (a two-pipe creole table row inside a multi-line action) stays in the population with an empty census rather than being excluded by a parser-like rule. | The needle is a transcription of `CommandSwimlane.java:59-67`; excluding it would need multi-line-action tracking in the instrument, and the entry is harmless (0 lanes both sides). | `.agent-notes/asr-T0.md` |
| 2026-09-09 | T0 | The JAR's `lanes` are derived from consecutive ascending dividers; OURS from `ActivityGeometry.swimlanes`. Asymmetric by design. | The jar exposes no geometry; the port's geometry is the quantity T4/T5 move and our SVG today lacks the outer right divider to derive it from. | `swimlane-census.ts#lanesFromDividers` |
| 2026-09-09 | T0 | Gates: `npm test` 695 files / 18847 tests green (baseline 694 / 18741, +1 file +106 tests), `typecheck` 0 errors, `lint` 0, `build` ok. | — | this session |
| 2026-09-09 | T1 | D4 AMENDED, flagged for review: the band-fill key upstream is `SwimlaneTitleBackgroundColor` (`FromSkinparamToStyle.java:160`); `SwimlaneHeaderBackgroundColor` does not exist upstream. T1 wires the upstream spelling and keeps the local one as an alias; mechanism (two PName-backed fields) unchanged. Proceeding under push-forward "equivalent spellings" rather than halting. | The decision's mechanism is intact and the corpus (`vidada-17-xuse810`, `#EEE` band) needs the upstream key; halting on a spelling would stall the mission on a choice with one defensible answer. | `decisions.md#d4`, `swimlane-baseline.json` vidada entry |
| 2026-09-09 | T1 | The five contract fields go under `theme.colors.graph.activity`, where `swimlaneBorder` already lives, not directly under `theme.colors.graph` as the contract's comment reads. | `theme-graph-colors-b.ts:321-330` nests every activity colour under `activity?`; the contract comment is imprecise, the existing field's location is the precedent. | `theme-graph-colors-b.ts:329` |
| 2026-09-09 | T1 | Landed `0217b1e6`. Gates: `npm test` 695 files / 18852 tests, typecheck 0, lint 0, build ok. Conformance suites (`tests/oracle/svg-conformance`) 26 files / 3040 tests before and after — byte-identical. Orchestrator re-probe: aggregate weightedScore 52563 -> 52563, swimlane census gate 106/106. | Agent claims audited against the probe and the gate, not trusted. | probe + gate this session |
| 2026-09-09 | T1 | `SwimlaneTitleFontName`/`FontStyle` (also yielded by `addConFont`, `FromSkinparamToStyle.java:159`) NOT wired; only the five contract fields. | Nothing in this mission consumes them; filed as a follow-on at close-out rather than widened silently. | agent report |
| 2026-09-09 | T2 | Resolver precedence: T1's explicit `graph.activity.swimlane*` field first, then the shared `swimlane` element bucket (`<style>`), then the `plantuml.skin` constant. | Upstream mutates one style builder in declaration order (last wins), which the resolved theme cannot reproduce; the explicit skinparam key is the narrower, more deliberate declaration. Recorded so T6 does not re-decide it. | `activity-style-defaults.ts` |
| 2026-09-09 | T2 | Landed `9e515d64`. Gates: `npm test` 695 files / 18863 tests, typecheck 0, lint 0, build ok; three activity oracle gates 873/873. Orchestrator re-probe: aggregate 52563 -> 52563. `docs/catalog.md` regenerated on drift (push-forward). | Audited against the probe and gates. | probe + gates this session |
| 2026-09-09 | T2 | Bucket tier reads `theme.colors.elements.swimlane.{border,font}` DIRECTLY, not through `resolveElementPaint`, whose own fallback is the diagram-wide generic colour rather than the `plantuml.skin:311`/`:9` constant. T1's colour fields are stored RAW and normalised ONCE here via `resolveColorToSvgHex`; T6 must not re-resolve them. | The cascade's third tier is the swimlane constant, not the generic default; a shared helper would have skipped it. | agent report; `activity-style-defaults.ts:396-436` |
| 2026-09-09 | T3 | Landed `19705b15`. Orchestrator re-probe: aggregate 52563 -> 52563, 0 risers / 0 fallers (stop condition 6 NOT triggered). Orchestrator full `npm test`: 695 files / 18866 tests (the agent's own run reported 694 files — a `coverage/.tmp` under-collect; re-run by the orchestrator, not trusted). typecheck 0, lint 0, build ok. `tileNodes` exported test-only; `docs/catalog.md` regenerated on drift. | Audited against the probe; the file-count discrepancy is the known under-collect pattern. | probe + full suite this session |
| 2026-09-09 | T3 | Agent corrected the brief's citation: `getSwimlaneIn`/`getSwimlaneOut` live on `activitydiagram3/ftile/Swimable.java`, which `Instruction` extends — not on `Ftile.java`. The tile field cites `Swimable`. | Brief pointed at the wrong file; the agent read the Java. | `tiles/tile.ts` JSDoc |
| 2026-09-09 | T3 | FINDING for T5 (parser, not this mission's write-set): a composite node's recorded `swimlane` is whichever lane was active at the END of its construct (`if-dispatch.ts#tryIf` evaluates `swimlaneSpread(ctx)` after the branches are parsed), not the lane active when the construct opened. Upstream's `getSwimlaneIn` for an if is the lane the diamond was declared in. Verified empirically by the T3 agent. | Affects where T5 places a composite's diamond when lanes change inside it; filed here so T5 reads the AST's value knowingly and the close-out files the parser defect. | agent report; `if-dispatch.ts` |
| 2026-09-09 | T4 | Landed `f77709cf`. Orchestrator re-probe: aggregate 52563 -> 52563 (nothing consumes T4 yet). Orchestrator full `npm test`: 696 files / 18886 tests; typecheck 0, lint 0, build ok. New test file `tests/diagrams/activity/layout/swimlane-context.test.ts` (file-organisation push-forward — the write-set named no test file). | Audited against probe + full suite. | this session |
| 2026-09-09 | T4 | Stop condition 4 NOT triggered: the lane padding is `Swimlanes#getHalfMissingSpace`'s literal 5 (`Swimlanes.java:438,444`), rising by `(titleWidth - laneWidth)/2` on title overflow; ported as `SWIMLANE_HALF_MISSING_SPACE` + `halfMissingSpace()`. The title widens the DIVIDERS, never the lane (`:408` takes `max(min, contentWidth)` only). Hand-check on `pakema-21-xema183`: reconstructing the three observed dividers gives lane B's title width as exactly 300.937, the golden's own `textLength`. | The brief's "~5px a side" was a measurement; the source is the literal. | `swimlane-context.ts` JSDoc; agent report |
| 2026-09-09 | T4 | `skinparam swimlaneWidth` (`SkinParam.java:1121-1130`; `same` = sentinel -1, `ISkinParam.java:71`) is modelled as the `min` PARAMETER of `computeLaneWidths` with upstream semantics; the skinparam KEY is unparsed and filed as a follow-on (fixtures `nikinu-06-sace939`, `cemipu-87-dinu624`). | Wiring the key is a `src/core` change outside every write-set. | agent report |
| 2026-09-09 | T4 | NOTE for T6/T7: `swimlane-baseline.json`'s `jar.lanes[i].width` is the DIVIDER-TO-DIVIDER span (by construction, `lanesFromDividers`), not upstream's `swimlaneActualWidth`; it includes both adjacent half-spaces. Lane A's title `x` (33.15) does not reconcile with a naive left-aligned placement — `CenteredText` centres the title over the CONTENT width from the content's left (`Swimlanes.java:369-377`), which T6 must port literally. | So nobody validates `SwimlaneGeo.width` against that JSON field directly. | agent report; census doc comment |
| 2026-09-09 | T5 | Landed `3d0bd2c5`. Orchestrator re-probe: aggregate 52563 -> **52988 (+0.81%)**, 25 risers / 32 fallers, ALL 57 movers are swimlane fixtures (subset 18419 -> 18844); the 208 no-lane fixtures are byte-identical. `svg/g[][childCount]` 24911 -> 23644. typecheck 0, lint 0, build ok. Expected red: swimlane census (59), style census (57), ratchet (25) — all equality/ratchet pins of the old model, re-pinned once at T7. | The exit bar is mission-level ("aggregate falls", "zero UNEXPLAINED rises"); T5 alone is not the bar. Every riser has a mechanism below. | probe `--list` this session |
| 2026-09-09 | T5 | RISER MECHANISM 1 (verified by the orchestrator on `pakema-21-xema183`): lane widths are now content-fitted, and our content is `ACTION_MIN_WIDTH = 120` (`tiles/gtile-action.ts:16`, unsourced, left by `activity-style-defaults` T4 deliberately) where the jar's box is 26.675 — so lane A is 130 wide against the jar's 38.338 and every divider and lane-B node sits ~100px further right than before. The old equal-division overlay was closer by accident. Outside every write-set of this mission; filed for the close-out as the single largest residual on the swimlane subset. | A correct port of the WIDTH formula exposes an already-wrong INPUT; fitting is forbidden. | our pakema: dividers 17/147/457.9 vs jar 20/58.3/369.3 |
| 2026-09-09 | T5 | RISER MECHANISM 2 (agent, checked against the Java by the orchestrator): a cross-lane edge's horizontal jog is at `(p1.y + p2.y)/2` (`vcompact/ConnectionVerticalDown.java:87-100`, ported), but upstream computes it on the PRE-compression geometry where `FtileFactoryDelegatorAssembly#assembly` inserts a 35px spacer (`:58-65`) that `CompressionXorYBuilder` ON_Y later squeezes; our fixed `NODE_MARGIN_Y = 20` has no such phase, so our jog is 5px lower than the jar's on `pakema`/`patagi`. Pre-existing vertical model; outside the write-set. | Named, not fitted. | jar jog y=117.5 vs ours 94 (in our coordinates: midpoint) |
| 2026-09-09 | T5 | Placement is a POST-SHIFT of the single-column walk (one additive dx per lane, `swimlane-placement.ts#computeLaneOrigins` porting `Swimlanes.java:416-431`), not a re-walk: upstream's translate is one `dx` per lane applied to the whole sub-tree. New module `swimlane-placement.ts` (+test) under the 500-line limit push-forward. Structural children with no own lane (a composite's diamonds, fork bars) inherit the nearest ancestor's lane. Fork/split cross-lane jogs use the shared average rule rather than `ParallelBuilderFork.java:166-184`'s bar-relative literals (which are sized against a bar geometry we do not reproduce). | Judgment calls the agent made and reported; accepted. | agent report |
| 2026-09-09 | T5 | `SWIMLANE_MIN_WIDTH` NOT deleted: still imported by the superseded `activity-layout-swimlane.ts` (stop condition 5 forbids touching it). Its live call site is gone; the constant is dead on the render path. | Deleting it needs the superseded-engine mission. | agent report |
| 2026-09-09 | T5 | FINDING (agent): `Gtile.USE_GTILE` is false in the reference jar, so the pinned oracles are rendered by the OLD Ftile engine (`ConnectionCross`/`ConnectionTranslatable`, `vcompact/ConnectionVerticalDown.java`), not the newer Gtile/GConnection classes. Future activity connector work reads the Ftile tree first. | Recorded for `.agent-notes/asr-T5.md` at close-out. | agent report |
| 2026-09-09 | T6 | Landed `a67cab41`. Orchestrator re-probe: aggregate 52563 -> **48291 (−8.13%)**, 2 risers / 57 fallers; swimlane subset 18419 -> 14147 (−23.2%); `svg/g[][childCount]` 24911 -> 19771. typecheck 0, lint 0, build ok. Expected red: swimlane census 59, style census 59, ratchet 2. Our `pakema` now emits band / lane-A shapes / divider / lane-B shapes / divider / divider / edges / titles — the jar's order. | Audited against the probe and a dump of the fixture. | probe this session |
| 2026-09-09 | T6 | D2 mechanism found by the agent and verified by the orchestrator: `AtomText#calculateDimensionSlow` floors the text height at 10 (`klimt/creole/legacy/AtomText.java:179-181`), which is why `SwimlaneTitleFontSize 8` yields a 10px band on `sikino`. Ported in `swimlane-placement.ts#measureSwimlaneTitlesHeight` (bounder height per title, floor 10, max over lanes), shared by layout and renderer. | The band height is measured, not the font constant, exactly as D2 requires; the floor is upstream's, cited. | `AtomText.java:179-181` |
| 2026-09-09 | T6 | The two ratchet risers `decudi-92-bisu741` (464 -> 471) and `maketa-43-juja264` (354 -> 367) are net risers against the T0 floor only: against the pre-T6 tree both FELL (492 -> 471, 382 -> 367). Their rise is T5's mechanism 1 (the unsourced `ACTION_MIN_WIDTH = 120` widening split/fork lane content); T6 recovered 21 and 15 of it. Named, expected, not a T6 defect. | Measured in a pre-T6 worktree, family by family. | `fixture-families.ts` probe |
| 2026-09-09 | T6 | Band geometry: `band.x = lanes[0].x`, `band.width = Σ lane.width − 1` — the agent showed algebraically that the two outer half-spaces cancel out of `drawTitlesBackground`'s `dx(5)` / `− 2·5 − 1` (`Swimlanes.java:357-367`); exact on the jar's pakema (20 / 348.275). `SWIMLANE_HEADER_H` kept DEFINED (superseded `layout.old.ts:53` imports it, stop condition 5) but off the render path. `swimlaneHeaderBackground` resolver added beside T2's four; a transparent band is spelled `fill="none"` as the jar spells it. | Judgment calls reported by the agent and accepted. | agent report |
