# Decision journal — class-guide-line-glyphs

Appended during execution. One row per non-trivial judgment call. Every count
comes from a command run on a clean tree.

| Date | Decision / finding | Evidence / why | Flag for review? |
|---|---|---|---|
| 2026-08-17 | Baseline captured in detached worktree at `7ba67fcd` (SI24 recipe, six ignored dirs symlinked): shape-match 785/26,255, class DOT EQUAL 705/711, `gobuco` 14/22 168x102 vs 168x102, `lapoma` 14/22 68x232 vs 72x232 — matches README starting state. Per-fixture rows saved for the T4 diff. | Commands run in the worktree; scratchpad `baseline-shape.txt`/`baseline-dot-class.txt`. | No |
| 2026-08-17 | T1 done directly by the orchestrator (no subagent): a ~60-line refactor + tests, under the ~30-min delegation threshold. `direction` is `'forward'\|'backward'` (`core/edge-label-box.ts:212`), so the spec's "right/left" reads as forward/backward. Gates: test 595/14496 pass, cov 95.37/90.36/96.93/96.46, typecheck/lint/build green, class DOT EQUAL 705/711 unchanged. | `git diff --name-only HEAD~1` = the two write-set files. Commit `refactor(T1)`. | No |
| 2026-08-17 | T2 done directly by the orchestrator (single task, no parallel sibling; the Java reading stays in one context). | Same rationale as T1. | No |
| 2026-08-17 | `LinkArrow#mute` reading (D4 push-forward): `abel/LinkArrow.java:55-71` -- DIRECT_NORMAL returns the edge guide unchanged, BACKWARD wraps it with `Math.PI +`; a multi-guide-line link's own `LinkArrow` is `NONE_OR_SEVERAL` (`StringWithArrow.java:63-65` via `Labels.java:64` -> `Link#setLinkArrow`), so the guide (`SvekEdge#getArrowDirectionInRadian`, `:201-206`) carries the un-reversed internal angle. Per line this is exactly `magicArrowAngle(fromToPoints, direction)` -- the whole-label path, mirrored. | Java read + `gobuco` jar polygons: forward tips point right (76.68 > 67.635), backward left, on a left-to-right edge. | No |
| 2026-08-17 | D2 seam shape: `buildEdgeGeos`/`attachEdgeLabel` take one `EdgeGeoTextContext { measurer, labelFont, fontFamily }` rather than swapping `fontFamily: string` for `labelFont: FontSpec` in place. Reason: tail/head cardinality labels must keep `theme.fontFamily` + `CARDINALITY_FONT_SIZE` (D2 "do not touch"), and `labelFont.family` differs from `theme.fontFamily` under `ArrowFontName` (`ticuxa`), so both strings are needed; adding a 9th positional param to `buildEdgeGeos` would worsen a pre-existing hook violation. `portLabelAnchor` now takes a `FontSpec` (main label passes `labelFont`; `attachPortLabels` passes `{fontFamily, 13}` -- behaviour unchanged, `class-tail-head-swap.test.ts` green). | Same seam, 8 -> 7 params; `attachEdgeLabel` 6 -> 5, `attachMagicArrow` 6 -> 4. | No |
| 2026-08-17 | `guideLinesAnchor` text baseline uses `size - descent` from the line top (the existing `multiLineLabelAnchor` rule) rather than `mergeLR`'s `(H - textHeight)/2` centring term: under this port's measurers `height === font.size` (`core/measurer.ts` ADR-001, `StringBounderFixed`), so `H = max(size, textHeight) = size` and the term is identically 0; the arrow slot's `dy = (blockHeight - size)/2` IS mirrored. Documented in the function's JSDoc with `TextBlockHorizontal.java:79-91`. | jar `gobuco`: text y 17.5 = line top 7.389 + (13 - 13/4.5); ours identical (`y` byte-equal to the pre-T2 multi-line path). | No |
| 2026-08-17 | **Flag:** `fix(T1)` commit `722c2bee` outside T2's write-set: `magicArrowGlyphPoints` used the 13-based `ARROW_GLYPH_SIZE` (10) as its ink radius for EVERY `arrowFontSize`, but `TextBlockArrow2.java:64-65` derives `triSize = (int)(size * .80)` from the resolved arrow font (`:57`) -- T2's acceptance "glyph is 20px under FontSize 20" is unmeetable without it. New `magicArrowTriSize(size)`; `ARROW_GLYPH_SIZE = magicArrowTriSize(13)`, byte-identical at the default (full class SVG dump: no fixture but the expected four moved). Committed separately so each commit's diff matches its own task's write-set; `class-magic-arrow.ts` is T1's file, not outside the mission, so stop 1 does not trigger. Zero corpus reach (no fixture combines a token with a font-size override). | `tests/unit/class/class-magic-arrow.test.ts` pins triSize(20)=16, triSize(58)=46, triSize(13)=ARROW_GLYPH_SIZE. | **Yes** -- reviewer to confirm the T1-file fix commit is acceptable. |
| 2026-08-17 | T2 movement, measured by a full class SVG dump (722 fixtures) at `7ba67fcd` (detached worktree) vs T2: exactly 4 fixtures differ -- `gobuco` (text now stripped, x 79.45 vs jar 79.68 = the N25/N62 residual, y byte-equal), `lapoma` (doc 68x232 -> 70x232, jar 72x232), `ticuxa` (`toto` at 58: x 39.969/y 131.111/textLength 96.425 vs jar 39.97/131.111/96.425; doc 114 -> 151 vs jar 152), `camuna` (`foo1`/`foo2` textLength 27.213 = jar's, baseline fraction .889 = jar's). `nafiki` has no main label -> unmoved. All toward jar; everything else byte-identical. Class DOT EQUAL 705/711; gates green (595/14505, cov 95.37/90.35/96.93/96.46). | Scratch dump script (`render-fixture-class` over `test-results/dot-cache/class`), `diff -rq`. | No |
| 2026-08-17 | Named follow-ons (not widened): (a) tail/head cardinality ink is anchored at `theme.fontFamily` + `CARDINALITY_FONT_SIZE` while the DOT box uses `theme.cardinalityFontFamily/Size` (`class-dot-graph.ts:390`) -- an ink/box drift under `arrow.cardinality { FontSize }` (`camuna`, `nafiki`); (b) `class-ink-box.ts#addEdgeTextInk` uses the constant 13 for the text ascent regardless of the label's font (`ticuxa` doc 151 vs 152). | Out of D2's stated scope (main label only). | No |
