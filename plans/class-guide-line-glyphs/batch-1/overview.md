# Batch 1 — shared per-line walk (zero movement)

| ID | Description | Agent | Writes | Depends On | Done |
|---|---|---|---|---|---|
| T1 | `splitGuideLines` in `class-magic-arrow.ts`; `computeGuideLinesBox` consumes it | `typescript-pro` (sonnet) | `src/diagrams/class/class-magic-arrow.ts`, `tests/unit/class/class-magic-arrow.test.ts` | — | [x] |

**Batch exit:** four gates; class DOT EQUAL 705/711 unchanged; `computeGuideLinesBox` output byte-identical (pinned).
