# T4 — Close-out

Every number from a command run on a clean tree at the final code commit
(detached worktree for the census). Template: `plans/edge-label-box-followups/
README.md#close-out-2026-08-16`.

1. Measure: `dot-sync-report class` (and state/component/usecase/object —
   must be unchanged), `shape-match-report` per-fixture diff vs the start
   baseline, ratchet pin counts, test count/coverage.
2. Append a "Close-out" section to `README.md`: exit bar scored ✓/✗ per clause
   with the measurement; fixtures moved (each toward jar); corrections owed;
   tasks planned vs executed; follow-ons (description/state per-line glyphs
   if named; anything new).
3. `planning/mission-index.md`: row **SI25** (format: SI23/SI24 rows).
   `planning/next-missions.md`: refresh the standing-signals block, mark §1
   follow-on #1 done, dated.
4. Journal: tasks, decisions flagged for review, gate results.

Write-set: the four files above. Reversible — docs only.

Commit: `docs(T4): close class-guide-line-glyphs — scored bar, SI25 row`
