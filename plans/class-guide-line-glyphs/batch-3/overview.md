# Batch 3 — renderer: draw the per-line glyphs

| ID | Description | Agent | Writes | Depends On | Done |
|---|---|---|---|---|---|
| T3 | `renderer-edge.ts` draws `labelLines[i].glyph` `<polygon>` beside each `<text>` | `typescript-pro` (sonnet) | `src/diagrams/class/renderer-edge.ts`, `tests/unit/class/renderer.test.ts` (or `class-edge-labels.test.ts`) | T2 | [ ] |

**Batch exit:** four gates; `gobuco`/`lapoma` SVG carry four glyphs; class
ratchet pins hold or move toward jar; census (clean worktree) no fixture rises.
