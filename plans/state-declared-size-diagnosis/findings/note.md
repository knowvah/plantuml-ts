# note — T8

### fatupo-62-bemu777

- **bucketLabel:** note
- **rows:**
  | scope | axis | idx | ours (in) | jar (in) | Δpx |
  |---|---|---|---|---|---|
  | 1 | width | 1 | 2.786719 | 2.278906 | +36.563 |
  | 1 | height | 0 | 0.680556 | 0.694444 | -1.000 |
  | 1 | height | 1 | 0.694444 | 0.736111 | -3.000 |
- **status:** resolved
- **mechanism:** State notes size their body via `state-note-layout.ts#measureNote`, a text-only model that ignores creole markup entirely: it treats each raw source line (including pipe-table syntax `|= header 1 |...`) as ONE literal text string and measures it with `measurer.measure`, with no column/border computation at all. Width becomes the widest RAW line's flat character width + fixed side margins; height is `lineCount * NOTE_FONT_SIZE` with no per-row correction. The REAL, faithful table pipeline (`StripeTable`/`AtomTable`/`SheetBlock1`) exists in this port and is correct in isolation, but state notes never reach it.
- **originFileLine:** src/diagrams/state/state-note-layout.ts:84-93 (function `measureNote`)
- **javaRef:** ~/git/plantuml/src/main/java/net/sourceforge/plantuml/svek/image/EntityImageNote.java:114-118 (real `BodyFactory.create3` call, always creole-aware) + ~/git/plantuml/src/main/java/net/sourceforge/plantuml/klimt/creole/atom/AtomTable.java:90-96 (`calculateDimensionSlow`, the real border-to-border table box) + ~/git/plantuml/src/main/java/net/sourceforge/plantuml/klimt/creole/legacy/StripeTable.java:82-84 (`new AtomWithMargin(table, 2, 2)`, the +4px vertical pad jar's number includes)
- **causalChain:** Jar's note content width = real table box (143.081px border-to-border, verified via `scripts_scratch/T8/probe2.ts` calling `buildNoteBody` directly on the same 3 lines) + `NOTE_MARGIN_H`(21) = 164.081px = 2.278906in (matches `svek-1.dot`). Ours measures the raw markup string `"|= header 1 |= header 2 |= header 3 |"` (longest of the 3 source lines) as literal text, giving ~179.644px content + 21 margin = 200.644px = 2.786719in → Δwidth = 200.644-164.081 = +36.563px, exact match to the row. Height: jar's real table box is 39px (3 rows × 13) border-to-border + 2×2 `AtomWithMargin` = 43px, + `NOTE_MARGIN_V`(10) = 53px = 0.736111in. Ours: 3 lines × 13 (`NOTE_FONT_SIZE`) = 39px, +10 = 49px = 0.680556in → the TRUE per-node height delta is 49-53 = -4.0px, all on the note. State X's own height is an EXACT match on both sides (0.694444 both, verified via `scripts_scratch/T8/probe.ts`'s direct node dump) — it happens to sit numerically between our note's 49px and jar's note's 53px, so sorted-per-axis pairing splits the one true -4.0px note delta into two smaller rows: idx0 (-1.000, ours-note vs jar-state) and idx1 (-3.000, ours-state vs jar-note).
- **ruledOut:** (1) padding — `skinParam.getPadding()` in both jar and this port default to `same(0)` when `skinparam padding` is unset (`SkinParam.java:1124-1140`/`getAsDouble`), read directly; not the cause. (2) text-height model — jar's `StringBounderFromWidthTable.calculateDimension` (`klimt/drawing/font/StringBounderFromWidthTable.java:67-79`, the class `-DPLANTUML_DETERMINISTIC_TEXT=true` selects) and this port's `WidthTableMeasurer.measure` (`src/core/measurer.ts:186-193`) both return `height: font.size` flatly — no ascent/descent divergence. (3) the real ported `StripeTable`/`AtomTable`/`SheetBlock1` pipeline is CORRECT in isolation: `scripts_scratch/T8/probe2.ts` called `buildNoteBody` directly on the identical 3-line display and got width=143.081/height=43, exactly matching jar's content dimensions — the divergence exists only because state notes never route through it. (4) `EntityImageNoteLink.ts#measureLinkNoteDim` (the note-on-link sizer) was considered and ruled out: this note is `note right of X` (an entity note, not a link note), resolving through `state-note-layout.ts#measureNote`, a third, separate function.
- **pairingRisk:** likely — State X's exact-match height (0.694444 both sides) sits between our note's true height and jar's, turning one real -4.0px note delta into two reported rows; see causalChain.
- **sharedCauseWith:** xeziki-47-zomo866#a (identical `state-note-layout.ts#measureNote` mechanism, same note-with-creole-body shape). NOT shared with PARTITION.md's "1.0"/"3.0" repeated-Δpx groups (decede-10-buvu414, gokife-89-boja382, jijuze-43-ceva131, juvagu-33-dupa212, pacami-67-dafe414, tofezi-64-koda860, xojudi-20-keco020 / joleju-94-maru748) — those rows land at the same rounded magnitude by coincidence (none of those fixtures are notes; they are composite/concurrent-region/stereotype fixtures), and this fixture's OWN two rows are themselves a pairing artifact of a single -4.0px cause, not two independent -1/-3 defects.
- **proposedWriteSet:** src/diagrams/state/state-note-layout.ts (`measureNote`, `measureAllNotes`) — route through the real creole `buildNoteBody`/`StripeTable` pipeline (`src/core/svek/image/leaf-sizing.ts#measureNote`, already correct) instead of the raw-line-split model; `src/diagrams/state/renderer-note.ts` (the matching draw-time fallback) needs the same swap in lockstep, per `leaf-sizing.ts`'s own doc comment on sizer/renderer parity (the G2 defect class).
- **sizeEstimate:** 1 core sizing file + 1 renderer file + `state-composite-edge-label.ts` (a byte-identical duplicate per `EntityImageNoteLink.ts`'s own doc comment, though that file covers link-notes not entity-notes) — medium blast radius; every state note with creole markup or a table body in the corpus is a verification target, not just this bucket's 3.
- **confidence:** high
- **nextStep:** n/a

### tumaba-64-tosu281

- **bucketLabel:** note
- **rows:**
  | scope | axis | idx | ours (in) | jar (in) | Δpx |
  |---|---|---|---|---|---|
  | 2 | width | 3 | 1.339236 | 1.516806 | -12.785 |
  | 2 | height | 3 | 5.013888 | 5.013889 | -0.000 |
- **status:** resolved
- **mechanism:** `transitionLabelText` (`state-dot-graph.ts:114-120`) only reads `t.label`/`t.guard`/`t.action` — an inline `-->` label — and has no branch for `t.linkNote` (a `note ... on link`'s text). `attachTransitionLabel` (`state-transition-label.ts:200-201`) therefore returns `undefined` for the `State1-->State2` transition carrying `note right on link \n hi1 \n end note`, so `TransitionGeo.label` is never populated for it. This SPLITS jar's single mechanism into two disconnected halves in this port: `state-dot-graph.ts:183` (`noteDim: measureLinkNoteDim(t.linkNote, ...)`) still correctly feeds the note's real 48×33px box into the DOT graph's `labelWidth`/`labelHeight` layout attributes (confirmed byte-identical to jar's own dumped `WIDTH="48" HEIGHT="33"` FIXEDSIZE table), but nothing carries the graphviz-returned label POSITION back onto `TransitionGeo.label` for a link-note — so (1) the note is silently never drawn, and (2) `computeSvekResultGeometry`'s `addTransitionInk` (`layout-ink-extent.ts:386-394`, `if (transition.label !== undefined)`) can never fold the note's reserved horizontal space into `SubState`'s own ink extent, undersizing its declared width.
- **originFileLine:** src/diagrams/state/state-dot-graph.ts:114-120 (`transitionLabelText`) — the single site `attachTransitionLabel` gates on; needs a `t.linkNote` branch alongside `label`/`guard`/`action`.
- **javaRef:** ~/git/plantuml/src/main/java/net/sourceforge/plantuml/svek/SvekEdge.java:741-747 (`if (hasNoteLabelText() || link.getLinkConstraint() != null) { ... this.labelXY = hasNoteLabelText() ? TextBlockUtils.asPositionable(labelText, stringBounder, pos) : ... }` — jar folds a note-on-link's real graphviz-read-back position into `labelXY` UNCONDITIONALLY, the exact same field/ink path an inline transition label uses) + `SvekEdge.java:383` (`hasNoteLabelText()`) + `SvekResult.java:126-133`/`InnerStateAutonom.java:186-193` (the ink-extent recipe that then walks `labelXY`, mirrored by `computeSvekResultGeometry`/`addTransitionInk`).
- **causalChain:** Hand-derived from our own rendered SVG (`scripts_scratch/T8/probe5.ts`, real `renderSync` output, no cache): State1/State2/State3 sit at `x=19,width=60.425` each; applying `addStateBoxInk`'s own rule (divider present: `addPoint(x-1,y-1)`, `addPoint(x+w,y+h-1)`) over all three plus the `[*]` ellipse gives raw ink `[minX=18,maxX=79.425]`; `+INK_DELTA(15)` = `childImg.width=76.425px`. Feeding that through `measureAutonomWrapper`'s real formula (`mergedWidth=max(textWidth("SubState"),0,76.425)=76.425`, `+MARGIN*2+2*MARGIN_LINE(20)`) gives `96.425px = 1.339236in` — an EXACT, byte-for-byte reproduction of the harness's own `ours` value, proving no other term (node sizes, ellipse, transition points, wrapper margin, name text) is off. Back-solving the SAME formula from jar's target (`1.516806in×72=109.21px`, `-20` margin) gives jar's real `childImg.width=89.21px` — exactly `76.425+12.785`. The entire Δ is thus the SINGLE missing `childImg.width` term, and that term's magnitude matches the note's own DOT-reserved `labelWidth=48px` (confirmed present in our own raw `DotInputGraph` dump, `scripts_scratch/T8/probe5.ts`) — consistent with graphviz widening a labeled edge's rank to fit the label, an effect jar's ink-extent walk captures via `labelXY` (see javaRef) and ours cannot, because `TransitionGeo.label` is `undefined` for this edge (see mechanism).
- **ruledOut:** the bajelo-54-dixe684 `clusterPosMap:undefined`/nested-cluster mechanism (`composite-a.md`, now filed) — structurally inapplicable: `SubState` classifies `autonom`, not `cluster` (`isAutarkic`, `state-composite-detect.ts:201-216`: `subtreeIds` excludes a composite's OWN id, so `[*] --> SubState` does not break its autarky), and `State1`/`State2`/`State3` are plain leaves with no nested composite child, so the `materializeSpecs(..., undefined, ...)` `clusterPosMap` argument at `state-composite-autonom.ts:195` is never consulted for this pass — the `-12.03`/`-12.000` magnitude match to bajelo-54-dixe684 is coincidental, not a shared mechanism. DOT-input parity for BOTH the plain node sizes (byte-identical to jar's own `svek-1.dot`, diffed directly) and the note's label box (`labelWidth:48,labelHeight:33`, byte-identical to jar's dumped table) — ruling out any note-sizing (`measureLinkNoteDim`) error. Wrapper-margin/`measureAutonomWrapper` arithmetic error — ruled out by the exact hand-reproduction above. Ellipse/transition-point ink domination — ruled out via direct SVG coordinate extraction (both stay within `[18,79.425]`).
- **pairingRisk:** none (both sides' largest-width node in scope 2 is unambiguously the composite box; verified via direct node dump).
- **sharedCauseWith:** none confirmed. Checked composite-a.md/composite-b.md (T1/T2, now filed) and the raw `in.puml` for every composite-a fixture (`bajelo-54-dixe684`, `cupesu-59-sajo991`, `dapunu-39-kava045`, `decede-10-buvu414`, `duzazu-41-telu529`, `fotuje-06-fifa085`, `kinuca-03-nice683`, `lojeju-04-fadu517`, `nimana-36-veco708`, `nuvura-69-mafe604`) — none contains a `note ... on link` inside a composite scope, so no sibling fixture in this slice or T1's exercises this exact code path; flagged for SYNTHESIS to grep the full 94-fixture set for `note.*on link` nested inside `state X { ... }` in case a composite-b (T2) or precision (T11) fixture also exercises it.
- **proposedWriteSet:** src/diagrams/state/state-dot-graph.ts (`transitionLabelText`, add a `t.linkNote` branch returning the note text so `attachTransitionLabel` can attach a real `label`/`inkBox`) + src/diagrams/state/state-transition-label.ts (`attachTransitionLabel` may need a link-note-specific box/anchor conversion rather than the same `measureLabel`/inline-label path, since a note's own box formula (`roseNoteDim`) differs from an inline label's) + the renderer path that currently never draws `note ... on link` at all (confirmed via this task's own probe: no note elements anywhere in the rendered SVG) — likely `renderer-note.ts` or wherever `TransitionGeo.label` is drawn.
- **sizeEstimate:** 2-3 files, medium blast radius — this is a genuine missing-feature gap (note-on-link is never drawn AND never sized into its container), not a formula tweak; every state fixture with a `note ... on link` on an INTERNAL composite transition is a verification target (this task found only tumaba-64-tosu281 in the note/composite-a slices; a full-corpus grep is the proposed nextStep for SYNTHESIS).
- **confidence:** high — the arithmetic closes exactly (childImg.width term, 76.425 vs 89.21, Δ=12.785, matches the row to 3 decimals) and the mechanism is pinned to a single function (`transitionLabelText`) with both jar (`SvekEdge.java:741-747`) and our own DOT-input dump (`labelWidth:48`, present but unused downstream) directly read and cited.
- **nextStep:** n/a

### xeziki-47-zomo866#a

- **bucketLabel:** note
- **rows:**
  | scope | axis | idx | ours (in) | jar (in) | Δpx |
  |---|---|---|---|---|---|
  | 1 | width | 0 | 0.694444 | 0.641493 | +3.812 |
  | 1 | width | 3 | 2.892795 | 0.694444 | +158.281 |
- **status:** resolved
- **mechanism:** Same mechanism as fatupo-62-bemu777: `state-note-layout.ts#measureNote` measures the RAW, un-parsed source lines of note "n"'s body (`  <color:#FF000020>12⬤</color>`, etc.) as literal text, including the literal `<color:...>`/`</color>` markup characters — it never recognizes them as zero-width creole color directives. The longest raw line measures far wider than the visible "12⬤"-only content jar actually renders.
- **originFileLine:** src/diagrams/state/state-note-layout.ts:84-93 (same function as fatupo-62-bemu777)
- **javaRef:** ~/git/plantuml/src/main/java/net/sourceforge/plantuml/svek/image/EntityImageNote.java:114-118 (real `BodyFactory.create3`, which DOES run the display through creole `<color>` tag parsing) + ~/git/plantuml/src/main/java/net/sourceforge/plantuml/klimt/creole/legacy/StripeSimple.java (the `<color:...>` tag recognizer the real pipeline uses, confirmed present in this port's own `StripeSimple.ts` for non-table text — the gap is only that state notes never reach it)
- **causalChain:** Jar's real note width = 0.641493in×72 = 46.19px total, incl. `NOTE_MARGIN_H`(21) → ≈25.19px of visible "12⬤"/"13⬤"/"14⬤" content (color tags contribute 0). Ours measures the longest raw source line character-by-character via `WidthTableMeasurer` (treating `<`,`>`,`:`,`#` etc. as literal glyphs) → 2.892795in×72 = 208.28px total. Because jar's note (46.19px) is the SMALLEST node in this scope while ours (208.28px) is the LARGEST, sorted-per-axis pairing inverts which row maps to which true node: idx3 (Δ+158.281 = 208.28 - 50.0, ours-note vs a jar-state's exact 0.694444) is the dominant, note-caused row; idx0 (Δ+3.812 = 50.0 - 46.19, a jar-state's exact 0.694444 vs the TRUE note-vs-note residual pushed down to the smallest slot by ours' own inflation) is the pairing-collision remainder of the SAME cause, not an independent defect.
- **ruledOut:** padding, text-height model, and the real `StripeTable`/`AtomTable` pipeline's correctness in isolation — all ruled out identically to fatupo-62-bemu777 (this fixture has no table markup, so only the padding/text-height rulings transfer directly; the `<color>`-tag gap is confirmed separately via `state-sizing.ts`/`state-note-layout.ts` grep: neither imports anything from `klimt/creole/legacy/`, so no creole tag of any kind — table or `<color>` — is ever interpreted by either).
- **pairingRisk:** likely — jar's note is the smallest node in the scope, ours the largest; sort order fully inverts which raw row pairs with which true node (see causalChain).
- **sharedCauseWith:** fatupo-62-bemu777 (identical `state-note-layout.ts#measureNote` root cause — missing creole parsing generally, exercised here via `<color>` tags instead of a table). NOT shared with tumaba-64-tosu281 (that fixture's delta is unrelated — an inner-canvas/composite-geometry issue; its own note text is plain, non-creole, and measures correctly under the SAME naive model on a DIFFERENT code path, `EntityImageNoteLink.ts`).
- **proposedWriteSet:** same as fatupo-62-bemu777 — one mechanism, one fix (`state-note-layout.ts#measureNote` routed through the real creole pipeline).
- **sizeEstimate:** same as fatupo-62-bemu777.
- **confidence:** high
- **nextStep:** n/a

### xeziki-47-zomo866#b

- **bucketLabel:** note
- **rows:**
  | scope | axis | idx | ours (in) | jar (in) | Δpx |
  |---|---|---|---|---|---|
  | 1 | width | 2 | 2.236806 | 0.694444 | +111.050 |
- **status:** resolved
- **mechanism:** DISTINCT from #a. State entity DISPLAY NAMES are sized by `state-sizing.ts#measureLines`, which calls `measurer.measure(line, font)` directly on the raw split display text with NO creole parsing at all — this is a separate, dedicated non-creole sizing path for state labels (as opposed to note bodies). The `<color:#FF000020>` inline tag in `state "A<color:#FF000020>A" as A` is measured as 20 literal characters instead of being stripped/interpreted as a zero-width creole color directive.
- **originFileLine:** src/diagrams/state/state-sizing.ts:176-186 (`measureLines`), invoked at :191 (`measureLines(splitStateDisplayLines(state.display), font, measurer)`) for the state's own name/label.
- **javaRef:** ~/git/plantuml/src/main/java/net/sourceforge/plantuml/svek/image/EntityImageState.java:86 (`Display.create(entity.getBodier().getRawBody())`), :98 (`list.create8(...)`, the real creole-aware `TextBlock`), :104-106 (`calculateDimensionSlow` uses that block's `.calculateDimension()`)
- **causalChain:** Verified by an isolated probe (`scripts_scratch/T8/probe4.ts`, `state "A<color:#FF000020>A" as A` alone, no note in scope): our width = 2.236806in = 161.05px for a visually 2-character label ("AA"), while a plain uncolored sibling state ("Plain") measures 0.711632in = 51.24px for the SAME 2-glyph shape — confirming the ~110px excess is entirely the raw `<color:...>` markup being measured as literal characters. Jar's real width for state "A" (creole-parsed, tag contributes 0) is 0.694444in = 50.0px, matching the ordinary state baseline. Δ = (161.05-50.0) = +111.05, exact match to the row (idx2, since idx3 is already consumed by #a's dominant note row and idx0/1 by the other two states' exact 0.694444 values).
- **ruledOut:** NOT a `state-note-layout.ts` bug — that file only handles `note` entities and note-on-link edges (confirmed by reading it: no state-name/label code path exists there). `state-sizing.ts` is confirmed (grep) to import nothing from `klimt/creole/legacy/` at all — a completely separate, non-overlapping sizing path from #a's.
- **pairingRisk:** likely — this row's jar value (0.694444) is state B or C's ordinary width, not directly "A"'s own physically-paired jar row; the true per-node values happen to coincide numerically (every plain/uncolored state in this scope is 0.694444), so the VALUE is correct even though the sort has reshuffled which physical row maps to which physical node (see #a's causalChain for the same scope's full inversion).
- **sharedCauseWith:** none identified within this slice. Likely shares `state-sizing.ts:176-186` with OTHER buckets' fixtures that also apply inline creole markup (`<color>`, `<b>`, etc.) to state names (skinparam-style/stereotype buckets use per-state colors too, though typically via `skinparam`/`#color` suffix rather than inline `<color:>` markup) — flagged for SYNTHESIS to cross-check T5/T7's records against this file before assuming no overlap.
- **proposedWriteSet:** src/diagrams/state/state-sizing.ts (`measureLines` and its 3 callers: state label, description, and fields) — needs real creole-aware measurement (reusing `leaf-sizing.ts`'s `buildDesc`-based pipeline, the same fix direction as the note bug) instead of raw `measurer.measure` on unparsed text.
- **sizeEstimate:** `state-sizing.ts` is a hub — name/description/fields all route through `measureLines` — wide blast radius across every state fixture with inline creole markup in its name/body; larger than the note fix (proposedWriteSet is one function change but the verification surface is every styled state label in the corpus, not just this bucket's fixtures).
- **confidence:** high
- **nextStep:** n/a
