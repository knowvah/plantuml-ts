# Decision journal — state-declared-size-diagnosis

| When | Task | Decision | Why | Evidence |
|---|---|---|---|---|
| 2026-08-18 | start | Branch `docs/state-declared-size-diagnosis` created at `e544038d`; README stop 7 (`.claude/catalog.md` absent) read as a "do not create" guard, not a halt | Same clause in SI27 README stop 9 was honoured by not creating the file; file has never existed in git history | `git log --all -- .claude/catalog.md` empty |
| 2026-08-18 | T0 | Baseline pinned: two runs byte-identical, sha256 `b790fabcfcf3511d291d4bf1f0985ee98c26b0c00509983b34d0dc5b533505e0`; summary `fixtures 272, declarations 2654, exact 2481, mismatched 144, lastDigitOnly 29, unmatched 4, dirty 79` — README numbers unchanged | Exit bar 4 / gate 6 ruler | `test-results/state-declared-size-baseline.jsonl` (gitignored) |
| 2026-08-18 | T0 | Pseudo-state classifier pattern matched case-insensitively (only that pattern) | Spec regex had no flags; `<<entrypoint>>` lowercase in bitaxo-18/resido-15 would move them to `stereotype`, contradicting the preview and T4's fixture list; PlantUML stereotype names are case-insensitive | PARTITION.md == preview (63/27/4, buckets 10/10/8/7/7/6/5/3/3/4) |
| 2026-08-18 | T0 | `check-schema.py` skips the slug-completeness check when 0 records exist; also emits `findings/partition.json` (machine-readable slug sets) | T0 acceptance requires `0 records, 0 violations` on the empty dir while the gate requires 94 at close-out; both hold with this rule | negative test (empty originFileLine) exits 1 |
| 2026-08-18 | T0 | Orchestrator executed T0 directly (no subagent) | Mechanical: harness runs, generated tables, a 90-line checker; the sha must land in the journal anyway | — |
| 2026-08-18 | gate | Batch 0 gates: test 601 files/14599 pass (95.4/90.4/96.9), typecheck ✓, lint ✓, build ✓, diff has no src/tests/oracle/scripts path, schema `0 records, 0 violations` | — | — |
| 2026-08-18 | batch 1 | Execution plan: run 1a+1b as ONE 13-way parallel batch (T1–T13), all `typescript-pro` (ADR-9), each writing only its `findings/<name>.md`; probes confined to `scripts_scratch/T<N>/` (per-task subdir, deleted before finish); temporary `src/` tracing FORBIDDEN for these agents (13 agents share one worktree — a src edit would corrupt others' measurements); agents run no git and do not write `.agent-notes/` (observations come back in the report; orchestrator files them) | README allows the merged batch; the schema gate needs all 13 files to reach 94 records, so a split batch would fail gate 7 after 1a alone | write-sets disjoint by construction |
| 2026-08-18 | T9 | Done: 3/3 resolved, all `state-sizing.ts:182/210` (no creole strip before measure; jar `Display.create8` strips). Flag for T14: state sizing never strips creole anywhere (state-sizing, composite-header:31, composite-sizing:34, leaf-node:55, json-sizing, note-layout:87) — a cross-cutting group candidate with T8's note fixtures | agent report | commit T9 |
| 2026-08-18 | T12 | Done: 4/4 `divergence-proposed` — all jar ERROR renders (0 svek-N.dot). cagego/xacona: `StateDiagram#checkConcurrentStateOk` guard absent in `state-parse-resolve.ts:358`; fugedo: jar root-only dotted-path (`CucaDiagram.java:250-288`) vs our hierarchical walk; zecivu: sequence-before-state dispatch (`PSystemBuilder.java:135`) vs our whole-doc scan. Nothing pairable — feeds T13 nothing | agent report | commit T12 |
| 2026-08-18 | T5 | Done: 5/7 resolved (A: `[[link]]` measured raw, `state-sizing.ts:207`, +2.55 ×3; B: `wrapWidth` unimplemented for state labels, 445/−40 ×2), 2 unresolved (kejabo/pavuzo composite ink box vs dot-engine routing, distinct nextSteps → stop 6 not triggered). Note: `[[link]]` raw-measure is the same family as T9's no-creole-strip — T14 to reconcile | agent report | commit T5 |
| 2026-08-18 | T4 | Done: 7/7 resolved. −36 ×4 fixtures = `state-leaf-node.ts:44` expansion pins ignore rankdir (`EntityPosition.java:120-128`); resido-15 = `<<O-O>>` +10 (`EntityImageState.java:108-110`, shared with T7 dogeji/viguto); bitaxo-18 +10 is a DIFFERENT cause (`hide empty description` not threaded to `buildLeafNode`, `state-leaf-node.ts:65`) — the |Δ|=10 ×5 group is two causes, reconciled; mefici-97 raw `**bold**` (T9 family). Agent-notes candidate: `state-entity-position.ts:110-112` comment claims no corpus fixture exercises EXPANSION_* — falsified by 4 fixtures | agent report | commit T4 |
| 2026-08-18 | T10 | Done: 2 resolved (fovafu-44#a + tubojo-49 = dotted-id display overwrite, `state-parse-resolve.ts:379`, +5.788), 3 unresolved (bunade-42/nimise-04 = RE-PIN label-position group of six with bajelo/fotuje/nimana/pavuzo, `layout-ink-extent.ts:386-392`; fovafu-44#b nested wrapper margin, arithmetic 4.594+6 = 10.594 matches). Two nextSteps distinct | agent report | commit T10 |
| 2026-08-18 | T6 | Done: 6/6 resolved (7 records). One root for 5: field lines never creole-routed (`state-sizing.ts:209-210`, `EntityImageState.java:98-99`) — `<math>` (corumi/gupeto +79.675), `<sup>`+tab-stop (juvagu), `**` (fibudu#b), wrapWidth (kubona +373/−40), tabSize stop (lokija). fibudu#a −28 = trailing-`\` continuation unported (`ReadLineReader.ts:45-59`), shared with T1 duzazu / T2 vixobo, NOT the pseudo-state −28. Hint's tabSize→divergence-proposed rejected: tab-stop is part of the same missing creole pipeline. Agent's claim that kubona's −40 row was absent from PARTITION is wrong (it is present) — record content unaffected | agent report; verified PARTITION | commit T6 |
| 2026-08-18 | T13 | Done: pairingRisk none 73 / possible 17 (all exact ties) / likely 0, proven by exhaustive pairwise re-pairing over 205 scope-instances; counters match harness exactly; label-text pairing infeasible (jar dot has `label=""`), declaration-order after `shape==='point'` filter viable (~40-60 lines). T13 calls the 4 unmatched "absent-oracle caching gap"; T12 showed they are jar ERROR renders — consistent (no dot because jar errored) but T14 must state it that way, not as re-cache candidates. Agent-notes candidate: `__init_`/`__zaent_` id filter is asymmetric with jar (jar `[*]` is `shape=circle`, anchors are bare `zaent0003`) | agent report | commit T13 |
| 2026-08-18 | T2 | First pass returned 10/10 `unresolved` (vixobo-14 with `confidence: high`, three trios with nextSteps that are probes it could run). Resumed the agent (SendMessage, context intact) to execute its own nextSteps and reconcile vixobo-14 with T6's resolved fibudu#a instead of accepting deferred instrumentation | diagnosis.md: "instrument next" is an in-progress state, not a stopping point when the instrument is available; stop 6 not triggered (nextSteps are per-group probes, not a missing tool) | composite-b.md first pass |
| 2026-08-18 | T3 | First pass 2/8 resolved (fimivu-15 `--`/`||` separator not distinguished, `state-composite-sizing.ts:121-128` vs `ConcurrentStates.java:63-89`, exact); 6 unresolved "within budget" with runnable probe nextSteps. Resumed the agent to execute them (same rationale as T2) | diagnosis.md | concurrent-region.md first pass |
| 2026-08-18 | T8 | First pass: fatupo-62 + xeziki-47#a resolved (`state-note-layout.ts:84-93` raw-measures note body, no creole/table), xeziki-47#b resolved (`state-sizing.ts:176-186`, T9 family); tumaba-64 unresolved with a hand-off nextStep. Resumed to trace it (agent already had the ablation repro) | diagnosis.md | note.md first pass |
| 2026-08-18 | T11 | First pass: cluster A (18 fixtures, ≤7.2e-5 px) resolved = engine X/Y never echo-corrected (`graph-layout.ts:189-194`, `ROUND_TRIP_EPSILON` applies to size only) — real, not formatting; cluster B (14 records, 0.001–0.005 px) unresolved with ONE shared nextStep "diff width-table entries" — that is a runnable probe, not a missing tool (stop 6 not triggered); resumed to run it. Agent-notes candidate: CLAUDE.md "shells out to real graphviz" text is stale vs `plans/burn-graphviz-engines` | diagnosis.md | precision.md first pass |
| 2026-08-18 | T1 | First pass 6/10 resolved (kinuca creole table raw-measured `state-composite-sizing.ts:73`; duzazu backslash continuation `state-commands.ts:258-267`; bajelo/cupesu/lojeju/nuvura = `buildPlainAutonomSpec` passes `clusterPosMap: undefined` (`state-composite-autonom.ts:195`) → `boundingBox` fallback drops cluster title/margin, medium confidence, arithmetic not closed), 4 unresolved with distinct probe nextSteps. Resumed to close arithmetic + run probes; T1's cluster candidate is the same as T3's (`state-composite-geo.ts:380`) — cross-cite requested | diagnosis.md | composite-a.md first pass |
| 2026-08-18 | T7 | Done: 4/5 resolved (`<<O-O>>` +10 term missing, `state-sizing.ts:206-214` vs `EntityImageState.java:107-112`, clamps explain −2.575/−8), gokife-89 unresolved = −1 px composite family with T1 decede / T2 pacami trio. Schema check now reports 94 records (all files present) | agent report | commit T7 |
| 2026-08-18 | T11 | Second pass: width-table hypothesis REFUTED by instrumenting every `measure()` call; cluster B is the same position-residual mechanism as cluster A on the width axis → 30/30 resolved, one mechanism (`graph-layout.ts:189-194`, `SvekResult.java:130-136`) | agent report | commit T11 |
| 2026-08-18 | T8 | Second pass: tumaba-64 RESOLVED — `transitionLabelText` (`state-dot-graph.ts:114-120`) ignores `t.linkNote`, so `attachTransitionLabel` yields no label and `addTransitionInk` never folds the note's space (jar `SvekEdge.java:741-747` folds it via `labelXY`); the note is also not drawn. Back-solved gap 12.785 px = whole row. bajelo's −12.03 is coincidental (autonom, no nested cluster) | agent report | commit T8 |
| 2026-08-18 | T2 | Second pass: 7/10 resolved — −1 px trio = `addNodeInk` misses `RoundedSouth` south-cap uninset path (`RoundedSouth.java:65-83`, `LimitFinder.java:159-162`; 115 = 99+1+15 exact; T14: also decede-10 (T1) and gokife-89 (T7)); −1.34 trio = self-loop arrowhead ink excluded at `layout-ink-extent.ts:522` (union gives 85.338 vs jar 85.34); vixobo-14 resolved (ReadFilterMergeLines). rovese/zoriza/zizemo: "missing cluster-margin path" REFUTED (materializeCluster already uses real cluster box); unresolved with nextStep (ClusterHeader/ClusterDotString uninset term + probe label wiring) | agent report | commit T2 |
| 2026-08-18 | T1 | Second pass: 8/10 resolved — bajelo/cupesu/lojeju/nuvura closed bit-exact on `clusterPosMap: undefined` (`state-composite-autonom.ts:195`; second origin site `state-composite-concurrent.ts:129` → T3); decede-10 = T2's RoundedSouth; nimana-36 = T10's label-position. dapunu-39 (2.033 px short on every ink hypothesis) and fotuje-06 (3-deep cluster chain, same mechanism structurally, numeric closure failed on probe tree assembly) unresolved with nextSteps. Flag for T14: T10's RE-PIN group of six wrongly lists bajelo-54 and fotuje-06 (no −3.836 rows on them) | agent report | commit T1 |
| 2026-08-18 | T3 | Second pass: 5/8 resolved — darime/giniti/lumamo close exactly on `clusterPosMap: undefined` at `state-composite-concurrent.ts:129` (same bug as T1's `state-composite-autonom.ts:195`; monkeypatched from a probe, src untouched); giniti's tab guess disproven by A/B; jetuse height closes, width 5 px residual; jijuze re-diagnosed to `REGION_LEAF_MARGIN`/`tightContentDimension` (`state-composite-cluster.ts:197-265`); zacajo DOT byte-identical to jar → external (graphviz drift, METRIC-AUDIT candidate); joleju note-only-region stacking, retracted fatupo link. Removed stray `findings/.agent-notes/.gitkeep` + `findings/.gitignore` (Serena activation artefacts, no task write-set) | agent report | commit T3 |
| 2026-08-18 | batch 1 | Batch 1 complete: 13 files, 94 records, 0 violations; resolved/unresolved tally to be computed by T14. Unresolved: T1 dapunu-39, fotuje-06; T2 rovese-43, zoriza-41, zizemo-86; T3 jetuse-93, jijuze-43, zacajo-09, joleju-94; T5 kejabo-83, pavuzo-79; T10 bunade-42, nimise-04, fovafu-44#b — every one carries probe evidence and a concrete nextStep | — | — |
| 2026-08-18 | gate | Batch 1 gates: harness byte-identical to T0 baseline; no src/tests/oracle/scripts path in diff; test 601/14599 pass (90.4 % branches); lint ✓; build ✓; schema `94 records, 0 violations`; typecheck ✓ on 3 consecutive runs — ONE transient failure (`include-resolver-node.ts` TS2591 "Cannot find name 'node:fs/promises'") when run right after `npm test` in the same shell, not reproducible; tree unchanged (docs-only), treated as an environment flake, not src drift | gate rule: docs-only tree, failure means src drift — verified no drift by `git status`/`git diff` | — |
| 2026-08-18 | batch 2 | T14 launched (Opus, `general-purpose`, ADR-9) with the journal's cross-bucket contradictions enumerated in the prompt; agent runs no git. Orchestrator filed `.agent-notes/si28-state-declared-size-observations.md` (four observations agents surfaced). T11's claim that CLAUDE.md's "shells out to real graphviz" is stale was checked and REJECTED: that sentence describes the JAR (which is why its geometry is a target), not this port | ADR-8; memory.md | — |
| 2026-08-18 | T14 | Done: SYNTHESIS.md — 24 true-cause groups over 100 records / 94 fixtures; tally records 81 resolved / 15 unresolved / 4 divergence-proposed (fixtures 75 / 1 partial / 14 / 4); all 28 repeated-|Δ| reconciled (13 one-group, 15 split); fix mission `state-declared-size-fix` proposed in 5 batches with disjoint write-sets, G14 (precision, ≤0.005 px) explicitly not scheduled; RE-PIN group settled as 3 (T1 right, T10's six wrong); gokife-89 refuted from the RoundedSouth quartet; clusterPosMap = one group two call sites. Orchestrator applied the two flagged corrections: `fix(T5)` kejabo −0.000 row, `fix(T13)` withdraw re-cache. T14's CLAUDE.md "stale" claim rejected for the same reason as T11's (sentence describes the jar) | agent report + verification | commit T14 |
| 2026-08-18 | gate | Final gates on the T14 tree: harness byte-identical to baseline; no src/tests/oracle/scripts path; test 601/14599 (95.41/90.40/96.93/96.50); typecheck ✓; lint ✓; build ✓; schema 94/0 | — | — |
