# Decision journal — sequence-oracle-harness

| When | Task | Decision | Why | Evidence |
|---|---|---|---|---|
| 2026-08-20 | orchestrator | Branch `feat/sequence-oracle-harness` cut from `main` @ bd20b917 | Brief says "from main at or after c7751ee1"; bd20b917 is the brief commit itself, a descendant | `git log --oneline -1` |
| 2026-08-20 | orchestrator | Batch 0 execution plan: T0 + T1 in parallel (disjoint write-sets), sonnet each | Brief batch-0/overview.md declares them parallel; T0 writes only `test-results/**` + its note, T1 only `tests/oracle/svg-conformance/render-fixture-sequence*` + its note | batch-0/overview.md |
| 2026-08-20 | orchestrator | Injected 3 prior observations into T0's prompt (absolute out-dir; non-zero exit still writes in.svg; batch-render .dot collision) | `~/.claude/rules/parallelism.md` §0 — pre-load `.agent-notes/` findings that bear on the write-set rather than hoping the agent rediscovers them | `.agent-notes/si16-oracle-cache-recapture.md` |
| 2026-08-20 | T0 / orchestrator | **STOP 9 raised** — the classified "sequence" corpus is ~18% non-sequence. Capture continues; re-pin and gates held. | `populate-corpus.py:20` `TYPE_PATTERNS[0]` is `sequence` and its first pattern `r"^\s*\w[\w ]*->[\w ]"` matches any `A -> B` line; `detect_type` returns on FIRST match, so usecase/class/state/timing fixtures containing a plain arrow are claimed by sequence. Hence 1427 classified vs the brief's ~473 `participant`/`actor` estimate. D3 names the classifier as the selection mechanism, so narrowing it is not an orchestrator call. | Over the first 164 captured: 135 `data-diagram-type="SEQUENCE"`, 29 not (DESCRIPTION 10, CLASS 7, TIMING 5, STATE 4). 17 slugs carry a `svek-*.dot`; **all 17 are non-SEQUENCE, zero SEQUENCE slugs emit DOT** — T0's "no svek-*.dot" acceptance criterion is satisfiable exactly when the set is sequence-only. |
| 2026-08-20 | orchestrator | **D3 amended, user-approved**: classifier = candidate generator; admission gate = the jar's own `data-diagram-type="SEQUENCE"` stamp on `in.svg`. Rejected candidates leave no cache entry. | Mechanical and jar-authored, so the corpus is sequence-only without any agent judgment about what a diagram "looks like"; makes T0's "no `svek-*.dot`" criterion true by construction and keeps the diff-baseline measuring one engine rather than four under one label. | `decisions.md` "D3 amendment"; AskUserQuestion 2026-08-20, option "Filter by jar diagram type" |
| 2026-08-20 | T1 | Accepted and committed `b9905252`. Single-measurer property verified as an ASSERTION, not an arrangement | A counting measurer over a bare/titled pair: `titledCalls > bareCalls` is only true if `applyChrome` measured the title through the passed instance; a second internal measurer would leave the counts equal | `render-fixture-sequence.test.ts:60-80` |
| 2026-08-20 | T1 / orchestrator | T1's `npm test` wall-clock (61.5–62.6 s, over the 60.3 s ceiling) is **CONFOUNDED, not a stop 11** | T0 was concurrently running ~1427 JVM starts in the same tree during both measurements; T1's own 2-file write-set (4 tests, 4.09 s standalone) cannot explain a suite-wide slowdown. Re-measure on an idle tree at batch-gate time before judging the ceiling | T1 report; `.agent-notes/g1h-T1.md` |
| 2026-08-20 | T0 / orchestrator | **Stop 9 #2 resolved by orchestrator** — `dasutu-58-saje713` stays in the corpus and its two `svek-*.dot` are KEPT, not deleted. Amendment's Evidence footnote corrected; its Decision clause untouched | The fixture is correctly `SEQUENCE`-stamped, so the admission rule is not contradicted — only the sample-based claim "zero SEQUENCE emit DOT" was over-strong. Verified upstream: `EmbeddedDiagram.java` declares `EMBEDDED_START = "{{"` and dispatches a bare `{{` to `"uml"`, so the note's `object o1 {…}` renders as a nested OBJECT (DOT-backed) diagram; `-DPLANTUML_DUMP_DOT` is JVM-wide, not diagram-scoped. Deleting faithful jar output to make an invariant read "zero" would be fitting, and would be re-violated by every honest re-capture — so the invariant is restated as the singleton set instead | `~/git/plantuml/src/main/java/net/sourceforge/plantuml/EmbeddedDiagram.java` (`EMBEDDED_START`, the `suffixLen == 0 -> "uml"` dispatch); orchestrator re-verified on disk: 1141 dirs, 2 dot files in 1 slug, 0 non-SEQUENCE survivors, 0 missing `.done` |
| 2026-08-20 | T0 / orchestrator | `xobebi-29-jilu859` excluded as **structurally unrepresentable**, not as a capture failure — a better reason than T0's report gave | The fixture is `@startuml file4` + `newpage`: **multi-page**, so the jar writes `file4.svg` AND `file4_001.svg`. The cache entry contract is a single `in.svg` and cannot hold two pages. The `@startuml file4` naming only changes the basename; `newpage` is the actual blocker. Recorded as a gap the rebuild inherits — multi-page sequence has no oracle entry — and consistent with `SequenceDiagramAST` having no `.pages` field (T1's omission #1) | `tests/corpus/sequence/xobebi-29-jilu859.puml` read directly |
| 2026-08-20 | orchestrator | Verified T0's report against disk rather than taking it at face value | `~/.claude/.../verify-agent-claims-si31` — SI31/SI32 both produced confident subagent claims that measurement disproved. Every number in T0's report reproduced exactly; its stop was correct and its one imprecision (the `xobebi` reason) was caught by re-reading the fixture | commands + output in this session |
| 2026-08-20 | orchestrator | **Batch 0 gates GREEN**, run by the orchestrator on the committed tree | `npm test` exit 0, 620 files / 14809 passed + 1 todo, vitest Duration **56.15 s** (`/usr/bin/time` real 56.33 s) — under the 60.3 s ceiling. Coverage 95.42 / 90.33 / 96.94 / 96.51, all ≥ 90. typecheck 0 · lint 0 · build 0 (the 3 `[unplugin:dts]` notes are the pre-existing ones). Parity ratchets verified per-file, not just in aggregate: state **270**, class **721**, description **357**, object **80** — all at their required counts; layering + catalog green. `manifest-diff.py`: "OK: 0 expected moves, 0 unexpected". `git diff --name-only main..HEAD -- src/` EMPTY | this session's command output |
| 2026-08-20 | orchestrator | T1's confounded 61.5–62.6 s reading **confirmed confounded**; no stop 11 | The same suite measures 56.15 s on an idle tree. The delta was T0's ~1427 concurrent JVM starts, exactly as predicted when the reading was quarantined rather than acted on | 56.15 s vs 61.5–62.6 s, same tree |
| 2026-08-20 | orchestrator | Brief wording inaccuracy recorded, NOT a stop: `test-results/render-manifest-baseline.json` cannot be re-pinned "in its own commit" — it is gitignored (`.gitignore:24`) and untracked | Established repo convention, not a gap. SI32's own T0 commit `1c674757` says these baselines "live under test-results/, which is gitignored and re-pinned by every batch gate"; the *tracked* copy SI32 added was of the HARNESS pin (`tests/fixtures/si32-harness-baseline.jsonl`), a different artifact. Nothing in this mission's exit criteria depends on the manifest baseline being tracked | `git ls-files --error-unmatch` fails on it; `git check-ignore` exit 0; SI32 `1c674757` commit body |
| 2026-08-20 | orchestrator | Batch 1 plan: T2 alone (serial), opus. Injected into its prompt: the real corpus size (1141, not ~473), the `dasutu-58-saje713` DOT exception, the multi-page exclusion, and the CLEAN 56.15 s baseline it must renegotiate from | Batch 1 has one task by design. T2 sets the gate's semantics and the new wall-clock ceiling; it must not inherit T1's confounded 61.5–62.6 s reading as its starting point | batch-1/overview.md; batch-0 gate output |
| 2026-08-20 | T2 / orchestrator | Baseline shape verified against description rather than against the task file's shorthand | The task file's contract reads `{slug, diffs}`, but its own instruction is "mirror description's field names exactly rather than inventing new ones". `oracle/goldens/svg-description/diff-baseline.json` uses **`diffCount`**, and T2 used `diffCount`. Correct — `diffs` was shorthand in the prose, not the contract | both files compared directly |
| 2026-08-20 | T2 / orchestrator | D2's error rule verified on disk, not taken on report: all 3 error entries carry `diffCount: **null**` plus a `reason` | D2 forbids an error ever carrying a numeric baseline, so a fixture that stops erroring cannot be misread as "reached 0 diffs". All 3 are `!include` stdlib-bundle resolution failures (`tupadr3`, `logos`, an `!includedef` macro) — a legitimate, well-explained class, not a harness defect | `diff-baseline.json` error entries |
| 2026-08-20 | orchestrator | **Measured, not interpreted: 1010 of 1138 baselined fixtures (88.8%) sit at exactly 12 diffs.** Corpus min **10** / median 12 / max 139, total 16462 diffs, **zero fixtures at 0**. *(Corrected: an earlier row of mine said min 5 / 16526 total. Wrong — I sampled `diff-baseline.json` while T2's rise-proof had temporarily perturbed a fixture to 5. Same concurrent-access root cause as the row below, in the opposite direction. T2's reported min 10 was right and mine was not.)* | A single value covering 88.7% of the corpus is the signature of one systematic per-fixture divergence, not 1138 independent ones — the ranked work queue the mission exists to produce. Flagged to T4 as its headline question ("what are the 12?"), to be answered by committed code per D5. I am NOT asserting the cause here; that would be exactly the SI32 failure mode D5 guards against | `diff-baseline.json` histogram: 12→1009, 31→62, 11→15, 10→7 |
| 2026-08-20 | T2 / orchestrator | **The ratchet was verified to actually FAIL**, by the orchestrator, not taken on report | A ratchet nobody has seen fail is not a gate. Perturbed `SequenceArrows_0001_Test`'s baseline 12→3 and re-ran: 1 failed / 1149 passed, message `diff count REGRESSED -- baseline=3, now=12` naming fixture, baseline and live count, plus "do not paper over a real regression". The live count was RE-COMPUTED from a real render (it produced 12 against a baseline of 3), so the suite measures rather than echoes. Baseline restored and `cmp`-verified byte-identical | this session's command output |
| 2026-08-20 | T2 / orchestrator | Suite confirmed to do real work, not skip: 1150 tests, **0 skipped**, 2.03 s of test time standalone | The failure mode this repo has hit before is a suite that silently reports conformance without measuring (`oracle-freshness.test.ts`'s own reason for existing). Checked explicitly rather than inferred from a green tick | `npx vitest run sequence.diff-baseline.ratchet.test.ts` |
| 2026-08-20 | T2 / orchestrator | **Ceiling stays 60.3 s** — T2 measured a rise the brief anticipated and did not find | 4 consecutive idle-tree runs: vitest Duration 55.42/55.73/55.82/55.56 s (spread 0.40 s), 4.5 s headroom. The new suite adds 1150 tests but only ~1.7 s of test time — it packs into an existing worker rather than extending the critical path. T2 also identified the real confounder behind T1's 61.5–62.6 s reading better than I had: the **Spotlight reindex** of T0's 1141 new files (`suggestd`/`corespotlightd` ~120% CPU, load1 11.7), not the JVM starts | README "Method (T2, 2026-08-20)" |
| 2026-08-20 | orchestrator | Risk taken and closed: I mutated `diff-baseline.json` (T2's write-set) to prove the gate while T2 may still have been live | Restored byte-identical immediately and re-verified with `cmp`; re-checked again at commit time. Flagged so the next orchestrator does the perturbation in a scratch COPY instead, or waits for the owning agent to finish | — |
| 2026-08-20 | orchestrator | **Batch 1 gates GREEN** on the committed tree | `npm test` exit 0, **621 files / 15959 passed + 1 todo**, Duration **54.99 s** (ceiling 60.3 s). Coverage 95.44 / 90.47 / 96.94 / 96.52. typecheck 0 · lint 0 · build 0. `manifest-diff.py` "OK: 0 expected moves, 0 unexpected". `git diff main..HEAD -- src/` EMPTY. Write-set exactly T2's four declared paths | this session |
| 2026-08-20 | T2 | **OPEN, carried forward, NOT written off as a flake**: `tests/integration/stdlib-remote-e2e.test.ts` failed 2 tests in 1 of 7 of T2's runs (`Cannot find module .../stdlib-tupadr3/generated/tupadr3.remote.js`), and did not reproduce in the other 6 or in either of my own batch-gate runs | T2 ruled out its own write-set, a second in-run writer (`buildStdlibPackages` has exactly one caller, `build-stdlib-globalsetup.ts:87`), `prepack` (targets `assets/`, not `generated/`), and the preceding build (controlled experiment `npm run build && npm test` → green) — but could NOT state a positive mechanism, so per `rules/diagnosis.md` it recorded an in-progress state rather than a candidate fix. Pre-existing infrastructure, outside every write-set in this mission; not a stop. T6 must surface it as a follow-on | `.agent-notes/g1h-T2.md` |
| 2026-08-20 | orchestrator | Batch 2 plan: T3, T4, T5 in parallel — disjoint write-sets, all depending only on T2's landed baseline. T4 opus, T3/T5 sonnet | batch-2/overview.md declares them parallel; verified no overlap: T3 owns `ratchet.json`/`README.md` under `svg-sequence/`, T4 owns `diff-census.json` + the census module, T5 owns two pre-existing cross-type files. **All three are told to leave `diff-baseline.json` READ-ONLY** — this session already produced two bad measurements from concurrent access to a live agent's file, in both directions | batch-2/overview.md; the two race rows above |
| 2026-08-20 | T5 / orchestrator | **Cross-surface inconsistency caught**: T5's census reports `errors: 1`, T2's baseline records **3**. Two committed surfaces disagree about the same corpus | Mechanism, read from both files: `scripts/svg-conformance-census.ts:234` renders with `{ includeStore: censusIncludeStore() }` — `withStdlib(..., buildStdlibAssetsStore())`, the VENDORED stdlib — whose own comment states it exists "so `<bundle/...>` fixtures render instead of erroring (mirrors the dot-sync-report/parity-ratchet wiring from T9)". `sequence.diff-baseline.ratchet.test.ts:109` calls `renderFixtureSequence(markup, new DeterministicMeasurer())` with **no options**, so every `!include <bundle/…>` fails. The 2 stdlib-bundle fixtures (`tupadr3`, `logos`) resolve under the census and error under the ratchet; the third (`nuvoja-46-dezu541`, `!includedef 'macro'`) is not a bundle and errors under both | `svg-conformance-census.ts:168-174,234` vs `sequence.diff-baseline.ratchet.test.ts:109` |
| 2026-08-20 | orchestrator | Decision: **fix the ratchet to pass the same stdlib store**, re-pin, rather than documenting the divergence | The ratchet is the surface that diverges from the repo's established convention, not the census. Leaving it costs 2 fixtures of real measurement and leaves the mission shipping two deliverables that disagree — precisely the "gate that cannot detect its own bad input" class D7 exists to prevent. Only fixtures with unresolvable includes can change, so the blast radius is bounded and verifiable by diffing the re-pinned baseline. Caught only because T5's independent surface was cross-checked against T2's | — |
| 2026-08-20 | T4 / orchestrator | **The mission's most important finding, verified at source not taken on report**: for **88.8% of the corpus the diagram body is never compared at all** | All 1010 fixtures at exactly 12 diffs share ONE identical path-set, including `svg/g[1][childCount]`. `compare.ts:353` reads `return; // structural mismatch — stop recursing into children` — I read the branch directly. So their "12" measures reaching the body and STOPPING, not body fidelity. Root chrome (6 absent root attributes, 4 root geometry, 2 child counts) is the whole of it | `compare.ts:347-354`; `diff-census.json` |
| 2026-08-20 | orchestrator | Consequence accepted and being documented rather than designed around: closing root chrome will trip the ratchet on ~1010 fixtures **at once**, and that is progress, not regression | The gate cannot distinguish "got worse" from "now comparing deeper", because both raise the count. Rather than weaken the gate (which would forfeit the one thing the mission delivers), T2 is amending its doc comment and failure message to state the mechanism, cite `compare.ts:353`, and distinguish the mass-rise signature (concentrated on baseline-12 fixtures ⇒ re-baseline deliberately) from an isolated rise (still the regression the gate exists for) | T2 resumed with the instruction |
| 2026-08-20 | T4 | `other` = 8624 (52%) accepted as an honest result, not a bucketing failure | Fully enumerated by T4: 6812 absent root attributes, 1420 `rect`-where-jar-has-`text` tag substitutions, 202 `fill`, 110 text content, 50 `stroke-width`, 22 `stroke`, 5 `stroke-dasharray`, 3 singletons. T4 deliberately kept `missing-element`/`extra-element` about ELEMENTS (read off `[childCount]`, the only place the comparator counts nodes); folding absent ATTRIBUTES in would cut `other` to ~11% by mislabelling 6812 records. D5 explicitly blesses a large honest `other` over an invented bucket | `.agent-notes/g1h-T4.md`; totals sum to 16462, matching the baseline exactly |
| 2026-08-20 | T4 | `format-units` = 0 is a measurement, not a gap | No value pair is equal modulo unit suffix or case — consistent with D1's own note that `width="371.513"` vs `166px` normalizes to a single numeric diff because `width` is in `NUMERIC_ATTRS` and `parseFloat("166px")` is `166` | D1; `diff-census.json` |
| 2026-08-20 | T2 (resumed) / orchestrator | Ratchet include-store defect FIXED and independently verified: exactly **2 entries changed, 1139 of 1141 unchanged** | I diffed the re-pinned file against `HEAD:diff-baseline.json` myself rather than accepting T2's summary: `nereka-67-deco609` and `tuzaga-87-gene496` both `error → baseline/12`; `nuvoja-46-dezu541` correctly still `error` (an `!includedef` macro, not a stdlib bundle). No other `diffCount` moved, confirming the store affects only include resolution and nothing else. `measuredAt`/`measuredAgainstCommit` moved on all 1141 because the render configuration genuinely changed — claiming the old provenance would have been false | `git show HEAD:...` vs working tree, compared per-slug |
| 2026-08-20 | T2 (resumed) | Failure message rewritten so the gate guides instead of misleading; **no bypass added** | It now names the `compare.ts:353` short-circuit, cites `svg/g[1][childCount]`, gives 1010/1141 = 88.8%, and separates **mass rise on baseline-12 fixtures ⇒ progress, re-pin deliberately** from **isolated rise ⇒ the regression this gate is for**. The gate still FAILS on every rise. Without this, the first genuine root-chrome improvement would have fired ~1010 failures all saying "got LESS faithful" | `sequence.diff-baseline.ratchet.test.ts#checkNoRise` |
| 2026-08-20 | T2 / orchestrator | T2 correctly **kept the fix and reported RED** rather than reverting to restore green | Reverting would have restored green only by re-creating the ratchet-vs-census divergence — trading a real defect for a green tick. The RED is `sequence-diff-census.test.ts:246`, i.e. T4's own acceptance criterion detecting that its artifact is now stale against the re-pinned baseline. The test doing its job, not a new defect. T4 resumed to regenerate | T2 report; `diff-census.json` still 1138+3 vs baseline 1140+1 |
| 2026-08-20 | T4 (resumed) | Regeneration alone did NOT fix the census — the same include-store defect existed in T4's own module, and it was fixed at origin | `censusForFixture` called `renderFixtureSequence(markup, measurer)` with no `options`, so both stdlib includes threw and both fixtures were re-captured as errors. T4 ruled out a stale read, caching and the classifier before locating it. Census now 1140 + 1 error; totals reconcile at **16486** against the re-pinned baseline (verified independently by me, not taken on report) | `.agent-notes/g1h-T4.md`; my own reconciliation check |
| 2026-08-20 | T4 (resumed) | The 2 restored fixtures are on the plateau **at path level**, not merely the same shape | Their sorted diff paths are byte-for-byte the shared 12-path root-chrome set. Resolving a stdlib include changes what the diagram CONTAINS and nothing about WHERE we diverge, because comparison still halts at `svg/g[1][childCount]` first. Plateau is now 1012/1140 — share unchanged at 88.8% | `diff-census.json`; profile check reproduced by me (two distinct profiles: 836 + 176) |
| 2026-08-20 | T4 / orchestrator | **Latent divergence recorded for a follow-on, deliberately NOT fixed here**: the include-store seam now exists in THREE duplicated copies — the ratchet, `svg-conformance-census.ts:168`, and `sequence-diff-census.ts` | `render-fixture.ts#fixtureIncludeStore` is private and its module's exports are outside every write-set in this mission. Three copies of the seam that ALREADY caused one measurement divergence this session is the same defect class waiting to recur. Exporting it once is the fix; that belongs to a follow-on, not to a mission whose whole discipline is bounded write-sets | `.agent-notes/g1h-T4.md` |
| 2026-08-20 | orchestrator | **Batch 2 gates GREEN** on the committed tree | `npm test` exit 0, **623 files / 16001 passed** + 1 skipped + 1 todo, Duration **56.06 s** (ceiling 60.3 s). Coverage 95.44 / 90.47 / 96.94 / 96.52. typecheck 0 · lint 0 · build 0. Parity ratchets + architecture: 6 files / 1439 passed. `manifest-diff.py` "OK: 0 expected moves, 0 unexpected". `src/` EMPTY both vs main and in the working tree. **`ratchet.json` is still `{"fixtures": []}` and zero `golden.svg` exist** — stop 13 respected | this session |
| 2026-08-20 | T6 / orchestrator | **Batch 3 gates GREEN; close-out figures independently re-verified by the orchestrator** | My own reconciliation, run after T6 finished: 1141 cache dirs · baseline 1141 entries = 1140 measurable + 1 error · sum 16486 · min 10 · max 139 · census 1140 + 1 error · bucket sum 16486 — **RECONCILES: True**. `git diff --name-only main..HEAD -- src/` EMPTY. `ratchet.json` still `{"fixtures": []}`. T6's re-measurement agreed with the journal's final post-fix state on every figure | this session |
| 2026-08-20 | T6 | A **third distinct wall-clock confound** was identified and discarded rather than published | T6's first reading was 58.49 s at load1 51.82 with WebStorm at 534.8% CPU; it polled the load down and re-measured **55.15 s**. The three confounds this mission — sibling JVM starts, the Spotlight reindex of 1141 new files, and an IDE indexing spike — are why no wall-clock reading here was accepted without a load check | `.agent-notes/g1h-T6.md` |
| 2026-08-20 | T6 | Ratchet counts re-run **per file** after a combined-file grep tally undercounted class by 3 | A tooling artifact of grepping an aggregate vitest summary, not a real ratchet movement. Per-file: state 270/270 · class 721/721 · description 357/357 · object 80/80 · layering 9/9 with `KNOWN_DEBT []` · catalog 2/2. Worth recording because an aggregate tally that silently undercounts is exactly how a ratchet drop would be missed | `.agent-notes/g1h-T6.md` |
