# Decision Journal — A2s

| Date | Task | Decision / Finding | Why | Flagged for review |
|------|------|--------------------|-----|--------------------|
| 2026-08-04 | D1 | ROOT CAUSE (orchestrator-verified): `oracle/goldens/class/*/svek-*.dot` were generated WITHOUT `-DPLANTUML_DETERMINISTIC_TEXT=true` (`scripts/oracle-corpus.ts:66-70` omits the flag; the non-deterministic warm cache was promoted into goldens). All 489 deltas are AWT-vs-width-table glyph metrics baked into the oracle, not src bugs. Proof: stored bosiki-11 gives 1-char classes A/B DIFFERENT widths (0.578600/0.556288 — impossible under a width table); deterministic regen gives A=B=0.574479 = our pipeline byte-exact. Independently reproduced by orchestrator on bosiki + befasi; D1 sampled 16 fixtures across all clusters, 16/16 exact (≤1e-6). Structure preserved under regen (befasi 27 nodes/31 edges both). Description goldens spot-checked DETERMINISTIC — pollution is class-only. | Mechanism per diagnosis.md: cause + origin file:line + causal chain + ruled-out (floors, margins, table divergence, SI1 need — all excluded by exact match). | yes |
| 2026-08-04 | orch | D2/D3/D4 stopped mid-run (diagnosing an artifact). D2's partial result retained: 21/22 sampled fixtures match det-jar exactly; **nadono-22-gidu983 is a genuine divergence** — first real residual for post-regen diagnosis. | Cluster menu predicted ≥4 mechanisms; ONE shared root spans all clusters (push-forward: mechanism-count change, journaled). | no |
| 2026-08-04 | orch | STOP invoked per README stop condition 5: the fix (regenerate 708 class goldens + flag `runOracle`) is OUTSIDE every declared write-set and rewrites oracle data. Amended plan presented to maintainer for approval before any golden is touched. | Oracle rewrites need explicit sanction; reversible via git but source-of-truth-altering. | yes |
| 2026-08-04 | G1 | Maintainer APPROVED regen. Executed: flag added to `runOracle` (oracle-corpus.ts), 542 golden dirs regenerated deterministically (zero svek-count mismatches; 166 no-DOT fixtures + 6 input.svg harness goldens untouched). Re-measure vs corrected oracle: **635/708 conformant (89.7%)**, 442 phantom deltas closed, **73 genuine residuals** re-pinned in size-backlog.json (38 pins GREW vs their polluted predecessors — the ADR-5 sanctioned re-pin; slugs listed in the measure log and visible as the 38 `widened` rows of scratchpad/measure-post-regen.jsonl). Residual clusters are integer-px signatures (12.00px×3, 22.00px×2, 14.00px×2, 3.00px×2, 14.26px×5, 49.70px×3, ~233px×4) → G2 queue. Gates: full suite green, typecheck/lint/build clean, class DOT-sync 710/710 EQUAL, description 320/351 w0, parity guard 9/9. | ADR-5 execution; backlog `_doc` records the recapture. | no |
