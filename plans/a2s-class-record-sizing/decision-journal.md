# Decision Journal — A2s

| Date | Task | Decision / Finding | Why | Flagged for review |
|------|------|--------------------|-----|--------------------|
| 2026-08-04 | D1 | ROOT CAUSE (orchestrator-verified): `oracle/goldens/class/*/svek-*.dot` were generated WITHOUT `-DPLANTUML_DETERMINISTIC_TEXT=true` (`scripts/oracle-corpus.ts:66-70` omits the flag; the non-deterministic warm cache was promoted into goldens). All 489 deltas are AWT-vs-width-table glyph metrics baked into the oracle, not src bugs. Proof: stored bosiki-11 gives 1-char classes A/B DIFFERENT widths (0.578600/0.556288 — impossible under a width table); deterministic regen gives A=B=0.574479 = our pipeline byte-exact. Independently reproduced by orchestrator on bosiki + befasi; D1 sampled 16 fixtures across all clusters, 16/16 exact (≤1e-6). Structure preserved under regen (befasi 27 nodes/31 edges both). Description goldens spot-checked DETERMINISTIC — pollution is class-only. | Mechanism per diagnosis.md: cause + origin file:line + causal chain + ruled-out (floors, margins, table divergence, SI1 need — all excluded by exact match). | yes |
| 2026-08-04 | orch | D2/D3/D4 stopped mid-run (diagnosing an artifact). D2's partial result retained: 21/22 sampled fixtures match det-jar exactly; **nadono-22-gidu983 is a genuine divergence** — first real residual for post-regen diagnosis. | Cluster menu predicted ≥4 mechanisms; ONE shared root spans all clusters (push-forward: mechanism-count change, journaled). | no |
| 2026-08-04 | orch | STOP invoked per README stop condition 5: the fix (regenerate 708 class goldens + flag `runOracle`) is OUTSIDE every declared write-set and rewrites oracle data. Amended plan presented to maintainer for approval before any golden is touched. | Oracle rewrites need explicit sanction; reversible via git but source-of-truth-altering. | yes |
| 2026-08-04 | F1-F6 | Fix round 1 landed (six parallel agents + orchestrator wiring). Mechanisms closed: B1 member tags ×10, A1 annotation-@ ×7, A2 USymbol-leaf routing ×5, hide-family (A5/A6/B2) ×5, notes creole (A4/A11/A12/B5-note) ×6 + 3 authored hline fixtures ratcheted in, B5-member (julixi/rulite partial, besepi closed), B6 creole color/back ×2, B7 sameClassWidth, A7 minClassWidth, A9 headerFontSize, A10/B3 groupInheritance ×5, B4 wrapWidth (zepeki closed; ponono/sumocu 3.25→0.18). Orchestrator wired the cross-cutting plumbing agents escalated: style-map-theme.ts wrapWidth pass-through, theme/accumulator/key-handler/builder fields for sameClassWidth + groupInheritance, classCascadeHeaderFontSize cascade + read. **683/711 (96.1%), widened 0** pre-F-G. A8+A13 wiring delegated to F-G. | Per-agent reports in task transcripts; each fix cites its Java expression. | no |
| 2026-08-04 | F-F/orch | MAINTAINER RULING: B6 ships; the 2 description fixtures whose pins it widened (gevozu-46, sunuju-01, 0.611632→1.263889) are RE-PINNED — the old pins encoded raw-tag text width accidentally compensating the DELIBERATE latex 0-width sizing divergence (bodyenhanced README: preserved by design). Cause stays `latex`. Description ratchet green at 320/351 w0 after re-pin. | The creole consumption is upstream-correct; unmasking ≠ regression. | yes |
| 2026-08-04 | F-C | Colocated test file relocated to tests/unit/class/ (vitest include is tests/** — a colocated src test never runs under npm test). Naming-convention colocation yields dead tests in this repo; keep class tests under tests/unit/class/. | Discovered by F-C; orchestrator moved + fixed imports. | no |
| 2026-08-04 | G2 | D2's mid-run "genuine divergence" claim on nadono-22-gidu983 did NOT survive the regen — the fixture measures conformant against the corrected oracle (it is absent from the 73-entry residual list). The claim was made against stale goldens before the regen landed; no further action. G2 queue = batch-3/residual-clusters.md (12 multi-fixture clusters + singletons). | Verify a subagent's load-bearing claim before acting — it was checked against post-regen measurement rather than repeated. | no |
| 2026-08-04 | G1 | Maintainer APPROVED regen. Executed: flag added to `runOracle` (oracle-corpus.ts), 542 golden dirs regenerated deterministically (zero svek-count mismatches; 166 no-DOT fixtures + 6 input.svg harness goldens untouched). Re-measure vs corrected oracle: **635/708 conformant (89.7%)**, 442 phantom deltas closed, **73 genuine residuals** re-pinned in size-backlog.json (38 pins GREW vs their polluted predecessors — the ADR-5 sanctioned re-pin; slugs listed in the measure log and visible as the 38 `widened` rows of scratchpad/measure-post-regen.jsonl). Residual clusters are integer-px signatures (12.00px×3, 22.00px×2, 14.00px×2, 3.00px×2, 14.26px×5, 49.70px×3, ~233px×4) → G2 queue. Gates: full suite green, typecheck/lint/build clean, class DOT-sync 710/710 EQUAL, description 320/351 w0, parity guard 9/9. | ADR-5 execution; backlog `_doc` records the recapture. | no |
| 2026-08-04 | F-G/orch | F-G wired A8+A13 (all 3 slugs to 0); mid-task it caught and fixed a point-free `.map(formatMemberText)` feeding the map index into the new optional param (3 transient widenings, diagnosed at origin). Round-1 close: **686/711 (96.5%), widened 0**; full gates + all ratchets green (suite exit 0, DOT-sync 710/710, description 320/351 w0, parity 9/9). Backlog pruned 73→25; commits 72a2c8bb (FA), 7d61aed1 (FB), 0566d358 (FC), a519d4ac (FD), 1bae3023 (FE), cc7a5b10 (FF), 1b01053c (FG), dc58d328 (chore). | Batch gate protocol. | no |
| 2026-08-04 | plan | Round 2 queue (25): flatten blank-line origin (vivifa + pejone/xonamo partials — core preprocessor, orchestrator); ponono/sumocu 0.18 residual; julixi/rulite quoted-name-header \n; rakuci parser quoted-display; B8 survey items (TIM-in-notes roputo/rozudo, embedded xadado, unicode lecelo, monospace curupe, sprites lozego/rotisi, theme font mizupo, stereo attr font sovuxo, jecopa hide-in-package scoping PROBE FIRST, cukaze, dibinu, daxeno, pasova, puvono/sekame 3px); gujigi = SI1-gated ledger candidate. | ADR-3: exit needs each either closed or ledgered. | no |
