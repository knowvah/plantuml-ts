# Skin-file-loading — decision journal

| Date | Batch | Decision | Rationale | Flagged |
|------|-------|----------|-----------|---------|
| 2026-07-24 | B1 | Batch 1 landed (c065310): skin <name> loads rose/debug/strictuml into the theme cascade as a base layer (D6); colors + Shadowing VALUE resolve; no-skin fixtures byte-identical; niveno/sumiri improved. Deliberate gaps flagged: element{} not a general N-SName subset matcher; FontColor unconsumed; class/description test-harness cascade duplicates not wired. | Reuses parseStyleBlock+resolveSkinparam+applyStyleMap; verified seam index.ts:204-227. | No |
| 2026-07-24 | B2 | SCOPE DECISION: Batch 2 does STATE shadow rendering + ink ONLY. The brief's D3 ("shadow resolved+drawn for all diagram types", incl. fixing Cluster.ts:110's hardcoded shadowing:0 for class/description) is DEFERRED to a follow-on increment. Reason: a concurrent session is actively working class-diagram rendering in this shared repo; touching Cluster.ts / class render files now would collide at merge for no immediate gain — nimana-36 (the mission's primary target) is a STATE fixture, closable with state-only shadow. Class/description shadow tracked as a deferred D3 sub-item. | Coordination: avoid a cross-session merge conflict; achieve the primary deliverable (close nimana) without it. Push-forward, journaled. | No |
| 2026-07-24 | B2 | Landed (bedf58b): state drop-shadow render + ink. Finding: jar's SvgGraphics filter is FIXED (dx/dy=4, stdDev=2), not parameterized by the Shadowing value. nimana 0.111111->0.043169. Shadow-off byte-identical. | Reused the existing jar-exact filter emitter; ink via LimitFinder rule. | No |
| 2026-07-24 | B3 | Close-out: backlog tightened (nimana 0.111111->0.043169, sumiri 0.25->0.000026, niveno removed at 0); nimana's G8 tracked-exception _doc note retired (skin-loading landed, exception resolved). State MVP COMPLETE; class/desc shadow (D3), Batch 4 preprocessor skins, element{} subset matcher, FontColor, and the class/desc harness duplicates deferred as tracked follow-ons. Gates green. | Primary deliverable (close nimana + load skins for state) achieved; cross-type + preprocessor skins are separable increments. | No |
| 2026-07-25 | B4 | Landed: `reddress`/`sonyxperiadev` route through `preprocess()` + `resolveSkinparam` (D1's 2nd grammar family), applied as the SAME D6 base layer. `sonyxperiadev` embedded verbatim with upstream `SkinParam` capitalization -- the shared `preprocessor.ts` collector matches lowercase `skinparam` only, so the loader normalizes ONLY its own in-memory copy of the skin text before calling `preprocess()` (the embedded constant stays byte-verbatim). Threaded the document's own bare `!define NAME` flags (`documentRawSourceLines`, optional 3rd param on `applySkinLayer`) so `reddress`'s `!ifdef DARKBLUE`-family gates can fire -- verified via the theme's `colors.background` flipping only when threaded. | Reuses the existing TIM macro engine + collector exactly as D1 specified; no new parser. | No |
| 2026-07-25 | B4 | SCOPE DECISION: did NOT wire `documentRawSourceLines` into `src/index.ts#buildTheme`'s production call site. `src/index.ts` is already a pre-existing 556-line file (over this repo's 500-line complexity-hook limit) with no place in Batch 4's write-set; widening its signature tripped the hook and splitting it is a materially larger, out-of-scope refactor. `applySkinLayer`'s 3rd param stays optional (omitted callers unaffected); only the STATE test harness (`render-fixture-state.ts`, exempt from the file-length hook under `tests/`) threads it, so the mechanism is provable via fixture-level tests without touching production. Net effect: `!define DARKBLUE` + `skin reddress` in a REAL document rendered via `renderSync`/`render` today does NOT thread the gate (same as before Batch 4); only `applySkinLayer` called directly (unit tests) or via `renderFixtureState` exercises it. | Avoided a hard-blocked, high-blast-radius file split for a corner-case (a document combining `!define` with `skin reddress`) with zero existing corpus/harness dependency. | Yes -- flagging for maintainer review: production `skin reddress` + `!define DARKBLUE` combo is a known, documented gap, not silently broken. |
| 2026-07-25 | B4 | FINDING (not fixed, out of scope): `preprocess()`'s skinparam-line collector captures a line's VALUE before TIM `!define`/`!$var` substitution runs on it -- verified with a bare, skin-unrelated 2-line repro. Blocks reddress's ENTIRE FONTNAME/FONTSIZE/ACCENT/PRIMARY/BOXBG mechanism (renders as literal token text) in both bare and `!ifdef`-variant modes; does NOT block literal (non-macro) skinparam values like `skinparam backgroundColor 777`. See `.agent-notes/skin-batch4-preproc.md`. | Root cause is in `preprocessor.ts`/`TContext`'s PlainLineFilter hook ordering -- outside Batch 4's write-set, affects every document combining `!define`+`skinparam` corpus-wide; too large/risky to fix inline. | Yes -- flagging for a dedicated follow-up mission. |
| 2026-07-25 | B4 | FINDING (blocks Part B/C pinning): the PINNED ORACLE JAR crashes (NullPointerException / StyleParsingException) rendering `skin reddress` or `skin sonyxperiadev` on every diagram type tried (state, object/class, sequence) -- confirmed jar-side via a same-jar `skin rose` control (clean) and a bundled-resource-presence control (both `.skin` files present, byte-identical to upstream source). No usable jar SVG oracle exists for either skin today; no new SVG fixture was authored into `oracle/goldens/` (would have no valid golden to compare against). Resolution proven instead via `tests/unit/skin-loader.test.ts` (theme-level assertions, cross-checked against the verbatim embedded skin text). Deleted the scratch `.puml`/jar-output probes used to establish this (never committed). | An SVG-fixture pin requires a working oracle; forcing one against a crash-diagram golden would be actively misleading, not verification. | Yes -- flagging: re-attempt SVG fixtures once the oracle jar is re-pinned to a version without this defect. |
