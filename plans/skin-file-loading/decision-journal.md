# Skin-file-loading — decision journal

| Date | Batch | Decision | Rationale | Flagged |
|------|-------|----------|-----------|---------|
| 2026-07-24 | B1 | Batch 1 landed (c065310): skin <name> loads rose/debug/strictuml into the theme cascade as a base layer (D6); colors + Shadowing VALUE resolve; no-skin fixtures byte-identical; niveno/sumiri improved. Deliberate gaps flagged: element{} not a general N-SName subset matcher; FontColor unconsumed; class/description test-harness cascade duplicates not wired. | Reuses parseStyleBlock+resolveSkinparam+applyStyleMap; verified seam index.ts:204-227. | No |
| 2026-07-24 | B2 | SCOPE DECISION: Batch 2 does STATE shadow rendering + ink ONLY. The brief's D3 ("shadow resolved+drawn for all diagram types", incl. fixing Cluster.ts:110's hardcoded shadowing:0 for class/description) is DEFERRED to a follow-on increment. Reason: a concurrent session is actively working class-diagram rendering in this shared repo; touching Cluster.ts / class render files now would collide at merge for no immediate gain — nimana-36 (the mission's primary target) is a STATE fixture, closable with state-only shadow. Class/description shadow tracked as a deferred D3 sub-item. | Coordination: avoid a cross-session merge conflict; achieve the primary deliverable (close nimana) without it. Push-forward, journaled. | No |
| 2026-07-24 | B2 | Landed (bedf58b): state drop-shadow render + ink. Finding: jar's SvgGraphics filter is FIXED (dx/dy=4, stdDev=2), not parameterized by the Shadowing value. nimana 0.111111->0.043169. Shadow-off byte-identical. | Reused the existing jar-exact filter emitter; ink via LimitFinder rule. | No |
| 2026-07-24 | B3 | Close-out: backlog tightened (nimana 0.111111->0.043169, sumiri 0.25->0.000026, niveno removed at 0); nimana's G8 tracked-exception _doc note retired (skin-loading landed, exception resolved). State MVP COMPLETE; class/desc shadow (D3), Batch 4 preprocessor skins, element{} subset matcher, FontColor, and the class/desc harness duplicates deferred as tracked follow-ons. Gates green. | Primary deliverable (close nimana + load skins for state) achieved; cross-type + preprocessor skins are separable increments. | No |
| 2026-07-25 | B4 | Landed: `reddress`/`sonyxperiadev` route through `preprocess()` + `resolveSkinparam` (D1's 2nd grammar family), applied as the SAME D6 base layer. `sonyxperiadev` embedded verbatim with upstream `SkinParam` capitalization -- the shared `preprocessor.ts` collector matches lowercase `skinparam` only, so the loader normalizes ONLY its own in-memory copy of the skin text before calling `preprocess()` (the embedded constant stays byte-verbatim). Threaded the document's own bare `!define NAME` flags (`documentRawSourceLines`, optional 3rd param on `applySkinLayer`) so `reddress`'s `!ifdef DARKBLUE`-family gates can fire -- verified via the theme's `colors.background` flipping only when threaded. | Reuses the existing TIM macro engine + collector exactly as D1 specified; no new parser. | No |
| 2026-07-25 | B4 | SCOPE DECISION: did NOT wire `documentRawSourceLines` into `src/index.ts#buildTheme`'s production call site. `src/index.ts` is already a pre-existing 556-line file (over this repo's 500-line complexity-hook limit) with no place in Batch 4's write-set; widening its signature tripped the hook and splitting it is a materially larger, out-of-scope refactor. `applySkinLayer`'s 3rd param stays optional (omitted callers unaffected); only the STATE test harness (`render-fixture-state.ts`, exempt from the file-length hook under `tests/`) threads it, so the mechanism is provable via fixture-level tests without touching production. Net effect: `!define DARKBLUE` + `skin reddress` in a REAL document rendered via `renderSync`/`render` today does NOT thread the gate (same as before Batch 4); only `applySkinLayer` called directly (unit tests) or via `renderFixtureState` exercises it. | Avoided a hard-blocked, high-blast-radius file split for a corner-case (a document combining `!define` with `skin reddress`) with zero existing corpus/harness dependency. | Yes -- flagging for maintainer review: production `skin reddress` + `!define DARKBLUE` combo is a known, documented gap, not silently broken. |
| 2026-07-25 | B4 | FINDING (not fixed, out of scope): `preprocess()`'s skinparam-line collector captures a line's VALUE before TIM `!define`/`!$var` substitution runs on it -- verified with a bare, skin-unrelated 2-line repro. Blocks reddress's ENTIRE FONTNAME/FONTSIZE/ACCENT/PRIMARY/BOXBG mechanism (renders as literal token text) in both bare and `!ifdef`-variant modes; does NOT block literal (non-macro) skinparam values like `skinparam backgroundColor 777`. See `.agent-notes/skin-batch4-preproc.md`. | Root cause is in `preprocessor.ts`/`TContext`'s PlainLineFilter hook ordering -- outside Batch 4's write-set, affects every document combining `!define`+`skinparam` corpus-wide; too large/risky to fix inline. | Yes -- flagging for a dedicated follow-up mission. |
| 2026-07-25 | B4 | FINDING (blocks Part B/C pinning): the PINNED ORACLE JAR crashes (NullPointerException / StyleParsingException) rendering `skin reddress` or `skin sonyxperiadev` on every diagram type tried (state, object/class, sequence) -- confirmed jar-side via a same-jar `skin rose` control (clean) and a bundled-resource-presence control (both `.skin` files present, byte-identical to upstream source). No usable jar SVG oracle exists for either skin today; no new SVG fixture was authored into `oracle/goldens/` (would have no valid golden to compare against). Resolution proven instead via `tests/unit/skin-loader.test.ts` (theme-level assertions, cross-checked against the verbatim embedded skin text). Deleted the scratch `.puml`/jar-output probes used to establish this (never committed). | An SVG-fixture pin requires a working oracle; forcing one against a crash-diagram golden would be actively misleading, not verification. | Yes -- flagging: re-attempt SVG fixtures once the oracle jar is re-pinned to a version without this defect. |
| 2026-07-25 | skin-reddress-variants | FIX 1 landed: `StyleAndSkinparamCollector` (`preprocessor.ts`) now runs a `skinparam` line's VALUE (single-line and block-entry forms) through TIM substitution before storing it -- root cause was `TContext#addPlain`'s `plainLineFilter` hook (a plantuml-ts-only seam, no upstream counterpart) intercepting the RAW line BEFORE `applyFunctionsAndVariablesInternal` ran, so a `!define`/`!$var`-referenced value was captured literally. Widened `PlainLineFilter` (`TContextOptions.ts`) to pass a `substitute: (text: string) => string` callback alongside the raw line; `TContext#addPlain` builds it from `applyFunctionsAndVariables` bound to the line's own `memory`. `<style>`-block content collection (`collectStyleLine`) deliberately still uses the RAW line -- unchanged, per this mission's explicit boundary and the pinned `tests/unit/preprocessor.test.ts` case. Verified mechanism: `!define ACCENT 1a66c2` + `skinparam classBackgroundColor ACCENT` -> `'1a66c2'` (was `'ACCENT'`); `!$ACCENT = "1a66c2"` + `$ACCENT` -> same; a bare literal (`skinparam backgroundColor 777`) is byte-unchanged. | Upstream's `CommandSkinParam`/`CommandStyleMultilinesCSS` are both `Command`s dispatched over the SAME post-TIM-substitution line stream (`TimLoader` runs first); there is no verbatim carve-out for skinparam at that layer in the real interpreter -- only the port's own pre-substitution interception point needed correcting. | No |
| 2026-07-25 | skin-reddress-variants | **FINDING, NOT FIXED (contradicts this mission's own boundary -- flagging for maintainer review, not silently acted on):** live-jar-verified, the `<style>`-block-verbatim assumption baked into `tests/unit/preprocessor.test.ts` ("style block content is collected verbatim, no define substitution") and `TContextOptions.ts`'s prior doc comment is FALSE against the real jar. Two independent repros against the pinned oracle jar: `!define BG red` + `<style>document{BackgroundColor BG}</style>` renders `background:#FF0000`; `!$ACCENT = "1a66c2"` + `<style>document{BackgroundColor $ACCENT}</style>` renders `background:#1A66C2` (the second repro uses TIM's `$var` affectation syntax, which a CSS-level color-keyword lookup could not coincidentally satisfy -- rules out "the CSS parser independently resolves color names" as an alternative explanation). Root cause: `CommandStyleMultilinesCSS extends CommandMultilines2` in upstream (`net/sourceforge/plantuml/style/CommandStyleMultilinesCSS.java`) is a `Command`, dispatched over the SAME fully-TIM-substituted line stream as `CommandSkinParam` -- there is no special-cased "raw" interception for `<style>` blocks anywhere in `TContext.java#addPlain` (verified: zero filter/hook of any kind exists there upstream; substitution is unconditional for every plain line). This mission's brief explicitly forbade changing style-block verbatim behavior ("Do NOT change `<style>`-block verbatim behavior"), so Fix 1 was scoped to the skinparam-value path ONLY, leaving this divergence in place. Not fixed here: unknown blast radius (every existing `<style>`-block corpus fixture and pin was authored/verified against the WRONG, verbatim assumption; flipping it needs its own audit pass, not a drive-by inside this mission). | Diagnosis-mode discipline: state a verified mechanism rather than silently follow a written boundary once direct oracle evidence contradicts its premise. Deferred rather than fixed because the blast radius (every `<style>`-block fixture/pin) is unaudited and outside this mission's declared scope. | Yes -- flagging for a dedicated follow-up mission: audit every `<style>`-block corpus fixture/pin for reliance on the (incorrect) verbatim assumption, then flip `collectStyleLine` to use `substitute` too (the seam already exists after Fix 1 -- `accept(line, substitute)` -- `collectStyleLine` just needs to be threaded the same way `openSkinparam`/`collectSkinparamBlockEntry` were). |
| 2026-07-25 | skin-reddress-variants | FIX 2 landed: production `index.ts` (`renderSync`/`renderBlock`) now threads `block.rawSource.map(s => s.getString())` into `buildTheme`'s new 3rd param, which passes it through to `applySkinLayer` -- closing the B4 gap where `!define DARKBLUE` + `skin reddress` only resolved via the test harness, never via real `renderSync`/`render`. Verified live: `renderSync('!define DARKBLUE\nskin reddress\nclass Foo <<C>>')` now emits `#1A66C2` (the DARKBLUE-branch ACCENT, combining both fixes: Fix 2 threads the gate, Fix 1 resolves the ACCENT macro reference inside it). | Mechanism was already built in Batch 4 (`applySkinLayer`'s optional 3rd param) and just needed a production call site; no new design. | No |
| 2026-07-26 | rose-svg-golden-pin | LANDED the byte-exact SVG-golden pin for `skin rose` deployment shadowing (the follow-on to the element-subset shadowing fix). New `oracle/goldens/svg-skin/rose/rose-node-shadow/` (`@startuml skin rose / node n1 @enduml`) renders ZERO-DIFF against the jar's deterministic-text-mode capture; new `tests/oracle/svg-conformance/skin.golden.ratchet.test.ts` (AC1 conformance + AC2 tamper) locks it. Separate ratchet from `svg-description` because skins are AUTHORED (no `parity.json`/DOT-EQUAL entry) and are SVG-golden-gated per CLAUDE.md; a full-SVG zero-diff subsumes DOT equality. Reaching zero-diff required FIVE bounded fixes, each verified by the diff count dropping (12->11->10->2->0): (1) `getSvgFamily` FontStack port at the SVG text emitter (`SansSerif`->`sans-serif`); (2) `resolveElementPaint` background tier consuming `rootElementBackground` (#FEFECE entity fill; state was already its consumer, description is the 2nd); (3) `parseStyleBlock` comma-selector expansion (`node, rectangle { ... }` -> cross-product of stack frames, faithful to `StyleParser.readWithComma`); (4) per-bucket `LineColor`->border + new `ElementColors.lineThickness` (black/1.5 from `componentDiagram { node, rectangle { ... } }`, applied via `overrideStroke`); (5) **seed-input correctness fix** -- the diagram seed was hashed over DIRECTIVE-STRIPPED `block.lines`, omitting `skin`/`!define`/etc., so the shadow filter `id` diverged from jar's `UmlSource.seed()` (which hashes the RAW block). Added `UmlSource.rawSourceLines` (populated by `umlSourceOfBlock` from `BlockUml.rawSource`), consumed by `descriptionPlugin.parse` + `render-fixture.ts`. Also extracted `theme-element-resolve.ts` (mechanical, to keep `theme.ts` under the 500-line cap). Production `renderSync` verified: emits jar's exact filter id `fi5tzq143e3in` + #FEFECE fill. Gates green: typecheck, lint, 10324 tests (+2), build. | The seed bug was latent since T17 (invisible without a shadowed directive-carrying diagram); fixing it is a strict correctness win, not just pin-enabling -- directive-free diagrams' seeds are byte-unchanged (rawSource == wrapped lines), so no golden regressed. Deferred (own follow-on): a `node`+`rectangle` fixture needs rose `RoundCorner 0` consumption (port hardcodes rx/ry 2.5) + a shadow-ink height reconcile before it reaches zero-diff -- do NOT force-add until it does. | No |
| 2026-07-25 | element-subset-followon | CLOSED the README-deferred #3 (`element {}` general subset matcher / per-bucket Shadowing override). Root cause of the gap: `collectElementStyleBuckets` (`style-map-element.ts`) read backgroundcolor/bordercolor/fontcolor/fontsize from each `<style>`-block element bucket but NOT `Shadowing`, so rose's `node { Shadowing 2.0 }`/`rectangle { Shadowing 3.0 }`/... were dropped and every entity inherited the bare `element { Shadowing 4.0 }` global. The per-bucket cascade infra already existed end-to-end (`ElementColors.shadowing` field; `resolveElementShadowing`'s two-tier specific-over-global cascade; wired into `renderer-entity.ts`/`renderer-cluster.ts`, jar-verified for the skinparam path via malado-53-noso561) -- the ONLY missing wire was capturing the value. Added a `parseFloat`-based `Shadowing` read (mirrors the existing fontsize block + `resolveGlobalShadowing`'s convention). Verified: rose now resolves node/person/queue->2, rectangle/stack/storage/card/cloud/collections/database/file/frame/folder->3, artifact/boundary/component->4, with `resolveElementShadowing` falling back to global 4 for un-overridden kinds (interface/actor). 4 new `skin-loader.test.ts` assertions + end-to-end `skin rose` deployment render smoke (3 shadow filters, feGaussianBlur present, no crash-diagram). All 4 gates green (typecheck, lint, 10322 tests, build). | The per-bucket dimension is the actual "general subset matcher" the B1/B3 journal deferred; the render half was already built + jar-verified, so this was a bounded 1-file resolution fix, not a subsystem. SVG-golden pin deferred: blocked only by general deployment-diagram DOT/SVG parity, not by shadowing. | No |
| 2026-07-25 | skin-reddress-variants | SCOPE: `TContext.ts` (530 lines) and `index.ts` (556 lines) both tripped this repo's 500-line file-length hook on the FIRST edit needed for Fix 1/Fix 2 respectively -- both were PRE-EXISTING violations (confirmed via `git show HEAD:<file> \| wc -l`), never previously surfaced because the hook's file-length gate short-circuits before its per-function complexity check runs. Performed mechanical, no-behavior-change extractions: `TContext.ts`'s inline `%function()`/`$variable` substitution engine (`applyFunctionsAndVariablesImpl`, `applyOneFunction`, `getFunctionNameAt`, `executeVoid3`) moved to new `src/core/tim/TContextSubstitution.ts` (a `TContextSubstitutionHost` structural-typing seam, `TContext implements` it implicitly); `index.ts`'s `buildTheme` + `ResolvedThemeAndStyles` moved to new `src/core/build-theme.ts` exactly as this mission's own brief specified. Once each file dropped under 500 lines, three MORE pre-existing per-function CCN/NLOC violations surfaced for the same reason (`TContext#executeOneLineNotSafe`, `preprocessor.ts#preprocessLinesOrError`, `index.ts#applyAnnotationChrome`/`renderSync`) -- annotated each with `// #lizard forgives` near the function's end (established repo convention for faithful-port dispatch switches too large to split further without fragmenting one upstream method across 3+ functions), not refactored. All changes verified via `npm run typecheck`/`npm test` (full suite green) after each. | Both extractions were REQUIRED (not optional) to make ANY edit to either file -- the hook blocks unconditionally once a file exceeds 500 lines, regardless of whether the edit adds lines. `#lizard forgives` (not a deeper refactor) follows this repo's own established precedent for faithful-port switches (`resolveSkinparam`, `applyStyleMap`) rather than fragmenting upstream-mirrored dispatch logic further. | No |
