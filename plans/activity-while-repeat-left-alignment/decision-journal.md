# Decision journal — `activity-while-repeat-left-alignment`

| Date | Task | Decision | Why | Evidence |
|---|---|---|---|---|
| 2026-09-16 | orch | Executed T1 in the orchestrator instead of dispatching `typescript-pro`. | The change is ~20 lines across two files with the Java already open in context; `CLAUDE.md` global: handle tasks under ~30 min directly; a subagent would have re-paid the whole read-set for no parallelism gain. | this row; `git diff` of T1 |
| 2026-09-16 | orch | Rebuilt the diagonal scanner (`scratchpad/diag-scan.ts`, the aitp copy was never committed): counts `<line>` elements with both dx and dy > 0.01, and drops `kill`/`end` cross glyphs (two 45-degree lines sharing one bounding box). Tolerance 0.01, not 1e-6: at 1e-6 `firibi-00-puki721` (dx 0.001) and a second `jupoxe` segment appear as float artifacts and the count is 20, not the brief's 19; at 0.01 the set equals `fixtures.md`'s 19 exactly. | The brief's 19 is the calibration point; a sub-pixel dx is a rounding artifact, not a slanted edge. | `diag-pre.json` == brief19 (checked by set equality) |
| 2026-09-16 | T1 | Baseline reproduced before any edit: probe aggregate 49647 / 268 fixtures; scan 19 fixtures, 21 segments. | Control per aitp-T1's precedent: no measurement trusted until the baseline reproduces. | `pre.json`, `diag-pre.json` |
| 2026-09-16 | T1 | Ported the merger: `GtileWhile` computes `contentLeft = max(header.left, body.left)`, `headerOffsetX`/`bodyOffsetX = contentLeft - child.left`, `width = max(offset + child.width) + BACK_EDGE_MARGIN`, hooks at `contentLeft` (`FtileGeometryMerger.java:44-47`; `FtileWhile.java:584,593,621-641`); `walkWhile` places `hX = x + headerOffsetX`, `bX = x + bodyOffsetX`. Gutters unported (D2). Test stubs now return `left = width/2` by default (they returned 0, which the old code never read); four asymmetric cases added. | D1 verbatim; the stub default keeps every pre-existing assertion's numbers, so no assertion was deleted (stop 10). | `gtile-while.test.ts` 21/21; `tile-coordinates`, `tile-layout`, `compress/invariant`, `unit/activity/layout` untouched and green (136/136 across the five) |
| 2026-09-16 | T1 | Movers, by SVG byte-diff of all 268 rendered before (stashed) and after: exactly 5 -- `bareka-88-fusu160`, `nafaxo-62-boso912`, `pixako-75-kumi821`, `ruzica-16-deli877` (an `if` in the while body: `left != width/2`) and `vamazo-19-tufu812` (a nested `while` in the body: its own `left` is `(width - BACK_EDGE_MARGIN)/2`, not `width/2`). Every other fixture, including all 15 symmetric `while` fixtures and all 14 `repeat` diagonals, byte-identical (stop 13 clean, stop 5 clean). In each mover the header shifted to sit over the body's `left` (bareka: header polygon x -6, forward edge `(64.025,52)->(58.025,72)` became `(58.025,52)->(58.025,72)`). | Byte-identity of the rendered SVG is the strong form of "score unchanged"; the comparator is magnitude-blind (`oracle-score-blind-to-magnitude`). | `t1-changed-svgs.txt` (5 lines); `svg-pre/` vs `svg-t1/` |
| 2026-09-16 | T1 | Scan 19 -> 14 (segments 21 -> 16): the 5 `while` fixtures are gone, the 14 `repeat` fixtures unchanged. Probe aggregate 49647 -> 49647, 0 score movers, 0 risers -- consistent with the comparator charging one unit per endpoint regardless of magnitude; `t1.json` written. | D5: the scan is the exit signal, the aggregate is reported only. | `diag-t1.json`, `measurements/t1.json` |
