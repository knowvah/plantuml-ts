# Decision journal — `activity-while-repeat-left-alignment`

| Date | Task | Decision | Why | Evidence |
|---|---|---|---|---|
| 2026-09-16 | orch | Executed T1 in the orchestrator instead of dispatching `typescript-pro`. | The change is ~20 lines across two files with the Java already open in context; `CLAUDE.md` global: handle tasks under ~30 min directly; a subagent would have re-paid the whole read-set for no parallelism gain. | this row; `git diff` of T1 |
| 2026-09-16 | orch | Rebuilt the diagonal scanner (`scratchpad/diag-scan.ts`, the aitp copy was never committed): counts `<line>` elements with both dx and dy > 0.01, and drops `kill`/`end` cross glyphs (two 45-degree lines sharing one bounding box). Tolerance 0.01, not 1e-6: at 1e-6 `firibi-00-puki721` (dx 0.001) and a second `jupoxe` segment appear as float artifacts and the count is 20, not the brief's 19; at 0.01 the set equals `fixtures.md`'s 19 exactly. | The brief's 19 is the calibration point; a sub-pixel dx is a rounding artifact, not a slanted edge. | `diag-pre.json` == brief19 (checked by set equality) |
| 2026-09-16 | T1 | Baseline reproduced before any edit: probe aggregate 49647 / 268 fixtures; scan 19 fixtures, 21 segments. | Control per aitp-T1's precedent: no measurement trusted until the baseline reproduces. | `pre.json`, `diag-pre.json` |
| 2026-09-16 | T1 | Ported the merger: `GtileWhile` computes `contentLeft = max(header.left, body.left)`, `headerOffsetX`/`bodyOffsetX = contentLeft - child.left`, `width = max(offset + child.width) + BACK_EDGE_MARGIN`, hooks at `contentLeft` (`FtileGeometryMerger.java:44-47`; `FtileWhile.java:584,593,621-641`); `walkWhile` places `hX = x + headerOffsetX`, `bX = x + bodyOffsetX`. Gutters unported (D2). Test stubs now return `left = width/2` by default (they returned 0, which the old code never read); four asymmetric cases added. | D1 verbatim; the stub default keeps every pre-existing assertion's numbers, so no assertion was deleted (stop 10). | `gtile-while.test.ts` 21/21; `tile-coordinates`, `tile-layout`, `compress/invariant`, `unit/activity/layout` untouched and green (136/136 across the five) |
| 2026-09-16 | T1 | Movers, by SVG byte-diff of all 268 rendered before (stashed) and after: exactly 5 -- `bareka-88-fusu160`, `nafaxo-62-boso912`, `pixako-75-kumi821`, `ruzica-16-deli877` (an `if` in the while body: `left != width/2`) and `vamazo-19-tufu812` (a nested `while` in the body: its own `left` is `(width - BACK_EDGE_MARGIN)/2`, not `width/2`). Every other fixture, including all 15 symmetric `while` fixtures and all 14 `repeat` diagonals, byte-identical (stop 13 clean, stop 5 clean). In each mover the header shifted to sit over the body's `left` (bareka: header polygon x -6, forward edge `(64.025,52)->(58.025,72)` became `(58.025,52)->(58.025,72)`). | Byte-identity of the rendered SVG is the strong form of "score unchanged"; the comparator is magnitude-blind (`oracle-score-blind-to-magnitude`). | `t1-changed-svgs.txt` (5 lines); `svg-pre/` vs `svg-t1/` |
| 2026-09-16 | T1 | Scan 19 -> 14 (segments 21 -> 16): the 5 `while` fixtures are gone, the 14 `repeat` fixtures unchanged. Probe aggregate 49647 -> 49647, 0 score movers, 0 risers -- consistent with the comparator charging one unit per endpoint regardless of magnitude; `t1.json` written. | D5: the scan is the exit signal, the aggregate is reported only. | `diag-t1.json`, `measurements/t1.json` |
| 2026-09-16 | B1 gate | Batch 1 closed on T1 `9f328e5f`. Orchestrator verified: commit = write-set (5 files: 2 src, 1 test, `t1.json`, journal); `npm test` collected 721/721 (JSON reporter vs `find`), 19817 passed, red ONLY on three activity census pins -- `pixako-75-kumi821` style (canvas 256x317 -> 223x323: the while no longer stacks both children's full widths) and `ruzica-16-deli877` swimlane (dividers `[17,207.281,383.892] -> [17,215.281,383.213]`) and text (inset `10 -> 9.999` on 2 entries, a float artifact of the shifted x) -- both journaled T1 movers, inside the red allowance; typecheck, lint, build exit 0; `git diff --name-only HEAD~1` = write-set only. | Gate row per the brief; the census gates are equality pins, re-pinned once at T3. | `t1-npm-test.log`, `t1-vitest.json` |
| 2026-09-16 | T2 | Executed in the orchestrator (as T1). Ported `FtileRepeat#getLeft`/`getRight` (`vcompact/FtileRepeat.java:767-786`): `left = max(body.left, condition.w/2, backward.left)`, `right = max(body.w - body.left, condition.w - condition.w/2, backward.w - backward.left)`, `width = left + right + BACK_EDGE_MARGIN` (`:707`, gutters unported per D2), children at `BACK_EDGE_MARGIN/2 + left - child.left` (`:740,747,764`), hooks at `BACK_EDGE_MARGIN/2 + left` (`:696-699`). The jar's `dimDiamond1.w/2` term is ABSENT: our tile has no entry diamond (D3; the spec's own note). | D1 verbatim for the terms the jar has. | `gtile-repeat.ts` JSDoc; `gtile-repeat.test.ts` 4 asymmetric cases |
| 2026-09-16 | T2 | The backward body JOINS the left/right merge. The jar excludes it from `getLeft`/`getRight` because it places it at the RIGHT edge (`:750-757`, `x = width - backward.w`) and adds its width to the total (`:709-710`); ours stacks it in the column under the condition (`backwardOffsetY`), joined by `condition.SOUTH -> backward.NORTH` (a `GConnectionVerticalDown`, straight) and `backward.SOUTH -> body.NORTH` (`GConnectionDownThenUp`, orthogonal). A stacked child that stays centred would slant the first of those; the merger is what the jar applies to every stacked child (`FtileGeometryMerger.java:44-47`). The side placement is `activity-repeat-connector-draw-order`'s deferred work (README "does NOT do"). | Axis-aligned edges under OUR structure; the jar's structure for backward is out of this mission's scope. | `gtile-repeat.ts` JSDoc; scan 0 diagonals |
| 2026-09-16 | T2 | Movers by SVG byte-diff (t1 -> t2): exactly 16, all `repeat` rows of `fixtures.md` -- the 14 diagonal fixtures plus `rujuxa-07-neco067` (a `repeat` whose body holds an `if`, inside an `if`'s else branch: asymmetric body, no visible diagonal) and `felega-00-saxi785` (`repeat :R1;`, symmetric: ONLY a divider x `67.213 -> 67.212` and its title text -- a 3-decimal rounding tie: the old `(x + width/2) - body.w/2` evaluated to `24.418999999999997` where the new `x + offset` gives `24.419`, 3.6e-15 apart; tile width/left/hooks are bit-identical, `tile-dump.ts`). No mover outside the loop table (stop 5 clean); no symmetric fixture's SCORE moved (stop 13 by the brief's own instrument; felega's geometry is identical to 1e-14). | The brief defines "moves" by score; a float-tie flip in the printed 3rd decimal is not a placement change. | `t2-changed-svgs.txt` (16); `tile-dump.ts felega` |
| 2026-09-16 | T2 | Scan 19 -> 14 -> **0** over all 268 (segments 21 -> 16 -> 0). Probe aggregate 49647 -> 49658: two risers, both journaled below; no fallers (the comparator is magnitude-blind, D5). | Exit bar clause 1 met. | `diag-t2.json`, `measurements/t2.json` |
| 2026-09-16 | T2 | RISER `tobajo-64-mipi810` +10: ten NEW comparator paths (`g[1]/line[52]/@x2`, `line[53]/@x1,x2`, `polygon[44]/@points[0,2,4,6]`, `line[63]/@x1,x2`, `line[64]/@x1`), all the middle fork branch's repeat forward edge + arrowhead, ours `388.753` vs golden `394.744`. At t1 that edge was the DIAGONAL `(388.753,472)->(394.753,492)`: its far endpoint `394.753` matched the golden within tolerance by coincidence (the old centred x), while `x1` already mismatched. Now the edge is vertical at the body's `left` (388.753 = the `if` body's diamond centre, 6 px left of the body's centre) so every attribute of that edge mismatches by the same 5.991. The residual is where our repeat tile sits inside its fork branch relative to the jar's -- the jar's tile is wider by its unported gutters (D2) and the branch centres it; not this mission's. | Class (2)-like: a coincidental positional match traded for a correctly-shaped edge with a D2 offset. | `family-diff.ts tobajo svg-t1 svg-t2` |
| 2026-09-16 | T2 | RISER `jupoxe-15-sugo110` +1: one NEW path `g[1]/line[29]/@y1` ours 593 vs golden 591. The repeat condition hexagon moved from y 499-539 to 501-541 (+2) with everything under it, canvas height 717 -> 719: an x-only placement change altered which shapes overlap in x, so klimt vertical compression (`compress-geometry.ts`) removes a 2 px smaller gap; one y that coincidentally equalled the golden's 591 now reads 593. | Compression is overlap-driven; a child moving in x changes its overlap set. | `family-diff.ts jupoxe`; element diff t1 vs t2 |
| 2026-09-16 | T2 | Census-gate reds outside the byte-level mover list: `katopo-68-xajo866` swimlane `lanes.width 91.35000000000002 -> 91.35000000000001` and `bulasi-17-vafa634` `98.19999999999999 -> 98.2` are raw-float pins of OUR lane geometry (`censusOf(svg, lanes)` takes the layout's lanes, not the SVG's), moved by the same evaluation-order ulp as felega; katopo's SVG is byte-identical across pre/t1/t2 (`cmp`). Every other census red is a journaled mover. | An equality pin on a raw double records the evaluation order, not the geometry; T3 re-pins once. | `t2-npm-test.log`; `cmp svg-pre/katopo svg-t2/katopo` |
| 2026-09-16 | B2 gate | Batch 2 closed on T2 `6540a782`. Orchestrator verified: commit = write-set (5 files: 2 src, 1 test, `t2.json`, journal); `npm test` collected 721/721 (JSON reporter vs `find`), 19800 passed, red ONLY on the four activity oracle gates (weighted-score ratchet: `jupoxe`, `tobajo` -- the two journaled risers; style 9, swimlane 8, text 5 census pins -- every slug a journaled T1/T2 mover or the two raw-float ulp pins `katopo`/`bulasi` journaled above); typecheck, lint, build exit 0; `docs/catalog.md` no drift; `git diff --name-only HEAD~1` = write-set only. | Gate row per the brief; all reds re-pinned once at T3. | `t2-npm-test.log`, `t2-vitest.json` |
