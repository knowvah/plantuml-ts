# Decision journal — sequence-participant-symbols

Append a row for every non-trivial judgement call. "Non-trivial" means: if a
reasonable developer might have chosen differently, log it.

| Date | Task | Decision / finding | Rationale and evidence | Flag for review |
|---|---|---|---|---|
| 2026-08-28 | T1 | Executed the mission in-session rather than dispatching each task to the agent named in its table (`typescript-pro` here). | This session's harness instructs "Do not call the AgentTool unless the user requested it"; the user asked for the brief to be executed, not for agents. Every task spec is self-contained enough to execute directly, and the orchestrator reads the Java itself, which removes the `verify-agent-claims-si31` failure mode. | Flagged — revisit if a later batch's parallelism makes serial execution the bottleneck. |
| 2026-08-28 | T1 | **AMENDS D1/D2 (stop condition 3).** Only TWO of the six contracted `SymbolParticipantType` values are USymbol-backed upstream. `database` → `USymbols.DATABASE.asSmall` (`ComponentRoseDatabase.java:70`) and `queue` → `USymbols.QUEUE.asSmall` (`ComponentRoseQueue.java:63-64`). `boundary`/`control`/`entity` build their glyph from the `svek/` drawing classes directly — `new Boundary(biColor)` / `new Control(biColor)` / `new EntityDomain(biColor)` — never through a `USymbol`. `collections` has no glyph at all: `Rose.java:145-150` dispatches it to `ComponentRoseParticipant(..., collections=true, ...)`, which draws a SECOND `URectangle` offset by `getDeltaCollection() = 4` (`ComponentRoseParticipant.java:93-96, :107-112`). | Read from `Rose.java:137-190` and all five `ComponentRose*` bodies, not from a filename. D1's "faithful path, not the simplified emitters" survives intact — the correction is only about WHICH faithful primitive each kind uses. The README's Batch-3 framing ("the five missing types … All five are already ported under `src/core/decoration/symbol/`") is wrong for four of the five; the classes ARE all ported, but three live in `src/core/svek/` and collections needs no class. | **Flagged for review** — Batch 3 (T4/T5) must be read against this table, not against the README's. |
| 2026-08-28 | T1 | Contract widened additively, not changed: `geo` gained optional `background`/`border` (`Paint`); `opts` has NO `measurer`; `display` is accepted but read by no branch yet. | The glyph needs a fill and a stroke, and `ParticipantGeo.background`/`.border` already carry the inline-`#color`-over-style precedence layout resolved — reading colours from `theme` alone would drop `participant X #red`. `ParticipantGeo` satisfies the widened shape structurally, so any call site written against the brief's literal type still compiles. No measurer: `renderSequence(geo, theme)` holds none (measurement is layout-only in this engine), and no branch here lays text out — `renderer.ts#renderNameBlock` already draws the label, with the stereotype rows and sprite badges this engine models separately from upstream's single `getTextBlock()`. A `measurer?` field would have been a knob with no caller. | — |
| 2026-08-28 | T1 | Inlined `document-shell.ts#renderDrawableToFragment`'s six-line body instead of calling it, solely to thread `SvgOption.scale`. | That helper hard-codes scale 1. The glyph geometry comes from klimt's own upstream pixel constants, which `scale-geo.ts`'s input-scaling cannot reach, so the seam uses klimt's own scale seam — `SvgGraphicsCore#format`'s `x * option.scale`, upstream's `SvgGraphics.java:466-473` — and hands klimt an unscaled `geo.x / k`. Rejected: a `<g transform="scale(k)">` wrapper, which the jar never emits (`scale-geo.ts` header) and which would change the `g` child count the mission measures against. Round-trip is exact at k=1 and exact to the emitter's 4 decimals elsewhere. | — |
| 2026-08-28 | T1 | `COLLECTIONS_ROUND_CORNER = 0`, not 5. | `RoundCorner 5` is scoped to `sequenceDiagram { participant { … } }` alone (`skin/plantuml.skin:183-185`) and `COLLECTIONS`'s style signature is `root, element, sequenceDiagram, collections` with no `participant` bucket (`ParticipantType.java:77-78`), so it falls through to the root default `RoundCorner 0` (`skin/plantuml.skin:13`). Derived, not fitted; T4 should still confirm it against a real collections golden. | Flagged for T4. |
| 2026-08-28 | T1 | **Write-set expansion (journaled, generated-artifact only): `docs/catalog.md`.** Regenerated with `npm run catalog`; the diff is the one new module row plus the module/name counts. | `tests/architecture/catalog.test.ts` is a drift gate over a generated index, and it was GREEN on `main` and RED with the new module present (verified both ways by moving the file aside). No task in this mission owns the file, but leaving it stale adds a FOURTH red test beyond the three the brief pins as the known baseline — a worse violation of the same gate than the write-set expansion. Same "write-set expansion (journaled, additive-only)" pattern this repo already uses (e.g. `u-graphic-svg.ts`'s own note). | Flagged — later batches will hit this again only if they add another module. |
| 2026-08-28 | T1 | Batch-1 gate verified, not assumed: `git status` shows only the two declared files plus `docs/catalog.md`, and the sequence ratchet reports exactly the three known failures at their pinned baselines (`fobube` 375, `junaxa` 673, `rugeco` 433). Four per-task gates green (`typecheck`, `lint`, `vitest run tests/unit` — 584 files / 11700 tests, `build`). New file coverage 98.4 stmt / 97.3 branch / 90.9 func. | The one uncovered function is `DRIVER_BOUNDER.calculateDimension`: `UGraphicSvg.build` requires a bounder but no glyph in this module draws a `UText`, so it is unreachable by construction rather than untested. | — |
| 2026-08-28 | T2/T3 | Batch-2 gate: `sequence-ratchet-adjudicate.ts --base a885f9da` over 1141 fixtures — **regression=0**, artefact=0, substructure=0, improved=10, inconclusive=20, unchanged=1111, skipped 0. **`junaxa-14-biko373` CLOSES**: 725 → 323, and its body-group child count is now 41 with histogram `g 7, rect 6, ellipse 2, path 7, text 13, line 3, polygon 3` — the golden's, exactly. `fobube-11-nifo424` (375) and `rugeco-70-muro754` (433) both `unchanged`. | Histogram measured through `renderFixtureSequence` + `DeterministicMeasurer` + `fixtureIncludeStore()` explicitly, never `renderSync`; throwaway script deleted. | — |
| 2026-08-28 | T2/T3 | **Three fixtures went red that were green: `cebeje-70-bada975` (492→812), `fenino-82-nusu462` (458→719), `tukobo-89-zebi935` (457→734).** The adjudicator returns `inconclusive` for all three, not `regression`, because a distance-0 fixture emits no `svg/g[1][childCount]` record and absence is ambiguous by design. Resolved by direct measurement in a detached worktree at `a885f9da` rather than guessed. **Diagnosis (full artefact). Mechanism:** the pre-T2 `renderDatabaseShape` emitted FIVE top-level elements per database glyph (`rect` + 2 `line` + `ellipse` + arc `path`) where the jar emits two (`USymbolDatabase.java:62-79`); in these same three fixtures `boundary`/`control`/`entity` fall through to `renderer.ts`'s default `rect(...)` branch and emit ONE element where the jar emits an ellipse-plus-path glyph. The two errors cancelled in the aggregate count, so all three measured child-distance 0 with a wrong composition. T2 removed the over-emission; the surviving under-emission is now visible. **Origin:** `src/diagrams/sequence/renderer.ts:151-158` — the default rectangle branch. **Causal chain:** measured at both refs — cebeje 52→46 against golden 52, fenino 45→39 against 45, tukobo 44→41 against 44, each delta exactly `-3 x (number of database glyphs)`, the 5-to-2 element swap; golden composition for cebeje is `ellipse 8, path 8, polygon 6, line 6, rect 2` against our `ellipse 2, path 6, polygon 4, line 4, rect 8`, so the surplus `rect` ARE the un-glyphed participants. **Ruled out:** (a) T3's sizing — a width/height change moves coordinates, not element counts, and every histogram delta is an element-kind swap matching the database glyph's own shape, with `text` and `g` counts identical at both refs; (b) an error in T1's new glyph — `junaxa`'s histogram now matches its golden exactly, which is impossible if the database path were wrong. **Not a fidelity loss: a previously-masked deficit made measurable, owned by Batch 3 (T4/T5).** | Batch 2's stated invariants both hold (zero `regression`; `junaxa` closed; `fobube`/`rugeco` untouched), so this is not a stop condition. It IS a hard expectation on the next gate. | **Flagged — Batch 3 MUST return these three to green. If it does not, that is the stop, and this artefact is the starting point, not a fresh diagnosis.** |
| 2026-08-28 | T4/T5 | Batch-3 gate: `sequence-ratchet-adjudicate.ts --base a0c37e50` over 1141 fixtures — **regression=0**, artefact=13, substructure=0, improved=17, inconclusive=17, unchanged=1094, skipped 0. Every one of the 17 `inconclusive` rows has a NULL score at BOTH refs, i.e. the fixture fails to measure at all; none is an unadjudicated rise. All 13 `artefact` rows have a FALLEN child distance (e.g. `caluni-22-noci624` 44→24, `gijamu-35-vale058` 39→27, `zedama-25-riru948` 8→2). | The rise-with-falling-distance shape is exactly what the adjudicator exists to sanction: `compareNodes` charges each short-circuit `units(actual) + units(expected)`, so the same verdict costs more once our document grows toward the golden's size. | — |
| 2026-08-28 | T4/T5 | **Two of the three fixtures Batch 2 exposed are resolved structurally: `cebeje-70-bada975` and `fenino-82-nusu462` now match their goldens' body-group histograms EXACTLY** (cebeje 52 `ellipse 8, g 6, line 6, path 8, polygon 6, rect 2, text 16`; fenino 45 `ellipse 8, g 6, line 4, path 8, polygon 4, rect 2, text 13`). `junaxa-14-biko373` stays closed at 41. Four more match exactly that did not before: `butofu-60-kene642` (26), `fatake-97-ciki135` (18). Their `weightedScore` is still above its ORIGINAL pin because the pins predate the mission and the residual is coordinate/attribute drift, not structure — T7 adjudicates against `main`, not against the pin. | Measured through `renderFixtureSequence` + `DeterministicMeasurer` + `fixtureIncludeStore()` explicitly; script deleted. | — |
| 2026-08-28 | T4/T5 | **`tukobo-89-zebi935` does NOT return to green, and it is not a participant-symbol defect. Diagnosis (full artefact). Mechanism:** its golden draws `== System Starts ==` as two `<rect>`s — a full-width 3px `#EEE` rule at `y=150.5` and a `91.188x21` label box at `x=163.658` — plus a `<line>`; this port emits none of the three, at either ref. That is the sequence DIVIDER/separator feature (`skin/plantuml.skin`'s `sequenceDiagram { separator { … } }`), not a participant glyph. **Origin:** the divider arm of the event pass, outside every write-set in this mission. **Causal chain:** ours went 44 → 41 at Batch 2, a delta of exactly `-3` = the database glyph's 5-element hand-rolled cylinder becoming 2 `UPath`s, once (the fixture carries `hide footbox`). The golden's surviving 3-element surplus is the divider's 2 rects + 1 line, never emitted at either ref; at the parent the two errors cancelled and the aggregate count matched at 44 with a wrong composition. **Ruled out:** a participant-symbol cause — the fixture's five participants are 4x `participant` + 1x `database`, all dispatched, and the rectangle dump shows our 4 participant boxes against the golden's 4, with the database a `<path>` on both sides; the only unmatched rectangles are the two divider ones, which carry `fill="#EEE"` and no participant geometry. | Answers the flag raised at the Batch-2 gate: two of three resolved, the third explained and out of scope. | **Flagged for T7 / `planning/next-missions.md`: sequence `==` divider emits nothing.** |
| 2026-08-29 | T6 | **Actor joins the seam as `GlyphParticipantType`, a widening of `SymbolParticipantType` rather than a second composition.** `ComponentRoseActor.java:44-84` is byte-for-byte `ComponentRoseDatabase` — same `topRightBottomLeft(0, 3, 0, 3)`, same `drawInternalU`, same `getPreferredWidth`/`Height` — differing only at `:50`, `actorStyle.getTextBlock(biColor)`. Two type names, one composition. | Duplicating the offset math, the `SvgOption.scale` round-trip and the `head` flip into `renderer-participant-shapes.ts` would have been a structural divergence for the sake of a write-set line. D4's correction survives in the type names: `SymbolParticipantType` still excludes actor. Sizing verified against the jar BEFORE writing: `cebeje-70-bada975`'s golden puts actor Foo1's stickman at cx=84.906, which only `max(27, text+6)` with `dx = (W-27)/2 + 13.5` produces. | — |
| 2026-08-29 | T6 | **Write-set expansions (journaled): `renderer-participant-symbol.ts` (the actor branch, above) and a NEW `sequence-layout-participant-sizing.ts`.** | The citations added by T3/T5/T6 pushed `sequence-layout-participants.ts` to 558 lines, past the 500-line cap the complexity hook enforces; the split takes it to 475. The hook did not fire because every edit in this mission went through Bash rather than Write/Edit — an accidental bypass, so the caps were checked by hand with `lizard` at each gate instead. `renderer.ts` had the same problem after T2 (502 lines) and is 352 now. | Flagged: the Bash-vs-Edit hook bypass is worth knowing for any future autonomous run. |
| 2026-08-29 | T6 | **DIAGNOSED AND FIXED at the Batch-4 gate: this port drew every participant's glyph BEFORE its label; upstream draws the label first.** Found by dumping the body group's child TAG SEQUENCE against the golden rather than reading the score. **Mechanism:** every stacked `ComponentRose*#drawInternalU` calls `textBlock.drawU(...)` in BOTH arms of its `head` branch and `stickman.drawU(ug)` only afterwards (`ComponentRoseDatabase.java:81-88`, `ComponentRoseActor.java:73-80`, identical in Boundary/Control/Entity). **Origin:** `renderer-participant-shapes.ts#renderParticipantBlock`. **Causal chain:** `junaxa`'s golden reads `… text ellipse path text path path rect …`; ours read `… ellipse path text path path text rect …` — a one-position shift that made 8 same-index pairs mismatch on TAG, each charged 12–15 by `compareNodes`'s short-circuit. After the fix junaxa matches the golden at all 41 positions and has ZERO weighted diffs. **Ruled out:** that T6 introduced it — it predates the mission for the head (pre-T6 order was already glyph-then-label there) and T6 only extended it to the footer, where pre-T6 code was accidentally right. Corpus-wide: exact tag sequences 506 (`main`) → 511 (batches 1-3) → **576**; matched positions 48159 → 48327 → **49014** of ~105.9k. | Committed separately as `fix(T4)` so `feat(T6)` stays the isolated actor change the brief asked for. `queue` and `collections` keep glyph-then-label — upstream's own two exceptions (`ComponentRoseQueue#drawInternalU` draws only the glyph; `ComponentRoseParticipant` draws both rects before its text). | — |
| 2026-08-29 | T6 | `weightedScore` RISES on ~73 fixtures across `feat(T6)` + `fix(T4)` while those fixtures get structurally closer, and the adjudicator returns `inconclusive` for every one. **Not a stop condition — the rise is diagnosed, not merely labelled.** Mechanism: removing a tag-mismatch short-circuit removes its 12–15 charge but exposes the subtree it was skipping to real per-attribute comparison; those attributes are dominated by two pre-existing out-of-mission divergences (sequence `LEFT_MARGIN` 30 vs the jar's 10; `text-anchor="middle"` vs the jar's left-anchored `textLength` runs). The adjudicator's `svg/g[1][childCount]` proxy is structurally blind to it: the record is absent whenever the child COUNT already matched, which is exactly when a pure re-ordering happens. | Recorded in `DIVERGENCES.md` as an instrument limitation, alongside `sequence-frame-background-pass`'s mirror-image entry. | — |
| 2026-08-29 | T7 | Close-out. Adjudicated at BOTH refs, `--base` alone (never with `--snapshot`). vs Batch-4 parent `74bc5c93`: regression 0, artefact 0, improved 68, inconclusive 22, unchanged 1051, skipped 0. vs `main`: **regression 0**, artefact 10, improved **80**, inconclusive 19, unchanged 1032, **skipped 0**. `junaxa-14-biko373` 725 → **333** and CLOSED on the child-count criterion; `fobube-11-nifo424` 402 and `rugeco-70-muro754` 543 both `unchanged`. Every one of the 12 ratchet risers carries a verdict: 10 `artefact` (child distance fell), plus `tukobo-89-zebi935` and `gucare-93-petu502` diagnosed in full in `findings/adjudication.md` §3a/§3b. | Σ `weightedScore` deliberately NOT reported as a single number: over 1141 fixtures it is dominated by a handful of 2000–7500 rows whose size swamps everything this mission moved, so it would be a worse summary than the per-fixture split. Said so in the findings rather than printing a figure that reads as precision. | — |
| 2026-08-29 | T7 | **Re-pinning NOT performed; left to the maintainer.** Zero regressions and zero unadjudicated rises, so 10 `artefact` rows plus `gucare-93-petu502` qualify on the adjudicator's own rule — but `tukobo-89-zebi935` does not: its rise is the sequence `==` divider gap (`ComponentRoseDivider#drawInternalU` draws five elements to our two), which this mission neither caused nor fixed, and which was invisible at `main` only because the database glyph's over-emission cancelled it. | `repin-sequence-baselines.ts` was never run and no baseline JSON was edited by any task. A baseline write that bakes in a known-unfixed deficit is the maintainer's call, not the orchestrator's. | **Flagged for the maintainer — decision required.** |
