# Decision journal — sequence-participant-symbols

Append a row for every non-trivial judgement call. "Non-trivial" means: if a
reasonable developer might have chosen differently, log it.

| Date | Task | Decision / finding | Rationale and evidence | Flag for review |
|---|---|---|---|---|
| 2026-08-28 | T1 | Executed the mission in-session rather than dispatching each task to the agent named in its table (`typescript-pro` here). | This session's harness instructs "Do not call the AgentTool unless the user requested it"; the user asked for the brief to be executed, not for agents. Every task spec is self-contained enough to execute directly, and the orchestrator reads the Java itself, which removes the `verify-agent-claims-si31` failure mode. | Flagged — revisit if a later batch's parallelism makes serial execution the bottleneck. |
| 2026-08-28 | T1 | **AMENDS D1/D2 (stop condition 3).** Only TWO of the six contracted `SymbolParticipantType` values are USymbol-backed upstream. `database` → `USymbols.DATABASE.asSmall` (`ComponentRoseDatabase.java:70`) and `queue` → `USymbols.QUEUE.asSmall` (`ComponentRoseQueue.java:63-64`). `boundary`/`control`/`entity` build their glyph from the `svek/` drawing classes directly — `new Boundary(biColor)` / `new Control(biColor)` / `new EntityDomain(biColor)` — never through a `USymbol`. `collections` has no glyph at all: `Rose.java:145-150` dispatches it to `ComponentRoseParticipant(..., collections=true, ...)`, which draws a SECOND `URectangle` offset by `getDeltaCollection() = 4` (`ComponentRoseParticipant.java:93-96, :107-112`). | Read from `Rose.java:137-190` and all five `ComponentRose*` bodies, not from a filename. D1's "faithful path, not the simplified emitters" survives intact — the correction is only about WHICH faithful primitive each kind uses. The README's Batch-3 framing ("the five missing types … All five are already ported under `src/core/decoration/symbol/`") is wrong for four of the five; the classes ARE all ported, but three live in `src/core/svek/` and collections needs no class. | **Flagged for review** — Batch 3 (T4/T5) must be read against this table, not against the README's. |
| 2026-08-28 | T1 | Contract widened additively, not changed: `geo` gained optional `background`/`border` (`Paint`); `opts` has NO `measurer`; `display` is accepted but read by no branch yet. | The glyph needs a fill and a stroke, and `ParticipantGeo.background`/`.border` already carry the inline-`#color`-over-style precedence layout resolved — reading colours from `theme` alone would drop `participant X #red`. `ParticipantGeo` satisfies the widened shape structurally, so any call site written against the brief's literal type still compiles. No measurer: `renderSequence(geo, theme)` holds none (measurement is layout-only in this engine), and no branch here lays text out — `renderer.ts#renderNameBlock` already draws the label, with the stereotype rows and sprite badges this engine models separately from upstream's single `getTextBlock()`. A `measurer?` field would have been a knob with no caller. | — |
| 2026-08-28 | T1 | Inlined `document-shell.ts#renderDrawableToFragment`'s six-line body instead of calling it, solely to thread `SvgOption.scale`. | That helper hard-codes scale 1. The glyph geometry comes from klimt's own upstream pixel constants, which `scale-geo.ts`'s input-scaling cannot reach, so the seam uses klimt's own scale seam — `SvgGraphicsCore#format`'s `x * option.scale`, upstream's `SvgGraphics.java:466-473` — and hands klimt an unscaled `geo.x / k`. Rejected: a `<g transform="scale(k)">` wrapper, which the jar never emits (`scale-geo.ts` header) and which would change the `g` child count the mission measures against. Round-trip is exact at k=1 and exact to the emitter's 4 decimals elsewhere. | — |
| 2026-08-28 | T1 | `COLLECTIONS_ROUND_CORNER = 0`, not 5. | `RoundCorner 5` is scoped to `sequenceDiagram { participant { … } }` alone (`skin/plantuml.skin:183-185`) and `COLLECTIONS`'s style signature is `root, element, sequenceDiagram, collections` with no `participant` bucket (`ParticipantType.java:77-78`), so it falls through to the root default `RoundCorner 0` (`skin/plantuml.skin:13`). Derived, not fitted; T4 should still confirm it against a real collections golden. | Flagged for T4. |
| 2026-08-28 | T1 | **Write-set expansion (journaled, generated-artifact only): `docs/catalog.md`.** Regenerated with `npm run catalog`; the diff is the one new module row plus the module/name counts. | `tests/architecture/catalog.test.ts` is a drift gate over a generated index, and it was GREEN on `main` and RED with the new module present (verified both ways by moving the file aside). No task in this mission owns the file, but leaving it stale adds a FOURTH red test beyond the three the brief pins as the known baseline — a worse violation of the same gate than the write-set expansion. Same "write-set expansion (journaled, additive-only)" pattern this repo already uses (e.g. `u-graphic-svg.ts`'s own note). | Flagged — later batches will hit this again only if they add another module. |
| 2026-08-28 | T1 | Batch-1 gate verified, not assumed: `git status` shows only the two declared files plus `docs/catalog.md`, and the sequence ratchet reports exactly the three known failures at their pinned baselines (`fobube` 375, `junaxa` 673, `rugeco` 433). Four per-task gates green (`typecheck`, `lint`, `vitest run tests/unit` — 584 files / 11700 tests, `build`). New file coverage 98.4 stmt / 97.3 branch / 90.9 func. | The one uncovered function is `DRIVER_BOUNDER.calculateDimension`: `UGraphicSvg.build` requires a bounder but no glyph in this module draws a `UText`, so it is unreachable by construction rather than untested. | — |
| 2026-08-28 | T2/T3 | Batch-2 gate: `sequence-ratchet-adjudicate.ts --base a885f9da` over 1141 fixtures — **regression=0**, artefact=0, substructure=0, improved=10, inconclusive=20, unchanged=1111, skipped 0. **`junaxa-14-biko373` CLOSES**: 725 → 323, and its body-group child count is now 41 with histogram `g 7, rect 6, ellipse 2, path 7, text 13, line 3, polygon 3` — the golden's, exactly. `fobube-11-nifo424` (375) and `rugeco-70-muro754` (433) both `unchanged`. | Histogram measured through `renderFixtureSequence` + `DeterministicMeasurer` + `fixtureIncludeStore()` explicitly, never `renderSync`; throwaway script deleted. | — |
| 2026-08-28 | T2/T3 | **Three fixtures went red that were green: `cebeje-70-bada975` (492→812), `fenino-82-nusu462` (458→719), `tukobo-89-zebi935` (457→734).** The adjudicator returns `inconclusive` for all three, not `regression`, because a distance-0 fixture emits no `svg/g[1][childCount]` record and absence is ambiguous by design. Resolved by direct measurement in a detached worktree at `a885f9da` rather than guessed. **Diagnosis (full artefact). Mechanism:** the pre-T2 `renderDatabaseShape` emitted FIVE top-level elements per database glyph (`rect` + 2 `line` + `ellipse` + arc `path`) where the jar emits two (`USymbolDatabase.java:62-79`); in these same three fixtures `boundary`/`control`/`entity` fall through to `renderer.ts`'s default `rect(...)` branch and emit ONE element where the jar emits an ellipse-plus-path glyph. The two errors cancelled in the aggregate count, so all three measured child-distance 0 with a wrong composition. T2 removed the over-emission; the surviving under-emission is now visible. **Origin:** `src/diagrams/sequence/renderer.ts:151-158` — the default rectangle branch. **Causal chain:** measured at both refs — cebeje 52→46 against golden 52, fenino 45→39 against 45, tukobo 44→41 against 44, each delta exactly `-3 x (number of database glyphs)`, the 5-to-2 element swap; golden composition for cebeje is `ellipse 8, path 8, polygon 6, line 6, rect 2` against our `ellipse 2, path 6, polygon 4, line 4, rect 8`, so the surplus `rect` ARE the un-glyphed participants. **Ruled out:** (a) T3's sizing — a width/height change moves coordinates, not element counts, and every histogram delta is an element-kind swap matching the database glyph's own shape, with `text` and `g` counts identical at both refs; (b) an error in T1's new glyph — `junaxa`'s histogram now matches its golden exactly, which is impossible if the database path were wrong. **Not a fidelity loss: a previously-masked deficit made measurable, owned by Batch 3 (T4/T5).** | Batch 2's stated invariants both hold (zero `regression`; `junaxa` closed; `fobube`/`rugeco` untouched), so this is not a stop condition. It IS a hard expectation on the next gate. | **Flagged — Batch 3 MUST return these three to green. If it does not, that is the stop, and this artefact is the starting point, not a fresh diagnosis.** |
