# Decision journal — stdlib-run-isolation

| When | Task | Decision | Why | Evidence |
|---|---|---|---|---|
| 2026-08-21 | orch | Branch `fix/stdlib-run-isolation` cut from `main` @ db4c19ce | Brief says "from main at or after 2202f55c"; db4c19ce is the brief's own last doc commit, a descendant. | `git log --oneline -1` |
| 2026-08-21 | orch | Batch 0 executed in parallel: T0 -> `debugger`, T1 -> `typescript-pro`, both sonnet | D5 routing; write-sets disjoint (`scripts_scratch/T0/**` + `.agent-notes/sri-T0.md` vs `.agent-notes/sri-T1.md`). No dependency between them. | batch-0/overview.md |
| 2026-08-21 | orch | Pre-loaded `.agent-notes/sre-T0.md` (SI34 mechanism) into T0's prompt as a read-set pointer, not verbatim | rules/parallelism.md s0: pass only what bears on the write-set. The note is 171 lines and the agent's read-set already names it; a pointer plus the three load-bearing facts avoids distractor leakage while guaranteeing it is read. | `.agent-notes/sre-T0.md` present on disk |
| 2026-08-21 | orch | Amended both batch-0 tasks mid-flight: `npm test` and `npm run build` deferred to the orchestrator's batch gate; agents run only typecheck + lint | Both gates invoke `buildStdlibPackages()` against `packages/<pkg>/generated/` via `globalSetup`. T0 is deliberately mutating `assets/stdlib/` inputs and racing builders on that exact path, so a concurrent suite would (a) confound both duration readings and (b) inject a third builder into T0's reproduction, risking a result that is an artifact of the measurement rather than the mechanism. Push-forward call (harness shape); the gate itself is not weakened, only relocated to the orchestrator on a settled machine. | `tests/helpers/build-stdlib-globalsetup.ts:86-89` calls `buildStdlibPackages()` once per vitest process |
| 2026-08-21 | T1 | Census complete: **21 consumers, 8 concurrent readers, 8 seam-eligible, 13 immovable**. SI34's "two import sites" is confirmed a substantial under-count; this brief's "at least six" is confirmed and sharpened to 8. | Independent second measurement, method stated in the note (grep then manually open every hit to separate a real fs/import/pack call from a doc-comment mention). `src/` confirmed zero references. | `.agent-notes/sri-T1.md` |
| 2026-08-21 | orch | Verified T1's claims rather than accepting them | Standing lesson from SI31: subagent reports have carried confident claims that measurement disproved. Spot-checked 3 load-bearing cites -- `stdlib-dts-import-specifier.test.ts:63` (`readdirSync(.../generated)`), `stdlib-eager-omission.test.ts:27` (`generatedPath` helper), `sprite-package-files.test.ts:182` (`readFileSync(.../generated/*.js)`) -- all three match the cited lines exactly. Also independently confirmed `grep -rn "packages/stdlib" src/` is empty and the two `npm pack` sites are `sprite-package-files.test.ts:81` / `stdlib-package-files.test.ts:57`. | this session's greps |
| 2026-08-21 | T1 | Gates: typecheck exit 0, lint exit 0. `npm test` / `npm run build` deferred to the orchestrator's batch gate per the mid-flight amendment; T1 correctly declined to report numbers it did not measure. | note-only write-set (`.agent-notes/sri-T1.md`), nothing to move | agent report |
| 2026-08-21 | T0 | **Residual REPRODUCED.** `reproduced:true`, signature `ENOENT: no such file or directory, open '<repo>/packages/stdlib-tupadr3/generated/tupadr3.remote.js'`, rate **2/5 (40%)**, meanAttemptsToFail **21325.5**, `silentCorruptionObserved:false`. Stop 1 NOT triggered; mission proceeds to batch 1. | Writer toggles a throwaway `.puml` under `assets/stdlib/tupadr3/` between every real `buildStdlibPackages()` call; predicate flipped false on every iteration (rebuild count == toggle count, never a skip). | `.agent-notes/sri-T0.md`, `scripts_scratch/T0/writer-mutate.ts` |
| 2026-08-21 | orch | Verified T0's mutation is a REAL build input, not a synthetic stand-in -- the claim the whole reproduction rests on | `readBundlePaths` (`scripts/build-stdlib-packages/emit-remote-manifest.ts:54-66`) walks `assets/stdlib/<assetFolder>` and filters `.puml` via `isPumlFile`, so adding/removing `_race-sentinel-T0.puml` genuinely changes `tupadr3.remote.js`'s emitted content and therefore the content hash. Confirmed by reading the function, not by trusting the harness's own doc comment. | this session |
| 2026-08-21 | orch | Tree restore verified after T0's mutation loop | `git status --short` shows no modified tracked file except this journal; `ls assets/stdlib/tupadr3/` has no `_race-sentinel-T0.puml`; untracked set is exactly the two notes + `scripts_scratch/T0/{writer-mutate,reader-verify,reader-diag}.ts`. | this session |
| 2026-08-21 | T0 | **Exposure is ~30x narrower than the bug SI34 fixed**: 21325.5 mean attempts vs SI34's 700.6, and 2/5 runs vs SI34's 5/5. This is the single most decision-relevant number for T2's ADR. | Both figures measured with the same two-process shape against the same file; the difference is that B must now win a race against a much shorter rmSync window that only opens on a genuine input change. | `.agent-notes/sre-T0.md` vs `sri-T0.md` |
| 2026-08-21 | T0 | Signature wording delta recorded, not chased: `ENOENT: no such file or directory, open '...'` here vs SI34's `Cannot find module '...'`. Same missing-file condition, different Node error surface (fs read vs module resolution). | T0 flagged it rather than silently normalising it. Not root-caused -- out of scope for a task whose job was existence, not error-surface taxonomy. | agent report |
| 2026-08-21 | T0 | Silent-corruption variant actively looked for and NOT found: every non-throwing import checked across ~100k+ attempts, zero mismatched/truncated bodies. | Explained by write-size vs syscall atomicity, not merely absence of observation -- T0 gave a mechanism, which is what makes the negative usable in the ADR. | `.agent-notes/sri-T0.md` |
| 2026-08-21 | orch | Batch 0 gates ALL GREEN. `npm test` exit 0, 626 files / 16034 passed / 2 skipped / 1 todo, coverage 95.44 / 90.47 / 96.95 / 96.53. typecheck exit 0. lint exit 0. build exit 0 with exactly the 3 documented pre-existing TS2591/TS2503 notes in `src/core/include-resolver-node.ts`. `git diff --name-only -- src/` EMPTY and `main..HEAD -- src/` EMPTY. render-manifest: **"OK: 0 expected moves, 0 unexpected"** over 3158 fixtures. | this session | all six gate commands |
| 2026-08-21 | orch | Suite wall-clock measured TWICE, both reported honestly. Run 1: 60.03 s wrapped, load1 4.87, spotlight quiesced. Run 2: 58.46 s wrapped / 57.45 s vitest, load1 18.71 with `corespotlightd` at 172.6% (run 1's own file churn re-triggered the indexer). No hard ceiling per the brief; both readings sit in the 55.75-57.80 s band SI34 measured and near `main`'s 56.85 s. No regression to investigate. | Counter-intuitively the *higher-load* run was faster, which is why a single reading is not trusted here. | `/tmp/sri-b0-test.log` |
| 2026-08-21 | orch | Waited for `corespotlightd` (98.3%) to reach 0.0% before the first timing run rather than measuring through it | The brief names this exact trap: SI34 burned two readings on self-induced Spotlight reindexing after heavy file churn, and T0's harness churns `assets/stdlib/` hard. Polled at 10 s until clear (~2 min). | `uptime` + `ps -Aceo pcpu,comm` before each run |
| 2026-08-21 | orch | `scripts_scratch/T0/` kept on disk but NOT committed | Push-forward item ("probes deleted before commit") satisfied: committing by pathspec leaves it untracked, so it lands in no commit. Kept on disk because T3's acceptance clause requires re-running T0's harness and quoting before/after -- deleting it now would destroy the only proof T3 can offer. If the session ends before the user resumes, `.agent-notes/sri-T0.md` documents the harness well enough to rebuild. | `batch-2/T3-implement.md` acceptance clause 1 |
| 2026-08-21 | T2 | ADR-003 written (Status: **Proposed**). Four options: A read seam + snapshot, B per-run isolated dir (the declined one), C accept permanently, D extend the lock to readers. **Recommends A.** | `planning/adr/ADR-003-stdlib-run-isolation.md` | ADR-003 |
| 2026-08-21 | T2 | **Crux finding: option B collapses into option A once D3 is honoured, and neither touches the published surface.** `npm pack --dry-run` resolves via `cwd` against the real package dir, and the 4 `tsconfig.json` type-check the real on-disk tree, so the canonical tree can never be *relocated* -- only *supplemented*. The 2026-08-21 refusal's stated premise (relocating `generated/` changes what the packages publish) therefore does not survive the pack-test constraint. Its surviving basis was a cost/benefit call against 2 known import sites; the real number is 8 concurrent readers. | This is the single most decision-relevant output of the mission and the reason it was re-opened. | `stdlib-package-files.test.ts:57-60`, `packages/*/tsconfig.json:6` |
| 2026-08-21 | orch | **Routing defect found: `architect-reviewer` has no Write/Edit/Bash** (Read/Grep/Glob + Serena only). It returned the ADR text in its report and disclosed plainly that it could neither write files nor run gates, rather than claiming gates it had not run. Orchestrator transcribed both files and ran the gates. D5's routing table assigned a document-*authoring* task to a review-only agent. | Recorded for future missions: route authoring tasks to an agent with `Write`, or plan for orchestrator transcription. Not a stop condition -- no work was lost. | `.agent-notes/sri-T2.md` |
| 2026-08-21 | orch | **Amended option A before presenting it: the snapshot must be taken INSIDE the build lock.** As drafted, A copied "immediately after the canonical build", relying on globalSetup's before-any-worker-spawns guarantee -- which is intra-process and does not make the copy safe. `release()` runs in a `finally` at `scripts/build-stdlib-packages.ts:301` before `buildStdlibPackages()` returns, so a caller-side snapshot is taken outside the lock and a second process can `rmSync` canonical mid-copy. Result would be a **torn snapshot**: silently incomplete, read for the whole suite -- strictly worse than today's loud `ENOENT`. | Recorded as a visible amendment in the ADR rather than silently corrected, because it changes the shape of the work: A is no longer a pure caller-side change. | `scripts/build-stdlib-packages.ts:293-302`, `build-lock.ts:264` |
| 2026-08-21 | orch | **Measured the copy cost the ADR left as an unmeasured "<100 ms" estimate**: 2.3 MB / 24 files across the four trees; 5 `cpSync` runs = 6.5 / 4.3 / 4.1 / 3.9 / 4.0 ms. ~20x cheaper than estimated, once per process against a ~57 s suite. | The brief demands measured cost, not adjectives. Cost is now settled and is not a reason to prefer any option. | this session |
| 2026-08-21 | T2 | Gates: typecheck exit 0, lint exit 0 (orchestrator-run). `npm test` / `npm run build` not re-run -- the full six-gate set passed on this identical tree minutes earlier and T2 changed only two new untracked docs. | docs-only change | `/tmp/t2-tc.log`, `/tmp/t2-lint.log` |
| 2026-08-21 | orch | **MISSION HALTED at stop 3, as designed.** Batch 2 is not dispatchable: `batch-2/T3-implement.md` still carries its "not dispatchable as written" paragraph, and D4 forbids any agent beginning implementation -- including of the recommended option -- before the user decides. This halt is the mission's purpose, not a failure state. | D4, stop 3, `batch-1/overview.md` | -- |
| 2026-08-21 | USER | **Option D approved by the user**: extend the existing build lock to cover readers. Not the ADR's recommendation (which was A). The mission resumes at batch 2 on this authority and no other. | User's decision at the stop-3 gate, in response to the options table in ADR-003. D closes all in-worker consumers including the 2 pack tests, which A and B structurally cannot reach. | this session |
| 2026-08-21 | orch | Accepted risks that come with D, stated so they are not rediscovered as surprises: (1) up to `maxWaitMs`=30 s reader-side tail latency under contention -- a new failure class not present today; (2) coupling of test files to `build-lock.ts` internals, a coupling smell per `rules/architecture.md`. Both were disclosed in the ADR before the choice. | The user chose D with these on the table. | ADR-003 option D row |
| 2026-08-21 | orch | **Hazard 1 for D: the lock steals itself after 60 s, and `release()` does not verify ownership.** `isStale` (`build-lock.ts:203-205`) reclaims a *parsed* lock when `!isProcessAlive(pid) OR now - acquiredAt > staleAgeMs` (`DEFAULT_STALE_AGE_MS = 60_000`) -- deliberately, since "a live-but-wedged holder is exactly as dangerous as a dead one" for a builder. But `release()` is an unconditional `rmSync` (`:270`). So a reader holding the lock longer than 60 s has it reclaimed underneath it: mutual exclusion is silently lost AND the reader's own release then deletes a stranger's lock. Correct for a ~1 s builder; a real ceiling for a reader. | T3 must keep critical sections far below 60 s and make release ownership-safe (or state why unnecessary). | `build-lock.ts:196-210, 264-270` |
| 2026-08-21 | orch | **Hazard 2 for D: acquisition blocks synchronously.** `sleepSync` (`build-lock.ts:128`, wired at `:147`) is "a genuine synchronous sleep, not a spin loop and not a timer". Inside a vitest worker that freezes the event loop for up to `maxWaitMs`=30 s. With 8 readers contending, this could dominate suite wall-clock or trip per-test timeouts. The ADR disclosed a 30 s tail-latency risk; this is its concrete mechanism. | T3 must measure suite impact, not assume it. The brief forbids trading correctness for speed, but an unexplained wall-clock jump is still a finding to report. | `build-lock.ts:127-128, 147, 290` |
| 2026-08-21 | orch | **Corrected the ADR's file count for D from 10 to 8.** ADR-003's option D row says "10 of 10 canonical-touching in-worker consumers ... 10 files", counting "the 8 concurrent-reader tests **and** the 2 pack tests". The 2 pack tests are *within* the 8 (census rows #11 and #15), so that double-counts them. The true in-worker set is 8 files: rows #11-#18. Rows #19-#21 are spawned child processes, env-gated and skipped by default, and are immovable by design. | Corrected before pinning T4's write-set, since the count defines it. | `.agent-notes/sri-T1.md` consumer table, "In vitest worker?" column |
| 2026-08-21 | orch | Checked and CLEARED a suspected deadlock: `prepack` does NOT call the builder. `packages/stdlib-{aws,tupadr3}/package.json` `prepack` runs `copy-assets.mjs` only (copies `assets/stdlib/` -> the package's `assets/`); `stdlib` and `stdlib-all` have no prepack. So wrapping a pack test in the lock cannot re-enter `acquireBuildLock` from the spawned npm child. | Had prepack rebuilt, D would have self-deadlocked the pack tests for 30 s then thrown. Verified rather than assumed. | `packages/*/package.json` scripts |
