# expected-moves.txt — stdlib-run-isolation
#
# Fixtures allowed to move `render-manifest`'s SHA-256 between batches
# (`manifest-diff.py`). One bare slug per line; `#` comments ignored.
#
# THIS MISSION CHANGES NO `src/` AND NO FIXTURE. The list is intentionally
# EMPTY: every batch must report "0 unexpected". This is test/build
# infrastructure; any rendering movement means something is wrong.
#
#   Batch 0 (T0, T1) — none.
#   Batch 1 (T2)     — none (an ADR; no code).
#   Batch 2 (T3, T4) — none.
#   Batch 3 (T5)     — none.
