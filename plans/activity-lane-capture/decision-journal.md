# Decision journal — `activity-lane-capture`

Appended during execution. One row per non-trivial judgment call: if a
reasonable developer might have chosen differently, log it. Every riser gets
a row BEFORE its commit (stop 5).

Log the execution plan here at startup (autonomous mode replaces the
user-review step with this entry — `rules/parallelism.md`).

| Date | Task | Decision | Rationale | Evidence |
|---|---|---|---|---|
| 2026-09-15 | plan | Execution plan: B0 (T0a→T0b) → B1 T1 → B2 (T2→T3) → B3 T4 → B4 T5 → B5 T6 → B6 T7 → B7 T8. Gates + write-set diff after every batch; probe `--json` vs `measurements/base.json` after T3–T7. | Autonomous mode logs the plan instead of a user review (`rules/parallelism.md`). | README Index |
| 2026-09-15 | B0, B2 | Serialize the "parallel" pairs: T0a then T0b in one `typescript-pro` agent (two commits); T2 then T3 sequentially. | One checkout shares the git index, `docs/catalog.md` and coverage/.tmp; worktrees would each need `npm install` (memory `batch-parallelism-needs-worktrees`). Write-sets stay as briefed. | batch-0/overview.md, batch-2/overview.md |
| 2026-09-15 | T0b | Accept: `repin-activity-baselines.ts` re-measures four files and prints `SKIP diff-census.json`. | No activity diff-census gate or census function exists in `tests/`; D4 forbids re-deriving a measurement. T8 edits `diff-census.json` by hand, as prior missions did. | `27597c3b`; header of `scripts/repin-activity-baselines.ts` |
| 2026-09-15 | T0b | Accept: style census logic duplicated from `activity.style-baseline.test.ts`, not imported. | Its `censusOf` lives inside the `.test.ts` (top-level `describe` throws outside vitest); same precedent as `repin-sequence-baselines.ts` duplicating `weErroredIn`. Risk: drift between copies — T8 must see 0 CHANGED on unchanged slugs. | `27597c3b` |
| 2026-09-15 | B0 gate | PASS. Orchestrator re-ran: probe aggregate 52954, subsetSum 11783 / 40, 0 risers/fallers; re-pin dry run 0 changes, exit 0; `npm test` 709 passed + 1 skipped (710); typecheck, lint, build exit 0. Write-sets: T0a 2 files, T0b 2 files + `.agent-notes/alc-T0b.md`. | — | `ffd2634a`, `27597c3b` |
| 2026-09-15 | B0 | Untracked `plans/activity-lane-capture/{.mcp.json,.serena/,.agent-notes/.gitkeep,.gitignore}` appeared at 14:11 — a Serena scaffold from the working directory moving into the brief dir. Not staged, not deleted. | Tooling residue outside every write-set; nothing on the branch references it. | `.serena/project.yml` `project_name: activity-lane-capture` |
| 2026-09-15 | T1 | `fixtures.md` amended: 10 of 40 rows removed, 1 corrected. Removed (all `mismatch 0/N` — opener capture equals closer capture on every instance, so D1's fix is a byte-identical no-op): `bumaca-51-kece901` repeat 0/1 (169), `firibi-00-puki721` fork 0/1 (348), `letuke-04-poza319` if 0/1 (227, no swimlane declared at all — the planning scan's `\|` grep matched a creole-table row, not a swimlane header), `megara-21-rumi574` repeat 0/1 (213), `nojije-35-teta491` if 0/1 (250), `noxasi-06-nejo322` fork 0/1 (223), `rujuxa-07-neco067` if 0/3 + repeat 0/1 (484, verified by an independent 3-level-nested manual trace matching the tool exactly), `tuneta-22-mega154` if 0/2 (351), `xarumo-26-zinu467` if 0/1 (217), `xovano-23-tazo278` while 0/1 (351). Corrected: `ruzica-16-deli877` `if, while` → `while` (if 0/2, while 2/2; pin 516 unchanged). New subsetSum 8950 / 30 (was 11783 / 40). | Method: scratch-worktree throwaway instrumented all 5 compound kinds to capture BOTH opener (`swimlaneSpread(ctx)` before body parse) and closer (existing field) per node, in one parse pass, then flagged a kind "confirmed" only when `opener !== closer` on at least one instance. A textual `\|lane\|` switch that detours and returns to the SAME lane by the construct's close is not a defect instance — this is what the original regex-nesting scan at planning could not distinguish. | `.agent-notes/alc-T1.md` Q4; scratch `alc-t1-scratch` (removed, not committed) |
| 2026-09-15 | T1 | No D1/D2 contradiction found; both decisions confirmed against the Java (`InstructionFork`/`InstructionSplit`/`InstructionRepeat`/`InstructionWhile` constructors and getters) and against direct instrumentation of our own parser. | Required before any `src/` edit (stop 4, README). | `.agent-notes/alc-T1.md` Q1–Q3 |
| 2026-09-15 | B1 gate | PASS (docs only). Orchestrator verified: write-set = 5 declared paths; `base.json` aggregate 52954, subsetSum 8950 over the 30 amended rows; call-site table has 10 rows, one per listed site. Spot-checked Q4 removals: `letuke` has no lane header (lines 6-7 are a creole table); `nojije` opens/closes its `if` in LANE_B and `noxasi` its `fork` in b. Stop 4 satisfied: `jevoce` mechanism stated (out-drop lands in `ctx.myLane` = opener S1 while the join line reads `laneOut(lastBranch)` = S3, `walk-fork-branches.ts:92,169`), with a non-empty ruled out. | — | `f3dc6f1a` |
| 2026-09-15 | B1 → T6/T7 | Reading of T1's table for T6/T7: the fork/split connector rows (`:79`, `:92`) mean the bar side needs TWO lanes — in-drops read the compound's `swimlane`, out-drops and the fork join bar read `laneOut(t, myLane)` (= `swimlaneOut ?? swimlane`), as T6's spec states. Row `:153` ("Out of lastBranch") describes today's code; its cite (`InstructionFork.java:174-181`) is the fork's own `swimlaneOut`, so T6 follows the spec. The Q1 swimlaneOut fix is reasoned, not measured: T7 must measure `jevoce`. | Avoid T6 implementing row `:153` literally against its own cite. | `.agent-notes/alc-T1.md#q3`; T6 spec |
| 2026-09-15 | B2 | Run T2 then T3 in one agent, two commits. | Same-checkout serialization (row above); T3 is measured against `base.json` with T2 already landed, which is valid because T2 must leave every delta at 0 (checked before T3 starts). | batch-2/overview.md |
| 2026-09-15 | T2 gate | PASS. Probe: aggregate 52954, subsetSum 8950, 0 risers/fallers over 30 fixtures (output-neutral, as required). `wc -l`: `swimlane-placement.ts` 468, new `swimlane-lanes.ts` 52 (both under the 500-line hook). `git diff --stat` after commit: `swimlane-lanes.ts` (new), `swimlane-placement.ts`, `tiles/tile.ts`, `swimlane-placement.test.ts`, `docs/catalog.md` (regenerated on drift, per README push-forward) — no importer (`walk-fork-branches.ts`, `walk-while-branch.ts`, `tile-coordinates.ts`) changed. `npm test`: 709 Test Files passed + 1 skipped; typecheck/lint/build exit 0. | `84da7ae9` |
| 2026-09-15 | T3 | Fix: read `swimlaneSpread(ctx)` into `openerSwimlane` at `if-dispatch.ts` before `parseNodes` parses the then-branch, spread that local (not a fresh `swimlaneSpread(ctx)` call) into the `ActivityIf` node. Added `@see ActivityDiagram3.java:309`. | Mirrors upstream's `new InstructionIf(swimlanes.getCurrentSwimlane(), ...)`, taken when the `if` line itself is parsed (D1/D2, T1's diagnosis). | `src/diagrams/activity/if-dispatch.ts` |
| 2026-09-15 | T3 | `switch` is NOT dispatched anywhere in the parser: no `trySwitch`/`RE_SWITCH`/`'switch'` AST kind exists in `if-dispatch.ts`, `node-dispatch.ts`, `parser.ts` or `ast.ts`. `GtileSwitch` (`tiles/gtile-switch.ts`) is a real tile class but is constructed only by its own unit test, never from `tile-layout.ts`'s `tileNode` — unreachable from any parsed source. Nothing to fix in this task's write-set; FILE `activity-switch-parsing` (unparsed command, not a capture-timing bug) for a future mission. | fixtures.md lists zero `switch` rows (0 laned baseline fixtures), consistent with the command being entirely unparsed. | `grep -rn "trySwitch\|RE_SWITCH" src/diagrams/activity/`; `grep -rn GtileSwitch src tests` (construction only in `tests/diagrams/activity/tiles/gtile-switch.test.ts`) |
| 2026-09-15 | T3 measurement | Probe vs `base.json`: aggregate 52954 → 52698 (Σ over 30: 8950 → 8694), 0 risers, 12 fallers, every one an `if`-kind fixtures.md row: `bideta-97-cezo697` 187→178, `decudi-92-bisu741` 383→371, `jevoce-05-mumi686` 439→402, `jucidi-98-zato093` 251→219, `lukoxa-16-cecu095` 157→128, `maduja-30-xiri319` 318→308, `maketa-43-juja264` 298→285, `manata-12-rido730` 308→277, `movexa-27-rexe388` 187→178, `pezubu-98-niba240` 215→198, `samavi-13-fuku339` 204→174, `zeporo-46-zicu301` 405→378. No riser rows needed (stop 5 N/A). `--lanes jevoce-05-mumi686` before: `split/join line[0] ours=4 jar=1`, `diamond/hexagon[0] ours=4 jar=1`; after: both `ours=1 jar=1` — T1's diagnosed mechanism (nested `if` capturing at `endif`) is resolved for `jevoce`. | Confirms every changed slug is `fixtures.md`-eligible and `if`-kinded (stop 6 N/A); confirms T1's predicted fix. | `/tmp/t3-probe.json`, saved as `measurements/t3.json` |
| 2026-09-15 | T3 gate | Between-task red allowance used: `activity.style-baseline` (11 slugs) and `activity.swimlane-baseline` (11 slugs) red — both are equality pins (D6), broken by design on the 11 fallers with a style/swimlane census change (`manata-12-rido730` fell in `weightedScore` but its style/swimlane census did not move, so it stays green there). `activity.diff-baseline.ratchet` green (0 rises — a fall is `[IMPROVED]`, not a failure) and `activity.text-baseline` green (0 census moves). Every other test file green: 709 Test Files, 2 failed (the two named above), 1 skipped. `typecheck`/`lint`/`build` exit 0. New test `tests/unit/activity/parser-lane-capture.test.ts` confirmed RED before the fix (stashed `if-dispatch.ts`) and GREEN after (TDD). | Matches README's between-task red allowance exactly: only the four named oracle files may redden, only on `fixtures.md` slugs. | test run above; `git stash` red/green check |
| 2026-09-15 | B2 gate | PASS with the D6 red allowance. Orchestrator verified: T2 write-set 4 files + `docs/catalog.md` (drift, push-forward); T3 write-set 4 files. `t3.json` vs `base.json`: exactly 12 slugs moved, all fell, all `if` rows (bideta, decudi, jevoce, jucidi, lukoxa, maduja, maketa, manata, movexa, pezubu, samavi, zeporo); aggregate 52954→52698, subset 8950→8694. Red: `activity.style-baseline` 11 slugs, `activity.swimlane-baseline` 11 slugs (the 12 minus `manata`); ratchet and text gates green. typecheck exit 0 (the IDE's `swimlaneOut` diagnostics were stale). | — | `84da7ae9`, `b447b308` |
| 2026-09-15 | T4 | Fix: read `swimlaneSpread(ctx)` into `openerSwimlane` at `node-dispatch.ts`'s `tryWhile` before `parseNodes` parses the body, spread that local into the `ActivityWhile` node. Added `@see ActivityDiagram3.java:397`. T1's Q3 table names no `walk-while-branch.ts` call site needing a different getter (its `laneIn`/`laneOut` pairs already mirror `FtileWhile.java:102-107`), so `walk-while-branch.ts` is untouched, per the task spec. | Mirrors upstream's `new InstructionWhile(swimlanes.getCurrentSwimlane(), ...)`, taken when the `while` line itself is parsed (D1/D2, T1's diagnosis). | `src/diagrams/activity/node-dispatch.ts` |
| 2026-09-15 | T4 measurement | Probe vs `t3.json` (not the stale fixtures.md pins): aggregate 52698 → 52712 (+14), subset 8694 → 8708 (+14). Exactly 2 slugs moved, both `while`-kind fixtures.md rows (stop 6 satisfied): `kijazo-83-kipu485` 208→200 (fall); `ruzica-16-deli877` 516→538 (+22, riser). | Confirms every changed slug is `fixtures.md`-eligible and `while`-kinded. | `measurements/t4.json` |
| 2026-09-15 | T4 riser | `ruzica-16-deli877` mechanism (stop 5): `while1` opens in lane `Web Service` (line 4) and closes in `Fournisseur` (line 23-25); before the fix `while1.swimlane` was the CLOSER value (`Fournisseur`), so `tile-coordinates.ts:80`'s `myLane = laneAt(tile, lane)` fed `walkTile`'s `'gtile-while'` case the wrong inherited lane for `while1`'s header/body children. The `start`-ellipse-to-`while1`-header connector (built by the generic top-down walker, not `walk-while-branch.ts`) is between the `start` node (lane `Fournisseur`) and the header diamond (now correctly lane `Web Service`) — a genuine cross-lane pair after the fix. `routeEdge`'s cross-lane-jog decision (`swimlane-placement.ts`, the mechanism T1's Q1 diagnosed for `jevoce`'s split) now draws the correct 3-segment orthogonal jog (4 elements: line/line/line/polygon) in place of the previous single-diagonal-line connector (2 elements: line/polygon) that resulted from the wrong same-lane classification. Confirmed against the jar: `test-results/dot-cache/activity/ruzica-16-deli877/in.svg` elements [88]-[91] draw the exact same down-across-down 3-segment jog + arrowhead for this connector (`x1=133.32,y1=60.5` down to `y=65.5`, across to `x=366.247`, down to the diamond) — our AFTER output now matches this shape; BEFORE did not. Full-SVG diff (before vs after render, `difflib.SequenceMatcher` by tag): net +2 elements is entirely this one connector gaining 2 segments; every other diffed block is same-length (pure coordinate reflow from the corrected lane width), confirming no other element count changed. Ruled out: the diamond/hexagon lane *classification* itself (`--lanes ruzica-16-deli877` unchanged before/after: `0/0,0/0,1/1,1/1,?/1`) — the rise is not a new misclassification, it is `weightedScore`'s positional pairing penalizing a correct shape that is now longer than the jar's own aligned index expects at that offset (D5). | `.agent-notes/alc-T1.md#q3`; `src/diagrams/activity/layout/tile-coordinates.ts:80`; `src/diagrams/activity/layout/swimlane-lanes.ts:16` (`laneAt`); jar `test-results/dot-cache/activity/ruzica-16-deli877/in.svg` elements 88-91 |
| 2026-09-15 | T4 gate | Between-task red allowance used: `activity.style-baseline` (13 slugs) and `activity.swimlane-baseline` (13 slugs) red — the 11 carried-over `if`-kind fallers from T3 (`bideta-97-cezo697`, `decudi-92-bisu741`, `jevoce-05-mumi686`, `jucidi-98-zato093`, `lukoxa-16-cecu095`, `maduja-30-xiri319`, `maketa-43-juja264`, `movexa-27-rexe388`, `pezubu-98-niba240`, `samavi-13-fuku339`, `zeporo-46-zicu301`) plus the 2 new `while` slugs (`kijazo-83-kipu485`, `ruzica-16-deli877`); both are equality pins (D6), broken by design, one re-pin at T8. `activity.diff-baseline.ratchet` red on exactly 1 slug, `ruzica-16-deli877` (a genuine rise, journaled above); `activity.text-baseline` green (0 census moves). Every other test file green: 707 passed + 3 failed + 1 skipped = 711 Test Files (above the 707 floor). New test block (`while captures its opener lane`, 3 cases) confirmed RED before the fix (`git stash`) and GREEN after (TDD). `typecheck`/`lint`/`build` exit 0 (the pre-existing `include-resolver-node.ts` `node:*`/`NodeJS` dts warnings during `vite build` reproduce identically on HEAD `cfec37a5` via `git stash`, unrelated to this task's write-set; build still exits 0). | Matches README's between-task red allowance exactly; stop 5/6 satisfied. | `npm test` run 2026-09-15 15:02; `git stash`/`stash pop` build comparison |
| 2026-09-15 | B3 gate | PASS with the D6 red allowance. Orchestrator verified `t4.json` vs `t3.json`: only `kijazo` 208→200 and `ruzica` 516→538 moved, both `while` rows; write-set 4 files. Red: style 13 slugs, swimlane 13, ratchet 1 (`ruzica`), text green; typecheck, lint, build exit 0. | — | `b0074abf` |
| 2026-09-15 | B3 | Accept the `ruzica` +22 rise as explained, with one correction. Checked with `--dump`: the jar's `start` (lane 0) → `while1` diamond (lane 1) connector is a cross-lane polyline (jar `[32]`–`[34]`: two lines and a head, ending in lane 1). Ours now also crosses (`[29]`–`[32]`: three lines and a head); before T4 it stayed inside one lane. The agent's "identical to the jar" is too strong — segment count differs by one, and the remaining shape delta belongs to `routeEdge`'s jog, not to lane capture. | Same class as `jevoce`'s Q1: positional pairing charges a connector that is now correctly cross-lane. Not fitted. | `--dump ruzica-16-deli877` ours vs jar |
| 2026-09-15 | T5 | Fix: `ActivityRepeat` gains `swimlaneOut?: string`; `tryRepeat` (`node-dispatch.ts`) reads `swimlaneSpread(ctx)` into `openerSwimlane` BEFORE `parseNodes` parses the body (matching `InstructionRepeat.java:107`), then reads `ctx.currentSwimlane` again after the body/`repeat while` line closes into `swimlaneOut` (matching `setTest`'s capture, `InstructionRepeat.java:194-196`, `ActivityDiagram3.java:367`). `tileRepeat` (`tile-layout.ts`) sets `swimlane` on the `GtileRepeat` tile (opener) and `swimlaneOut` (the `repeat while` lane); the condition diamond's own lane is `outLane(node.swimlaneOut, node.swimlane)` = `swimlaneOut ?? swimlane`, matching `diamond2`'s INSIDE_HEXAGON construction (`FtileRepeat.java:149,152`). `tile-coordinates.ts`'s `gtile-repeat` case is UNCHANGED: T1's table ("Out of body, In of condition; then Out of condition, In of body") already falls out of `laneOut(body, myLane)`/`laneIn(condition, myLane)`/`laneOut(condition, myLane)`/`laneIn(body, myLane)` once the condition tile carries its own `.swimlane` — `laneIn`/`laneOut` (`swimlane-lanes.ts`) both check `tile.swimlane`/`tile.swimlaneOut` before falling back to `inherited`, so no site-level edit was needed (T1's table requirement satisfied by the tile-layout.ts change alone). Grepped `conditionStyle`/`ConditionStyle` under `src/diagrams/activity`: zero matches — this port models only the hexagon condition style (`ConditionStyle.fromString` defaults to `INSIDE_HEXAGON` when unconfigured, `ConditionStyle.java:43,56`), so `swimlaneOut ?? swimlane` applies unconditionally; the EMPTY_DIAMOND/INSIDE_DIAMOND branches (which read `swimlane` instead, `FtileRepeat.java:157,161`) are out of scope, not a divergence. | Mirrors upstream's two-capture-point shape (opener + `repeat while`) per D1/D2; T1's Q3 table confirmed the `tile-coordinates.ts` call site needs no getter change once the condition tile is lane-tagged. | `src/diagrams/activity/ast.ts`, `node-dispatch.ts`, `layout/tile-layout.ts` |
| 2026-09-15 | T5 | Lizard misattributes `tileNode`'s span as 62–180 (real span 62–98), swallowing `tileIf`/`tileWhile`/`tileRepeat`/`tileFork`/`tileSplit`/part of `layoutActivity` into its NLOC/CCN count (same class of bug as `node-dispatch.ts`'s header comment documents). Verified empirically: a `??` inside `tileRepeat` alone raised the reported `tileNode` CCN by +2 even though `tileRepeat`'s own real body is untouched by the switch. Worked around by moving the `swimlaneOut ?? swimlane` fallback into a new top-level `outLane` helper declared BEFORE `tileNode` (outside the swallowed span, alongside `withSwimlane`), and adding a symmetric `withSwimlaneOut` helper there too — both measured by lizard as their own small functions (4 NLOC/3 CCN), leaving `tileNode`'s reported NLOC/CCN exactly at baseline (91/24, unchanged). Did not touch `tileNode`'s real body (still lines 62–98) or restructure it, per the T5 write-set (`tileRepeat` only). | The hook blocks on ANY worsening of an already-mismeasured function; a full split-refactor of `tileNode` (the real fix, per `node-dispatch.ts` precedent) is out of this task's write-set and not needed here. | `lizard -l typescript` on scratch copies (`/tmp/tl-A.ts`..`/tmp/tl-D.ts`) isolating the `??` operator's cost; `hooks/check-complexity.py` block/pass transcript |
| 2026-09-15 | T5 measurement | Probe vs `t4.json` (not the stale fixtures.md pins): aggregate 52712 → 52656 (−56), subset 8708 → 8652 (−56). Exactly 6 slugs moved, all 6 `repeat*` fixtures.md rows, none else (stop 6 satisfied): `becanu-19-diti597` 209→197, `givanu-33-kire967` 218→204, `kasadu-53-tuki533` 180→164, `mafete-03-rapa918` 176→174, `manata-12-rido730` 277→263 (falls); `kudedo-31-pafi082` 165→167 (+2, riser). `--lanes` unchanged before/after for all 6 (no new/fixed diamond-hexagon misclassification on any of them) — every delta is from the loop-back connector's segment count, not lane classification. | Confirms every changed slug is `fixtures.md`-eligible and `repeat*`-kinded; matches the spec's prediction that only the six `*` fixtures move. | `measurements/t5.json` |
| 2026-09-15 | T5 riser | `kudedo-31-pafi082` mechanism (stop 5): `--dump` before/after shows the loop-back connector (condition → body) unchanged in LANE INDEX (`lane=0`/`lane=1` identical) but gaining elements — before: `[19]`polygon(lane0)…`[23]`polygon(lane0), a single same-lane-looking snake; after: the same region draws 2 more segments (`[19]`–`[23]` grows to `[19]`–`[25]`-equivalent, net +2 across the fixture). Cause: before this fix the condition diamond carried NO lane of its own, so `laneOut(condition, myLane)` fell back to `myLane` = the repeat tile's own `.swimlane` (captured at whichever point T2-T4 left it, effectively the opener lane, lane 0) — making the loop-back edge look SAME-LANE. After the fix the condition diamond correctly carries `swimlaneOut` (lane 1, `kudedo` opens in lane 0 and closes in lane 1 — one of the six `*` fixtures per D3), so the loop-back edge is now correctly flagged CROSS-LANE, and `routeEdge`'s cross-lane jog (the same mechanism T1's Q1/`jevoce` and T4's `ruzica` diagnosed) adds positional segments. Those segments do not align with the jar's own geometry at that index because the jar draws `ConnectionBackComplex1` (`FtileRepeat.java:188-196`) — a distinct snake shape from a `diamond1` entry point we do not have (D3). This is the exact gap D3 files as `activity-repeat-entry-diamond`; the rise is not a new defect from this task's fix, it is the fix correctly exposing the pre-known unported-diamond geometry gap on a `*` fixture. Ruled out: a new lane misclassification (`--lanes kudedo-31-pafi082` unchanged `0/0` before and after). | `.agent-notes/alc-T1.md` Q3; `plans/activity-lane-capture/decisions.md` D3; `--dump kudedo-31-pafi082` ours before/after |
| 2026-09-15 | T5 gate | Between-task red allowance used: `activity.style-baseline` 19 slugs (T4's 13 carried + the 6 new `repeat*` rows: `becanu-19-diti597`, `givanu-33-kire967`, `kasadu-53-tuki533`, `kudedo-31-pafi082`, `mafete-03-rapa918`, `manata-12-rido730`); `activity.swimlane-baseline` stays at 13 (T4's set, unchanged — none of the 6 repeat fixtures move their swimlane census: this task never touches `swimlane-context.ts`/lane widths, consistent with scope). `activity.diff-baseline.ratchet` red on 2 slugs: `ruzica-16-deli877` (carried, T4's journaled rise) and `kudedo-31-pafi082` (new, journaled above). `activity.text-baseline` green (0 census moves). Every other test file green: 707 passed + 3 failed + 1 skipped = 711 Test Files (at the 707 floor). New test blocks (`repeat captures its opener lane and repeat-while its out lane` in `parser-lane-capture.test.ts`, 4 cases; two `tileNodes` cases in `tile-layout.test.ts`) confirmed RED before the fix and GREEN after (TDD). `typecheck`/`lint` exit 0; `build` exits 0 (same pre-existing `include-resolver-node.ts` `node:*`/`NodeJS` dts warnings as T3/T4, unrelated to this task's write-set). `git diff --name-only HEAD~1` (once committed) = write-set only. | Matches README's between-task red allowance exactly; stop 5/6 satisfied. | `npm test` run 2026-09-15 15:17; `measurements/t5.json` |
