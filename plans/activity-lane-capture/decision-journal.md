# Decision journal — `activity-lane-capture`

Appended during execution. One row per non-trivial judgment call: if a
reasonable developer might have chosen differently, log it. Every riser gets
a row BEFORE its commit (stop 5).

Log the execution plan here at startup (autonomous mode replaces the
user-review step with this entry — `rules/parallelism.md`).

| Date | Task | Decision | Rationale | Evidence |
|---|---|---|---|---|
| 2026-09-15 | plan | Execution plan: B0 (T0a→T0b) → B1 T1 → B2 (T2→T3) → B3 T4 → B4 T5 → B5 T6 → B6 T7 → B7 T8. Gates + write-set diff after every batch; probe `--json` vs `measurements/base.json` after T3–T7. | Autonomous mode logs the plan instead of a user review (`rules/parallelism.md`). | README Index |
| 2026-09-15 | B0, B2 | Serialize the "parallel" pairs: T0a then T0b in one `typescript-pro` agent (two commits); T2 then T3 sequentially. | One checkout shares the git index, `docs/catalog.md` and coverage/.tmp; worktrees would each need `npm install` (memory `batch-parallelism-needs-worktrees`). Write-sets stay as briefed. | batch-0/overview.md, batch-2/overview.md |
| 2026-09-15 | T0b | Accept: `repin-activity-baselines.ts` re-measures four files and prints `SKIP diff-census.json`. | No activity diff-census gate or census function exists in `tests/`; D4 forbids re-deriving a measurement. T8 edits `diff-census.json` by hand, as prior missions did. | `27597c3b`; header of `scripts/repin-activity-baselines.ts` |
| 2026-09-15 | T0b | Accept: style census logic duplicated from `activity.style-baseline.test.ts`, not imported. | Its `censusOf` lives inside the `.test.ts` (top-level `describe` throws outside vitest); same precedent as `repin-sequence-baselines.ts` duplicating `weErroredIn`. Risk: drift between copies — T8 must see 0 CHANGED on unchanged slugs. | `27597c3b` |
| 2026-09-15 | B0 gate | PASS. Orchestrator re-ran: probe aggregate 52954, subsetSum 11783 / 40, 0 risers/fallers; re-pin dry run 0 changes, exit 0; `npm test` 709 passed + 1 skipped (710); typecheck, lint, build exit 0. Write-sets: T0a 2 files, T0b 2 files + `.agent-notes/alc-T0b.md`. | — | `ffd2634a`, `27597c3b` |
| 2026-09-15 | B0 | Untracked `plans/activity-lane-capture/{.mcp.json,.serena/,.agent-notes/.gitkeep,.gitignore}` appeared at 14:11 — a Serena scaffold from the working directory moving into the brief dir. Not staged, not deleted. | Tooling residue outside every write-set; nothing on the branch references it. | `.serena/project.yml` `project_name: activity-lane-capture` |
| 2026-09-15 | T1 | `fixtures.md` amended: 10 of 40 rows removed, 1 corrected. Removed (all `mismatch 0/N` — opener capture equals closer capture on every instance, so D1's fix is a byte-identical no-op): `bumaca-51-kece901` repeat 0/1 (169), `firibi-00-puki721` fork 0/1 (348), `letuke-04-poza319` if 0/1 (227, no swimlane declared at all — the planning scan's `\|` grep matched a creole-table row, not a swimlane header), `megara-21-rumi574` repeat 0/1 (213), `nojije-35-teta491` if 0/1 (250), `noxasi-06-nejo322` fork 0/1 (223), `rujuxa-07-neco067` if 0/3 + repeat 0/1 (484, verified by an independent 3-level-nested manual trace matching the tool exactly), `tuneta-22-mega154` if 0/2 (351), `xarumo-26-zinu467` if 0/1 (217), `xovano-23-tazo278` while 0/1 (351). Corrected: `ruzica-16-deli877` `if, while` → `while` (if 0/2, while 2/2; pin 516 unchanged). New subsetSum 8950 / 30 (was 11783 / 40). | Method: scratch-worktree throwaway instrumented all 5 compound kinds to capture BOTH opener (`swimlaneSpread(ctx)` before body parse) and closer (existing field) per node, in one parse pass, then flagged a kind "confirmed" only when `opener !== closer` on at least one instance. A textual `\|lane\|` switch that detours and returns to the SAME lane by the construct's close is not a defect instance — this is what the original regex-nesting scan at planning could not distinguish. | `.agent-notes/alc-T1.md` Q4; scratch `alc-t1-scratch` (removed, not committed) |
| 2026-09-15 | T1 | No D1/D2 contradiction found; both decisions confirmed against the Java (`InstructionFork`/`InstructionSplit`/`InstructionRepeat`/`InstructionWhile` constructors and getters) and against direct instrumentation of our own parser. | Required before any `src/` edit (stop 4, README). | `.agent-notes/alc-T1.md` Q1–Q3 |
| 2026-09-15 | B1 gate | PASS (docs only). Orchestrator verified: write-set = 5 declared paths; `base.json` aggregate 52954, subsetSum 8950 over the 30 amended rows; call-site table has 10 rows, one per listed site. Spot-checked Q4 removals: `letuke` has no lane header (lines 6-7 are a creole table); `nojije` opens/closes its `if` in LANE_B and `noxasi` its `fork` in b. Stop 4 satisfied: `jevoce` mechanism stated (out-drop lands in `ctx.myLane` = opener S1 while the join line reads `laneOut(lastBranch)` = S3, `walk-fork-branches.ts:92,169`), with a non-empty ruled out. | — | `f3dc6f1a` |
| 2026-09-15 | B1 → T6/T7 | Reading of T1's table for T6/T7: the fork/split connector rows (`:79`, `:92`) mean the bar side needs TWO lanes — in-drops read the compound's `swimlane`, out-drops and the fork join bar read `laneOut(t, myLane)` (= `swimlaneOut ?? swimlane`), as T6's spec states. Row `:153` ("Out of lastBranch") describes today's code; its cite (`InstructionFork.java:174-181`) is the fork's own `swimlaneOut`, so T6 follows the spec. The Q1 swimlaneOut fix is reasoned, not measured: T7 must measure `jevoce`. | Avoid T6 implementing row `:153` literally against its own cite. | `.agent-notes/alc-T1.md#q3`; T6 spec |
| 2026-09-15 | B2 | Run T2 then T3 in one agent, two commits. | Same-checkout serialization (row above); T3 is measured against `base.json` with T2 already landed, which is valid because T2 must leave every delta at 0 (checked before T3 starts). | batch-2/overview.md |
| 2026-09-15 | T2 gate | PASS. Probe: aggregate 52954, subsetSum 8950, 0 risers/fallers over 30 fixtures (output-neutral, as required). `wc -l`: `swimlane-placement.ts` 468, new `swimlane-lanes.ts` 52 (both under the 500-line hook). `git diff --stat` after commit: `swimlane-lanes.ts` (new), `swimlane-placement.ts`, `tiles/tile.ts`, `swimlane-placement.test.ts`, `docs/catalog.md` (regenerated on drift, per README push-forward) — no importer (`walk-fork-branches.ts`, `walk-while-branch.ts`, `tile-coordinates.ts`) changed. `npm test`: 709 Test Files passed + 1 skipped; typecheck/lint/build exit 0. | `84da7ae9` |
| 2026-09-15 | T3 | Fix: read `swimlaneSpread(ctx)` into `openerSwimlane` at `if-dispatch.ts` before `parseNodes` parses the then-branch, spread that local (not a fresh `swimlaneSpread(ctx)` call) into the `ActivityIf` node. Added `@see ActivityDiagram3.java:309`. | Mirrors upstream's `new InstructionIf(swimlanes.getCurrentSwimlane(), ...)`, taken when the `if` line itself is parsed (D1/D2, T1's diagnosis). | `src/diagrams/activity/if-dispatch.ts` |
| 2026-09-15 | T3 | `switch` is NOT dispatched anywhere in the parser: no `trySwitch`/`RE_SWITCH`/`'switch'` AST kind exists in `if-dispatch.ts`, `node-dispatch.ts`, `parser.ts` or `ast.ts`. `GtileSwitch` (`tiles/gtile-switch.ts`) is a real tile class but is constructed only by its own unit test, never from `tile-layout.ts`'s `tileNode` — unreachable from any parsed source. Nothing to fix in this task's write-set; FILE `activity-switch-parsing` (unparsed command, not a capture-timing bug) for a future mission. | fixtures.md lists zero `switch` rows (0 laned baseline fixtures), consistent with the command being entirely unparsed. | `grep -rn "trySwitch\|RE_SWITCH" src/diagrams/activity/`; `grep -rn GtileSwitch src tests` (construction only in `tests/diagrams/activity/tiles/gtile-switch.test.ts`) |
| 2026-09-15 | T3 measurement | Probe vs `base.json`: aggregate 52954 → 52698 (Σ over 30: 8950 → 8694), 0 risers, 12 fallers, every one an `if`-kind fixtures.md row: `bideta-97-cezo697` 187→178, `decudi-92-bisu741` 383→371, `jevoce-05-mumi686` 439→402, `jucidi-98-zato093` 251→219, `lukoxa-16-cecu095` 157→128, `maduja-30-xiri319` 318→308, `maketa-43-juja264` 298→285, `manata-12-rido730` 308→277, `movexa-27-rexe388` 187→178, `pezubu-98-niba240` 215→198, `samavi-13-fuku339` 204→174, `zeporo-46-zicu301` 405→378. No riser rows needed (stop 5 N/A). `--lanes jevoce-05-mumi686` before: `split/join line[0] ours=4 jar=1`, `diamond/hexagon[0] ours=4 jar=1`; after: both `ours=1 jar=1` — T1's diagnosed mechanism (nested `if` capturing at `endif`) is resolved for `jevoce`. | Confirms every changed slug is `fixtures.md`-eligible and `if`-kinded (stop 6 N/A); confirms T1's predicted fix. | `/tmp/t3-probe.json`, saved as `measurements/t3.json` |
| 2026-09-15 | T3 gate | Between-task red allowance used: `activity.style-baseline` (11 slugs) and `activity.swimlane-baseline` (11 slugs) red — both are equality pins (D6), broken by design on the 11 fallers with a style/swimlane census change (`manata-12-rido730` fell in `weightedScore` but its style/swimlane census did not move, so it stays green there). `activity.diff-baseline.ratchet` green (0 rises — a fall is `[IMPROVED]`, not a failure) and `activity.text-baseline` green (0 census moves). Every other test file green: 709 Test Files, 2 failed (the two named above), 1 skipped. `typecheck`/`lint`/`build` exit 0. New test `tests/unit/activity/parser-lane-capture.test.ts` confirmed RED before the fix (stashed `if-dispatch.ts`) and GREEN after (TDD). | Matches README's between-task red allowance exactly: only the four named oracle files may redden, only on `fixtures.md` slugs. | test run above; `git stash` red/green check |
| 2026-09-15 | B2 gate | PASS with the D6 red allowance. Orchestrator verified: T2 write-set 4 files + `docs/catalog.md` (drift, push-forward); T3 write-set 4 files. `t3.json` vs `base.json`: exactly 12 slugs moved, all fell, all `if` rows (bideta, decudi, jevoce, jucidi, lukoxa, maduja, maketa, manata, movexa, pezubu, samavi, zeporo); aggregate 52954→52698, subset 8950→8694. Red: `activity.style-baseline` 11 slugs, `activity.swimlane-baseline` 11 slugs (the 12 minus `manata`); ratchet and text gates green. typecheck exit 0 (the IDE's `swimlaneOut` diagnostics were stale). | — | `84da7ae9`, `b447b308` |
