# Batch 3 — `while`

Sequential after Batch 2: shares `node-dispatch.ts` with T5–T7.

| ID | Description | Agent | Writes | Depends On | Done |
|---|---|---|---|---|---|
| T4 | Capture the `while` swimlane at its opener | typescript-pro | `src/diagrams/activity/node-dispatch.ts` (`tryWhile` only), `layout/walk-while-branch.ts` (only if T1's table requires), `tests/unit/activity/parser-lane-capture.test.ts` | T2, T3 | [ ] |

Spec: [`T4-while-capture.md`](T4-while-capture.md). Expected movers:
`kijazo`, `ruzica`, `xovano` (per the amended `fixtures.md`).
