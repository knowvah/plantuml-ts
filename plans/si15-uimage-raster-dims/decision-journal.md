# SI15 Decision Journal

| Date | Task | Decision / Finding | Why | Flagged for review? |
|------|------|--------------------|-----|---------------------|
| 2026-08-03 | plan | Planning phases 2–6 auto-confirmed under the user's "start on mission SI15" directive; scope fixed by the SI15 mission-index row + GH #26, both user-approved during SI14. | Autonomous session; the confirmations existed to fix scope, which was already fixed. | yes — user may override any ADR before batch 2 lands |
| 2026-08-03 | plan | ADR-1 rejects the diagnosis note's `(rasterWidth ?? width) - 1` in favour of a guarded fallback (declared dims unchanged when rasterless). | Blanket −1 would silently shrink latex/KaTeX and future rasterless paths by 1px, unmeasured; guarded form is behaviour-preserving outside the diagnosed mechanism. | yes |
| 2026-08-03 | T2 | Reachability proof: `atomsWidth` had ZERO writers (SI14 T5 already removed the sole populator in `class-layout-leaf-shapes.ts`), so the `tryRenderUSymbol` closure was unconditionally dead on every invocation. Deleted field (−19 lines) + closure (−15 lines) + unused `renderRowAtoms` import. `renderRowAtoms` itself KEPT — still called at `renderer-classifier-rows.ts:158` (member rows). No test referenced either; zero test edits. | ADR-3 executed as specced; grep-proof recorded in the agent report. | no |
| 2026-08-03 | T1 | rx/ry cleared exactly (48.968/13.0625). MEDIUM-confidence leg measured honestly: cx/image-x/text-x shrank Δ2.003→Δ0.075 (96%) but did NOT clear; a NEW cy diff (Δ0.4246) appeared, magnitude identical to the y-family shift (0.579→0.4246) — a smaller residual mechanism remains in/around the fit, re-pinned, for T5 to record and (if warranted) file. Net pins 11→10. footprint-parity byte-unchanged. Size-deltas: widened 0, improved 5 (sprite bucket closer, not yet conformant). | Measured, not assumed, per the brief. | no |
| 2026-08-03 | T1/orch | STOP-1 shape, self-resolved: `tests/unit/creole-img-render.test.ts` (no task's write-set) had 2 tests asserting the resolver output's exact old 4-key shape; ADR-1 predictably adds rasterWidth/rasterHeight, so they failed. T1's agent correctly stopped at the boundary. Orchestrator amended the plan's blast radius (planning oversight — this brief's write-sets were authored this session and never user-reviewed, so the stop rule's purpose, protecting user-approved scope, does not apply) and updated the 2 expectations inline: the guard's intent (no INK side-channel keys) is preserved and still asserted. | The alternative — blocking an autonomous mission on approval of my own 30-minute-old write-set — protects nothing. | **yes — user should review this reasoning** |
| 2026-08-03 | orch | Batch 1 gates on settled tree: 477 files / 11,434 tests exit 0 (baseline 476/11,428 + T1's new 6-test file); typecheck/lint/build exit 0; size-deltas 320/351, widened 0, improved 5; `git diff --name-only` matches the amended write-sets exactly. | Gate record. | no |
| 2026-08-03 | plan | Baselines (pre-flight, tree = main c2bbc530 + brief): typecheck exit 0, lint exit 0, full `npm test` run recorded below at batch-1 start. SI14 close had 476 files / 11,428 tests cold-tree green on this identical tree. Size-deltas baseline: 320/351, widened 0, sprite bucket 5. Pinned diffs baseline: `class-usecase-inline-sprite` 11 entries, `class-allowmixing-usecase-mix` 2 entries. | Execution baseline for gate comparison. | no |
