# Decision journal — transition-label-ink

Appended during execution. Every non-trivial judgement call gets a row.

Record here, because the checkpoint and Batch 2 are measured against them:

- **T1's answer for the 1.000** — the mechanism with its `file:line`, or the
  proof that the premise is wrong. This is the mission's pivot.
- **Whether jar's ink is really 357.335.** The whole framing rests on
  `composite width = ink + 35`. If the excavation shows otherwise, say so
  here and correct `evidence.md` in place rather than working around it.
- **The write-set proposed at the checkpoint, and what the human approved.**
- **The `exact` count** before and after, from
  `measure-composite-declared-size.ts` (baseline 2454 / 2642).
- **Any fixture that moved and should not have.**

| Date | Task | Decision | Why | Alternative rejected |
|---|---|---|---|---|
| 2026-08-15 | B1 | Ran T1+T2 in the orchestrator, not two `debugger` subagents | The brief's own rules make "read the Java yourself, and verify any mechanism a subagent reports" load-bearing (`CLAUDE.md`), and the checkpoint report is the orchestrator's deliverable. Both tasks are pure reading over the same two source trees, so the parallelism buys nothing and the relay risks a laundered mechanism. Notes were still written to the two paths the batch table names. | Spawning the two agents as tabled. Rejected on the above; logged rather than silently substituted. |
| 2026-08-15 | T1 | **The (B) mechanism is `marginLabel`, on the right-hand side of the label's marged box** — `SvekEdge.java:372-373` → `TextBlockUtils.java:64-69` → `TextBlockMarged.java:74-77,83` (`ug.draw(UEmpty.create(dim))`) → `LimitFinder.java:159-162`. `UEmpty` renders nothing, so jar's ink legitimately exceeds every element in its own SVG. | Derived from the Java, then confirmed: 261.86 + 113.475 − 18 + 15 + 20 = 392.335 = jar's `5.449097 in × 72`, exact. Predicted in advance on two further fixtures with different labels and matched to within the known quantization. | "Text measurement differs", "a formula-level +1 in the margin layer" (evidence.md §5.6, reopened) — both dead: the +1 is in the INK, and only for labels. |
| 2026-08-15 | T1 | **The brief's two-opposing-mechanisms framing is wrong, and (A) is restated.** Upstream folds the marged BLOCK (`measuredWidth + 2·marginLabel`), not the drawn text. There is one error, `floor(w + 2m) − w − m` = `1 − frac(w)`, and no opposing term. | The −1.525/+0.998 pair was an artefact of assuming the drawn text is the ink. `evidence.md` §3/§5 corrected in place, per this journal's own instruction. | Landing (A) and (B) as two separate terms. There is only one; D3 is satisfied vacuously. |
| 2026-08-15 | T1 | **Confirmed jar's ink IS 357.335** — the `composite = ink + 35` premise the mission rests on holds. | Independently closed on the y axis, which needed no correction hypothesis: inner ink [122, 329] → 207 + 15 + title 14 + 20 = 256 = jar's `height="256"`, exact. `MinMax.getDimension()` is `maxX − minX` (`MinMax.java:151-153`), so the extent is translation-invariant. | Treating the premise as an assumption to work around. |
| 2026-08-15 | T1 | **The `exact` bar is probably not reachable by this fix alone.** After the correction the three fixtures land at ~0.0017 px, not 0.000 — the residual is the already-filed 2dp read-seam quantization (`class-edge-spline-2dp-quantization.md`), 20–80× the harness's 3.6e-5 px epsilon. Reported at the checkpoint rather than resolved. | That note's own ruling defers quantization to the maintainer, and blanket quantization measured 1969/1969 → 1961/1969. Closing it is a separate mission, not a term in this one. | Quantizing `labelX`/`labelY` inside this mission. Would be faithful but is an unapproved scope expansion across every diagram type with edge labels. |
| 2026-08-15 | T2 | **The fix site is inside `src/diagrams/state/`, not `SvekNode`/`GroupMakerState`.** Three files; `TransitionGeo.label.width/height` has exactly one reader (`layout-ink-extent.ts:388-390`). | `grep -rn "label\.width\|label\.height" src/`; the transition renderer reads only `.x/.y/.text`. So the write-set the brief left open can be narrow. | The broad `src/core/svek/` + `src/diagrams/state/` write-set the brief anticipated. |
| 2026-08-15 | B1 | **Repaired the canonical-SVG cache for `somuke-94-buzi673` (gitignored), and DELETED the tracked dot-cache entry the repair induced.** DOT-parity now reads the documented 268/268; the composite baseline stays the documented 271 / 2642 / 2454. | `@startuml Test` makes the jar write `Test.svg`, so the fixture was permanently "missing" and the gate read 267 — a false stop-condition trip waiting for Batch 2. Keeping the induced dot-cache entry would instead have moved the composite baseline to 272 / 2654 / 2466. Full mechanism in `.agent-notes/transition-label-ink-port.md`. | Committing the induced dot-cache entry (a malformed one: `Test.svg`, no `in.svg`, no `.done`), or reverting the canonical too and letting Batch 2 face a 267 gate. |
| 2026-08-15 | B1 | Baseline recorded: `measure-composite-declared-size.ts` **2454 / 2642 exact**, 160 mismatched (111 on width), `lastDigitOnly` 28. The +0.52668 family is **six** fixtures, not three — add `jorere-75-peja265`, `ketibo-84-juzo029`, `zitifa-97-bizo337`. | Re-run at batch start; matches the brief. The extra three carry the identical `Configuring` composite at scope 2. | — |
