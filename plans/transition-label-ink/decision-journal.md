# Decision journal — transition-label-ink

Appended during execution.

Record here, because Batch 2 is measured against them:

- **T1's answer for the 0.998** — the mechanism, or the reason it could not
  be established. This is the mission's pivot.
- **Whether jar's `minX` is 0.** The published arithmetic assumes it; if it
  is not, there is one bug here rather than two, and the brief's framing
  needs correcting in place.
- **The `exact` count** before and after, from
  `measure-composite-declared-size.ts` (baseline 2454 / 2642).
- **Any fixture that moved and should not have.**

| Date | Task | Decision | Why | Alternative rejected |
|---|---|---|---|---|
| | | | | |
