# Decision journal — transition-label-ink

Appended during execution. Every non-trivial judgement call gets a row.

Record here, because the checkpoint and Batch 2 are measured against them:

- **T1's answer for the 1.000** — the mechanism with its `file:line`, or the
  proof that the premise is wrong. This is the mission's pivot.
- **Whether jar's ink is really 357.335.** The whole framing rests on
  `composite width = ink + 35`. If the excavation shows otherwise, say so
  here and correct `evidence.md` in place rather than working around it.
- **The write-set proposed at the checkpoint, and what the human approved.**
- **The `exact` count** before and after, from
  `measure-composite-declared-size.ts` (baseline 2454 / 2642).
- **Any fixture that moved and should not have.**

| Date | Task | Decision | Why | Alternative rejected |
|---|---|---|---|---|
| | | | | |
