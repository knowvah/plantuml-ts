# Decision Journal — si10-usecase-actor-routing

Append one row per non-trivial judgment call. "Non-trivial" means: a
reasonable developer might have chosen differently.

Also log here: quality-gate results per batch, any brief line-number
correction, every measurement the brief asks you to record, and every STOP
with its full output.

**T1 must record the FINAL exported entry-point signature — T2 codes against
that, not against the sketch in `batch-1/overview.md`.**
**T3 must record the per-fixture measured delta — T4 quotes those numbers.**

| Date | Task | Decision | Rationale |
|---|---|---|---|
| 2026-08-01 | planning | Scope set to class-engine routing + the inert sprite guard; full retirement declined | Offered the maintainer four options; option B chosen. Probe B (`widened 2` with both guard branches disabled) shows the `<latex>` route is load-bearing, so retirement needs a home for latex-bearing usecases — its own mission |
| 2026-08-01 | planning | **Probe A: the multi-line `<$sprite>` guard is INERT.** Disabled → `widened 0`, conformant 320/351, cause histogram identical to baseline; `bootstrap-0` and `ruziru-69-xixo434` both `delta 0, conformant true` with AND without it | The 0.029321in widening its doc comment cites no longer reproduces — `svg-sprite-nanoparser`'s two-channel architecture closed it. The guard's own comment records it was "not re-tested for dissolution" after that mission, because `measureUsecase` was then another mission's STOP. This is that re-test |
| 2026-08-01 | planning | Probe A verified NON-VACUOUS before being trusted | Instrumenting the predicate showed it fires on 4 real displays: `"<$bi-globe>\nbi-globe"` (both named fixtures) and two `<$maxime>` link displays. A no-op probe would have produced the same `widened 0` for the wrong reason |
| 2026-08-01 | planning | **Probe B: `<latex>` is load-bearing** — both branches disabled → `widened 2` | Distinguishes "permanent by policy" from "measurably required". This is why ADR-1 keeps `measureUsecase` alive rather than retiring it |
| 2026-08-01 | planning | `sprites` threading added to scope (item 3), maintainer-approved | It is already in scope at `class-layout-helpers.ts:286` — the line above passes it to `measureObjectClassifier` — and simply is not forwarded, so class-diagram usecases are sized with no sprite awareness. Zero class goldens contain a sprite, so it cannot move an existing golden; T3 authors the fixture that covers it |
| 2026-08-01 | planning | Class-engine reachability instrumented rather than assumed | `class C` + `usecase` (no `allowmixing`) does NOT reach `measureUsecaseOrActor` — it routes to the description engine, byte-identical to a pure usecase. Only `actor` (any class diagram) and `allowmixing` (both kinds) reach it. Fixtures must use one of those two or they silently test the wrong engine |
| 2026-08-01 | planning | T3 measures the jar gap rather than assuming zero-diff (ADR-4) | Class conformance is low (census `0/718 → 29/718`) and zero of 310 class goldens carry a usecase, so a new fixture arriving zero-diff is optimistic. Forcing a conformance claim would be the "fabricated oracle data" failure SI9's row already names |
| 2026-08-01 | planning | Authored class fixtures are NOT ratchetable, and the fix is a follow-up not scope | `dot-sync-fixtures.ts`'s `GOLDEN_DIR` is hardcoded to `oracle/goldens/svg-description` and `authoredFixtures` expects `<type>/<slug>/`; class goldens are `oracle/goldens/svg-class/<slug>/`. So no `parity-class.json` entry can exist and the ratchet's eligibility rule cannot be met. Extending it is SI9-shaped work in another subsystem — registered by T4 |
| 2026-08-01 | planning | Batches serialised rather than parallelised | T2 needs T1's export; T3 can only measure after T2; T4's two halves both write `planning/mission-index.md`. The mission is small enough that serialising costs little and removes the shared-worktree hazard entirely |
| 2026-08-02 | T1 | **EXPORTED SIGNATURE (T2 codes against THIS):** `measureUsecaseOrActorLeaf(display: string, symbol: 'usecase' \| 'actor', fontSpec: FontSpec, measurer: StringMeasurer, sprites?: SpriteDimsLookup): Dim` | Defined in the new `leaf-sizing-entity.ts`, re-exported from `leaf-sizing.ts` so that module stays the single owner per ADR-2. Internally builds `{ id: '', display, symbol, children: [] }` and calls `measureEntityLeaf(node, fontSpec, { opts: undefined, sprites, measurer }, false)` |
| 2026-08-02 | T1 | **Write-set crossed: new file `src/diagrams/description/leaf-sizing-entity.ts`** — authorized, not a stop | Adding the entry point pushed `leaf-sizing.ts` to 526 lines, over the repo's 500-line hook cap, making the alternative literally unwritable. The mission README's push-forward conditions name "a ~500-line split" explicitly, and the repo has the precedent (`svg.ts`→`svg-markers.ts`, `leaf-sizing-text.ts`). Agent flagged it rather than proceeding silently, which is the required behaviour. **Verified a PURE MOVE:** `measureEntityLeaf`, `buildSizingEntityParams` and `minWidthFloorBaseline` are byte-identical to the bodies that left `leaf-sizing.ts`. Result: 338 + 219 lines. `measureEntityLeaf` stays module-private (only `measureUsecaseOrActorLeaf` is exported) |
| 2026-08-02 | T1 | `hasUnroutedUsecaseMarkup` is now `return display.includes('<latex>')` | The multi-line sprite arm measured inert (see the planning probes). Its doc comment was rewritten rather than left describing a mechanism that no longer reproduces |
| 2026-08-02 | T1 | Rewritten routing test PROVEN able to fail | Perturbed the pinned width 68.578→99.578, watched it go red, restored, green. A tautological `toEqual(viaOldPath)` was what ADR-3 retired; a replacement that cannot fail would have repeated the defect |
| 2026-08-02 | batch 1 | Gates: `npm test` **472 files / 11,372 tests** green · typecheck 0 · lint 0 · size-deltas **320/351, widened 0** (identical histogram to baseline) | Verified independently by the orchestrator, not taken from the agent's report. **Brief correction:** the README's stated baseline of 471/11,358 predates an unrelated sequence-diagram fix; the true pre-T1 baseline was 472/11,370, and T1 adds 2 entry-point tests |
| 2026-08-02 | T2 | `measureUsecaseOrActor` takes `sprites?: SpriteRegistry` and converts via `spriteDimsLookupFor`, rather than taking a `SpriteDimsLookup` | That is the type actually in scope at `class-layout-helpers.ts:286`, so the conversion happens once at the boundary instead of pushing it onto the caller. A reasonable developer might have taken the lookup type and converted at the call site |
| 2026-08-02 | T2 | Sprite threading PROVEN observable, not assumed | The forwarding at `:286` was temporarily reverted and the call-site test went red (`expected 6 to be close to 21.23`), then restored and re-confirmed green. Note the agent reported honestly that its unit-level test 3 still passed under that revert — it targets a different link in the chain — which is why the call-site test (test 4, via `measureClassifier`) is the one that actually guards scope item 3 |
| 2026-08-02 | batch 2 | Gates: `npm test` **473 files / 11,376 tests** green · typecheck 0 · lint 0 · **all 312 class goldens byte-identical**, 449 golden+ratchet tests green · size-deltas **320/351, widened 0** | Verified independently by the orchestrator. The class goldens holding is the meaningful signal: none contains a usecase, actor or sprite, so any movement would have meant the change reached further than intended |
| 2026-08-02 | T3 | **Three fixtures authored, each dual-verified to reach the CLASS engine** — `classAccepts(lines) === true` on the exact lines `buildBlockUmls` produces, AND `renderSync` emitting `data-diagram-type="CLASS"` | `class-allowmixing-usecase-mix`, `class-actor-bare-no-allowmixing`, `class-usecase-inline-sprite`. Verifying the engine per fixture was mandatory: a usecase without `allowmixing` routes to the DESCRIPTION engine, so an unverified fixture would silently have tested the wrong thing |
| 2026-08-02 | T3 | **MEASURED DELTAS — none zero-diff, exactly as ADR-4 anticipated.** `class-allowmixing-usecase-mix` **9 diffs** · `class-actor-bare-no-allowmixing` **6 diffs** · `class-usecase-inline-sprite` **3 diffs** | ADR-4 required measuring rather than assuming, and low class conformance (census `0/718 → 29/718`) made zero-diff optimistic. All three are pinned as CHARACTERISATION guards against known gaps, labelled as such, and ratchet-ineligible per ADR-4's registration reason. No fixture's `golden.svg` is anything but verbatim jar output |
| 2026-08-02 | T3 | **FINDING (pre-existing, NOT SI10): this port is more PERMISSIVE than upstream for `actor` in a class diagram.** The jar's `golden.svg` for `class-actor-bare-no-allowmixing` is upstream's ERROR PAGE | `CommandCreateElementFull2.java:197-198` gates `actor`/`usecase`/etc. behind `allowmixing` via `Mode.NORMAL_KEYWORD` and errors otherwise ("Use 'allowmixing'..."). This port's `class-descriptive-leaf-command.ts` has no equivalent gate, so it renders a diagram upstream refuses. **This corrects the brief:** the README's reachability table says `class C` + `actor a1` reaches the class engine — true for THIS port, but I never checked what upstream does with that input. Dates from mission A3, not SI10. Candidate follow-up, deliberately not fixed here (outside every task's write-set) |
| 2026-08-02 | T3 | **FINDING: sizing is sprite-aware, RENDERING is not.** `class-usecase-inline-sprite` shows the ellipse rx/ry close (T2's threading working) while the label renders the literal escaped text `&lt;$Gear&gt; Configure` instead of resolving to an `<image>` plus a separate text row | Confirms scope item 3 landed AND bounds it: T2 threaded sprites into the SIZER only. The class renderer's sprite resolution is untouched and remains a gap. Exactly the kind of half-wired seam si11b's `spriteSplitStdlib` finding warns about |
| 2026-08-02 | T3 | Guard proven able to fail by the ORCHESTRATOR, not just the agent | The agent's own proof mutated `ours` in-memory in a scratch script — evidence the assertion is sensitive to diff values, but not that the committed guard reacts to a real code change. Re-verified by perturbing `measureUsecaseOrActorLeaf` (+7 width) in `leaf-sizing-entity.ts`: all THREE pinned-diff tests went red, restored, green. A guard nobody has watched fail is the failure mode this task exists to prevent |
| 2026-08-02 | batch 3 | Gates: `npm test` **474 files / 11,382 tests** green · typecheck 0 · lint 0 · build 0 · `ratchet.json` and `parity-class.json` UNCHANGED · only 4 new paths added, nothing pre-existing modified | Verified independently by the orchestrator |
| 2026-08-02 | T4 | **Executed by the orchestrator, not the assigned technical-writer agent** | That agent died mid-task with a connection error (verified NO partial write — tree was clean, mission-index untouched). It has no Edit tool, so it must rewrite all 415 dated rows to change one — the exact hazard that appended a stray `\|` to SI11b's row and downgraded an apostrophe in G4's during SI12's close. Re-running it would have re-rolled that dice; surgical `Edit` cannot. Result: `git diff -U0` shows exactly TWO hunks — the SI10 row and the one inserted row |
| 2026-08-02 | T4 | Follow-up registered as **SI13** — extend SI9's authored-fixture registration to the CLASS corpus | `dot-sync-fixtures.ts`'s `GOLDEN_DIR` is hardcoded to `oracle/goldens/svg-description` and `authoredFixtures` expects `<type>/<slug>/`; class goldens are `oracle/goldens/svg-class/<slug>/` — wrong root AND a different shape. Row also records that eligibility would additionally need the 9/6/3 conformance gaps closed, so a future reader does not mistake registration for sufficiency |
| 2026-08-02 | batch 4 | Gates: `npm test` **474 files / 11,382 tests** green · typecheck 0 · lint 0 · build 0 · size-deltas **320/351, widened 0** · `vendor-stdlib --verify` all files verbatim | Mission closed. All four batches `[x]`, executed exactly as planned — no batch added, no task split |
