# Decision journal — edge-label-box-and-class-ports

Append one row per non-trivial judgement call: anything a reasonable developer
might have decided differently. Log it either way — pushed forward or stopped.

| date | decision | why | needs review? |
|---|---|---|---|
| 2026-08-13 | Mission drafted from the DOT-attribute audit's three feature-sized remainders, as ONE brief with batches ordered by dependency. | Batches 1 and 2 are the same seam with a measured dependency (wiring 2 alone regressed `jecici` 143 -> 159 because the measurement feeding it is wrong). Batch 3 is independent and is last so it can be split out cleanly. | no |
| 2026-08-13 | Batch 1 scoped as "reuse `computeReservedLabelBox` + strip markup + port `arrowFontSize`", NOT "wire link labels into the creole stack". | The state engine's existing helper already reproduces the oracle byte-for-byte once fed clean input at the right size (72x22 and 67x12, both exact). The large framing was wrong before the helper was found. | no |
| 2026-08-13 | The kermor `constraint` path was ported during the preceding audit after the plan declared it unverifiable. | Recorded here because it is the precedent for D3's stop condition: "no fixture isolates it" was an assumption, and the gate falsified it. Check before declaring something unverifiable. | no |
| 2026-08-14 | Issue 12 (per-end port-label placement) folded in as batch 4 rather than left unowned or given its own mission. | Same family as batches 1-2 — those fix the SIZE of an edge label's reserved box, batch 4 fixes where the TEXT sits relative to a box that is already correct — and it was the last open item with no owner. Write-sets are disjoint, so it stays independently liftable. | no |
| 2026-08-14 | Batch 4 split into a diagnosis task (T11) and a fix task (T12), with T12 blocked on T11. | The two per-end constants are measured and would close 14 diffs immediately if hard-coded. Separating the tasks makes shipping them without a mechanism structurally awkward rather than merely discouraged. The head end already has a derived rule; only the tail's extra ~15.2 is open. | no |
| 2026-08-14 | **Batch 1 complete.** The shared box now reproduces the oracle byte-exactly on both checked fixtures: `jecici-56-bimu826` 346x13 -> 72x22 (oracle 72x22) and `babafi-51-dixi026` 20x15 -> 22x15 (oracle 22x15). | T1 pure move (state DOT 269/269 unchanged), T2 stripping (3 of 9 tests fail without it), T3 arrowFontSize as a key handler not a bucket key, T4 emits the RESERVED box. | no |
| 2026-08-14 | A unit test was corrected rather than the code: it asserted `labelWidth` equalled the raw size-13 measurement (20.9625), which is our own PRE-margin number. The fixture's oracle declares `WIDTH="22"`. | The oracle decided, not convenience. Checked the fixture's own svek-1.dot before touching the assertion; the font-size discrimination the test existed for is preserved. | no |
| 2026-08-14 | Batch 1 leaves SVG output completely unmoved — census 802 fixtures, ZERO per-fixture changes — and that is the expected result, not a miss. | `labelWidth`/`labelHeight` reach the DOT emitter but NOT the layout engine, which still receives plain text; only `labelBoxWidth`/`labelBoxHeight` reach it. Making the engine read the box is batch 2. The brief predicted this dependency from the regression side (wiring batch 2 first took jecici 143 -> 159); it is now confirmed from the other side too. | no |
| 2026-08-14 | **Batch 2 landed T5/T6; T7 is BLOCKED by a contradiction in this brief.** The exit bar's clause 1 says pin `class-inheritance-interface-assoc` in `ratchet.json`; batch-2's own watch-out says to expect ~13 residual diffs. AC1 asserts `expect(diffs).toEqual([])` — zero. A fixture at 13 cannot be pinned. The bar as written was unmeetable. | Recorded rather than fudged, per the README's "do not redefine the bar to make it look met". The fixture went 202 -> 13, which is the predicted outcome; closing the last 13 (8 polygon/@points, 3 text/@x, 2 text/@y) is separate work the brief explicitly put out of scope. Clause 1 should have read "reaches its predicted ~13 residual, with the residue named". | **yes — bar needs restating** |
| 2026-08-14 | `jecici-56-bimu826` 151 -> 138; exit-bar clause 2 wanted <=133. Baseline LOWERED to 138, clause recorded as UNMET. | Lowering always allowed; the <=133 figure came from a measurement taken with `norank` disabled, so it was never a like-for-like target for the current code. The label boxes are now byte-exact against the oracle (72x22, 67x12), so the residue is NOT the label box — it is something else on that fixture, unidentified. Not chased here. | **yes — 138 vs 133 unexplained** |
| 2026-08-14 | Two fixtures REGRESSED (`xamule-03-jeda376` 1496 -> 1507, `nugecu-04-tona107` 158 -> 160) against 64 improved and a total of 52,123 -> 44,683 diffs. Proceeded, with the cause isolated by controlled experiment. | Withholding the box restores both exactly, so the cause is the box reaching the engine. Mechanism: the engine now ACTS on our declared box instead of measuring text. On `nugecu` our box matches the oracle exactly (9x15), so the +2 is second-order geometry on a fixture already 158 divergent. On `xamule` our boxes still differ substantially from the oracle (38x15 vs 91x32), so a pre-existing MEASUREMENT gap that was hidden while the engine ignored the box is now acted upon. The stop condition is "rises AND the cause is not identified" — it is identified. | **yes — xamule's label measurement is still wrong** |
| 2026-08-14 | **T11 COMPLETE — the tail-end ~15.2 is not a rule, it is collision avoidance, and it is NOT a constant.** Mechanism: `SvekEdge#manageCollision` (`svek/SvekEdge.java:1204-1216`) grows EVERY node by an 8px margin (`PositionableUtils.addMargin(sh, 8, 8)`) and, for any port label that intersects one, replaces its position with `PositionableUtils.moveAwayFrom` (`klimt/geom/PositionableUtils.java:86-117`) — a five-iteration BINARY SEARCH along the node-centre→label-centre vector, stopping at the smallest coefficient that clears the overlap. | Causal chain: both ends are treated identically at assignment (`SvekEdge.java:750-767` differ only in the colour key handed to `getXY`) and identically at draw (`:956-980`). The asymmetry can only enter at `manageCollision`. The tail label sits close enough to its node to intersect the +8 box and is pushed clear; the head label does not intersect, keeps its `getMinXY` position, and therefore still matches the derived `boxTop + ascent` rule (+3.022 against a predicted +3.0). | no |
| 2026-08-14 | **This vindicates D8 concretely.** `+18.244` is the OUTPUT of a geometry-dependent binary search over this fixture's node positions, not a placement constant. Hard-coding it would have turned 14 diffs green while encoding one fixture's search result as if it were a rule. | The issue file called the two constants "evidence, not a formula" without knowing why. Now the reason is mechanical: there is no per-end formula to find, because the per-end difference is not a formula. T12 must port `manageCollision`, not a number. | no |
| 2026-08-14 | **Ruled out** (each by reading the Java, not by inference): a per-end draw rule — the two branches are structurally identical; a per-end assignment rule — identical but for the colour key; a marker-emission difference — same `getXY`/`getMinXY` path for both; `place_portlabel` — gated on `labelangle`/`labeldistance`, which our DOT never sets (issue 12's own earlier finding); the engine — verified against the canonical oracle on four inputs. | The candidate list in issue 12's "next thing to read" had two entries. The first (markers emitted at different anchors) is eliminated; the second (`moveAwayFrom`/cluster avoidance) is confirmed, though via `manageCollision` rather than the cluster pass. | no |
| 2026-08-14 | ONE step is inferred rather than measured and T12 must confirm it first: that on `tobuka-93-jale775` the TAIL label actually intersects its node's +8 box and the HEAD label does not. The arithmetic fits (+3.022 matches the un-moved rule exactly, +18.244 does not) but the intersection itself was not instrumented. | Stated so it is not mistaken for a verified step. Everything above it is read from the Java; this is the one link that is deduced from the numbers. | **yes — verify before implementing** |
| 2026-08-14 | **T11's ONE inferred step is FALSE, and measuring it changed T12.** T11 inferred that on `tobuka-93-jale775` the TAIL port label intersects its node's +8 box and the HEAD does not. Instrumented (upstream's own `intersect(addMargin(node,8,8), labelBox)` over the fixture's real geometry): **all 14 labels intersect, both ends, every edge.** | The mechanism (`manageCollision`) survives — but the asymmetry is NOT "one end is pushed". Both ends are pushed; `moveAwayFrom` slides along the node-centre -> label-centre ray, so a tail label below its node goes further down and a head label above its node goes further up. Opposite-signed y, one rule. D14's "the head keeps its `getMinXY` position" is wrong; its +3.022 agreeing with the un-moved rule is a coincidence of that end's push being small. Recorded because the flagged-for-review row demanded exactly this check before implementing. | no |
| 2026-08-14 | **T13's 14 x diffs are not a separate question — same mechanism.** The push coefficient is the smallest that clears ANY axis. On these fixtures the cheap axis is vertical, so `c = required_dy / deltaY` and the x displacement is whatever `deltaX * c` drags along — large and non-constant exactly when the ray is far from vertical. | This is why the x deltas ranged 0.98-39.36 with no constant: they are a ratio, not an offset. `edge-7`'s dx of -41.2 comes with the same +6.59 dy as every other tail. T13 should be closed as explained-by-T12 rather than scoped as new work. | **yes — T13 no longer needs scoping** |
| 2026-08-14 | **The reserved box was NOT "already correct", contradicting batch 4's own premise.** The batch overview says batch 4 fixes "where the TEXT sits relative to a box that is already correct". Measured false: we hand the LAYOUT engine the raw tail/head label TEXT (`graph-layout-build-edges.ts`), so it reserves a box of its own measurement, while jar hands graphviz a FIXEDSIZE TABLE truncated to whole pixels (`SvekEdge.java:447-468` -> `appendTable`'s `(int)` casts). `getXY`'s `getMinXY` reads that box, so the difference lands directly on placement. | Same seam batch 1 found for the MAIN label ("reaches the DOT emitter but not the layout engine") — `tailLabelWidth`/`tailLabelHeight` were explicitly documented as "emitter-only" in `graph-layout.types.ts:195-198`. Batch 2's own title is "hand the engine a FIXEDSIZE box, not plain text"; it did the centre label and left the two ends. | no |
| 2026-08-14 | Ported `PositionableUtils` in full as `src/core/klimt/geom/PositionableUtils.ts` rather than inlining the two functions the caller needs. | The file's siblings (`Positionable`, `PositionableImpl`, `XRectangle2D`) were all already ported into that exact directory, and `XRectangle2D#intersects` already carried Java's empty-rectangle rule. Inlining would have duplicated primitives that existed. The five-iteration bisection is ported verbatim, NOT solved analytically: its returned midpoint is not the exact minimal clearing coefficient, and reproducing the iteration count is what reproduces jar's coordinates. | no |
| 2026-08-14 | `class-edge-geo.ts` hit the 500-line hook cap; split the four label-anchor functions into `class-edge-label-anchor.ts` rather than adding an ignore entry. | They were already one documented cluster (`multiLineLabelAnchor` and `attachPortLabels` are both specified in terms of `portLabelAnchor`'s formula). `hooks/complexity-ignore` is for faithful ports of code we do not own — a stop condition in this brief. `attachPortLabels` then hit the 5-param cap and took a `PortLabelContext` object. | no |
| 2026-08-14 | **Measured result: `tobuka-93-jale775` 41 -> 17 diffs; 12 of its 14 edges are now byte-exact on BOTH x and y.** Census class+object 44,683 -> 44,249 (-434), **55 fixtures improved, 0 regressed**, 392 at zero. component/usecase/state censuses **byte-identical** — the shared-file change is provably inert there because neither engine sets the tail/head label TEXT, only its width/height. | Exit bar asked for 14 y diffs -> zero; delivered 14 -> 2. The 2 remaining (plus 2 x) are all on ONE edge, `edge-7`, at 0.033-1.12px. | **yes — bar is 2 short** |
| 2026-08-14 | `edge-7`'s residual is NOT diagnosed. Hypothesis: it is the already-named pre-collision placement residual (N25/N62, engine-vs-graphviz sub-pixel box centring) amplified by that edge's near-horizontal push ray, whose `deltaX/deltaY` of ~-5.2 turns a sub-pixel centre error into ~1px of x. Arithmetic is the right order of magnitude but does NOT close: predicted ~0.27, measured 1.12. | Stated as unconfirmed rather than asserted. `edge-7` is the most collision-sensitive edge on the fixture (largest x drag, -41.2), which is consistent with amplification, but consistency is not a mechanism. Do not close this by tuning anything. | **yes — needs instrumenting** |
| 2026-08-14 | **STOP for sign-off: the change exceeds T12's declared write-set.** T12 declared `src/diagrams/class/class-edge-geo.ts`. Landing the exit bar needed `src/core/graph-layout-build-edges.ts` (the FIXEDSIZE reservation) as well, which is in no task's write-set in this brief. | Per `autonomous-execution.md` this is a stop condition, not a push-forward one. Everything is measured and all four gates are green; the work is held uncommitted pending a decision on the wider write-set, because the shared file is the layout-input seam for every engine. | **yes — approve write-set** |
| 2026-08-14 | **`edge-7`'s residual is DIAGNOSED, and the previous row's hypothesis was WRONG.** That row guessed a sub-pixel N25/N62 placement residual amplified by a near-horizontal push ray. Both halves are false: the pre-collision error is **0.855px, not sub-pixel**, and the amplification is **x1.32, not x20**. Mechanism: `edge-7`'s SPLINE is displaced from jar's by 0.85-1.10px in x and 0.02-0.10px in y; graphviz derives the tail/head xlabel anchors FROM that spline, so the labels inherit the displacement verbatim, and `moveAwayFrom` then scales it by `(1 + c)` = 1.31875. | Origin: `object/tobuka-93-jale775` `g[15]/path[1]/@d` — the edge `Produkt "*" -- "*" Attribut` — NOT `class-edge-label-anchor.ts`. Decisive evidence: **all 17 remaining diffs on the fixture are on that one `g[15]`** (13 path + 4 text); the label's inferred pre-collision delta `(-0.855, -0.040)` equals the spline's own first-control-point delta `(-0.85, -0.04)` to 2dp; and feeding the inferred pre-collision position through the real `moveAwayFrom` reproduces jar's post-collision position to **0.0035px in x and 0.0000px in y**. | no |
| 2026-08-14 | **Ruled out** for `edge-7`, each by measurement: node ORDER / multiple sequential pushes — traced every call, all 14 labels are pushed by exactly ONE node, so order cannot matter; the collision arithmetic — the forward check above is exact given the right input; the reserved-box truncation — same 5x13 box as the 12 byte-exact edges; our DOT input — `parity-object.json` records `dotEqual: true`, so the emitted svek DOT is identical to jar's; graphviz-issue 01 (getLayout/render spline mismatch) — RESOLVED in the pinned `.tgz`, and its stated ~0.03-0.06px x-drift is an order of magnitude below the 0.85-1.10 measured here. | The candidate I most expected to find (node order) is the one the trace killed first. Worth stating because the code comment on `manageCollision` still warns that order is load-bearing — it is, in general; it just is not what happens on this fixture. | no |
| 2026-08-14 | **The exit bar was unreachable as written, for a reason outside batch 4.** "14 y diffs go to zero" cannot hold while the geometry the labels are anchored to diverges: a label placed correctly relative to a wrong spline is still a diff. 12 of 14 edges have a matching spline and are byte-exact on both axes; the 2 that remain are the 1 edge whose spline does not match. | Not a redefinition of the bar to make it look met — the bar is recorded as MISSED at 2. The correction is to the bar's premise, which assumed port-label placement was independent of edge routing. It is not: routing is upstream of it. | **yes — bar premise** |
| 2026-08-14 | The `edge-7` SPLINE divergence itself is NOT diagnosed and is NOT filed. It is characterized (0.85-1.10px x, 0.02-0.10px y, one edge, one fixture, `dotEqual: true` so it enters at or after the engine's spline computation) but attributing it needs the canonical check issue 01 used: run the fixture's own `svek-1.dot` through the engine AND through real graphviz and compare. | Deliberately not filed on inference. CLAUDE.md requires a verified finding before a `docs/graphviz-issues/` entry, and "our spline differs from jar's" is not yet "the engine is wrong" — our extraction path is the other candidate. This is the next task, not part of T12. | **yes — needs filing** |
| 2026-08-14 | **Canonical check run — it EXONERATES the engine, so there is nothing to file.** On the fixture's own `svek-1.dot`, `@knowvah/dot-engine` matches real graphviz 15.1.1 **byte-for-byte** on `edge-7`: same spline `d`, same taillabel/headlabel polygons. Confirmed on BOTH engine entry points — `renderSvg(dot)`, and `parse` -> `render` -> `getLayout` (production's own call order), the latter returning taillabel centre `(1102.5920, 40.9171)` against graphviz's polygon centre `(1102.59, 40.92)`. | The premise of the previous row — "our spline differs from jar's, so the engine is a candidate" — is falsified. The engine is correct through both APIs. A `docs/graphviz-issues/` entry would have been filed against the wrong component, which is exactly why the earlier row refused to file on inference. | no |
| 2026-08-14 | **Root cause is OURS: the layout builder and the DOT emitter disagree on node-dimension PRECISION.** `svek-dot-emit.ts#inches` writes `(px/72).toFixed(6)` — jar's own 6dp — so jar's graphviz reads `width=5.555556` and computes **400.000032px**. `graph-layout-build.ts#addNodes` passes `(n.width/72).toString()`, full float, giving the engine **400.0px**. The node the jar lays out is 0.000032px wider than the node we lay out. | Isolated by controlled experiment, not inference: injecting the full-precision width into the ORACLE DOT text reproduces our builder path's output exactly — `(1103.4450, 40.9602)` against our `(1103.4450, 40.9602)`. Ruled out first, each the same way: `remincross`, `searchsize`, `nodesep`/`ranksep` precision, and node HEIGHT precision — the text path is insensitive to all four. | no |
| 2026-08-14 | **The precision fix WORKS on the target and is NOT shippable as-is.** `tobuka-93-jale775` **41 -> 2** (all 14 y, 14 x and 13 path diffs gone; only `svg/@height`/`viewBox[3]` 336-vs-337 remain). But the class+object census regresses **97 fixtures by +2 each** and drops zero-diff fixtures **392 -> 334**, even though the total falls 44,249 -> 44,125. Reverted, not committed. | The brief's census gate is "no fixture's diff count RISES; on_fail: stop". 97 rise. Mechanism of the regression: rounding HEIGHT to 6dp makes nodes 33.999984 tall, so accumulated rank positions come out a hair short and the document rounds 1px under — while jar computes its final canvas from its OWN integer geometry, never from graphviz's, so upstream never inherits that shrinkage. | **yes — needs the 2-part fix** |
| 2026-08-14 | Echoing the DECLARED node dims back through `graph-layout.ts#mapNodes` (instead of the engine's round-tripped `width`/`height`) does NOT fix the canvas — tried and measured, still 336 vs 337. The shrinkage lives in the POSITIONS the engine returns, not in the dimensions. | Recorded so it is not re-tried. The correct fix is two-part: round BOTH dims to 6dp (faithful — it is what graphviz actually sees), AND make canvas sizing independent of the engine's round-tripped positions. Rounding WIDTH ONLY would pass every gate today and is refused: width is the variable that happens to move the metric, and picking it for that reason is fitting, not porting. | **yes — next task** |
| 2026-08-14 | **Two-part fix landed — and it is really THREE parts, because the second one only surfaced under the unit tests.** (1) `graph-layout-build.ts#addNodes` now routes node dims through `svek-dot-emit.ts#inches`, so the engine lays out the numerically identical graph jar's graphviz does. (2) `graph-layout.ts#mapNodes` echoes the DECLARED dims instead of the engine's echo — without this a 49.938px node is DRAWN as 49.937968, which is what `class-hide-entity.test.ts` caught. (3) `absorbLayoutEpsilon` (`core/layout-epsilon.ts`) at the three `(int)(v+1)` truncation sites, so the ≤3.6e-5px round-trip artifact cannot cost a whole pixel of document extent. | Part 2 was tried and DISCARDED earlier in the session as "doesn't fix the canvas" — true, and irrelevant: its job is drawn-size fidelity, not extent. It was only re-derived when a unit test asserted an exact rect width. Recorded because the earlier dismissal was the mistake. | no |
| 2026-08-14 | `mapNodes` cannot blanket-echo declared dims: a `shape=plaintext` HTML-label node is declared with NO width/height and graphviz sizes it from the label (49x18 label -> 65x36 node, `graph-layout.test.ts`'s M4 cases). `ROUND_TRIP_EPSILON` (1e-3) separates "our own number coming back" from "the engine's own decision" without re-deriving `addNodes`' per-shape branches. | Chosen over duplicating the shape predicate: `addNodes` has four declaration paths (plain, record, point, plaintext/HTML) and a fifth would silently break the copy. The epsilon is self-validating — it asks the question that actually matters. | no |
| 2026-08-14 | **Result: `tobuka-93-jale775` 41 -> 0, byte-exact.** class+object **44,683 -> 43,932**, zero-diff **379 -> 396**, **59 improved, 0 regressed**. component/usecase/state **29,958 -> 29,837**, zero-diff 84 -> 85, 5 improved, **2 regressed**. All four gates green (12,847 tests, 95.34/90.24/96.91 coverage). | The exit bar's item 4 ("`tobuka`'s 14 port-label y diffs go to zero, via a rule whose every term traces to a Java line") is now MET outright, and the fixture is fully conformant rather than merely label-correct. | no |
| 2026-08-14 | **The 2 state regressions are caused by part 1, isolated by controlled experiment** (reverting only the `inches` call restores `buniva` 53->36 and `temuxi` 5->3, with parts 2-3 still in). Both are pre-existing `verdict: diverged` fixtures with `dotEqual: true`. `temuxi`'s +2 is the canvas artifact on a 1108px-wide diagram, where ~20 stacked nodes accumulate ~7e-4 — past `EPSILON_DP=3`'s 5e-4 absorption. `buniva`'s +17 is real layout movement (a node x lands 111 vs jar's 112), i.e. this fixture sits on a layout tie that 6dp resolves the other way. | Named per the brief's "rises AND the cause is not identified" stop condition — the cause IS identified for both, so this is a recorded trade, not an unexplained regression. `temuxi` also documents the epsilon approach's ceiling: absorption is size-dependent, and a wide enough diagram out-accumulates it. | **yes — 2 named trades** |
| 2026-08-14 | `state/leloja-87-tebi184` pinned into `oracle/goldens/state/size-backlog.json` at 2.0e-6. State re-layouts iteratively, each pass re-quantizing node dims to 6dp, so the 1e-6-inch granularity COMPOUNDS about one unit per pass — `svek-3.dot` drifts 1e-6, `svek-5.dot` 2e-6. Only this fixture reaches a 5th pass with a size delta. | The backlog is the sanctioned shrink-only mechanism and its existing entries run 0.16-0.5, five orders larger. Flagged anyway: pinning a fixture to make one's own change pass deserves a second opinion, and reverting the entry is a one-line change. | **yes — concession** |
| 2026-08-14 | The state DOT-parity assertion now compares with float slack (`Number.EPSILON * max(...)`), mirroring `compare.ts#exceedsTolerance`'s own note that a naive `>` is wrong at the boundary. The BAND is unchanged. | 4 fixtures reported `maxSizeDeltaIn = 0.000001000000000139778` against a bound of `allowed + 1e-6`: the drift is exactly the 1e-6 quantization unit the band was written to admit, failing only because 1e-6 is not representable. Loosening the band itself would have been the wrong fix and is not what happened. | no |
| 2026-08-14 | **`buniva-95-zije634` DIAGNOSED: it was landing on jar's answer because TWO errors cancelled, and part 1 removed one of them.** Mechanism: our emitted DOT declares the transition label's reserved box as **19x13 where jar's declares 21x15** — 2px short in BOTH dims, pre-existing and untouched by this work. A narrower label box pulls node B LEFT. The old full-precision node width independently pushed B RIGHT by one point. Net zero, so the fixture rendered B at 112 and looked correct. Part 1 fixed the width encoding; the label shortfall is now unmasked and B renders at 111. | Origin: the label box in the emitted DOT, NOT `class-edge-label-anchor.ts` or anything in parts 1-3. Verified on jar's OWN `svek-1.dot`: 6dp width puts B at **112, exactly jar's value**, and full precision puts it at **113** — so part 1 is unambiguously correct and the pre-part-1 agreement was luck. Substituting our 19x13 label into jar's DOT moves B by -2px (112 -> 110), confirming the label term's sign and magnitude. | no |
| 2026-08-14 | **Ruled out for `buniva`**, each by measurement: float dust / rounding fragility — the engine's node SEPARATION changes by a full point (105 vs 106), not an ulp, so this is a real layout decision and not a truncation artifact; parts 2-3 — both were in place throughout, and reverting ONLY the `inches` call restores 112; node dimensions differing from jar's — identical `0.694444` in both DOTs; multi-pass compounding (the `leloja` mechanism) — `buniva` runs exactly ONE layout pass, 2 nodes, 2 edges. | The candidate I expected — a position landing an ulp under an integer and getting floored — is real elsewhere but is NOT what happens here. `layoutGraph` does report B at `103.99999999999999`, which is genuinely one ulp low, but the pixel was already lost a step earlier in the separation. | no |
| 2026-08-14 | INFERRED, not confirmed: that the 2px label shortfall is a missing `2 * marginLabel` term (`marginLabel` = 1). It matches `state-transition-label.ts`'s own documented `reservedHeight = measuredHeight + 2*marginLabel` and the +2/+2 signature exactly, but the emitter path that produces `buniva`'s transition-label box was NOT traced. | Stated as inference so it is not mistaken for a verified step. This is a batch-1-shaped defect (that batch's whole subject was the edge-label reserved box) that batch 1's own verification did not reach — its checked cases were `jecici` 72x22 and `babafi` 22x15, both exact, so the gap is in WHICH path feeds the state transition box, not in `computeReservedLabelBox`'s formula. | **yes — next task** |
| 2026-08-14 | Consequence for the census: `buniva`'s 36 -> 53 is **not a regression in the ordinary sense** — it is one pre-existing defect becoming visible once its compensator was removed. Its 53 diffs are a truer measurement than its 36. Left as a named trade rather than reverted, because reverting part 1 would restore a wrong number via a wrong mechanism, and would give back tobuka's 41 -> 0 and 58 other fixtures. | This is the same shape as the `xamule` entry from batch 2 — "a pre-existing MEASUREMENT gap that was hidden while the engine ignored the box is now acted upon". Second time this mission that fixing a seam has un-masked a measurement error rather than created one. | no |
| 2026-08-14 | **Emitter path traced — and the earlier `2 * marginLabel` inference was RIGHT, though I briefly abandoned it for the wrong reason.** Upstream origin: `SvekEdge#addVisibilityModifier` (`svek/SvekEdge.java:372-373`) ends `final double marginLabel = startUid.equalsId(endUid) ? 6 : 1; return TextBlockUtils.withMargin(block, marginLabel, marginLabel);` — so `labelText.calculateDimension()` is ALREADY measured + 2*marginLabel in both dims. `labelShield` is 0 here (`:354`, `getMiddleDecor() == NONE`), so the emitted `<TABLE>` is exactly that box: 19+2 = **21**, 13+2 = **15**. | I nearly dropped the inference on seeing `labelShield` is 0-or-7 and never 1. That was the wrong place to look — the margin is applied when the label BLOCK is built, not when the table is emitted. Reading `:441`'s `delta(2 * labelShield)` without also reading where `labelText` comes from is exactly the "read the method, not the line" failure this repo's first rule names. | no |
| 2026-08-14 | **This port ALREADY has the exact formula and the flat state path does not call it.** `core/edge-label-box.ts#computeReservedLabelBox:89-103` mirrors upstream precisely, self-loop 6 included: `marginLabel = isSelfLoop ? 6 : 1`, `reservedWidth = Math.floor(measuredWidth + 2*marginLabel)`, `reservedHeight = measuredHeight + 2*marginLabel`. `state-dot-graph.ts#edgeLabelAttrs:162-178` (the FLAT path, `buniva`'s) instead uses raw `measurer.measure(text, font)` and sets **no `labelBoxWidth`/`labelBoxHeight` at all**. | Two consequences, not one: the emitted DOT declares 19x13 against jar's 21x15, AND — because `graph-layout-build-edges.ts` gates the FIXEDSIZE reservation on `labelBoxWidth`/`labelBoxHeight` being present — the layout engine receives a PLAIN TEXT label and measures it itself. That second half is batch 2's own defect, never applied to the flat state path. | no |
| 2026-08-14 | The COMPOSITE state path is half-fixed: `state-composite-edge-label.ts:81-83` does call `computeReservedLabelBox` and sets `labelBoxWidth`/`labelBoxHeight` for layout, but still emits raw `labelWidth`/`labelHeight` into the DOT text. So composite state diagrams lay out correctly and emit a wrong box; flat ones do neither. | Recorded so the fix is scoped to both paths rather than just `buniva`'s. | no |
| 2026-08-14 | **Why this survived**: `edgeLabelAttrs`'s own comment says it — *"Widths/heights are measured but tolerant: the DOT-parity comparator only checks label PRESENCE, not pixel size."* True of the gate, false of the layout: the box is a FIXEDSIZE reservation that decides node separation. The mission README's line that `computeReservedLabelBox`'s only user was the state engine referred to its DRAW path (`state-transition-label.ts`); its DOT path never adopted it. | Fourth builder/emitter-or-gate blind spot this mission, all the same species: a value the DOT gate cannot see, feeding layout. The others were tail/head label boxes, node-dim precision, and batch 2's main label. | no |
| 2026-08-14 | Fix NOT applied: route both state paths' `labelWidth`/`labelHeight` (and the flat path's missing `labelBoxWidth`/`labelBoxHeight`) through `computeReservedLabelBox`. Small change, corpus-wide state blast radius — every transition label box grows 2px in both dims, so node separations move on every labelled state fixture. Needs its own before/after census, not a fold into this one. | The task was to TRACE the path; tracing is done and the origin is a `file:line`. Landing it is a separate measured change, and `buniva` is now a ready-made verification case: it should return to 112 for the RIGHT reason, with its 53 diffs dropping rather than its 36 being restored. | **yes — next task** |
| 2026-08-14 | **Landed: both state paths now reserve the label box.** `state-dot-graph.ts#edgeLabelAttrs` (flat) and `state-composite-edge-label.ts#edgeLabelAttrs` (composite) take `labelWidth`/`labelHeight` from `computeReservedLabelBox` instead of the raw measurement, and the flat path additionally sets `labelBoxWidth`/`labelBoxHeight` so the engine gets a FIXEDSIZE reservation rather than plain text. The margin is applied to the LABEL component before the note merge, mirroring `SvekEdge.java:302` vs `:319-325`. | The `labelBox*` fields stay scoped to note-free labels on BOTH paths, preserving the composite path's existing documented scoping — the merged label+note margin story is still unverified, and this task had no evidence to change that. | no |
| 2026-08-14 | **`buniva-95-zije634` verified exactly as predicted: 53 -> 4, with node B back at x=112.** It returned to jar's position by DROPPING diffs, not by restoring the 36 it had when two errors cancelled. | This was the stated verification criterion before the fix was written, and it is the whole reason the earlier "regression" was left standing rather than reverted. | no |
| 2026-08-14 | Census component/usecase/state **29,837 -> 29,351 (-486), 16 improved, 0 regressed.** Against the pre-T12 baseline: 21 improved, 1 regressed (`temuxi-28-cega322` +2, the already-named canvas-epsilon ceiling). `fakali-52-zuje420` 130 -> 13, `livuni-63-fira764` 120 -> 4, `datodo-22-beci222` 39 -> 9, `rorebi-96-gane144` 36 -> 6. class+object census **byte-identical**, confirming the state-only write-set. All four gates green (12,847 tests). | The 2px box was distorting node separation on every labelled state transition, not just `buniva` — which is why one 2px term is worth 486 diffs. | no |
| 2026-08-14 | Six unit tests updated, not the code: they asserted `labelWidth` equalled the RAW size-13 measurement, which is our own PRE-margin number. Corrected to the reserved box via a local `reserved()` helper. | Same call as batch 1's own precedent entry, and settled the same way — by the oracle, not convenience: `buniva`'s `svek-1.dot` declares `WIDTH="21"` for "xxx" and `floor(19.5 + 2)` is 21. The font-size discrimination the tests exist for is preserved (13 reserves 21, 14 reserves 23), which is the property that made them worth keeping. | no |
| 2026-08-14 | **`temuxi-28-cega322` diagnosed PARTLY, and my earlier hypothesis for it is FALSIFIED.** I recorded its +2 as the canvas artifact out-accumulating `EPSILON_DP=3` on a 1108px-wide diagram. Instrumented: the raw pre-truncation extent is **exactly 1107.0000000000000000**, and with part 1 reverted **exactly 1108.0000000000000000**. Both are exact integers, so `absorbLayoutEpsilon` is a NO-OP on this fixture and there is no float artifact of any size. The +2 is a genuine 1px ink-extent difference, not a rounding boundary. | Correcting it explicitly rather than quietly: the epsilon-ceiling story was plausible, matched the symptom, and was never measured. The general bound argument in `layout-epsilon.ts`'s doc comment still stands on its own arithmetic — but `temuxi` is NOT an instance of it, and should not be cited as one. | no |
| 2026-08-14 | Established for `temuxi`: the 1px is attributable to part 1 by controlled revert (raw extent flips 1108 <-> 1107; jar's is 1108). And our emitted DOT is **not the jar's graph** — cluster title label boxes are 1px WIDER than jar's (`WIDTH="46"` vs `45`, `WIDTH="37"` vs `36`), we emit an extra `sh0012 [shape=point]`, and cluster membership/ordering differs. So, exactly as with `buniva`, the pre-part-1 agreement on width was not evidence of correctness. | `parity-state.json` records `dotEqual: true` for this fixture regardless, because the structural comparator ignores label PIXEL SIZES by design — the same blind spot named three times already this mission. `verdict: diverged`, `maxDelta: 102`, `firstDiff: svg/g[1][childCount]`: this fixture is missing ~102px of content outright, so it was never a like-for-like comparison. | no |
| 2026-08-14 | NOT established: which term accounts for the 1px. Several input divergences are present at once, and the sign does not fall out trivially — our cluster titles being 1px WIDER should push the extent wider, not narrower, so at least two effects are in play. Next instrumentation: substitute jar's cluster-title widths (45/36) into our input and re-measure the raw extent, then resolve the extra `shape=point` node. | Stopping here rather than guessing, per `rules/diagnosis.md`: "report what you have ruled out and what you will instrument next — do not paper over uncertainty with a candidate fix". The cluster-title width gap is itself a real, separate defect worth its own task whatever it turns out to explain here. | **yes — next task** |
| 2026-08-14 | **Cluster-title width RULED OUT for `temuxi` by substitution.** Forcing every cluster's `titleTableWidth` to its floored (jar) value through the layout-input observer leaves the raw extent at **exactly 1107** — unchanged. Reason, found while setting the experiment up: `graph-layout-build.ts:417` ALREADY applies `Math.floor(c.titleTableWidth)`, so layout was always using jar's 45/36. The 46/37 lives only in the DOT TEXT. | The experiment I proposed was a no-op by construction, which I did not know when I proposed it. Ran it anyway rather than reasoning it away — the whole point of the last entry was that I had asserted an unmeasured mechanism once already on this fixture. | no |
| 2026-08-14 | **Separate real defect found en route: `svek-dot-emit-labels.ts#labelTable` ROUNDS the cluster title table where jar's `appendTable` TRUNCATES** (`(int)` cast, `SvekEdge.java:505-506`). Our 45.9375 emits `WIDTH="46"`, jar's emits `45`; 36.575 -> `37` vs `36`. Emitter-only: layout floors independently at `graph-layout-build.ts:417`, so nothing lays out differently. | The function carries a comment (`svek-dot-emit-labels.ts:35-39`) deliberately keeping it un-truncated, on the grounds that truncating "would change laid-out geometry on every type that draws a package/namespace frame". That reasoning is FALSIFIED: the emitter does not feed layout at all — the builder's own `Math.floor` does — so truncating here changes only the DOT text, toward jar. Invisible to the DOT gate, which ignores label pixel sizes. | **yes — fix + its own gate run** |
| 2026-08-14 | `temuxi`'s 1px remains UNATTRIBUTED. Ruled out so far: the epsilon/float story (raw extents are exact integers), and now cluster-title width. Remaining candidates from the DOT diff, in order: the extra `sh0012 [shape=point]` node we emit and jar does not, and the differing cluster membership/ordering. | Still a valid in-progress state, not a stopping point dressed up as one. The fixture carries a ~102px structural gap (`childCount`, `maxDelta: 102`) that plausibly subsumes the 1px, so the next step is to resolve THAT rather than keep bisecting a 1px on a graph that is missing content. | **yes — next task** |
| 2026-08-14 | **Cluster-title emitter now truncates; all four of `temuxi`'s title boxes match jar exactly** (45/36/22/46, previously 46/37/23/47). Origin found by reading one layer further up than the previous entry did: `ClusterHeader.java:91` — `titleAndAttributeWidth = (int) Math.max(dimLabel.getWidth(), attributeWidth) + suppWidthBecauseOfShape`. The `(int)` there is the truncation; `appendTable(sb, int, int, col)` (`:510`) genuinely does no conversion, exactly as the old comment said. | The old comment's ERROR was elsewhere: it read `Cluster#getTitleAndAttributeWidth`'s `Math.ceil` (`Cluster.java:262`) as ceiling the width. That ceil applies to the `MinimumWidth` STYLE FLOOR it is maxed against, never to the measured label. So our 45.9375 truncates to jar's 45 — derived from `ClusterHeader:91`, not fitted to the oracle. | no |
| 2026-08-14 | The old comment's second claim — that truncating "would change laid-out geometry on every type that draws a package/namespace frame" — is FALSIFIED and now recorded as such in the file. Measured: class+object **43,932 -> 43,932** and component/usecase/state **29,351 -> 29,351**, byte-identical, 0 changed fixtures. All four gates green (12,847 tests). | It cannot change geometry: this emitter is parity-inspection only, and the layout builder floors the SAME field independently (`graph-layout-build.ts`'s `Math.floor(c.titleTableWidth)`). A zero-delta census is the expected result here, not a disappointing one — the defect was always DOT-text-only. | no |
| 2026-08-14 | Note for whoever picks up `temuxi`: this did NOT move its 1px, and was never expected to — the term was already ruled out by substitution before the fix was written. `temuxi` stays unattributed, with the extra `sh0012 [shape=point]` node and the ~102px structural gap as the live candidates. | Recorded so the two are not conflated: fixing the emitter was worth doing on its own merits (our DOT text now matches jar's on a value it previously disagreed on), not as a `temuxi` fix. | no |
