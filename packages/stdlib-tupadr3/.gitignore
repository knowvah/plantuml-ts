# Copied vendored .puml assets (scripts/copy-assets.mjs, run as `prepack`).
# Regenerable, byte-verbatim copy of ../../assets/stdlib/tupadr3/ --
# mirrors the root .gitignore's treatment of generated/.
assets/
