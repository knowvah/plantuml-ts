# Copied vendored Twemoji artwork (scripts/copy-assets.mjs, run as `prepack`).
# Regenerable byte-verbatim copy of ../../assets/emoji/ — mirrors the root
# .gitignore's treatment of packages/*/generated/ and stdlib-aws's assets/.
assets/
