# SI32 T3 — retire the divergence, file the pass's residue

## Observation: Step 1's premise verified in code, not from T2's report
- **Context**: before deleting the composite-anchor-clip `DIVERGENCES.md`
  entry, per the task's explicit instruction not to trust T2's own report.
- **Finding**: `grep -rn clipTransitionPoints src/` returns nothing —
  `layout-ink-transition.ts#addTransitionInk` (line 66) folds
  `transition.points` directly with no clip call, and
  `state-renderer-transitions.ts:186` (`applyHeadTrim(transition.points, ...)`)
  reads the same field. Both are populated once, at construction, in
  `state-composite-pass.ts:370` and `layout.ts:178`
  (`clipTransitionSpline(...)` assigned into the `points` field of the
  `TransitionGeo` both consumers later read).
- **Impact**: the entry's claim ("renderer draws unclipped, ink walk clips")
  is false as of this commit; deletion was correct to proceed.
- **Confidence**: High — verified by reading the three files directly, not
  by re-quoting T2's `.agent-notes` entry.

## Observation: all seven Java citations in the task's Step 4 draft resolved
- **Context**: the task file states these citations were written by the
  orchestrator from method bodies and must be checked, not trusted.
- **Finding**: checked against `~/git/plantuml/src/main/java/net/sourceforge/plantuml/`
  by `grep -n` for exact line numbers: `svek/DotStringFactory.java:461-463`
  (comment + `DotSplines.ORTHO` guard + `alignEdgesAtLabelNodes()` call,
  between the `:458-459` clip loop and the `:465-466` `manageCollision`
  loop); `svek/SvekEdge.java:660-663` (`if (projectionCluster != null)` +
  `manageEntryExitPoint` call); `svek/SvekEdge.java:671` (`simulateCompound`
  reassignment); `svek/Cluster.java:410-430` (`manageEntryExitPoint` method,
  `ensureMinWidth` at 428, `rectangleArea` reassignment at 430);
  `svek/ClusterDotString.java:101-105` (`entityPositionsExceptNormal`
  decl/check/loop/`setProjectionCluster` call); `svek/SvekEdge.java:1205-1216`
  (`manageCollision` method body); `class-edge-label-anchor.ts:199`
  (this port's `manageCollision`, class-only). All seven matched exactly.
- **Impact**: no citation needed correction before filing as a permanent
  record.
- **Confidence**: High.

## Observation: the diagram-repo pin and its ASCII label were both stale
- **Context**: Step 2 named the `^1.5.0` pin; checking the surrounding
  paragraph per the task's instruction found a second staleness.
- **Finding**: `docs/architecture/overview.md`'s ASCII diagram labels the
  dependency edge `(file: tarball dep)`, but the very next paragraph says
  "a published package, not a local tarball" — and `package.json:68` confirms
  `"@knowvah/dot-engine": "^1.6.0"` (npm registry dep). `git log -p` shows
  the tarball label predates the switch to npm publish and was never
  updated when the prose was.
- **Impact**: fixed both — pin to `^1.6.0`, diagram label to `(npm dep)`.
- **Confidence**: High.

## Observation: no number from T2's notes was restated without attribution
- **Context**: per the orchestrator's prior-observation warning that two
  measurements in this mission did not survive re-measurement.
- **Finding**: the Step 4 `DIVERGENCES.md` entry restates T2's rect deltas
  (156x118.720/126x104.720, 123x277.000/109x277.000) and the fixture slugs
  as T2-measured (`hasDirectBorderPointChild`, `borderPointBox`), and
  restates the orchestrator's own endpoint-distance numbers
  (761.735->663.541, 121.133->77.401) as orchestrator-measured, per the task
  file's own attribution. Neither was re-derived nor promoted to a new
  confidence tier.
- **Impact**: avoids repeating the SI32 pattern (mefici-97-tudu030) of
  restating an unverified number as fact.
- **Confidence**: High.
