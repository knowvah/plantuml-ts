# cdd-close-b5 — batch 5 close (class-divergence-drive)

Written 2026-09-22. Survey 468/68/187 → 468/92/163; census 470; ratchet 470;
DOT 711/712. A "structural-only" batch: 24 fixtures left `diverged`, none
reached `conformant`.

## Observation: layout-moving batches convert diverged → structural-match, not → conformant
- **Finding**: every B3 mechanism (Kal margins, protected-leaf triangles,
  role anchors) reproduces the jar's element tree exactly, but the numeric
  residue is owned by other mechanisms — two dot-engine deltas (issues 18,
  19), a header re-centre gap, a protected-box canvas gap, and style
  residues. The survey score does not move; the census cannot pin. Judge
  such a batch by structural counts and Σ numeric, not by the conformant
  column.
- **Confidence**: High.

## Observation: "no AC requires it" is not a stop for a half-ported Java method
- **Finding**: T16 left `Neighborhood#drawU`'s second loop out; T17 left
  `getInv()`'s role swap out. Both were the exact named residual and both
  were one follow-up fix commit. Brief the agent that the Java METHOD is
  the unit, not the acceptance list.
- **Confidence**: High.
