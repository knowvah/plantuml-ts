## Observation: "every other fixture byte-identical" is scored by a full SVG dump + `diff -rq`, not by the census
- **Context**: SI25 exit-bar clause 2 (only named fixtures may move; everything else byte-identical in SVG). `shape-match-report`/`svg-conformance-census` compare against the JAR, so a fixture whose diff-vs-jar count is unchanged can still have moved (a change that trades one diff for another is invisible there).
- **Finding**: a 25-line jiti script that calls `tests/oracle/svg-conformance/render-fixture-class.ts#renderFixtureClass(markup, new DeterministicMeasurer(), { includeStore: withStdlib({get:()=>undefined,has:()=>false}, buildStdlibAssetsStore()) })` over every `test-results/dot-cache/class/<slug>/in.puml` and writes `<out>/<slug>.svg` renders all 722 class fixtures in ~1 min; run it once in a detached baseline worktree (SI24 recipe, six ignored dirs symlinked) and once in the working tree, then `diff -rq base new` lists exactly the moved fixtures. Placing the script under `scripts/` (temporarily) is required so its relative imports resolve; delete it before committing.
- **Impact**: the only direct measurement of "no unintended movement"; SI25 T2 used it to prove exactly 4 of 722 fixtures changed and to catch that the `fix(T1)` ink-radius change was byte-identical at the default 13. Adapt the import to `render-fixture-state.ts`/description for the other engines.
- **Confidence**: High.
