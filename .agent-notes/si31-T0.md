# SI31 T0 — Baselines and allow-list

## Observation: baseline pins established at branch point 7933a8b6

- **Context**: T0 of `state-residual-fix-batch` (SI31), establishing the
  measurement origin for Batches 1-6.
- **Finding**:
  - `test-results/state-declared-size-baseline.jsonl` — 98 lines (60
    mismatched data rows across 42 dirty fixtures + 1 summary line).
    `summary`: `{"fixtures":273,"declarations":2660,"exact":2563,
    "mismatched":60,"lastDigitOnly":37,"unmatchedFixtures":0,
    "dirtyFixtures":42}` — matches the expected `273/2660/2563/60/37/0/42`
    exactly. sha256:
    `e456ae76da3305243efeb2fba8fd7af69b51ef2643e1b33c78e12752bb144dc`
  - `test-results/render-manifest-baseline.json` — 2017 fixtures (matches
    expected 2017). sha256:
    `3fb6408fcf31ede5815bb42d43cbe7ba03e3e10c1f23571be964fe0a14866574`
  - Self-consistency proven: a fresh `--mismatched-only` run diffed against
    the baseline via `harness-diff.py` prints `OK: 0 rows went exact, 0
    rows appeared or grew` (exit 0). A fresh `render-manifest.ts` run
    diffed via `manifest-diff.py` against the (still-empty)
    `expected-moves.txt` prints `OK: 0 expected moves, 0 unexpected`
    (exit 0).
  - `plans/state-residual-fix-batch/expected-moves.txt` already existed
    (landed in the planning commit) and already conforms to the T0
    contract: header explains the file's purpose/contract, an empty
    per-batch heading for each of Batches 1-6, no pre-populated slugs.
    Left untouched, not rewritten.
- **Impact**: Batches 1-6 re-pin these two files after their own gate
  passes; any regression against these baselines is a stop condition per
  `harness-diff.py`/`manifest-diff.py`.
- **Confidence**: High — both gate scripts run and printed the exact `OK`
  strings the acceptance criteria require.

## 16 target rows (verbatim, from individual re-measurement 2026-08-19)

All re-measured via
`npx jiti scripts/measure-composite-declared-size.ts <slug...>` (no
`--mismatched-only`, so exact rows for these fixtures are visible too).
Format: fixture / scope / axis / index / ours / jar / deltaPx.

**G20b — `pavuzo-79-zodu430`** (1 row, Batch 1)
- scope 2, width, idx 2: ours 1.954201, jar 1.988368, deltaPx -2.460023999999988

**G17 — `joleju-94-maru748`** (6 rows, Batch 2)
- scope 9, width, idx 2: ours 4.157639, jar 4.199306, deltaPx -3.0000240000000247
- scope 9, height, idx 2: ours 2.763889, jar 2.805556, deltaPx -3.0000240000000247
- scope 11, width, idx 2: ours 4.157639, jar 4.199306, deltaPx -3.0000240000000247
- scope 11, height, idx 2: ours 4.708333, jar 4.75, deltaPx -3.0000240000000247
- scope 12, width, idx 0: ours 5.842448, jar 5.884115, deltaPx -3.0000240000000247
- scope 12, height, idx 0: ours 8.847222, jar 8.972222, deltaPx -9

**G21 — `zacajo-09-tamu628`** (1 row, Batch 3)
- scope 4, width, idx 1: ours 4.112429, jar 4.164271, deltaPx -3.732624000000044
  (non-target noise also present at this fixture: scope 4, height, idx 1,
  deltaPx -0.00007200000001006401, lastDigit=true — not a target row)

**G21? — `jetuse-93-gopi146`** (1 row, Batch 3, verify D6)
- scope 3, width, idx 0: ours 1.986111, jar 2.055556, deltaPx -5.0000400000000145

**G5 — `pacami-67-dafe414`** (1 row, Batch 4)
- scope 3, height, idx 2: ours 2.055556, jar 2.069444, deltaPx -0.9999359999999768
  (non-target noise also present: scope 3, width, idx 2, deltaPx
  0.0024480000000224322, lastDigit=false — not a target row, pre-existing
  small rounding gap unrelated to G5)

**G5 — `tofezi-64-koda860`** (1 row, Batch 4)
- scope 3, height, idx 3: ours 2.055556, jar 2.069444, deltaPx -0.9999359999999768
  (same non-target width noise present at idx 3: deltaPx 0.0024480000000224322)

**G5 — `xojudi-20-keco020`** (1 row, Batch 4)
- scope 3, height, idx 3: ours 2.055556, jar 2.069444, deltaPx -0.9999359999999768
  (same non-target width noise present at idx 3: deltaPx 0.0024480000000224322)

**G5 — `decede-10-buvu414`** (1 row, Batch 4)
- scope 3, height, idx 3: ours 2.055556, jar 2.069444, deltaPx -0.9999359999999768
  (same non-target width noise present at idx 3: deltaPx 0.0024480000000224322)

**G20a — `kejabo-83-vinu490`** (1 row, Batch 6)
- scope 2, width, idx 2: ours 2.14434, jar 2.133924, deltaPx 0.7499520000000146
  (non-target noise also present: scope 2, height, idx 2, deltaPx
  -0.00007200000001006401, lastDigit=true — not a target row)

**G15 — `fovafu-44-mifu394`** (1 target row + 1 recorded non-target, Batch 5)
- TARGET scope 2, width, idx 0: ours 3.025285, jar 2.916667, deltaPx 7.8204959999999915
- NON-TARGET (G14 sub-pixel band — record so T5 can prove it did not grow)
  scope 2, height, idx 0: ours 2.908109, jar 2.908056, deltaPx 0.0038159999999898275

Total target rows: 1 (pavuzo) + 6 (joleju) + 1 (zacajo) + 1 (jetuse) +
1 (pacami) + 1 (tofezi) + 1 (xojudi) + 1 (decede) + 1 (gokife, below) +
1 (fovafu width) + 1 (kejabo) = 16.

**G5 — `gokife-89-boja382`** (1 row, Batch 4, reclassified into G5 by SI29 T18)
- scope 3, height, idx 0: ours 4.027778, jar 4.041667, deltaPx -1.0000080000000509
  (non-target noise also present: scope 2, height, idx 0, deltaPx
  0.00007200000001006401, lastDigit=true — not a target row)

## `fovafu-44-mifu394` size-backlog pin (T4 must tighten or remove, never loosen)

`oracle/goldens/state/size-backlog.json:18`:
```
"fovafu-44-mifu394": 0.10861799999999988,
```
Matches the task text's "currently 0.108618". T5 (G15, Batch 5) touches this
fixture's scope-2 WIDTH row; per the backlog's own `_doc` T9 account this
entry is bound by a dot-engine ranking delta on the `zaent0001` point node,
not a sizing rule — T5's spline clip may or may not move this pin; whichever
way, the ratchet is shrink-only.

## Guard list — SI29 T9's 9 regressed fixtures (for T3's later use)

Re-measured individually 2026-08-19 (no `--mismatched-only`); these are
ALL currently jar-exact except one shared, pre-existing, non-G21 rounding
mismatch on 3 of the 9 (`lasasi-13-nona547`, `lonuti-97-voko521`,
`soxene-95-domu248` — same class of noise as the G5 fixtures' width row
above: scope 3, width, idx 2 or 3, deltaPx 0.0024480000000224322,
lastDigit=false). One fixture (`pexiku-77-japi217`) carries a lastDigit-only
noise row (scope 3, height, idx 0, deltaPx -0.00007200000001006401). The
remaining 5 (`kenuci-20-cane702`, `nelupe-49-xova546`, `sizife-41-buje191`,
`sapelo-46-jafe280`, `nivanu-50-zajo916`) are fully exact — every row
deltaPx 0, match true. Batch summary:
`{"fixtures":9,"declarations":118,"exact":114,"mismatched":3,
"lastDigitOnly":1,"unmatchedFixtures":0,"dirtyFixtures":3}`.

T3's guard obligation: none of these 9 may gain a new mismatched (non-
lastDigit) row after Batch 3 (G21 accumulator wiring) lands — that is the
regression SI29 T9 hit with an unconditional +1 px fold (see
`oracle/goldens/state/size-backlog.json`'s T9 doc paragraph: G5 folding
unconditionally regressed exactly these 9 default-styled, transparent-
background fixtures because `RoundedSouth.drawU` only draws its +1 cap
when `southBackcolor` is resolved non-transparent, and
`plantuml.skin:266-271`'s default sets it transparent).

Per-fixture rows are in `/tmp/guard-rows.jsonl` (not committed; regenerate
via `npx jiti scripts/measure-composite-declared-size.ts <the 9 slugs>` if
needed — T3 should re-run rather than trust this scratch copy).

## Quality gates at branch point (7933a8b6, no source changed)

- `npm run typecheck` — exit 0, clean (`tsc --noEmit && tsc --project
  tsconfig.node.json --noEmit`)
- `npm run lint` — exit 0, clean (`eslint ... src tests demo`)
- `npm test` — exit 0. 613 test files passed (613), 14747 tests passed + 1
  todo (14748). Coverage: statements 95.41%, branches 90.34%, functions
  96.93%, lines 96.5% — all above the 90/90/90 floor.
- `npm run build` — exit 0 (`vite build`). Note: the `[unplugin:dts]`
  declaration-bundling step prints 3 non-fatal TS diagnostics against
  `src/core/include-resolver-node.ts` (`Cannot find name 'node:fs/promises'`,
  `Cannot find name 'node:path'`, `Cannot find namespace 'NodeJS'`) plus a
  warning that its bundled API Extractor TS engine (5.9.3) is older than
  the project's TypeScript (6.0.3). These come from api-extractor/rollup-
  plugin-dts's own internal TS instance, not `tsc` (which is clean per
  `npm run typecheck` above) — a known dts-bundler/project-TS-version skew,
  pre-existing on this branch since T0 touched no source. `npm run build`
  still exits 0 and emits `dist/plantuml-ts.js` and `dist/plantuml-ts.cjs`.
  Flagging per CLAUDE.md's TS6-forward stance in case a later batch wants
  to silence it (e.g. pin api-extractor's TS peer, or scope
  `include-resolver-node.ts` out of the dts bundle's node-types
  resolution) — out of T0's write-set to fix.
