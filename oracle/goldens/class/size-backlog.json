{
  "_doc": "Per-fixture DOT node-size ratchet for the class DOT-parity RATCHET suite. Same convention as oracle/goldens/state|object|description/size-backlog.json: structurally EQUAL fixtures whose maxSizeDeltaIn (inches, max across their svek-N graphs) exceeds the 0.01in conformant bar; the test asserts delta <= pinned + 1e-6 per fixture, so pins only shrink. Absent slug => conformant required. Recaptured 2026-08-06 (SI1 T12): gujigi-63 (package/folder USymbol leaf) closed by the create2/BodyEnhanced1 folder-title un-narrowing (delta 0.152778 -> 0, conformant) and DELETED per the shrink-only convention; 1 entry remains, mechanism-named in plans/a2s-class-record-sizing/ledger.md: rotisi-30 (enhanced-body sprite row, 1.0769px fingerprint documented). No inherent-tolerance exclusions for class. rotisi-30 CLOSED 2026-08-10, backlog now EMPTY -- class is 712/712 (100%) conformant. Mechanism: layoutPlainDividerRows / layoutTitledDividerRows computed a block's content height as lines.length * fontSize, where upstream sums each ROW's own height (MethodsOrFieldsArea#calculateDimensionOnlyMembers, java:161-166) -- a 15px sprite row is 15*(fontSize/13)=16.154 and the 2x2 $point sprite row is 2.154, so the flat 14 was wrong in BOTH directions. class-member-rows.ts got this in R2i; the enhanced-body path kept the flat stepping. The per-row errors (-4.308, -2.154, -2.154, 0, +11.846, -2.154) nearly cancelled, leaving the +1.0769 net that made the residual look like a single 14*(14/13-1) scale bug and sent the earlier raster-1 hypothesis after the wrong thing. Isolated single-row probes ALL matched the jar; only the cumulative 1..7-row probe exposed it. SI30 creole-exposant-port T0 (2026-08-18): pinned exposant-01-class at 2.246875 -- <sup>/<sub> creole (CommandCreoleExposantChange) is not yet ported, so `<sup>2</sup>`/`<size:20>` render as literal text today, inflating the node's declared size against the jar's muted superscript metrics. Structurally EQUAL; size-only. Expected to shrink toward 0 once T1/T3-T5 land the port; tighten-only. T4 (2026-08-18): exposant-01-class CLOSED, delta 2.246875 -> 0 (measured via tests/oracle/svek-dot.ts#compareStructural directly) once class-member-creole.ts's resolveMemberAtoms measured `<sup>`/`<sub>` atoms at the EFFECTIVE (muted) size through Sea (decisions.md#D1-D3) -- DELETED per the shrink-only convention; backlog now EMPTY, class DOT-parity 721/721 (100%) conformant."
}
