{
  "_doc": "Slugs whose svek DOT differs from the oracle's ONLY in edge-endpoint PORTS -- the unported RECTANGLE_HTML_FOR_PORTS row-port emission (upstream svek/SvekNode.java:269-311; port ids are \"p\"+MD5Hex(portName), svek/Ports.java:53-55). Upstream anchors an edge to a specific member ROW; this port emits a 3x3 shield table with a single PORT=\"h\" and anchors every edge to the whole node. Until 2026-08-11 the comparison discarded ports entirely and scored these EQUAL -- 20 object and 22 class fixtures. Making the gate port-aware (tests/oracle/svek-dot.ts#sortedPorts) surfaced them. A listed slug is NOT exempt from the gate: the suite asserts that portOk is the ONLY failing check, so any other regression on these fixtures still fails. ADD nothing here without that being true. DELETE an entry when the mechanism lands -- this list only shrinks. Object: plans/object-close/ledger.md M1+M4. Class: its own tracked mission, same mechanism, different corpus.",
  "_object_residue_2026_08_12": "SI17/B1 fixed the `:h` fall-through in the SHARED emitter `src/core/svek-dot-emit.ts#edgeRef` -- it suffixed `:h` to every plaintext endpoint that named no member row, but upstream gates `:h` on SvekNode#isShielded() (svek/SvekNode.java:383-396), a QUALIFIED-ASSOCIATION test (hasKal1/hasKal2 + a non-zero shield), not a ports test and not a shape test. Object routes through the same emitter, so the same three slugs closed here as a cross-type consequence: guzojo-14-muxa584, kavako-54-zipa815, style-stereotype-on-arrow-7 -- each jar-verified (guzojo's oracle emits bare `sh0007`, not `sh0007:h`). Object DOT 74/80 -> 77/80. The genuinely shielded control fonulu-92-libi014 (the one object oracle that DOES carry PORT=\"h\") stayed EQUAL, so the `:h` path is intact, not disabled. rozuxo-44-fudi093 remains: it is the real missing object row-port producer, NOT this mechanism -- object's own equivalent of the class work SI17 did, and it needs its own mission.",
  "slugs": [
    "rozuxo-44-fudi093"
  ]
}
