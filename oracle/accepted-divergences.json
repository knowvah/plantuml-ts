{
  "comment": "Single source of truth for ACCEPTED / known divergences from real PlantUML jar SVG output, for the svg-conformance golden suite (T6). Mirrors dot-engine's test/corpus/accepted-divergences.json schema (see that project's PARITY.md for the dashboard/rules-gate consumption pattern this is designed to slot into). An accepted entry is a deliberate, maintainer-signed, won't-fix difference; everything non-conformant NOT listed here is an unaccepted gap the emitter is expected to close. match: {id} exact | {idPattern} glob. scope: describes which gate the entry applies to. `retired` holds entries that were accepted and later withdrawn, kept so the reversal is auditable rather than silent; a retired entry is NOT in force.",
  "entries": [],
  "retired": [
    {
      "match": {
        "id": "svg-class/bipudo-23-xavu432"
      },
      "scope": "svg-conformance class golden ratchet (edge spline control points)",
      "acceptedAt": "2026-08-08",
      "acceptedBy": "maintainer",
      "retiredAt": "2026-08-13",
      "retiredBy": "maintainer",
      "reason": "ORIGINAL (2026-08-08): The jar's edge-spline control points are quantized to 2 decimals; ours carry full precision, and clipping amplifies the difference into ~0.0097 at the trimmed end -- just over the harness's 0.01 tolerance. The quantization is Smetana's, not ours: on the jar's own svek-1.dot our engine returns exactly what real graphviz 15.1.1 returns. Smetana is a transpile of graphviz 2.38 and is years out of date; the maintainer's decision is that this port does NOT chase compatibility with it, so the cause is moot and our value is the better one.",
      "retiredReason": "Withdrawn on two independent grounds, both measured. (1) THE PREMISE WAS FALSE. ~0.0097 is INSIDE the 0.01 band, not over it. Two artifacts stacked to make it fail anyway: 3-decimal SVG emission rounds 75.1940275463084 and 75.1843 apart to exactly 0.010, and the comparator's `Math.abs(a-b) > tolerance` then rejected that because IEEE-754 yields 0.010000000000005116 for the subtraction. The second is a harness defect, fixed in compare.ts#exceedsTolerance; the fixture now measures 0 diffs and is re-pinned. It was never outside the declared conformance band. (2) THE STATED CAUSE WAS WRONG. The quantization is not Smetana's. This fixture never touches Smetana -- it has a svek-1.dot, and Smetana paths emit none. svek/DotStringFactory.java:316 calls create(skinParam, dotString, \"svg\"), and GraphvizRuntimeEnvironment#create dispatches to GraphvizLinux, the external dot binary; PlantUML then scrapes that SVG TEXT. `dot -Tsvg` prints coordinates with 2 decimals, and real graphviz 15.1.1 on this fixture's own svek-1.dot emits M24.58,-107.74C24.58,-90.06 24.58,-65.87 24.58,-48.21 -- reproducing every jar value including the 24.58 the original diagnosis could not place. dot-engine's full-precision points rounded to 2dp are bit-identical to that output at all four control points, so the two engines AGREE and the difference is purely serialization precision. It therefore will NOT disappear when the jar updates graphviz. The 'we don't chase a 2.38 transpile' rationale does not apply to this fixture at all."
    }
  ]
}
